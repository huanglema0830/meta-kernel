# Rust 构建产物
/target

# WASM 构建产物复制（由 CI/本地构建生成，不入库）
examples/wasm_canvas/npb.wasm
examples/zen-oscilloscope/npb.wasm
# embed_wasm.py 生成的离线直开版
examples/*/index_embedded.html

# 编辑器与系统
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# WASM 前端构建产物（CI 重建）
manifest-ui/dist/

# UI 自动目验（Playwright，仅 devDependency；产物保持零依赖）
node_modules/
examples/ui-verify/ui_verify.png
examples/ui-verify/package-lock.json
ui_gw.log
ui_dom.html

# 宿主 crate 的构建产物（独立 crate，不并入 workspace）
host/**/target/

# 宿主运行期产物（gene_store 写到 CWD；属"运行状态"非源码，重启可加载/重建，不入库）
# 路径可用 META_KERNEL_GENE_LIB / _TRACES / _HABITS / _QUAD 覆盖；见 host/field-render/src/gene_store.rs
gene_library.txt
gene_library.txt.bak
trace_store.txt
trace_store.txt.bak
habit_pool.txt
habit_pool.txt.bak
quad_state.txt
quad_state.txt.bak

# 宿主验收/本地干跑的运行产物（R9 同族；避免误入库）
page.html
window.log
unittest.log
selftest.log
sortcheck.log
diagcheck.log
diag_env.log

# no_std 构建门禁的本地干跑产物
nostd-build.log
phase1-test.log
gateway-test.log
