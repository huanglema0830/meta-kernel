# 云内核项目 · 顾问简报（advisor_brief）

> **用途**：顾问侧对话断裂后，**读这一份即可恢复上下文**（不需要翻对话历史）。
> **维护**：WorkBuddy 在用户说"这段讨论落库 / 归档"时更新本文件（流程见 `TEMPLATES.md` §八）。
> **保密口径**：本文件只写**框架层与结论**；算法层（公式/阈值/权重）不写（见 `workflows/universal_workflow.md` 分层保密）。
> 最后更新于：HEAD 27785a0（对应 v0.161）——快照口径（C2）。

---

## 一、项目身份（一句话）

**在现有操作系统之上生长的「意识补充层」——不替代、不淘汰**；最终要产出可独立安装、真实运行的操作系统与浏览器。

## 二、当前阶段

**阶段二 · 可引导 ISO**　→　子任务1 ✅ 已收尾 ｜ **子任务2 · 2.1 ✅ ｜ 2.2 ✅** → 下一步 **2.3（最简内存管理 + 承接 alloc 迁移）**

> 子任务2 路线已于 2026-09-16 修正：**放弃 mkosi**（Linux-only，且产出是"带 systemd 的 Linux"，与"元内核自己成为内核"不符）
> → 改**元内核自举**：`no_std` → GRUB/Limine → 最简内存 → **DRM/KMS 直渲** → field-render 作首个用户态进程 → ACPI/CPUID 软识别 → QEMU 验证。
> **2.1 成果**：新建 `meta-kernel-core-nostd/`（10 模块 + `quad` 契约 + **自实现 7 个超越函数**，13 个源文件）；
> **`cargo build --target x86_64-unknown-none` 成功**；该 crate 60 项测试全绿；**原 core 411 项全绿且一字未改**；
> CI 新增 **no_std 构建门禁**（阶段二自举第一道门禁，已上线并核实）。
> **D28/D29 已收口**：`l7` 归 2.3 承接；精度口径取 **ULP ≤4**（实测 ≤1，不做 `f64` 版）。
> **2.2 状态（本轮）**：✅ **已执行完毕**（D31 四项全部确认）。**口径修正已采纳**：由"接入 **GRUB / Limine**"改为
> **`bootloader` 0.11** —— 官方文档明写内核用 **`x86_64-unknown-none` 即可**、**"不再需要自定义 JSON target、不再需要 bootimage/xargo"**
> ⇒ **2.1 成果零返工**；GRUB/Limine/ISO 化（`xorriso`）留到 **2.7**。
> **结果**：`meta-kernel-boot/` 建成；**CI 上 QEMU 真实引导 + 帧缓冲截屏断言通过**（1280×720、**25 采样点全绿**）；
> **路线 A 零 unsafe**；原 core 411/0、nostd 60/0 且**均未改动**。
> ⚠️ 本机**未跑 QEMU**（本机无 QEMU，按 D31-c 装在 CI）——「真实引导」这一条**只由 CI 证成**，如实标注不冒充（C5/C7）。
>
> **机制侧（2026-09-17）**：机制 **17 → 19 项**（+**18 全景阅读指令**／+**19 夜间自动化**），约束 **C12 → C14**（+**C13 顾问阅读指令**／+**C14 夜间自动化**），
> **报告必带项 13 → 14 项**（**第 0 项＝顾问阅读指令，置于最前端**）。
> **夜间自动化已实际落地双通道**：平台定时任务 **21:00 出「今夜 P3 清单」** ＋ **23:00 夜间 P3 自动开工**（均 ACTIVE，**默认不推送**）；
> **GHA nightly workflow 未创建**（属 C10，待 D32 确认）。
>
> **本轮新增机制 17「拆分机制（指针-实现分离）」**：因 **D30 已定"保持仓库现状"**（R20：仓库为 PUBLIC），
> ⇒ 必须有**结构性**的骨架/实现分离。**骨架层**（公开仓库，只写"做什么"）／**实现层**（**仓库外**私有文件夹，
> 写"怎么做"），靠环境变量 **`PRIVATE_ASSETS`** ＋ **`{PRIVATE_ASSETS}/<路径>` 占位符**连接。
> **私有文件夹是仓库根目录的兄弟目录** ⇒ **结构上不可能被提交**（不靠 `.gitignore` 补丁）。
> 已落地两处：资产清单（指针↔全量）、**通用工作流八步**（"[占位]"→ 指针，8 个实现文件已建、内容待填）。
>
> ⚠️ **D30 已收口**：① **保持现状 + 清单永久留本地**（不转 private、不入库）② **语言替换取 C 档（只做增量换壳）**，
> **本轮不做替换**（等盘点完成后另开）。**R20 如实标注"未解除"**——已推送内容在 git 历史与公网缓存里**不可撤回**，
> 本轮只是**停止扩大**。

## 三、最近讨论结论（时间倒序，最多 5 条）

1. **2026-09-17 凌晨（三项机制）**（**全景阅读指令 + Git 历史处置 + 夜间自动化**）
   - **新增 C13 顾问阅读指令**（机制 18）：WorkBuddy 每次返回的**最前端**必须放固定 **8 步**文本
     （全面阅读／核状态卡／核保密／核验收／核边界／核三量／核决策／**输出审核结论**）；
     **报告必带项由十三项升为十四项**（第 0 项置顶）。理由：放末尾＝**读完了才看到要读什么**。
   - **Git 历史处置已定（D30-1：选路径 A 接受现状）**：**不追改历史、不 force push**。
     理由＝已推送的**只是骨架**（机制条文／术语／模板结构），真正要保密的（**算法实现／具体阈值／基因库内容**）**从未入库**；
     `git filter-repo` 的风险（所有克隆失效、fork 保留、重新污染）**远大于收益**。
     **R20 已改为"保持未解除 ＋ C 档增量换壳 ＋ 接受现状"**，并在 BASELINE §八 立「**已推送内容的处理原则**」。
   - **新增 C14 夜间自动化**（机制 19）：**09–21 前台／21–23 过渡／23–08 全自动（只跑 P3）**；
     **21:00 前出「今夜 P3 清单」→ 23:00 自动开工 → 21:00–23:00 可调整**；`ROADMAP.md` 已加「**可夜间跑**」列。
   - **⚠️ 落地时补了两处必要修正（T-015，本轮最重要的一条判断）**：
     ① **只靠 GHA cron 跑不了"agent 类 P3"**（GHA **只能跑 shell、没有 agent**，无法"读 coordination/ → 判断 → 写报告"）
     ⇒ 立 **§11.7 双通道**，并**实际创建 2 个平台定时任务**（**21:00 出清单 / 23:00 执行**，均 ACTIVE）；
     ② **§11.8 夜间安全边界：默认不推送**（推送＝公开可见动作，C10；夜间无人监督故取保守值）。
     顺带**修掉既有自动化的旧缺陷**：21:05 那个的 prompt 指向**已归档**的旧仓库路径。
   - **本轮待你拍板**：**D32**（是否创建 `.github/workflows/nightly.yml`；建议**暂不创建**，yaml 已备好一句话可启用）｜
     **D33**（夜间是否允许 push；建议**保持不允许**）。
2. **2026-09-16 夜（子任务2.2 执行）**（**bootloader 0.11 接入 + QEMU 真实引导通过**）
   - **D31 四项全部确认并执行完毕**：`bootloader_api` 只进 boot 层（内核零依赖未破）／nightly **只在 boot 层生效**／QEMU 在 CI／
     验证走 **路线 A（零 unsafe）**。**口径修正已采纳**：由"接入 GRUB/Limine"改为 **`bootloader` 0.11**（官方文档：
     内核用 `x86_64-unknown-none` 即可，不需自定义 target / build-std）⇒ **2.1 成果零返工**。
   - **结果（已逐行核实真实输出）**：CI 新增 **`boot-image`** job；**QEMU 真实引导 + 帧缓冲截屏通过** ——
     截屏 **1280×720**、**25 个采样点全为绿**、脚本输出 `[PASS] 引导成功且内核自检通过`。镜像 **3,638,272 字节**、
     MBR 签名 **`55aa`**（与本机构建体积一致 ⇒ 可复现）。
   - **★ 判定协议（新方法，值得复用）**：内核把自检结果**编码成整屏颜色**——绿＝引导成功且自检全过、红＝失败、
     其它（未进内核/无帧缓冲）＝一律判失败。⇒ **一条断言同时证明两件事**：引导器真的进入了内核入口 **且**
     2.1 迁出的内核子集在**裸机上算对了**（fmath + L4 判据 + 四戒律风险，含**反向断言**防判据空转）。
   - **三条环境事实（已登记 R21/R22/R23）**：① 本机 rustup 默认 host 是 **msvc 但不可用**（`link.exe` 被 Git Bash 的
     `link` 抢占、且无 VS）⇒ 本机须用 gnu 限定名，且 **`rust-toolchain.toml` 不能钉 host 名**；
     ② `-Zbuild-std` **需要 `rust-src` 组件**（只装 llvm-tools-preview 不够）；
     ③ **CI 上 nightly 未装裸机 target** ⇒ 首跑红（`can't find crate for 'core'`）——
     **本机因 2.1 装过而被掩盖**，属 **R14 同族"本机绿 ≠ CI 绿"**。修法：**工具链＝工具链＋组件×2＋target，全写全且各配一条断言**。
   - **如实自报**：我**第 5 次**犯"追加行变替换行"（改 `.gitignore` 时把 `phase1-test.log` 删了），
     **靠 R-b（编辑后 grep 计数）当场抓到**；另**靠编号连续性发现 R15 是"曾引用但从未登记"的空号**（已留痕说明，不编造内容）。
3. **2026-09-16 深夜**（**拆分机制** + 子任务2.2 清单 + **D30 收口**）
   - **D30 已收口**（用户决策）：① **保持现状 + 资产清单永久留本地**（**不转 private**、不入库）② **语言替换取 C 档
     （只做增量换壳）**，**本轮不做替换**。**R20 如实标注"未解除"**——已推送内容在 git 历史与公网缓存里
     **不可撤回**，本轮只是**停止扩大**。
   - **新增机制 17「拆分机制（指针-实现分离）」**（T-012）：**骨架层**（公开仓库，只写"做什么"）与
     **实现层**（**仓库外**私有文件夹，写"怎么做"）**物理分离**；约定 **`PRIVATE_ASSETS`** 环境变量 ＋
     **`{PRIVATE_ASSETS}/<路径>` 占位符引用**；**私有文件夹＝仓库根目录的兄弟目录** ⇒ **结构上不可能被提交**
     （**不靠 `.gitignore` 补丁**——那是事后补丁，一次 `git add -f` 就漏）。
     已落地两处：资产清单（指针↔全量）＋ **通用工作流八步**（`sub/01..08` 的"[占位]"→ 指针，**8 个实现文件已建**）。
     **机器校验已过**：仓库内 `[占位]` 残留 **0**、9 个引用目标**全部存在**。
   - **子任务2.2 清单已出、待确认（D31）**，含**一条口径纠正**：原"接入 **GRUB/Limine**"建议改为
     **`bootloader` 0.11**——官方文档明写内核用 **`x86_64-unknown-none` 即可，不需自定义 JSON target／build-std**
     ⇒ **2.1 成果零返工**。三条先决条件：需 **nightly**（实测本机只有 stable，但可**只关在 boot crate 目录内**）／
     **C1 边界**（`bootloader_api` 只进 boot 层）／**本机无 QEMU**（建议装 CI）。
     验证路线 A **帧缓冲 + QEMU screendump（零 unsafe，★）**／B 串口（需走 **C9**）。
   - **两条自纠（真实缺陷）**：① 我写的检查清单判据（"引用数 ≤ 文件数"）**本身是错的**（多处引用同一文件合法）
     → 已改为"引用目标必须存在"，并在条文里**留痕写明"初版即犯此错"**；
     ② **`git ls-files` 判"是否在仓库外"在中文路径下会误报**（`Invalid path`，与 **R19** 同族）
     → 判据改用 **`realpath` 前缀比较**。
4. **2026-09-16 夜**（C11 七要素 + 优先级纳入 + **私有资产盘点** + ⚠️ **发现仓库为 PUBLIC**）
   - ⚠️ **最重要的发现（R20 / 待决策 D30）**：**本仓库是 PUBLIC** —— 匿名 `curl` 取仓库内文件返回 **HTTP 200**，
     `coordination/` **45 个文件**（命名体系／层级架构／16 项机制条文／C1–C12 全文／R·D 编号与描述／阈值符号与中性点口径）
     **已在公网**。**本轮止损**：把「私有资产清单 + 分级 + 换壳对照表」**改存仓库外本地目录**，
     `coordination/ASSETS_INVENTORY.md` **只放指针**（不含资产内容）⇒ 资产分级与对照表**未公开**。
   - **C11 扩为七要素**（T-009）：**新增「机会」**。理由：把"机会"混进"优势"会**虚增确定性**——
     用户会以为附加可能性**已经到手**。**优势＝确定的** vs **机会＝可能的**；机会 ≥1 条且**必须可被证伪**。
   - **优先级纳入规则**（T-010）：每项任务必须标优先级（指令模板/报告模板/`ROADMAP.md` 均加字段，
     ⇒ **指令 8→9 必带、报告 12→13 必带**）；可动态调整（须写明"由 X → Y 及原因"）；
     冲突排序 **P0>P1>P2>P3>P4**；**P3 可在用户离线时后台执行**。口径保持 **P0–P4、不用颜色**。
   - **私有资产盘点机制**（T-011，机制第 16 项）：五类独创资产（模式/机制/流程/算法/术语）× 四级别；
     触发词「**资产盘点**」／「**语言替换**」；**替换范围由用户审核后决定**。
     顺带查出 **C12 内部口径重叠**——🔐（算法公式/阈值/权重，"屏蔽后拷"）与 ⛔（基因库"具体公式/阈值/权重"，"绝不拷"）
     **指向同一类内容** ⇒ 已提**消歧建议**：**方法层＝🔐**（"这是什么方法"可谈）／**参数层＝⛔**（"我们调成了多少"不可谈）。
   - 2.1 收口：**D28** `l7` 归 2.3 ✅ ｜ **D29** 精度取 **ULP ≤4**（实测 ≤1，不做 `f64` 版）✅
5. **2026-09-16 深夜**（决策评估 + **保密机制** + **子任务2.1 执行完成**）
   - 新增 **C12 保密机制**（**三方共用**）：四级别 🔓公开／🔒半公开／🔐保密／⛔机密；
     **⛔ 绝不拷给顾问**（密钥/token、账号、本机路径、网络细节、服务器地址、**基因库内容**）；
     报告模板**新增「保密标记」字段**并升为**报告必带十二项之第 1 项**；三方都有提示义务。
   - 新增 **C11 决策评估机制**（**顾问侧**）：凡涉及决策必须给全要素、**用户决策前顾问不得擅自执行**
     （与 WorkBuddy 侧的跨项目"决策呈现规范"是**两套并存**，已注明适用范围以防混淆）。
   - **2.1 已执行完成**：路径 A（无 alloc 子集）＋ 数学函数自实现（守 C1）；
     调研发现 **alloc 用量 ≠ 可迁性** —— 关键看 alloc 出现在**算法**里还是**序列化/接口**里
     （`l5_baseline` 的 5 处全在 `to_json`/解析，核心算法 0 处）⇒ 据此定为"**10 模块 + `quad` 契约**"。
（注：2026-09-16「晚」的结论已按 §三 的**5 条硬上限**挤出本节——R2 纠名与 SSH over 443 见 §四 D19，
「通用工作流」见 §六 术语表，R12–R18 的修复见 `BASELINE.md` §六；**完整历史仍在 `discussions/` 与 git 中，不丢失**。）

## 四、待决策项

| 编号 | 事项 | 状态 / 建议 |
|---|---|---|
| **D32** | 是否创建 `.github/workflows/nightly.yml`（GHA cron · 机器类通道） | ⏳ **待确认**：**建议暂不创建**——现在每天都有 push 触发 CI，nightly 的增量≈0；其真正价值场景是"**项目冷期探测环境漂移**"。yaml 已在 `TEMPLATES.md` **§11.2** 备好，**一句话可启用**（可逆） |
| **D33** | 夜间是否允许 **push** | ⏳ **待确认**：**建议保持"不允许"**（`TEMPLATES.md` **§11.8** 默认值）——推送＝**公开可见动作**（C10），且自动 push 后若 CI 红会**红着过夜**；"本地 commit ＋ 次日一句话推送"成本极低 |
| **D19** | 切 **SSH over 443** 推送 | ✅ **已完成**（2026-09-16，用户添加公钥后）：`origin` → `ssh://git@ssh.github.com:443/...` ＋ `core.sshCommand` 显式 key；`ls-remote` exit=0、**真实推送 13 秒成功**（此前最坏 14 分钟）⇒ **R2 视为已根治**。⚠️ 途中发现 **R19**（本机 ssh 对**中文用户名**编码不匹配 → 用户级 config 不加载、认证成功却返回 1），已规避（不依赖 config、判断只看输出文本） |
| **D23** | 子任务2.1 走哪条路径 | ✅ **已定：路径 A**（2.1 只做无 alloc 子集，alloc 迁移交给 2.3） |
| **D24** | 浮点函数自实现 vs 引 `libm` | ✅ **已定：自实现**（守 C1，精度 <1e-9） |
| **D26** | 2.1 迁移范围：A-1 或 A-2 | ✅ **已定：A-2（核心集）** |
| **D27** | **A-2 精确范围**（实测与预期"~15"有差） | ✅ **已确认并执行完毕**（12 模块 + 6 留 2.3）：**2.1 已完成**（见上） |
| **D31** | 子任务2.2 引导器选型 | ✅ **已确认并执行完毕**（a/b/c/d 四项全 ✅）：口径改为 **`bootloader` 0.11**；nightly **只在 boot 层**；QEMU 在 **CI**；验证走 **路线 A（零 unsafe）**。**结果**：QEMU 真实引导 + 截屏断言通过（1280×720、25 点全绿）。<br>**⇒ D1–D31 全部收口**；**新增待你拍板的 D32 / D33 见本表首两行** |
| **D30** | ⚠️ 本仓库为 PUBLIC（R20）+ 资产清单落点 | ✅ **已确认：① 保持现状 + 清单永久留本地**（不转 private、不入库）。**R20 保持"未解除"**——已推送内容**不可撤回**，本轮只是**停止扩大** |
| **D30-b** | 语言替换档位 | ✅ **已确认：C 档（只做增量换壳）**——新写内容用物理语言、**历史不追改**（git 历史改不掉，追改＝假安全）；**本轮不做替换**（等盘点完成后另开） |
| **D28** | `l7` 的归属（执行中发现） | ✅ **已确认：归 2.3** —— `l7` 的 4 个子模块（共 **1,122 行，全含 alloc**）由 2.3 承接；2.1 范围为 **10 模块 + `quad` 契约** |
| **D29** | 数学函数"精度 < 1e-9"的口径 | ✅ **已确认：取 ULP 判据（≤4），不做 `f64` 版** —— `f32` 下绝对 1e-9 物理不可达；实测 **max ULP ≤ 1**（`atan2` = 0），已是 f32 最优水平，**门线未放宽** |
| **D25** | 保密机制（四级别） | ✅ **已建立**（C12 + 报告保密标记字段）；🔬 试用中，已实践 |
| **D20** | 通用工作流的**算法层**何时填充 | ✅ **已定：延后**（框架层先用） |
| **D22** | 决策评估机制（C11 + `TEMPLATES.md` §九） | ✅ **已建立**，并**扩为七要素**（+**机会**，T-009）：**优势＝确定的**／**机会＝可能的**（须可证伪）；🔬 试用中 |
| **T-009/010/011/012** | 已生效的机制变更 | ✅ **C11 七要素**（T-009）｜**优先级纳入**（T-010）｜**私有资产盘点**（T-011，机制 16）｜**拆分机制（指针-实现分离）**（T-012，机制 17）；均 🔬 试用中 |
| **D15** | 机制是否引入"轻量档" | 观察中（OBS-002，≥3 轮）；**三量台账与边界说明永远不省**（C5/C6） |
| **D16** | 4 项新机制试用期 | 第 1 轮已过，**继续试用满 3 轮**再定 |

## 五、方向判断（当前主线）

- **技术主线**：阶段二子任务2 —— **元内核自举**。第一里程碑是"**能在 QEMU 里跑起来并按 DRM/KMS 输出场域画面**"。
- **机制侧**：已收尾；后续按"试用 3 轮 → 回顾"节奏微调，不再做大改。
- **一条正在生效的纪律**：**同一口径不得在多处各自实现**（R17 的根因就是 CI 与宿主各写了一份帧率口径）——两处必须逐字对齐并注明"改一处同步另一处"。
- **一条正在生效的方法**：**能升成门禁的就升**（升完立刻会暴露被掩盖的缺陷）；但**绝不用"放宽断言"来打绿**。
- **⚠️ 一条现实**：**仓库是 PUBLIC** ⇒ **"写进仓库"＝"对外可见"**。在可见性定下来之前，
  任何"换壳／脱敏"都只能算**增量收敛**（旧内容仍在 git 历史与公网缓存里），**不能声称已脱敏**（诚实标注，C5）。
  **⇒ 由此确立一条长期做法（机制 17）**：**骨架放仓库、实现放私有文件夹，物理分离**；
  分离靠**目录结构**（兄弟目录）而非 `.gitignore`，且**指针必须可机器校验**（引用目标存在性）。
- **一条工程纪律**：**判据本身也要被检验** —— 本轮我的检查清单判据写错过一次（"引用数 ≤ 文件数"），
  这类"判据错"比"实现错"更危险（会让人以后不信判据）⇒ **判据改动要留痕**（写明"初版即犯此错"）。
- **一条本项目的反复出现的规律（已第三次命中）**：**"本机绿 ≠ CI 绿"**——
  R14（Dx12 shader）／powi 次正规区跨平台／本轮 **R23（CI 上 nightly 缺裸机 target）**，
  三次的**共同根因都是"本机有历史状态、CI 是干净环境"**。
  ⇒ **对策（可复用）**：**新增一条工具链时，"工具链" ≠ "工具链 + 组件 + target"**，三者都要写进 CI 步骤**并各配一条断言**。
- **一条新增的验收方法（值得推广）**：**把判定结果编码成"可机读的物理量"**（本轮＝整屏颜色），
  这样一条断言就能**同时**证明"被正确加载"与"算得对"，且**无法用"跑过了"糊过去**（对应 OBS-001 的口径）。

## 六、关键术语（顾问快速对照）

| 术语 | 含义 |
|---|---|
| **元内核系统** | 电信号族：基因内核／云操作系统／空天浏览器／云海大模型（本仓库） |
| **量子云系统** | 量子族（设计）：量子钟／正源操作系统／正源浏览器／采微大模型 |
| **彩虹输入法** | 统一输入法 |
| **层号** | L4 拒绝／L5 诊断／L6 交互／L7 执行／L8–L10 推演（🔒冻结） |
| **四场** | 地／水／火／风（场域拆解的四维） |
| **四态** | 存量＝基态／变量＝激励态／补充增量＝耦合态／创新增量＝新稳态 |
| **证据调制** | `world_adjust = gain×(2m−1)`、`pe_adjust = gain×(2s−1)`，**中性点 m=s=0.5**（断言必须与中性点对齐） |
| **field-render** | 宿主渲染器（wgpu 直渲 + egui，**无 WebView2**），11 个验收模式 |
| **boot 层（2.2）** | `meta-kernel-boot/`（**独立 crate，不入 workspace**）：打包器 + `kernel/`（no_std bin）+ 截屏断言脚本。**判定协议**：内核把自检结果编码成**整屏颜色**（绿＝引导成功且自检全过／红＝失败／其它＝一律判失败） |
| **C1–C14** | 硬约束：内核零依赖／版本号自动／不用 unsafe 取巧／改前列清单／诚实标注／三量台账／不用 CI 冒充真实运行／约束可扩充／unsafe 边界／**动作约束**／**决策评估（七要素）**／**保密（四级别）**／**顾问阅读指令**／**夜间自动化** |
| **R1–R23** | 已知漏洞/风险编号（清单见 `BASELINE.md` §六；**R20＝仓库为 PUBLIC**、**R21＝本机 msvc 不可用**、**R22＝`-Zbuild-std` 需 rust-src**、**R23＝CI 缺裸机 target**；**R15 为"曾引用但从未登记"的空号，已留痕说明**） |
| **D1–D33** | 待决策/已决策编号（见 `BASELINE.md` §九；**D1–D31 全部收口；D32 GHA nightly、D33 夜间是否 push 待你拍板**） |
| **私有资产 / 骨架-实现** | 独创术语＝真正的私有资产；**物理语言**＝中性工程措辞（换壳用）。**骨架层**（公开仓库，只写"做什么"）／**实现层**（**仓库外**私有文件夹，写"怎么做"），经 **`PRIVATE_ASSETS`** ＋ **`{PRIVATE_ASSETS}/<路径>`** 连接（机制 17，`TEMPLATES.md` §十） |
| **优先级 P0–P4** | **不用颜色**；P0 阻塞｜P1 需决策｜P2 需验证｜P3 **可自主（可离线后台跑）**｜P4 有空再做；**每项任务必须标** |

## 七、恢复话术（把这段发给 WorkBuddy 即可）

```
继续云内核项目。读 coordination/advisor_brief.md 恢复顾问侧上下文，
再读 BASELINE.md 看当前状态。
告诉我：当前到哪一步、下一步是什么、需要我决策什么。
```

> 若需要**给顾问的接续包**（换一个新对话），对 WorkBuddy 说：
> **"顾问对话断了，生成接续包"** —— 生成格式见 `CONTINUITY.md`「顾问侧接续」章节。
