# 云内核项目 · 基准

最后更新于：**本次提交**（对应版本一律以 `git rev-list --count HEAD` 为准 —— 按 **R71**，**不再抄具体 hash**）｜本行此后只记**当前** HEAD，历史行见各轮报告｜本轮（**R72 处置 · 4 处过期事实点修订 ＋ 机制 25 文档一致性（新建判据）＋ 汇编正文第 1 批复核／第 2 批启动 ＋ Q11 可测契约稿 ＋ advisor_brief／ROADMAP 更新**）更新内容见下方 **§十四**（本次更新随提交计入下一版）
更新时间：2026-09-19

> **口径说明**：上面一行记录的是「**本文件最后一次更新时所处的 HEAD / 版本**」，**不是「当前版本」**。
> 本文件的内容（指标、状态）反映的是那次更新时的事实快照，会随提交而自然滞后。
> 「当前版本」请一律以 `git rev-list --count HEAD` 为准（本项目版本号自动生成、不手写，见 CONSTRAINTS.md C2）。

## 〇、项目根（唯一权威）

| 项 | 值 |
|---|---|
| **唯一权威项目根** | **本文件所在仓库的根** —— 运行时用 `git rev-parse --show-toplevel` 取得。⚠️ **绝对路径属 ⛔**（C12），**不入库**（原写法为同目录级的字面路径，2026-09-17 按 R25 换壳） |
| 版本号来源 | 上述仓库的 `git rev-list --count HEAD` |
| 陈旧分叉归档位置 | **仓库根的同级 `archive/`**（`$(dirname "$(git rev-parse --show-toplevel)")/archive/`）—— 内含 `meta-kernel-v0.48/`（自带 git，仍可自证 v0.48 / `cbf1b1d` / 74 处未提交改动原样封存）与 `meta-kernel_repair-0907/`（更早的修复副本，自带 git）。**均为移出根目录、保留不删**。⚠️ 其**绝对路径同样属 ⛔**，不入库 |
| 其它同名/相似目录 | `研发开发/` 本身**不是** git 仓库；根目录下已无陈旧分叉 |

> **判据**：任何"当前版本/提交/改动"的统计，一律以上表仓库为准；发现别处也有 `meta-kernel-core/` 时，**默认它是陈旧副本**。
> **D1-a 已执行**：`研发开发/meta-kernel/` → `archive/meta-kernel-v0.48/`（移动前后均 240 个文件，无删除）。
> **D13 已执行**：`研发开发/meta-kernel_repair_0907/` → `archive/meta-kernel_repair-0907/`（移动前后均 100 个文件，无删除）。
> ⇒ `研发开发/` 根目录**已无任何陈旧分叉**。

## 一、项目定位

在现有操作系统之上生长的"意识补充层"，不替代、不淘汰。
让每个设备、每个系统、每个界面都获得"生命感"。

## 二、命名体系

| 版本 | 系统名 | 内核层 | 操作系统层 | 浏览器层 | 大模型 |
|---|---|---|---|---|---|
| 电信号 | 元内核系统 | 基因内核 | 云操作系统 | 空天浏览器 | 云海大模型 |
| 量子 | 量子云系统 | 量子钟 | 正源操作系统 | 正源浏览器 | 细分粒度模型 |

输入法：彩虹输入法（统一）

## 三、层级架构

| 层级 | 名称 | 状态 |
|---|---|---|
| L0 | 0锚点/量子真空 | ✅ |
| L1-L3 | 基因内核（含参数库、学习机制、世界模型） | ✅ |
| L4 | 拒绝层（含四元组风险戒律） | ✅ |
| L5 | 诊断层（含语境、四元组、证据、注意力） | ✅ |
| L6 | 交互层（空天浏览器） | 🚧 |
| L7 | 执行层（多内核互联+修复执行） | 🚧 设计完成 |
| L8-L10 | 传承/反哺/自明 | 🔒 冻结 |

## 四、已完成

**内核**：三引擎、气泡沙漏、镜像池、模式准入闸门、痕迹/习气/自我、正源场域、参数库四层、学习机制、场域解析器、场域映射库、世界模型、七维向量、不非时食/不捉持、风险戒律四项、四场拆解、本底场、语境模块、三态判定、诊断结论、多语言翻译、四元组、证据模块、注意力模块、多内核互联、动作分级、T1闭环、动作账。

**宿主**：wry降级、field-render离屏、window.rs窗口、egui UI、双pass复用、动态LOD、一致性映射、gene_store持久化。

**持久化**：参数库、痕迹、习气、四元组。

## 五、未完成

| 项 | 状态 |
|---|---|
| 宿主真实运行验证 | ✅ **已达成**（2026-09-16 本机真实开窗 + 输入 URL + 取源码 + 上屏回读 PASS；见 `reports/2026-09-16_R1宿主真实运行调研.md`） |
| **CI 覆盖宿主（R6）** | ✅ **已落地并已全绿**：`host-windows` job（windows-latest）**全步骤 success**，含 ①②③ 客观断言 **与 ④ 真实开窗**（run 35044362728） |
| **CI 全绿（本轮目标）** | ✅ **达成并已逐行核实**（2026-09-16，run `35069720108`，headSha `d4c4902`）：`test` ✅ ／ `host-windows` ✅，**5 项验收全部真实生效**（见 §七 指标） |
| **机制总纲缺失** | ✅ **已建，并按评审简化为"能跑的最小集"**：`CHARTER.md`＝**自包含版**（前八节只读它就有全图；新增 **§五 启动回执**、**§六 动作约束 C10**、§七 紧急通道、§八 冲突解决）；活相机制三文件**合并为一个**（`TEMPLATES.md` §七「变更记录」，原 `TEMPLATES_CHANGELOG.md` / `OBSERVATIONS.md` 已删除）；`README.md` 改为极简入口 |
| **C9 门禁无 CI 断言（R10 / D11）** | ✅ **已补**：CI `host-windows` **验收⑤**——`transmute` 零出现 + 真实 unsafe 必须全在白名单 + `// SAFETY:` 覆盖数 ≥ unsafe 处数 + **门禁自检（正反两侧样例）**；本地原样干跑 **exit=0** |
| **CI 验收④为诊断性（R11 / D10）** | ✅ **已升为门禁**：去掉 `continue-on-error`，改为硬断言（退出码 0 + 回读来源＝surface + 结论 PASS） |
| **C10 动作约束缺失** | ✅ **已增补**：`CONSTRAINTS.md` C10 + `CHARTER.md` §六；须用户确认的动作清单与判据见正文（本行为机制登记，不含任何实际操作） |
| **CI 宿主 job 链接失败（R1-b）** | ✅ **已修**：libgcc **语义映射**（`libgcc.a←compiler-rt builtins`、`libgcc_eh.a←libunwind`）；本机在官方 20260908 干净包上实测 exit=0 |
| **`--diag-check` 断言（R7）** | ✅ **已修**（断言对齐中性点 m=0.5；本机 PASS） |
| **meta-kernel/ 陈旧分叉（R3/D1-a）** | ✅ **已归档**（→ `研发开发/archive/meta-kernel-v0.48/`） |
| 老笔记本实测 | ⏳ 待用户 |
| L5诊断端到端（宿主运行期字段级） | ⏳ 部分 |
| 推送SNI摩擦根因 | ❌ 未定位 |
| `--quad-check` 喜欢维 | ⚠️ PARTIAL（变化 0.5%，已知待加强；**须在内核映射层改，禁止宿主调参打绿**） |
| 阶段二（可引导 ISO） | 🚧 子任务1 ✅ **已收尾**；**子任务2 路线已修正**（mkosi → **元内核自举**，见 **D17** 与 `ROADMAP.md` §三）——待启动 |
| **通用工作流机制** | ✅ **已建**（`workflows/universal_workflow.md` + `sub/01–08`）：八步场域演化，框架层公开、算法层占位保密；已登记进 `CHARTER.md` 机制清单（第 11 项） |
| **CI 覆盖 field-render 单元测试（R18 遗留 / D-a）** | ✅ **已补**：`host-windows` 新增「单元测试 field-render」步骤（显式断言 ≥30 passed，防空转）；本地原样干跑 **35 passed / exit=0**；CI 上已逐行核实 |
| **子任务2.1（no_std 子集）** | ✅ **已完成**（2026-09-16，D27 执行）：新建 `meta-kernel-core-nostd/`（**13 个源文件**：10 模块 + `quad` 契约 + `fmath` 7 函数）；`cargo build --target x86_64-unknown-none` **成功**（产物 `.rlib` 已生成）；该 crate **60 项测试全绿**；**原 `meta-kernel-core` 411 项全绿且未被改动**；CI 新增 **no_std 构建门禁**（第一道自举门禁）。**报告**：`reports/2026-09-16_子任务2.1执行_no_std子集建立.md`。待定：D28（`l7` 归 2.3）、D29（精度口径） |
| **顾问侧接续机制缺失** | ✅ **已建**（D21）：`advisor_brief.md`（滚动摘要，触发词「这段讨论落库／归档」）＋ `CONTINUITY.md` **§8 顾问侧接续**（接续包生成＝四读一写、9 节格式、用户用法、与 advisor_brief 的分工）；`TEMPLATES.md` **§八** 固化更新流程；**同时修正 CONTINUITY 的历史错误**（仓库根曾指向已归档的 `研发开发/meta-kernel`） |
| **私有资产盘点缺失** | ✅ **已建立**（D30）：`coordination/ASSETS_INVENTORY.md`＝**指针**（公开安全）；**全量清单（39 项 × 五类 × 四级别 + 原术语↔物理语言对照表）存于私有文件夹**（`{PRIVATE_ASSETS}/ASSETS_INVENTORY.md`）——因为**本仓库为 PUBLIC**（见 **R20**），把清单写进仓库等于公开资产与换壳对照表。触发词「资产盘点」／「语言替换」；已登记 `CHARTER.md` 机制 **16** |
| **骨架-实现分离机制缺失** | ✅ **已建立**（机制 17，2026-09-16）：`TEMPLATES.md` **§十 拆分机制**——**骨架层**（公开仓库，只写"做什么"）／**实现层**（私有文件夹，写"怎么做"）**物理分离**；约定 **`PRIVATE_ASSETS`** ＋ **`{PRIVATE_ASSETS}/<路径>` 占位符引用**；**私有文件夹＝仓库根目录的兄弟目录**（结构性隔离，已用 `realpath` 前缀比较实测确认为"在仓库外"）。<br>**已落地实例**：① `ASSETS_INVENTORY.md`（骨架↔实现）② **通用工作流八步**的"实现层"节由"[占位]"改为指针，8 个实现文件已建于 `{PRIVATE_ASSETS}/workflows/sub/0N_*_impl.md`（内容待填，**不猜测**）。<br>**机器校验已过**：仓库内 `[占位]` 残留 **0**；9 个引用目标**全部存在**（`data/` 显式标注"待建"）。<br>**顺手修正**：检查清单初版第 4 条判据（"引用数 ≤ 文件数"）**写错了**——多处引用同一实现文件是合法的，已改为"**每个引用目标必须存在，未建的须显式标注（待建）**"；并发现 **`git ls-files` 判"是否在仓库外"在中文路径下会误报**（`Invalid path`，与 **R19** 同族）⇒ 判据改为 **`realpath` 前缀比较** |
| **子任务2.2（引导器接入）** | ✅ **已完成**（2026-09-16，D31 执行）：`meta-kernel-boot/` 建成（独立 crate，**9 个文件**：打包器 3 + 内核 4 + 断言脚本 1 + 配置 1）；**bootloader 0.11.17** 接入；产出可引导 `boot-bios.img`（**3,638,272 字节**、MBR 签名 **`55aa`**）；CI 新增 **`boot-image`** job（nightly 隔离 + QEMU **真实引导**）；**真实引导与截屏断言通过**（run `35110443157`：1280×720、**25 个采样点全为绿** ⇒ 引导成功 **且** 内核裸机自检全过）；**路线 A 零 unsafe**（`kernel/src` 按 C9 口径真实 unsafe = **0**）；原 core **411/0**、nostd **60/0** 且**均未改动**。报告：`reports/2026-09-16_子任务2.2执行_bootloader接入.md` |
| **L6感知、L7执行器** | ⏳ P2 |
| **子任务2.3（最简内存管理）** | ✅ **内存管理部分完成**（2026-09-17，D34/D34-b 执行）：新建 **`meta-kernel-mem/`**（纯逻辑层：位图 + 4 KiB 帧分配 + 64 B 小块分配，**`#![forbid(unsafe_code)]` ⇒ 零 unsafe 是结构性保证**，**25 项 host 测试全绿**）；boot 层新增 `mem/`（mod + **唯一 unsafe 边界 `global.rs`**）；内核判定升级为**三色**（**绿=全过／黄=拿不到堆区就停／红=自检或分配器失败**）；`BOOTLOADER_CONFIG` 开 **`physical_memory` 映射**（内存门禁的前提）。<br>**C9 首次放行**：白名单登记 **1 条**（`mem/global.rs`：unsafe **21** 处、`// SAFETY:` **22** 条、`transmute` **0**）；⚠️ 同轮修复**会让门禁空转**的问题——CI 的 C9 扫描范围原先**不含**阶段二新增目录，已补入。<br>⏳ **未做（2.3b）**：**alloc 迁移**（10 文件 **3,098 行**：`l5_*`／`fourier`/`interference`/`energy`／`l7` 四子模块）；先做分配器并证明可用，是迁移的前置。<br>**已知边界（写进代码注释）**：分配器**非重入**（前提＝阶段二尚未开中断/多核，届时须加锁并**另申 C9**）；`size > 4 KiB` 的请求**返回 null**（不凑合） |
| **机制 18/19（全景阅读指令 / 夜间自动化）** | ✅ **已建立**（2026-09-17，T-013/T-014/T-015）：① **C13 顾问阅读指令** — `TEMPLATES.md` §二 **第〇项**（固定文本 9 步，含「对齐用户意图」）＋ 固定相 B **十三项→十四项**（第 0 项置顶）＋ `CHARTER.md` 机制 **18** ② **C14 夜间自动化** — `TEMPLATES.md` **§十一**（时段规则／触发方式／P3 清单／约束／折扣利用／开工前置）＋ `ROADMAP.md` 加「**可夜间跑**」列（子步表 + 里程碑表 + 子任务4/5/6）＋ `CHARTER.md` 机制 **19**。<br>⚠️ **落地修正（T-015，WorkBuddy 补）**：**只靠 GHA cron 跑不了"agent 类 P3"**（GHA 只能跑 shell、**没有 agent**）⇒ 已加 **§11.7 双通道** 与 **§11.8 夜间安全边界（默认不推送）**；并**已实际创建两个平台定时任务**：**21:00 今夜 P3 清单** ＋ **23:00 夜间 P3 自动开工**（均 ACTIVE）。<br>⚠️ **未创建** `.github/workflows/nightly.yml`（**D32 已定：暂不创建**；再评估＝连续 7 天无 push 或再现环境漂移缺陷；届时优先"轻量版：只跑 test+boot-image"） |
| 覆盖率、知识库 | ⏳ P2 |

## 六、已知漏洞/风险

| 编号 | 漏洞 | 状态 |
|---|---|---|
| R1 | 宿主从未真实端到端运行 | ✅ **已解除**（2026-09-16 本机真实运行 PASS） |
| R2 | 推送**间歇性连接超时**——旧称"SNI 摩擦"，**该命名不准确**：实测为 **TCP/连接层偶发超时**，且与 **TLS 重协商**相关（非 SNI 拦截） | ✅ **已根治（2026-09-16）**：改用 **SSH over 443**（`ssh://git@ssh.github.com:443/...` + `core.sshCommand`），**完全绕开 HTTPS + Schannel + 重协商路径**；切换后 `ls-remote` exit=0、真实推送成功。排查报告见 `reports/2026-09-16_R2推送连接超时根因报告.md`。**保留重试脚本作为兜底**（网络层仍有其它偶发因素） |
| R3 | meta-kernel/ 陈旧分叉 | ✅ **已解除**（归档至 `研发开发/archive/meta-kernel-v0.48/`，保留不删） |
| R4 | ~~本地缺 dlltool，宿主无法本地链接~~ **实为 PATH 未含 llvm-mingw `bin`** | ✅ **已澄清并绕过**，且**已固化为 CI 步骤**（勿删） |
| R5 | 二进制banner显示CARGO_PKG_VERSION而非commit count | 🟢 待评估 |
| R6 | **CI 从不构建宿主** → 宿主验收无自动回归 | ✅ **已解除**（新增 `windows-latest` job：GNU+llvm-mingw 构建 + 4 项客观断言） |
| R7 | `--diag-check` 子项①断言与 `m=0.5` 中性点不符 | ✅ **已修**（改为按 (2m−1) 符号预测方向 + 新增 m<0.5 探针强制覆盖；本机 PASS） |
| R8 | `--quad-check` 喜欢维变化仅 0.5%（阈值 >10%） | 🟡 已知待加强（**不许在宿主层调参打绿**） |
| R9 | 宿主运行期产物写到 CWD 易误入库 | ✅ **已修**（`.gitignore` 增 `gene_library.txt`/`trace_store.txt`/`habit_pool.txt`/`quad_state.txt` 及 `.bak`） |
| R10 | C9 门禁曾**只有文字约定、无 CI 断言** | ✅ **已修**（D11 执行：验收⑤落地 = `transmute` 零出现 + 白名单子集 + `// SAFETY:` 覆盖数 + **门禁自检**） |
| R11 | CI 的「真实开窗」步骤曾为**诊断性**（`continue-on-error`） | ✅ **已升为门禁**（D10 执行）；升级依据＝run `35044362728` 上 ④ **真实通过**（开窗 1080×760 · Dx12(WARP) · 取源码 · **surface 回读** · PASS · exit=0） |
| **R15** | ⚠️ **编号缺口（如实登记，不编造内容）**：该编号曾在本文件/报告里被**引用**（如"同轮修掉 **R12–R15**"），但**从来没有对应的登记行**。经核对，当时那一项实际指向的缺陷是 **"WARP 被误判为硬件"**，而同一缺陷后来已由 **R17**（宿主帧率门线无环境感知）**正式登记并修复** | ✅ **已澄清**（2026-09-16，本轮发现）：**不新增内容、不重编号**，仅登记"此号曾空引用、其实质即 R17"以避免后人误以为丢了记录。**C 编号规则是同款的"只增不改"**——此处按同一精神处理：**留痕说明**，历史报告中的"R12–R15"表述指向 R12/R13/R14/R17 四项 |
| **R16** | **C9 门禁首跑出现假阳性**：初版判据用 `grep -rn "unsafe"`（无词边界）→ 把 `let unsafe_q = ...`（**变量名**）与注释里提到的 `transmute` 都判成违规（本地干跑即被拦下） | ✅ **已修**：判据锚在**关键字/调用的确切用法**上（只认 `unsafe{` / `unsafe fn` / `impl` / `trait` / `extern` 与 `transmute` 调用）+ 剔除整行注释；并把该案例**固化为门禁自检回归样例**（正反两侧，防止将来放宽正则时又漏回去） |
| **R17** | **宿主自检的帧率门线没有环境感知**：`window.rs::finish()` 硬编码 `present_fps > 30 || raw_fps > 30`。CI runner 无 GPU、落到 Dx12 **WARP**（自报 `IntegratedGpu`）时只有 ~14 FPS ⇒ 宿主自身返回 **FAIL / exit=1**。在"诊断性步骤"时代被 `continue-on-error` 掩盖；**升为门禁后立刻暴露**（run `35067319775`：链路全通——开窗/取源码/surface 回读/方差 100.3 全 PASS，唯独帧率门线把它判死） | ✅ **已修，并已在 CI 上端到端确认**（run `35069720108`）：新增 `adapter_is_software()`（关键词表与 CI 逐字一致，**名字优先**——WARP 自报 IntegratedGpu）+ `finish()` 改**环境感知**。CI 实测输出：`适配器: Microsoft Basic Render Driver / Dx12`｜`验收① 帧率：呈现 18.1｜裸 33.5 → PASS（口径：软件适配器 → 仅要求 >0）`｜`结论：PASS`｜`exit=0`，且软件口径被**如实标注**。**硬件适配器门槛一字未改（≥30）**。附 3 条单元测试 |
| **R18** | **`field-render` 有 3 个单元测试长期必红**（本地 `cargo test` 恒 FAILED：`rotation_keeps_previous_as_bak`、`corrupt_main_falls_back_to_bak`、`corrupt_traces_falls_back_to_bak`）——CI 的 `test` job 只跑 workspace（**不含**这个独立 crate），故从未被人发现 | ✅ **已修**（**测试自身缺陷，非产品缺陷**）：① `rotation_*` 把期望值硬编码为 `1.618`（3 位小数），而实际种子值是 `GOLDEN_HIGH`（黄金比例 1.6180339887…），容差 1e-12 ⇒ 必然失败 → 改用权威常量；②③ 两个 `corrupt_*` 只 save 一次，而轮转语义是"主文件已存在时才复制为 `.bak`" ⇒ 根本不会产生 `.bak` → 各补一次 save。**产品语义未改**；并已把该 crate 的单测**纳入 CI**（D-a） |
| **R19** | **本机 ssh 解析中文用户名时编码不匹配** → 算错 home 路径 ⇒ 三个连带现象：① 用户级 `~/.ssh/config` **不被加载**（`ssh -v` 只打印 `/etc/ssh/ssh_config`）② `ssh -T` **认证成功却返回退出码 1**（因写 `known_hosts` 报 `Could not create directory '/c/Users/<乱码>/.ssh'`）③ 脚本据此**误判"公钥未生效"** | ✅ **已规避（非修复环境编码）**：① 不依赖 config 加载 → 改用 `git config core.sshCommand`（**显式** key 路径 + 443 + `IdentitiesOnly`）② 判断认证**只看输出文本**（`successfully authenticated`），**不看 ssh 退出码**，且**刻意不加** `StrictHostKeyChecking=accept-new`（避免触发写 known_hosts）③ `deploy/github_switch_to_ssh.sh` 已按此重写。**根因（环境编码）本身未动**——彻底解决属系统层面事项，记此处备查 |
| R20 | 本仓库为 PUBLIC，45 个 coordination 文件公网可读 | ⚠️ **保持"未解除"**（已推送内容撤不回）｜**策略：C 档增量换壳，不再新增暴露**｜**决策：D30-1 选①接受现状** |
| **R21** | **本机 rustup 默认 host 是 `msvc`，但 msvc 在本机无法链接**：`link.exe` 被 **Git Bash 的 `/usr/bin/link.exe`（coreutils `link`）** 抢占 → 报 `link: extra operand '...rcgu.o'`（**不是** MSVC 链接器的报错），且本机无 Visual Studio ⇒ **msvc 工具链在本机不可用** | ✅ **已查清并规避**（2026-09-16）：本机一律用 **`nightly-x86_64-pc-windows-gnu`**。⚠️ **连带约束**：`rust-toolchain.toml` **不能钉 host 限定名**（钉了会同时破坏本机与 ubuntu CI）⇒ 2.2 **不创建 `rust-toolchain.toml`**，nightly 的隔离改由"不引入依赖 + 只在 CI 步骤内 `+nightly`"实现（效果等价且无陷阱） |
| **R22** | **`-Zbuild-std` 需要 `rust-src` 组件**（只装 `llvm-tools-preview` **不够**）——首次构建 `bootloader` 时失败：`.../lib/rustlib/src/rust/library/Cargo.lock does not exist, unable to build with the standard library` | ✅ **已修**：安装步骤补齐 `--component rust-src`（本地与 CI **双处**）；**并新增显式断言**（缺组件即 `::error`，不让它退化成难懂报错） |
| **R23** | **CI 上 nightly 未装裸机 target** → `boot-image` 首跑红：`error[E0463]: can't find crate for 'core'`（`the x86_64-unknown-none target may not be installed`）。**本机早在 2.1 装过该 target（stable），掩盖了缺口** | ✅ **已修**（2026-09-16）：步骤内显式 `rustup target add x86_64-unknown-none --toolchain nightly` + **显式断言**（`rustup target list --installed` 必须含它）。**性质＝R14 同族**（"本机绿 ≠ CI 绿"，差异来自**环境既有状态**）；**教训**：新增一条工具链时，"工具链" ≠ "工具链 + 组件 + target"，**三件事都要写全且各配一条断言** |
| **R24** | **CI 的"诊断性产物上传"会把整条流水线判红**：`actions/upload-artifact@v4` 偶发 **`Failed to FinalizeArtifact: (403) Forbidden`**（GitHub 侧中间层返回）⇒ 整个 `test` job 判红，**掩盖真实状态**。实测（run `35148724033`，@`14edb8b`）：**20/20 个真实步骤全部 success**，唯一失败的是 `Upload UI verify artifacts`；**重跑即全绿**（同 sha、`conclusion: success`）⇒ **基础设施抖动，非代码缺陷** | ✅ **已加固**（2026-09-17）：**把"诊断"与"门禁"分离** —— **7/7 个 `upload-artifact` 步骤一律 `continue-on-error: true`**（注释里写明 R24 与理由）；**真正的门禁（workspace 测试／no_std 构建／像素断言／C9 门禁）保持硬失败，断言一条未放宽**。<br>⚠️ 注意与 **D10** 的区别：D10 当年**去掉** `continue-on-error` 是因为它**掩盖了验收④的真实失败** ⇒ 本次只对**纯上传步骤**加固，**任何带判定语义的步骤一律不加** |
| **R25** | **公开仓库（R20）内仍存在 ⛔ 级内容，且与历轮报告的声明不符**：实测 `coordination/BASELINE.md` **2 行**本机绝对路径（含用户名）、`reports/*` **1 行**本机 PATH（含用户名）、`docs/*` **13 处**内网 IP ＋ **6 处**用户名 ＋ **6 行**桌面/家目录路径、`deploy/win64/README.md` **1 处**内网 IP。⚠️ **要点不是"有泄漏"，而是"历轮报告每轮都写『⛔ 已预屏蔽』"——声明与事实不符**（C5/C7 意义上更严重：它让人以为已安全） | ✅ **本轮处置（2026-09-17）**：① 建**机器判据**＝`coordination/tools/check_private_pointers.py --mode=syntax`（**已入 CI**：校验指针格式 ＋ 比对泄漏基线，**只减不增**）② 按 **D30-1「C 档增量换壳」**把 **`coordination/` 内 ⛔ 泄漏清零**（`BASELINE.md` 项目根 → `git rev-parse --show-toplevel` 写法；`reports/2026-09-16_R1…` 的 PATH → "仓库根的同级目录"写法）③ `docs/` 与 `deploy/` 的 **14 条**进基线 `coordination/security/leak_baseline.txt`（**换壳范围待 D35**）<br>⚠️ **未解除部分**：`docs/` 历史报告内的路径/IP **仍在公网**（git 历史撤不回）；基线只是**锁住"不再新增"** |
| **R26** | **判定编号不可观测**：`Verdict::Fail(code)` **只刷红屏、不含编号** ⇒ 判定协议能机读"**失败了**"，但读不出"**为什么失败**"（内存层 1–13 与算法层 101–113 无法区分）⇒ **每次诊断都要重新猜**。本轮实测代价＝**1 轮 CI** 才定位 | ✅ **已修（2026-09-17）**：`verify.rs` 新增 **判定编号诊断条**（`COLOR_DIAG` 白；失败时把编号写成**第 0 行最左 N 个白像素**）。**判定不受影响**：像素断言只采 25 点（`y=120/240/…`）、**不含 `y=0`** ⇒ 红屏仍是红屏、门禁照样判红（与 **R24** 的"诊断／门禁分离"同族）。**已实测有效**：一轮即读出编号 **10**。<br>**通用法**：**判据的判据**——"断言失败"必须**一眼看出哪一号**，否则诊断靠猜 |
| **R27** | **`l5_baseline` 序列化不保真**：`to_json` 写出 `object`/`established`，而 `from_json` **固定回填占位值**（`"obj"`/`"loaded"`）⇒ **写进去的对象标识读不回来**（**编码/解码不对称**）。⚠️ **既有缺陷**（2.1 前后即存在；2.3b 片2 迁移时**逐字保留**，非本轮引入） | ⏳ **未处置（如实登记）**：本轮**不改算法**（迁移的保真度判据要求逐字相同）⇒ ① 已在内核断言处写明"只比数值字段"并注明原因（防后人误比 `object`）② 待**另开**修复（属"行为修复"，须走 C4：先列清单）。**影响面**：对象标识在"内核侧编 → 宿主侧读"链路上依赖宿主另行注入（现由宿主负责注入，故**当前不致命**，但**不对称本身是隐患**） |
| **R28** | **「反例泄露」：在公开层写反例时，把条目的真实全拼/原名当例子写进去** —— 本轮实测抓到 **1 处**：`TERMS.md` 的规则 1 写「禁止拼音转写（`<真实全拼>` ❌）」，**那一行本身就等于泄露了该条目的原名** | 写公开层文档时，**反例必须用与本表无关的占位串**；且**反例行也要过「公开层不含原名/全拼」的判据**（本轮判据当场抓到） |
| **R29** | **「按函数名猜语义」写断言**：凭函数名想当然地断定语义，**不对实现**——本轮实例：`ontology::analyze` 我以为是「**层级振幅**」（0-10 层各一个分量），实测它是「**特征轴（结构/关系）**」⇒ 我写的反向断言「全黑 vs 全白必须不同」**必然失败**（实测二者输出**逐位相同**）。**CI 首跑即红**（`134 = 100 + 34`） | **凡断言，期望值必须先读实现**（同族第 3 次：R7 中性点 / R27 序列化 / 本轮）；**把裸机断言镜像到 host**（`meta-kernel-core-nostd/tests/mirror_bare_assertions.rs`）⇒ 同一批断言**本地就能跑**，不必等一轮 CI 才拿到一个编号 |
| **R30** | **判据覆盖盲区：`username_literal` 判据依赖「本机 home 名」，在 CI 上无法评估** —— 本机（`<用户名>`）能匹配、CI（`runneradmin`）**永远匹配不到** ⇒ CI 日志会显示「命中数 **< 基线**」（看起来像「已修好」），**实际只是不可评估**。另一面：**裸用户名**出现在仓库里时，CI 侧的 `win_abs_path`（要求 `Users/` 后跟具体段）**能抓住带路径的**，但**抓不住「光秃秃一个用户名」**的写法 | ① **不得**因「CI 命中数下降」而下调基线（**C15：只减不增**）；② 检查器已**显式打印**该盲区提示（不再靠人记得）；③ **裸用户名**的检出**必须在本机跑** `--mode=syntax`（CI 只能覆盖带路径形态） |
| **R31** | **判据与机制不同步：`purge` 删明文后，历史文档里的指针目标变成 `.enc` ⇒ `resolve` 判红** —— D39 执行**当场暴露**：`reports/…机制20术语双层映射…md:67` 指向 `{PRIVATE_ASSETS}/terms/INDEX.md`，purge 后该明文不存在（只剩 `INDEX.md.enc`）。**这不是文档错**（按 D30-b 历史不追改），而是**判据没跟上机制的形态变化** | ① 检查器新增「**密文等价**」：目标不存在但**同名 `.enc` 存在** ⇒ **视为已解析**（并打印说明）；② **反面对照**：引用既无明文也无密文的目标 ⇒ **仍判红**（证明未放宽）。**通用法**：**凡「删掉东西」的动作，都要回头问一句——有谁（哪条判据、哪份文档）还在按旧形态找它？** |
| **R32** | **同一正则、grep 与 Python 结果不一致（129 vs 0），至今未复现** —— `grep -rlE '^[0-9a-f]{64}$' .`（排除 `.git`）报 **129**；同样模式用 Python 遍历（含/不含 UTF-8 过滤两版）均得 **0**。**未定论**（可能是 grep 对二进制文件的处理差异，也可能是我的 Python 漏扫某类文件）| **不纠缠谁对，改用不依赖该判据的决定性判据**：`--exclude-dir=target` ⇒ **0**；抽样命中路径**全在 `host/field-render/target/debug/build/…`** ⇒ 129 条均为**构建产物内的零散 64 位十六进制序列**；并被 `.gitignore` 覆盖（**未跟踪且未忽略文件数 = 0** ⇒ `git add -A` 不会提交）。**最关键判据**（不依赖该模式）：全文件搜**密钥本体（⛔ 机密）的十六进制** ⇒ **0**。**通用法**：**判据有分歧时，用「排除法＋抽样看真路径」定论；未拿到清单前不得写原因**（我本轮先写后查，当场撤回） |
| **R33** | **清单漂移：按「当前剩余集」算出的分片，一旦开始执行就会算出下一片** —— 片5 的迁移脚本每次运行都取「剩余集的层 1」；首跑迁完 16 模块后，**重跑**时这 16 个已不在剩余集，于是算出的「层 1」变成片6 的 4 个模块（`l1_mapping`／`positive_source`／`l5_diagnosis`／`l7/repair`），**误迁了其中 2 个**（另 2 个被新加的 std 残留判据拦下） | ① 迁移脚本新增 **`--mods`**（**显式冻结清单，重跑必须用它**）；② 误迁文件已删，并复核「nostd 无未声明的游离文件」= 无；③ **通用法：凡「按当前状态算出待办」的脚本，输出一旦开始执行就必须冻结** |
| **R34** | **判据白名单滞后于迁移（R31 的同族，本轮两次）** —— ① **迁移脚本**漏登 `std::collections::VecDeque` ⇒ 4 文件编译失败（**编译器兜住**）；② **保真度判据**的 `ALLOWED_SUBS` 同样漏登 ⇒ 同样 4 文件被**假红**（判据说「没通过」，其实编得过） | ① 迁移脚本新增「**代码区不得残留 `std::`**」硬判据（**拦在编译器之前**）；② `ALLOWED_SUBS` 补登 `VecDeque` ＋ 预留 `std::f64::consts::{PI,TAU}`（片6 已探到）；③ **通用法：每新增一类替换，必须同时更新「迁移白名单」与「保真度白名单」——两处不同步就会出现「编得过但判不过」或「判得过但编不过」** |
| **R35** | **计数口径不稳（本轮 2 处，都不是功能缺陷，但会误导判断）** —— ① 「指纹字面量 8 处」实为 **8 行 / 10 处**（2 行各含两处），我按「行」报成了「处」；② 剩余量：上轮报 **24 模块 / 6,528 行**，本轮脚本报 **23 模块 / 6,538 行**（差 1 模块 10 行，源于是否含 `lib.rs`、是否含目录型 `mod`） | ① 报数时必须**写明口径**（行／处／文件）；② **以脚本口径为准**并在文档里统一；③ C15（编辑前后双向计数）之所以要求「差值须与预期一致」，正是为了把这种口径漂移暴露出来 |
| **R36** | **编辑治理表时「整行替换」再次吃掉相邻行（C15 当场抓到）** —— 2026-09-17 把阅读指令从 8 步扩为 9 步时，`CHARTER.md` 机制表的 **机制 19（夜间自动化机制）整行被我替换掉**（用 20 行覆盖了 19 行），**缺号 19 一直没被发现**，直到本轮登记机制 21、用 C15 复算编号序列才暴露 | **C15（编号连续性复核）** | ① 已恢复机制 19 整行；② 机制 21 起，**「编号序列校验」（查缺号/重号）纳入编辑后必做**；③ 与 **T-019** 所记「同类失误已复发 5 次」**同族第 6 次** ⇒ 根因仍在**编辑动作**，只能靠机械判据兜住 |
| **R37** | **对话里宣布的编号未落地到权威文件** —— 2026-09-17 我先后在对话中给出 **D41（片6 的 `HashMap`）** 与 **D42–D45（签到任务／浏览器形态／OS 基线／只读授权）**，但 `BASELINE.md`／`advisor_brief.md` 的决策表里**一条都没有**（实测 `D4[1-9]` 命中 = **无**）| 本轮登记机制 21 时顺带核查发现 | ① **补齐 D41–D45 五行**；② **新规则：凡在回复里给出 `Dxx` 编号，必须在同一轮写入 `BASELINE.md`**（否则顾问侧「待决策项是否已处理」的核验会失真） |
| **R38** | **路线图缺四条「承重轴」**（规划缺口，非实现缺陷）—— 实测内核侧 **输入／网络／存储／网页引擎 = 0**，而 `ROADMAP.md` 阶段二 **2.4（DRM/KMS 输出）／2.6（ACPI·CPUID 读硬件）** 一条都不覆盖；2.5（用户空间进程）、2.7（ISO）也不产生网页能力 ⇒ **按现路线走完阶段二，「能看网页」仍不可达** | 本轮「通用工作流八步」对比分析（ROADMAP × 本机实测） | ① 已在本轮报告 §八 给出「补四条轴」的 B2 段；② **回填 `ROADMAP.md` 属路线变更，须用户确认后执行**（勿自动改） |
| **R39** | **阶段一「可安装浏览器」名与实不符** —— 实现走 `host/sky-browser` 的 `wry`，而 **Windows 上 `wry` 即 WebView2 运行时**（实测 668 MB，与 Edge 本体 690 MB 同量级）⇒ **用要替换的东西的内核去做替换**，逻辑上自相矛盾 | 依赖清单实测（tao+wry）＋ BASELINE「wry 降级」记载 | ① 现役路径已纠正（`field-render` 无 WebView2，实测 0 处）；② **但 `ROADMAP.md` 里阶段一的表述仍在** ⇒ 待用户确认后一并订正（与 R38 同一批） |
| **R40** | **里程碑缺「引擎决策」前置项** —— M5「能看网页」未列任何前置决策（引擎自研／嵌入／兼容层），却在里程碑表里写着 5 周 ⇒ **无法估工，必然漂移**（正是 ROADMAP §七 自己警告的「过期地图」）| 本轮对比分析 | ① 已把决策提为 **D46**（引擎路线）并建议 M5 口径改为「**靶子清单达标率**」（**D48**） |
| **R41** | **同一约束在两处不一致，且摘要处过期** —— `BASELINE.md` §八 的 C13 摘要写着「顾问按 **8 步**执行」，而 C13 正文与 `CHARTER.md` 均为 **9 步**（第 9 步＝对齐用户意图）| 本轮编制「总纲＋规划＋行动路线图 全量汇编」时**交叉核对**发现 | ① 已改 §八 摘要为 **9 步**并补第 9 步说明；② **通用法：凡「摘要/速查」类内容，正文一改就必须回头改它**（与 R36「整行替换」、T-019「编辑动作」同族——**都是「改了 A 忘了 B」**） |
| **R42** | **文档头部同一行重复 3 次** —— `BASELINE.md` 开头「更新时间：2026-09-17」**连续出现 3 行**（疑为多次编辑叠加）。危害：**读者会以为有三次不同时间的更新**，而实际只有一次 | 同上（本轮交叉核对发现） | ① 已删至 **1 行**；② **通用法：头部「元信息」行也要过 C15 式的计数复核**——这类重复**不会触发任何断言**，只能靠「读全文」兜住 |
| **R12** | **本地 llvm-mingw sysroot 里存在「伪装库」**：`libgcc.a`/`libgcc_eh.a` 实为 `libunwind.a` 的拷贝（两文件 md5 相同、非官方包内容，时间戳 Sep 8）——本机长期"能构建"建立在此 hack 上，**不可复现**，且命名与内容不符会误导诊断 | ✅ **已查明并修正**（2026-09-16）：本机 sysroot 已改为**语义映射**（`libgcc.a←compiler-rt builtins`、`libgcc_eh.a←libunwind`），原文件留痕为 `*.hackbak`；CI 同步采用该映射 |
| **R13** | CI 断言步骤存在**诊断盲区**：GitHub 的 `shell: bash` 外层自带 `-e`，步骤内 `set -uo pipefail` **去不掉它** → 被测程序非零退出即终止脚本，`CODE=$?`/`cat 日志` 被跳过，只见 exit 码不见原因 | ✅ **已修**（验收①/②/③ 显式 `set +e` + `RUST_BACKTRACE=1`；与验收④对齐） |
| **R14** | **Dx12 后端下宿主根本起不来**（真实缺陷，非 CI 环境问题）：`sort.wgsl` 的 `Splat2D.cov2d: mat2x2<f32>` 经 naga 的 **HLSL 后端**在"整结构体赋值"降级时生成的代码被 FXC 拒绝 —— `error X3018: invalid subscript 'cov2d'`（`Device::create_compute_pipeline` → panic 101）。本机走 Vulkan 不经 FXC，故长期未暴露；**Windows 用户默认后端即 Dx12** | ✅ **已修**：`cov2d` 改为 `array<vec2<f32>, 2>`（WGSL 布局完全相同，64 字节不变）。**Dx12 与 Vulkan 双后端本机实测均 exit=0 且 PASS，方差/pHash 逐位一致** |

> **R20 取证与止损留痕**（2026-09-16 实测；**取证不删**，供日后复核）：
> `gh repo view --json visibility` → `"PUBLIC"`；**匿名 `curl` 取仓库内文件 → HTTP 200**；`coordination/` **45 个文件**公网可读；Pages 亦 `"public": true`。
> ⇒ 已在公网的内容类别：命名体系／层级架构／机制条文／C1–C12 全文／R·D 编号与描述／**阈值符号与中性点口径**。
> **本轮已做止损**（可逆、不破任何纪律）：私有资产清单**改存仓库外**（`{PRIVATE_ASSETS}/`），`coordination/ASSETS_INVENTORY.md` **只放指针**
> ⇒ **资产分级结论与换壳对照表未公开**（这一条是本轮最有价值的止损）。
> **明确未做**：**不追改历史**（成本高、收益低、会破所有克隆）——原则见 §八「已推送内容的处理原则」。

## 七、关键指标

| 指标 | 数值 |
|---|---|
| 宿主是否真实运行 | ✅ **是**（2026-09-16，本机 Intel Arc / Vulkan，窗口 2160×1520，URL 取源码成功，回读自 surface） |
| 离屏帧率（本机 debug） | **191.8–209.3 FPS**（1024 splat）｜本轮本机实测方差 84.0 / 600.2 / 876.8（均 > 1） |
| 窗口帧率 | 呈现 **59.5 FPS**（垂直同步）｜管线裸口径 **355.0 FPS**（debug，256 splat） |
| WebView2依赖 | 0处 |
| unsafe | **真实 unsafe 关键字 0 处**（`unsafe_whitelist.txt` 为空 ⇒ 未放行任何条目；机制见 C9，**CI 门禁见验收⑤**）。ℹ️ `meta-kernel-core/src/l4_risk.rs` 里的 `unsafe_q` 是**变量名**（"不安全状态的四元组"），不是 unsafe 语法——C9 门禁首跑曾误报它，见 R16 |
| 内核测试 | 411项（lib 390 + 集成 21） |
| **no_std 子集（2.1 / 2.3b 片1–片5）** | ✅ `meta-kernel-core-nostd` **331 项测试全绿**（2.1 时 60；+11＝片1；+27＝片2；+48＝片3；+93＝片4；**+93＝片5（238→331）**）＋ **host 镜像测试 12 项**（裸机断言在 host 原样跑，`--nocapture` 可读诊断值）｜`cargo build --target x86_64-unknown-none` **通过**（**权威判据**）｜**迁移保真度**：10 个已迁文件**全部通过** `coordination/tools/check_migration_fidelity.py`（**多重集差**判据：源行不丢 + 新增行必须登记；**自带 `--selftest` 正反对照**） |
| **alloc 迁移（2.3b）** | **片1 ✅｜片2 ✅｜片3 ✅｜片4 ✅｜片5 ✅**（`ontology` 429＋`state` 325＋`energy` 377＋`sanitizer` 235＋`interference` 319 = **1,685 行**）。<br>⚠️ **片3 执行中发现两处「清单未预见」的硬障碍**：① **`HashSet` 在 `no_std` 不存在**（编译级证据）⇒ `ontology` 两处换 `BTreeSet`（**只用 insert/len、从不迭代 ⇒ 逐位等价**，见 **D37**）；② **`FloatOps` 初版只实现 `f32`**，而片3 用到 **f64 的 `round`/`sqrt`/`log2`** ⇒ `fmath` 补 **f64 全套**（含 **Cody-Waite 两段 ln2**、`atan` 泰勒 10→16 项、`powi` 改「最后取倒数」、新增 `rem_euclid`）。**f64 精度实测（vs `std`）**：`sqrt` **1 ULP**／`log2` **1**／`ln` **1**／`exp` **2**／`powi` **≤4**／`round`、`rem_euclid` **完全相等**。<br>**裸机断言扩到第 ⑥ 段**（片3 五模块：sanitizer 钳位／ontology 特征向量＋**正反两侧不同**／energy 决议边界／state 熵→物态／interference 相位差）⇒ 一条绿屏同时证成 **2.1＋片1＋片2＋片3＋`alloc`**。<br>**片4（修正版 10 模块／3,920 行）**：`trace`／`dna_generate`／`dna_trace`／`habit`／`gene_library`／`l5_context`／`l1_field_parse`／`l3_world`／`l5_quad`／`l5_evidence`（原订 5 模块**不封闭** ⇒ 扩为 10）。**片5（16 模块／3,651 行，清单由脚本产出）**：`l1_source_parse`／`thinking_chain`／`hourglass`／`evolution`／`l5_attention`／`gate`／`self_recognizer`／`l7/ledger`／`dna_adapt`／`double_chain`／`mirror`／`senses`／`evo_deconstructor`／`persist`／`executor`／`l5_compare`。<br>⚠️ **片5 又暴露一条「清单未预见」的硬障碍**：**`std::collections::VecDeque`** —— `no_std` 下 `std` 拿不到、也不在 prelude，但 `alloc::collections::VecDeque` 有（**纯路径改写**）；首跑漏登 ⇒ 4 个文件报 `cannot find module or crate std`（**编译器抓到**）。⇒ 迁移脚本**新增硬判据：代码区不得残留 `std::`**（把这类问题拦在编译器之前）。<br>**裸机断言扩到第 ⑩ 段**（⑧＝51–55 三态判定**三态齐备且互不相同**／动作账链**篡改必拒**／沙漏**每 tick 至多 1 粒**；⑨＝片6 四模块；⑩＝片7–片8 三模块）⇒ 一条绿屏同时证成 **2.1＋片1–片8＋`alloc`**。<br>**片6–片8 ✅ 已收口**（脚本口径 7 模块：`l1_mapping`／`positive_source`／`l5_diagnosis`／`l7/repair`／`l5_translate`／`l6_face`／`l5_router`）—— 片6 的 **`std::collections::HashMap`** 已按「方式 A」换 `BTreeMap`（「类型替换」类，**已单独论证**）；收口实测见本文件 **§十二** |
| **机制 20（术语映射 · 机制 17 第三实例）** | ✅ **升级为三层 + AES-256-GCM**（2026-09-17，**D38**）：**公开层** `coordination/TERMS.md`（编号＋**英文名**＋中文功能说明＋指针，**不加密**）｜**索引层** `{PRIVATE_ASSETS}/terms/INDEX.md.enc`（编号＋英文名＋**代码代号**＋指针；**不含全拼/原名**）｜**内容层** `{PRIVATE_ASSETS}/terms/TERM-001.md.enc`（**全拼＋原名**＋原始内容＋**真实代码路径**）——后两层**仓库外、已加密**。<br>**加密机器判据 4 条**（`tools/terms_crypt.py verify`）：密钥**在仓库外** ✅｜仓库内**含密钥文件 0 个** ✅｜6 个密文**全部可解且原名 0 处** ✅｜**错密钥被拒**（`InvalidTag`）✅。<br>**指针门禁**：`check_private_pointers.py` **syntax + resolve 双 PASS**（`.md.enc` 可直接解析）。<br>⚠️ **边界（不许含糊）**：**换壳 ≠ 消除**（仓库内**仍有 298 处原名**可检出：`docs/` 270／`reports/` 15／`discussions/` 7／`instructions/` 6 ⇒ 待 **D35/D36**）；**加密 ≠ 挡住仓库内存量**；**代码代号不是强边界**（源码本身公开）。| ｜**密钥级别：⛔ 机密**（本体／路径／其余一律先按机密）<br>**2026-09-17 补全**：**密钥 C12 级别**写入 `CONSTRAINTS.md` **C12「密钥专项级别」**＋ `TEMPLATES.md` **§十二**（本体 ⛔／指纹 🔒／路径 ⛔）；检查器新增 **[5] 密钥级别判据**（活文件中凡含「密钥」的行必须带级别标识；**已纳入 CI 的 syntax 模式**，阴阳两侧自检通过）。<br>**换壳（D35③＋D36②）**：`deploy/` 1 条 ＋ `docs/` 设计说明类 4 文件 8 条 **已掩码** ⇒ 泄漏基线 **14 → 5 条** |
| **机制 17 第二实例（大模型接入骨架）** | ✅ 公开层 `coordination/llm/`（4 文件：任务分级／降级规则／成本记录 三接口 ＋ 总览，**只写"做什么"**）｜实现层 `{PRIVATE_ASSETS}/llm/`（4 文件，⛔；**合同已定、取值待填**，未编造）｜**机器校验**：`coordination/tools/check_private_pointers.py`（`--mode=syntax` **已入 CI**／`--mode=resolve` 仅本机）＋ 泄漏基线 `coordination/security/leak_baseline.txt` |
| **内存管理（2.3）** | ✅ `meta-kernel-mem`（纯逻辑层）**25 项 host 测试全绿**；`#![forbid(unsafe_code)]`（**零 unsafe 结构性保证**）｜boot 层 `mem/` 可建堆并完成**经真实 `GlobalAlloc` 的往返**（分配→写入→读回→释放→**再分配复用**＋帧路径对齐）；**判定升级三色**（绿=全过／**黄=拿不到堆区就停**／红=失败）<br>**C9 门禁**：真实 unsafe **仅 1 个文件**（21 处／`// SAFETY:` 22 条／`transmute` **0**）；白名单**首次登记 1 条**；**扫描范围已扩到阶段二新增目录**（原先不含 ⇒ 会让门禁空转） |
| **引导层（2.2）** | ✅ `meta-kernel-boot`：可引导 `boot-bios.img` **3,638,272 字节**／MBR 签名 **`55aa`**；**CI `boot-image` job 上 QEMU 真实引导 + 帧缓冲截屏断言通过**（**1280×720**、**25 个采样点全为 `(0,255,0)`**）；**真实 unsafe = 0**（路线 A：屏幕输出只经安全 API） |
| 本地验收模式实跑 | **11 个全跑**：**10 PASS**（含修复后的 `diag-check`）｜1 PARTIAL（`quad-check`·已知待加强） |
| CI 覆盖 | ✅ **全绿并已逐行核实**（run `35077993177`，headSha `c84afc0`）：`test` job（ubuntu）＋ **`host-windows` job（windows-latest）**，后者含 **6 项门禁**——⑤ C9 门禁（**前置**：纯静态、秒级、无 GPU）／**单元测试 field-render（35 项，D-a 新增）**／① 离屏自检（退出码0+PASS+方差>1+帧率口径）／② 排序／③ L5 诊断／④ **真实开窗 + surface 回读**；另含「环境诊断」步骤（打印适配器，不计门禁） |
| **CI 上真实运行** | ✅ **是，且已逐行核实真实输出**（run `35069720108` 验收④）：runner 无 GPU，落 **Dx12 WARP 软件适配器**，完成 **开窗 → 输入 URL → 取源码（108 字）→ 从 surface（真实上屏纹理）回读（方差 100.3、非纯黑非纯白）→ 结论 PASS → exit=0**；呈现 18.1 FPS（**软件口径，如实标注、未按硬件 ≥30 断言**） |
| 宿主单元测试 | ✅ **35 项全绿**（`cargo test --bin field-render`，含 R17 判据的正反两侧）。⚠️ 该 crate **不在 workspace** ⇒ CI 的 `test` job **不会跑到它**（R18 的 3 个必红测试因此长期无人知）；建议在 CI 里加一步独立 `cargo test` |
| 验收模式 | 11个：`selftest`／`sortcheck`／`quad-check`／`like-check`／`lod-check`／`world-check`／`diag-check`／`link-check`／`l4-check`／`persist-check`／`ui-selftest` |

## 八、硬约束

全文见 `CONSTRAINTS.md`；一句话速查见 `CHARTER.md` §四。**十四条**摘要：
C1 内核零依赖｜C2 版本号自动生成不手写｜C3 不用 unsafe 取巧代码（无例外）｜C4 改前先列清单、确认后执行｜
C5 诚实标注未完成项（本地/CI/真实/未验证）｜C6 每次报告带三量台账｜C7 不用 CI 通过代替真实运行｜
C8 硬约束可扩充（用户确认后生效）｜C9 unsafe 只允许边界形态（边界集中 + `// SAFETY:` + 禁 transmute + 白名单，**已有 CI 门禁**）｜
C10 **动作约束**（凡"外部可见或不可逆"的动作须用户明确确认；清单与判据见 `CONSTRAINTS.md` C10 / `CHARTER.md` §六）｜
C11 **决策评估机制**（**顾问侧**：凡涉及决策必须给**七要素**——评估／优势／劣势／风险／**机会**／建议／**等待用户决策**；**前六要素不含机会时即为不合规**）｜
C12 **保密机制**（**三方共用**：报告须标四级别 🔓/🔒/🔐/⛔；**机密绝不拷给顾问**；⚠️ 本轮发现 🔐 与 ⛔ 的**范围重叠**，建议消歧见 `ASSETS_INVENTORY.md` §0.1，待用户确认）｜
C13 **顾问阅读指令**（**三方共用**：WorkBuddy 每次返回**最前端**必须放固定文本；顾问按 **9 步**执行并输出审核结论——**第 9 步＝对齐用户意图**（先复述「用户真正要什么」再下结论）；未执行＝阅读不到位）｜
C14 **夜间自动化**（**WorkBuddy 侧**：23:00–08:00 **只跑 P3**；21:00–23:00 仅收尾；**双通道触发**、**默认不推送**）

### 已推送内容的处理原则

- 已推送的 Git 历史**撤不回**（Git 设计特性）
- **不追改历史**（成本高、收益低）
- 当前版本开始执行 **C 档增量换壳**：公开仓库**不再携带敏感内容**
- 未来版本继续**骨架-实现分离**（机制 17）
- **判据**：判断某个内容能不能写进仓库时，先问"**它会不会永久留在公网？**"——会 ⇒ 按 **⛔** 处理，放仓库外私有文件夹。
- **配套**：新写内容一律用**物理语言**（`ASSETS_INVENTORY.md` 对照表），历史不追改（`TEMPLATES.md` §十、D30-b）

## 九、待用户决策

| 编号 | 事项 | 建议 |
|---|---|---|
| D1 | ~~meta-kernel/ 陈旧改动如何处置~~ | ✅ **已执行**：归档至 `研发开发/archive/meta-kernel-v0.48/`，保留不删 |
| D2 | 推送SNI摩擦是否专项排查 | 本轮 push 未见复发，可后置 |
| D3 | 宿主真实运行路径选择 | ✅ **已定并已执行**：PATH 加 llvm-mingw `bin`（并已固化为 CI 步骤） |
| D6 | ~~是否修 `--diag-check` 断言（R7）~~ | ✅ **已执行**（对齐中性点 m=0.5，本机 PASS） |
| D7 | ~~是否给 CI 加 windows job（R6）~~ | ✅ **已执行**（新增 `host-windows` job） |
| D8 | `quad-check` 喜欢维（R8）排期 | P2；须在**内核映射层**改，禁止宿主调参打绿 |
| D9 | ~~阶段二 unsafe 边界是否放行~~ | ✅ **已执行**：C9 已增补 + 白名单机制就位（当前为空，未放行任何条目） |
| **D10** | CI「真实开窗」何时从**诊断性**升为**门禁**（R11） | ✅ **已执行**：去掉 `continue-on-error`，验收④＝门禁（退出码0 + 回读来源＝surface + 结论 PASS）；依据＝run `35044362728` 真实通过 |
| **D11** | C9 门禁尚无 **CI 断言**落地（R10） | ✅ **已执行**：CI 验收⑤（`transmute` 零出现 + 白名单子集 + `// SAFETY:` 覆盖 + **门禁自检**）；本地原样干跑 exit=0 |
| **D12** | CI FPS 断言在**软件适配器**（runner 无 GPU）下按 >0 而非 ≥30 | ✅ **已定（接受现状）**：如实标注软件口径、不按硬件断言；**不为此自建带 GPU runner**（成本不划算）；若将来需要，按 D12 重开 |
| **D13** | `研发开发/meta-kernel_repair_0907/` 如何处理 | ✅ **已执行**：归档至 `研发开发/archive/meta-kernel_repair-0907/`（100 个文件，移动前后一致，无删除）；根目录已无陈旧分叉 |
| **D14** | CI 的 libgcc 语义映射依赖 llvm-mingw 内部布局（`lib/clang/<ver>/lib/windows/libclang_rt.builtins-x86_64.a` + `x86_64-w64-mingw32/lib/libunwind.a`） | 已在安装步骤内**断言两个映射源存在**（缺源即明确报错）；升大版本时若布局变化，按报错同步即可，不必钉版本 |
| **D15** | 机制是否引入**轻量档**（小任务只带基准/任务/状态/边界说明四项） | 🔬 **观察中**（OBS-002）：至少观察 3 轮再定，不急于加复杂度；**三量台账与边界说明任何时候都不省**（C5/C6） |
| **D16** | `CHARTER.md` 自包含版 / `TEMPLATES.md` §七 变更记录 / C10 动作约束 / 启动回执 —— 本轮四项新机制 | 🔬 **试用中（3 轮）**：第 1 轮已过；**用户已定「继续试用满 3 轮」**（D18）。3 轮后回顾决定"生效 / 调整 / 废弃"（记入 `TEMPLATES.md` §7.2） |
| **D17** | **子任务2 路线修正：mkosi → 元内核自举** | ✅ **用户已决策（2026-09-16）**：放弃 mkosi（① Linux-only、② 产出是"带 systemd 的 Linux"，与"元内核自己成为内核"的目标不符）；改为 **元内核自举**（no_std + GRUB/Limine + 最简内存 + DRM/KMS 直渲 + field-render 作首个用户态进程 + ACPI/CPUID 软识别 + QEMU 验证）。原**子任务3「元内核注入」已并入**。**已同步 `ROADMAP.md`**（含 2.1–2.7 与 M2/M3 里程碑） |
| **D18** | 上轮两项待决策（独立 crate 加 CI 测试步 / 4 项新机制试用期） | ✅ **用户已决策**：D-a **做**（已执行 → 见"未完成"表末行）；D16 **继续试用满 3 轮**（不提前判定） |
| **D19** | R2 的对策：切换为 **SSH over 443** | ✅ **已完成切换并验证**（2026-09-16，用户添加公钥后）：`origin` → `ssh://git@ssh.github.com:443/huanglema0830/meta-kernel.git` ＋ `core.sshCommand` **显式**指定 key 与 443 端口（**本地配置，不入库**）；`git ls-remote` **exit=0**、**真实推送成功**（见 §七 指标）。<br>⚠️ 过程中查清**三个同根因现象**（详见 **R19**）：① `~/.ssh/config` 不被加载 ② `ssh -T` 认证成功却**返回 1** ③ 我最初的脚本误判"公钥未生效"。**根因同为**：本机 ssh 解析**中文用户名**时**编码不匹配**，算错 home 路径。<br>**对策**：不依赖 config 加载（改用 `core.sshCommand`）；判断**只看输出文本、不看 ssh 退出码**；**刻意不加** `StrictHostKeyChecking=accept-new`（写 known_hosts 会失败并污染退出码）。<br>**回退**：`git remote set-url origin <HTTPS>` + `git config --unset core.sshCommand`（或在账号删公钥） |
| **D20** | 通用工作流的**算法层**是否/何时填充 | ✅ **用户已定：延后**（框架层先用；算法层等真正需要时再填） |
| **D21** | 顾问侧接续是否纳为常设机制 | ✅ **已建立并登记**（`CHARTER.md` 机制 12/13）：`advisor_brief.md`（顾问侧滚动摘要 ≤5 条）＋ `CONTINUITY.md` §8（接续包生成与用法）；🔬 试用中 |
| **D22** | 决策评估机制（顾问侧） | ✅ **已建立**（2026-09-16 用户决策）：新增 **C11** + `TEMPLATES.md` **§九 决策评估模板**；**用户决策前顾问不得擅自执行**。🔬 试用中<br>**↳ 2026-09-16 扩展（T-009）**：六要素 → **七要素**（评估／优势／劣势／风险／**机会**／建议／等待用户决策）。**新增"机会"的理由**：把"机会"混进"优势"会**虚增确定性**（用户会以为附加可能性已经到手）。**优势 vs 机会**＝**确定的** vs **可能的**；机会 ≥1 条且**必须可被证伪**。已同步 CONSTRAINTS C11 / TEMPLATES §九 / CHARTER（C11 + 机制 14） |
| **D23** | 子任务2.1 走哪条路径（no_std 迁移） | ✅ **用户已确认：路径 A（分两步）**——2.1 只做"**无 alloc 子集**"，每步可独立验证、**C1 不破**；**alloc 迁移由 2.3 承接** |
| **D24** | 浮点超越函数：自实现 vs 引 `libm` | ✅ **用户已确认：自实现**（守 C1，不引 libm）；**精度要求＝与 std 对照 <1e-9** |
| **D25** | **保密机制**（四级别 + 机密清单） | ✅ **已建立**（2026-09-16 用户决策）：新增 **C12** ＋ `TEMPLATES.md` 报告模板**新增「保密标记」字段**（并升为**报告必带十二项**之第 1 项）。四级别：🔓公开／🔒半公开／🔐保密／⛔机密；⛔ 含 密钥/token、账号、本机路径、网络细节、服务器地址、**参数库内容**。**适用范围：三方共用**；双方都有提示义务；分级宽严由用户定（C11）。已同步 CHARTER（C12 + 机制第 15 项）/README；🔬 试用中，**本轮报告首次实践** |
| **D26** | 2.1 迁移范围：A-1（最小闭包 7 模块）或 A-2（核心集） | ✅ **用户已确认：A-2（核心集）** |
| **D27** | **A-2 的精确范围**（实测后与预期"~15"有差） | ✅ **用户已确认并执行完毕**（2026-09-16）："12 模块 + 6 留 2.3"分期。**2.1 已完成**——见 §六 未完成项表与 `reports/2026-09-16_子任务2.1执行_no_std子集建立.md` |
| **D28** | **`l7` 的归属**（执行中发现的偏差） | ✅ **用户已确认：归 2.3**（2026-09-16）：`l7.rs` 仅 23 行模块声明，其 **4 个子模块（`grade`/`ledger`/`mesh`/`repair`，共 1,122 行）全部含 alloc** ⇒ 由 **2.3**（最简内存管理 + alloc 迁移）一并承接。2.1 实际迁移范围定为 **10 模块 + `quad` 数据契约**。已在 `ROADMAP.md` 2.3 行注明 |
| **D29** | **数学函数"精度 < 1e-9"的口径** | ✅ **用户已确认：取 ULP 判据（≤4），不做 `f64` 版**（2026-09-16）：理由——`f32` 下**绝对误差 1e-9 物理不可达**（例 `exp(88)` 的 1 ULP ≈ 3.9e31）；实测本实现 **max ULP ≤ 1**（`atan2` 为 0，相对误差 ~1.1e-7），**已是 f32 的最优水平**。判据落在 **ULP** 上（数值计算的标准做法），**门线未放宽**。`powi` 的次正规区差异另记（std 基准跨平台不一致，改用 f64 真值作基准） |
| **D30** | **本仓库为 PUBLIC**（R20 发现的处置）+ 私有资产清单的落点 | ✅ **用户已确认（2026-09-16）**：**① 保持现状 + 私有资产清单永久留本地**（不转 private、不入库）。<br>⇒ **不再新增暴露**：机密/保密内容一律落**仓库外**私有文件夹（见 机制 **17 拆分机制**）；仓库内只保留**指针**（`coordination/ASSETS_INVENTORY.md`）。<br>**未处置部分（如实标注）**：git 历史与公网缓存中**已推送**的内容**无法撤回**；`coordination/` 现有 45 文件仍在公网 ⇒ **R20 保持"未解除"**，仅"停止扩大" |
| **D30-1** | Git 历史是否追改（**2026-09-17 用户确认**） | ✅ **选路径 A：接受现状**。**用户给出的四条理由**：① 已推送的内容**只是骨架**（机制条文／术语体系／模板结构）② **真正需要保密的**（算法实现、具体阈值、参数库内容）**从未进入公开仓库** ③ **`git filter-repo` 的风险远大于收益**（所有克隆失效、fork 保留、重新污染）④ **C 档增量换壳**已确保**未来不再新增暴露**。<br>⇒ **不追改历史、不做 force push、不重写历史**。处理原则已写入 §八「**已推送内容的处理原则**」；R20 已按此更新为"保持未解除 + C 档 + 接受现状" |
| **D30-b** | **语言替换档位** | ✅ **用户已确认：C 档（只做增量换壳）** —— **新写内容一律用物理语言；历史不追改**。理由：git 历史改不掉 ⇒ 追改当前文件属**假安全**；C 档让**未来不携带旧术语**，成本最低、可逆。<br>**本轮不做替换**（用户明确："等盘点完成后，通过对话再定"）——对照表已产出，替换执行另开 |
| **D31** | **子任务2.2 引导器选型** | ✅ **用户已确认并执行完毕**（2026-09-16）：**a** `bootloader_api` 只进 boot 层、内核与 nostd crate 保持零依赖 ✅｜**b** 引入 nightly 且**只在 boot 层生效**（其余仍 stable）✅｜**c** QEMU 放 **CI** ✅｜**d** 验证路线选 **A（零 unsafe，帧缓冲 + screendump）** ✅。<br>**口径修正已采纳**：由"接入 GRUB/Limine"改为 **`bootloader` 0.11**（官方文档：内核用 `x86_64-unknown-none` 即可，不需自定义 JSON target/build-std）⇒ **2.1 成果零返工**。<br>**结果**：`meta-kernel-boot/` 建成并可用；CI 新增 **`boot-image`** job；**QEMU 真实引导 + 截屏断言通过**（run `35110443157`，1280×720、25 采样点全绿）。详见 `reports/2026-09-16_子任务2.2执行_bootloader接入.md` |
| **D32** | **是否创建 `.github/workflows/nightly.yml`**（机制 19 的"机器类"通道，T-014/T-015） | ✅ **用户已确认（2026-09-17）：选① 暂不创建**。<br>**再评估条件（满足任一即重提）**：① **连续 7 天无 push** ② 再次出现"**本机绿／CI 红**"的**环境漂移类缺陷**（同 R23 家族）。<br>**届时优先选③「轻量 nightly」**：**只跑 `test` + `boot-image`**（低噪声；不跑 `host-windows`）。<br>**yaml 已在 `TEMPLATES.md` §11.2 备好**，一句话可启用（可逆） |
| **D33** | **夜间是否允许 push**（机制 19 安全边界，T-015） | ✅ **用户已确认（2026-09-17）：选① 夜间默认不自动 push**。**夜间只允许本地 commit**；**次日由用户一句话放行推送**。若确需推送，**必须走 C10（用户明确确认）**。<br>⇒ 已同步写入 23:00 定时任务 prompt（禁止清单第 5 条）与 `TEMPLATES.md` §11.8 |
| **D34** | **是否放行 C9 白名单**（首次，机制 19 → 2.3 触发） | ✅ **用户已确认（2026-09-17）：同意放行，仅限 `meta-kernel-boot/kernel/src/mem/global.rs`**。<br>**四项要求**：① 每块 unsafe **必须有 `// SAFETY:` 论证** ② **禁 transmute** ③ 白名单登记 ④ **纳入 CI 门禁**。<br>⇒ 已执行：白名单登记该 1 条；CI 的 C9 门禁（词边界 + 剔注释 + SAFETY 计数 + 禁 transmute + **门禁自检**）自动覆盖 |
| **D34-b** | **`alloc` 是否破 C1「内核零依赖」** | ✅ **用户已确认（2026-09-17）：认定 `alloc` 属标准分发，不破 C1**。<br>**要求写的三条界线**（已写入 `CONSTRAINTS.md` **C1 举例**）：① `core`/`alloc` 属标准分发、**不算外部依赖**；② **`alloc` 使用不得引入外部 crate**；③ **`GlobalAlloc` 实现受 C9 约束** |
| **D36** | **术语换壳的范围要不要扩大**（机制 20 试点后的下一步） | ✅ **用户已确认（2026-09-17）：选 ② —— `docs/` 的「设计说明类」也换，历史报告不追改**。**已执行（4 文件 / 8 条）**：`HOST_BUILD.md`／`HOST_UI_DECISION.md`／`SECURITY_TROUBLESHOOTING.md`／`SILA_IMPLEMENTATION.md`。<br>**未换（不追改，沿用 D30-b）**：`FULL_AUDIT_REPORT.md`（13+3+3 条）／`L5_VALIDATION_REPORT.md`（3 条）／`PERFORMANCE_REPORT.md`（1 条）。<br>**泄漏基线**：**14 条 → 5 条**（**只减不增**，C15 纪律） |
| **D35** | **`docs/` 与 `deploy/` 历史文件内 ⛔ 内容的换壳范围**（R25 的**未解除部分**） | ✅ **用户已确认（2026-09-17）：选 ③ —— 只换 `deploy/`，不动 `docs/` 历史报告**。**已执行**：`deploy/win64/README.md` 的 1 条 `lan_ip` 已掩码为 `<内网网段>`。⚠️ **换壳 ≠ 消除**：**git 历史里仍可检出** |
| **D40** | **剩余模块的迁移分片，是否改为「脚本产出」** | ✅ **用户已确认（2026-09-17）：选「脚本产出分片」，不再手写**。**已落地**：`coordination/tools/check_migration_closure.py` 新增 **`--plan`**（按依赖分层产出分片）／**`--emit N`**（打印第 N 层清单供脚本消费）／**`--selftest`**（阴阳自检：封闭样例必放行、普通缺口必判红、**仅测试缺口必判红并单独标出**、环形样例分层不发散）。**片5 即为脚本产出的第一片**（16 模块 / 3,651 行） | **理由**：手写清单**已连续两片出错**（片4 把「严格链式」写错；片5 手算漏认 `l7::ledger` 依赖，把 `l7/repair` 误放进层 1）⇒ **人算不如脚本算**。**代价**＝新增一个「会漂移」的口径（见 **R33**），已用 `--mods` 冻结清单解决 |
| **D41** | **片6 的 `std::collections::HashMap` 如何处理**（**类型替换类**，须先论证） | ⏳ **待确认**：`alloc` 里**没有 `HashMap`**，只有 `BTreeMap`。**关键前提＝该集合是否被迭代**：若**被迭代** ⇒ `BTreeMap` **不逐位等价**（顺序不同）。<br>**建议**：先出**用法论证清单**（读用法＋编译探针），确认后再迁片6 |
| **D42** | **是否把「签到 ＋ 派猫猫」接成每日定时任务**（免费积分目前**零自动化**、正在流失） | ⏳ **待确认**：**建议接**（建议 09:00，与既有晨间任务错开）。**可逆、零成本、当天见效**；涉及登录态 ⇒ 属**对外动作**，按 **C10** 须用户放行 |
| **D43** | **动作②（替换浏览器）的目标形态**：宿主态 还是 内核态 | ⏳ **待确认**：**建议「宿主态」**（不动内核；起点＝`host/field-render`／`host/sky-browser`）。理由：浏览器**必须**感知/动作（网络·文件·进程）⇒ 按机制 21 **本就不能进内核** |
| **D44** | **动作③（替换操作系统）下一步**：先出只读对比基线 还是 直接攻阶段二 | ⏳ **待确认**：**建议先出只读对比基线**（给「差多少」的硬数）。⚠️ 如实说明：**当前不可达**——显示/输入/网络/存储/驱动 **= 0**，且隔着**驱动模型与用户生态**两道第三方闸门 |
| **D45** | **是否授权「只读枚举」**（注册表／服务／驱动／浏览器内部配置） | ⏳ **待确认**：**建议授权只读**；任何**写操作**（改注册表/停服务/动驱动）**单独报批** |
| **D46** | **浏览器引擎路线**（自研／嵌入非 Chromium／兼容 Chromium／退回接管） | ⏳ **待确认**（**阻塞轨 A 全部后续**）：**建议先「A 专用（可逆）＋ 用 C 接管做过渡」**。<br>⚠️ **「嵌入 WebView2」不在选项内** —— 实测它等于 **Edge 内核**，与「替换 Edge」自相矛盾（R39）。<br>**B 兼容层**是唯一满足字面目标的路，但**须按「以年为单位」投入**（对手体积 690MB–1.6GB 即复杂度指纹）|
| **D47** | **是否把「浏览器替换」从阶段三拆出、独立并行推进** | ⏳ **待确认**：**建议拆**。依据＝**机制 21**：浏览器需要网络/文件/进程/凭证 ⇒ **天然整条落在宿主/用户态，本就不能进内核** ⇒ 无理由等内核 |
| **D48** | **M5「能看网页」口径重定义** | ⏳ **待确认**：**建议改为「靶子清单达标率」**（删掉「任意现代网站」的隐含要求）。理由＝「任意网站」≈ 重做 Chromium 可观子集（数千人年量级）⇒ 不重定义则 M5 必然长期漂移 |
| **D49** | **是否授权「只读采集」两个浏览器的书签／历史／扩展清单**（靶子清单取证，轨 A0） | ⏳ **待确认**：**建议授权只读**（产出「必须兼容／可降级／可放弃」三档清单）；**任何写操作单独报批**；采集结果**不入仓库**（放本机私记） |
| **D39** | **私有层「删明文」（`terms_crypt.py purge`）是否放行** | ✅ **用户已定（2026-09-17）：先离线备份密钥，再放行删除明文**；**备份方式＝用户自行抄写密钥本体到物理介质**；**备份确认后才执行 `purge`**；**若未确认备份 ⇒ 保持明文与密文并存**。<br>**当前状态：✅ 已执行（2026-09-17 17:25）** —— 用户确认「D39 备份完成」后**执行了 `purge`**，**6 份明文已删除，磁盘只剩 `.enc`**。<br>**执行前的前提（三重，全部通过才允许删）**：① 密钥指纹复核一致（🔒 `〈本机私记〉`）；② 6 份密文解密结果与明文**逐字节相同**（工具内建校验 ＋ **独立复核**）；③ **阳性对照**——错密钥被拒（`InvalidTag`）⇒ 比对判据非空转。<br>**执行后复核**：残留明文 **0**｜6 份密文**均可完整解密**（长度与删前记录逐一一致）｜`verify` 四项（密钥在仓库外／仓库内无密钥／密文可解且不含原名／错密钥被拒）**全过**。<br>⚠️ **自此密钥成为唯一依靠**：**密钥丢失 ⇒ 私有层永久不可读**（无后门）。<br>**密钥级别**：**本体 ⛔ 机密**／**指纹 🔒 半公开**（`〈本机私记〉`）／**路径 ⛔ 机密**。<br>⚠️ **不可逆风险**：**密钥丢失 ⇒ 私有层永久不可读，无后门**。<br>⚠️ **技术提醒（防止备份失败）**：密钥本体是 **32 随机字节（64 个十六进制字符）**，**手抄极易出错** ⇒ 推荐**整文件复制到物理介质**；若手抄，**必须用指纹核对**（`terms_crypt.py keycheck` 打印指纹，与 `〈本机私记〉` 逐字比对） |
| **D38** | **机制 20 的编码方式：由「不加密、靠物理隔离」改判为「AES-256-GCM 加密」**（T-021 写的是不加密；本轮用户改判） | ✅ **用户本轮已定（2026-09-17）**：三层 + 加密。**已落地**：密钥 32 随机字节、位于 `PRIVATE_ASSETS` **同级**、**绝不进仓库**（本轮实测：仓库内**含密钥文件 0 个**）；算法取 **GCM**（AEAD）而非 CBC —— 理由：**CBC 的「被篡改」不可判**，而本项目首要是「**判据能证伪**」 |✅ 已生效；⚠️ **不可逆风险**：**密钥丢失＝私有层永久不可读** ⇒ 用户须**离线备份密钥** | ｜**密钥级别：⛔ 机密**（本体／路径／其余一律先按机密）
| **D37** | **`ontology` 的 `std::collections::HashSet` → `alloc::collections::BTreeSet` （类型替换，非纯路径改写）是否放行** | ✅ **用户已追认（2026-09-17）**。依据：`HashSet` 在 `no_std`/`alloc` 里**不存在**（**编译级证据**：`std::…` ⇒ `cannot find module or crate std`；`alloc::collections::HashSet` ⇒ `could not find HashSet in collections`）；两处集合**只用 `insert`/`len`、从不迭代** ⇒ **逐位等价**。⚠️ **反例情形**：若将来该集合**被迭代**，BTreeSet 与 HashSet 的**顺序不同** ⇒ 前提失效，**须重评** |

---

## 十、2026-09-18 裁定记录（七项裁定 ＋ Q1–Q11 落地）

> **来源**：发起人 2026-09-18 指令（基准 v0.197）｜**性质**：裁定落地记录（非新提案）
> **落地原则**：**裁定结果一律记入本文件**；**原始底图 `docs/` 只在裁定明确指向时才改**，改则同步更新底图哈希基线（判据 F）。

### 10.1 裁定 1 · §5 越界定性（首夜 09-17 21:00 → 09-18 08:00）

| 项 | 内容 |
|---|---|
| **对象** | 2026-09-18 00:37–06:03 期间：**治理文件改动**（`CHARTER`×3／`CONSTRAINTS`×1／`ROADMAP`×1／`TEMPLATES`×1／`.github/workflows/ci.yml`×1；**`BASELINE` 未动**）＋ **夜间 push**（v0.191–v0.197） |
| **裁定** | ✅ **记为「字面越界但归属豁免」** |
| **归属** | **来源＝用户在场指令会话**（00:00–00:40 一轮起因＝发起人批评并下发 D50–D55；05:05–06:03 一轮同属该指令后续）——**非夜间自动化** |
| **夜间自动化本身** | ✅ **未越界**：其 4 个 commit（`54b8f3b`／`cfb57cf`／`2746878`／`2fdb277`，23:06–23:13）只动 `coordination/reports/*.md` ＋ `meta-kernel-core-nostd/src/fmath.rs`（**仅 `#[cfg(test)]` 区**），未碰治理文件、未碰 `.github/workflows/`、**未 push** |
| **字面越界的部分** | 只按 **C14／D33 的字面时段判据**，上述改动与 push **确实落在 23:00–08:00 内** |
| **配套机制** | **机制 23（夜间时段例外机制）** 本轮建立 ⇒ 此后同类情形**不再需要逐次裁定** |

### 10.2 裁定 2 · Q15/Q16 执行确认

| 项 | 结论 |
|---|---|
| **执行质量** | ✅ **确认通过**。共改 **11 处**（3 处主误判 ＋ 8 处连带），**未触碰任何原始底图条文**。 |
| **Q15** | `docs/LAYER_BASEMAP_L0_L6.md` **v0.1 → v0.2**：① L4-M1 τ「全库无定义」→ **τ 有定义且已实测**（`FIELD_REPAIR_DESIGN` 行35；`L5_VALIDATION_REPORT` 行93-95 τ=6.587），真实缺口降级为「L4 定型钢用例 τ 恒为 1.0 ＋ τ 归一化口径未写」，**由「缺失」改列「不足」**；② L3-M2／L6-M2「用户确认入口缺失 ⇒ 卡住 L7」→ **已裁决并实现**（`L7_EXECUTION_DESIGN` §2.3 行126 ＋ §7.3 行268-273，v0.109），**L7 未被卡**；③ L0-V1「真空重启作用域自相矛盾」→ **底图 v1.1 已自收敛**（`MATH_SPEC` §4 A5 行46 正文括注 ＋ 行48-49），**由「漏洞」撤销**。连带撤销 ⟨补 5⟩／⟨补 12⟩，修正 ⟨补 14⟩ 原注，§10.1 台账按 C15 复算迁移（**合计 44 不变**；缺失 17→14／不足 15→19／漏洞 12→11；⟨补⟩ 17→15）。 |
| **Q16** | `README.md` **行75**：「细分粒度模型（Micro-Granularity Model）」→「**采微大模型（Caiwei Model）**」（**1 行**）。依据：`LAYER_ARCHITECTURE.md` §3.2＝全库命名唯一权威。 |
| **产出** | `coordination/discussions/2026-09-18_顾问核验包.md`（块 0–10，1236 行；⛔ 已预屏蔽 6 行本机用户名） |

### 10.3 裁定 3 · `--update-baseline` 授权

- ✅ **已授权并已执行**。
- **动作**：`python coordination/tools/check_source_of_truth.py --update-baseline` ⇒ 重生成 `coordination/security/basemap_hashes.txt`。
- **改动理由（必填项）**：**Q15 授权更正 `docs/LAYER_BASEMAP_L0_L6.md` 三处误判措辞**（v0.1 → v0.2），该文件属 `docs/`（判据 F 覆盖范围）⇒ 内容变化必然使基线失效，须显式确认。
- **未受影响的项**：**Q16 的 `README.md` 不在基线内**（基线只覆盖 `docs/` 全 35 份 `.md`）⇒ Q16 **不触发**判据 F。

### 10.4 裁定 4 · Q15/Q16 写入 BASELINE

- ✅ **已写入**：见本文件 **§10.2**（Q15/Q16 执行结果与质量确认）。

### 10.5 裁定 5 · 首夜报告用户名清理

- ✅ **已授权并已执行**：`coordination/reports/2026-09-18_首夜结果.md` 内 **6 处本机用户名**（原行 90／139／140／141／142／226）已替换为 `〈本机用户名〉` 占位。
- **验证**：清理后 `check_private_pointers.py --mode=syntax` ⇒ **PASS**（原为 `FAIL … username_literal｜实测 6 > 基线 0`）。

### 10.6 裁定 6 · Q1–Q11 落地（发起人已同意）

| 编号 | 级 | 裁定结果 | 落地形式 |
|---|---|---|---|
| **Q1** | P0 | ✅ **选甲 ＋ 切换条件**：L4 冷启动用**单位基线（七维全 1.0）**，本底场由 L5 边诊断边建立；L4 只消费「已存在的」本底场，没有就用单位基线 | 记入 `LAYER_BASEMAP_L0_L6.md` ⟨补 13⟩（由「待裁定」→「已裁定」） |
| **Q2** | P0 | ✅ **加成熟度门槛**：本底场样本数 **< N** 且 high/low 计数**恰好 == 3** ⇒ **降级为 Pass 并标记 `baseline_immature`**（可追溯） | 同上 |
| **Q3** | P1 | ✅ **N = 7**（**七维场域天然依据** —— 每一维至少一个样本） | 同上（**取代**此前建议的 N=5） |
| **Q5** | P0 | ✅ **可**，标 **`source="human"`**，**不参与校准**（只解除「未显化」状态，不证明「显化有效」），补在 **L6 契约层**，**不动 `SILA` 行89 原文** | 记入 ⟨补 17⟩ 相关条目 |
| **Q6** | P1 | ✅ **是（不参与校准权重）** | 同 Q5 |
| **Q7** | P1 | ✅ **先 A 后 B**（先人工回执路径，稳定后再实现 `WorldAdapter`） | 记入 L6 契约条目 |
| **Q8** | P0 | ✅ **物态**（依 `GENE_LIBRARY_DESIGN` 行61 四档：入/出 >1.2 能量态；>1.05 气态；≥0.8 液态；<0.8 固态）。拟补条文：**固态→线性／液态→斐波那契／气态→斐波那契／能量态→指数** | 记入 ⟨补 7⟩（由「待裁定」→「已裁定」） |
| **Q9** | P1 | ✅ **确认排除「四行驱动」**（违反 `PERTURBATION_MODEL` 行63「行属性**不改变内核数学**」） | 记入 ⟨补 7⟩ 排除说明 |
| **Q10** | P1 | ⏳ **先跑 V2 实测**（阈值 0.8／1.05／1.2 两侧各取 5 个采样点），拿到跳变幅度再回禀。**在此之前不上线** | 本文件 §10.7 |
| **Q11** | P2 | ✅ **入**（三引擎补齐后入 CI），但**须等 Q8 裁定生效 ＋ Q10 的 V2 实测完成** | 待 Q10 后执行 |
| **Q4** | P2 | ✅ **写**（方案乙「基线独立前置」作为**中长期目标**写入；依据 `L7_EXECUTION_DESIGN` §1.2 行65 已在交换本底场引用） | 记入 ⟨补 13⟩ 附注 |
| **Q12** | P0 | ⏳ **暂不裁定，先取证**（见本轮报告 §五 5.2） | — |
| **Q13** | P0 | ⏳ **暂不裁定，先取证**（见本轮报告 §五 5.3） | — |
| **Q14** | P1 | ⏳ **暂不裁定，先取证**（见本轮报告 §五 5.4） | — |
| **Q15/Q16** | P1 | ✅ **已执行**（见 §10.2／§10.4） | — |

> **Q3 的口径变更留痕**：清单原建议 **N=5**（与 `stall_limit=5` 同量级，属**类比**）；发起人裁定 **N=7**，
> 依据＝**七维场域**（每一维至少一个样本）⇒ **从「类比得出」改为「结构上天然得出」**，举证强度提高。

### 10.7 裁定 6 · Q10 的 V2 实测（待回禀）

- **状态**：⏳ 本轮已跑（见本轮报告 §五 5.5），**待发起人据跳变幅度裁定**（加迟滞／加插值／阈值处不切换／其他）。
- **在裁定前**：三引擎在物态阈值处的**切换规则不上线**（避免输出跳变）。

### 10.8 机制 23 建立（本轮）

| 落点 | 内容 |
|---|---|
| `CHARTER.md` | 机制表新增 **机制 23（夜间时段例外机制）**；机制关系说明补 23 |
| `CONSTRAINTS.md` | **C14 补「★ 例外条款」**：夜间时段约束**仅适用于无人监督的自动化任务**；**用户在场指令会话不受约束**；含**判据表**、**记录义务**、**防滥用（无法区分⇒默认从严）**、**不豁免清单（C10／C12／push 须当轮明示）** |
| `TEMPLATES.md` | 新增 **§11.8-bis**（条文＋三条区分判据＋记录义务＋防滥用＋边界＋首次判例）；**T-028 登记** |

---

## 十一、2026-09-18 裁定记录（第二轮 · 7 项裁定 ＋ 片6 执行 ＋ 汇编重做启动）

> **来源**：发起人 2026-09-18 指令（基准 v0.199）｜**性质**：裁定落地记录（**非**新提案）
> **落地原则**：裁定结果一律记入本文件；原始底图 `docs/` **只在裁定明确指向时才改**，改则同步更新底图哈希基线（判据 F）。

### 11.0 七项裁定一览

| # | 裁定 | 结果 | 主要落点 |
|---|---|---|---|
| 1 | **Q10 前提 · 输出口径** | ✅ **选单步口径**（与 `linear.rs` 单测 `step(0.5)==0.51` 一致） | `docs/MATH_SPEC.md` **新增 §5.1**（v1.1 → **v1.2**） |
| 1b | **Q10 前提 · 映射规则** | ⏳ **待裁定**：已出候选**甲（分段线性）／乙（对数压缩）／丙（物态只选引擎）**，各含七要素；**本轮推荐丙** | `docs/MATH_SPEC.md` **§5.2** |
| 2 | **Q12 端点表权威** | ✅ 选 **`LAYER_ARCHITECTURE.md` §4.L3** 为**全库唯一权威**；**23 个路径**逐行列全（方法／用途／层归属）；其余 **6 处**改为指向权威 | 7 份文档（见 §11.4） |
| 3 | **Q13 层归属** | ✅ 裁定 **P-b**：消息／报告／告警／任务族**另有层归属、借道 L3 传输**；新增 **§4.L3.1** 区分「L3 本体」／「借道 L3」 | `docs/LAYER_ARCHITECTURE.md` **§4.L3.1**；**T-030** |
| 4 | **Q14 建 `check_endpoint_table.py`** | ✅ 已建并**入 CI**（`test` job）；**机制 24** 登记 | 脚本 ＋ 白名单 ＋ `CHARTER.md` 机制 24 ＋ **T-029** ＋ `ci.yml` |
| 5 | **D41 片6 放行** | ✅ **方式 A**：`HashMap` → `BTreeMap` ＋ **修订 3 行 `O(1)` 注释**；片6 已执行，**片7／片8 连带解冻** | 4 个迁移模块 ＋ `l7.rs`／`lib.rs` ＋ `verify.rs` 第 ⑨ 段 ＋ 保真度白名单 |
| 6 | **R61 口径统一** | ✅ 统一为 **7 份**（与 CHARTER 机制 22 及判据 D 实现一致）；docstring ＋ `CORE` ＋ **自检夹具** ＋ `ci.yml` 注释（**第三处**） | `check_source_of_truth.py`、`ci.yml` |
| 7 | **R60 加机器提醒** | ✅ CI 机制 22 **之后**新增一步「R60 前置提示」（**只提示、不判红**） | `.github/workflows/ci.yml` `test` job |

### 11.1 裁定 5 · D41 片6 执行细节（**可追溯项**）

| 项 | 实测 |
|---|---|
| **迁移模块（4 个）** | `l1_mapping.rs`（层1）／`l5_diagnosis.rs`／`positive_source.rs`／`l7/repair.rs` |
| **容器替换** | `std::collections::HashMap` → `alloc::collections::BTreeMap`（1 处类型 ＋ 1 处 `use`） |
| **成立前提（片6 论证＋独立复核）** | ① `twin_index` **从不迭代**（方法集恰为 `{insert, get}`）；② `PositiveSource` derive = `Debug, Clone, Default`，**无 `PartialEq`/`Eq`/`Hash`** ⇒ 遍历序**不可观测** |
| **★ 3 行注释修订（显式登记）** | `positive_source.rs` 源行 **35／337／409** 的 `O(1)` → **`O(log n)`**（`BTreeMap::get` 为 `O(log n)`；`HashMap` 才是均摊 `O(1)` ⇒ 换容器后原叙述**不实**）。**std 侧 `meta-kernel-core` 保持 `HashMap` 不变 ⇒ 那边 `O(1)` 依然为真，不改源文件。** |
| **保真度判据** | **多重集差**，四份**全部通过**；上述改文字已登记 `check_migration_fidelity.py::ALLOWED_SUBS` |
| **闭包判据** | **非测试依赖 ＋ 测试依赖同时封闭** ⇒ PASS（目标 crate 模块 **50 → 54**） |
| **裸机断言** | **扩到第 ⑨ 段**（`meta-kernel-boot/kernel/src/verify.rs`） |
| **裸机构建** | ✅ 通过（`--target x86_64-unknown-none`） |
| **nostd 单测** | ✅ **390 passed / 0 failed**（331 → 390，**+59**） |
| **⑨ 段 host 镜像** | `meta-kernel-core-nostd/tests/mirror_bare_assertions.rs` **5/5 全过** |
| ⚠️ **边界** | 本机**无 `dlltool`／`link.exe`／`clang`** ⇒ **boot 层无法本地构建** ⇒ 采用既有做法：**把 ⑨ 段镜像成 host 测试**（本地可跑），**boot 层真实引导由 CI 的 boot-image job 保证** |
| **解冻** | 片6 完成后，**片7／片8 连带解冻** |

### 11.2 裁定 4 · 机制 24（端点表一致性）设计要点

- **范围**：`npb-gateway/src/http.rs` **非测试区**（`#[cfg(test)]` 之前）—— 与机制 21 同口径。
- **比对口径**：**路径口径**（不含 method）—— R35。
- **判据**：**代码有、权威表无** ⇒ **判红**（新端点必须先写权威表；确属内部／调试须登记 `endpoint_whitelist.txt`，**只减不增**）；
  **权威表有、代码无** ⇒ **只提示**（可能是"已设计未实现"或"已下线"，但表里须如实标注状态）。
- **判据设计模式**（同机制 21／22）：**正反对照** ＋ **自检** ＋ **基线只减不增** ＋ **口径写清**。
- **自检**：**五侧**（合规／代码多一个／表多一个／仅测试／**真实仓库锚点**）。
- ⚠️ **第 ⑤ 侧的来历（本轮自曝缺陷，普适教训）**：脚本初版**锚点常量多写一个字**（写作 `§4.L3 对端点表`），而**自检夹具与常量同错**
  ⇒ **自检全绿、实跑判红**。⇒ 据此新增第 ⑤ 侧「**真实仓库锚点必须解析到路径（>0）**」，**禁止只测自造夹具**。
  **教训**：**自检与实跑的口径必须同源**；夹具不能与常量共用同一处人工写法。
- **实跑结果**：权威 **23** ＝ 代码 **23**，差集 **0** ⇒ ✅ 一致。

### 11.3 裁定 1 · Q10 前提的两项

- **已定**：输出口径 ＝ **单步口径**（`docs/MATH_SPEC.md` §5.1）。
- **待裁定**：映射规则 —— 候选**甲／乙／丙**（§5.2）；**本轮推荐丙**（物态只选引擎、不做数值映射，**不动 A1**）。
- **阻塞根因（须向发起人报告）**：物态阈值 `1.2／1.05／0.8` 判的是「**入/出比**」（域 `(0,+∞)`），而引擎输入是 **`[0,1]`** 种子 ⇒ **量纲不同，阈值无法直接施加**。
- **V2 实测（口径敏感性）**：`0.80` 处 单步 **0.010** ／ 稳态 **0.260**（**26×**）；`1.20` 处 单步 **0.500** ／ 稳态 **0.167**（**3×**）；`1.05` 处两口径均 **0**（同引擎，不切换）。

### 11.4 裁定 2／3 · 底图变更清单（触发判据 F）

**本轮改了 `docs/` 下 7 份** ⇒ **判据 F 必判红** ⇒ 提交前**必须**跑 `--update-baseline`：

| # | 文件 | 改法 |
|---|---|---|
| 1 | `docs/LAYER_ARCHITECTURE.md` | **§4.L3 收敛为唯一权威端点表（23 路径）** ＋ **新增 §4.L3.1 层归属** |
| 2 | `docs/SELF_DIAGNOSIS_REPORT.md` | 端点口径 → **指向权威表**（保留原文 ＋ 非权威标记） |
| 3 | `docs/API_GATEWAY_DESIGN.md` | 同上 |
| 4 | `docs/FULL_AUDIT_REPORT.md` | 同上 |
| 5 | `docs/GENE_LIBRARY_DESIGN.md` | 同上 |
| 6 | `docs/L7_EXECUTION_DESIGN.md` | 同上 |
| 7 | `docs/MATH_SPEC.md` | **新增 §5**（Q10 前提两项）；v1.1 → **v1.2** |
| ＋ | `README.md` | 仓库结构行端点口径 → **指向权威表**（**不在基线内**，不触发判据 F） |

**改动理由（`--update-baseline` 必填项）**：**Q12／Q13 底图条文补充**（端点表权威收敛 ＋ 层归属条文）＋ **Q10 前提新增 §5**。

### 11.5 裁定 6 · R61 统一为 7 份

- `check_source_of_truth.py` docstring「核心底图集（6 份）」→ **7 份**；`CORE` 列表补第 7 份 `LAYER_BASEMAP_L0_L6.md`；**自检夹具**同步补第 7 份名；
  `.github/workflows/ci.yml` 机制 22 注释「6 份」→ **7 份**（**第三处口径，本轮一并修**）。
- **新增阴性对照**：夹具**抽掉第 7 份** ⇒ 判据 D **判红**（证明该条款**真在检查**，不是空转）。

### 11.6 裁定 7 · R60 机器提醒

- 落点：`.github/workflows/ci.yml` `test` job，机制 22 步骤**之后**新增一步。
- **口径（R35）**：**只提示、不判红** —— 硬门禁是机制 22 的判据 F；本步只把"唯一正确处置"前置，
  并打印**本次提交触及的 `docs/` 清单** ＋ **基线是否同步更新**。

### 11.7 汇编重做（裁定 2.3）启动

- **结构稿**已出（见本轮报告 §八）；**以底图当骨架**，WorkBuddy 的机制／台账降为**附录"工程台账"**。
- **分两阶段**：**先出结构稿供发起人确认**，**确认后再填充**（本轮**不填充**）。

---

## 十二、2026-09-18 · 2.3b 分片迁移**收口**（片7／片8 执行）

> **依据**：裁定 5「片6 解冻后**片7／片8 连带解冻**」｜**性质**：里程碑记录（**非**新裁定）

| 项 | 实测 |
|---|---|
| **片7** | `l5_translate`（L5 编译翻译）／`l6_face`（L6 脸切换）——**零「替换类」** |
| **片8** | `l5_router`（L5 零依赖 JSON 路由）——**零「替换类」**（末片） |
| **清单口径** | `check_migration_closure.py` 的 `--emit 1／2` 产出（**D40：不手写**） |
| **保真度** | **多重集差 3/3 通过**：替换 **0**／丢失 **0**／未登记新增 **0** ⇒ **纯插入**（仅补 `alloc` 的 `use` ＋ 迁移说明块） |
| **闭包判据** | **PASS**；源 crate **55** ｜ 目标 crate **57** 模块；**非测试 ＋ 测试依赖双侧封闭** |
| **裸机编译** | **0 error**（权威判据；仅 2 个既有 warning，非本片引入） |
| **单测** | lib **407 passed**（390 → 407）｜host 镜像 **22 passed**（17 → 22） |
| **裸机断言** | `verify.rs` **第 ⑩ 段**（码 101–105，CI 读作 201–205）＋ host 镜像 5 条 |
| **收口信号** | `--emit 1` ⇒ 「**层号越界（共 0 层）**」⇒ **2.3b 无剩余片** |

### 12.1 R69（本轮新增）

- `meta-kernel-core-nostd/src/lib.rs` 模块文档**两处不实叙述**（**已订正**）：
  1. 行 11 写「**不使用 `alloc`**（本层不出现 `Vec`/`String`）」—— 与同文件下方「`alloc` 的地位（D34-b）」**自相矛盾**，且与本层实测**大量使用 `alloc`** 不符；
  2. 「**仍不迁**：`l5_*`（baseline/quad/evidence）、`fourier`／`interference`／`energy`、`l7::{ledger,mesh,repair}`、`gene_library`、`ontology`、`sanitizer`、`state`、`trace`、`habit`、`dna_*`、`executor` 等」—— 该列表**已被后续片全部迁完**（当时为真、**现已过期**）。
- **教训**：**「迁移进度」这类状态型文档必须绑定机器判据**，否则会**静默过期**，且**没有任何判据在检查它** —— 属 **R62（判据盲区）** 的**文档侧同族**。
- **处置**：整段替换为**机器判据实测口径**（源 55／目标 57／封闭性 PASS）＋ 修订说明（注明"当时的真实、现在的过期"）。

---

## 十三、2026-09-18 裁定记录（第三轮 · Q10 两项裁定 ＋ R64 固化 ＋ 夜间 P3 复核 ＋ push 放行）

> **来源**：发起人 2026-09-18 指令（基准 v0.207）｜**性质**：裁定落地记录（**非**新提案）

### 13.1 裁定 · Q10 前问：**物态 ＝ 选择器**（不是调制量）

- **裁定（逐字）**：**物态＝选择器**（不是调制量）。
- **理由（逐字）**：若为选择器，物态只决定"用哪个引擎"，不改变输入值；问题收敛为单变量。
- **落地**：`docs/MATH_SPEC.md` **§5.2.0**（文件 v1.2 → **v1.3**）。

### 13.2 裁定 · Q10 映射：**选丙**（阈值处不切换）

- **裁定（逐字）**：**选丙**（阈值处不切换）。
- **理由（逐字）**：不动 A1，问题收敛为单变量，成本最低。
- **落地口径（把「阈值处不切换」写清，避免歧义）**：**不引入映射 `g: r → x`**；物态（由「入/出比」分档）**只选择引擎**；
  **引擎输入 ＝ 本次已有的 `[0,1]` 种子**（A1 **原样**）⇒ **除「引擎切换」这一处固有切换外，阈值处不再产生任何额外的数值跳变**。
  ⚠️ **不得**把"阈值处不切换"读成"到了阈值不换引擎"——**Q8 的「物态 → 引擎」映射仍然成立**。
- **落地**：`docs/MATH_SPEC.md` **§5.2.1**；候选**甲／乙保留为历史留痕**（§5.2.2，注明"未采用"）。
- **附带结论**：**Q11（三引擎补齐后入 CI）** 的前置条件（Q8 生效 ＋ Q10 完成）**已满足**，可排期。

### 13.3 裁定 · R64 固化

- **裁定**：**固化**。
- **新增硬约束**：**C18**（`coordination/CONSTRAINTS.md`）——「**判据自检须与实跑同源，且必须回读真实仓库**」。
  **三条要求**：① **同源**（自检夹具必须复用被测实现的同一常量／同一解析路径，**禁止手写第二份**）；
  ② **回读真实仓库**（至少一侧读真实文件／目录，**禁止只测自造夹具**）；
  ③ **断言"真实仓库可解析"**（形如「解析到 >0 个条目」，且「0」与「解析失败」**必须可区分**）。
- **编号台账**：**T-031**（`coordination/TEMPLATES.md`）登记 C18。
- **来历（R64）**：`check_endpoint_table.py` 的锚点常量多写一字，**夹具与常量同错** ⇒ **自检全绿、实跑判红**。
- **已按此执行**：`check_endpoint_table.py` 补**第 ⑤ 侧**（真实仓库锚点 ⇒ 23 路径）；`check_private_pointers.py` 补**第 ⑪ 侧**（格式错 0｜解析引用 49）。
- **C15 复算**：机制 **1–24**｜C **1–18**｜T **1–31**，**无缺号、无重号**；对照（阳性＝表内最大号／阴性＝最大号+1）**6/6 PASS**。

### 13.4 夜间 P3 三个 commit 复核（本机独立复核，**非**转述）

| commit | 复核项 | 复核结果 |
|---|---|---|
| `c18509d` P3-1 | 指针判据 `--selftest` | ✅ **十一侧 / 12/12 通过、exit 0**（**声称实测复现**） |
| | 死分支修复 | ✅ 已改到**未被清洗的原串 `raw`**（`check_private_pointers.py` 行 175–177：`if re.match(r"^[A-Za-z]:", raw) or clean.startswith("..")`） |
| `2086782` P3-2 | 6 判据自检 | ✅ **6/6 exit=0**（**声称实测复现**） |
| `93f69c9` P3-3 | 收口信号 | ✅ `--emit 1` ⇒「层号越界（共 0 层）」 |
| | 4 处过期事实点 | ✅ **属实**（**已于 v0.210 修订**，见 §14.1）：`coordination/ROADMAP.md` 行 59 与 `coordination/BASELINE.md` 行 154 各含 2 处（**原文**为「剩余片6–片8」／「裸机断言已到第 ⑧ 段」） |
| | 内部自相矛盾 | ✅ **属实**（**已于 v0.210 随修订消解**）：**修订前** `BASELINE.md` 行 154 写「第 ⑧ 段」而行 426 写「第 ⑩ 段」⇒ **R69 家族第 2 个实例**；**修订后两处口径一致**（均为 ⑩） |

> **结论**：三 commit **无异常**。（原登记为「⚠️ 4 处过期点尚未修」—— **该状态已于 v0.210 变更**：经发起人授权后修订完成，见 §14.1。）

### 13.5 push 放行与 CI

- **已 push 4 个 commit**：`c18509d`／`2086782`／`93f69c9`（夜间 P3）＋ `bf633a6`（v0.207）；`origin/main` 由 `a389e18` 推进到 `bf633a6`。
- **CI**：run **`35366013117`** ⇒ **三 job 全绿**（test 1m59s／boot-image 2m10s／host-windows 6m47s），**`##[error]` ＝ 0**。

### 13.6 待办（本轮未授权，需发起人授权后再动）

1. ~~**4 处过期事实点**（`ROADMAP.md` 行 59／`BASELINE.md` 行 154）~~ ⇒ ✅ **已处置（v0.210）**：按 P3-3 的**排序建议**执行完毕 —— ① 先更新治理文件（**过 C15 双向计数 ✅**）；② 再建判据 `coordination/tools/check_doc_consistency.py`（覆盖「**文档-机器一致性**」＋「**文档内部一致性**」）。详见 **§14.1／§14.2**。
2. **两个迁移判据是否入 CI**（`check_migration_closure`／`check_migration_fidelity` —— 现 6 判据中**仅 4 个在 CI**）。

---

## 十四、2026-09-19 · 4 处过期点修订 ＋ 机制 25 ＋ 汇编第 1 批复核／第 2 批 ＋ Q11 契约稿

> **来源**：发起人 2026-09-19 指令（基准 **v0.209**）｜**性质**：裁定落地 ＋ 推进
> **授权范围**：4 处过期事实点修订（§一）／汇编第 2 批（§三）／Q11 契约稿（§四）／`advisor_brief` 与 `ROADMAP` 更新（§五）

### 14.1 R72 处置 · 4 处过期事实点修订（**已授权、已执行**）

**执行顺序**（按 P3-3 的排序建议，避免"先建判据 ⇒ 常亮红灯"）：
**① 先改治理文件 → ② 过 C15 双向计数 → ③ 再建判据（机制 25）**。

**逐字前后对照**：

| # | 位置 | **修订前**（逐字） | **修订后**（逐字） |
|---|---|---|---|
| **1** | `coordination/ROADMAP.md` 行 59 | **修订前**：`**2.3b（alloc 迁移）🚧 片1 ✅｜片2 ✅｜片3 ✅｜片4 ✅｜片5 ✅**（…）。**剩余片6–片8 = 7 模块 / 2,887 行**（脚本口径）。⚠️ 片6 已探到 std::collections::HashMap（**类型替换类，须单独论证**，同 D37 口径）。**裸机断言已到第 ⑧ 段**（三态判定／动作账链篡改必拒／沙漏每 tick 至多 1 粒）。**可夜间跑 ✅**` | `**2.3b（alloc 迁移）✅ 已收口（片1–片8 全部完成）**（…；片6–片8 收口实测见 coordination/BASELINE.md §十二）。⚠️ 片6 遇到的 std::collections::HashMap **已按「方式 A」换 BTreeMap**（…**逐位等价论证 ＋ 保真度多重集差均已通过**）。**裸机断言已到第 ⑩ 段**（⑧＝…；⑨＝片6 四模块；⑩＝片7–片8 三模块）。**可夜间跑 ✅**` |
| **2** | `coordination/BASELINE.md` 行 154 | **修订前**：`**裸机断言扩到第 ⑧ 段**（51–55：三态判定…／动作账链…／沙漏…）⇒ 一条绿屏同时证成 **2.1＋片1–片5＋alloc**。` | `**裸机断言扩到第 ⑩ 段**（⑧＝51–55 …；⑨＝片6 四模块；⑩＝片7–片8 三模块）⇒ 一条绿屏同时证成 **2.1＋片1–片8＋alloc**。` |
| **3** | `coordination/BASELINE.md` 行 154 | **修订前**：`**剩余 片6–片8**（脚本口径 7 模块 / 2,887 行：…）—— ⚠️ 片6 已探到 **std::collections::HashMap**（属「类型替换」类，须单独论证） |` | `**片6–片8 ✅ 已收口**（脚本口径 7 模块：…）—— 片6 的 **std::collections::HashMap** 已按「方式 A」换 BTreeMap（…**已单独论证**）；收口实测见本文件 **§十二** |` |
| **4** | 同第 2／3 处（**行 154 为同一行，含 2 个独立事实**） | —— | —— |

> **口径（R35）**（**引述** P3-3 原登记）：P3-3 登记的"**4 处**"＝ **2 行 × 2 个事实**（`ROADMAP:59` 与 `BASELINE:154` 各含"剩余片6–片8"＋"第 ⑧ 段"）；
> 故上表 #1／#2／#3 覆盖全部 4 个事实点（#4 与 #2／#3 同位置，不重复计）。

**C15 双向计数**：
- **改前**：机制 **1–24**｜C **1–18**｜T **1–31**（缺号 无／重号 无／连续 是）
- **改后（不含机制 25 登记）**：**同上，差值 0** —— 本轮 4 处修订**未触碰任何编号表**（差值合预期 ✅）
- **改后（含机制 25 登记）**：机制 **1–25**｜C **1–18**｜T **1–32**（见 §14.3）

### 14.2 新发现：`advisor_brief.md` 2 处过期（P3-3 **未登记**的第 3、4 处）

- **发现方式**：**由本轮新建的机制 25 判据首次实跑抓出**（不是人工翻出来的）。
- **位置与处置**：

| 位置 | 问题 | 处置 |
|---|---|---|
| `coordination/advisor_brief.md` 行 107 | 段号落后 **2 段**（写"到第 ⑧ 段"，机器实测 ⑩） | 加时间限定 ⇒ `**（片5 时）裸机断言到第 ⑧ 段**`（**保留历史精度**，不冒充当前状态） |
| `coordination/advisor_brief.md` 行 111 | **引述**其当时仍称"**剩余片6**"（机器已收口） | 改为"片6 已按「方式 A」换 `BTreeMap`"，并**新增一条**「**★★ 2.3b ✅ 已收口**」条目（含第 ⑩ 段） |

- **教训（并入 R72 家族）**：P3-3 只普查了 `ROADMAP`／`BASELINE` **两份文件**，**漏了 `advisor_brief.md`** ——
  说明"**人工普查"必然有漏**，**这正是需要机器判据的理由**。

### 14.3 机制 25 建立（文档一致性）＋ 机器判据

| 落点 | 内容 |
|---|---|
| `CHARTER.md` | 机制表新增 **机制 25（文档一致性机制）**；机制关系说明补 25 |
| `TEMPLATES.md` | **T-032** 登记 |
| **新建判据** | `coordination/tools/check_doc_consistency.py`（**自带六侧自检**） |

**三条判据（逐条可证伪）**：
- **P1 段号一致性**（文档 ↔ 机器）：**逐文件**取"活跃段号陈述"的**最大值**，须 == `verify.rs` 实测最大段号。
- **P2 收口一致性**（文档 ↔ 机器）：机器已收口时，文档不得仍有**活跃**"剩余 N 片"陈述。
- **P3 文档内部一致性**（同文件自洽）：同一文件不得既称"已收口"又称"剩余 N 片"。

**防误伤的两条设计（关键）**：
1. **"逐文件取最大值"而非"逐条比对"** —— 因为段号是**单调递增的历史叙事**（文档会记录"扩到 ⑥→⑧→⑩"的过程）；**落后的是"最大值"**。
2. **历史留痕豁免**：含 **`修订前／原为／原文／已处置／已修订／订正／引述／当时／历史／~~`** 的行**不参与判断** ——
   否则判据会把**它自己的登记表**判红（见 R73）。

**扫描范围**：`coordination/*.md` ＋ `README.md` ＋ `docs/*.md`；**排除** `reports/`／`discussions/`（当轮快照，天然落后）。

**首次实战结果**：建立后**立即**抓出 `advisor_brief.md` **2 处真实过期点**（§14.2）⇒ **判据有效，非空转**。

### 14.4 汇编正文第 1 批复核 ＋ 第 2 批启动

**第 1 批复核**（`coordination/discussions/2026-09-18_汇编正文_第1批_设计定位层.md`）：

| 项 | 实测 |
|---|---|
| 规模 | **1838 行 / 157,209 字节** |
| 9 份清单 | `VISION`(74)／`CONTRAST_REPORT`(50)／`COSMIC_COMPUTING`(142)／`ONTOLOGY_SPEC`(88)／`LAYER_ARCHITECTURE`(449)／`LAYER_BASEMAP_L0_L6`(685)／`MATH_SPEC`(150)／`PERTURBATION_MODEL`(79)／`L5_TRANSLATION_TABLE`(64) |
| **逐字保真** | ✅ **9/9 逐字一致**（回读真实文件比对） |
| **行号前缀** | ✅ **1..N 连续正确**（**本项为上轮未覆盖、本轮补验** —— 见 R74） |
| **R71** | ✅ 通过（**我写的部分 0 处派生值／0 处本机信息**） |

**第 2 批启动**（`coordination/discussions/2026-09-18_汇编正文_第2批_层设计与机制.md`）：

| 项 | 实测 |
|---|---|
| 规模 | **1447 行 / 104,195 字节** |
| 9 份清单 | `L5_TRANSLATION_TABLE`(64)／`L5_DESIGN`(191)／`L6_DESIGN`(110)／`FIELD_PRESENTATION_DESIGN`(183)／`L7_EXECUTION_DESIGN`(305)／`SILA_IMPLEMENTATION`(119)／`FIELD_REPAIR_DESIGN`(168)／`FIELD_RENDER_SOURCELESS_DESIGN`(132)／`WORK_CONSOLE_PLAN`(113) |
| 落位 | **第三篇 3.3（补录）＋ 第五篇（5.2–5.5）＋ 第七篇（7.3／7.4）** |
| **逐字保真** | ✅ **9/9 逐字一致 ＋ 行号前缀 1..N** |
| **R71** | ✅ 通过 |

⚠️ **重复收录（不掩盖）**：`docs/L5_TRANSLATION_TABLE.md` **已在第 1 批 3.3 收录**，而发起人第 2 批清单仍含此份
⇒ **按指令收录，并在文件内扉页显式标注**；**是否归并为一份待发起人裁定**（**未做静默去重**）。

### 14.5 Q11 可测契约稿（只出稿，**不落地**）

- **文件**：`coordination/discussions/2026-09-18_Q11可测契约稿_引擎选择表.md`
- **核心结论**：**Q8 的四档映射到三引擎后，`1.05` 处"物态切换、引擎不切换"** ⇒ **实际只有 2 个引擎切换点（0.8 与 1.2）**，不是 3 个。
  **与既有 V2 实测吻合**（`1.05` 处单步／稳态差值均 `0.000000`、比值 **1×**）。
- **判据**：**D1–D6**（区间中点／边界点（严格不等式）／不切换性／不调制性／域护栏／**正反对照**）。
- **前置未定项（须先裁定）**：
  - **U1** 物态取 **`state_of_flow_ratio`（比值态）** 还是 **`state_of_energy_budget`（比值⊕预算，取更固者）**？—— **代码里两者都在**。
  - **U2** 输出 = 枚举标签／执行结果／两者？
  - **U3** **`state_pace(s) -> (bias, gain)`（物态 → 调度调制）是否与 Q10 前问「物态＝选择器」冲突？** —— **不冲突但须写明作用域**（Q10 前问管的是"是否调制**引擎输入**"；`state_pace` 调制的是**调度层**）。
- **本稿引用核对**：**8/8 符号位置回读真实仓库核对通过**（含修正后的 154／111 —— 见 R75）。

### 14.6 本轮登记的 R（三条）

| # | 内容 | 来历 |
|---|---|---|
| **R73** | **"判据 ↔ 判据的登记"之间的张力**（**两个子实例，均已实测**）：<br>**（a）引述型局限**：机制 25 判据**把自己刚登记的 T-032 判红** —— 因 T-032 **引述**了"剩余片6"这个被修订的原文。<br>**（b）词表与用词不一致**：本文件 §14.1 对照表用「**改前**」，而判据词表只列「**修订前**」⇒ **5 条假阳性**<br>（含 §14.1 三条数据行 ＋ §14.2 一条 ＋ 一条 P3；且**列头标记不覆盖数据行** ⇒ 数据行须**逐行**标记）。<br>**处置（双侧，非单侧放宽）**：① 书写侧 —— 对照表统一为「**修订前**／**修订后**」，**数据行逐行**加显式前缀，引述行加「**引述**」；② 判据侧 —— `HISTORY_MARKS` **补同义词「改前」**（**"修订前"的同义表达，语义上确属历史留痕**，属**规则完善**而非放宽）。<br>**教训**：**判据与"判据的登记"之间有天然张力** —— 登记文本必须**按判据口径书写**；且**同义表达的覆盖度**直接决定判据会不会"误伤自己的记录"。 | 本轮实测 |
| **R74** | **复核脚本也须与实跑同源**（**C18 第三次实例**）：复核第 1 批时我自己的脚本未剥 `NNNN│` 前缀 ⇒ **假报 9/9 ❌**（实为 ✅）。**教训**：C18 说的"**自检**须与实跑同源"，同样适用于"**复核脚本**"。 | 本轮实测 |
| **R75** | **行号引用会漂移**：Q11 契约稿两处代码行号我**数错**（`state_of_energy_budget` 153→**154**、`state_pace` 108→**111**），由**回读核对**抓出。**教训**：**引用行号后必须回读验证**；**定位以符号名为准**，行号只作辅助。 | 本轮实测 |

### 14.7 发现但**未处置**（未授权，登记待裁）—— R72 家族第 3 例

`coordination/BASELINE.md` **行 153**（"no_std 子集"行）仍为**旧口径**：

| 项 | 文档写的 | 机器实测 |
|---|---|---|
| nostd 测试数 | **331 项全绿** | **407 passed** |
| host 镜像测试 | **12 项** | **22 passed** |
| 已迁文件数 | **10 个已迁文件** | 目标 crate **57 模块** |

⇒ **本轮未授权修改该行**（指令 §一 只给 `ROADMAP:59` 与 `BASELINE:154`）⇒ **仅登记**，**请发起人授权后处置**。

> ⚠️ **注**：机制 25 判据**目前也不会抓这一处** —— 因为 P1/P2/P3 覆盖的是"段号"与"收口"两类事实，
> **不覆盖"测试计数"这类数值型过期**。**这是判据当前的已知盲区**（登记在下轮改进方向）。
