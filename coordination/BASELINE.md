# 云内核项目 · 基准

最后更新于：HEAD bbcdd38（对应 v0.126）
更新时间：2026-09-16

> **口径说明**：上面一行记录的是「**本文件最后一次更新时所处的 HEAD / 版本**」，**不是「当前版本」**。
> 本文件的内容（指标、状态）反映的是那次更新时的事实快照，会随提交而自然滞后。
> 「当前版本」请一律以 `git rev-list --count HEAD` 为准（本项目版本号自动生成、不手写，见 CONSTRAINTS.md C2）。

## 〇、项目根（唯一权威）

| 项 | 值 |
|---|---|
| **唯一权威项目根** | `C:/Users/香忆/WorkBuddy/研发开发/tmp_a45_review/gh_clone2` |
| 版本号来源 | 上述仓库的 `git rev-list --count HEAD` |
| 其它同名/相似目录 | `研发开发/` 本身**不是** git 仓库；`研发开发/meta-kernel/` 是**陈旧分叉**（停在早期版本，74 处陈旧工作副本，**只归档不合并**，见 `reports/2026-09-16_meta-kernel旧分叉摘要.md`） |

> **判据**：任何"当前版本/提交/改动"的统计，一律以上表仓库为准；发现别处也有 `meta-kernel-core/` 时，**默认它是陈旧副本**。

## 一、项目定位

在现有操作系统之上生长的"意识补充层"，不替代、不淘汰。
让每个设备、每个系统、每个界面都获得"生命感"。

## 二、命名体系

| 版本 | 系统名 | 内核层 | 操作系统层 | 浏览器层 | 大模型 |
|---|---|---|---|---|---|
| 电信号 | 元内核系统 | 基因内核 | 云操作系统 | 空天浏览器 | 云海大模型 |
| 量子 | 量子云系统 | 量子钟 | 正源操作系统 | 正源浏览器 | 采微大模型 |

输入法：彩虹输入法（统一）

## 三、层级架构

| 层级 | 名称 | 状态 |
|---|---|---|
| L0 | 0锚点/量子真空 | ✅ |
| L1-L3 | 基因内核（含基因库、学习机制、世界模型） | ✅ |
| L4 | 拒绝层（含四元组风险戒律） | ✅ |
| L5 | 诊断层（含语境、四元组、证据、注意力） | ✅ |
| L6 | 交互层（空天浏览器） | 🚧 |
| L7 | 执行层（多内核互联+修复执行） | 🚧 设计完成 |
| L8-L10 | 传承/反哺/自明 | 🔒 冻结 |

## 四、已完成

**内核**：三引擎、气泡沙漏、镜像池、摩尼宝珠、痕迹/习气/自我、正源场域、基因库四层、学习机制、场域解析器、场域映射库、世界模型、七维向量、不非时食/不捉持、风险戒律四项、四场拆解、本底场、语境模块、亢枯平、诊断结论、多语言翻译、四元组、证据模块、注意力模块、多内核互联、动作分级、T1闭环、动作账。

**宿主**：wry降级、field-render离屏、window.rs窗口、egui UI、双pass复用、动态LOD、一致性映射、gene_store持久化。

**持久化**：基因库、痕迹、习气、四元组。

## 五、未完成

| 项 | 状态 |
|---|---|
| 宿主真实运行验证 | ✅ **已达成**（2026-09-16 本机真实开窗 + 输入 URL + 取源码 + 上屏回读 PASS；见 `reports/2026-09-16_R1宿主真实运行调研.md`） |
| 老笔记本实测 | ⏳ 待用户 |
| L5诊断端到端（宿主运行期字段级） | ⏳ 部分 |
| 推送SNI摩擦根因 | ❌ 未定位 |
| meta-kernel/ 陈旧改动 | ⏳ **摘要已出**（74 处经哈希证明无独有价值），待决策 |
| **CI 覆盖宿主** | ❌ **未覆盖**（CI 仅 ubuntu job，从不构建 field-render —— 见 R6） |
| `--diag-check` 子项① | ❌ FAIL（断言与中性点 0.5 不符，已定位，待修） |
| `--quad-check` 喜欢维 | ⚠️ PARTIAL（变化 0.5%，已知待加强） |
| L6感知、L7执行器 | ⏳ P2 |
| 覆盖率、知识库 | ⏳ P2 |

## 六、已知漏洞/风险

| 编号 | 漏洞 | 状态 |
|---|---|---|
| R1 | 宿主从未真实端到端运行 | ✅ **已解除**（2026-09-16 本机真实运行 PASS） |
| R2 | 推送SNI摩擦反复出现 | 🟡 未定位 |
| R3 | meta-kernel/ 陈旧分叉 | 🟡 摘要已出，待决策 |
| R4 | ~~本地缺 dlltool，宿主无法本地链接~~ **实为 PATH 未含 llvm-mingw `bin`** | ✅ **已澄清并绕过**（非环境限制，是配置缺半——dlltool 一直就在） |
| R5 | 二进制banner显示CARGO_PKG_VERSION而非commit count | 🟢 待评估 |
| **R6** | **CI 从不构建宿主**（仅 `ubuntu-latest` 单 job，`field-render` 命中 0）→ 宿主验收**无自动回归** | 🔴 **关键（新增）** |
| **R7** | `--diag-check` 子项①断言与 `m=0.5` 中性点不符 → 结论 FAIL | 🟡 待修（**验收脚本缺陷**，非内核缺陷） |
| **R8** | `--quad-check` 喜欢维变化仅 0.5%（阈值 >10%） | 🟡 已知待加强（**不许在宿主层调参打绿**） |

## 七、关键指标

| 指标 | 数值 |
|---|---|
| 宿主是否真实运行 | ✅ **是**（2026-09-16，本机 Intel Arc / Vulkan，窗口 2160×1520，URL 取源码成功，回读自 surface） |
| 离屏帧率 | **209.3 FPS**（1024 splat，**debug** 构建，本轮本机实测）｜历史 release 记录 444 FPS |
| 窗口帧率 | 呈现 **59.5 FPS**（垂直同步）｜管线裸口径 **355.0 FPS**（debug，256 splat） |
| WebView2依赖 | 0处 |
| unsafe | 0次 |
| 内核测试 | 411项（lib 390 + 集成 21） |
| 本地验收模式实跑 | **11 个全跑**：9 PASS｜1 PARTIAL（`quad-check`·已知）｜1 FAIL（`diag-check`·断言缺陷） |
| CI目验 | 82项｜⚠️ **不含宿主**（CI 仅 `ubuntu-latest` 单 job，见 R6） |
| 验收模式 | 11个：`selftest`／`sortcheck`／`quad-check`／`like-check`／`lod-check`／`world-check`／`diag-check`／`link-check`／`l4-check`／`persist-check`／`ui-selftest` |

## 八、硬约束

见 CONSTRAINTS.md。摘要：
- 内核零依赖
- 版本号自动生成，不手写
- 不用unsafe取巧代码
- 改前先列清单，确认后执行
- 诚实标注未完成项
- 每次报告带三量台账

## 九、待用户决策

| 编号 | 事项 | 建议 |
|---|---|---|
| D1 | meta-kernel/ 陈旧改动如何处置 | 归档后移出，**不删** |
| D2 | 推送SNI摩擦是否专项排查 | 本轮 push 未见复发，可后置 |
| D3 | 宿主真实运行路径选择 | ✅ **已定并已执行**：PATH 加 llvm-mingw `bin` |
| **D6** | 是否修 `--diag-check` 断言（R7） | **建议修**（1 处，与 m=0.5 中性点对齐） |
| **D7** | 是否给 CI 加 **windows job** 构建宿主（R6） | **强烈建议**——否则"真实跑通"无法自动回归 |
| **D8** | `quad-check` 喜欢维（R8）排期 | P2；须在**内核映射层**改，禁止宿主调参打绿 |
| **D9** | 阶段二 unsafe 边界是否放行（建议补 C9） | 放行 + 补 C9（见阶段二报告 §5） |
