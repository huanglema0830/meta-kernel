coordination/TEMPLATES.md｜| T-045 | 2026-09-22 | 用户 | **新增条文 · 报告内版本号口径**（治 **R69／R72 家族**：数值/标注**静默过期**） | **规则逐字**＝「**① 报告内版本号 ＝ 报告「首次提交」时的 `count`**（**时点值**）；**② 后续追加订正提交 ⇒ 不改原报告版本号**，**在订正处标注「订正时的 count」**；**③ 理由**：与底图 `README.md` **§文档数值口径（数值 ＝ 时点值）** **一致**，且**避免"报告版本号随订正无限递归"**；**④ 判据：暂无**（★ **技术可行**：报告版本号 ↔ `git rev-list --count <首次提交>` 可机器比对 —— **本轮不立**）」。<br>**来历（实测）**：2026-09-21 夜间 —— 该夜报告初稿写「收尾 **312**／v0.312」（**311＋3＝314，算术笔误**）⇒ **订正提交使 count 314 → 315** ⇒ 出现「**报告写的版本号 ≠ 报告所在提交的 count**」；当时按「数值＝时点值」判为**成立**。**首例落地**：**2026-09-22**（本轮报告版本 ＝ **323**，**提交后实测复核一致**）。★ **本条的触发**：**v0.316 轮（A–G）52.F 里"讨论要不要补记 `T-045`"本身造出一条 `T-045` 假告警**（**自指污染第 5 例**）⇒ **补记后该告警消失**（**先例＝机制 28**）。 | **依据**：底图 **§文档数值口径（数值 ＝ 时点值）** ＋ **R69／R72**（**标注型内容静默过期**） | ✅ **生效**（落点 `BASELINE.md` **§五十二 · 52.F** ＋ **§五十三**） |
coordination/TEMPLATES.md｜| T-055 | 2026-09-23 | 用户 | **机制调整 · 判据套件扩充**（**11 → 14**；落地 `T-050` 规则⑤ ＋ 为 `T-051`／`T-052` 各配轻判据） | **`coordination/tools/` 新增 3 个判据脚本**（**均"先只告警、不判红"**）：**① `check_version_label.py`**（`T-050` 规则⑤ 的落地件；判**头注标名对 ＋ 同行不混写**；**时代豁免 ＝ 文件自身轮次号 ≤ 323**）／**② `check_rewrite_pipeline.py`**（`T-051` 轻判据；判**五段可追溯 ＋ 半冻检测**）／**③ `check_effect_column.py`**（`T-052` 轻判据；判**效力标记齐备 ＋ 🟢/🟡 表述一致 ＋ 依据列非空**）。<br>★ **`run_all_checks.py` 的 `CALLS` 表同步扩至 14 条**（该表**写死**且自检要求"**真实脚本集 ⊆＝ CALLS 集**"）⇒ **`T-048` 条目中"当前 11 个"的口径由此更新为 14**（**原句保留不动** · `C19`）。 | **依据**：**`F-5`（"有判定、无动作、无触发"）** —— `T-050`／`T-051`／`T-052` **三条条文均自带"判据：无"** ⇒ 若不落地，三条**都是纸面**（**v0.328 轮 A-1／A-4 用户裁定"配"**）。 | ✅ **生效**（落点 `BASELINE.md` **§六十四**） |
