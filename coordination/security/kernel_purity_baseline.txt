# 机制 21 · 内核纯度**白名单**（执行体归属判据）
#
# 用途：coordination/tools/check_kernel_purity.py 把本文件当作「已知情、允许存在」的集合——
#   * 某文件某类的命中数 **> 白名单** ⇒ **判红**（＝内核里新增了「感知/动作」，必须改设计或走用户放行）
#   * **< 白名单** ⇒ 只提示「可精简」，建议下调
#
# ⚠️ 维护纪律（C15 同口径）：**只减不增**。
#   新增一条 ⇒ 先修代码（把该能力移到宿主/WorkBuddy 侧），**不是**改这里。
#
# 格式：<crate>/<相对文件>|<类>|<条数>
# 类是 7 种之一：file_io / net / process / clock / env / thread / syscall
#
# 生成时间：2026-09-17
# 当前状态：**0 条** —— 内核三个 crate 的非测试代码区，七类「感知/动作」命中全部为 0。
#
# 说明（重要）：`env!("CARGO_PKG_VERSION")` 属**编译期宏**、不读运行环境 ⇒ **不算感知**，
#   故判据的 env 类只匹配 `env::var|std::env`，**不匹配 `env!`**。
#   （用字面词 `grep env` 会误计 1 处 —— 这是"判据必须精确"的实例。）
