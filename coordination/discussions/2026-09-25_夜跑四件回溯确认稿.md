# 夜跑四件回溯确认稿（机制 32 **追溯适用** · `T-076`）

> **本文性质**：**草稿待确认**（机制 32「夜跑」定义：下指令之后的运行，可以先做草稿，白天再来确认）。
> **立法依据**：`TEMPLATES.md §二十四 24.9`（`T-076` · 2026-09-25）—— **机制 32 立规之前**由无人监督定时任务产生的**自动 commit**（未经白天确认即改了仓库状态）⇒ **须补白天确认**；**唯一程序**＝本稿（逐件六项）。
> **产出方式**：本稿由 **用户在场指令会话**（`云内核-C3-013`）生成 —— 与四件的生成方式（23:09–23:11 无人监督）**不同源**，这正是"补确认"的意义。
> **状态**：**等待发起点头** —— **未 push**（默认不推送＝机制 19 §11.8 ＋ 当轮禁区⑥）。
> **唯一权威取值均以实跑为准**：本文所有行数／文件数来自 `git show --stat` 实测（`count=413` 时点）。

---

## 〇、四件总览（实测表）

| # | commit | 时间 | 文件数 | 行数 | 性质 |
|---|---|---|---|---|---|
| 1 | `ffae676` | 2026-09-24 **23:09** | 1（新增） | **+144** | 只读取证 → 报告归档 |
| 2 | `b30cc6a` | 2026-09-24 **23:09** | 1（新增） | **+161** | 只读取证 → 报告归档 |
| 3 | `236775f` | 2026-09-24 **23:10** | 1（新增） | **+171** | 只读取证 → 报告归档 |
| 4 | `d197acd` | 2026-09-24 **23:11** | 1（修改） | **+1 / −1** | **自引入缺陷的当场订正** |

**合计**：**4 个 commit ／ 3 份新增报告 ＋ 1 次单行订正 ／ 净 +476 行**。
**全部改动落点**：`coordination/reports/`（**均为报告，无一处代码、无一处治理件、无一处 CI 配置**）。
★ **未改动**：`CHARTER.md`／`TEMPLATES.md`／`BASELINE.md`／`CONSTRAINTS.md`／`TERMS.md`／`ROADMAP.md`／`coordination/tools/*`／`.github/workflows/*` —— **实测 0 行**（四件 `--stat` 全表核对）。

---

## 一、逐件六项

### 件 1 · `ffae676`（23:09）

| 项 | 内容 |
|---|---|
| **① 哈希 ＋ 时间** | `ffae676`｜**2026-09-24 23:09**｜标题＝「夜间 P3-1：机制19「机器侧半通道」取证 ＋ 与 D32 厘清（只读取证 · 报告归档；夜间 P3，便于回退）」 |
| **② 改了什么** | **新增 1 份报告**（`coordination/reports/2026-09-24_夜间P3-1_机制19双通道半通道取证与D32厘清.md`，**144 行**）。<br>内容＝**只读取证**：核实机制 19 §11.7「双通道」里的**机器侧（GHA cron）通道**是否真实存在；结论＝**不存在**（`.github/workflows/nightly.yml` **从未建立**），且与 `D32`（**已定"暂不创建"**）**厘清关系** ＝ **有意留空，不是漏做**。报告按**十四项模板**写（含第 0 项顾问阅读指令）。 |
| **③ 是否可逆** | **本地完全可逆**（纯新增文件 ⇒ 撤回＝删该文件，**零连带**）。<br>★ **但**：**一旦 push 即不可逆**（公开仓库，历轮自陈 **⛔ 事实**＝已推送内容撤不回，见 `R20`）；届时若要撤回，**只能用一个新的反向 commit**（不得以 rebase/cherry-pick/reset 改写历史 —— 当轮禁区⑧）。 |
| **④ 是否与 C3 链冲突** | **文件层：无冲突** —— 实测 `git log d197acd..HEAD -- <该三份夜跑报告>` ＝ **0 个 commit**（后续轮次**从未触及**这三份文件）。<br>**内容层：被后续采纳，非冲突** —— 本件结论（"半通道＝有意留空"）**已被 `C3-012` 裁定④ 正式写入治理件**（`CHARTER.md` 机制 19 行末加 `D32` 注）⇒ **事后已获正式确认**。 |
| **⑤ 补确认建议** | **接受入库**。理由：① 性质＝**只读取证、报告归档**（属 P3 白名单）；② 结论**已被后续治理裁定采纳**，事实面无争议；③ 若不入库，该结论在本仓库**没有任何事实留痕**（只剩 CHARTER 里的一句结论，丢失取证过程）。 |
| **⑥ 若拒绝如何处置** | 见 **§二「拒绝的结构性约束」** —— **不能从历史中剔除**（禁区⑧）；可行处置＝**保留本地不推**，或**后续轮次以反向 commit 调整文件内容后再一并推**。 |

### 件 2 · `b30cc6a`（23:09）

| 项 | 内容 |
|---|---|
| **① 哈希 ＋ 时间** | `b30cc6a`｜**2026-09-24 23:09**｜标题＝「夜间 P3-2：今夜 P3 清单缺件三解取证（只读取证 · 报告归档；夜间 P3，便于回退）」 |
| **② 改了什么** | **新增 1 份报告**（`coordination/reports/2026-09-24_夜间P3-2_今夜P3清单缺件三解取证.md`，**161 行**）。<br>内容＝对"21:00 今夜 P3 清单**偶发不出件**"作**三解取证**：**解①未触发**（同夜 21:05 蒸馏**正常落盘** ⇒ 排除调度器级／平台级停摆，**获侧证**）；**解②写往别处**（递归 0 命中 ⇒ **证据面内否证**）；**解③写了但未成功**（**无失败留痕 ⇒ 不反推**）。实测面＝**8 夜 3 夜缺件（37.5%）**。 |
| **③ 是否可逆** | 同件 1 —— **本地完全可逆**（纯新增）；**push 后不可逆**，撤回只能走反向 commit。 |
| **④ 是否与 C3 链冲突** | **文件层无冲突**（**0 个**后续 commit 触及）。<br>**内容层：已上升为正式登记** —— 本件证据是 `C3-012` 裁定③ 登记 `A-003`（`known_accepted.md`「夜跑缺件偶发 · 已裁定接受」）的**唯一依据** ⇒ 若不入库，**`A-003` 将失去证据来源**。 |
| **⑤ 补确认建议** | **条件接受 —— 接受入库，条件＝其结论（`A-003`）继续有效**。理由：该结论**已被用户裁定接受**（`C3-012` §三③），本体无争议；且 `A-003` 的"三解结清"论证**只存在于本件**。 |
| **⑥ 若拒绝如何处置** | 同件 1（§二）。★ **附带的诚实提示**：若本件被拒 ⇒ `A-003` 的**依据列会悬空** ⇒ 须同时重议 `A-003`（属连带影响，本轮不处置）。 |

### 件 3 · `236775f`（23:10）

| 项 | 内容 |
|---|---|
| **① 哈希 ＋ 时间** | `236775f`｜**2026-09-24 23:10**｜标题＝「夜间 P3-3：清单来源口径取证 ＋ ROADMAP 回退实测 0 行（只读取证 · 报告归档；夜间 P3，便于回退）」 |
| **② 改了什么** | **新增 1 份报告**（`coordination/reports/2026-09-24_夜间P3-3_清单来源口径取证与ROADMAP回退实测.md`，**171 行**）。<br>内容＝三个问答的**只读取证**：**问一** 21:00 任务的**配置来源**；**问二** 回退来源（`ROADMAP`）**逐行结算**还剩几项可跑 ⇒ **实测 0 行**（即"清单另有来源"的**惯性假设被否证**）；**问三** 本夜的**来源处置**（如实记录）。★ **改 0 行** 其它任何文件。 |
| **③ 是否可逆** | 同件 1 —— **本地完全可逆**（纯新增）；**push 后不可逆**。 |
| **④ 是否与 C3 链冲突** | **文件层无冲突**（**0 个**后续 commit 触及）。<br>**内容层**：本件结论（"回退来源实测 0 行"）**未被任何后续治理件推翻**；与 `C3-013` 本轮改动**无交集**（本轮改的是 `TEMPLATES`／`CHARTER`）。 |
| **⑤ 补确认建议** | **接受入库**。理由：**纯只读取证**，零副作用；且"回退来源 0 行"是**可复算的机器事实**（任何一轮重跑都能验）。 |
| **⑥ 若拒绝如何处置** | 同件 1（§二）。 |

### 件 4 · `d197acd`（23:11 · **自引入缺陷的订正**）

| 项 | 内容 |
|---|---|
| **① 哈希 ＋ 时间** | `d197acd`｜**2026-09-24 23:11**｜标题＝「夜间 P3-3 订正：阅读指令块与模板逐字对齐（check_reading_instruction 当场抓出自引入缺陷；夜间 P3，便于回退）」 |
| **② 改了什么** | **单行改动**（`+1 / −1`），落在**件 3 那份报告**的第 9 步：<br>`− …若复述不出**、**或复述错**… ⇒ `＋ …若复述不出**或**复述错…`。<br>**性质**＝把夜间手写的阅读指令块**改回与 `TEMPLATES §二 第〇项` 逐字一致**（多打了一个顿号）。★ **触发者＝自家判据** `check_reading_instruction.py` 当场抓出（`C3-012` 收口时实测该判据仍能 PASS）。 |
| **③ 是否可逆** | **本地完全可逆**（改回一字即可，代价≈0）；**push 后不可逆**（同前，撤回须走反向 commit）。 |
| **④ 是否与 C3 链冲突** | **无**。★ 反向地：**它是在消除冲突** —— 若不做这行订正，该报告第 0 项将与权威模板**不逐字一致**，属"夜跑件自引入的形态缺陷"。 |
| **⑤ 补确认建议** | **接受入库**（**最优先**）。理由：**不改它反而是留着一个已知缺陷**；该订正使该文件**重新合规**。 |
| **⑥ 若拒绝如何处置** | 同件 1（§二）。★ 特别提示：**拒绝本件的代价最大而收益最小** —— 会让 `check_reading_instruction` 失去"整块逐字一致"的事实面。 |

---

## 二、★ 拒绝的结构性约束（**必须先看这一节**）

> 四件**不是四个可单独摘取的物件** —— 它们是后续 **9 个 commit**（`C3-011` 的 4 个 ＋ `C3-013` 本轮起）的**直系祖先**：

```
origin/main (e0fa133) → 夜跑 4 件 → C3-011 的 4 个 → C3-012 的 3 个 → C3-013 g1
                        ↑ 单一线性链，实测 merge 数 = 0，fa53223 的父节点 = d197acd
```

⇒ **可做到的**：① **逐件接受**（推荐）；② **整体暂不推**（默认，零风险）。
⇒ **做不到的**：把某一件**从历史里单独剔除**后推送 —— 那必须 rebase／cherry-pick／reset，**均为当轮禁区⑧**，且会破坏 `C19`（原文不回改）与溯源链，**不采用**。
⇒ **个别不接受的唯一合法形态**：**保留本地不推**（或后续轮次以**反向 commit** 调整该文件内容后再一并推）—— **历史不改，只在前向增补**。

---

## 三、执行者的建议（汇总 · 由发起人裁定）

| 处置 | 内容 | 判据/理由 |
|---|---|---|
| **推荐（甲）** | **四件全部接受入库，一并推送** | 四件合计 **476 行、全部落在 `reports/`、零代码零治理；后续 0 个 commit 触及 ⇒ 无重叠**；其中两件的结论已被后续治理正式采纳 ⇒ **事后已获确认**（`T-076` 追溯适用的核心） |
| 备选（乙） | **暂不推**，待进一步挑读 | 零风险，代价＝本地堆积 13 个起、CI 覆盖范围停在远端旧 HEAD |
| 不荐（丙） | 历史重写以剔除个别件 | 破 `C19`、破溯源、属禁区⑧ |

**★ 接受后的动作（待裁定）**：按 `T-038` push 门控执行推送 → 回读 `origin/main` → 等 CI 三 job。
**★ 若在本次 push 后仍未消失的风险**：推送即公开（四件含三份完整报告），届时**撤回只能靠反向 commit**。

---

## 四、边界说明（四类 · 齐全）

| 类 | 内容 |
|---|---|
| **本地跑过** | 四件的 `--stat`／`--format` 取证、后续重叠度核测（`git log ..HEAD -- <files>` = 0）、治理件改动面核测（0 行） |
| **CI过** | **无**（未推送 ⇒ 无 run；本轮禁区⑥） |
| **真实跑过** | 平台 23:00 定时任务**确于 2026-09-24 23:09–23:11 真实运行**（即为四件的由来，`C3-012` §79.4 已取证） |
| **未验证** | ① 推送后 CI 三 job 的结论（**须推送后才可得**）② 四件内容是否有**用户尚未发现的史实偏差**（本稿只核"机器事实"，未逐行复核三份报告的每一句结论） |

---

## 五、等待发起人裁定

1. **件 1 `ffae676`**：接受／不接受／条件接受 —— 执行者建议 **接受**
2. **件 2 `b30cc6a`**：接受／不接受／条件接受 —— 执行者建议 **条件接受**（条件＝`A-003` 继续有效）
3. **件 3 `236775f`**：接受／不接受／条件接受 —— 执行者建议 **接受**
4. **件 4 `d197acd`**：接受／不接受／条件接受 —— 执行者建议 **接受（最优先）**
5. **整体**：点头后**一并推送**（推荐甲）／**暂不推**（乙）／~~历史重写~~（不荐）

> **本稿未做任何 push、未 rebase、未 cherry-pick、未 reset、未改夜跑四件原文**（`C19` ＋ 当轮禁区⑥⑧）。
