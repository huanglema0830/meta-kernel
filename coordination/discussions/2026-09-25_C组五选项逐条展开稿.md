# C 组五选项**逐条展开稿**（`§11.8` vs `§二十四 24.4` 口径张力 · `T-080` 续）

> **本文性质**：**列事实、列选项、不给结论**（`T-067`）。针对 `C3-015` §三.2 的六项要求**逐条展开**。
> **触发（承上一轮教训）**：`C3-014` 只给了代号（甲–戊）⇒ **用户看不到内容就定不了**。
> **边界**：**只展开，不改条文**；**不擅自选择某一项**（本轮禁区⑪）。
> **取值口径**：条文引文**逐字**；行号＝`coordination/TEMPLATES.md` 在 `count=420` 时点实测。

---

## 〇、十二事实索引（供下方"依据"列引用）

| 编号 | 事实（逐字／实测） | 出处 |
|---|---|---|
| `F1` | `§11.8` 表标题＝「夜间执行的安全边界（**默认值，待用户确认**）」 | 行 700 |
| `F2` | `§11.8` 表首行＝「读 coordination/、跑测试、**写报告草稿** ｜ ✅ 允许 ｜ **无副作用**」 | 行 706 |
| `F3` | `§11.8` 同表**其余四行的"理由"列用的都是"对外可见／不可逆"这把尺子**（push＝「公开可见动作（C10）＋ 会触发 CI」；改可见性／删除／改写历史＝「C10 不可逆」；切模型＝「C10 明列」）—— **无一行**以「`git` 跟踪范围」为理由 | 行 707–710 |
| `F4` | `§11.4` 明文＝「每完成一个 P3 任务，**记录到 `reports/`**」 | 行 670 |
| `F5` | 红线逐字＝「**改仓库状态的，必须走夜跑（出草稿等确认），不得自动化。**」 | 行 1308 |
| `F6` | 「改仓库状态」**算**：新增／修改／删除 `git` 跟踪文件 —— 含 commit、含 CI 配置、含治理件与底图，**也含写入 `reports/`／`discussions/` 的报告**（**口径从严**） | 行 1311 |
| `F7` | 「改仓库状态」**不算**：**仅读写仓库外记忆文件**；**仅读取与统计** | 行 1312 |
| `F8` | 一句话口径＝「**写入处是否在 `git` 跟踪范围内** —— **在 ⇒ 红线适用（必须夜跑）**」 | 行 1313 |
| `F9` | `§24.4` 的「配套既有约束」**只引**「**默认不推送**（机制 19 §11.8）」—— **未引**「写报告草稿＝无副作用」 | 行 1315 |
| `F10` | `§24.6` 分工表：机制 19 管**时段**，**时段 ≠ 确认需求**——"**不给『无人』发通行证**" | 行 1327 |
| `F11` | `§11.10`（`T-078`）**已如实登记该张力为待裁**（未处置） | 行 762–764 |
| `F12` | **行为面实测**：21:00 任务 prompt 明文「写入 `coordination/reports/<日期>_今夜P3清单.md` … **不推送、不提交**」⇒ **落盘到 `reports/` 且不 commit**；23:00 旧行为＝**落盘并 commit**（`C3-012` 据此判触红线）；全仓「无副作用」共 **9 处**，**仅 1 处**（`F2`）属本张力面 | 平台任务配置 ＋ `git log` 实测 |

---

## 一、甲 · **认定「定义域不同」**（只在 `§11.8` 加注）

| 项 | 内容 |
|---|---|
| **① 选项名** | 甲 —— 定义域不同 |
| **② 内容** | 主张两条**不冲突**：`§11.8` 的「写报告草稿」＝**只进会话／不落盘**的稿；凡**落盘进 `git` 跟踪范围**者，**一律按 `§24.4`** 判为"改仓库状态"。落点＝在 `§11.8` 该行下**加一行注**（并可对 `F4` 行同加一注）。 |
| **③ 影响** | **`§11.8`**：**原句一字不改**，仅补注 ⇒ 与 `C19`（原文不回改）**天然兼容**。**`§24.4`**：**一字不改** ⇒ 红线强度**不变**。**机制 32 红线**：**不变**。 |
| **④ 依据** | `F2`（被解释对象）、`F3`（理由列的尺子是"对外可见／不可逆"，非"`git` 跟踪"）、`F9`（立法时未以此句为前提） |
| **⑤ 稿中推荐** | **稿中未给推荐**（`T-067`：不预设结论） |
| **⑥ 风险** | ①「**无副作用**」四字**仍在原位**，检索与速读时**仍先命中它**（注在下一行，易被绕过）；② 若事实面确实是"落盘"（`F4`＋`F12`），则**注与事实相反** ⇒ 甲**不能单独成立**，须同时处理 `F4`；③ 需明确"不落盘的稿"在现行流程里**还有没有真实场景**（否则该注属对空解释）。 |

---

## 二、乙 · **认定「定义域重叠」**（收紧 `§11.8` 该行）

| 项 | 内容 |
|---|---|
| **① 选项名** | 乙 —— 定义域重叠 · 收紧 |
| **② 内容** | 认定两处**指同一件事** ⇒ 把 `§11.8` 该行改述为「✅ 允许，**但按 `§24.4` 属改仓库状态 ⇒ 仍须夜跑（草稿待确认）**」。因 `C19` 只增不改，落点＝**保留原句作历史留痕** ＋ 紧随一行"**收紧注**"（写明原句的"无副作用"限缩为"**不改变既有内容**，**但仍属改仓库状态**"）。 |
| **③ 影响** | **`§11.8`**：**表面含义被改变** —— 读者不再能由该行推出"夜间写 `reports/` 无副作用 ⇒ 可自动化"。**`§24.4`**：**不变** ⇒ 红线不变。**机制 32 红线**：**被强化**（与 `C3-012` 把 21:00 归类为"夜跑"一致）。 |
| **④ 依据** | `F4`（`§十一` 自身要求"记录到 `reports/`"）、`F6`（明文含 `reports/`）、`F8`（一句话口径）、`F12`（21:00 现行行为就是落盘） |
| **⑤ 稿中推荐** | **稿中未给推荐** |
| **⑥ 风险** | ① 夜间"允许读/跑/写"的动作面**表观收窄**："允许"与"须等确认"并列，执行者可能误读为"**必须先等白天确认才能落盘**" ⇒ 与 `F4`「记录到 `reports/`」形成**新的操作歧义**；② 需**同步**处理 21:00 任务表述与其"汇报"口径；③ 触及 `§11.8` 表（外界可见面 🔓）⇒ 需重估对外可见性。 |

---

## 三、丙 · **反向统一**（改 `§24.4`，把 `reports/`／`discussions/` 剔除）

| 项 | 内容 |
|---|---|
| **① 选项名** | 丙 —— 反向统一 · 红线口径收窄 |
| **② 内容** | 把 `reports/`／`discussions/` **从"改仓库状态"中剔除**；只把 **commit／CI 配置／治理件／底图／工具脚本** 等算作"改仓库状态"。 |
| **③ 影响** | **`§24.4`**：**红线被削弱** —— 落盘但未 commit 的稿**不再触发夜跑**。**`§11.8`**：原句**自动成立**（若"落盘"也被视为无副作用）。**机制 32 红线**：**被削弱**；且与 `C3-012` 对 23:00 的判定**部分自相矛盾**（当时判其为"名义夜跑、实际已自检自 commit" ⇒ 若只算 commit，"罪"就在 commit；但该 commit **本身包含落盘**，二者难分割）。 |
| **④ 依据** | `F2`（若把"草稿"读作落盘，则丙是该读法下的一致性选择）、`F7`（"不算"清单给出可比照的窄口径先例） |
| **⑤ 稿中推荐** | **稿中未给推荐** |
| **⑥ 风险** | ① **削弱红线** ⇒ 与用户明写的红线（`F5`）**方向相反**；② `T-075` 指令模板字段（引用 `§24.4` 作红线指引）**需连带同步**；③ 机制条文属 🔓 对外可见面 ⇒ 改动会被顾问看到并引用 ⇒ **需重估对外表述**。 |

---

## 四、丁 · **维持现状**（只补双向交叉引用）

| 项 | 内容 |
|---|---|
| **① 选项名** | 丁 —— 维持现状 · 只补交叉引用 |
| **② 内容** | 两条**原句都不动**、**判定都不动**；仅在 `§11.8` 行下加"另见 `§24.4`"、在 `§24.4` 行下加"另见 `§11.8`"。 |
| **③ 影响** | **`§11.8`**：不变｜**`§24.4`**：不变｜**机制 32 红线**：不变 —— 三条**全部零改动**。 |
| **④ 依据** | `F11`（张力**已登记**待裁，属"已知且留痕"状态）、`F9`（两处**从未正面互相引用** ⇒ 加互引即补上唯一缺口）、`F1`（该表自标"默认值，待用户确认"，**本就未定型**） |
| **⑤ 稿中推荐** | **稿中未给推荐** |
| **⑥ 风险** | ① 张力**继续存在** ⇒ 归类**仍靠人**；② **下轮盘点仍可能由两条得出相反结论**（`F5` 家族：有条文、无唯一解）；③ 与 `T-079` 同型问题：**机器不守、靠人工**。 |

---

## 五、戊 · **再延一轮**（等 23:00 新行为实证后再定）

| 项 | 内容 |
|---|---|
| **① 选项名** | 戊 —— 再延一轮 · 等实证 |
| **② 内容** | 等 23:00 新行为的**首个真实样本**（补丁是否真落**仓库外**、是否 **0 commit**）落地后再定；理由是"**只看条文有分歧，看行为更直接**"。 |
| **③ 影响** | **`§11.8`**：不变｜**`§24.4`**：不变｜**机制 32 红线**：不变（本轮无动作）。 |
| **④ 依据** | `F12`（行为面是最强证据）、`F7`（"**不算**"清单把"**仅读写仓库外记忆文件**"列为合法 ⇒ 确立过"仓库外＝不触红线"的先例） |
| **⑤ 稿中推荐** | **稿中未给推荐** |
| **⑥ 风险** | ★★ **样本面与张力面错位** —— `T-078` 已把夜间产出**改为"仓库外补丁"** ⇒ **未来不再产生"夜间写入仓库内 `reports/`"的新样本** ⇒ **等样本也解不了 `F4`／`F6` 这条仓库内张力**；即：**戊 只能验"新形态是否合规"，不能验"§11.8 与 §24.4 谁对"**。 |

---

## 六、五选项汇总对照（一页看全）

| 维度 | 甲（定义域不同·加注） | 乙（重叠·收紧 `§11.8`） | 丙（反向·改 `§24.4`） | 丁（维持·加互引） | 戊（再延·等实证） |
|---|---|---|---|---|---|
| **改哪一处** | `§11.8`（加注） | `§11.8`（加收紧注） | `§24.4`（删 `reports/`） | 两处（各加互引） | 都不改 |
| **`§11.8` 影响** | 原句留 · 加解释 | **含义收紧** | 原句成立 | 不变 | 不变 |
| **`§24.4` 影响** | 不变 | 不变 | **红线削弱** | 不变 | 不变 |
| **机制 32 红线** | 不变 | **强化** | **削弱** ⚠ | 不变 | 不变 |
| **主要依据** | `F2 F3 F9` | `F4 F6 F8 F12` | `F2 F7` | `F11 F9 F1` | `F12 F7` |
| **成本** | 最小 | 小（含连带同步） | 中（≥3 处连带） | 最小 | **0（但要等）** |
| **可逆性** | 完全可逆 | 可逆（留痕） | 可逆但波及面大 | 完全可逆 | 完全可逆 |
| **最大风险** | 注被绕过／与事实相反 | 形成新的操作歧义 | **削弱红线** | 张力长期悬空 | **样本解不了本题** |

---

## 七、待用户判断的问题（**不给结论**）

1. `§11.8` 的「**写报告草稿**」，指**不落盘的稿**，还是**落盘到 `reports/` 的稿**？（`F2` vs `F4` ＋ `F12`）
2. 若"落盘"确为既有实践，**是否保留**？保留则其归类应如何表述，才能让 `§11.8` 与 `§24.4` **给出同一答案**？
3. 是否接受"**通过跑样本无法解本题**"（第五节 ⑥）这一判断 —— 若接受，则**必须在甲/乙/丙/丁 中择一**。

---

## 八、本文边界（如实）

- **只展开、未改任何条文**（`§11.8`／`§24.4` 一字未动）。
- **未做**的取证：23:00 新行为样本（**今晚 23:00 才可能产生**）；21:05 蒸馏是否落盘（**仍未核**）。
- **不预设结论**：五选项**并列呈现**；第五节 ⑥ 的"样本错位"是**事实判断**（基于 `T-078` 的行为变更），**不是选项推荐**。
