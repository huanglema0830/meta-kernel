# 外部格式化残留 —— **来源查明报告**（`云内核-C3-020` §四）

> **本文性质**：**只查明 —— 列证据、给判定，不改文件**（本轮禁区⑪：**不还原**）。
> **被查对象**：`coordination/discussions/2026-09-25_两份件完整度口径.md`（`C3-019` 中断检查发现其**被外部 markdown 格式化**）。
> **★ 免责留痕**：本报告**不含**"推定结论即事实"的表述；凡**证据不足**处**显式标注**。

---

## 〇、结论摘要（逐项回答用户的 6 问）

| # | 问题 | **查明结果** | 证据强度 |
|---|---|---|---|
| 1 | **什么工具？** | **未能确定到具体工具**（无任何格式化配置可依），**排除** prettier／markdownlint／editorconfig 类仓库侧工具 | **强**（配置全无） |
| 2 | **何时跑的？** | **落在 `C3-018` g1 提交之后 → `C3-019` 开工之前**（约 **9 分钟窗口**）；**精确时刻不可得**（mtime 已被我方后续写入覆盖） | **中** |
| 3 | **为什么跑？** | **未查明**（无配置文件、无 hook、无授权记录）⇒ **判为"非配置驱动的、编辑器/预览侧的一次性触发"** | **中** |
| 4 | **定时还是触发？** | **触发**（一次性）。**排除定时**：本机定时任务均在 21:00–23:30 夜间窗口；**09-25 白天无定时触发记录**，且**全库仅此 1 个文件受影响** | **强** |
| 5 | **是否还有其他文件被同样处理？** | **否** —— 全库扫描：工作区 `git status --porcelain` ＝ **0 行**（无遗留差异）；`C3-019` g1 提交中**仅此一件**带格式化特征 | **强** |
| 6 | **是第二个"自动来源"吗？** | ★ **否** —— **不构成**继"夜跑／自动化"之后的第三个自动来源（**无配置、无 hook、非定时、单文件**） | **强** |

---

## 一、改动内容（`C3-019` 已登记的形态，此处复录）

`git diff` 判明其性质为：

| 特征 | 说明 |
|---|---|
| **表格对齐填充** | 例：`\| # \| 要素 \|` → `\| # \| 要素                   \|`（列宽补齐） |
| **表头分隔行改形** | 例：`\|---\|---\|` → `\| - \| -------------------- \|` |
| **行尾双空格** | markdown 硬换行（`  ` 结尾） |
| **空行增删** | 94 → 97 行 |

⇒ 这是**典型的"markdown 美化/格式化器"行为**（对齐表格、规范化分隔行）。

---

## 二、逐项取证（六路）

### 2.1 仓库侧工具链（**排除**）

| 检查项 | 结果 |
|---|---|
| `.prettierrc` / `prettier` 配置 | **无** |
| `.markdownlint*` 配置 | **无** |
| `.editorconfig` | **无** |
| `eslint` / `stylelint` 配置 | **无** |
| `.vscode/`（编辑器工作区配置，常含 format-on-save） | **不存在** |
| `husky` / `.git/hooks`（自定义非 sample hook） | **无** |
| CI 中是否有格式化步骤 | **无**（CI 三 job：test／引导镜像／宿主验收；另有 pages） |

⇒ **结论**：**不是仓库侧工具链**、**不是 commit 时 hook 触发**、**不是 CI 触发**。

### 2.2 时间窗口（**中强度**）

| 证据 | 值 |
|---|---|
| 该文件**创建**（`C3-018` g1） | commit `803e908` |
| `C3-019` **开工**时状态 | 已为**已修改未提交**（` M`） |
| 该文件**当前 mtime** | **2026-09-25 11:54:57** |

★ **关于 mtime 的重要说明（防误判）**：`11:54:57` **不是**格式化的时间戳 —— 实测**另有两个文件 mtime 与该文件同为 11:54:57（毫秒级间隔）**：`reports/…第一部分_技术与产品_详尽版.md`、`reports/…第二部分_管理与运行_详尽版.md`。**三者同秒**＝**我方 `C3-019` 落地脚本一次批量写入**所致 ⇒ **该 mtime 已被我方写入覆盖**，**不能用于反推格式化时刻**。

⇒ **可确定的窗口**：**`C3-018` g1 提交（约 11:44）→ `C3-019` 开工（11:53）之间**（约 **9 分钟**）；**更精确时刻不可得**（如实标注）。

### 2.3 触发方式（**强**）

| 假设 | 检验 | 结论 |
|---|---|---|
| **定时任务** | 本机定时任务清单中，03:00／06:00／07:00／07:30／08:00／09:00／20:30／21:00／21:05／21:07／21:30／22:00／23:00／23:10／23:20／23:25／23:30 等**均在 09-25 白天窗口之外**（本机无"白天每 N 分钟格式化 markdown"的任务） | ❌ **排除定时** |
| **批量全库** | 若为批量，应有多文件受影响 —— 实测**仅此 1 件** | ❌ **排除批量** |
| **人工会话内触发** | 时间窗落在**人工会话期间**（`C3-018` 收尾 → `C3-019` 开工） | ✅ **最可能** |

### 2.4 影响面（**强**）

- 全库扫描结果：**仅此 1 个文件**；工作区**无任何遗留差异**（`git status --porcelain` ＝ 0 行）。
- `C3-019` g1 提交的 4 个文件中，**其余 3 个**（`TEMPLATES.md`／两份件）**均为我方主动改动**，**无格式化特征**。

### 2.5 是否"第二个自动来源"（**强：否**）

判据（对照机制 32 的"自动来源"特征：**无指令、无需确认、直接完成**）：

| 特征 | 本次事件 |
|---|---|
| 有指令吗 | **无**（我方未下任何格式化指令） |
| 是例行/可重复的吗 | **否**（单次、单文件） |
| 有配置或调度吗 | **无** |
| 会周期性复现吗 | **未知，但无机制支撑** ⇒ **倾向"不会"** |

⇒ ★ **不登记为"第三个自动来源"**；按指令 §4.3，**属"一次性触发"** ⇒ **登记原因（未查明）与影响面（单文件、内容无损）**。

### 2.6 我方排除项（自证）

| 项 | 说明 |
|---|---|
| 本链是否运行过格式化器 | **否** —— `C3-014`–`C3-020` 全程**未调用**任何 formatter／linter（可核：命令历史仅用 `git`／`python` 脚本／`gh`） |
| 是否由我方 python 脚本造成 | **否** —— 该文件在 `C3-019` 开工时**已是 ` M`**，而 `C3-019` 的脚本**尚未运行**（脚本运行于 11:54:57，晚于此状态确立时刻） |

---

## 三、判定与影响评估（**如实**）

| 项 | 判定 |
|---|---|
| **性质** | **一次性、单文件、编辑器/预览侧的 markdown 格式化**（**具体工具未查明**） |
| **内容损失** | **无** —— 格式化只改**空白与表格对齐**，**语义与文字未变**（`C3-019` 已核：追加于其上的第七章内容**正常**） |
| **治理影响** | 该改动**已随 `C3-019` g1 提交入库**（`e57b7c8`）—— ★ **这是本事件的实质性后果**：**一份非本链改动进入了历史** |
| **是否需要动作** | **不还原**（本轮禁区⑪）；**保留**；**本报告即为留痕** |

---

## 四、若再次发生，如何捕获（**建议，供裁定**）

| 手段 | 说明 | 代价 |
|---|---|---|
| **① 逐轮"非本链改动"检查（已具备）** | `C3-019` 的中断检查**已经抓到了它** ⇒ **现有机制有效**，**无需新增** | 0（已是每轮首件事） |
| **② 记录 mtime 快照** | 每轮开工记录关键件 mtime，便于反推"何时被谁动过"（本轮 mtime 被覆盖即因缺此快照） | 小 |
| **③ 提交前 diff 复核** | 提交前对"本轮未主动改动的文件"做 `git diff --stat` 复核 | 小（已部分在做） |
| **④ 若确认是编辑器 format-on-save** | 可在平台侧**关闭该行为**（属环境设置 ⇒ **`C10` 范畴，须你确认**） | 需你操作 |

---

## 五、边界（如实）

- **只查明，未还原、未改文件**（禁区⑪）。
- **强证据**：无配置、无 hook、非定时、单文件、非本链。
- **未查明**：① **具体工具**；② **精确触发时刻**（mtime 被覆盖）；③ **触发原因**（是否有第三方会话/编辑器动作）。
- **未做**：未检查 WorkBuddy 平台的编辑器/预览是否内置格式化（**平台侧不可观测 ⇒ 不推定**）。
