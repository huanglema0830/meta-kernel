# unsafe 白名单（C9 门禁）
#
# 一行一个**相对仓库根**的文件路径；只有列在这里的文件才允许出现真实 unsafe。
# 空文件 / 只有注释 ⇒ 表示"当前不放行任何真实 unsafe"。
#
# 登记规则（缺一不可）：
#   1. 该文件是**边界模块**（如 host/field-render/src/drm_ffi.rs），不是业务逻辑
#   2. 文件内每处 unsafe 块都有紧邻的 `// SAFETY:` 论证
#   3. 全仓库禁 `transmute`
#
# 现状（2026-09-17 · 2.3）：**首次放行 1 条**（D34 由用户确认，仅限该文件）。
# 此前（v0.127–v0.166）真实 unsafe = 0 处、本白名单为空。
#
# ── 已放行 ──────────────────────────────────────────────────────────────
meta-kernel-boot/kernel/src/mem/global.rs
#   ↑ `GlobalAlloc` 实现 + 物理内存切片（2.3 内存管理的唯一边界）
#   · 算法本体在 `meta-kernel-mem`（该 crate 有 `#![forbid(unsafe_code)]` ⇒ 零 unsafe 是**结构性保证**）
#   · 本文件 unsafe 仅三类：① 元数据区→切片 ② 访问 `static mut STATE`（单线程前提）③ `unsafe impl GlobalAlloc`
#   · ⚠️ 已知边界条件：**非重入**（前提＝阶段二尚未开中断/多核）；`size > 4 KiB` 的请求**返回 null**
#
# ── 格式示例（**尚未生效**，仅示范）────────────────────────────────────
# host/field-render/src/drm_ffi.rs
