【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# push v0.279–v0.283 ＋ 3 项裁定落地 ＋ 第八批改触发式（2026-09-21 · v0.286）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。
（本轮改动文件新增行扫描：本机用户名 **0**、绝对路径 **0**、端口数字 **0**。）

**② 全景状态卡**（★ 数字为**生成时快照**；提交会推高版本号 ⇒ **现值以复核命令为准**，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 283** ⇒ 收尾 **286**（本轮 **3 个本地提交**：v0.284 `93dd04e`／v0.285 `b34a5f3`／v0.286＝本报告）⇒ label **v0.286**（`label = count`，C21） |
| **推送** | **已推 `db4d37f..fab377f`（v0.279–v0.283，exit 0）**；**本轮新 commit 未推**（机制 23 须当轮明示） |
| **阶段** | 阶段二 · **2.4 边界层已验**｜**L7 = 执行层**（判定与记账（T1 闭环）已实现、执行侧待确认） |
| **编号** | 机制 **1–27**｜C **1–21**｜T **1–36**｜D **1–44**｜R **1–52**｜TERM **1–9**（本轮**无编号增删**；`TEMPLATES.md` **未动**） |
| **三表** | **连续、无缺号、无重号**（C15 实测，同一脚本取两次数） |
| **新增口径** | **`README.md` §文档数值口径**（G-2 总声明）｜**`docs` 第八批 ＝ 触发式**（§35.4） |
| **工作树** | 见 ⑬（本报告提交前为「已改待提交」） |

**③ 任务**：基准 **count = 283**｜总规划定位：**push v0.279–v0.283 ＋ 3 项裁定落地 ＋ docs 第八批改触发式**｜类型：**放行 ＋ 裁定落地 ＋ 机制调整**。

**④ 状态：完成**（含 **4 处诚实边界**：① **CI 三态按 C20 只记当日日报**（仓库外），本报告**不内联 run 号**；② **本轮零内核／boot 改动** ⇒ **无 QEMU／裸机侧新结论**；③ **`docs` 第八批触发条件未满足 ⇒ 本轮不产出**（**这是"触发式"的实效，非缺项**）；④ **"已交付"判定＝静态落点检索 ＋ 既有 CI 门禁**（非本轮运行期穷尽）。

---

## ★ 主体

### 〇 · Push 放行 v0.279–v0.283（用户 §〇）· **先 push、后改文件**

- `git push origin main` ⇒ **`db4d37f..fab377f`（exit 0）**；推送前 `ahead 5`／推送后 **`ahead 0`**。
- **在本轮任何编辑之前**执行（**§30.0 经验第六次生效**）。
- **CI**：**三 job 全绿**（`test` ／ `元内核引导镜像（bootloader 0.11 + QEMU 真实引导）` ／ `宿主 field-render（Windows 构建 + 客观验收）`）；**一次通过、无判红、无重跑**。
- **⚠️ C20**：run 号与逐 job 结论的权威记录＝**当日日报**（仓库外），本报告不内联该观测值。

### 一 · 裁定 2：`docs/HOST_UI_DECISION.md` **H-1 六处加锚**（用户 §一 · **改底图，获授权**；**原句未改**）

| # | 位置 | 加锚要点（逐字摘要） |
|---|---|---|
| 1 | **L4** 头部结论 | 标为 **v1.0（2026-09-15）时点口径**；**至 v0.283 窗口宿主已交付** —— `host/field-render/src/window.rs`（`winit` `ApplicationHandler` ＋ `wgpu` surface ＋ **`egui 0.30` UI**）＋ CI **验收④「真实开窗 + URL 取源码 + 上屏回读」硬门禁**（`.github/workflows/ci.yml` **L1016**，已去 `continue-on-error`） |
| 2 | **§4 L69** 窗口能打开 | **已过期**（见 §6.2：✅ **1536×1536**、方差 43.9／1182.8／1370.7、非纯黑非纯白） |
| 3 | **§4 L70** 地址栏 URL | **已过期**（`fetch_source` 系统 `curl`；CI 验收④「URL 取源码」） |
| 4 | **§4 L71** 多标签 | **已过期**（`struct Tab`／`NewTab`／`CloseTab`） |
| 5 | **§4 L72** 下载 | **已过期**（`Action::Download`／`fn download()` 写 `field.json`＋`frame.ppm`；产物 `host/field-render/downloads/`） |
| 6 | **§6.4** | 标为 **v1.1 时点口径**；至 v0.283 **四者均已交付** |

- **★ 过程处置（R75 同类）**：首版把 **L4 锚写成新起一行** ⇒ 该文件行数 +1 ⇒ 汇编**带行号前缀**、其后**行号全漂移** ⇒ 汇编 diff 达 **232 行**。**已改为并入同一行** ⇒ 行数仍 **235（=HEAD）**、汇编 diff 收敛为 **第3批 6 行**。

### 二 · 裁定 3：**G-1 加锚** ＋ **G-2 总入口总声明**（用户 §二 · **改底图，获授权**）

**G-1 · `docs/FULL_AUDIT_REPORT.md` L77「已知未接线」加锚**（**原句未改**）
- 标为 **2026-09-14 · v0.102 时点口径**；其中 **①③ 已过期** —— ① L4 **已接线**（`threshold::from_library`；调用点 `l4_gate.rs:66`、`host/field-render/src/main.rs:1373`）｜③ 基因库**持久化已实现（v0.107）**（`to_text`/`from_text` ＋ 网关 `POST/GET /v1/genelib`）；**② 仍成立**（`scene_baseline*` 全仓无外部调用点）。

**G-2 · 根 `README.md` 新增一条总声明**（**不逐行加锚**）—— 逐字新增内容：
> `## 文档数值口径（2026-09-21 定稿）`
>
> `> **本仓库全部文档（含本 `README.md` 与 `docs/` 各文档）中出现的所有测试数 / 模块数 / 行数等具体数字，除明确标注"实测"外，一律为时点值（成文时快照）；现值以 `git rev-list --count HEAD` 实时值与 CI 实测为准。**`
> `>`
> `> 依据：`coordination/BASELINE.md` **§三十四**（第七批 **G-2**；**R71／R82**）。**不逐行加锚** —— 逐行维护不可持续（**R79 同族**）。`

- **落点实证**：`docs/README.md` **不存在** ⇒ 取**根 `README.md`**（用户 §二.2 授权由我方取现状后定）；**插于 `## 许可 · License` 之前** ⇒ 其前各行行号（55／89／105 等**被引用行**）**零漂移**。
- **判据 F 阳性对照**：`FAIL（3 项：README.md／FULL_AUDIT_REPORT.md／HOST_UI_DECISION.md）` → `--update-baseline`（**diff 3 行**）→ **`PASS`**。
- **汇编同步**：第3批 **6**／第4批 **1**／合订本 **7** 行；**反向自检 PASS**。
- **C15**：全 **±0**。

### 三 · 裁定 4：固化进 `TEMPLATES.md §11.3` —— **本轮不做**（用户 §三）
- **本轮零治理文件改动**（`TEMPLATES.md` 未动）；正式固化须走 `TEMPLATES §7.2`（提议 → 记 §7.2 → 试用 3 轮），**触发＝你下一轮明确授权**。

### 四 · `docs` 第八批 **改为触发式**（用户 §四）
- **新口径（逐字）**：`docs 第八批 ＝ 触发式；不再每轮固定做。触发条件：① 有新文档加入 docs/；② 或重大状态变化（阶段 / 机制编号）。`
- **★ 本轮触发评估（实证）**：① `docs/*.md` ＝ **35 份**（**与上轮同为 35，无新增**）⇒ **不满足**；② 阶段仍为**阶段二 · 2.4 边界层已验**、机制编号 **1–27 未变** ⇒ **不满足** ⇒ **本轮不产出第八批**（**这正是"改触发式"的实效：不为凑数而扫**）。
- **配套**：`advisor_brief` 复扫亦为触发式（§34.4）⇒ **夜间固定"复扫类"项自此清零**。

### 五 · 夜间持续任务（用户 §五）

- **5.1 `docs` 第八批（触发式）**：**触发条件未满足** ⇒ **本轮不产出**（见「四」）。
- **5.2 汇编后续批次**：本轮因裁定 2／3 改 `docs/` 2 份（`HOST_UI_DECISION`／`FULL_AUDIT_REPORT`）**已重生成**（第3批 6／第4批 1／合订本 7 行）；**重跑验证幂等 = 零差异**；**R71 合规**（只引路径＋命令，不抄派生值）。

### 六 · 验收对照（用户 §九 · 8 条）

| 验收标准 | 结果 |
|---|---|
| push 完成、CI 三 job 全绿 | ✅ `db4d37f..fab377f`；CI 三 job 全 success（**run/结论只记日报，C20**） |
| H-1 六处加锚、逐字对照；判据 F 更新并 PASS | ✅ 见「一」（FAIL **3** → diff **3 行** → PASS） |
| G-1 加锚、逐字对照 | ✅ 见「二」 |
| G-2 总入口总声明、逐字给出 | ✅ 见「二」（`README.md` 新增「文档数值口径」一节） |
| 汇编合订本重生成、逐字保真 | ✅ 第3批 6／第4批 1／合订本 7；反向自检 PASS；5.2 幂等零差异 |
| `docs` 第八批改触发式、写入 BASELINE | ✅ 见「四」（`§35.4`） |
| C15 复算通过，编号表连续无缺重 | ✅ `BASELINE §35.7` |
| 下一次返回第 0 行含 9 步指令 | ✅ **本报告 P1/P2 PASS、泄漏 0** |

### 七 · 未完成项

- **无强制未完项**。**主动留档**：裁定 4 固化（**待授权**，保守留到下轮）。

### 八 · 真实缺陷

- **0 缺陷**。**3 条过程观察**：① **行号漂移陷阱实证**（L4 锚另起一行 ⇒ 汇编 diff **232 行**；并入同行后 ⇒ **6 行**）—— **R75 同族**，当场发现并修正；② **基线时序疏漏**（`--update-baseline` **之后**又编辑同一 `docs/` 文件 ⇒ 基线哈希过期 ⇒ 全量判据**判红 1 项**，点名 `HOST_UI_DECISION.md`；**已重取基线** → PASS）—— **R82／C19 同族**；★ **沉淀规则：`--update-baseline` 之后若再动 `docs/`／`README.md`，必须重取基线**；③ **"触发式"首次生效**：第八批**未触发而主动不跑**（对比前七轮"每轮固定"）⇒ **机制调整已产生可观测的产能变化**（夜间不再被固定复扫占用）。

### 九 · 三量台账

| 量 | 本轮内容 |
|---|---|
| **存量** | `docs/` 35 份；`host/field-render`（window.rs／main.rs／downloads）；`.github/workflows/ci.yml`；`meta-kernel-core`（`gene_library.rs`／l4 threshold）；`npb-gateway`；判据工具 6 支 |
| **变量** | `docs/HOST_UI_DECISION.md`（6 处）｜`docs/FULL_AUDIT_REPORT.md`（1 处）｜根 `README.md`（**新增 6 行**）｜`basemap_hashes.txt`（3 行）｜汇编（第3批／第4批／合订本）｜`BASELINE §三十五` |
| **补充增量** | `coordination/reports/` 新增 **1 份**（本报告）；**2 条新口径**（`README` 文档数值口径 ＋ `docs` 第八批触发式） |
| **创新增量** | **1 条机制调整**（复扫类全部改触发式：`advisor_brief` 复扫 §34.4 ＋ `docs` 第八批 §35.4）⇒ **夜间固定复扫项清零** |

### 十 · 边界说明（**四类**）

- **本地跑过**：✅ 判据实跑（源一致性／阅读指令／文档一致性十二侧／端点表／内核纯净度／私有指针）＋ 符号检索＋汇编重生成与幂等复核。
- **CI 过**：✅ 本轮推送的 v0.279–v0.283 **三 job 全绿**（**结论按 C20 只记当日日报**）。
- **真实跑过**：⛔ **未跑** —— 本轮**零内核／boot 改动**；**无 QEMU／裸机侧新结论**；**未在本机运行窗口宿主**（H-1 的"已交付"取自**既有 CI 硬门禁**）。
- **未验证**：⚠️ G-2 总声明的**措辞范围**（"全部文档" vs 仅 `README`）｜裁定 4 固化｜`docs` 第八批的"重大状态变化"边界。

### 十一 · 决策评估（新决策 · **七要素**）

**决策 D-新1：G-2 总声明的覆盖范围**
- **评估**：第七批 G-2 的 **9+ 处**数值型**全部在 `docs/`**；若总声明只管 `README.md`，则**失效**。
- **优势（宽口径）**：一次覆盖全库；**劣势**：措辞比用户原句宽（"本仓库全部文档"vs"本文档中"）。
- **机会**：把口径放到**总入口**，成为读文档第一条规则。
- **建议**：**维持"本仓库全部文档"**（已在锚内注明依据与落点）；若你本意收窄为 `README` 自身，**一句话即可改**。
- **等待决策**：**是**（⑫-③）。

**决策 D-新2：`docs` 第八批触发式下"重大状态变化"是否含阶段推进**
- **评估**：现口径为"阶段 / 机制编号"（**不含**普通版本递增，否则每轮必触发 ⇒ 等于没改）。
- **优势**：与"改触发式"目的一致；**劣势/风险**：若长期无阶段推进，**底图时效可能久不复查**。
- **机会**：可加"**距上次核对 ≥ N 轮**"作兜底（如 N=10）。
- **建议**：**维持现口径**（如需兜底，可加 N）。
- **等待决策**：**是**（⑫-④）。

### 十二 · 待你裁定（**4 项**，各附推荐）

| # | 项 | 推荐 |
|---|---|---|
| 1 | **本轮新增 commit（v0.284 起）是否 push** | **放行**（机制 23 须当轮明示） |
| 2 | **裁定 4 固化**（把"复扫类＝触发式"写入 `TEMPLATES.md §11.3`；**治理文件**） | **授权**（使口径与台账一致） |
| 3 | **G-2 总声明的措辞范围**（"本仓库全部文档" vs 仅 `README.md`） | **维持"全部文档"** |
| 4 | **第八批触发式的"重大状态变化"是否含阶段推进**／是否加"距上次核对 ≥ N 轮"兜底 | **维持现口径**（如需要可加 N） |

### 十三 · 下一步建议

1. （待授权）裁定 4 固化进 `TEMPLATES.md §11.3`（走 §7.2 程序）。
2. **复扫类自此触发式** ⇒ 夜间产能可转投**尚未触及的方向**（如 `coordination/tools/` 判据自检扩容、`docs/` 之外的口径核对）。
3. 建议下轮起**把"每轮固定复扫"从夜间清单彻底移除**（已在台账留痕）。

### 十四 · 启动回执 ＋ 本轮读了底图哪几份、哪几行

**启动回执**：已读 `CHARTER.md`｜当前状态 **count = 283**（实时取）｜阶段二 · 2.4 边界层已验｜本次任务：**push v0.279–v0.283 ＋ 3 项裁定落地 ＋ `docs` 第八批改触发式**｜是否继续：**执行完毕**。

| 底图 | 行 | 用途 |
|---|---|---|
| `docs/HOST_UI_DECISION.md` | L1–6、L65–76、L93–123、L163–193 | 裁定 2（H-1 六处） |
| `docs/FULL_AUDIT_REPORT.md` | L1–14、L70–80、L212 | 裁定 3（G-1） |
| 根 `README.md` | L1–20、L64–95、L108–133 | 裁定 3（G-2 落点判定） |
| `.github/workflows/ci.yml` | L672–684、L926–949、L985–1034 | H-1 举证 |
| `coordination/TEMPLATES.md` | L102–119（9 步指令）、L536–591（§十一） | 报告第 0 行／裁定 4 |
| `coordination/BASELINE.md` | §三十四（上轮）、§三十五（本轮） | 台账 |

---

## 附 · 晨报（用户 §七 · 明早 08:00 交付）

- **夜间触发结果**：**按预期跑完**；**`docs` 第八批按新口径"未触发即不跑"**（**合规**，非中断）。
- **完成项**：① push v0.279–v0.283 ＋ CI 三 job 全绿；② **H-1 六处加锚**＋基线＋汇编（含行号漂移处置）；③ **G-1 加锚**；④ **G-2 总入口总声明**（`README.md` 新增「文档数值口径」）；⑤ **裁定 4 本轮不做**（登记）；⑥ **`docs` 第八批改触发式**（写入台账）；⑦ 汇编**幂等复核**（零差异）。
- **失败项**：**无**。
- **未推送 commit 清单（3）**：`93dd04e` v0.284 裁定2+3 加锚｜`b34a5f3` v0.285 裁定4登记＋第八批触发式｜**本报告** v0.286（台账 §35.5–35.9 补全）—— **以 `git log origin/main..HEAD --oneline` 实测为准**。
- **未跟踪文件**：`coordination/reports/2026-09-20_今夜P3清单.md`。
- **需用户目验／放行项**：**⑫-1 push**（唯一需一句话的放行）；⑫-2 裁定 4 固化授权（治理文件）。
