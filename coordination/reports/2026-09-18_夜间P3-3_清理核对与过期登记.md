【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

**保密标记**（对应 C12；**拷贝给顾问前先看这一段**）：
- ⛔ 机密（**绝不拷**）：**无** —— 不含凭证、账号、本机绝对路径、私有根字面、网络细节
- 🔐 保密（**屏蔽后拷**）：**无**
- 🔒 半公开（**可拷**）：仓库内相对路径与行号、判据命令与输出口径
- 🔓 公开（**直接拷**）：过期判定、清理核对结论、教训归档

> 【给用户的提示】本次报告包含 **0** 处机密、**0** 处保密。无需屏蔽即可整份拷给顾问。

**全景状态卡**：

```
┌─ 云内核项目 · 全景状态卡 ─────────────┐
│ 版本   v0.206（rev-list --count HEAD）│
│        （含本夜三个本地 commit）        │
│ 阶段   ROADMAP 阶段二 · 子任务2        │
│        ⚠️ ROADMAP/BASELINE 的状态行**  │
│        仍写「剩余片6–片8」，实测已收口  │
│ CI     快照：run 35329365685 三 job 全绿│
│        ⚠️ 本夜**未触发**任何 CI run     │
│ 漏洞   R1–R70；本轮登记 5 条待裁定      │
│ 推送   本地 **ahead origin 3**（未 push）│
└────────────────────────────────────────┘
```

**任务**：**夜间 P3-3 —— 清理核对 ＋ 过期条目的机器取证与登记（治理文件只登记不改）**｜基准 v0.205→v0.206｜定位：阶段二 · 子任务2｜**优先级 P3**
**状态**：完成（**清理核对 3 项通过**；**过期条目 2 处 × 2 个事实点取证在案**；**新增登记 1 条：治理文件内部自相矛盾**）

---

## 一、清理核对（3 项，全部实测）

| # | 核对项 | 判据 | 实测 | 结论 |
|---|---|---|---|---|
| C1 | R67 复发检查：`__pycache__`／`*.pyc` 是否**误入库** | `git ls-files \| grep -c "pycache\|\.pyc$"` | **0** | ✅ **未复发** |
| C2 | `.gitignore` 规则是否在位 | 读 `.gitignore` | **第 61–63 行**：`__pycache__/`、`*.pyc`（注释写明是 R67 修复） | ✅ 在位 |
| C3 | 连跑判据是否产生**未跟踪文件**（R67 的成因） | `git status --porcelain \| wc -l` | **0**（本轮跑了 6 判据 × 多轮 ＋ 47 对循环 ＋ 自检 6 次） | ✅ **无残留** |

> **C3 的意义**：R67 的成因正是"跑判据 → 生成 `__pycache__` → 被 `git add -A` 带进仓库"。
> 本轮**刻意重复了这一条件**（把 6 个判据全跑一遍）并核对工作树 —— **规则确实挡住了**。

---

## 二、过期条目登记（**治理文件 ⇒ 夜间只登记、不改**）

**触发**：2.3b 片1–8 已于 v0.203 **收口**，而两处治理文件仍以"**剩余**"口吻描述迁移进度。

### 2.1 机器判据（本轮**亲自复现**，不是引用）

```
$ python coordination/tools/check_migration_closure.py --emit 1
❌ 层号越界：1（共 0 层）
```

⇒ **无可执行的第 1 层** ＝ **无剩余片**（`ROADMAP.md` 附录口径：这即是**收口信号**）。
旁证：`check_migration_closure.py` 实跑「源 55 模块／目标 57 模块，无缺口」（见 P3-2 报告 A3）。

### 2.2 过期事实点（**4 处 = 2 行 × 2 个事实**）

| # | 位置 | 原文（节录） | 实测现状 | 判定 |
|---|---|---|---|---|
| 1 | `coordination/ROADMAP.md:59` | 「**剩余片6–片8 = 7 模块 / 2,887 行**（脚本口径）」 | 片6（v0.200）／片7·片8（v0.203）**已完成**；`--emit 1` 报无剩余片 | ❌ **过期** |
| 2 | `coordination/ROADMAP.md:59` | 「**裸机断言已到第 ⑧ 段**」 | 代码实测最大段号 **⑩**（`meta-kernel-core-nostd/tests/mirror_bare_assertions.rs:320`） | ❌ **过期** |
| 3 | `coordination/BASELINE.md:154` | 「**剩余 片6–片8**（脚本口径 7 模块 / 2,887 行）」 | 同上 | ❌ **过期** |
| 4 | `coordination/BASELINE.md:154` | 「裸机断言扩到**第 ⑧ 段**」 | 同上 | ❌ **过期** |

### 2.3 ★ 新增登记：**同一份治理文件内部自相矛盾**

`BASELINE.md` 内部对**同一个事实**（裸机断言段数）给出**两个不同的值**：

| 位置 | 值 |
|---|---|
| `BASELINE.md:154` | 第 **⑧** 段 |
| `BASELINE.md:426` | 第 **⑩** 段（码 101–105，CI 读作 201–205） |

⇒ 与代码实测（⑩）比对，**第 426 行正确、第 154 行过期**。
**注意**：第 154 行可能是**历史留痕**（D30-b 不追改历史），**但那行用的是"剩余"这种现在时口径** ⇒ **读者无法区分"历史"与"现状"**。这正是 **R69 家族**（状态型表述静默过期）的**第二个实例**。

### 2.4 处置建议（**均属治理文件 ⇒ 白天**）

| # | 建议 | 为什么不在夜晚做 |
|---|---|---|
| ① | 更新/改写上述 4 处（或明确标注为历史留痕） | 两文件**都是治理文件**，本夜**明令禁止修改**（夜间清单亦排除） |
| ② | **★ 加一条机器判据**（R69 的落地）：把"ROADMAP／BASELINE 的剩余片数、裸机段数"与 `closure --emit 1`、`mirror_bare_assertions.rs` 实际最大段号做一致性检查 | **顺序不能颠倒**：此刻就建这条判据 ⇒ 它会**立刻常亮红**（4 处未改）⇒ 又是一盏"没人信的红灯"。**必须先更新治理文件，再上判据** |
| ③ | 明确"治理文件里的状态行"的**时态约定**（现在时＝现状、过去时＝历史），否则每次进展都要靠人肉回忆哪行是现状 | 属**机制层**（要动 `TEMPLATES.md`／`CONSTRAINTS.md`）⇒ 白天 |

---

## 三、教训归档（承 P3-1 §八③）

**「把'我为了保守自定的限制'写成'任务明令'」会自锁产能。**
昨夜 §八 把"改判据工具"列为"**本夜任务明令排除的类别**"，但机制原文并无此条 ——
`TEMPLATES.md` §11.3 反而**明确把「测试／清理／修复」列为夜间可做**，§11.8 亦写「改代码（重构/清理/修复）✅ 允许，但必须独立 commit、可回退」。
⇒ **本夜 P3-1 因此可以合法地把'补自检（测试）＋ 修死分支（清理/修复）'一次做完**，并拿到"改前红→改后绿"的证据链。
**归档口径（R69 同族）**：**凡"某类事不能做"的说法，必须给出可核对的出处（文件:行）；给不出，就是自限，不是规则。**

---

## 四、验收对照（标准 vs 实测）

| # | 标准 | 实测 | 结论 |
|---|---|---|---|
| 1 | 清理核对**须有机器判据**（不得凭印象） | C1／C2／C3 三条命令，输出在案 | ✅ |
| 2 | 过期判定**须能复现**（不是"我看着像过期"） | `--emit 1` 输出原样 ＋ 代码行号（`mirror_bare_assertions.rs:320`） | ✅ |
| 3 | **治理文件不得改动** | 本项只**读** `ROADMAP.md`／`BASELINE.md`／`.gitignore`；`git status` 除本记录外无改动 | ✅ |
| 4 | 新增登记**须区分"确证"与"存疑"** | §2.2 四点为**确证过期**（有机器判据）；§2.3 的"历史留痕 vs 过期"**已明确标为待裁定** | ✅（C17 口径） |
| 5 | 独立 commit、未 push | 1 个本地 commit（`夜间 P3-3：` 前缀） | ✅ |

---

## 五、三量台账

- **存量**：[带入] `check_migration_closure.py`（`--emit` 收口信号）／`mirror_bare_assertions.rs`（裸机断言 10 段）／`.gitignore`（R67 规则）／`ROADMAP.md:59` 与 `BASELINE.md:154,426` 的状态行
- **变量**：[本次改动] **零代码改动、零治理文件改动** —— 跑 1 条判据、读 3 个文件、写本记录 1 个文件
- **补充增量**：[新增] ① **清理核对三条命令化**（R67 复发检查首次以"重复其成因条件"的方式验证）② **4 处过期事实点的机器取证**（判据输出＋代码行号）③ **"同文件内部自相矛盾"登记**（⑧ vs ⑩）④ **R69 落地建议及其排序理由**（先改文件、再上判据）
- **创新增量**：[产出] **无** —— 本项是**核对与登记**，不产出新能力

---

## 六、边界说明（C5）

- **本地跑过**：✅ ① `check_migration_closure.py --emit 1`（收口信号）② `git ls-files` 计数（pycache 残留 0）③ `git status --porcelain`（0 处）④ 读 `.gitignore` 第 61–63 行 ⑤ 读 `ROADMAP.md:59`、`BASELINE.md:154`、`BASELINE.md:426` ⑥ grep 代码区实际最大段号（`mirror_bare_assertions.rs` 第 320 行＝⑩）
- **CI过**：⚠️ **本夜未触发任何 CI run**。报告内 CI 结论均为**快照**。
- **真实跑过**：**不适用**（未跑 QEMU、未上真机）。
- **未验证**：① 未逐段核对 `mirror_bare_assertions.rs` 的 10 段**是否与 CI 读法（码 201–205）逐一对齐** ② 未普查**其它治理文件**是否还有同类过期状态行（本轮只查了"2,887／剩余片／第 ⑧ 段"三个检索词） ③ `BASELINE.md:154` 究竟是**历史留痕**还是**应更新**，**我未下断言**（须发起人裁定）
  ④ 清理核对只覆盖 `__pycache__` 一类，**未做死代码/编译警告级的清理普查**（属"清理"白名单，本轮未做）

---

## 七、决策评估（**六要素**，WorkBuddy → 用户）

**① 相关的点全部罗列**
- 两处治理文件、**4 个事实点**过期；其中 1 组构成**同文件内部矛盾**。
- 判据侧：`closure --emit 1` 已经能给出**收口信号**（机器可判）；**段数**目前只能靠 grep 代码（尚未判据化）。
- 与本夜其它项的关系：**P3-1 修了"判据死分支"、P3-2 登记了"判据表达力缺口"、P3-3 登记了"文档静默过期"** —— **三条同族**：**都会让"红/绿"失去信息量**。
- 牵连：更新 `ROADMAP.md`／`BASELINE.md` 属治理文件改动 ⇒ **必须白天、且须过 C15（编号表编辑纪律）**，因为 `BASELINE.md` 承载 D/R 编号表。

**② 风险点在哪里（含"不做"的风险）**
- **做的风险**：改 `BASELINE.md:154` 若用 Edit 的锚点取得太大，**可能连带吃掉相邻行**（C15 记载该类失误**已复发 5 次**）⇒ 必须**改前取数→改后复算**。
- **不做的风险**：① 任何看 ROADMAP 判断进度的人（含**顾问与未来的我**）都会以为**还有 2,887 行未迁** ⇒ 可能**重复开工已完成的活**；② 段数写 ⑧ 而实际 ⑩ ⇒ **低估已完成的验证强度**；③ 同文件两个值 ⇒ **谁也不知道该信哪个**。

**③ 有哪些经验教训**
- **R69**（状态型文档必须绑定机器判据）本轮**再现第二个实例**；
- **R31／R34 家族**：**判据与机制不同步**（本轮 P3-1／P3-2 各出一次）；
- **C15**：改编号表（`BASELINE.md`）必须双向计数 ⇒ **本类改动不能顺手做**；
- ⇒ **共性**：**"纸面状态"与"机器状态"之间没有任何自动校验** —— 这是本项目**目前最系统性的缺口**。

**④ 有什么好处（尽量可衡量）**
- **可衡量**：**4 处过期点**从"没人知道"变成"有判据输出＋行号"；
- **可衡量**：**R67 复发检查**首次以"**重复成因条件**"的方式验证（跑 6 判据 ×多轮后工作树 0 处）；
- **确定的**：**收口信号机器可复现**（`--emit 1`），因此"2.3b 是否完工"**今后不需要靠记忆**。

**⑤ 不同方式方法的差异点**

| 方式 | 做法 | 成本 | 风险 | 收益 | 可逆性 |
|---|---|---|---|---|---|
| **A（本轮已做）** 只登记＋给建议 | 跑判据、读文件、写记录 | **低**（已完成） | 低（零改动） | 过期点**留痕且可复现** | 高 |
| **B** 顺手更新 `ROADMAP.md:59`＋`BASELINE.md:154` | 改 2 行 | 低 | **中**：① 触碰**两份治理文件**（夜间**明令禁止**）② `BASELINE.md` 是**编号表载体**，须过 C15 双向计数 ③ 一旦改错，**无人在夜里复核** | 纸面立刻转正 | 中 |
| **C** 先建"文档-机器一致性"判据 | 新写判据工具 | 中 | **高**：4 处未改 ⇒ 判据**当即常亮红** ⇒ 又一盏没人信的红灯 | 长期根治 | 中 |
| **D** 什么都不做 | — | 0 | **高**：误判进度、低估验证强度 | 无 | — |

> **为什么选 A**：① B 触碰治理文件（**夜间禁**）且 `BASELINE.md` 承载编号表（**C15 高危区**）；
> ② C 的**正确顺序是 B → C**（先转正、再上判据），**颠倒会立刻制造假红**；
> ③ A 把**证据与排序建议一次交清**，B／C 的具体措辞**白天一句话就能定**。

**⑥ 继续往前推进**
- 本项已闭环。**本夜 P3 三项全部完成**（见 §八 汇报）。
- **需要你一句话的（6 件）**：① 更新 4 处过期点（**含 C15 双向计数**）；② 是否建"文档-机器一致性"判据；③ 治理文件状态行的**时态约定**；④ P3-1 的 **R71 候选**是否登记、`--selftest` 是否入 CI；⑤ P3-2 的 **D-3/D-4/D-5**；⑥ **推送本夜 3 个 commit**。
- **下一步（白天）**：按你的裁定推进 **2.2 节四项更新** 与 **D-3/D-4 判据对齐**。

---

## 八、当夜汇报（**≤6 行 · 固定格式**）

**① 触发结果**：21:00 清单**未生成**（实测：`reports/2026-09-18_今夜P3清单.md` 不存在；`reports/` 目录 20:00 后**无任何新文件**；21:00 定时任务在本平台侧显示 **ACTIVE**，故**原因待查**）⇒ 按任务书第 1 条**退回 ROADMAP「可夜间跑 ✅」未完成项**自择，**23:00 本次正常开工**，择定 **3 项**（全部落在白名单内：测试补充/修复·清理/报告归档）。
**② P3 完成项**：**P3-1** 指针判据补 `--selftest`（十一侧）＋修死分支（证据：`--selftest` 改前 `exit 2`·④侧红 → 改后 **12/12 绿**；`syntax` 改前后均**错 0／引用 49**）；**P3-2** 判据工具全量回归（证据：**实跑 5/5、自检 6/6 全绿**；保真度批量模拟 **47 对→43 通过 / 4 例逐例归因**，无一例可定性为保真度缺口）；**P3-3** 清理核对＋过期登记（证据：pycache 入库残留 **0**、重复成因条件后工作树 **0 处**；`--emit 1` ⇒ **无剩余片**，对照出 **4 处过期事实点**）。
**③ 失败项**：**无**（3/3 完成）。⚠️ 但**登记待裁定 5 条**：D-1 判据死分支（**已修**）／D-2 中文路径截断（当前命中 0）／D-3 替换白名单滞后（R34 同族第 3 次）／D-4 判据缺「有意移除」档／D-5 判据无批量模式；另**新增**「治理文件内部自相矛盾（⑧ vs ⑩）」。**均如实登记、未掩盖、未降标准打绿。**
**④ 未推送 commit 清单**（`git log --oneline`，**均未 push**，本地 `ahead origin 3`）：
　`c18509d` 夜间 P3-1：机制 17 指针判据补 --selftest（十一侧）＋修「盘符绝对路径」死分支【未 push】
　`2086782` 夜间 P3-2：判据工具全量回归（6 实跑 + 6 自检 + 47 对批量模拟），4 例逐例归因【未 push】
　`（随本文件一并本地提交）` 夜间 P3-3：清理核对 ＋ 4 处过期事实点机器取证与登记（治理文件只登记不改）【未 push】
**⑤ 需你目验／放行项**：**放行 1 句**＝推送这 3 个 commit；**目验 1 处**＝`ROADMAP.md:59` 与 `BASELINE.md:154` 是否照 §2.4 更新（**建议先目验再动手**）；**决策 5 件**＝P3-1 的 R71 候选／`--selftest` 入 CI；P3-2 的 D-3 补登·D-4 加档·D-5 加 `--batch`；以及 **F（21:00 清单为何没生成）**。
**⑥ 边界**：**本地跑过**＝上述全部（含 6 判据实跑自检、47 对循环、`--emit 1`、pycache 与工作树核对）；**CI过**＝**无**（本夜**未触发任何 CI run**，报告内 CI 结论均为快照＋对 `ci.yml` 的静态阅读）；**真实跑过**＝**部分**（`resolve` 侧在本机真实私有根上只读跑通；**未跑 QEMU、未上真机**）；**未验证**＝P3-1 改动未在任何 CI 上执行过／A6 成对口径未与官方迁移清单逐条对齐／两个迁移判据能否入 CI 仍未验／`BASELINE.md:154` 属历史还是现状**未下断言**。
