【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-24 · v0.333 第四补（`云内核-C3-006`）· push 放行 ＋ **L6② 最小闭环实现** ＋ Q7 重新立项 ＋ `source` 枚举入底图

> ★ **版本与轮次（`T-050` 标名）**：**版本 `count = 379`（开工实测）→ `382`（收口 · 3 组提交）** ｜ **轮次 `v0.333`** —— 两者不等。
> ★ **指令序号（`T-060` 试用第 5 轮）**：**本轮 `云内核-C3-006`** ｜ **下一轮 `云内核-C3-007`** ｜ **下一轮基准 `count`＝382**。
> ★ **性质**：**放行 ＋ 最小实现（单一主类型）**。
> ★ **本件要解决的一件事**：★★ **"循环开环"迈出闭环第一步** —— **人工确认 → 写入既有 L7 哈希链账本 → 既有读回通道读回**（**零新端点 · 零内核改动**）。

---

## ① 保密标记（`C12` · 逐件分级 ｜ 链上限见 `security/chain_visibility.md`）

| 产出物 | 级别 | 说明 |
|---|---|---|
| 本报告 | 🔒 **可拷** | 无本机路径／端口值／二进制体积；实跑结果**已脱敏** |
| `npb-gateway/src/{l7_exec,http,lib}.rs`（L6② 实现） | 🔐 **屏蔽后拷** | 含判据顺序与账本结构（**拷前屏蔽**） |
| `docs/LAYER_BASEMAP_L0_L6.md`（`source` 枚举块） | 🔒 **可拷** | 枚举与效力口径 |
| `coordination/security/chain_visibility.md`（第 2 轮） | 🔒 **可拷** | 可见性域登记 |
| 实跑原始输出（**仓库外**） | ⛔／🔐 **不入库** | 含**端口号**（⛔）与七维读数 |

- ⛔ **绝不拷**：本机绝对路径／账号／内网 IP／**端口号**／**密钥与 token**／二进制路径与体积 —— ★ **本报告一律不列具体值**（★ 仓库为 **PUBLIC**；报告内端口处**只写"非默认端口"**）。
- 🔐 **屏蔽后拷**：函数签名／阈值常量／T0–T3 判定顺序／场域读数。
- 🔒 **可拷**：crate 与模块名／相对路径／测试计数／层级映射／治理编号。
- 🔓 **直接拷**：版本号／缺陷编号／机制条文／阶段口径定义。

> 【给用户的提示】本报告 **⛔ 具体值 0 处** ⇒ 拷给顾问时**无需额外屏蔽**。

---

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡（C3-006 / v0.333 第四补）────┐
│ 版本   count 379（开工）→ 382（收口 · 3 组提交）        │
│ 轮次   v0.333 ｜ 指令 云内核-C3-006 ｜ 下一轮 C3-007    │
│ 阶段   ROADMAP 口径·阶段二（可引导 ISO）·子任务 2.4     │
│ CI     run completed/success；三 job 全 success（含 CQ）│
│ 判据   15/15 PASS；只告警类：效力列 0／编号差集 3／      │
│        报告结构 0／其余 0                                │
│ ★闭环  L6② 路径 A「人工回执」**已通**（开环→闭环第一步）│
│ 决策   见 ⑪（4 项）                                      │
└─────────────────────────────────────────────────────────┘
```

## ③ 任务（含基准／定位／优先级）

| 项 | 内容 |
|---|---|
| **指令基准** | `count = 【自动生成并核实】` ⇒ **实测 379**（＝上轮收口值） |
| **总规划定位** | **两口径并存**：README 四阶段（产品视角）× ROADMAP 三阶段（工程视角）；**引用阶段号必写明口径** |
| **任务类型** | **放行 ＋ 最小实现（单一主类型）** |
| **优先级** | ① push 放行 ＋ 等 CI **P0** ｜ ② L6② 最小闭环实现 **P0** ｜ ③ Q7 重新立项 **P1** ｜ ④ `source` 枚举入底图 **P1** ｜ ⑤ 机制 31 第 2 轮 **P1** |

**★ 本轮读了底图／权威件哪几份、哪几行**

| # | 文件 | 行／范围 | 读它做什么 |
|---|---|---|---|
| 1 | `docs/LAYER_BASEMAP_L0_L6.md` | 533–547（§8.0）／573–597（**§8.4 ⟨补 17⟩ Q5/Q6/Q7**） | `source` 枚举回写点 ＋ Q7 原文 |
| 2 | `docs/LAYER_DETAILS.md` | 325–332／343–356／590 | L6② 定义（复核） |
| 3 | `archive/reserved-interfaces/l6_interface.rs` | 全文（35 行） | `WorldReceipt` 四态契约 |
| 4 | `npb-gateway/src/l7_exec.rs` | 1–90（常量／`Link`／`link_hash`）／230–300（账本方法） | **实现落点与链语义** |
| 5 | `npb-gateway/src/http.rs` | 384–391（`audit.txt`）／497–520（`POST /v1/tasks`）／591–600（health） | **既有端点复用点** |
| 6 | `npb-gateway/src/lib.rs` | 455–480（`health_json_counted`） | 计数扩展 |
| 7 | `coordination/security/chain_visibility.md` | 全文 | 机制 31 第 2 轮 |
| 8 | `coordination/BASELINE.md` | §七十二 | 承接 |
| 9 | `coordination/reports/2026-09-24_…L6②调研稿`（discussions） | §二–§六 | 风险 5 条逐条对照 |

## ④ 状态

**状态**：**完成**（①–⑤ 全部达成；4 项待裁）

## ⑤ 验收对照（9 条 · 逐条实测）

| # | 验收标准（指令原文要点） | 结论 | 实测 |
|---|---|---|---|
| 1 | push 完成，CI 三 job 全绿（`T-038`） | ✅ | push `f0027ad..4aece46`（exit 0）；run **completed/success**；三 job 全 `success` |
| 2 | L6② 最小闭环：人工确认 → 账本写入 → `audit.txt` 读回成功；`cargo test` ≥40 | ✅ | **6 步全绿**（见 §L6② 实跑）；`cargo build` **rc=0**；`cargo test -p npb-gateway` **rc=0**，**41 passed**（**40 → 41**） |
| 3 | Q7 重新立项登记完成，原裁定保留留痕 | ✅ | 台账 **§73.3** 登记「**Q7 路径 A 未落地，重新立项**」；★ **底图原文不回改**（`C19`）＋ 注明"原写本轮落地，实测未落地，经用户裁定重新立项" |
| 4 | `source` 值枚举入底图，`D16'` 一次走完 | ✅ | 底图 §8.4 ⟨补 17⟩ 末尾**新增枚举块**；**三段留痕齐**；判据 F `--update-baseline` ⇒ PASS |
| 5 | 机制 31 第 2 轮：`C1`／`C2` 补登记（或注明信息不全） | ✅ | 逐列标注「**信息不全**」（不臆填）＋ 写明补登记条件；**返回件过上限检查＝未发现 ⛔** |
| 6 | 引用编号全部带登记状态 | ✅ | 机制 31／`T-049`／`T-060`–`T-062`（**均已登记**）；`MECH` 告警 **0** |
| 7 | `C15` 复算通过，编号表连续无缺号无重号 | ✅ | 机制 31／C 21／T 62／TERM 14／tools 15 **±0**｜底图 44（**只改内容**） |
| 8 | 判据 15/15 PASS | ✅ | `run_all_checks.py` rc=0，**15/15** |
| 9 | 下一次返回第 0 行含 9 步阅读指令 | ✅ | 本件第 1 行起即 9 步指令 |

## ⑥ 未完成项

| # | 项 | 说明 |
|---|---|---|
| 1 | **路径 B（真实外部协议）未做** | 触 **T2**（须逐次确认）⇒ 列为待裁 ⑪-① |
| 2 | **"真实世界回执"值名与效力未定** | 底图已标 🟡（**无来源 · 不猜**）⇒ 待裁 ⑪-② |
| 3 | **`baseline_immature` 对齐仍为底图未决项** | 本轮**未越界代答** |
| 4 | **端点表 `用途` 列未补「含人工回执」** | ★ **本轮未动端点表**（守禁区④）⇒ **如实登记为连带缺口**（⑪-④） |

## ⑦ 真实缺陷（本轮自捕获 2 条）

| # | 缺陷 | 严重度 | 处置 |
|---|---|---|---|
| ① | ★★ **底图已裁、路径未修（承上轮）**：Q7 原文写"路径 A **本轮落地**"，实测**零落点** | **P2** | **本轮以"重新立项 ＋ 当场实现"结清**（73.2／73.3）；★ **教训**：**裁定含"本轮/本期落地"时，须当轮在台账留落地证据**，否则**声明静默过期**（`R69／R72` 家族） |
| ② | ★ **新接口的"归一"若静默，会掩盖调用方错误**：`status` 非四态时如何处置 | P3 | **已防**：归一为 `accepted` **同时** `status_known=false` **如实回报**，并在运行日志记 **WARN** 级（**不静默**） |

## ⑧ 三量台账（**四量**）

| 量 | 本轮内容 |
|---|---|
| **存量** | 机制 31 ／ C 21 ／ T 62 ／ TERM 14 ／ tools 判据 15 ／ `ci.yml` step 58 ／ 底图 44 份 ／ reports 178 ／ `BASELINE` 顶层节 111 |
| **变量** | ① `cargo test -p npb-gateway` **40 → 41 passed** ② 底图 `LAYER_BASEMAP_L0_L6.md` 内容变更（**文件数不变**）③ reports **+1** |
| **补充增量** | ① **L6② 实现**（`Receipt`／`confirm_receipt`／`receipts_text`／`normalize_receipt_status`）② **`POST /v1/tasks` 人工确认入口**（复用）③ **`GET /v1/audit.txt` 追加回执区**（复用）④ **`health` 增 `receipts` 计数** ⑤ **底图 `source` 枚举块** ⑥ **Q7 重新立项登记** ⑦ **`chain_visibility.md` 第 2 轮** ⑧ **本报告** |
| **创新增量** | ★★ **"世界回执"从"裁定里有、代码里无"变成"代码里可跑"** —— 此前 **L6② 零代码落点**、系统按底图自陈处于**「未显化」**；本轮落成后，**人工确认可产生合法 `WorldReceipt`（`source="human"`）并进入哈希链账本、可读回** ⇒ **闭环第一步成立**。★ **方法论增量**：**"零新端点"是可以做到的** —— 把新语义**挂到既有通道**（`/v1/tasks` 入口 ＋ `audit.txt` 读回），**避免触发机制 24 与底图回写**，这是一条**可复用的"低成本接新语义"路子**。 |

### 8.1 `C15` 双向计数（同脚本同口径）

| 项 | PRE（`HEAD`＝`4aece46`） | FINAL | 差值 | 预期 |
|---|---|---|---|---|
| count | 379 | **382** | **+3** | ✅ 3 组提交 |
| 机制数（表口径） | 31 | 31 | 0 | ✅ |
| C 数 | 21 | 21 | 0 | ✅ |
| T 数 | 62 | 62 | 0 | ✅ |
| TERM 数 | 14 | 14 | 0 | ✅ |
| tools 判据数 | 15 | 15 | 0 | ✅ |
| `ci.yml` step 数 | 58 | 58 | 0 | ✅ |
| 底图 .md 份数 | 44 | 44 | **0** | ✅ 只改内容、未增删文件 |
| reports 文件数 | 178 | **179** | **+1** | ✅ 本报告 |
| `BASELINE` 顶层节 | 111 | **112** | **+1** | ✅ §七十三 |

## ⑨ 边界说明（四类 · 齐全）

- **本地跑过（M）**：`cargo build -p npb-gateway` **rc=0**｜`cargo test -p npb-gateway` **rc=0 / 41 passed**｜★ **L6② 闭环实跑**（真实网关进程 ＋ 5 个 HTTP 调用 ＋ 1 个对照用例）｜判据全跑 **15/15**｜`C15`。
- **CI 过（CB／CQ）**：★ **本轮有** —— run **completed/success**；三 job 全 `success`（含 **`CQ`：QEMU 真实引导**）。★ 按 `C20`：**run 号只记日报、不入库**。
  ★ **覆盖口径（如实）**：**本轮 CI 覆盖的是"开工前"状态**（推送发生在实现之前）⇒ **L6② 实现本身尚未进 CI**（将在下一轮第一节放行）。
- **真实跑过（H）**：**无**（实跑＝本机 M 层进程）。
- **未验证**：★ **L6② 实现未进 CI**（同上）｜★ 机制 31 **无机器判据**（人工背书）｜★ 探针 `at` 真时间戳＝**暂不处理**（裁定乙·维持）｜★ **路径 B 未做** ⇒ **真实世界回执**仍未验证。

## ⑩ 决策评估（七要素）

| 要素 | 内容 |
|---|---|
| **评估** | 五项全部达成；`P0` 两项（放行＋实现）**均有实测**；L6② **从裁定走向可跑** |
| **优势** | ① **闭环第一步成立**（可实跑、可读回、有测试）② **零新端点／零内核改动**（不触机制 24、不触底图端点表、合机制 21）③ **正反对照齐**（普通投递不写账本）④ **防自证循环写死在代码里**（`source=human` ＋ 不参与校准） |
| **劣势** | ① 本轮实现**未进 CI**（下轮补）② 端点表 `用途` 列**存在连带缺口**（未动以守禁区） |
| **风险** | ① **人工回执被读成"世界真的接受了"** ⇒ **须靠口径与文档持续守**（Q5／Q6 已防，但**传播面在外**）② 端点表 `用途` 缺口若长期不补，**机制 24 只能保路径、保不了语义** |
| **机会** | 若下一轮把"端点表 `用途` 补注"并入一次 `D16'`，**语义层也进机器保护**（**可被证伪**：改 `/v1/tasks` 用途后端点表判据仍绿 ⇒ 说明只保路径） |
| **建议** | ① 下一轮第一节放行本轮实现 ② 端点表 `用途` 缺口与 `baseline_immature` **同批一次 `D16'`** ③ 机制 31 **第 3 轮出结论** |
| **等待决策** | 见 ⑪（4 项） |

## ⑪ 需要用户决策（4 项）

| # | 待裁项 | 选项 | 推荐 |
|---|---|---|---|
| ① | **路径 B（真实外部协议）是否立项** | (甲) 立项（触 T2） (乙) 暂不 | **乙**（先把路径 A 跑 3 轮再说） |
| ② | **底图"真实世界回执"值的名称与效力** | (甲) 由发起人给定值名与效力 (乙) 维持 🟡 | **甲**（无来源不代答） |
| ③ | **`baseline_immature` 对齐** 是否与④同批 | (甲) 同批一次 `D16'` (乙) 分开 | **甲**（少开一次冻结） |
| ④ | **端点表 `用途` 列是否补「含人工回执」** | (甲) 与③同批 `D16'` 补 (乙) 不补（接受语义缺口） | **甲** |

## ⑫ 下一步建议

1. **下一轮第一节放行本轮实现**（`P0`，固定首项）⇒ 让 **L6② 进 CI**。
2. **裁 ②③④ 同批一次 `D16'`**（`P1`）。
3. **机制 31 第 3 轮出结论**（`P1`，起点 v0.333 ⇒ **≈v0.336 必须结**）。
4. **下一轮基准 `count`＝382`**。

## ⑬ 启动回执

> **已读 `coordination/CHARTER.md`**（§三 机制表 1–31）。
> **当前状态**：`count = 379`（开工）→ **382**（收口）｜阶段口径＝**README 四阶段 / ROADMAP 三阶段 并存**。
> **指令序号**：`云内核-C3-006` ｜ **下一轮** `云内核-C3-007` ｜ **下一轮基准 `count`＝382**。
> **本次任务**：push 放行 ＋ L6② 最小闭环 ＋ Q7 重新立项 ＋ `source` 枚举入底图 ＋ 机制 31 第 2 轮。
> **是否继续**：**执行完毕**。

---

## ★ L6② 闭环实跑结果（**脱敏** · 逐项附【本地跑过／CI过／真实跑过／未验证】）

| 步 | 动作 | 实测（脱敏） | 归属 |
|---|---|---|---|
| 1 | `GET /v1/health`（**前**） | `receipts.count = 0`；`actions.ledger_len = 0` | **本地跑过（M）** |
| 2 | **人工确认** `POST /v1/tasks`（体带 `"source":"human"`） | `{"accepted":true,"receipt":{"seq":1,"status":"accepted","source":"human","status_known":true}}` | **本地跑过（M）** |
| 3 | **第二条 · 未知 status** | 归一为 `accepted`，且 **`status_known=false`**（★ 如实回报"已归一"） | **本地跑过（M）** |
| 4 | **读回** `GET /v1/audit.txt` | ① **动作账**：`#1 … receipt:human · 世界回执·人工确认·accepted (prev=0x0)`／`#2 … (prev=0xb4ad7b7a0c1578e4)` ⇒ **哈希链相接**<br>② **回执区**：`#1 entry=demo-entry-1 status=accepted source=human hash=0xb4ad7b7a0c1578e4`／`#2 … hash=0x47d14ca1aa5d3f4a` | **本地跑过（M）** |
| 5 | `GET /v1/health`（**后**） | `actions.ledger_len = 2`／`ledger_head = 0x47d14ca1aa5d3f4a`（**＝第 2 条回执 hash · 可回指**）／`receipts.count = 2` | **本地跑过（M）** |
| 6 | **对照用例**：普通任务投递（**不带** `source=human`） | **不写账本**（回执数仍为 **2**） | **本地跑过（M）** |

> ★ **口径（防拔高）**：本闭环的"回血"是**人工回执**（`source="human"`）⇒ **只解除「未显化」状态，不证明「显化有效」**（Q5）⇒ **总图"开环"结论仍未被推翻**；**真实世界回执（路径 B）未做**。

### ★ 调研稿风险 5 条 · 逐条对照（实现后实测）

| # | 调研稿风险 | 实现后实测 | 判定 |
|---|---|---|---|
| ① | 把"人工确认"读成"世界真的接受了" ⇒ **自证循环** | **代码写死 `source = "human"`**；账本 note 与回执区**明写"不参与 L5 校准"**；**未加任何校准路径** | ✅ **已防** |
| ② | 改账本格式**破坏哈希链** | **只追加**、未改既有字段语义；`ledger_verify()` 在**新测试里断言**（回执入链后**仍可验**） | ✅ **未发生** |
| ③ | 误把"回执"当"诊断正确"的证据 | 代码注释／回执区文本／底图口径**三处**明写"只解除未显化" | ✅ **已防** |
| ④ | 🟡 `source` 枚举未定 | **本轮部分解决**：枚举入底图（`human` 已裁定；其余 🟡 待裁） | 🟡 **部分结** |
| ⑤ | 🟡 `baseline_immature` 对齐未写 | **仍未决**（**未越界代答**） | 🟡 **悬** |
| **新** | ★ **未知 status 归一会掩盖调用方错误** | 归一**同时**回报 `status_known=false` ＋ 日志 **WARN** ⇒ **不静默** | ✅ **已防**（新增风险，已处置） |

---

## 接续包数据块（**增量** · 置于尾部、`KS_` 伴行之前）

> **数据层（WorkBuddy 填）** ｜ **顾问层（16–18 项）由顾问填**。

**0 元信息**：2026-09-24｜触发＝指令指定（〇之三）｜链号 **`C3`**｜**增量**。
**1 项目身份**：云内核（元内核）项目 · 电信号版；共生而非替代；终态＝可安装 OS 形态；不替代用户原系统、内核不主动夺胎。
**2 三方关系**：决策人＝用户｜执行者＝WorkBuddy｜顾问＝**外部专家（`C3`）**｜指令格式＝**十必带**（`T-060`，试用第 5 轮）。
**3 当前位置**：阶段＝ROADMAP 口径·阶段二·子任务 2.4｜`count`＝**379 → 382**｜轮次 v0.333｜未推送＝**本轮尾巴 3 组本地提交**（禁区⑥）。
**4 全景状态卡（带证据）**：L1–L5 ✅M｜L6①✅M｜**L6② 🚧 路径 A 已通（M；本轮实跑）· 路径 B ❌**｜**L7 T1 ✅（已受 CI 覆盖）**｜L7 T2·T3 ❌｜2.4 边界层 ✅**CQ**｜效力列 🟡 **0**。
**5 命名族**：见 `docs/LAYER_DETAILS.md §3.2.1`。
**6 规则与机制**：机制 **31**（试用第 2 轮·人工承载）｜C **1–21**｜判据 **15**（入 CI **11**；仅本机 **4**）｜报告十四项｜能力矩阵 **M／CB／CQ／H**｜`C12` ＋ **指令侧密级义务（`T-060`）**｜七要素。
**7 三量台账**：见 §⑧（四量齐）。
**8 最近裁定（5 条）**：①L6② 走方案甲（复用账本，零新端点）②Q7 **重新立项**（不追认已落地）③`source` 枚举入底图（`D16'` 一次）④机制 31 第 2 轮（`C1`/`C2` 信息不全留空）⑤探针 `at`＝暂不处理。
**9 待办**：见 ⑫（4 项）。
**10 待裁定**：见 ⑪（4 项）。
**11 风险**：`R-A` 人工回执被误读为"世界已接受"（P2，靠口径持续守）｜`R-B` 端点表 `用途` 语义缺口（P2）｜`R-C` 本轮实现未进 CI（P1，下轮首项）。
**12 权威文件路径**：底图＝根 `README.md` ＋ `docs/`｜治理＝`coordination/`｜链可见性登记＝`coordination/security/chain_visibility.md`。
**13 边界说明**：见 ⑨（四类齐）。
**14 指令序号机制**：链号 `C3`｜本轮 `云内核-C3-006`｜下一轮 `云内核-C3-007`｜下一轮基准 `count`＝**382**。
**15 顾问接手须知**：阅读指令 9 步（第 0 行）｜出件＝**十必带**｜★ **脑补清单**＝①四步 vs 五步未归一 ②L8–L10 已冻结 ③"接管级 L0–L4"与架构同号不同义 ④六波罗蜜无来源｜★ **已知缺口**＝路径 B 未做／L4·L5 控制律无编号／机制 31 无判据／`baseline_immature` 未决／端点表 `用途` 语义缺口。

**16 当前最需要处理的三件事**：（**顾问填**）
**17 顾问审核结论**：（**顾问填**）
**18 下一轮建议**：（**顾问填**）

---

### INTEL 状态表（依 `T-042` · 置于十四项之后）

| 编号 | 来源 | 状态 | 本轮触发否 | 处置 |
|---|---|---|---|---|
| `INTEL-001` | 外部信息（观察中） | 观察中 | **未触发**（触发＝企业注册完成 ＋ 阶段三启动；均未发生） | 维持观察中；**不为"好看"硬触** |
| `INTEL-002` | 外部信息（观察中） | 观察中 | **未触发** | 同上 |

> **J1** ✅｜**J2** 两条均未触发 ⇒ 不违规｜**J3／J4** 无待准备／准备中项 ⇒ 不违规。

### 告警行（`B2` ＋ `C1`）

- **`B2`（本机判据告警）**：**15/15 PASS**｜只告警类：**效力列 0**（含**新入底图的 🟡 表**——已带首次标记 ⇒ 不告警）｜编号差集 **3**（`C22`／`T-00`／`TERM-999`，**历史元引用**，未新增）｜报告结构 **0**｜其余 **0**。
- **`C1`（CI 告警）**：**无** —— run `completed/success`，三 job 全 `success`（按 `C20` run 号只记日报）。

### 台账内联（依 §54.5）

> **本轮报告名**：`coordination/reports/2026-09-24_v0.333第四补_L6②最小闭环与Q7重新立项及source枚举.md`
> **对应台账节**：`coordination/BASELINE.md` **§七十三**。
