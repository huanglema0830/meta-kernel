【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# L3-V1 梳理稿 · 底图对 L3 的描述 vs 实现实际（2026-09-20 · v0.254）

> **性质**：**只出稿，不改底图**（用户指令 §一.3「出梳理稿（只出稿，不改底图）」）。
> **对象**：`docs/LAYER_ARCHITECTURE.md`（L3 段）＋ `README.md`（仓库结构）**vs** 实现 `npb-gateway/src/*`。
> **方法**：**底图逐字引用**（附行号）＋ **实现逐字/计数复核**（可复跑命令）；差异按**「已收口 / 残留」**分类。
> **定性纪律**（C17／`CONSTRAINTS.md:355`）：「底图落后于实现」**性质是"落后"不是"虚假"** ⇒ **本稿不使用诚信类定性词**。

---

## 0. 一句话结论

> **L3-V1 的「主体」（端点清单在三份底图里不一致）已于 2026-09-18 收口**（Q12／Q13／Q14 ＋ 机制 24），
> **但「底图对 L3 的描述」仍有 3 处落后**（定义句／快照 schema 值／L7 后驱动），
> **三处均属"文档侧落后、实现侧正确"** ⇒ **建议改底图（补写），不改实现口径**。**底图改动须你裁定 ⇒ 本稿零改动。**

---

## 1. L3-V1 的原始定义（逐字，含出处）

出处：`coordination/discussions/2026-09-18_汇编正文_第1批_设计定位层.md` **§5.3 ★ 漏洞**（源行 339–343）：

```
- **L3-V1｜端点清单在三份底图里不一致。**
  - ⟨源 LAYER §4 L3, 行182⟩：`/v1/push | state | events | persist/* | probe | probe/usb | health`（七项）
  - ⟨源 README §仓库结构, 行114⟩：`/v1/push|state|events|persist|health`（五项，**无 probe / probe/usb**）
  - ⟨源 GENE_LIBRARY_DESIGN §8, 行252⟩：另有 `POST/GET /v1/genelib`
  ⇒ **没有一份权威端点表**。这是本层最直接的漏洞：新人照 README 实现会漏掉探针端点与基因库端点。
```

**同一汇编 §5.1 的残留项**（源行 330）：`这 7 个端点**未进 ⟨源 LAYER §4 L3 行182⟩ 的端点表**，且 docs/ 全库命中 0` ⇒ 归入 L3-V1、**升为第 1 优先**。

**同层另两项**（源行 334–346）：
- **L3-U1｜快照 schema 版本号无具体值与演进规则**（底图只说"版本化"；旁证：L5 侧有 `schema=2`，L3 侧没有）。
- **L3-V2｜"不空转"与 L7 之后的"场域自驱动"存在张力**，底图只给了一半解释；**"L7 之后 L3 由谁驱动、tick 从哪来"底图未写**。

---

## 2. 底图说 L3 是什么（逐字）

### 2.1 `docs/LAYER_ARCHITECTURE.md` §4 · L3（源行 181–186）

```
#### L3 · 网关·剥离层（npb-gateway）· **转录层（RNA）**
- 功能：HTTP+SSE 传输接口；单一写者线程模型。**端点列表见下方 §4.L3 权威表。**
- 零：L2 输出流（内核读数与指令 JSON）。
- 扰动：外部请求/SSE 订阅/持久化调用/探针上报。
- 新增约束：**剥离**——只转语义：不缓存业务态、指令只转发不解释、不空转驱动、快照 schema 版本化。
- 仓库：`npb-gateway/src/*`。
```

### 2.2 底图总表行（源行 41）

```
| L3 | 网关·剥离层（基因内核 · **转录层 RNA**） | 传输接口：剥去传输壳，只转发语义 | L2 输出流 | 外部请求/SSE 订阅 | **剥离**：只传语义、剥离缓存壳；不解释不改写 | `npb-gateway/` |
```

### 2.3 `README.md` 仓库结构（现文 L113–L115）

```
npb/                基因内核 · NPB 桥接器（转录层 RNA；C ABI + wasm32 + 确定性自检摘要）
npb-gateway/        基因内核 · 应用网关（转录层 RNA；HTTP+SSE；**端点表权威＝
                    docs/LAYER_ARCHITECTURE.md §4.L3，共 23 路径**；本行原列举的 5 项为历史快照，非权威）
```

> ⚠️ **README 行 114 的旧文（5 项列举）已被替换**，现**只作指针**——这正是 Q12/Q14 收口的直接痕迹。

---

## 3. 实现里 L3 实际做了什么（复核）

### 3.1 文件与体量（复核命令：`wc -l npb-gateway/src/*.rs`）

| 文件 | 行数 | 角色 |
|---|---|---|
| `main.rs` | 58 | 二进制入口（解析 PORT/`--ui`；`http::spawn_on`；主线程 sleep 保活） |
| `msg.rs` | 222 | **消息通道**（`/v1/msg`） |
| `selfmon.rs` | 382 | **元内核自监控面**（report/alerts/tasks） |
| `lib.rs` | 561 | 网关核心（单一写者／快照／持久化） |
| `l7_exec.rs` | 637 | **L7 本地执行器**（actions/grants/execute/rollback/audit） |
| `http.rs` | 1057 | HTTP+SSE 路由与处理 |
| **合计** | **2917** | 6 文件 |

### 3.2 路由实况（复核命令：`grep -n '"/v1' npb-gateway/src/http.rs`）

**26 行（method+path）／去重路径 23** —— 与底图 §4.L3 权威表**逐行一致**。

| 分类 | 行数 | 路径 |
|---|---|---|
| **L3 本体** | 10 | `push`｜`state`｜`events`｜`persist/snapshot`｜`persist/restore`｜`health`｜`probe`(POST/GET)｜`probe/usb`(POST/GET) |
| **借道 L3 · 基因库** | 2 | `genelib`(POST/GET) |
| **借道 L3 · L7 执行闭环** | 7 | `actions`｜`grants`｜`grant`｜`revoke`｜`execute`｜`rollback`｜`audit.txt` |
| **借道 L3 · 元内核自监控面** | 5 | `report`｜`report.txt`｜`alerts`｜`tasks`｜`tasks.txt` |
| **借道 L3 · L6/L7 消息通道** | 2 | `msg`｜`msg.txt` |

⇒ **L3 进程宿主 3 个「有状态」服务模块**：`selfmon.rs`(382) ＋ `msg.rs`(222) ＋ `l7_exec.rs`(637) ＝ **1241 行，占 L3 总代码 42.5%**。

---

## 4. 差异总表（底图说 vs 实现做）

| # | 面 | 底图说 | 实现实际 | 判定 |
|---|---|---|---|---|
| D1 | **端点清单** | 原三份不一致（7 项／5 项／＋genelib） | **26 行／23 路径**；README 已改为指针 | ✅ **已收口**（Q12／Q14 ＋ 机制 24，已入 CI） |
| D2 | **层归属** | 原无「借道 L3」概念（msg/report/alerts/tasks 族底图 0 定义） | **§4.L3.1 已补**（Q13 选 P-b：另有层归属、借道 L3 传输） | ✅ **已收口** |
| D3 | **L3 定义句** | 「HTTP+SSE 传输接口；**单一写者线程模型**」＋「**剥离**——只转语义：**不缓存业务态**…」 | L3 进程**实宿主 1241 行有状态服务** | ⚠️ **残留**（落后） |
| D4 | **快照 schema** | 「快照 schema **版本化**」（**无具体值**）——L3-U1 | 实现内实体 `"schema":1`（`http.rs` L370／L380／L707） | ⚠️ **残留**（落后） |
| D5 | **L7 后驱动** | §4.1 行 340–344：L7 前 push 驱动／L7 后**场域驱动**；「不空转禁的是**无源空转**」 | **L7 T1 已实现**（v0.109） ⇒ **"L7 之后"时点已到**，但 **tick 来源未写**——L3-V2 | ⚠️ **残留**（落后） |

### 4.1 关于 D3 的精确表述（避免定性过头）

底图**并非"没写"**——`§4.L3.1`（Q13）**已写明**「借道 L3」四组及其实际归属（自监控面代码位 `npb-gateway/src/selfmon.rs`、消息通道代码位 `msg.rs`）。
**残留的是**：**§4 的「L3 功能定义句」（行 182／185）本身未加限定** ——
读者若**只读定义句**，会得出「L3 只做传输、不缓存业务态」；而事实是 **L3 进程同时是本项目**自监控面／消息通道／L7 本地执行器**的宿主**。
⇒ **属"定义句未随实现演进补限定"，不属"底图错误"**（C17：落后 ≠ 虚假）。

---

## 5. 建议：改底图 vs 改实现口径

### 5.1 结论：**改底图（补写），不改实现口径**

| 方案 | 内容 | 成本 | 风险 | 收益 | 可逆 |
|---|---|---|---|---|---|
| **① 改底图（推荐）** | 底图 L3 段**补三处**：定义句加限定／补 schema 值／补 L7 后 tick 源 | 低（3 处补写） | 低（纯增，不改旧文） | 顾问/新人读 L3 定义不再误判 | ✅ 可逆 |
| ② 改实现口径 | 把 `selfmon`／`msg`／`l7_exec` 移出 L3 进程 | **高**（拆进程／改部署） | **高**（破坏"网关单进程"与已发布端点） | — | ❌ 难逆 |
| ③ 维持现状 | 只留本稿 | 零 | 中（D3/D4/D5 继续落后） | — | ✅ |

**为什么①不是②**：实现侧**是对的**——端点齐备（26 行 ⊆ 权威表，CI 判据实证）、层归属清晰（§4.L3.1）、代码可复核；
落后的**只在文档侧的"定义句"**。**改实现＝为解决文档问题动真代码**，收益为负。

### 5.2 ① 的具体改法（**拟**，待你裁定；本轮不执行）

| # | 位置 | 拟补（**只增不改**） |
|---|---|---|
| a | `LAYER_ARCHITECTURE.md` L3 定义句（行 182／185） | 追加一句：`★ 2026-09-20 补：L3 进程**亦宿主**元内核自监控面／L6·L7 消息通道／L7 本地执行器（**借道 L3**，见 §4.L3.1）；上述"剥离·不缓存业务态"约束**只约束 L3 本体**。` |
| b | 同文件（行 185「快照 schema 版本化」） | 追加：`（**当前值 ＝ 1**；复核：`npb-gateway/src/http.rs` 内 `"schema":1`）` |
| c | 同文件 §4.1（行 340–344） | 追加：`★ L7 T1 已实现（v0.109）⇒"L7 之后"时点已到；**L3 的 tick 源＝？**（L3-V2 仍未答）` |

> ⚠️ **不写死**：a/b/c 中的行号来自**本稿取数时快照**；落笔须**以符号名定位**（R75）。

### 5.3 归属任务

| 项 | 归属 | 状态 |
|---|---|---|
| D1／D2 | Q12／Q13／Q14 ＋ 机制 24 | ✅ 已收口 |
| D3／D4（a／b） | **本轮 1.3 梳理 → 升格为待裁定** | ⚠️ 待你点头后落底图 |
| D5（c） | **L3-V2**（原已识别未决项） | ⚠️ 待裁定 |

---

## 6. 边界说明（四类）

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | 实现侧**逐文件 `wc -l` ＋ 逐路由 `grep`**实测；底图**逐字引用**（附行号） |
| **CI 过** | **不适用本轮**（零代码改动）；⚠️ 但 **D1 已由 CI 判据（机制 24）实证** |
| **真实跑过** | **不适用**（L3 属宿主态，无 boot/裸机侧） |
| **未验证** | D3／D4／D5 **均未改底图**（待裁）；② 底图 a/b/c 的**最终措辞**未定 |

## 7. 三量台账

| 量 | 内容 |
|---|---|
| **存量** | `docs/LAYER_ARCHITECTURE.md` §4 L3（行 181–249）｜`README.md` L113–115｜实现 `npb-gateway/src/*`（2917 行）｜汇编 §5.1–§5.4 |
| **变量** | **本轮 0**（只出稿，不改底图、不改实现） |
| **补充增量** | **L3-V1 状态订正**：主体（端点不一致）**已收口**；残留 3 处（D3／D4／D5） |
| **创新增量** | **归属判据**：同一"落后"须**先分「主体／残留」**——主体收口 ≠ 整条关闭（防"一处修好＝全部关闭"的假关单，**R87 同族**） |

> **保密**（C12）：本稿 **🔓 公开**；**⛔ 机密 0 处**（无密钥／账号／本机绝对路径／内网 IP）；**🔐 算法层 0 处**。
> **底图引用**（D50／D52）：本稿**读底图的地方** ＝ `README.md` L108–L120 ＋ `docs/LAYER_ARCHITECTURE.md` L41／L147–L260；**L3 实现事实来自 `npb-gateway/src/` 现场复核**（C18）。
