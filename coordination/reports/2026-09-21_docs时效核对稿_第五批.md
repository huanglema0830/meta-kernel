【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# `docs/` 时效核对稿 · **第五批**（定向深核 · 2026-09-21 · v0.273）

> **性质**：**只出稿，不改文件**（用户 §五.1「只出稿，不改文件」）。
> **保密标记**（C12）：🔓 公开。⛔ 机密 **0**、🔐 保密 **0**。
> **边界**：**未运行渲染程序**；"已接线"判定＝**只读调用点检索**（项目范式：**函数存在 ≠ 被调用**）。

---

## 0. 本批口径（**深核对象＝第三批标注的"同族未授权项"**）

- 第三批（`…_docs时效核对稿_第三批.md`）只落地了**授权的 4 处**；其中 `FIELD_RENDER_SOURCELESS_DESIGN` 的
  **§3#5／#6／#7** 与 **§4 #4／#5／#6／#7** 被**标注为"同族、未授权 ⇒ 留待裁定"**。
- **本批＝把这批"同族项"逐条取证**（**只出稿**；改底图须授权）。

## 1. ★ 结论：**4 处新过期项**（实证）

### P1 · `FIELD_RENDER_SOURCELESS_DESIGN.md` **§3#5**「四元组接入呈现决策」——**已过期**

- **原文**：`| 5 | 四元组接入呈现决策 | 🟡 **部分**（\`modulate_gabor\` 已接；尚未进渲染管线） |`
- **实测**：`modulate_gabor` 在 **`host/field-render` 有真实调用点**（`src/main.rs` L812／L975／L2175；`src/window.rs` L87／L558／L741／L978）⇒ **已进渲染管线**。
- **应改为（建议）**：`✅ **已接**（四元组经 \`modulate_gabor\` 进入 field-render 渲染管线；原「尚未进渲染管线」为 v0.114 时点快照）`。

### P2 · **§3#6**「探测策略未落地」——**已过期**

- **原文**：`| 6 | 探测策略未落地 | 🟡 **部分**（内核 \`decide_probe\` 已实现；尚未接呈现/宿主） |`
- **实测**：`decide_probe` **被宿主调用**（`host/field-render/src/window.rs:844`：`let d = decide_probe(conf, diag.deviation, stale);`）⇒ **已接宿主/呈现**。
- **应改为（建议）**：`✅ **已接**（\`decide_probe\` 经 field-render \`window.rs\` 接入；原「尚未接呈现/宿主」为时点快照）`。

### P3 · **§3#7**「Runtime 依赖未消除」——**部分过期**

- **原文**：`| 7 | Runtime 依赖未消除 | ⏳ **未做**（要等第三/四层；当前老笔记本仍需 Runtime 或降级 B） |`
- **实测**：`host/field-render/Cargo.toml` 自陈「**不依赖 WebView2 的场域渲染**」，且 CI 有专门 job ⇒ **native 渲染路径已不依赖 Runtime**。
- **应改为（建议）**：`🟡 **部分**（**native 渲染路径（field-render）已不依赖 Runtime**；WebView2 宿主保留为降级/对照；老笔记本仍可用降级 B）`。

### P4 · **§4 #4 渲染**「⏳ 第二阶段（pHash 对比方案待实现）」——**部分过期**

- **原文**：`| 4 渲染 | 能渲染/随场域变化/无 NaN/帧率 | ⏳ 第二阶段（pHash 对比方案待实现） |`
- **实测**：`.github/workflows/ci.yml` 的 **`宿主 field-render（Windows 构建 + 客观验收）`** job 已包含
  ① 构建（`field-render.exe` 存在性断言）② **单元测试 + 显式断言**（"test result: ok. N passed; 0 failed" **且 `N ≥ 30`**）
  ③ C9 门禁（unsafe 白名单 + 禁 `transmute`）。⇒ **渲染路径已在 CI 有客观验收**。
- **应改为（建议）**：`🟡 **部分**（**CI 已做构建 + 单测 ≥30 + C9 门禁的客观验收**；"随场域变化 / 帧率"的**逐项断言**与 **pHash 对比**待核/待实现）`。
- **⚠️ 置信度说明**：本项**只核到 job 名与其断言语句**；**未逐行核** CI 是否对"无 NaN／帧率"设了断言 ⇒ **按"部分"而非"已完成"**。

## 2. 核过但**维持原判定**的项（**不算过期**）

| 位置 | 原文 | 核验结论 |
|---|---|---|
| §4 **#5** | `不依赖 WebView2（核心验收）｜⏳ 第三阶段` | **维持 ⏳** —— "设计上不依赖"已成立，但**"无 Runtime 启动 / \`dumpbin\` 无 DLL"这个验收动作**未见执行证据 |
| §4 **#6** | `端到端｜🟡 现由 WebView2 宿主提供` | **维持 🟡** —— `host/sky-browser` 已编译，但其"打开网页/交互/多标签/下载"的**端到端验收**未在本轮核 |
| §4 **#7** | `性能｜🟡 工作台已达标；宿主未测` | **维持 🟡** —— 宿主（field-render／sky-browser）**性能未测**，与原文一致 |
| §3 表头 | `尚未做的（诚实清单）` | **成立**（表内 #1/#2 已 ✅、#3/#4 已于 v0.267 改 ✅；本批再改 #5/#6/#7 后**全表清空**） |

## 3. 5.3 `advisor_brief.md` 复扫（**无新增过期项**）

- **对象**：`coordination/advisor_brief.md`（**280 行**；L175 已于本轮 v0.271 加限定）。
- **结果**：风险词命中行**全部为历史段/已带后记锚**或**已加限定** ⇒ **无新增过期项**。

## 4. 累计口径（`docs/` **35 份全覆盖后的"过期项总账"**）

| 批次 | 落地（已改底图） | 出稿（未落地） |
|---|---|---|
| 第一/二批 | —（出稿） | A–E 家族；`HOST_UI_DECISION`／`WORK_CONSOLE_PLAN`／`GENE_LIBRARY_DESIGN`／`LAYER_BASEMAP` 等 |
| 第三批 | ✅ `FIELD_PRESENTATION_DESIGN` L111/L112/L125/L126 ＋ `FIELD_RENDER_SOURCELESS_DESIGN` §3#3/#4、§5、L104（**v0.267**） | 4 处"同族"项（**本批已取证**） |
| 第四批 | —（出稿） | ★ `GENE_LIBRARY_DESIGN` L245「已接线」⇒ **已于本轮 v0.270 订正** |
| **第五批（本批）** | —（只出稿） | **★ 4 处新过期项**（§3#5／#6／#7、§4#4） |

## 5. 边界说明（四类）＋ 只出稿声明

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | 只读检索实跑：`grep -rn modulate_gabor`／`decide_probe`（全仓 `.rs`）｜读 `.github/workflows/ci.yml` L684–L810（field-render job 断言）｜读 `host/field-render/Cargo.toml` |
| **CI 过** | **不适用**（本稿不改文件） |
| **真实跑过** | **不适用**（**未运行 field-render／sky-browser**） |
| **未验证** ⚠️ | ① 调用点检索是**静态**的（动态派发/宏会漏）⇒ 置信度**高但非穷尽**；② §4#4 的"渲染效果"**未运行** ⇒ 只判"部分"；③ **本批 4 处建议均未落地**（**改底图须授权**） |

> **只出稿声明**：本稿**未改 `docs/` 任何一份、未改治理文件、未提交**。
