【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# `docs/` 文档时效核对稿（第一批 · 2026-09-20 · v0.256）

> **性质**：**只出稿，不改文件**（用户指令 §二.2「只出稿，不改文件」）；**`docs/` 属底图 ⇒ 依本轮约束不得改动**。
> **对象**：`docs/*.md`（**35 份 / 5,823 行**）＋ 对照实现（`npb-gateway/`、`meta-kernel-core/`、`npb/`）。
> **口径**：找「**声称 X，实际 Y**」的过期项；过期项分 **数值型（R82 家族）**／**状态型（R87 家族）**。
> **每项四要素**：**文件:行 ／ 原文 ／ 实际 ／ 依据（复核命令）**。**行号取自本遍快照，落笔须以符号定位（R75）。**
> **范围声明（诚实）**：本稿为**第一批＝按"家族"定向扫描**（端点族／L7 状态族／状态行族／数值族），
> **不等于 35 份逐行穷举**；未覆盖部分见 §5「本批未覆盖」。

---

## 0. 汇总（快照）

| 家族 | 命中 | 严重度 | 结论 |
|---|---|---|---|
| **A · 端点族**（机制 24 覆盖域） | 1 处命名差（＋ 2 处**已正确标注**作反证） | 低 | ✅ **基本已收口** |
| **B · L7 实现状态族** | **2** 份文档（`LAYER_ARCHITECTURE` 内部矛盾 ＋ `SELF_DIAGNOSIS`） | **高** | ⚠️ **落后**（L7 T1 已实现） |
| **C · 状态行族** | **2** 处（`API_GATEWAY_DESIGN` 头部／`LAYER_BASEMAP` 端点句） | **中高** | ⚠️ **落后** |
| **D · 数值族** | **1** 份（`SELF_DIAGNOSIS` 的模块行数/测试数） | 中 | ⚠️ **静默过期**（4 项中 3 项已变） |
| **E · L6 对齐族** | 0（三处口径**一致**） | — | ✅ **无需改** |

**一句话**：`docs/` 的**端点族（最严重的历史漏洞 L3-V1）已收口**；**新暴露的集中在「L7 实现状态」**——
两份底图仍写「L7 尚未实现」，而 **L7 的 T1 闭环早已实现并随 CI 运行**。

---

## 1. 家族 B · L7 实现状态（**优先最高**）

### B-1 `docs/LAYER_ARCHITECTURE.md` —— **同文件自相矛盾**

| 位 | 行 | 原文（逐字摘） | 判定 |
|---|---|---|---|
| 标题 | **L53** | `### 2.1 L7–L10（L7 = 执行层 · **判定与记账已实现、执行待确认**；L8–L10 = 推演层）` | ✅ 现行 |
| 标题 | **L321** | `## 4.1 L7–L10 详述（L7 = 执行层 · **判定与记账已实现、执行待确认**；L8–L10 = **推演层**）` | ✅ 现行 |
| **正文** | **L65** | `> **L7 为「执行层」**（v1.4 定名）：由**多内核互联**与**修复执行**两个功能组成，**已定义、尚未实现**。` | ⚠️ **过期** |
| **正文** | **L323** | `> L7 为**已定义、未实现**的层（v1.4 定名）；L8–L10 为**系统推演**，非已验证事实。` | ⚠️ **过期** |

**实际**：L7 **T1 闭环已实现**——
- `docs/L7_EXECUTION_DESIGN.md` 头部逐字：`> 状态：**v1.2（2026-09-14 · T1 闭环已实现）**`；
- 实现落点 `npb-gateway/src/l7_exec.rs`（**637 行**）＋ 7 个授权/执行端点（`/v1/actions|grants|grant|revoke|execute|rollback|audit.txt`）。

**依据 / 复核命令**：
`grep -n "T1 闭环已实现" docs/L7_EXECUTION_DESIGN.md`｜`wc -l npb-gateway/src/l7_exec.rs`

> ★ **同文件自违其纪律**：`LAYER_ARCHITECTURE.md` **L30** 自定
> 「5. **文档内部不得自相矛盾**：若某层的"实现状态"有分歧，以**代码落点**为准（见 §2 表右列）」——
> 而 **L65／L323（"尚未实现"）与 L53／L321（"判定与记账已实现"）正是同一文档内的实现状态分歧**。
> ⇒ **应改 L65／L323**（以代码落点为准）。**但改动属底图 ⇒ 待你裁定。**

### B-2 `docs/SELF_DIAGNOSIS_REPORT.md`（表格行）

| 行 | 原文（逐字摘） | 实际 | 判定 |
|---|---|---|---|
| **L38** | `\| L7 \| 共生层 → **执行层**（v1.4 定名） \| 多内核互联 + 修复执行 \| 🟡 已定义未实现（本报告原为推演） \| — \|` | T1 闭环已实现（v0.109） | ⚠️ **过期** |

**建议**：该报告**通篇无日期/状态头**（`# 云操作系统 · 自诊断报告`）⇒ 建议**整体加「当时快照」标注**（如「本报告 = **2026-09-1x 时点快照**，计数与实现状态以复核命令为准」，R71／R82），并**不改数字**（C19）。

---

## 2. 家族 C · 状态行（"实现中"类）

| # | 文件:行 | 原文（逐字摘） | 实际 | 判定 / 建议 |
|---|---|---|---|---|
| **C-1** | `docs/API_GATEWAY_DESIGN.md:3` | `> 设计状态：**v1.1（2026-09-07 · L3 实现轮启动，并入架构顾问审核建议）** ｜ 阶段：**L3（实现中）**` | **L3 早已实现并发布**：`npb-gateway/` **2,917 行**／**26 行路由**；CI 含活体步骤；**机制 24 已把它纳入机器判据**（入 `test` job） | ⚠️ **状态型过期**。建议改为「阶段：**L3（已实现并发布）**；端点表权威见 `LAYER_ARCHITECTURE.md §4.L3`」 |
| **C-2** | `docs/LAYER_BASEMAP_L0_L6.md:44` | `\| L3 \| 轮询 /v1/probe 取最新采集（⟨源 L6_DESIGN §4, 行54⟩） \| 依赖；**端点表不统一（见 §3.3-①）** \|` | ① **端点表已统一**（Q12 裁 `LAYER_ARCHITECTURE.md §4.L3` 为唯一权威；Q14 建 `check_endpoint_table.py`；**机制 24 已入 CI**）；② **指针失效** —— 本文件 **§3.3（L203+）只列 L1-V1／L1-V2，无 ① 项** | ⚠️ **状态型过期 ＋ 死指针**。建议改「依赖（端点表权威见 `LAYER_ARCHITECTURE.md §4.L3`）」 |

**依据 / 复核命令**：
`grep -c '"/v1' npb-gateway/src/http.rs`（→ 26 行含测试）｜`grep -n "§3.3" docs/LAYER_BASEMAP_L0_L6.md`｜`grep -nE '^\- \*\*L[0-9]-V[0-9]' docs/LAYER_BASEMAP_L0_L6.md`

---

## 3. 家族 D · 数值族（`SELF_DIAGNOSIS_REPORT.md`）

**实测对照**（复核命令：`wc -l <crate>/src/*.rs`；`grep -rc '#\[test\]'`）：

| 行 | 文档记 | 实测（2026-09-20） | 变 |
|---|---|---|---|
| **L32** | `meta-kernel-core/src/`（35 模块 / **7488 行**） | **12,989 行** | ⚠️ **已变** |
| **L34** | `npb-gateway/`（**1149 行，16 测试**） | **2,917 行**；**40** 个 `#[test]` | ⚠️ **已变** |
| **L37** | `manifest-ui/`（**846 行**） | 以命令为准（未逐文件核） | ⚠️ **待核** |
| **L46** | `meta-kernel-core  7488 行 / 39 文件 / **222 测试**` | 行/文件/测试**均以命令为准**（已多次静默过期） | ⚠️ **已变** |
| **L33** | `npb/`（**947 行，11 测试**） | `npb/src/lib.rs` ＝ **947 行**（**未变**） | ✅ 行数一致（测试数待核） |

> 建议：**不改数**，**只加"当时快照"标注**（同 §1 B-2）。理由：改数＝篡改历史留痕（C19）；但**不标注**会让顾问把 7488/1149 当现值（R82）。

---

## 4. 家族 A · 端点族（**机制 24 覆盖域**）—— ✅ 基本收口（含反证）

**定向扫描**：6 份文档涉及 `/v1` 端点 → **5 份已合规**（指向权威表／与权威表一致）：

| 文件 | 处理 | 判定 |
|---|---|---|
| `L7_EXECUTION_DESIGN.md` L290-291 | 列 7 个 L7 端点 | ✅ **与权威表 §4.L3 逐条一致** |
| `GENE_LIBRARY_DESIGN.md` L271 | 列 `POST/GET /v1/genelib` | ✅ 与权威表一致 |
| `L6_DESIGN.md` L7／L24／L54／L61／L79 | 用 `/v1/probe` | ✅ 与权威表一致 |
| `FULL_AUDIT_REPORT.md` L212 | ~~5 项列举~~ ＋ 「**该列举为历史快照，非权威**」 | ✅ **已自我标注**（收口痕迹） |
| `API_GATEWAY_DESIGN.md` L50 | 「端点表唯一权威＝`LAYER_ARCHITECTURE.md §4.L3`（23 路径）；本表只保留下方 6 个 v0 基线端点的语义说明」 | ✅ **已限定范围** |

**唯一命中（低）**：

| # | 文件:行 | 原文（逐字摘） | 实际 | 判定 |
|---|---|---|---|---|
| **A-1** | `docs/FULL_AUDIT_REPORT.md:227` | `\| **③ 远程任务通道（令牌鉴权）** \| 网关新增 `GET /net-task`（读取任务）+`POST /v1/nettask`（回传结果）…` | 实现中**无** `/net-task`／`/v1/nettask`；实体为 **`/v1/tasks`**（GET/POST）＋ **`/v1/tasks.txt`** | **低**：该行为**方案选项③的拟定命名**（非"已实现"声称）⇒ 不属虚假。建议加括注「（**最终实现取名 `/v1/tasks`**）」 |

**依据 / 复核命令**：`grep -rn "nettask\|net-task" npb-gateway/src/`（→ **0 命中**）｜`grep -n '"/v1/tasks' npb-gateway/src/http.rs`

---

## 5. 家族 E · L6 对齐族 —— ✅ 三处口径一致（**无需改**）

| 文件:行 | 原文（摘） | 结论 |
|---|---|---|
| `LAYER_ARCHITECTURE.md` L44／L308 | `**对齐**：WorldAdapter（❌ **未实现**，契约见 archive/reserved-interfaces/l6_interface.rs）` | ✅ 与下两处**一致** |
| `LAYER_BASEMAP_L0_L6.md` L48／L536 | `**L6 对齐** \| ❌ **未实现** …` | ✅ 一致 |
| 实现侧 | `archive/reserved-interfaces/l6_interface.rs` **仍是契约**（`WorldReceipt` 仅声明，无实现） | ✅ 一致（`grep -rn "WorldReceipt" --include=*.rs` → 仅 archive 契约文件） |

⇒ **L6 对齐＝真"未实现"，三处自洽** ⇒ **无需改**。（L6-V2 判断**仍成立**。）

---

## 6. 本批**未覆盖**（诚实清单 —— 供下一批）

| 未覆盖 | 原因 |
|---|---|
| `docs/` 中**含日期的历史报告类**（`PHASE1_REVIEW`／`PHASE4_ACCEPTANCE_REPORT`／`L4_VALIDATION_REPORT`／`L5_VALIDATION_REPORT`／`PERFORMANCE_REPORT`／`FULL_AUDIT_REPORT` 的正文结论） | 属**留痕型报告**，通篇为"当时快照"；**逐条订正＝篡改历史**（C19）⇒ 建议**统加标注**而非逐条改 |
| `CONTRAST_REPORT`／`COSMIC_COMPUTING`／`CREATION_LOG`／`VISION`／`MATH_SPEC`／`ONTOLOGY_SPEC`／`PERTURBATION_MODEL`／`QUANTUM_CLOCK_DESIGN`／`SECURITY_TROUBLESHOOTING`／`FIELD_*` | 属**定位／规范／设计**类，非"实现状态"类 ⇒ **过期的定义面小** |
| `SILA_IMPLEMENTATION`／`HOST_UI_DECISION`／`WORK_CONSOLE_PLAN`／`REFERENCE_APP_DESIGN`／`APPLICATION_FRAMEWORK_DESIGN`／`DIAGNOSIS_VALIDATION`／`L5_TRANSLATION_TABLE`／`GENE_LIBRARY_DESIGN`／`LAYER_BASEMAP` 的**其余章节** | **留待第二批**定向扫（家族：设计-实现对齐／版本号族） |

---

## 7. 建议动作（**均待你确认**；**本批不改任何底图**）

1. **必改（实质误导）**：**B-1（`LAYER_ARCHITECTURE` L65／L323）**、**B-2（`SELF_DIAGNOSIS` L38）**。
2. **应改（状态行）**：**C-1（`API_GATEWAY_DESIGN` 头部"实现中"）**、**C-2（`LAYER_BASEMAP` L44 端点句＋死指针）**。
3. **建议加标注（不改数）**：**D（`SELF_DIAGNOSIS` 数值）** ＋ 各类历史报告统加「当时快照」。
4. **宜改（低）**：**A-1（`FULL_AUDIT` 方案③命名）**。
5. **无需改**：**E（L6 对齐）**。

> ⚠️ **底图改动需你裁定** ⇒ 本批**零改动**；**待你点头后**按上表逐项落。

---

## 8. 边界说明（四类）

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | 35 份文件头 `dump` ＋ 定向 `grep` ＋ 实现侧 `wc -l`／`grep -c '#\[test\]'` **实测** |
| **CI 过** | **不适用**（零代码／零 workflow 改动）；⚠️ 但**家族 A 的收口由 CI 判据（机制 24）实证** |
| **真实跑过** | **不适用** |
| **未验证** | ① 本批**未穷举 35 份**（见 §6）；② `manifest-ui` 行数（L37）**未核**；③ `npb/` 测试数**未核** |

## 9. 三量台账

| 量 | 内容 |
|---|---|
| **存量** | `docs/*.md`（35 份 / 5,823 行）｜实现 `npb-gateway/`（2,917 行）／`meta-kernel-core/`（12,989 行）｜`LAYER_ARCHITECTURE.md §4.L3` 权威表｜机制 24 |
| **变量** | **本轮 0**（只出稿；`docs/` 属底图，不改） |
| **补充增量** | **家族分类法**：把 `docs/` 时效项按 **A 端点／B L7 状态／C 状态行／D 数值／E 对齐** 五族归口 ⇒ 可**分批**推进且**可声明覆盖度** |
| **创新增量** | **"留痕型报告"处置口径**：历史报告类**不逐条改数**（C19），而**统加"当时快照"标注** ⇒ 一条标注替代 N 条订正（低成本、可证伪） |

> **保密**（C12）：本稿 **🔓 公开**；**⛔ 机密 0 处**（**未引用** `docs/L5_VALIDATION_REPORT` 等含内网 IP 的行）；**🔐 算法层 0 处**。
> **底图引用**（D50／D52）：本稿**读底图处** ＝ `docs/LAYER_ARCHITECTURE.md`（L20–L50／L300–L330）｜`docs/SELF_DIAGNOSIS_REPORT.md`（L24–L50）｜`docs/LAYER_BASEMAP_L0_L6.md`（L39–L60／L203–L222）｜`docs/API_GATEWAY_DESIGN.md`（L1–L60）｜`docs/FULL_AUDIT_REPORT.md`（L210–L232）｜`docs/L7_EXECUTION_DESIGN.md`（L17–L26）｜`README.md`（L108–L120）＋**实现侧现场复核**（C18）。
