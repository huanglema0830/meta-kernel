【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# v0.248 · 3 项裁定落地 · 报告

> 本轮把上一轮登记的**三项待裁**全部落地：**① `CHARTER.md` §四速查表补全 C16–C20**｜**② 汇编覆盖范围口径（R86）**｜**③ 仓库外汇编快照标注**。
> 版本号 = `git rev-list --count HEAD`（不写死）；本轮起点 count = **247**，提交后 **248** ⇒ 本报告标签 = **v0.248**。

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。
（说明：本轮改动 3 文件经泄漏扫描 —— **用户名 0 处**；合订本中的 `C:\Users\〈本机用户名〉\` 属 **R77 掩码形态**、`127.0.0.1` 属 **loopback**，均**非泄漏**；本轮**新增行**含泄漏模式 **0 处**。仓库外快照标注**只在本机、不入库**。）

**② 全景状态卡**（★ 数字为**生成时快照**；提交／推送会推高版本号 ⇒ **现值以复核命令为准**，R82）
```
┌─ 云内核项目 · 全景状态卡 ────────────────────────────────────────┐
│ 版本   以 git rev-list --count HEAD 为准（不写死，C2）           │
│        本轮起点 count = 247 ⇒ 提交后 248（label = count，机制 27）│
│ 阶段   阶段二 · 2.4 纯算层 ✅ ｜ 2.4 边界层 ✅（QEMU 已证）      │
│        2.4 product 路径已接线（v0.237，待 CI 真实引导证成）      │
│ 本轮   3 项裁定落地：速查表补全 ＋ 汇编覆盖清单 ＋ 快照标注      │
│        改动 3 文件 ＋ 1 报告 ＋ 仓库外 1（仅本机）              │
│ CI     按 **C20**：结论**只进日报／对话返回**，**不入仓库**      │
│        ⇒ 复核命令 `gh run list --limit 3`                        │
│ 决策   三项待裁**已落地**；新增待裁：① 是否纳入 REFERENCE_APP   │
│        ② 仓库外快照是否重生成（本轮按裁定只标注）              │
│ 暂缓   场源扩屏（选 B，先不定）｜D-7 身份档案（按指令暂缓）      │
└──────────────────────────────────────────────────────────────────┘
```
复核命令（R71：数值不抄进文档）：`git rev-list --count HEAD`｜`git rev-list --count origin/main..HEAD`｜`gh run list --limit 3`

**③ 任务**：基准 **count = 247**｜总规划定位：**3 项裁定落地**｜类型：**裁定落地**｜优先级 **P1**。
约束：按 `CONSTRAINTS.md`｜禁止内联含反引号／特殊字符命令（**全程脚本文件**，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**本轮不派其他任务**。

**④ 状态：完成**（含 **3 处诚实边界**：① **CI 结论不入库**（C20）⇒ 本报告不预写"全绿"；② **仓库外快照标注为"只新增、不改正文"**，其**正文仍为 2026-09-17 旧版**（`README.md` 为旧文）—— **故意不追改**；③ **汇编未纳入 `REFERENCE_APP_DESIGN.md`**，本轮**只写明缺口、未扩范围**。）

**⑤ 验收对照**（用户本次 6 条验收标准原文）

| # | 标准（用户原文） | 实测 |
|---|---|---|
| 1 | CHARTER §四速查表 C16–C20 补全，逐字对照给出 | ✅ 补入 **C16–C20** 五行（表列 **16 → 21 行**）；**逐字对照见文末专节**（含 5 行原文 ＋ 覆盖说明块前后） |
| 2 | 汇编覆盖清单写明，R86 登记 | ✅ 合订本 **§五 新增「覆盖范围（实际收录清单 · 治 R86）」**：**34 份名录**（脚本实测生成）＋ **缺 1 份** `REFERENCE_APP_DESIGN.md` ＋ **明确"不含根 `README.md`（只做行号回指）"** ＋ **明确"只改 README ⇒ 重生成是空转"**；**R86 状态：登记待裁 → 已处置**（`BASELINE.md` 行 1423） |
| 3 | 仓库外快照标注完成 | ✅ `研发开发/底图原件汇编_第1批_设计定位层_逐字_20260917.md` **文件头加标注**「**本文件为 2026-09-17 历史快照，不追改**」；**先备份**（SHA-256 一致）；diff ＝ **+6 / −0**；**幂等**（重跑跳过）；**不入库** |
| 4 | C15 复算通过，编号表连续无缺号无重号 | ✅ 改前 → 改后：机制 **27→27（±0）**／**§四表 16→21（+5）**／C **21→21（±0）**／T **36→36（±0）**／D **44→44（±0）**／R **49→49（±0）**；三张编号表 **连续、无缺号、无重号**；对照 **6/6 PASS**（同一脚本取两次数，C18 同源） |
| 5 | CI 全绿 | ⏳ **按 C20：结论不进仓库** ⇒ 以命令实取（`gh run list --limit 3`），**结论写在当轮对话返回／日报**。仓库侧本报告**只留命令** |
| 6 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行 ＝ **`TEMPLATES.md` 行 104–118 逐字抽取**（D40 不手写），**回读全等自检 PASS**（见 ⑭） |

**⑥ 未完成项**
① **CI 全绿待 push 后实取**（C20：只记日报／对话返回，**不预写结论、不入仓库**）；
② **汇编未纳入 `REFERENCE_APP_DESIGN.md`** ⇒ 缺口**已写明**但**未弥合**（属**范围扩大**，须另行授权）；
③ **仓库外快照未重生成** ⇒ 其正文仍为旧版（按裁定**只标注、不追改**）；
④ **场源扩屏**（选 B，按指令**先不定**）｜**D-7 身份档案**（按指令**暂缓**）。

**⑦ 真实缺陷（本轮 1 条：表述过强，当场订正）**

① ★ **汇编 §五 的一句"逐字保真"表述过强（当场订正）** —— §五 原文写「**对每个源文件**，断言其**全文（strip 后）是合订本正文的子串**」。
   **实测不成立**（**阳性对照暴露**）：结构稿 `2026-09-18_汇编重做结构稿.md` 的**整文件不是**合订本子串 ——
   因为装配器把结构稿按 **三段（扉页／主体／附录）**嵌入、且其**文件头（标题与状态块）由合订本自写替代**。
   ⇒ **性质**：**过强的判据表述**（与 **C16**「四缺一只写未检索到」、**R85**「三缺一即不成立」**同族** —— 断言必须**恰好**对得上证据）。
   **处置（本轮已改）**：§五 改为「对**每个嵌入段**断言（结构稿三段／四批整批）；**结构稿整文件非子串属预期**」；**反向自检脚本同步改为按段断言** ⇒ **5/5 子串 = True**。
   **教训**：**反向自检第一版就是照原句写的**（"整文件"），**跑出 False** ⇒ 说明**自检必须真跑**，不能照抄表述（否则这段"过强表述"会一直被当真）。

**⑧ 三量台账（四量）**

| 量 | 内容 |
|---|---|
| **存量** | 带入的：crate 8 个｜`nostd` lib **477** 测试｜host 镜像 **13**｜机制 **27**｜C **21**｜T **36**｜D **44**｜R **49**（含 R86）｜CI 三 job｜第 ⑫ 段｜底图 = 根 `README.md` ＋ `docs/` 35（判据 F 基线 36 份）｜汇编四批 ＋ 合订本 |
| **变量** | 本轮**改动 3 文件 ＋ 1 报告**：治理 1（`CHARTER.md` §四）＋ 派生副本 1（合订本）＋ 台账 1（`BASELINE.md`）＋ 报告 1；另有**仓库外 1 文件**（快照标注，不入库）。`git diff --stat` = **3 files changed, 118 insertions(+), 5 deletions(-)** |
| **补充增量** | **既有内容的补全与订正**（非新能力）：① §四速查表**补齐 C16–C20**（原缺 5 行）；② 合订本 §五**补覆盖清单**、并**订正**"逐字保真"过强表述；③ R86 **状态更新**（待裁 → 已处置） |
| **创新增量** | **全新条目**：① 合订本 §五 **「覆盖范围（实际收录清单）」**小节（**脚本实测生成**）；② 仓库外**快照标注机制**（"WorkBuddy 侧标注 ＋ 不追改 ＋ 幂等"）；③ `BASELINE.md` **§二十四**（24.1–24.5） |

**⑨ 边界说明（四类 · 与机制 26 四层对齐）**

| 类 | 层 | 本轮实测内容（**命令级**） |
|---|---|---|
| **本地跑过** | **M** | 判据脚本 **14 项全绿**（机制 17／18／21／22／24／25 ＋ 迁移闭包／保真 ＋ 屏幕转储，含 `--selftest` 与实跑）｜机制 25 实跑：**扫描 46 份**／最大段号 **⑫**／迁移收口剩余 **0** ⇒ ✅ 通过｜**C15** 双向计数 **6/6 PASS**｜**汇编反向自检 5/5 子串 = True**｜**装配确定性**（跑两次 SHA-256 一致）｜**泄漏扫描**：改动 3 文件 用户名 0、新增行泄漏模式 0 |
| **CI 过** | **CB** | ⏳ **待本轮 push 后实取**（`gh run list --limit 3`）。**按 C20：结论不进仓库** ⇒ 见**当轮对话返回／日报**。**不预写"全绿"** |
| **真实跑过** | **CQ** | 不适用本轮（**未改代码／未改内核**）—— 本轮只动**治理文件 ＋ 汇编副本 ＋ 台账 ＋ 仓库外快照**。⑫ 段的真实引导（QEMU）结论**沿用上轮**，本轮**不重跑、不新增断言** |
| **未验证** | **H** ＋ 上述缺口 | ❌ **CI 结论**（待 push）｜❌ `REFERENCE_APP_DESIGN.md` 纳入汇编（未授权）｜❌ 仓库外快照重生成（按裁定只标注）｜❌ **场源扩屏**（暂缓）｜❌ **D-7 身份档案**（暂缓）｜❌ 真机（H 层）帧缓冲回读 ——**能力矩阵唯一空白层** |

**⑩ 决策评估（七要素）** —— 本轮**无新建制决策**；剩余两项为**范围扩大类**（见 ⑪）

| 要素 | 内容 |
|---|---|
| **评估** | 三项裁定**已落地**；落地过程中**新增两项"范围"问题**：① 汇编**实际只收 `docs/` 34／35 份**，`REFERENCE_APP_DESIGN.md` **从未收录**（缺 1 份）；② 仓库外快照**本质是旧版 `README.md` 的逐字副本**，**是否要重建** |
| **优势** | ① **纳入缺的 1 份**：汇编**名副其实**（"全量汇编"）；② **重建快照**：仓库外读者拿到的也是新底图 |
| **劣势** | ① 纳入 ⇒ 要改**装配脚本／结构稿**（落位、篇序、附录 B 映射表），**动到"逐字装配"链**；② 重建 ⇒ 会**抹掉"2026-09-17 版本"这个历史留痕**（而它恰好是**底图演进的一个证据点**） |
| **风险** | ★ **不做的风险**：① 汇编标题写"全量"而实缺 1 份 ⇒ **名实不符**（但本轮已在 §五**写明缺口** ⇒ 风险已**降级为"已知差异"**）；② 快照旧 ⇒ 读者误当现值（本轮**已加标注** ⇒ 风险已**降级**）。**做的风险**：动装配链**可能破坏幂等**（R78），收益有限 |
| **机会** | ① 若未来要"汇编真正全量"，可**一次性**补入缺份并更新**附录 B 映射表**（35 份逐一）；② 若需**历史留痕**，可**改为"快照归档 + 现行重建"双件**并存 |
| **建议** | **推荐：两项均维持现状**（本轮**已写明缺口 ＋ 已加标注**，"名实不符"与"旧当新"两风险**均已降级**）。**理由**：两者都是**范围扩大**，且**收益有限、风险在装配链**；**当前处置已足以消除歧义**。**若你要"汇编真正全量"，建议单开一轮做**（含附录 B 同步）。 |
| **等待决策** | 见 ⑪ ①②（各自 (a)/(b)/(c)） |

**⑪ 需要你决策**
① **是否把 `docs/REFERENCE_APP_DESIGN.md` 纳入汇编？** **(a)** 纳入（**范围扩大**：须改结构稿／装配脚本 ＋ 更新附录 B 映射表）／**(b)** **维持现状**（本轮 §五**已写明缺口**，"名实不符"已降级为"已知差异"，**推荐**）／**(c)** 连标题也改（把"全量汇编"改为"汇编（收录 34/35）"）。
② **仓库外快照是否重生成？** **(a)** **维持"只标注、不追改"**（**推荐**：保留 2026-09-17 历史留痕）／**(b)** 重生成（抹掉旧版留痕）／**(c)** 归档旧件 ＋ 另建现行版（双件并存）。

**⑫ 下一步建议**
1. **本轮 push 后**：取三 job（`test`／`boot-image`／`field-render`）结论 —— **按 C20 只记日报／对话返回**，**不改仓库**；
2. **治理侧已达"自洽收口"**（速查表 C1–C21 全量、R86 已处置）⇒ 下一轮可**回到功能主线**：**场源扩屏**（需先定**网格尺寸策略**，`2^k+1` 与屏幕分辨率映射）—— 按你上轮"选 B，先不定"的指示，**仍可再等**；
3. **H 层判据**：真机帧缓冲回读（WC 显存语义）—— 目前 **0 判据**，是能力矩阵**唯一空白层**；
4. **可顺手做**：`fmath.rs` doc 旧值订正（**长期登记待办**，为不动被测物而未改）。

**⑬ 启动回执**：已读 `CHARTER.md`。版本 = `git rev-list --count HEAD`（**不写死**，C2；本轮起点 count = **247**，提交后 **248** ⇒ 本报告标签 **v0.248**）｜本次任务：**3 项裁定落地**｜**是否继续：等待裁定（⑪ ①②；均推荐维持现状）**。

**⑭ 本轮读了底图哪几份、哪几行**（D55／D52：报告须写明；**行号均当轮实测**，R75）

- **底图（根 `README.md` ＋ `docs/` 35 份）**：本轮**未逐字精读**（三项裁定均落在 `coordination/` 与仓库外），但**实取引用**：
  - 根 `README.md`：**行 89**（**上一轮已修订**，本轮**只作引用**，未再改）；
  - `docs/` 目录：`ls docs/*.md | wc -l` ＝ **35 份**（实测）；**逐份收录情况由脚本核对**（见下）。
- **判据工具（汇编覆盖的判据源）**：`asm_cover.py`（收录／差集核对）｜`asm_selfcheck_0920.py`（**反向自检 ＋ 覆盖清单一致性**）｜`asm_binder.py`（**装配器；本轮 §五 的生成处**）。
- **汇编（改动落点）**：`coordination/discussions/2026-09-19_汇编合订本.md` —— **§五（行 6399 起）**：装配说明 ＋ **R77 掩码** ＋ **R78 副本同步** ＋ **本轮新增「覆盖范围」小节**；**结构稿三段切分点**（实测）：`## ⚠️ 扉页声明`／`# 主体（底图骨架）`／`# 附录（**非底图**`；**四批节头样式** `## N.N 标题 — \`docs/xxx.md\``。
- **脚手架（非底图，改动落点）**：`coordination/CHARTER.md`（**§四「硬规则速查」**：表列 C1–C21 ＋ 覆盖说明）｜`coordination/CONSTRAINTS.md`（**C16–C21 原文**，**只读**，用于对齐口径）｜`coordination/BASELINE.md`（**R86 行** ＋ **§二十四**）｜`coordination/TEMPLATES.md`（**行 104–118** ＝ 阅读指令逐字源）。
- **仓库外（改动落点）**：`研发开发/底图原件汇编_第1批_设计定位层_逐字_20260917.md`（**文件头**；**只读正文 ＋ 只加标注**）。
- **被测物**：**本轮零代码改动** ⇒ 未改任何 `.rs`／`.yml`／`.toml`。

---

**逐字对照 · `CHARTER.md` §四速查表（补全前后）**（用户第 1 条验收标准要求"逐字对照给出"）

**（甲）表格新增 5 行（逐字）** —— 表列由 **C1–C15 ＋ C21（16 行）** 变为 **C1–C21（21 行）**，新增如下 5 行：

```
| C16 | **断言「不存在」须给四要素** | 检索式／检索范围／命中数／阳性对照，**四缺一 ⇒ 只能写「未检索到」**、不得写「不存在／全库无／从未」（落点：**三方共用** —— 对底图／代码／台账的"不存在"类断言） |
| C17 | **诚信类指控须穷举＋逐字引用** | 指控诚信**须穷举查过哪些地方 ＋ 贴冲突双方原文**；**举证不足 ⇒ 只写「存疑／待查」**、禁用定性词；**须先过 C16**（落点：**三方共用**；**WorkBuddy 侧从严**） |
| C18 | **判据自检须与实跑同源，且回读真实仓库** | `--selftest`**与实跑同源**（同一常量／同一解析路径）＋**至少一侧回读真实仓库**＋断言真实仓库**可解析**（`>0`）（落点：**三方共用** —— `coordination/tools/` 下**所有机器判据**） |
| C19 | **同一机器事实只许一份口径实现**（判据↔判据同源） | 同一机器事实**只允许一份实现**（其余判据 **`import` 复用**）＋输出**显式打印口径来源**＋**概念分行输出**（一个 `PASS` 只代表一件事）（落点：**判据↔判据**共享同一机器事实时 —— 模块集／路径集／编号表／段号／哈希基线） |
| C20 | **CI 结论不入库**（治 R82 自指递归） | 「本轮 CI 结论」**只写日报**（`.workbuddy/memory/YYYY-MM-DD.md`）、**不入任何跟踪文件**；仓库侧**只引命令**；**不为「记录 CI」再推一次**（落点：**一切会因提交而变化的观测值** —— CI 状态／run 号／制品 ID／部署哈希） |
```

**（乙）覆盖说明块（逐字对照）**

| | 内容（**逐字**） |
|---|---|
| **改前** | `> **本表覆盖说明（诚实标注）**：本速查表**历史上只列到 C15**，**C16–C20 未列**（本轮**未补**，避免超出本轮授权 ⇒ 已登记为 \`BASELINE.md\` 待裁项）；**本轮只新增 C21 行**。`<br>`> **条文全文一律见 \`CONSTRAINTS.md\`**（本表只是速查，不作为权威）。` |
| **改后** | `> **本表覆盖说明**：本速查表**历史上只列到 C15**；**C16–C20 于 2026-09-20 按用户裁定补入**、**C21 于同日新增**`<br>`> ⇒ 现**覆盖 C1–C21 全量，编号连续无缺**（补入前为 C1–C15 ＋ C21，**缺 C16–C20**）。`<br>`> ⚠️ **注**：本表**列的是「规则名 ＋ 一句话 ＋ 落点」**（落点置于「一句话」栏尾），条文全文一律见 \`CONSTRAINTS.md\`（本表只是速查，不作为权威）。` |

**（丙）口径说明**：`CONSTRAINTS.md` 内 **C16–C20 原文早已存在**（本轮**未改**）—— 本轮**只补速查表**；
表头 3 栏（**编号／规则／一句话**）**保持不变**，按发起人「**编号／一句话／落点**」口径，**落点置于「一句话」栏尾**（**不为补 5 行而改动既有 16 行结构**）。

---

**一句话**：**三项裁定全部落地** —— `CHARTER` §四速查表 **补齐 C16–C20**（16 → 21 行，**C1–C21 全量、编号连续无缺**）、汇编 §五 **写明实际覆盖清单**（**34/35 份 ＋ 不含根 README ＋ 改 README 重生成是空转** ⇒ **R86 已处置**）、仓库外快照**已加"历史快照·不追改"标注**（+6/−0、幂等、不入库）；**C15 复算 6/6 PASS**、**14 项判据全绿**、**反向自检 5/5 子串**。落地中**当场订正了一处"表述过强"**（汇编"逐字保真"原写"整文件"，实为"嵌入段"）。**需要你点头的：⑪ 两项是否维持现状（均推荐 (b)）。**
