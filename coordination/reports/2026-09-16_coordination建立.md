# 报告 · 三方对齐机制建立

**基准**：v0.125（提交前）；提交后 v0.126（BASELINE 按「原样写入」保留 v0.125，未擅自改）
**任务**：建立三方对齐机制（`coordination/`）
**状态**：完成
**归档时间**：2026-09-16（本轮补归档）

---

## 一、验收对照

| 验收标准 | 实测 | 结论 |
|---|---|---|
| `coordination/` 文件夹建立 | 建在项目根 `tmp_a45_review/gh_clone2` | ✅ |
| 7 个文件/文件夹全部就位 | README(21行)／BASELINE(91)／CONSTRAINTS(53)／TEMPLATES(58) + instructions／reports／discussions(.gitkeep) | ✅ |
| 各文件内容正确 | 四份 md **逐字照录**，未改一字 | ✅ |
| CI 全绿 | run `35033095556` = **success** | ✅ |
| 附加：不改现有代码 | `git status` 仅 `?? coordination/` | ✅ |

提交 `bbcdd38` → 版本自动 **126**；push 一次成功，`ls-remote` 校验远端 == 本地 sha。

---

## 二、未完成项

- 无（本轮全部达成）。附带暴露 2 项**基准文档自身**的问题，见下。

---

## 三、真实缺陷（发现即报，未自行改；已于 v0.127 修正）

| 编号 | 缺陷 | 状态 |
|---|---|---|
| F-1 | **C2 与 BASELINE 自相冲突**：C2 原写「禁止**文档**中出现手写版本号」，而 BASELINE 本身是文档且记录 `版本：v0.125`；且一落盘就滞后一版 | 已修（v0.127：BASELINE 改「最后更新于」，C2 收窄到代码/构建/提交信息） |
| F-2 | **BASELINE「验收模式 8 个」与实际 11 个不符**（漏 diag-check／link-check／l4-check／persist-check） | 已修（v0.127 列出全部 11 个） |

---

## 四、三量台账

- **存量**：v0.125 全部成果（内核 390 测试、宿主 11 验收模式、四类持久化）／顾问给定的机制内容与 BASELINE、CONSTRAINTS 草案
- **变量**：本轮任务＝建立协调文件夹（纯新增，零代码改动）
- **补充增量**：`coordination/` 7 项落地；`README → BASELINE → CONSTRAINTS → TEMPLATES → instructions` 启动流程生效
- **创新增量**：把「每次重新对齐」变成「基准持续存在」；C2 与 BASELINE 的口径冲突在机制启用当天就被抓出（机制自身即起了作用）

---

## 五、边界说明

| 边界 | 内容 |
|---|---|
| 本地跑过 | git 目录/文件清点、`git status`、`rev-list` 计数、`ls-remote` 校验 |
| CI 过 | run `35033095556` = success |
| 真实跑过 | 无（本轮不涉及运行时） |
| 未验证 | — |

---

## 六、需要用户决策（当时提出）

| 编号 | 事项 | 状态 |
|---|---|---|
| D-a | BASELINE 版本滞后一版如何解 | 已决议：v0.127 采用「最后更新于 HEAD xxx（对应 vX.Y）」口径 |
| D-b | 本报告是否归档进 `coordination/reports/` | 已执行：本文件即归档 |
| D-c | v0.126 宿主真实运行（R1）何时解冻 | 已解冻：v0.127 进入调研 |

---

## 七、下一步建议（当时）

1. 先定死 BASELINE 版本口径（已做）。
2. R1 解冻后先做只读调研，出改动清单再动手（已做）。
