【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

**保密标记**（C12）：⛔ 机密（**绝不拷**）**密钥本体**／**密钥存放路径**／原术语／全拼／本机路径／`PRIVATE_ASSETS` 真实值 → **已预屏蔽**（**本轮起仓库内亦不出现指纹字面量**，改占位 `〈本机私记〉`）｜🔒 半公开 ·模块结构·门禁口径·统计数｜🔓 版本号·CI·编号·C1–C15·英文名与 TERM 编号

> 【给用户的提示】本报告含 **1 类机密（⛔，已预屏蔽）**；删掉 ⛔ 那一行即可整篇外发。
> **本轮报告通篇只用英文名与编号**（机制 20 的持续实践）。

```
┌─ 全景状态卡 · v0.181 ───────────────────────────────────────┐
│ 阶段  阶段二 ｜ 1 ✅ ｜ 2.1 ✅ ｜ 2.2 ✅ ｜ 2.3 ✅             │
│ 2.3b  🚧 片1 ✅｜片2 ✅｜片3 ✅｜片4 ✅｜**片5 ✅（脚本产出）**    │
│       剩余 **片6–片8 = 7 模块 / 2,887 行**                     │
│ 机制  20 项 ｜ 机制 20 = 三层 + AES-256-GCM + 密钥 C12 三级     │
│ 约束  C1–C15 ｜ **T-023（指纹不入仓库）**                       │
│ CI    ✅ 见 §十（三 job 全绿，逐行核实）                        │
│ 漏洞  R1–**R35**（+R33 清单漂移／+R34 白名单滞后／+R35 口径）    │
│ 决策  D1–**D40 全部收口**（本轮新确认 D40 并落地）              │
│ 密文  **D39 已执行**：6 份明文已删、磁盘只剩 `.enc`              │
│ 夜间  **首夜仍未发生** → 今夜 21:00/23:00；**晨报已改为写证据文件** │
└──────────────────────────────────────────────────────────────┘
```

## 一、第 9 步·意图对齐（复述一遍，请核）
你要的是四件事：**① 把 D39 收尾归档**（已执行、R31/R32 归档、**密钥路径与指纹不再写入仓库**、通用法进记忆）；**② 确认 D40**（剩余模块的迁移分片**改用脚本产出**，且脚本判据要有阴阳自检）；**③ 执行片5**（脚本产出清单、保真度用多重集差、裸机断言扩到第 ⑧ 段、闭包须同时覆盖**非测试依赖与测试依赖**）；**④ 核验首夜晨报**（**如实、不粉饰，有违规就置顶加粗**）。
**不是**要我再手写清单，也**不是**要我把剩余 7 个模块也一并做掉。

## 二、⚠️ 一件必须前置说明的事：**首夜晨报此刻无法核验**
现在 **09-17 18:0x**，两条夜间任务**均在 ACTIVE**（21:00 出「今夜 P3 清单」／23:00 自动开工），**首次触发是今夜**。
⇒ **本轮不做任何"首夜结果"的汇报**（那会是编造）。验收里"夜间未自动 push／未改治理文件"**仍不可被实际证伪**。
**已把"可核验性"做进结构**（见 §七）。

## 三、验收对照（你的第七部分）
| # | 标准 | 实测 |
|---|---|---|
| 1 | D39 已执行并记录到 BASELINE | ✅ `BASELINE` **D39 = 已执行**（含三重前提、事后复核）；报告 §十三 有完整执行记录 |
| 2 | D40 确认并记录 | ✅ `BASELINE` **新增 D40**（含已落地内容与"手写清单连续两片出错"的理由）＋ 简报 §四 |
| 3 | 片5 由脚本产出、有清单并确认后执行、保真度通过 | ✅ 清单**由 `--plan` 产出**（16 模块 / 3,651 行）并单独落盘；**保真度 36/36 全过**（片5 十六 ＋ 片1–4 二十 作阳性对照） |
| 4 | 闭包同时对非测试依赖与测试依赖封闭 | ✅ `check_migration_closure.py` **默认即含测试块**；`--selftest` 四项**全过**（含"仅测试缺口必判红并单列"） |
| 5 | 机制 20 检查器持续 PASS | ✅ `syntax=0`／`resolve=0`（含新增 **[6] 指纹字面量判据**，阴阳自检通过） |
| 6 | 密钥 C12 三级标识持续执行 | ✅ **[5] 密钥级别判据继续 PASS**（本轮它又抓到我自己写的 3 行，已补标识） |
| 7 | 夜间未自动 push、未改治理文件 | ✅ 任务 prompt 含白名单＋禁止清单；⚠️ **首夜未发生 ⇒ 尚不可被实际证伪** |
| 8 | 首夜晨报如实核验、不编造 | ✅ **如实报"尚未发生"**；并把核验做成**独立复核任务**（不采信晨报结论） |
| 9 | CI 全绿 | ✅ 见 §十（逐行核实） |
| 10 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行即 9 步固定文本 |

## 四、片5：**16 模块 / 3,651 行**（清单由脚本产出，D40 首次落地）

**清单产出命令**：`python coordination/tools/check_migration_closure.py --plan`
**分层判据**：`层 k = 所有未迁依赖都已在「已迁 ∪ 更早层」的模块`（**引用含 `#[cfg(test)]` 块**，与封闭性判据同口径）

| 片 | 模块数 | 行数 | 状态 |
|---|---|---|---|
| **片5** | **16** | **3,651** | ✅ 已执行 |
| 片6–片8 | 7 | 2,887 | ⏳（片6 含 `l1_mapping`／`positive_source`／`l5_diagnosis`／`l7/repair`） |

**结果**
- **保真度**：**36/36 全过**（多重集差判据；片1–4 作**阳性对照**，防止"判据对新片放水"）
- **测试**：nostd **238 → 331**（+93）｜host 镜像 **9 → 12**
- **原 core 一字未改**；**C9 未新增 unsafe**；**C1** 未引第三方 crate
- **裸机断言扩到第 ⑧ 段**（**51–55**）：
  - **51 三态判定**：本底自比必**平**／2× 必**亢**／0.5× 必**枯**，且**三态互不相同**（否则判据空转）
  - **52/53 动作账链**：空账自洽 → 追加后链锚改变且仍自洽 → **篡改任一条必须 `verify()==false`**，复原后须再自洽
  - **54/55 沙漏瓶颈**：每 tick **至多放行 1 粒**，且放行值必在 `0..=1`
- **引导镜像**：ELF **1,846,512 → 1,901,344**（**+54,832 B**，证 ⑧ 段确已编入）

## 五、★ 片5 抓到第 5 类硬障碍（**编译器兜住**）
`std::collections::VecDeque`：`no_std` 下 **`std` 拿不到、也不在 prelude**，但 **`alloc::collections::VecDeque` 有**
⇒ 纯路径改写。首跑**漏登**该替换 ⇒ 4 个文件报 `cannot find module or crate std`。
**已把它做成硬判据**（不只改这一处）：迁移脚本新增「**代码区不得残留 `std::`**」——**拦在编译器之前**。
⚠️ **片6 已探到 `std::collections::HashMap`**：这属**类型替换**（不是路径改写），**须单独论证**（同 D37 口径）⇒ **下次需你先拍板**。

## 六、D39 归档 ＋ 指纹收紧（**与上轮 C12 三级不一致，必须点出**）
| 项 | 落地 |
|---|---|
| **D39 状态** | `BASELINE` 记为 **已执行**；R31（purge 后指针目标变更）与 R32（判据分歧）**均已归档** |
| **密钥路径** | ⛔ 从未写入仓库（本轮复核：仓库内**无任何具体路径**） |
| **密钥指纹** | ⚠️ 你本轮要求**不写入仓库** ⇒ 仓库内 **8 行 / 10 处**字面量全部改写为 `〈本机私记〉`；新增 **[6] 判据**强制 |
| **通用法** | 已记入项目记忆：**凡「删掉东西」，回头问一句「有谁还在按旧形态找它」** |

> ⚠️ **我必须提请注意的一致性冲突**：上一轮你定的是 **C12 三级（指纹 🔒 半公开＝可拷）**，
> 本轮收紧为「**可拷，但字面量不入仓库**」。**我以本轮为准**，并已把 C12／`TEMPLATES §十二` 的指纹行改成新口径、
> 同时写明**代价**：备份核对时的指纹**须从本机私记取**（`keycheck <副本> <本机私记的指纹>`）。
> 若你其实想保留"指纹写在文档里便于查阅"，**这是一行可逆的**，说一声我改回。

## 七、首夜：**仍未发生** ⇒ 把"可核验性"做进结构
| 动作 | 内容 |
|---|---|
| **晨报任务升级** | 除对话简报外，**必须另写证据文件** `coordination/reports/2026-09-18_首夜结果.md`，内含 6 条机器可读结论行 `RESULT <n> PASS/FAIL/UNKNOWN <说明>`；**证据不足必须写 UNKNOWN**，禁止"疑似成功" |
| **新增独立复核任务** | **09-18 08:30**：**不采信晨报结论**，自己跑 5 组命令重新取证并**逐条比对**；**不一致必须点名**；**只读、不 commit、不 push** |
| **违规处置** | 两个任务都写明：若夜间**越界改了治理文件** ⇒ **置顶加粗报违规** |

> 设计意图：**"agent 类任务是否真跑通"是本项目最大未验证点**。不给它一个"能自证的产物"，明早就只能听转述——
> 所以我要求**产物落到文件**，并让**第二个任务独立复核第一个**（与项目一贯的"独立比对"口径一致）。

## 八、三量台账（四量）
- **存量**：core 411｜nostd **331**（lib）＋ 镜像 **12**｜mem 25｜机制 20｜C1–C15｜报告 14 项｜CI 三 job｜工具 **6 个**（指针／保真度／闭包／加密／keycheck／keyexport）｜R1–**R35**｜D1–**D40**
- **变量**：`BASELINE`(指标 2 行 / +R33–R35 / +D40 / 快照头 / D39 状态)｜`ROADMAP`(2.3b 行)｜`CONSTRAINTS`(C12 指纹行收紧)｜`TEMPLATES`(§十二 两行 + T-023)｜`advisor_brief`(结论 + D40 + 快照头)｜`check_private_pointers.py`(+[6])｜`check_migration_closure.py`(重写：`--plan`/`--emit`/`--selftest`)｜`check_migration_fidelity.py`(ALLOWED_SUBS +VecDeque)｜`lib.rs`/`l7.rs`(模块声明)｜`verify.rs`(⑧段)｜`mirror_bare_assertions.rs`(+3)
- **补充增量**：nostd **16 个新模块**｜**片5 清单**（脚本产出）｜报告｜指令归档
- **创新增量**：① **D40 清单不再手写**（手写连续两片出错 ⇒ 脚本产出 + 阴阳自检）② **"代码区不得残留 `std::`"硬判据**（拦在编译器之前）③ **闭包工具会分层**（`l7::ledger` 那类依赖是我手算漏掉的）④ **[6] 指纹字面量判据**（把"不入仓库"变成可机器校验）⑤ **晨报写证据文件 + 独立复核任务**（把"可核验性"做进结构）⑥ **`--mods` 清单冻结**（治"清单漂移"）

## 九、边界说明（四类）
- **本地跑过**：nostd **331/0**｜镜像 **12/12**｜保真度 **36/36**（+判据自检）｜闭包 **PASS + 自检 4/4**｜裸机构建 ✅｜引导镜像（ELF +54,832）｜门禁 `syntax=0`/`resolve=0`｜**[5]/[6] 判据阴阳自检**｜C15 双向计数
- **CI 过**：见 §十
- **真实跑过**：✅ **QEMU 真实引导、截屏 25/25 全绿**（⇒ ⑧ 段断言已被真实证成；一条绿屏同时证成 **2.1＋片1–片5＋`alloc`**）
- **未验证**：① **夜间首夜（尚未发生）** ② **片6–片8（7 模块 / 2,887 行）** ③ **片6 的 `HashMap` 属类型替换，须先决策** ④ **物理介质上的密钥备份**（我看不到介质，仅凭你的确认）

## 十、CI（**逐行核实，非只看结论**）
- 提交 `52ac70d`(v0.180) → **`9bb6252`(v0.181)**，run **`35208996356`**，**三 job 全 success**
- **逐行核实（实测输出，不是只看结论）**：
  | 项 | 实测 |
  |---|---|
  | `##[error]` 计数 | **0** |
  | 截屏断言 | `[PASS] 全部 25 个采样点 == (0, 255, 0)`（1280×720）⇒ **⑧ 段断言已被真实 QEMU 证成** |
  | **[6] 指纹字面量判据** | **CI 首跑 PASS**（扫描 305 个文件） |
  | [5] 密钥级别判据 | **PASS**（持续） |
  | 其他 | `BOOT_IMAGE_BUILT_OK`／`FIELD_RENDER_UNIT_TESTS_OK(35 passed)`／`GATEWAY_TESTS_OK` |

## 十一、真实缺陷（本轮 5 条）
1. **R33 清单漂移**：按"剩余集"算的分片**一执行就漂移** ⇒ 重跑**误迁片6 的 2 个模块**（已删、已加 `--mods` 冻结）
2. **R34 判据白名单滞后**（R31 同族，本轮两次）：迁移白名单与保真度白名单各漏登 `VecDeque` ⇒ **一个编不过、一个假红**
3. **R35 计数口径不稳**：「指纹 8 处」实为 **8 行/10 处**；剩余量 **24/6,528 vs 23/6,538**
4. **我的断言期望值又错一次（自查拦下）**：`check_migration_closure --selftest` 首跑报"②不通过"，**根因是我把 `sorted()` 的输出 `['a']` 写成了 `{'a'}`**——**判据本身是对的**
5. **迁移脚本"按剩余集分层"导致重跑语义不安全**（与 1 同源）——这条不是"改文件"，是**设计缺陷**，靠 `--mods` 治

## 十二、决策评估（本轮新决策七要素摘要）
| 编号 | 决策 | 评估 | 优势 | 劣势 | 风险 | 机会（可证伪） | 建议 | 状态 |
|---|---|---|---|---|---|---|---|---|
| **D40** | 分片改脚本产出 | 手写清单**连续两片出错** | 口径由代码固定、可自检 | 新增"漂移"风险（R33） | 脚本判据错 ⇒ 清单错 | 若下一片脚本产出仍与手算不一致 ⇒ 证伪"脚本更准" | 采纳 | ✅ 已落地 |
| **T-023** | 指纹不入仓库 | 指纹虽单向，但进库即永留 git 历史 | 消除"核对依据公开" | 备份核对须查本机私记 | 私记丢失 ⇒ 核对不便（**非不可恢复**） | 若日后仍出现"库里查指纹"需求 ⇒ 说明占位符方案不适用 | 采纳 | ✅ 已执行 |

## 十三、需要你拍板（**1 项**）
| 编号 | 事项 | 我的建议 |
|---|---|---|
| **D41**（新） | **片6 的 `std::collections::HashMap` 如何处理** | **先论证再动**：须证明该集合**是否被迭代**；若被迭代 ⇒ `BTreeMap` **不逐位等价**（顺序不同）⇒ 需改成"排序后再用"或**另选容器**。**建议：先出论证清单，确认后再迁片6** |

**下一步（无需你决策即可做）**：为片6 做 **`HashMap` 用法论证清单**（只读分析，不改代码）；把 **`[6]` 判据的经验补进 C12**（已做）。
**首夜与晨报**：今夜 21:00 / 23:00 首次触发；**09-18 08:00 晨报（写证据文件）＋ 08:30 独立复核**，两者都会把结论落到可查的产物上。
