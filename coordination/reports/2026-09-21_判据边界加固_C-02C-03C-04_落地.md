【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 判据边界加固 · C-02／C-03／C-04 **落地**（2026-09-21 · v0.268）

> **性质**：**落地**（用户裁定「C-02/C-03/C-04 **合并为一次 P3**」）。
> **对象**：`coordination/tools/check_doc_consistency.py`（**机制 25 判据脚本**）。
> **保密标记**（C12）：🔓 公开。⛔ 机密 **0**、🔐 保密 **0**。

---

## 0. 一句话

三条同属 **R83 家族**（**"退化／跳过／空转"必须与"通过"取不同值**）—— 本轮把它们**从"设计稿"变成"判据"**：

| 编号 | 病根（R83 形态） | 落地做法 | 与「通过」的差异 |
|---|---|---|---|
| **C-02** | 豁免词（历史标记词）是本判据的**弱化旋钮**；加一个常用词即可让**一批过期陈述静默免检**（≡ 弱化判据本身） | 新增 `HISTORY_MARKS_BASELINE`（冻结集合）＋ **子集判定**：`set(HISTORY_MARKS) ⊆ BASELINE` ⇒ 超出即**判红**（**只许减、不许增**） | 新增词 ⇒ `[C-02 豁免词扩张]` **判红** |
| **C-03** | 文档一条活跃陈述都没有时，"**该文档没被检查**"与"**该文档检查通过**"**取同值** ⇒ 假绿 | 逐文件统计**活跃断言条数**；`0` ⇒ 标 **`NO_CLAIM`**（在 `stats` 与输出中**单列**） | `NO_CLAIM` ≠ `PASS`（**独立取值**） |
| **C-04** | 段号源空／闭包源不可读时，P1–P4 **静默跳过** ⇒ 与「通过」同值 | 段号源空 ⇒ `SEG_EMPTY`；闭包源不可读（`closed is None`）⇒ `CLOSURE_EMPTY`；**两者都并入 `errors` 判红** | 打标记 ＋ **判红** |

## 1. 改动清单（**只动 1 个文件**）

`coordination/tools/check_doc_consistency.py`：

1. **新增常量** `HISTORY_MARKS_BASELINE`（frozenset，11 词）＋ 纯函数 `history_marks_expansion(marks)`（便于自检注入）。
2. **`check()` 新增**：
   - C-02：`history_marks_expansion(HISTORY_MARKS)` 非空 ⇒ `errors.append("[C-02 豁免词扩张] …")`；
   - C-04：`mx is None` ⇒ 改打 **`[C-04 退化·段号源空]`**（并入 `SEG_EMPTY`）；`closed is None` ⇒ **`[C-04 退化·闭包源不可读]`**（并入 `CLOSURE_EMPTY`）；`stats["source_empty"]` 记录；
   - C-03：新增 `rel_claims`（逐文件活跃断言计数）⇒ `stats["no_claim_files"]` ＋ `stats["verdicts"]`（`NO_CLAIM`／`HAS_CLAIM`）。
3. **输出新增**：豁免词行（C-02）、退化标记行（C-04）、**`NO_CLAIM` 文档清单**（C-03，最多列 8 份）。
4. **自检扩为十一侧**：新增 **⑨ C-02**、**⑩ C-03**、**⑪ C-04**（各含**正反对照**；`--selftest` 帮助文本同步）。
5. **docstring 同步**：新增【★ 2026-09-21 加固】节，写明三条的**病根／口径／范围**。

> **未改**：`CONSTRAINTS.md`／`CHARTER.md`／`TEMPLATES.md`／`ROADMAP.md`／`BASELINE.md` 等**治理文件**（遵从用户 §六「不改治理文件」）。

## 2. 自检正反对照（**实跑证据**）

```
[侧⑨] C-02 豁免词（a）现行词表 ⇒ ✅ 无扩张（合规）；（b）注入反例词 ⇒ ✅ 可判红
[侧⑩] C-03 零陈述 ⇒ ✅ 标为 NO_CLAIM（**NO_CLAIM ≠ PASS**）；对照·含陈述 ⇒ ✅ 标为 HAS_CLAIM
[侧⑪] C-04 退化（a）闭包源不可读 ⇒ ✅ 判红且标 CLOSURE_EMPTY；（b）对照·源齐备 ⇒ ✅ 不报 C-04
自检结论：✅ 十一侧全部符合预期
```

- **⑨ 阳性/阴性**：现行词表 ⇒ **不扩张**（阴性，不该红）；注入反例词（"计划"）⇒ **可检出**（阳性，该红）。
- **⑩ 阳性/阴性**：零陈述文档 ⇒ `NO_CLAIM`；含一条段落陈述 ⇒ `HAS_CLAIM`。**两者取值不同**（＝ R83 正解）。
- **⑪ 阳性/阴性**：临时移走目标 crate（令闭包源不可读）⇒ **判红且打 `CLOSURE_EMPTY`**；复原后 ⇒ **不报**（防误报）。
- **⑧ 侧原有八侧未回归**：侧①–⑧ ＋ 侧④b-a/b/c **全部仍符合预期**（同一次 `--selftest` 输出）。

## 3. 实跑（真实仓库）结果

- `check_doc_consistency.py`（无参）⇒ **`结果：✅ 通过`**（P1 段号一致／P2 双侧／P3 内部自洽／P4 模块数一致），**rc=0**。
- **新增可见面**：`NO_CLAIM 文档（C-03）：43 份`（如 `README.md`／`CHARTER.md`／`CONSTRAINTS.md`／`TEMPLATES.md`／`TERMS.md` …）—— 这正是**此前"没查 ≈ 查过"的静默面**，现**被显式数出来**（**不改判红策略**）。
- 有活跃陈述的底图/台账仍正常判定（如 `BASELINE.md` max=第 ⑫ 段、`advisor_brief.md` max=第 ⑫ 段）。

## 4. 边界说明（四类）＋ 声明

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | `--selftest` **十一侧全绿**（rc=0）；实跑 **通过**（rc=0） |
| **CI 过** | **待 CI**（本项改动 `tools/**`，由 CI `test` job 的机制 25 门禁步骤验证；**本轮新 commit 未 push ⇒ CI 未跑**） |
| **真实跑过** | **不适用**（判据脚本，非设备程序） |
| **未验证** ⚠️ | ① **`NO_CLAIM` 清单较长（43 份）**，其"应有断言"的**逐份人工确认未做**；② C-02 基线冻结为**当前 11 词**，若将来**确需扩充**须**明示裁定**并同步改基线常量与自检侧 |

> **声明**：本项**只改判据脚本**；**未改底图、未改治理文件、未改 boot 层、未提交**（另行随本轮提交）。

## 5. 遗留／后续

- **C-05（机制 26 矩阵完整性）**：**本轮暂缓**（用户 §三；**会改 `TEMPLATES.md`**）⇒ 见主报告 ⑪。
- **建议**：C-02 的基线常量建议在 `BASELINE.md` 内**留一处指针**（"豁免词基线＝`check_doc_consistency.py::HISTORY_MARKS_BASELINE`"），避免将来只在脚本里找得到 —— **属治理层登记，需授权**。
