【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-24 夜间 P3-3 · **清单来源口径**取证 ＋ **回退路径（ROADMAP）实测 0 行**

> **来源**：① 本夜任务书 **§三.1**（清单缺失时"从 `ROADMAP.md`「可夜间跑 = ✅」未完成项中择定 1–3 项"）；② `coordination/reports/2026-09-21_F-2分析稿_夜间P3清单连续3夜0项.md` **§四 甲案**（建议扩源**六源**）；③ 今日当轮报告 §⑥／§⑪ 的待裁面。
> **性质**：**只读取证 ＋ 出稿 ＋ 如实记录本夜的处置口径**。**未改**治理／判据／CI／代码；**未改任何自动化任务配置**。

## ① 保密标记（对应 `C12`；**拷给顾问前先看这一段**）

- ⛔ 机密（**绝不拷**）：**本件中 0 处** —— 未写本机绝对路径、账号、密钥、内网地址、端口值；**引用自动化配置时只写"步骤与要求"，不逐字抄含路径的整段**。
- 🔐 保密（**屏蔽后拷**）：**无**。
- 🔒 半公开（**可拷**）：任务名称／调度／状态、仓库内相对路径与行号、编号体系。
- 🔓 公开（**直接拷**）：版本号、行数、计数、结论。

> 【给用户的提示】本件 **⛔ 级内容 0 处**；**无需屏蔽即可整份拷给顾问**。

## ② 全景状态卡

| 项 | 值 | 证据 |
|---|---|---|
| **版本（机器事实）** | `count = 399`（开工实测） | `git rev-list --count HEAD` |
| **轮次（叙事）** | `v0.333` | `T-050` |
| **判据** | **16/16 PASS**（`rc=0`） | `run_all_checks.py` |
| **只告警类告警数（开工实测）** | `chain_visibility` **0** ／ `effect_column` **0** ／ `id_set_diff` **3** ／ `judge_gaps` **0** ／ `memory_layers` **0** ／ `report_structure` **0** ／ `version_label` **0** ／ `rewrite_pipeline` **2** | 逐支实跑末行 |
| **回退来源（ROADMAP）** | ★ **实测 0 行**（P3 且 ✅ 且未完成） | §④ 逐行结算表 |
| **本项性质** | 夜间 P3-3 ／ 白名单类别＝**调研 ＋ 报告归档** | 本件 |

## ③ 任务（含基准／定位）

- **基准**：`count = 399`（开工实测；**无 21:00 清单**）。
- **总规划定位**：机制层（机制 19 **清单来源口径**）。
- **任务类型**：**调研（只读取证）**。
- **一句话任务**：**把"清单应当从哪来"这件事查清**——① 21:00 任务的**配置里逐字要求什么来源**；② `F-2` 甲案（扩源六源）**到底落地没有**；③ 本夜任务书指定的**回退来源（ROADMAP）实测还有几项**。

## ④ 状态

**状态**：**完成**（三问各有实测证据；并**如实记录本夜的来源处置**）。

### 4.1 问一：21:00 任务的**配置**里，来源是什么？（**只读取证**）

- **配置口径（按"步骤与要求"转述，不逐字抄含路径段）**：该任务的生成步骤**第 2 步**要求读 5 类件——`CHARTER.md`、`CONSTRAINTS.md`（`C10`／`C12`／`C13`／`C14`）、`TEMPLATES.md` §三／§十一、**`BASELINE.md` 的「未完成项」与「§九 待决策」**、**`ROADMAP.md` 的「可夜间跑」列**；**第 3 步**逐字要求：**"只取优先级 P3 且「可夜间跑 = ✅」的项；每项写清【任务／依据（ROADMAP 哪一行）／预计时长／可夜间跑】"**。
- ⇒ ★ **合法来源 = `ROADMAP`「可夜间跑」列 ＋ `BASELINE`「未完成项／§九 待决策」**（**均须同时满足 P3 且 ✅**）。
- ⇒ ★ **`F-2` §四 甲案建议的"六源"（台账未完成／判据盲区／docs 时效／汇编同步／R-D-T 残余）__未__写进该任务的配置** ⇒ **甲案未落地到自动化**（**仍处"建议"态**）。

### 4.2 问二：回退来源（`ROADMAP`）实测还剩几项可跑？（**逐行结算**）

| # | 行 | 项 | 优先级 | 可夜间跑 | 能否执行 |
|---|---|---|---|---|---|
| 1 | `ROADMAP.md:71` | **2.1** no_std 子集 | P3（已达成） | ✅ | ❌ **已完成** |
| 2 | `ROADMAP.md:72` | **2.2** 可被引导 | P3（已达成） | ✅ | ❌ **已完成** |
| 3 | `ROADMAP.md:73` | **2.3b** alloc 迁移 | — | ✅（段末） | ❌ **已收口** |
| 4 | `ROADMAP.md:74` | **2.4** 轻量帧缓冲 ＋ L6 接入 | **P1** | ✅ | ❌ **P1 ⇒ 需决策** |
| 5 | `ROADMAP.md:75` | **2.5** 首个用户空间进程 | **P1**（09-22 由 P3 改标） | ❌ | ❌ **P1** |
| 6 | `ROADMAP.md:76` | **2.6** ACPI/CPUID | **P1**（09-22 由 P3 改标） | ❌ | ❌ **P1** |
| 7 | `ROADMAP.md:77` | **2.7** 可引导 ISO | **P2** | ⚠️ 部分 | ❌ **P2 ⇒ 需验证** |
| 8 | `ROADMAP.md:119` | **子任务5** 安装程序 | **P3** | ✅ | ❌ **依赖子任务4（未达）**——★ 与 21:00 任务自陈的排除口径一致 |
| 9 | `ROADMAP.md:111` | 子任务4 硬件适配 | P2 | ❌ 需真机 | ❌ |
| 10 | `ROADMAP.md:127` | 子任务6 ISO 分发 | P1 | ❌ | ❌ |
| 11 | `ROADMAP.md:153` | M1 | P3 ✅ | ✅ | ❌ **已完成** |
| 12 | `ROADMAP.md:155`／`157` | M3（P1）／ M5（P2） | P1／P2 | ✅／⚠️ | ❌ **P1／P2** |

**逐行结算**：已完成 **4** ／ **P1（需决策）3** ／ **P2（需验证）4** ／ **依赖未达 1** ⇒ ★ **"P3 且 ✅ 且未完成"＝ 0 行**（**本夜第一手逐行复核**，非转述 `F-2`）。

### 4.3 问三：本夜的**来源处置**（**如实记录**）

- 任务书 **§三.1** 的回退路径 ⇒ **实测 0 行**（§4.2）⇒ **回退路径枯竭**。
- 但因"**当夜链条前置件缺失**"这一**异常本身**已由任务书 **§三.5** 明令"**如实记录失败原因与其证据**"，且"**调研／文档／报告归档**"**属 P3 白名单之内**，故本夜**改从**下列**已在案**的事实面取证（**均只读**）：
  1. **`BASELINE.md` §五 未完成表**「机制 18/19」行**附注**（⇒ P3-1：机器侧半通道 ＋ `D32`）；
  2. **今日当轮报告 §⑦ 登记的两项"未取证"**（⇒ P3-2：清单缺件三解）；
  3. **21:00 任务自身配置**（⇒ 本件 P3-3）。
- ★★ **偏离如实标注**：本夜三项**均不取自 `ROADMAP`**；**未处置任何 P0／P1／P2 项**（**未改代码／判据／治理／CI／任何自动化配置**）。**该偏离是否合规 ⇒ 列入待裁**（§⑪-④）。

## ⑤ 验收对照

| # | 验收标准 | 🟢/🟡 | 实测 |
|---|---|---|---|
| 1 | 三问各有**实测证据**，无一条靠推断 | 🟢 | §4.1（配置只读）／§4.2（逐行 12 行）／§4.3（来源落点） |
| 2 | **逐行结算**，不转述既有结论 | 🟢 | 12 行**本夜当场复核**（并给行号） |
| 3 | 甲案（六源）**落地与否**有明确判定 | 🟢 | §4.1 结论：**未落地**（配置第 3 步逐字为 ROADMAP 单列） |
| 4 | 本夜的**来源偏离**如实标注、不掩盖 | 🟢 | §4.3 ＋ §⑪-④ |
| 5 | **零落盘副作用**（治理／判据／CI／代码／自动化配置） | 🟢 | `git status` 本项仅本报告 1 份；平台任务**只读** |
| 6 | 结论**可复现** | 🟢 | 行号 ＋ 命令口径齐备 |

## ⑥ 未完成项

| # | 未完成项 | 原因 | 状态 |
|---|---|---|---|
| 1 | **甲案（扩源）落地** | 落点＝**自动化任务的配置**（平台侧）⇒ **须授权** | 🟡 **待裁** |
| 2 | **`BASELINE.md` §九 待决策**的逐条复核 | 本夜只取 §五；§九 未逐条展开 | 🟡 未做（另开） |
| 3 | 09-19／20／21／23 各夜清单**实际取自哪一源**的逐夜回填 | `reports/` 为当轮快照，回填受 `C19` 约束 | 🟡 未做 |

## ⑦ 真实缺陷

| # | 缺陷 | 严重度 | 处置 |
|---|---|---|---|
| ① | ★ **条文／建议与实现三层不一致**：`F-2` 乙案（改 `ROADMAP` 列标注）**须授权未做**；`F-2` 甲案（扩源六源）**被当作"本轮即可、不需授权"却未落地** ⇒ **"扩源"这条止血措施实际悬空** | **P2** | ★ **本轮只取证、不处置**；处置项见 §⑪-① |
| ② | 回退路径（`ROADMAP`）**实测 0 行**，而任务书把它写成**唯一回退路径** ⇒ **两条路径同时失效**时夜间无事可做 | **P2** | 登记（与 P3-2 §⑦-① 同一情形的**来源侧**）⇒ 处置见 §⑪ |

## ⑧ 三量台账（**四量**）

| 量 | 内容 |
|---|---|
| **存量** | `ROADMAP` 「可夜间跑」列 **12 行**（✅10 ＋ ⚠️2）／夜跑链任务 **3** ／历夜清单 **5** |
| **变量** | 本夜 **±0**（未改 `ROADMAP`、未改任何自动化配置）；新增本报告 1 份 |
| **补充增量** | ① **21:00 任务来源口径的定性**（= `ROADMAP` 单列 ＋ `BASELINE` 双入口）② **甲案未落地的判定** ③ **12 行逐行结算表**（本夜第一手）④ **本夜来源偏离的书面落点** |
| **创新增量** | ★ **把"机制空转"从"现象"定位到"配置行"**：此前两轮只知"连续多夜 0 项"，不知 **"为什么清单不会自己去找别的活"**——本件给出**唯一落点＝21:00 任务配置的第 3 步**（**平台侧一行**，**非治理文件**）⇒ ★ **治法成本从"改机制"降为"改一次任务配置"**；且**不需要动任何治理文件**（不受治理文件禁则约束） |

## ⑨ 边界说明（四类 · 齐全）

- **本地跑过**＝**有**（`ROADMAP.md` **逐行复核 12 行**、自动化**配置只读读取**、`run_all_checks.py` 全跑、8 支只告警判据逐支取告警数、`git rev-parse`／`git log`）。
- **CI过**＝**不适用**（未改被 CI 覆盖的载体、**未推送**）。
- **真实跑过**＝**无**（本机属 `M` 层）。
- **未验证**＝① 甲案落地后的**效果**（未落地 ⇒ 无从验）② 09-19／20／21／23 各夜清单的**实际来源**（未逐夜回填）③ `BASELINE` §九 **未逐条复核**。

## ⑩ 决策评估（七要素）

- **评估**：本件**不产生新方向**；它把"机制空转"的**处置对象**从"机制文件"**收窄到"一条任务配置的第 3 步"**。
- **优势**：① **治法不动治理文件** ⇒ 绕开夜间治理文件禁则，**授权后可在白天一次完成**；② 与 `F-2` 甲案**天然衔接**（甲案缺的正是"落到哪一行"）；③ 逐行结算**当场可复现**，不依赖历史表述（`F-2` §2.1 的口径校正问题**不再重复**）。
- **劣势**：本件**只定位不落地** ⇒ 若不改配置，**下一个缺件夜仍会重演**同一空转。
- **风险**：① **扩源后夜间产能上升** ⇒ 与"夜间只跑 P3"的**安全边界**叠加，需同时确认扩源清单**仍在白名单内**（甲案六源中的"判据盲区清单"若涉判据改动，**仍会撞治理／授权**）；② 若**只扩源不改回退路径写法**，任务书侧的"唯一回退路径"仍会在下次缺件时指向 0 行。
- **机会**（**可证伪**）：★ **"把静态标注列 → 改为可执行来源"**这一手法可推广到该列的**姊妹列**（优先级列／状态列）；**可证伪点**＝扩源后**连续 2 夜**若仍为 0 项，则"扩源即止血"不成立（`F-2` 丙案兜底）。
- **建议**：① 授权后，**只改 21:00 任务配置的第 3 步**（写明多源 ＋ **仍限 P3 且 ✅**），**不改任何治理文件**；② 同步把任务书侧的"**唯一回退路径 = ROADMAP**"**加一句兜底**（**回退路径也枯竭时 ⇒ 只做异常取证 ＋ 出稿，不空转**）——本夜即按此办，**建议正式化**。
- **等待决策**：见 §⑪。

## ⑪ 需要用户决策（**待裁**）

① **是否授权扩源**（改 21:00 任务配置第 3 步）：**甲 扩源六源**（推荐，`F-2` 原案）／**乙 仅补"当轮报告登记项＋基线附注"两源**（更保守）／**丙 维持单列**（则夜跑将持续空转）。
② **回退路径写法**是否加兜底（"回退源也枯竭 ⇒ 只做异常取证 ＋ 出稿"）？
③ **本项与 P3-1／P3-2 的来源偏离**（**均不取自 `ROADMAP`**）**是否合规**：**认可**（并正式化）／**仅此一夜、下不为例**／**否决**（则本夜三项应作废——**但报告已落盘，按 `C19` 只增不删，只能登记作废**）。
④ `F-2` **乙案**（`ROADMAP` 列加"最后复核"标记 ＋ `2.5`／`2.6` 改标）**仍待授权**，是否**本轮一并放行**？

## ⑫ 下一步建议

1. **等授权**：本项**全部治法**（扩源／兜底／列标注）**均需授权**，**本夜一律未做**。
2. **可立即做（白天）**：若采甲／乙案，**改动面仅一条任务配置的一行**，完成后**次日 21:00 即可验**（**可证伪、单夜可判**）。
3. **可立即做（白天）**：把"**当夜清单是否产出 ＋ 姊妹任务是否落盘**"两行并入**当夜开工记录**（**零成本**，见 P3-2 §⑫）。
4. **本夜不做**：不改 `ROADMAP`、不改任务配置、不改治理文件、不空转补做清单。

## ⑬ 本次读了底图／权威件哪几份、哪几行

| 件 | 位置 | 用途 |
|---|---|---|
| `coordination/ROADMAP.md` | 行 69–77（子步表）／行 109–131（子任务 4／5／6）／行 151–159（里程碑表） | ★ **逐行结算（本夜第一手）** |
| `coordination/BASELINE.md` | **§五 未完成表** | 第二来源面的取证（P3-1 主用） |
| `coordination/reports/2026-09-21_F-2分析稿_….md` | **§一／§2.1／§四（甲／乙／丙三案）** | 口径对照（**不转述其结论为本案结论**） |
| `coordination/CHARTER.md` | 行 97（机制 19）／行 84（优先级机制） | 边界 |
| `coordination/CONSTRAINTS.md` | `C10`／`C12`／`C14` 全文段 | 边界 |
| 自动化任务配置（**只读**） | 生成步骤第 2／第 3 步 | ★ §4.1 的取证主体 |

## ⑭ 启动回执

> 已读 `CHARTER.md`（行 97 机制 19、行 84 优先级机制）与 `CONSTRAINTS.md`（`C10`／`C12`／`C14`）。
> 当前状态：`count = 399`｜回退来源（`ROADMAP`）**实测 0 行**｜21:00 清单**未见产出**
> 本项：**夜间 P3-3（调研 · 只读取证 ＋ 报告归档）**——清单来源口径取证 ＋ 回退路径实测
> 是否继续：执行（已完成；**未推送**）
