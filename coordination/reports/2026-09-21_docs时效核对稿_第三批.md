【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# `docs/` 时效核对稿 · **第三批**（2026-09-21 · v0.266）

> **性质**：**只出稿，不改文件**（用户 §五「只出稿，不改文件」）。
> **对象**：`docs/` 下**前两批未覆盖的 20 份**（`docs/` 共 35 份）。
> **保密标记**（C12）：🔓 公开。⛔ 机密 **0**、🔐 保密 **0**（正文只用仓库相对路径）。
> **边界**：本稿**未运行渲染/宿主侧程序**；"已实现"证据＝**代码落点 + CI 作业定义**（只读取证）。

---

## 0. 批次口径（**先对齐，免与既有批次混淆**）

| 批次 | 文件 | 覆盖 |
|---|---|---|
| 第一批 | `coordination/reports/2026-09-20_docs文档时效核对稿.md` | 定向扫 **8 份**：`API_GATEWAY_DESIGN`／`FULL_AUDIT_REPORT`／`GENE_LIBRARY_DESIGN`／`L6_DESIGN`／`L7_EXECUTION_DESIGN`／`LAYER_ARCHITECTURE`／`LAYER_BASEMAP_L0_L6`／`SELF_DIAGNOSIS_REPORT` |
| 第二批 | `coordination/reports/2026-09-20_夜间P3-1_docs时效核对稿第二批.md` | 第一批未覆盖的 **9 份**：`SILA_IMPLEMENTATION`／`HOST_UI_DECISION`／`WORK_CONSOLE_PLAN`／`REFERENCE_APP_DESIGN`／`APPLICATION_FRAMEWORK_DESIGN`／`DIAGNOSIS_VALIDATION`／`L5_TRANSLATION_TABLE`／`GENE_LIBRARY_DESIGN`／`LAYER_BASEMAP_L0_L6` |
| **第三批（本稿）** | 本文件 | **剩余 20 份**（两批并集 15 份，35 − 15 ＝ 20） |

**本批 20 份**：`CONTRAST_REPORT`／`COSMIC_COMPUTING`／`CREATION_LOG`／`FIELD_PRESENTATION_DESIGN`／`FIELD_RENDER_SOURCELESS_DESIGN`／`FIELD_REPAIR_DESIGN`／`HOST_BUILD`／`L4_VALIDATION_REPORT`／`L5_DESIGN`／`L5_VALIDATION_REPORT`／`MATH_SPEC`／`ONTOLOGY_SPEC`／`PERFORMANCE_REPORT`／`PERTURBATION_MODEL`／`PHASE1_ASSUMPTIONS_REVIEW`／`PHASE1_REVIEW`／`PHASE4_ACCEPTANCE_REPORT`／`QUANTUM_CLOCK_DESIGN`／`SECURITY_TROUBLESHOOTING`／`VISION`（均 `.md`）。

## 1. 方法（可复现）

- **扫描器**：本机脚本对 20 份逐行匹配风险词 —— `未实现｜尚未｜待实现｜待建｜剩余｜TODO｜FIXME｜已实现｜已上线｜已完成｜已发布｜测试全绿｜项测试｜个模块｜将实现｜计划实现`。
- **判定**：逐条回到**代码落点**核对（`host/`／`meta-kernel-core/`／`.github/workflows/ci.yml`）；**数字类**在本轮**实测**。
- **口径**：命中 ≠ 过期 —— **历史段**（含日期/版本锚/验收记录）**不追改**，只建议**加锚**。

## 2. 汇总（20 份）

| # | 文件 | 行数 | 命中 | 结论 |
|---|---|---|---|---|
| 1 | `FIELD_RENDER_SOURCELESS_DESIGN.md` | 132 | 9 | ⚠️ **高**：3 处**状态型**过期 ＋ 1 处**数值型**过期（见 §3） |
| 2 | `FIELD_PRESENTATION_DESIGN.md` | 183 | 6 | ⚠️ **高**：1 处**状态型**过期（原生宿主"尚未编译"）（见 §3） |
| 3 | `L5_DESIGN.md` | 191 | 6 | 🟡 **中**：1 处"诊断主链尚未接线"（**待核**，见 §6） |
| 4 | `CREATION_LOG.md` | 55 | 4 | 🔓 **历史段**（Day 1 日志，测试数 27/66/81 为当日快照）⇒ 建议加锚，**不改** |
| 5 | `FIELD_REPAIR_DESIGN.md` | 168 | 2 | ✅ 一致（"只设计不实现"与代码落点相符） |
| 6 | `SECURITY_TROUBLESHOOTING.md` | 138 | 2 | 🔓 **历史段**（老笔记本事实，已实测发生）⇒ 不改 |
| 7 | `PERTURBATION_MODEL.md` | 79 | 1 | ✅ 一致 |
| 8 | `PHASE1_REVIEW.md` | 23 | 1 | 🔓 **历史段**（附 run 号与 commit，属当时快照）⇒ 不改 |
| 9–20 | `CONTRAST_REPORT`／`COSMIC_COMPUTING`／`HOST_BUILD`／`L4_VALIDATION_REPORT`／`L5_VALIDATION_REPORT`／`MATH_SPEC`／`ONTOLOGY_SPEC`／`PERFORMANCE_REPORT`／`PHASE1_ASSUMPTIONS_REVIEW`／`PHASE4_ACCEPTANCE_REPORT`／`QUANTUM_CLOCK_DESIGN`／`VISION` | — | **0** | ✅ **本批未发现状态型/数值型过期** |

## 3. 高置信过期项（**状态型 2 份 / 4 处 ＋ 数值型 1 处**）

### P1 · `docs/FIELD_PRESENTATION_DESIGN.md` L111（**状态型 · 高**）

- **原文**：`| **原生宿主程序 sky-browser.exe** | ⏳ **尚未编译**：需新增 Rust 原生依赖（webview2-com / wry）从 crates.io 拉取并本地构建；本轮**未做**…**这是下一步的第一件事** |`
- **实测**（只读取证）：`host/sky-browser/src/main.rs` **已存在**（头部自陈「空天浏览器 · **原生 WebView2 宿主**（v0.1）」，依赖 `tao`／`url`／`wry`）；`host/sky-browser/Cargo.toml` 已列 `wry = "0.57.0"`；`host/sky-browser/target/release/` **已有编译产物**（含 wry/tao 依赖树 rlib/rmeta）。
- **应改为（建议，待裁）**：`✅ **已编译**（v0.1，落点 host/sky-browser/src/main.rs；本行原为 v0.113 时点快照）`。
- **新增**：**L112** `| **多标签 / 下载** | ⏳ 待宿主落地后随宿主实现 … |` —— `main.rs` 头部已写明「**多标签＝多窗口**」、下载事件回传 ⇒ **同批过期**。

### P2 · `docs/FIELD_RENDER_SOURCELESS_DESIGN.md` §3（**状态型 · 高**，**3 处**）

- **原文（§3 表）**：
  - `| 3 | 场域呈现器未实现 | ⏳ **未做**（WGSL 骨架已就位；wgpu 管线未写） |`
  - `| 4 | WebGPU 宿主未实现 | ⏳ **未做**（WebView2 宿主保留为降级/对照） |`
  - `| 7 | Runtime 依赖未消除 | ⏳ **未做** … |`
  - 另 **§5 路线图**：`| **二（2周）** | 场域呈现器（WebGPU + 高斯泼溅） | 一 | ⏳ **下一轮** |`
- **实测**：`host/field-render/Cargo.toml` 头部自陈「**场域呈现器（wgpu 独立管线）**…依赖 `wgpu = "23"`／`winit = "0.30"`／`pollster`／`bytemuck`／`egui-wgpu`」，且注释明写「**与 wry 版 sky-browser 完全独立：那条是降级路径，这条是不依赖 WebView2 的场域渲染**」；`host/field-render/src/` 有 `main.rs`／`window.rs`／`gene_store.rs`；`shaders/sort.wgsl` 在库；**`.github/workflows/ci.yml` 有专门 job**：`宿主 field-render（Windows 构建 + 客观验收）`，内含「构建宿主 field-render（GNU + llvm-mingw）」「单元测试 field-render（独立 crate）」。
- **应改为（建议，待裁）**：
  - #3 → `✅ **已实现**（host/field-render，wgpu 独立管线；CI Windows job 构建 + 单测）`；
  - #4 → `✅ **已实现**（同上；即"不依赖 WebView2 的宿主"）`；
  - #7 → `🟡 **部分**（native 路径已不依赖 WebView2；WebView2 保留为降级/对照）`；
  - §5 阶段二 → `✅ **已完成**`。

### P3 · `docs/FIELD_RENDER_SOURCELESS_DESIGN.md` L104（**数值型 · 中**）

- **原文**：`| 1 编译与单测 | cargo build/test 全绿 | ✅ 内核 **343** 测试全绿；CI 全绿 |`
- **本轮实测**（`--list` 计数，`--target-dir` 指系统 Temp）：**`meta-kernel-core --lib` = 400**｜**`meta-kernel-core-nostd --lib` = 477**。
- **判定**：`343` 是 **v0.114（2026-09-15）时点值** ⇒ **静默过期**（R82）。**建议加锚**（如「截至 v0.114：343」）**而非改数**（改数会把历史验收记录变成不可复现的陈述）。

## 4. ★ 文档间**自相矛盾**（C19 同族 · 同一事实两处口径相反）

| 事实 | 一处 | 另一处 |
|---|---|---|
| **场域呈现器是否已实现** | `FIELD_RENDER_SOURCELESS_DESIGN.md` §3#3 ⇒ **⏳ 未做** | `FIELD_PRESENTATION_DESIGN.md` L126（三周冲刺·第二周）⇒ **场域呈现器 ✅** |

- **裁决依据（本稿）**：以**代码落点**为准 ⇒ **已实现**（`host/field-render` ＋ CI job）。
- **⇒ 两份底图对同一事实给出相反状态**（R79「两判据相反 ⇒ 同源＋概念分离」的**文档侧同族**）。
- **建议**：以 §3 修订为准（P2），使两份口径收敛。

## 5. 历史段（**不改，只建议加锚**）

| 文件 | 行 | 内容 | 处置 |
|---|---|---|---|
| `CREATION_LOG.md` | L23–27 | Day 1 日志：`27/66/81 测试全绿`、摘要 `4251318995` | **历史留痕**（含日期）⇒ 不改 |
| `PHASE1_REVIEW.md` | L9 | `25 单元测试 + 2 集成测试，0 失败（run #… ／ commit …）` | **历史快照**（附 run/commit）⇒ 不改 |
| `SECURITY_TROUBLESHOOTING.md` | L72／L84 | 老笔记本跨机闭环、IP 漂移对策 | **已发生的事实** ⇒ 不改 |

## 6. 待核项（**低置信，未定论**）

- **`L5_DESIGN.md` L172／L191**：`⚠️ 边界：**L5 诊断主链尚未改为"从场景公式读取本底场"**（当前仍用 l5_baseline 数值地图）——下一步接线。`
  - **未定论原因**：需读 `meta-kernel-core/src/l5/` 主链当前实现才能判定"是否仍用数值地图"。**本稿不臆断**（**不妄语**）。
  - **建议**：留作**第四批**首项，或由你指定由谁取证。

## 7. 本批未发现过期的 12 份

`CONTRAST_REPORT`／`COSMIC_COMPUTING`／`FIELD_REPAIR_DESIGN`／`HOST_BUILD`／`L4_VALIDATION_REPORT`／`L5_VALIDATION_REPORT`／`MATH_SPEC`／`ONTOLOGY_SPEC`／`PERFORMANCE_REPORT`／`PHASE1_ASSUMPTIONS_REVIEW`／`PHASE4_ACCEPTANCE_REPORT`／`QUANTUM_CLOCK_DESIGN`／`VISION`（＋`PERTURBATION_MODEL`，计 13 份 0 命中）。

## 8. 边界说明（四类）＋ 只出稿声明

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | 扫描器实跑（20 份）；**测试计数实测**（`--list`：core 400／nostd 477，`--target-dir` 指系统 Temp） |
| **CI 过** | **不适用**（本稿不改文件 ⇒ 不触发 CI） |
| **真实跑过** | **不适用**（未运行渲染/宿主程序） |
| **未验证** ⚠️ | ① `host/field-render` 的**实际渲染效果**未在本轮运行（只读代码 ＋ CI 定义）；② §6 待核项；③ **本稿未改任何文件** ⇒ 上述"应改为"**均为建议** |

> **只出稿声明**：本稿**未改 `docs/` 任何一份、未改治理文件、未提交**。

---

## 附：6.1 `advisor_brief.md` 全文时效核对（**第一批已完成 · 本轮收口**）

- **第一批**：`coordination/reports/2026-09-20_advisor_brief全文时效核对稿_二.md`（**已出稿**，280 行对象）。
- **其唯一未决项**：**P2 @L35**（「不做 f64 版」易被读成"f64 不存在"）⇒ **已于本轮裁定 5 落地**（加限定：`「不做 f64 版」＝不承诺 f64 精度门线；f64 函数本身后经 fmath 补齐`）。**逐字对照见本轮主报告 §四。**
- **本轮复扫**（风险词全文匹配）：**未发现新增过期项**。L33「原 core 411 项全绿」带「**原**」字 ⇒ 属**历史锚定陈述**（当前实测 core 400／nostd 477，§3-P3 同源事实）⇒ **不改**。
- **结论**：**6.1 视为完成**（无遗留）。
