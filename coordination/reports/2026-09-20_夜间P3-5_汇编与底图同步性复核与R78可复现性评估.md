【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 夜间 P3-5 · 汇编副本 ↔ 底图 同步性复核 ＋ 同步判据「可复现性」评估（只读，只出稿）

**保密标记**（C12）：⛔ 机密 **无**｜🔐 保密 **无**｜🔒 半公开：仓库内相对路径与行号、编号体系、覆盖统计口径｜🔓 公开：结论。
> ⚠️ **本报告遵 R25／C12 做掩码**：**不写本机绝对路径、不写局域网 IP、不写本机用户名**；凡涉及处一律抽象为「**含本机路径／内网 IP 的行**」。

**任务**（机制 19／C14 夜间 P3）｜**基准** v0.259｜**定位**：阶段二 · 子任务2｜**优先级 P3**｜白名单类别＝**报告归档／基准维护（BASELINE 以外的归档）**｜**边界：纯只读比对；不建文件、不入 CI、不改汇编、不改底图、不改治理文件、不提交远端**

**状态**：完成（**复核结论 ＋ 评估**）

---

## 一、做了什么

① 对 `coordination/discussions/` 的**四批汇编正文 ＋ 合订本**（共 **5 件**），与**当前** `docs/`（35 份 `.md`）＋ 根 `README.md` 做**逐行逐字比对**，列出**差异份数／差异行**；
② 评估**同步判据的落点**：R78 的"同步判据"当前是否**可被他人复现**；
③ 给出「是否值得在仓内固化同一口径」的**评估结论**（**今夜只评估**：不建文件、不入 CI）。

---

## 二、逐行同步性实测

**测量口径**：汇编正文以 `行号│原文`（`NNN│`，U+2502）形式**逐字收录**底图原文（见汇编第 1 批抬头 **C5**：「**逐字收录**底图原文；**行号**以每行前缀 `NNN│` 标注」）⇒ 比对时**必须先剥去 `^\s*\d+│` 前缀**（**本项第一个实测教训**，详见 §六）。
**测量件**：**建在仓库外**（不入库），独立脚本，只读。

| 指标 | 实测 |
|---|---|
| 汇编语料 | **5 件**：四批正文 ＋ 合订本｜总行 **12,566**｜去重后不同行 **4,302** |
| 底图源 | **36 份**（`docs/*.md` **35** ＋ 根 `README.md` **1**） |
| 有效比对行 | **4,317**（剔空行与 `---`／纯符号等通用行） |
| **命中** | **4,205** |
| **缺失** | **112** |
| **逐行命中率** | **97.41%** |
| **有缺失的文件** | **4 / 36**（其余 **32 份全命中**） |

### 差异明细（**4 份有缺失**，逐份定性）

| # | 文件 | 有效行 | 缺失 | **定性（实测依据）** |
|---|---|---|---|---|
| 1 | `docs/FULL_AUDIT_REPORT.md` | 195 | **16** | ✅ **掩码差异，非过期**：缺失行**全部**含「本机路径／内网 IP」；汇编同号行确实存在，形态为 `〈本机用户名〉`／`〈局域网 IP〉` 掩码（实测见汇编第 4 批 L1193／L1278／L1302 等） |
| 2 | `docs/L5_VALIDATION_REPORT.md` | 89 | **3** | ✅ **掩码差异，非过期**：同为含内网 IP 的"真实采集"节（汇编 L259／L261／L262 已掩码） |
| 3 | `docs/PERFORMANCE_REPORT.md` | 89 | **1** | ✅ **掩码差异，非过期**：含内网 IP 的一行（汇编 L430 已掩码） |
| 4 | **根 `README.md`** | 100 | **92** | ★ **未收录**（**非掩码**）：`README.md` 独有行在汇编 5 件中**命中 0**（阳性对照：该行确实存在于 `README.md`）⇒ **汇编不含根 README 正文** |

**掩码统计**（阳性取证）：汇编 5 件中掩码行合计 **24 行**，**全部集中在** `第4批_验证与自诊断`（**24**）与**合订本**（**24**，同一内容）；第 1–3 批 **0**。汇编抬头亦明写该口径：「凡底图原文含**本机用户名／局域网 IP** 的行，均掩码为 `〈本机用户名〉` / `〈局域网 IP〉`」（依 **R25**）。

### ⇒ 同步性结论

- **`docs/` 35 份：逐行同步成立**（除 **20 行**属**既定掩码差异** ⇒ **不构成过期**）。
- **根 `README.md`：未收录**（92 行缺失）。汇编与合订本**自述口径**为「基线**只覆盖 `docs/` 的 35 份 `.md`**；根 `README.md` **不在基线内**」（合订本 L50）。
- ⚠️ **但按机制 22，「底图 ＝ 根 `README.md` ＋ `docs/`」共 36 份**（`check_source_of_truth --ci` 实测**底图 .md 数 = 36**）⇒ **汇编覆盖 35/36 ⇒ 存在 1 份覆盖缺口**（与 **R86「汇编覆盖范围缺口」**同族；09-20 清单 §三 亦已登记）。
- **需澄清的口径冲突**（**只登记，不代行**）：汇编 L50 说"基线只覆盖 docs/ 35 份"，**而机制 22 说底图 36 份** ⇒ **同一"底图"两处口径不同**（**这正是 R86 的形态**）。

---

## 三、R78「同步判据」**可复现性**评估

| 检项 | 实测 | 结果 |
|---|---|---|
| 同步脚本是否在**仓内**（`verify_asm.py`／`verify_binder.py`／`asm_selfcheck_0920.py`） | `git ls-files \| grep -i "verify_asm\|verify_binder\|asm_selfcheck"` ⇒ **无命中** | ❌ **不在仓内** |
| `coordination/tools/` 内是否有"汇编"类判据 | `grep -rli "汇编" coordination/tools/` ⇒ **0 件** | ❌ **无** |
| **阳性对照**（证明检索式有效、产物确在仓内） | `git ls-files -- coordination/discussions/` ⇒ **23 份**被跟踪产物 | ✅ 检索式有效 |
| **阴性对照** | `git ls-files \| grep -ci "zzz_not_exist"` ⇒ **0** | ✅ |
| 相关非判据脚本（如 `asm_binder.py`，仅**生成**用） | 亦**不在** `coordination/tools/`（memory 记其"改 §五须改 `asm_binder.py`"，该脚本在**仓外**） | ❌ |

**⇒ 评估结论**：**R78 的"汇编与底图同步"这一结论，当前在仓内不可复现**。
- **性质**：与 **R89「声明 ≠ 发生」**同族；亦与 **R32「判据不可复现」**同族。
- **可复现性评级**：**不可复现（他人拿到仓库无法自行验证）**。**本报告 §二 的比对结果**是目前**唯一**一份"在仓内可据以复核"的口径记录 —— 但它**是报告，不是判据**（**下次仍要重做**）。

---

## 四、是否值得在仓内固化同一口径（**只评估**）

| 方案 | 内容 | 成本 | 风险 | 收益 |
|---|---|---|---|---|
| **① 不固化**（维持现状） | 每次靠临时测量件 | 低（本次已付） | **中**：底图变更后汇编会**静默过期**（**R78 本体**），且**无人能复现** | 0 |
| **② 固化判据（推荐）** | 把本报告 §二 口径写成 `coordination/tools/check_assembly_sync.py`：**剥 `NNN│` 前缀 → 逐行比对 docs/ ＋ 根 README → 报告缺失行**；**掩码行纳入白名单**（只减不增） | 中（1 脚本 ＋ `--selftest` 正反对照 ＋ 1 条 CI 步骤） | 低（只读判据，不写底图） | **高**：R78 由"声明"升为**可复现判据**；且能**同时暴露 R86 覆盖缺口**（README 未收录会**直接判红或告警**） |
| **③ 只固化"覆盖清单"** | 仅登记"汇编应收录哪 36 份"（数据），不写比对逻辑 | 低 | 低 | 中（解决 R86 口径，**不解决 R78 过期**） |

**七要素（C11 · 供你拍板，本报告不代行）**：
- **评估**：R78（汇编随底图变更而静默过期）＋ R86（覆盖范围缺口）**同根** —— 缺的是**一条可复现的同步判据**。
- **优势**（≥2）：① 方案 ② 使"同步"**可被任何人复现**（消除 R89／R32 家族）；② 顺带把 **R86 的 1 份覆盖缺口**（根 README）变为**机器可见**。
- **劣势**（≥2）：① 需处理**掩码白名单**（否则 20 行掩码差异会**常亮红** —— 与 fidelity 的 `ALLOWED_SUBS` 缺口同型教训）；② **汇编本身在 `coordination/`（非底图）** ⇒ 判据把"脚手架"纳入受检，须先确认**归属**（机制 21 口径）。
- **风险**（标概率/影响）：**R-1 掩码白名单漏登 ⇒ 常亮红**（概率**高**，影响**中**）；**R-2 汇编重排（篇序变更）⇒ 假红**（概率**中**，影响**中**，本夜已实测一次同类：**我自己的探针漏剥前缀 ⇒ 假红 96%**）。
- **机会**：若固化，可把"**汇编同步**"与"**底图哈希**"（判据 F）串成**一条链**：底图变 ⇒ 汇编必同步 ⇒ 否则判红。**这是 R78 的根治形态。**
- **建议**：**推荐方案 ②，但须白天做**（建判据 ＋ 可选入 CI；且入 CI 须改 `ci.yml` ⇒ **夜间禁改**）。**今夜只评估，不落码。**
- **等待决策**：请你在 **①／②／③** 之间择一（或继续留待）。

---

## 五、三量台账（C6）

| 量 | 内容 |
|---|---|
| **存量** | 汇编 5 件（12,566 行）＋ `docs/` 35 份 ＋ 根 `README.md`（共 36 份／4,317 有效行）全部逐行参与比对；汇编抬头 C1–C5 口径 ＋ 合订本 L50／L1688；R78／R86 登记（`BASELINE.md` §15.5／§16.6／§23.5）；机制 22 底图口径；掩码规则（R25）逐处取证 |
| **变量** | **0** —— **未改汇编、未改底图、未改治理文件、未改代码** |
| **补充增量** | **1 份**：本报告（**新写非覆盖**） |
| **创新增量** | **1 个**：**仓外测量件**（剥前缀 ∨ 逐行比对，可复用作 R78 判据骨架）—— **不入库** |

---

## 六、真实缺陷与**可复用方法**（只登记）

**缺陷-1（R89／R32 家族）：R78「汇编同步」在仓内不可复现** —— 证据见 §三（三重检索：`git ls-files` 无同名／`tools/` 内 0 命中／阳性对照 23 份产物在仓内）。

**缺陷-2（R86 家族）：汇编覆盖 35/36，根 `README.md` 未收录；且"底图"口径两处不一** —— 证据见 §二（README 独有行命中 0）＋ 合订本 L50（"只覆盖 docs/ 35 份"）↔ 机制 22（底图 36 份）。

**★ 可复用方法（本夜教训，建议入册）**：
1. **汇编比对必须先剥 `NNN│` 前缀**。本夜**首次测量**未剥，命中率只有 **3.80%**（假红）；剥后 **97.41%**。⇒ **凡"带行号前缀的逐字副本"，比对器第一步必须归一化前缀**，否则会把"格式差异"误判为"内容过期"（**假红**）。
2. **掩码差异 ≠ 过期差异**。判定时须查**同号行**是否存在掩码形态（`〈…〉`）；**掩码是 C12 的既定处置，不是同步失败**。若不区分，会得出"汇编严重过期"的**错误结论**（本夜若不查证，FULL_AUDIT 的 16 行会被误报为过期）。
3. **三态取证法**（本次沿用）＝「阳性对照（产物在仓内 23 份）＋ 阴性对照（不存在串 ⇒ 0）＋ 检索式本身」⇒ 用于**"不在仓内"类断言**（**C16 四要素**）。

---

## 七、边界说明（C5；四类齐全）

- **本地跑过**：✅ 有。逐行比对 **4,317 行 × 36 份**（仓外测量件，只读）｜掩码行取证（5 件／24 行）｜README 覆盖取证（独有行命中 0，阳性对照通过）｜同步脚本三重检索（阳性／阴性对照齐备）｜阳性对照 `check_source_of_truth --ci`（**底图 36 份**）。
- **CI 过**：❌ **无** —— 只读、未 push、未触发 CI。按 **C20**：CI 结论不入库。
- **真实跑过**：❌ **无** —— 未跑 QEMU、未上真机（本项不涉）。
- **未验证**：① 本比对是**逐行集合命中**（非"逐文件逐位 §"级重建），**行序与上下文未验**；② **重复行会掩盖"该行位置错乱"**（集合法固有盲区）；③ 汇编**合订本与四批正文的重复内容**未去重后再比（合订本含四批全部内容 ⇒ 掩码 24 行在两处重复计数）；④ R78 的**历史结论**（"当时已生效"）**未追溯验证**（只验"现在是否可复现"）；⑤ **未查平台侧／仓外目录**的实际脚本轨迹（**无法观测**，不反推）。

---

## 八、建议动作（**均待确认**）

| 优先级 | 动作 |
|---|---|
| **P1（白天）** | ★ **R78 口径澄清**：汇编 L50「只覆盖 docs/ 35 份」↔ 机制 22「底图 36 份」**择一为准**（并据此定 **R86** 缺口处置：**补收录根 README，或正式声明"汇编范围为 docs/ 35 份"**） |
| **P2（可夜间 · 建议单独立项）** | 把 §二 口径固化为 `check_assembly_sync.py`（**含掩码白名单 ＋ `--selftest` 正反对照**）；入 CI 部分**留白天** |
| **P3（可夜间）** | 把本报告「**剥前缀**」「**掩码 ≠ 过期**」两条方法写进判据工具的注释头（**不建新文件**，随 P2 一并做） |

---

**启动回执**（`CHARTER.md` §五）：已读 `CHARTER.md`（机制 1–27，含**机制 22 底图口径**）＋ `CONSTRAINTS.md`（**C5／C6／C12／C16／C20**）＋ `BASELINE.md`（§15.5 R78／§16.6／§23.5 R86）＋ 汇编 5 件抬头 ＋ 09-20 清单 P3-5 项。
当前状态：**v0.259**（开工时；本夜 4 个本地 commit 后为 **v0.263**）｜本次任务：汇编副本 ↔ 底图同步性复核 ＋ R78 可复现性评估（**P3**，只读 ＋ 只写本报告）｜是否继续：**是**（本夜 5 项已全部完成，进入收尾汇报）。
