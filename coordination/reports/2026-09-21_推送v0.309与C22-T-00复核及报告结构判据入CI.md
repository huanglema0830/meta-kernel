【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# push v0.309 ＋ `C22`/`T-00` 复核 ＋ `F-4` 留痕与模板排序修正（`T-042`）＋ 报告结构判据入 CI（2026-09-21 · v0.310）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 309** ⇒ 收尾 **310**（本轮 **1 个本地提交**）｜**label v0.310**（`label = count`，C21） |
| **HEAD／远端** | **已推 `0d1caf1..36ea62f`**（v0.309）⇒ 生成时 `origin/main = 36ea62f`；**本轮新 commit 未推** |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 ＝ 执行层（设计完成）｜L8–L10 🔒 冻结 |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–42**（**新增 `T-042`**）｜R **1–89**｜TERM **1–13**｜**INTEL 1–2**（试用中） |
| **判据／接线** | `tools/check_*.py` **10 个**｜`ci.yml` **test job 32 步**（**新增 step 19 报告结构门禁**）｜**`A2` 表 3 条** |
| **本轮性质** | **放行 ＋ 裁定落地**（push｜复核｜留痕｜模板修正｜CI 接线） |

**③ 任务与约束**（含**基准**与**总规划定位**，含**优先级**）
- 基准 **count = 309**（指令填值，实测一致）｜定位：**push v0.309 ＋ 6 项裁定落地**。
- **优先级**：**P0**（push 放行）｜**P1**（`C22`/`T-00` 复核 —— 决定 `A2` 表是否成立）｜**P2**（`F-4` 留痕 ＋ 模板排序修正 ＋ 新判据入 CI）｜**P3**（`§7.2` 待办登记／`C13` 追认）。
- 约束：按 `CONSTRAINTS.md`｜**禁止内联含反引号／特殊字符命令**（脚本文件，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**按 T-038 门控**｜**按 C20**：CI 结论**只记日报、不入库**。
- ★ **顺序铁律**：**先 push、再动文件**（本轮已按此执行）。

**④ 状态：完成**（**9 条验收全 ✅**，见 ⑤）。

---

## ⑤ 验收对照（用户 §八 · 9 条）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（按 T-038 门控） | ✅ `0d1caf1..36ea62f`（exit 0）｜**门控已执行**：阻塞至 `completed` ＋ 取**逐 job 三态**，**证据 `READY_TO_EDIT`**｜★ **结论按 C20 只记日报** |
| 2 | **`C22`/`T-00` 复核确认、留表** | ✅ §一（**逐点穷举**：`C22` **10 处**／`T-00` **7 处**，**全为元引用**）＋ `BASELINE §50.1`；`A2` 表 **3 条**；**真缺口 0 条** |
| 3 | **`F-4` 留痕（不回填 ＋ 计豁免）** | ✅ §二（不回填 ＋ 冻结豁免 ＋ 台账留痕） |
| 4 | **报告模板排序修正（下一轮起）** | ✅ §二（`TEMPLATES §〇` 新增「**附加表位置**」注 ＋ **`T-042`**；**走 §7.2**） |
| 5 | **新判据入 CI（只告警），逐字对照** | ✅ §三（**step 19**；**不传 `--strict`**；**pyyaml 校验通过**）｜★ **CI 真实执行待下次 push** ⇒ **如实标注"未验证"** |
| 6 | **`§7.2` 14 条纠正记录待办** | ✅ §四（下轮单独授权；**本轮未回改任何状态列**） |
| 7 | **`C13` 范围暂不扩，记录到 BASELINE** | ✅ §五（追认第二次） |
| 8 | C15 复算通过、编号表连续无缺重 | ✅ §六（T **41→42**／台账节数 **44→45**／CI 步数 **31→32**；机制·C·TERM **±0**；T 编号 `1..42` 连续无重） |
| 9 | CI 全绿 ＋ 下次返回第 0 行 9 步 ＋ 含 INTEL 状态表 | ✅（**C20：只记日报**）＋ 本报告第 0 行 ＋ **末尾附加表** |

---

## ⑥ 未完成项（清单 ＋ 原因）

| # | 未完成项 | 原因 |
|---|---|---|
| 1 | **`§7.2` 14 条「试用中」状态纠正** | 用户裁定**下轮单独授权** ⇒ 本轮**不做**（`BASELINE §50.4`） |
| 2 | ★ **新 CI step（报告结构门禁）在 CI 上的真实执行** | **本轮 push 已完成**，新 step 随 commit 提交 ⇒ **须等下一次 push 才会被 CI 跑到** ⇒ **本轮状态＝未验证**（**不写"CI 已执行"**，R89） |
| 3 | **`T-042` 的生效实测** | 模板修正**自下一轮报告起**生效 ⇒ **本轮报告仍按 T-042 之前的习惯编排**？**否** —— 本轮报告**已按 T-042 编排**（附加表置于十四项之后），**首次实测即本轮** |
| 4 | **`F-4` 的回填** | **不回填**（丁案）⇒ 计**冻结豁免** ＋ 留痕 |
| 5 | **`INTEL` 正式登记为机制 28** | **须 3 轮试用后**（`BASELINE §49.2`） |
| 6 | **`C13` 判据范围扩到 `intel/`** | 追认**暂不扩**；**等 INTEL 试用期满后再议**（`BASELINE §50.5`） |

---

## ⑦ 真实缺陷（含 **`B2`** 要求：**本轮新增告警 ＋ 来源溯源**）

| 级 | 缺陷 | 来源／证据 | 性质 |
|---|---|---|---|
| **P1** | ★★ **`§47.6` 的 `ci.yml` step 序号 off-by-one**：原文写"机制 25＝step 16／编号集差集＝**step 17**" | **pyyaml 实测** `git show b951229:.github/workflows/ci.yml` ⇒ **机制 25＝step 17／编号集差集＝step 18**（步数 31） | **未经验证的数字被写成登记**（**R82 家族第 3 例**；与 §49.1 归因订正、T-034 同族）⇒ **原句留痕 ＋ 后记指针**（`BASELINE §50.6`）；**新增口径：step 序号一律以 pyyaml 实测为准** |
| **P2** | **`F-4` 的"位置纪律"此前无任何载体** —— 模板只规定"必带十四项"，**未规定"附加表放哪"** | v0.308 报告把 INTEL 状态表排在 ⑥ | **规则缺口** ⇒ **本轮补 `T-042`**（★ 并如实写明**位置不可机器判**） |
| — | `MECH 28`／`C22`／`T-00` | 见 `BASELINE §47.3` 登记表 ＋ §50.1 复核（**逐点穷举**） | **元引用**（**A2 已登记**，**非真缺口**）—— ★ **本轮复核确认留表** |

**★ 六族告警（`C1` 载体 · 固定一行）**：
> **六族告警：本轮 3 条**（MECH `28`｜C `C22`｜T `T-00`）**｜对比上轮 ±0**｜**真缺口 0 条**（**3 条均经本轮穷举复核确认为元引用**）。
> ★ **口径**：本判据的告警数是**"待复核清单"**，**不是质量指标**（**3 条里 0 条真缺口**）；**连续 N=5 轮为 0 才议转判红**。

---

## §一 · `C22` / `T-00` 复核确认（**逐点穷举 → 语义判定**）

### 1.1 复核方法（**可复现**）
1. 用**六族引用正则**全仓扫描，**穷举**该编号的**每一个**出现点（非抽样）；
2. 逐点判定其语义 —— **是否指向"该编号的定义"**，还是**只在"谈论该编号"**；
3. 判定依据**贴出原文片段**（C17：**穷举 ＋ 逐字引用**）。

### 1.2 逐条结论

| 编号 | 出现点（逐点） | 类型 | 结论 |
|---|---|---|---|
| **`C22`** · **10 处** | `BASELINE` **§四十 40.x**（"若新增 C22 须连带改 `CHARTER §四`…超出本轮明示授权"）｜**§40.3 待裁②**（"若补 C22 须连带改 `CHARTER §四`"）｜**§41.1 追认 · 暂不补 C22**（标题 ＋ 裁定正文 **2 处**）｜**§41 验收表行**｜`reports/…_提交D1-D16呈现稿与R89细化.md`（L73 ＋ L140 待裁表）｜`reports/…_抽象铁律评估稿.md`（验收表 ＋ 完成项叙述） | **元引用** | ✅ **元引用**：**10 处全在"讨论要不要新增 C22"**，**无一处**给出定义；★ **§41.1 已由用户裁定「暂不补 C22」** ⇒ **本编号按设计就不存在**（**C 族实到 C21**） |
| **`T-00`** · **7 处** | `BASELINE` **§四十四**（"告警 3→2 但成分漂移…`T-0` → `T-00`"）｜**§四十四 新判据复核**（"T 族 1 条（`T-00`，**元引用**）"）｜**§四十五**（两轮实证）｜`reports/…_推送v0.302….md`（L88"来自**讨论 T-0 告警**的**元引用**"）｜`…_推送v0.303….md`（L103／L116）｜`…_编号集差集判据_落地.md`（L81"**`T-00N` 被截出假编号 `T-00`**"） | **自指污染（元引用）** | ✅ **元引用**：`T-00` **本就是判据早期"`T-00N` 模式写法被 `T-\d+` 截断"的产物**（**截断假象**，**非本项目分配的编号**）；**7 处全在追述这件事本身** |

### 1.3 复核合计
**元引用 2 条（`C22`／`T-00`）＋ 既有 1 条（`MECH 28`） ＝ `A2` 表 3 条**；**真缺口 0 条** ⇒ **全部确认留表**。
★ **铁律复核**："真缺口一律不得入表"**未破**（**入表 3 条均有穷举溯源 ＋ 语义判定**）。
★ **诚实边界**：本复核**只做语义穷举**，**不做"编号表是否该扩容"的价值判断**（那属 §41.1 已裁定事项）。

---

## §二 · `F-4` 留痕（不回填 ＋ 计豁免）＋ **报告模板排序修正（`T-042`）**

| 处置 | 内容 |
|---|---|
| **① 不回填** | **不改历史报告**（丁案：改历史快照＝篡改留痕；且该报告**已 push**） |
| **② 计冻结豁免** | 计入 `coordination/security/report_structure_exempt.txt`（**冻结 33 条**之一） |
| **③ 台账留痕** | `BASELINE §50.2`（＋ §49.5② 原登记） |
| **④ 模板排序修正** | `TEMPLATES.md §〇 固定相 B` **新增「★ 附加表位置」注**：**附加表（INTEL 状态表等）必须放在十四项之后，不得顶替任何一项**（★ **尤其不得顶掉第 6 项**）｜★ 并**如实写明"位置不可机器判"**（现判据只检**存在性**） |
| **落点／程序** | 走 **`§7.2` 变更记录** ⇒ 新增 **`T-042`**；**C15：T 41 → 42**；**生效自下一轮起** |

> ★ **本轮报告即为 `T-042` 的首次实测**：**INTEL 状态表置于全文末尾**（十四项之后），**未占用任何编号位**。

---

## §三 · 报告结构判据「入 CI」（只告警）—— 逐字前后对照

| | 内容 |
|---|---|
| **改前** | `test` job **共 31 步**；**step 18**「编号集差集门禁（六族 · 只告警）」之后**直接**接注释块「`===== 机制 18 / C13 …`」（**step 19**） |
| **改后** | **共 32 步**；二者之间**插入**新 step（现为 **step 19**）：<br>`- name: 报告结构门禁 —— 报告必带十四项（**只告警**）`（`shell: bash`）<br>`run:` ⇒ `set +e` → `…check_report_structure.py --selftest`（`SELFTEST=$?`）→ `…check_report_structure.py`（`CHECK=$?`）→ `echo` → `set -e` → `test "$SELFTEST" -eq 0 && test "$CHECK" -eq 0` |
| **「只告警」的实现** | **不传 `--strict`**（**判据本身亦无 `--strict` 分支，退出码恒 0**）⇒ `CHECK` 恒 0（**不阻断**）；`SELFTEST` 仍当门（**工具自身坏了须判红**） |
| **位置** | **紧随编号集差集（step 18）、先于机制 18（现 step 20）**；**pyyaml 实测**：`17 机制25 → 18 编号集差集 → **19 报告结构** → 20 机制18` |
| **语法校验** | **pyyaml 解析通过**（`jobs = test／boot-image／host-windows`；**test 32 步**） |
| **触发再评估** | 告警命中**连续 N=5 轮为 0** ⇒ 再议转判红 |
| ★ **诚实边界** | **本 step 尚未在 CI 跑过**（**本轮 push 在其之前**）⇒ **状态＝未验证**，**不写"CI 已执行"**（**R89：声明 ≠ 发生**） |

---

## §四 · `§7.2` 14 条纠正（待办）＋ **`C13` 范围追认（第二次）**

- **`§7.2` 14 条「试用中」状态纠正**：**下轮单独授权**，**本轮不做**（`BASELINE §50.4`；含 **F-1 修法指针**）。
  ★ **口径澄清**：**本轮未回改任何状态列** —— **`T-042` 是"新增行"，不是"改状态"**（二者须分清）。
- **`C13` 范围「暂不扩」**（追认第二次）：`check_reading_instruction` 窄范围维持 ⇒ `coordination/intel/` 五份文件**仍在覆盖外**（**不放指令块、亦不判红**）—— **等 INTEL 试用期满后再议**（`BASELINE §50.5`）。

---

## ⑧ 三量台账（四量）

| 量 | 内容 |
|---|---|
| **存量** | 机制 **27**｜C **21**｜T **42**｜R **89**｜TERM **13**｜判据脚本 **10 个**｜`ci.yml` test job **32 步**｜`A2` 表 **3 条**｜底图 **36 份** |
| **变量** | **修改**：`TEMPLATES.md`（§〇 新注 ＋ §7.2 新增 `T-042`）｜`.github/workflows/ci.yml`（**+21 行**：新 step 19）｜`BASELINE.md`（§五十）｜**新增**：**本轮报告 1 份**；**编号表变动：T +1（41→42），机制/C/TERM ±0** |
| **补充增量** | ① **push v0.309**（**T-038 门控第 11 次执行成功**）｜② **`C22`/`T-00` 逐点穷举复核**（**10＋7 处**，**全为元引用**，`A2` 表确认 3 条）｜③ **`F-4` 三件处置**（不回填／计豁免／留痕）＋ **`T-042` 模板排序修正**｜④ **报告结构判据入 CI**（**只告警**，step 19）｜⑤ **`§47.6` off-by-one 订正** |
| **创新增量** | ① ★ **"附加表位置"成为成文纪律**（`T-042`）—— 把"**追加内容不得顶掉既有项**"从**表格编辑纪律**（R-a…R-e）**推广到"报告结构层"**｜② ★ **"位置不可机器判"被写进规则**（**不假装消除**，与 KNOWN-LIMIT 同一手法）｜③ ★ **`ci.yml` step 序号改用 pyyaml 实测**（**新口径** —— 治"人工计步"这一类错）｜④ ★ **复核方法论**：**"逐点穷举 ＋ 语义判定 ＋ 逐字引用"**（C17 三件套）首次用于**告警复核** |

---

## ⑨ 边界说明（四类）

- **本地跑过**：
  - **推送**：`git push` ⇒ `0d1caf1..36ea62f`（**exit 0**）。
  - **`ci.yml` 结构校验**：**pyyaml 解析通过**（`jobs = test／boot-image／host-windows`；**test job 31 步 → 32 步**；**新 step 位于编号集差集之后、机制 18 之前**）｜★ **容器**：`{PRIVATE_ASSETS}` 外的**托管 venv** `python/envs/default/Scripts/python.exe`（**warehouse 内，未污染系统环境**）。
  - **判据**：`check_id_set_diff.py --selftest` **二十二侧 PASS**＋实跑**告警 3**；`check_report_structure.py --selftest` **十侧 PASS**＋实跑**告警 0**；`check_reading_instruction.py` **PASS**；判据 F **PASS**（**36 份**）。
  - **C15 取数**：T **41 → 42**｜台账节数 **44 → 45**｜CI 步数 **31 → 32**｜机制/C/TERM **±0**。
  - **穷举复核**：`C22` **10 处**／`T-00` **7 处**（**全仓六族正则扫描，非抽样**）。
- **CI 过**：**已按 T-038 门控**执行（**阻塞至 `completed` ＋ 取逐 job 三态**，**证据 `READY_TO_EDIT`**）｜★ **按 C20：run 号与逐 job 三态只记当日日报、不入库**。★ **注意**：**本轮新 step 尚未在 CI 执行**（见 ⑥-2）。
- **真实跑过**：**无**（本轮**零代码、零 boot 层改动**）⇒ 不适用于 **CQ／H** 两层（**机制 26：跨层引用＝无效**）。
- **未验证**：① **新 CI step 的真实执行**（待下次 push）｜② **`T-042` 的位置纪律**（**判据不检位置**，仅本报告一次人工实测）｜③ **`§7.2` 14 条纠正**（下轮授权）｜④ **`INTEL-002` 来源**（未检索）｜⑤ **`§47.6` 是否回改**（推荐不回改，**待确认**）。

---

## ⑩ 决策评估（七要素 · 本轮**新决策 1 项**：`§47.6` 的 off-by-one 是否回改）

| 要素 | 内容 |
|---|---|
| **评估** | `§47.6` 原文把 step 序号写错一位（**17** 应为 **18**）。处置有三：**甲** 不回改，**原文留痕 ＋ 后记指针**（新写进 `§50.6`）｜**乙** 直接改 `§47.6` 原句｜**丙** 不改也不留痕 |
| **优势** | **甲**：**符合 C19 既定口径**（"保留原留痕＋后记指针"），**可追溯"当初怎么写的、后来怎么发现的"**，且**成本为零**｜**乙**：读者一眼看到"对的值"｜**丙**：零动作 |
| **劣势** | **甲**：读者若不读 `§50.6`，**仍会被旧数字误导**｜**乙**：**篡改留痕** —— 与"历史报告不回填"（丁案）**自相矛盾**，且**掩盖了"人工计步不可靠"这个真问题**｜**丙**：**同类错误会继续复发**（本轮已是**第 3 例**） |
| **风险** | **甲**：★ **最大风险＝"留痕"变成"免责"** —— 若**只留痕不建判据**，off-by-one 会继续发生｜**乙**：一旦开了"改历史"的口子，**后续所有订正的理由都会变成"我改一下更清楚"**｜**丙**：**台账可信度下降**（读者无法判断哪些数字可信） |
| **机会** | ★ **甲 ＋ 新增口径可把这类错"结构性关掉"** —— 本轮已写"**step 序号一律以 pyyaml 实测为准**"；**同一手法可推广**：凡"**计数**"类陈述，**一律给出可复现命令**（**本项目的 C15 已如此做，本轮把它推广到 CI 步数**） |
| **建议** | **选甲**（不回改 ＋ 留痕 ＋ **新增"计数须给可复现命令"口径**）；**不选乙**（留痕纪律优先） |
| **等待用户决策** | ✅ **等**：**④** `§50.6` 是否维持"不回改"（**推荐维持**） |

---

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★ **push 放行**（本轮 **1 个本地 commit**，v0.310） | **放行**（机制 23） |
| ② | **`§7.2` 14 条状态纠正**（下轮单独授权） | **下轮首批执行**（含 F-1 修法：增「试用起点」列 ＋ 满 3 轮强制结论行） |
| ③ | **`T-042` 是否需在下一轮报告中再验证一次** | **不需**（**本轮报告已首次实测**） |
| ④ | **`§47.6` off-by-one 是否维持"不回改"** | **维持不回改**（留痕 ＋ 后记指针，C19） |
| ⑤ | **"计数类陈述须给可复现命令"是否升格为硬约束**（现为**口径**） | **先观察 3 轮**，再议是否写入 `CONSTRAINTS.md`（**不急于立 C 编号**） |

---

## ⑫ 下一步建议

1. **本 commit 放行后按 T-038 门控**：等 `gh run list` 显示 `completed` **再落笔任何 CI 结论**。
2. ★ **放行后请留意**：本轮新增的 **CI step 19（报告结构门禁）** 将**首次真实执行** ⇒ 其日志是**该 step 的第一份运行证据**（**建议下一轮报告核对它是否真跑了**，而非只看"CI 绿"）。
3. **下轮批次（推荐合并）**：② `§7.2` 14 条状态纠正 ＋ **`check_id_set_diff.py` / `check_report_structure.py` 告警分类机器化评估** ＋ **INTEL 第 3 轮**（满 3 轮 ⇒ 议正式登记）。
4. ★ **下轮开工第 1 步＝读 `coordination/intel/INDEX.md`**（**INTEL 机制动作 ①**）。

---

## ⑬ 启动回执
已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD` 的输出 ⇒ 310**。

---

## ⑭ 本轮读了底图哪几份、哪几行

| 文件 | 读取范围 | 用途 |
|---|---|---|
| 根 `README.md` | **未读**（本轮**不改底图**） | — |
| `docs/`（35 份） | **未读**（同上；**判据 F 实跑 PASS** 即以机器方式确认底图未被扰动） | — |
| `coordination/TEMPLATES.md` | **§〇 固定相 A/B（L18–77）** ＋ **§7.2 变更记录表（L307–362）** | **附加表位置注的落点** ＋ **`T-042` 追加点** ＋ C15 取数 |
| `coordination/BASELINE.md` | **§41.1／§44.x／§45（C22、T-00 的元引用取证）** ＋ **§四十九 尾部** ＋ **§47.3 A2 表** | **复核取证** ＋ **本节追加点** ＋ **`A2` 现状核对** |
| `.github/workflows/ci.yml` | **L341–375（编号集差集 step ＋ 机制 18 注释块）**；**改后 pyyaml 全量解析** | **接线位置** ＋ **逐字前后对照** ＋ **步数实测** |
| `coordination/tools/` | `check_report_structure.py`（**本判据，全文**）／`check_id_set_diff.py`（执行侧） | **入 CI 的命令与退出码口径** |
| `coordination/CONSTRAINTS.md` | **`^## C` 行位** ＋ C19/C20/C21 条文 | 订正口径（**C19 留痕**）与 **C20 不入库** |

> **★ 说明**：本轮**未读根 `README.md` 与 `docs/`** —— 因**零底图改动**（`git diff --stat` 可验）⇒ **按机制 22 判据 F 无需更新**。**不以此冒充"已核底图"**（C16 口径）。

---

## 附 · INTEL 状态表（**附加表 · 依 `T-042` 置于十四项之后** · `J1` 载体）

| 编号 | 状态 | 触发条件 | 本轮是否触发 | 本轮动作 |
|---|---|---|---|---|
| **INTEL-001** | **观察中** | **组合触发**：企业注册完成 ＋ 阶段三启动 | **✗ 未触发**（阶段定位＝**阶段二 · 2.4**） | 无（下一轮重新检查） |
| **INTEL-002** | **观察中** | **事件触发**：**L7–L10 落地后** | **✗ 未触发**（L7 设计完成／L8–L10 🔒 冻结中） | 无（下一轮重新检查） |

- **合计**：观察中 **2**｜待准备 **0**｜准备中 **0**｜已实施 **0**｜已归档 **0**。
- **`J1–J4` 自查**：J1 ✅（本表存在）｜J2 ✅（**两条均未触发，状态保持"观察中"正确**）｜J3 ✅（无"待准备"）｜J4 ✅（无"准备中"）。
- ★ **附加表位置即为 `T-042` 的首次实测**：**置于十四项之后，未占用任何编号位**（**尤其未顶掉第 6 项「未完成项」**）。
