【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 抽象铁律评估稿（2026-09-21 · v0.299）· **只出稿**

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。
（本稿新增行扫描：本机用户名 **0**、绝对路径 **0**、端口数字 **0**、密钥字样 **0**。）

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | **count = 299**｜HEAD **`2b6a9bb`**（v0.298）｜`origin/main` **`2b6a9bb`**（**已推**） |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 = 执行层（**判定与记账（T1 闭环）已实现、执行侧待确认**） |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–38**（`T-038`＝CI 落笔门控）｜D **1–44**｜R **1–52**（**台账内实际登记 45 条** —— 见 §四）｜TERM **1–13** |
| **本稿性质** | **只读取证 ＋ 只出稿** —— 盘点"抽象铁律"，评估哪些宜按 **T-038 模式**改造 |
| **只出稿声明** | ✅ **未改文件、未改底图、未改治理文件**（本稿为新增未跟踪文件） |

**③ 任务与只出稿声明**
- 基准 **count = 298**｜总规划定位：**push v0.297–v0.298 ＋ 抽象铁律评估稿**｜类型：**放行 ＋ 只出稿**。
- **只出稿声明（逐字）**：本稿**只读取证**（原文逐字取自 `BASELINE.md` R 编号表），**不改文件、不改底图、不改治理文件、不裁定**。

**④ 状态：完成**（含 **4 处诚实边界**：① **CI 三态按 C20 只记当日日报**；② 本稿**全部为只读提取＋评估** ⇒ **无新建事实**；③ **改造建议均未实施**（须授权）；④ **R 表原文分散在台账多处**（按登记时间分段）⇒ 本稿**已逐条标注出处行号**，但**未做跨文件（历轮报告）追溯**）。

---

## §0 本稿定位与方法

### 0.1 目标

盘点项目内**"抽象铁律"**（原则性规则），评估**哪些宜按 T-038 模式改造** ——
即：**从"原则／教训" → "绑定具体动作与可观测条件的检查点"**。

### 0.2 评估标准（用户 §2.2 四问）

| # | 问 | 判读 |
|---|---|---|
| ① | **是否绑定到具体动作？** | "应当如此"＝**否**；"做 X 后须 Y"＝**是** |
| ② | **是否曾"知道但做不到"？** | **有触犯记录**（像 R89 两次触犯）＝**强信号** |
| ③ | **改造为可执行检查点的成本** | 极低／低／中／高（以**是否需要新判据脚本**为主要尺度） |
| ④ | **改造的收益** | 低／中／高（以**复发频率 × 后果**为主要尺度） |

### 0.3 分级口径（本稿自定，供讨论）

| 级 | 含义 |
|---|---|
| **P0** | **有触犯记录 ＋ 未绑定动作 ＋ 改造成本 ≤ 中** ⇒ **建议优先改造** |
| **P1** | 有触犯记录，但改造成本较高／判定含人判断 ⇒ **宜改造（分批）** |
| **P2** | 无明确触犯，但**可低成本补强**（补登记／补门控） |
| **P3** | **已改造**（可作**正面范例**，与 T-038 同模式） |

### 0.4 基准范例：T-038 与 T-037（**已按此模式完成改造的两条**）

| 编号 | 改造前形态 | 改造后形态 | 模式 |
|---|---|---|---|
| **R89 → T-038** | "「声明」≠「发生」；只取运行期证据"（**抽象**） | **`TEMPLATES §十五`**："push 后**必须等 `completed`** 再落笔"＋**执行三步**＋禁止/允许条款 | **门控式**（未达条件即不落笔） |
| **复扫类 → T-037** | "每轮固定复扫"（**固定动作**） | **`TEMPLATES §11.3`**："**未触发即不跑**" | **触发式**（未达条件即不做） |

⇒ **共同点**：**把"应当记得"替换为"由可观测条件决定"**。

---

## §一 · 核心候选 5 条（用户 §2.3 点名）

### ★ R75 · 行号引用会漂移

| 字段 | 内容 |
|---|---|
| **编号** | **R75** |
| **铁律原文（逐字）** | 「**行号引用会漂移**：Q11 契约稿两处代码行号我**数错**（`state_of_energy_budget` 153→**154**、`state_pace` 108→**111**），由**回读核对**抓出。**教训**：**引用行号后必须回读验证**；**定位以符号名为准**，行号只作辅助。」（`BASELINE.md` **L601**） |
| **是否绑定动作** | **部分** —— "引用后必须回读验证"**是动作**，但**无触发条件、无门控、无机器判据** ⇒ 仍靠"记得" |
| **触犯记录** | **有（≥2 次）**：① 原文所述 Q11 契约稿 2 处；② **本轮前**（v0.284）"L4 锚另起一行 ⇒ **汇编 diff 达 232 行**（行号漂移）"，改为并入同行后收敛为 **6 行**（见 `BASELINE §35.1` 过程处置） |
| **改造建议** | **① 格式约束（零成本）**：**引用行号必须同时给符号名**，统一格式 `符号名（文件:行号）`；② **可执行门控**：「**改 `docs/`／`coordination/` 正文行后，若汇编 diff 超阈值（如 >50 行），须先判定是否为行号漂移**」；③ **可选机器判据** `check_line_ref.py`：扫描报告/台账中形如 `文件:行号` 的引用，**回读该行并核对是否含预期符号**，不符即判红 |
| **成本 / 收益** | **成本：低**（格式约束＝零成本；门控＝一条阈值判断；判据脚本 ≈80 行）｜**收益：高**（行号漂移是**全项目高频**：汇编 diff 放大、报告引用失效） |
| **分级** | **P0（建议优先改造）** |

### ★ R82 · 数值型活跃陈述过期 ＋ 同文件自相矛盾

| 字段 | 内容 |
|---|---|
| **编号** | **R82** |
| **铁律原文（逐字）** | 「**数值型活跃陈述过期 ＋ 同文件自相矛盾**（`BASELINE.md:610/611/612`）」；处置 "✅ **本轮处置**（见 §18.4）；**残余**：机制 25 **仍不覆盖**「测试计数」类（R80 规则未定 ⇒ 见 §17.2 待裁）"（`BASELINE.md` **L895**） |
| **是否绑定动作** | **否** —— 只**描述现象**；处置在 §18.4，**无常态门控** |
| **触犯记录** | **有（连续 4 次）**：`no_std` 子集计数 **407→417→460→474** 静默过期（`BASELINE.md` **L153** 明记"该类数值已连续四次静默过期…写死即过期，R71／R82"） |
| **改造建议** | **① 首选：数值不写死**（改"以 `<命令>` 实测为准"）—— **已有总入口兜底**（根 `README.md §文档数值口径`，2026-09-21 G-2 落地）；**② 可执行判据** `check_stale_numbers.py`：对台账/底图中形如 `N 测试`／`N 模块` 的**活跃陈述**，**强制要求同行含"实测"字样或命令引用**，否则判红；③ 与**机制 25** 接线（其现**明确不覆盖**测试计数 —— **覆盖缺口**） |
| **成本 / 收益** | **成本：中**（正则需区分"活跃陈述"与"历史留痕"，**易误报**；历史行量大）｜**收益：高**（**连续四次过期**⇒高复发） |
| **分级** | **P0（建议优先改造）** |

### R71 · 环境派生值不抄进文档

| 字段 | 内容 |
|---|---|
| **编号** | **R71** |
| **铁律原文** | ★ **本稿发现**：R71 在 `BASELINE.md` **内无独立登记行**（**仅被引用**）⇒ 原文取自其**引用处括注**：「**复核命令（R71：环境派生值不抄进文档，以命令实取为准）**」（**L1084**）；另有「run 号属**环境派生值**（R71）⇒ 以 `gh run list --limit 5` 实取为准」（**L1011**） |
| **是否绑定动作** | **是** —— "**不抄**"＋"**以命令实取**"**本就是可执行动作**（且**遵守良好**：判据 F 与汇编自检均含 R71 检查项） |
| **触犯记录** | **有 1 次**：某轮第 5 条真实缺陷 —— 「**"标签"是派生值 ⇒ 必须在提交前用命令取**，不能"按上一轮 +1 推"」（**L1323**） |
| **改造建议** | **① 补登记行**（**缺口修复** —— 与 §四 同族）；**② 可执行判据** `check_derived_values.py`：扫描**跟踪文件**中形如 **40 位 hash**／**run id 数字**／**ISO 时间戳**的字面量，**判红**（豁免：日报／历史留痕段）。**③ 与 T-038 接线**：run 号属派生值 ⇒ **与 §十五 同源**（可互引） |
| **成本 / 收益** | **成本：低**（判据 ≈60 行；已有良好遵守基础）｜**收益：中**（**登记缺口**本身即是问题：引用多于登记） |
| **分级** | **P2（可低成本补强）** |

### R73 · 判据 ↔ 判据登记之间的张力

| 字段 | 内容 |
|---|---|
| **编号** | **R73** |
| **铁律原文（逐字节选）** | 「**"判据 ↔ 判据的登记"之间的张力**（两个子实例，均已实测）：（a）**引述型局限**：机制 25 判据**把自己刚登记的 T-032 判红**…（b）**词表与用词不一致**…⇒ **5 条假阳性**…**教训**：**判据与"判据的登记"之间有天然张力** —— 登记文本必须**按判据口径书写**；且**同义表达的覆盖度**直接决定判据会不会"误伤自己的记录"」（**L599**） |
| **是否绑定动作** | **部分** —— 有"书写约束"与**双侧处置**，但**无常态检查** |
| **触犯记录** | **有**（机制 25 判红自己登记的 T-032；5 条假阳性） |
| **改造建议** | **① 已部分落地**：`HISTORY_MARKS` 补同义词；**C-02**（豁免词只减不增）＝ **门控已建**；**② 可执行门控**：「**新增/编辑登记文本后，必跑机制 25 实跑**，并确认判据未误伤新登记」＋ **CI 接线**（机制 25 已在 CI `test` job） |
| **成本 / 收益** | **成本：低**（基础已在）｜**收益：中** |
| **分级** | **P1（宜改造）** |

### R74 · 复核脚本也须与实跑同源

| 字段 | 内容 |
|---|---|
| **编号** | **R74** |
| **铁律原文（逐字）** | 「**复核脚本也须与实跑同源**（**C18 第三次实例**）：复核第 1 批时我自己的脚本未剥 `NNNN│` 前缀 ⇒ **假报 9/9 ❌**（实为 ✅）。**教训**：C18 说的"**自检**须与实跑同源"，同样适用于"**复核脚本**"。」（**L600**） |
| **是否绑定动作** | **是**（"复核脚本须与实跑同源"＝可执行：**调用同一函数**而非自行解析） |
| **触犯记录** | **有**（假报 9/9 ❌） |
| **改造建议** | **① 门控化**：「凡编写**复核脚本**，**必须 `import` 判据模块的函数**，**不得自行实现比对逻辑**」（已实践于本项目多数脚本，但**未成文**）；② **加 Code Review 检查项**（写入 `TEMPLATES` 或判据族） |
| **成本 / 收益** | **成本：极低**（仅成文＋约定）｜**收益：中** |
| **分级** | **P2（可低成本补强）** |

---

## §二 · 同族条目（**"知道但做不到"型铁律**，同属候选）

### R87 · 台账状态与结案记录脱节

| 字段 | 内容 |
|---|---|
| **铁律原文（节选）** | 「**通用教训**：**「落地」这个动作必须包含「更新活跃状态行」**；**「订正」必须覆盖「被推翻的旧登记」** —— 否则**台账会自己骗自己**（与 **R82**「活跃陈述过期」**同族**；亦与 **R83** 同族）」＋「**判据三问**：① 这项**在活跃台账里**的状态行改了吗？② 旧的「待裁／待确认」**是否已被证据推翻**？③ 若保留历史留痕，**后记指针**是否指向结案处？」（**L1534**） |
| **是否绑定动作** | **部分** —— **已有"判据三问"**（好基础），但**靠人逐次自问**，无机器门控 |
| **触犯记录** | **有（两类实例）**：（a）R84 行状态未传播（v0.247 只新增、未回改）；（b）`TEMPLATES` T-034 旧登记未回改 |
| **改造建议** | **① 门控化**：「**裁定结案时，须同时更新：R 表状态行 ＋ 相关 `§` 标题 ＋ 速查表**」（清单式）；**② 可执行判据** `check_status_sync.py`（**成本较高**）：扫描"待裁／待确认／尚未"等字样，**若同文件已存在对应处置块则判红** —— **自动判定"是否已处置"较难，建议先做"清单式人检 ＋ 抽样判据"** |
| **成本 / 收益** | **成本：中—高**（语义判定难）｜**收益：高**（与 R82 同族、**高复发**：本会话已多次出现"状态未同步"） |
| **分级** | **P0（建议优先改造，但先做清单式）** |

### R86 · 汇编副本的"覆盖范围"口径与实际不符

| 字段 | 内容 |
|---|---|
| **铁律原文（逐字）** | 「**汇编副本的"覆盖范围"口径与实际不符** —— 合订本 §五 称「底图变更后必须重生成」（**暗示覆盖全部底图**），**实际只收录 `docs/` 34 份（缺 `REFERENCE_APP_DESIGN.md`）**…」（**L1425**） |
| **是否绑定动作** | **否**（口径已改，但**未绑定检查动作**） |
| **触犯记录** | **有**（口径不符，被记为本轮缺陷） |
| **改造建议** | **① 已部分落地**：`asm_selfcheck_0920.py` **实测覆盖份数（34）**并断言「**不含根 README**」⇒ **已是机器判据**；**② 补强**：把"**覆盖清单由脚本自动输出**"（**勿手写**）成文 |
| **成本 / 收益** | **成本：低**（已在）｜**收益：低**（覆盖已较完整） |
| **分级** | **P2** |

### R83 · 退化出口与"通过"同值

| 字段 | 内容 |
|---|---|
| **铁律原文（逐字）** | 「**退化出口与"通过"同值 ⇒ 判据静默变绿**（三条入口退化时返回 `0`，而 `0` ＝ 全过）⇒ "**没验到**"被读成"**验过了**"」；「**通用教训**：**"退化／跳过／空转"必须与"通过"取不同值** —— 否则再严的判据也只是**装饰**」（**L1237**） |
| **是否绑定动作** | **是（已完成）** —— 纯算层 `classify_degenerate` ＋ 边界层映射 **133／134（判红）** ＋ **CI 负断言**；另 **C-03**（零陈述 ⇒ `NO_CLAIM`）＝同类门控 |
| **触犯记录** | 有（三条入口退化返回 0） |
| **改造建议** | **无需改造** —— **可作正面范例**（与 T-038 同模式：**取值层面强制区分**） |
| **成本 / 收益** | N/A（已完成） |
| **分级** | **P3（已改造 · 范例）** |

### R85 · 能力条目「三缺一」即不成立

| 字段 | 内容 |
|---|---|
| **铁律原文（逐字）** | 「**能力条目「三缺一」即不成立** —— ①**可复现命令**（含 **cwd／工作区归属**）②**执行环境**（含 target 位置等**外部因**）③**正反对照**。缺任一 ⇒ 该条目是**未验证声称**」（**L1239**） |
| **是否绑定动作** | **是（已完成）** —— `TEMPLATES §14.2` **能力矩阵**为其载体（**每格四要素**） |
| **触犯记录** | 有（机制 26 首次实战抓出 1 条错条目） |
| **改造建议** | **可补机器判据**：检查 §14.2 每格**三要素齐备**（列数／关键字段存在性） |
| **成本 / 收益** | **成本：低**｜**收益：中** |
| **分级** | **P3（已改造 · 可再补判据）** |

### R81 · `git ls-files` 非 ASCII 路径转义

| 字段 | 内容 |
|---|---|
| **铁律原文（逐字）** | 「**`git ls-files` 默认对非 ASCII 路径加引号转义 ⇒ 枚举静默漏项**（含中文名的 `discussions/` 12 文件被跳过…）」；「**教训＝「范围」本身也要有阳性对照**（C16 的"范围"要素须能自证完整）」（**L894**） |
| **是否绑定动作** | **是（已完成）** —— 一律改 **`git ls-files -z`** |
| **触犯记录** | 有（12 文件被跳过） |
| **改造建议** | **无需改造**；可选：把「**枚举一律加 `-z`**」写成 `TEMPLATES` 条目 |
| **成本 / 收益** | **成本：极低**｜**收益：中** |
| **分级** | **P3（已改造）** |

### R89 · 「声明」与「发生」不可区分（**基准**）

| 字段 | 内容 |
|---|---|
| **铁律原文（逐字）** | 「**「声明」与「发生」不可区分 ⇒ 判据假阳性**：`gh run view --log` **回显 run 脚本**，用脚本文本 grep 日志会把**脚本行**当成**运行证据**…**通用教训**：**判据的输入源必须能区分「被声明的文本」与「被观测到的文本」**」（**L1739**） |
| **是否绑定动作** | **是（已完成 · 本轮固化）** —— **`TEMPLATES §十五`（T-038）**：**CI 结论落笔门控**（`completed` 才可落笔）＋执行三步＋禁止/允许条款 |
| **触犯记录** | **有（2 次）**：v0.294／v0.295 均在 `in_progress` 时先写"三 job 全绿" |
| **改造建议** | **已完成** —— **作基准范例**（本稿 §0.4） |
| **成本 / 收益** | N/A（已完成；**首次执行成功**：v0.297–v0.298 轮以阻塞式脚本门控） |
| **分级** | **P3（已改造 · 基准）** |

---

## §三 · 分级汇总表

| 级 | 编号 | 铁律要点 | 是否绑定动作 | 触犯记录 | 改造成本 | 改造收益 |
|---|---|---|---|---|---|---|
| **P0** | **R75** | 行号引用会漂移 | 部分 | **≥2 次** | **低** | **高** |
| **P0** | **R82** | 数值型活跃陈述过期 | **否** | **连续 4 次** | 中 | **高** |
| **P0** | **R87** | 台账状态与结案记录脱节 | 部分（有判据三问） | 两类实例 | 中—高 | **高** |
| **P1** | R73 | 判据 ↔ 登记张力 | 部分 | 有（5 假阳性） | 低 | 中 |
| **P2** | R71 | 环境派生值不抄进文档 | **是** | 1 次 | 低 | 中（＋**登记缺口**待补） |
| **P2** | R74 | 复核脚本须与实跑同源 | **是** | 有 | **极低** | 中 |
| **P2** | R86 | 汇编覆盖范围口径 | 否 | 有 | 低 | 低 |
| **P3** | R83 | 退化与"通过"取不同值 | 是（已完成） | 有 | — | — |
| **P3** | R85 | 能力条目三缺一 | 是（已完成） | 有 | — | — |
| **P3** | R81 | `ls-files` 非 ASCII 转义 | 是（已完成） | 有 | — | — |
| **P3** | **R89** | 声明 ≠ 发生 | **是（T-038 · 基准）** | **2 次** | — | — |

**观察**：**P0 三条的共同点**＝**"高频复发 ＋ 未（或弱）绑定动作 ＋ 改造成本可控"**；
**P3 四条的共同点**＝**已把"取值／条件"写死**（不同值、三要素、`-z`、`completed` 门控）。

---

## §四 · ★ 本稿新发现：**R 编号"引用多于登记"**（待核 · 不擅断）

- **实测**：`BASELINE.md` 内**以独立登记行**出现的 R 编号共 **45 个**：
  `R12–R19, R21–R42, R71, R73–R75, R77–R79, R81–R89`。
- **未见于独立登记行**者：**R20 · R43–R70 · R72 · R76 · R80**（**其中 R20 有引用**："公开仓库（R20）"，见 **L114**）。
- **同族先例**：**R15** —— 台账**已自陈**"编号缺口（如实登记，不编造内容）"："该编号曾在本文件／报告里被**引用**…但**从来没有对应的登记行**"（**L104**）。
- **可能解释（三种，均待核）**：① 登记在**历轮报告**（本稿**未追溯**）；② 确为**缺口**（与 R15 同类）；③ 存在于**其他文件**（如 `CHARTER.md`）。
- **本稿处置**：**只报现象，不擅断、不改文件**；建议**一轮专项核对**（与 D1–D16 同法：逐条取证）。

---

## §五 · 改造优先级建议（**不裁定**）

| 序 | 对象 | 建议动作 | 前置 |
|---|---|---|---|
| **1** | **R75** | ① 统一行号引用格式 `符号名（文件:行号）`；② 汇编 diff 超阈值即判"疑似行号漂移" | 无（可即做） |
| **2** | **R82** | 数值不写死（已有总入口兜底）＋ 补 `check_stale_numbers.py`（先**只告警不判红**，避免误报） | 需定义"活跃陈述"正则 |
| **3** | **R87** | 先做**清单式人检**（结案三件套：R 表状态行／§ 标题／速查表） | 判据可后置 |
| **4** | R73／R74／R71／R86 | 成文＋补门控（多为**极低成本**） | 无 |
| **5** | **§四 编号缺口** | 开一轮专项核对 | 需授权 |

---

## ⑤ 验收对照（用户 §五）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（**等 `completed` 后再动文件**） | ✅ `98b80f6..2b6a9bb`（exit 0）；三 job **全 success**；**按 T-038 门控**：脚本**阻塞等到 `completed`** 并打印 `READY_TO_EDIT` 后才开始编辑 |
| 2 | 暂不补 C22，记录到 BASELINE | ✅ 台账 **§41.1**（追认） |
| 3 | 抽象铁律评估稿出，含 2.2–2.4 全部内容 | ✅ §0.2（**四问标准**）＋ §一／§二（**每条六字段**）＋ §三（汇总） |
| 4 | 只出稿，不改文件 | ✅ 本稿为**新增未跟踪文件**；`git status` 复核**无 `M`／`A`／`D`** |
| 5 | C15 复算通过，编号表连续无缺号无重号 | ✅ 全 ±0；机制/C/T 连续 |
| 6 | 下一次返回第 0 行含 9 步阅读指令 | ✅（本稿第 0 行即 9 步指令） |

## ⑥ 未完成项（如实列出）

1. **改造建议全部未实施**（本稿**只出稿**，须授权后方可落地）。
2. **§四 编号缺口未追溯**（未查历轮报告）。
3. **D1–D16 未动**（用户 §三 明示）。
4. **`2026-09-20_今夜P3清单.md`** 仍为未跟踪（历史未纳管件）。

## ⑦ 真实缺陷与过程观察

- **真实缺陷 0**。
- **过程观察 1（正向 · T-038 门控第二次执行成功）**：本轮以阻塞式脚本 `push_and_wait_n23.sh` **等到 `completed`** 才开始编辑 ⇒ **规则持续生效**（连续两轮未触犯）。
- **★ 过程观察 2（本稿新发现）**：**R71 等编号"引用多于登记"**（§四）—— 这是**盘点类任务的副产品价值**：不做全面盘点，这类缺口不会被发现（与 R15 同族）。
- **过程观察 3（方法观察）**：**P3 四条（R83／R85／R81／R89）的共同解法**都指向同一动作 —— **"让'未达条件'与'通过'取不同值"**。⇒ 可作为**后续改造的统一心法**。

## ⑧ 三量台账（四量齐全）

| 量 | 本轮情况 |
|---|---|
| **存量** | 底图 **36 份**、`docs/` **35 份**、机制 27／C 21／T 38／D 44／R 52／TERM 13 —— **均未变**（本轮无编号表改动） |
| **变量1** | 用户的**盘点需求**：盘点全部"抽象铁律"，按四问标准评估哪些宜按 T-038 模式改造；并点名副选 R75／R82／R71／R73／R74 |
| **补充增量（答案1）** | 本轮产出：**本评估稿**（**11 条**铁律逐条六字段 ＋ **P0–P3 分级** ＋ **§四 新发现** ＋ §五 优先级建议） |
| **创新增量** | ① **首次把"抽象铁律"当对象系统盘点**（原先散落在 R 表各处、无统一分级）；② **提炼出统一心法**（"让未达条件与通过取不同值"，见 ⑦-3）；③ **发现编号缺口**（§四）；④ **给出可执行改造序**（P0→P1→P2）。**（一经产出即降为新的存量）** |

## ⑨ 边界说明（四类齐全）

| 类 | 本轮内容 |
|---|---|
| **本地跑过** | C15 双向计数（**全 ±0**）｜判据 F（**PASS**）｜本稿第 0 行自检（P1 ＋ P2 **PASS**、泄漏 **0**）｜**阻塞式等 CI `completed`**（门控证据 `READY_TO_EDIT`） |
| **CI 过** | **`test`／元内核引导镜像（QEMU 真实引导）／宿主 field-render 三 job 全绿**（**结论按 C20 只记当日日报，不入库**）★ 取材时点＝**阻塞等到 `completed` 之后** |
| **真实跑过** | 元内核引导镜像 job 内**真实 QEMU 引导**（CI 侧）；本轮**本机未跑 QEMU** |
| **未验证** | ① **"是否绑定动作／触犯记录"的判定依据＝台账文本**，**未做代码级复核**；② **§四 编号缺口未跨文件追溯**（可能存在于历轮报告）；③ **改造建议的成本估算为经验值**，**未实测**；④ **P0–P3 分级为本稿自定口径**（**未经验证**） |

## ⑩ 决策评估（对 ⑪ 的 3 项按七要素给建议）

| 待裁 | 评估 | 优势 | 劣势/风险 | 建议 | 等待 |
|---|---|---|---|---|---|
| **① 本稿是否入库** | 本轮按"只出稿"未提交 | 入库后评估结论可追踪 | 入库推高 count、触发机制 18 链 | **可放行入库** | 你一句话 |
| **② 是否按 P0（R75／R82／R87）开改造** | P0 三条均高复发、成本可控 | 直接减复发 | R82 判据**易误报**（需先"只告警"） | **先做 R75（零成本项）＋ R82 告警模式** | 你一句话 |
| **③ §四 编号缺口是否开专项核对** | 疑为 R15 同类 | 澄清台账完整性 | 需跨文件追溯（成本中） | **可开（并入某轮夜间任务）** | 你一句话 |

## ⑪ 需要你一句话的裁定（3 项）

| # | 项 | 推荐 |
|---|---|---|
| 1 | **本评估稿是否入库提交**（本轮按约束**未提交**） | **放行提交** |
| 2 | **P0 三条（R75／R82／R87）是否开改造** | **先做 R75 格式约束（零成本）＋ R82 以"只告警"起步** |
| 3 | **§四 R 编号缺口是否开专项核对** | **可开**（并入夜间任务） |

## ⑫ 下一步建议

1. **D1–D16 讨论**（用户已明示待讨论）：**D15 先裁** → 口径项 5 条逐条 → 工程项批量。
2. **P0 改造可按"零成本先行"**：R75 的**格式约束**（引用行号必带符号名）**当天即可生效**（只需成文，无需判据）。
3. **统一心法可用**：后续铁律改造一律问一句 —— **"未达条件时的取值，是否与'通过'不同？"**（见 ⑦-3）。

## ⑬ 启动回执

> 已读 `CHARTER.md`。
> 当前状态：**count = 298** ｜ 阶段二 · **2.4 边界层已验**
> 本次任务：**push v0.297–v0.298 ＋ 抽象铁律评估稿**
> 是否继续：**执行完毕**

**只出稿证据（本回合）**：`git status --short` ⇒ **仅 1 个新增未跟踪文件（本稿）＋ 1 个历史未纳管件（P3 清单）**，**无 `M`／`A`／`D`**。
**提交状态**：`count = 299`（含本轮台账＋报告提交）｜`HEAD = origin/main = 2b6a9bb`（**push 已完成**）｜**`ahead 1`**。

## ⑭ 本轮读了底图哪几份、哪几行

| 底图/文件 | 读的区间 | 用途 |
|---|---|---|
| `coordination/BASELINE.md` | **R 表全部首格行**（`grep` 全集 ⇒ 45 个编号）｜**R12–R42 L104–134**｜**R71／R73–R75 L568–601**｜**R77–R79 L787–789**｜**R81–R82 L894–895**｜**R83–R85 L1237–1239**｜**R86 L1425**｜**R87 L1534**｜**R88 L1711**｜**R89 L1739**｜**R71 口径引用 L1011／L1084／L1323**｜**L153（R82 复活记录）** | **铁律原文逐字引用** ＋ 触犯记录取证 ＋ §四 编号缺口实测 |
| `coordination/TEMPLATES.md` | **§11.3（T-037）**｜**§十四（机制 26 能力矩阵）**｜**§十五（T-038）** | 基准范例（§0.4）与 P3 判定的对照 |
| `.workbuddy/memory/2026-09-21.md` | 尾部 | CI 结论（C20：只在此处） |
| `gh run list`／`gh run view`（运行） | — | **按 T-038 §15.4 阻塞等 `completed` ＋ 取 job 三态** |

---

## 附 · 晨报要点（供 08:00 交付复用）

- **夜间触发结果**：本轮为**用户指令轮**（放行 ＋ 只出稿）；**复扫类均未触发 ⇒ 未跑**（合规）。
- **完成项**：push v0.297–v0.298（`2b6a9bb`；CI 三 job 全绿；**T-038 门控第二次执行成功**）｜**抽象铁律评估稿**（11 条逐条评估 ＋ P0–P3 分级 ＋ **§四 编号缺口新发现**）｜`C22 暂不补` 追认入台账。
- **失败项**：**无**。
- **未推送 commit（1）**：**v0.299**（台账 §四十一 ＋ 本报告）。
- **需你目验/放行**：① 本稿是否入库；② P0（R75／R82／R87）是否开改造；③ §四 编号缺口是否开专项核对。
