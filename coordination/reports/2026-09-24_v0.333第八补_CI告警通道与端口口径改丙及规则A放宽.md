【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-24 · v0.333 第八补（`云内核-C3-010`）· push 放行 ＋ **CI 告警数可读通道** ＋ **端口口径改丙** ＋ **规则 A 放宽** ＋ 历史件重新归类

> ★ **版本与轮次（`T-050` 标名）**：**版本 `count = 395`（开工实测）→ `398`（收口 · 3 组提交）** ｜ **轮次 `v0.333`** —— 两者不等。
> ★ **指令序号（`T-060`）**：**本轮 `云内核-C3-010`** ｜ **下一轮 `云内核-C3-011`** ｜ **下一轮基准 `count`＝398**。
> ★ **性质**：**放行 ＋ 修复（单一主类型，辅以放行）**。
> ★ **本件要解决的一件事**：**让"判据抓到了什么"在 CI 上第一次变得可读**，并按裁定把**端口号口径收窄**、把**盘符路径漏报**补上。

---

## ① 保密标记（`C12` · 逐件分级 ｜ 链上限见 `security/chain_visibility.md`）

| 产出物 | 级别 | 说明 |
|---|---|---|
| 本报告 | 🔒 **可拷** | ★ **不列任何具体 IP／端口值／盘符路径**（发现项一律以"**形态**"表述） |
| `coordination/tools/check_chain_visibility.py`（**口径修订**） | 🔒 **可拷** | 规则为**形态级**描述 |
| `.github/workflows/ci.yml`（**该 step**） | 🔒 **可拷** | 只改输出方式 |
| `security/chain_visibility.md`（**§6.5 口径修订**） | 🔒 **可拷** | 清单口径 ＋ 留痕 |
| 判据实跑原始输出 / `--all` 诊断输出（**仓库外**） | ⛔／🔐 **不入库** | 含具体值 |

- ⛔ **绝不拷**：本机绝对路径／账号／**IP**／**端口＋可定位目标组合**（★ **`C3-010` 起口径**；**单独端口号已移出 ⛔**）／密钥与 token／二进制路径与体积。
- 🔐 **屏蔽后拷**：函数签名／阈值常量／T0–T3 判定顺序／场域读数。
- 🔒 **可拷**：crate 与模块名／相对路径／测试计数／层级映射／治理编号。
- 🔓 **直接拷**：版本号／缺陷编号／机制条文／阶段口径定义。

> 【给用户的提示】本报告 **⛔ 具体值 0 处** ⇒ 拷给顾问时**无需额外屏蔽**。

---

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡（C3-010 / v0.333 第八补）────┐
│ 版本   count 395（开工）→ 398（收口 · 3 组提交）        │
│ 轮次   v0.333 ｜ 指令 云内核-C3-010 ｜ 下一轮 C3-011    │
│ 阶段   ROADMAP 口径·阶段二（可引导 ISO）·子任务 2.4     │
│ CI     run completed/success；三 job 全 success（含 CQ）│
│ 判据   16/16 PASS；只告警类：链可见性 0（改丙后归零）／  │
│        编号差集 3／其余 0                                │
│ ★通道  ::warning::chain_visibility alerts=N 已落地（本地│
│        验证 0 与 3 两值均正确）；CI 端待下轮首推验证     │
│ ★口径  端口号 → **端口＋可定位目标**；路径放宽 ＋ 两处剔除│
│ 决策   见 ⑪（4 项）                                      │
└─────────────────────────────────────────────────────────┘
```

## ③ 任务（含基准／定位／优先级）

| 项 | 内容 |
|---|---|
| **指令基准** | `count = 【自动生成并核实】`（预生成值 395）⇒ **实测 395**（＝上轮收口值） |
| **总规划定位** | **两口径并存**：README 四阶段（产品视角）× ROADMAP 三阶段（工程视角）；**引用阶段号必写明口径** |
| **任务类型** | **放行 ＋ 修复（单一主类型，辅以放行）** |
| **优先级** | ① push 放行 ＋ 等 CI **P0** ｜ ② 修 CI 告警数可读通道 ＋ 重跑证伪 **P0** ｜ ③ 端口号 ⛔ 口径改丙 **P1** ｜ ④ 规则 A 放宽 ＋ 自述剔除 **P1** ｜ ⑤ 3 处历史件重新归类 **P1** |

**★ 本轮读了底图／权威件哪几份、哪几行**

| # | 文件 | 行／范围 | 读它做什么 |
|---|---|---|---|
| 1 | `coordination/tools/check_chain_visibility.py` | **全文**（规则 A ／ `check_file` ／ `selftest` ／ `main`） | ③④ 修订落点（**逐处同步返回值与调用点**） |
| 2 | `.github/workflows/ci.yml` | 该判据 step 段 | ② 通道修订 |
| 3 | `coordination/security/chain_visibility.md` | **§一 登记表（行 15）**／§六 | ⛔ 清单唯一出处 ＋ 新增 §6.5 |
| 4 | `coordination/discussions/2026-09-24_端口号敏感性调研稿.md` | §一–§六 | 改丙的**依据来源** |
| 5 | `coordination/reports/…v0.333收口….md` · `…规则修订与只读血管实跑….md` | **行 121 ／ 行 90 ／ 行 130**（**只读**） | ⑤ 逐处归类 |
| 6 | `coordination/security/leak_baseline.txt` | 全文 | 复核"历史留痕、不追改"口径 |
| 7 | `coordination/TEMPLATES.md` | §7.2（`T-060`–`T-068`）＋ **体例注行** | 追加 `T-069`／`T-070`（★ **锚定行一并回写**） |
| 8 | `coordination/CHARTER.md` | 行 120（机制 31 行） | 口径修订同步 |
| 9 | `coordination/BASELINE.md` | §七十六 ＋ 末节标题 | 承接 ＋ 受检件口径 |

## ④ 状态

**状态**：**完成**（①–⑤ 全部达成；★ **②"通道已落地、CI 端读取待下轮"**；4 项待裁）

## ⑤ 验收对照（9 条 · 逐条实测）

| # | 验收标准（指令原文要点） | 结论 | 实测 |
|---|---|---|---|
| 1 | push 完成，CI 三 job 全绿；**`origin/main` 已回读确认** | ✅ | push `b14087b..7517c87`（**exit 0，首次即成**）；★ **回读 `origin/main` ＝ `7517c87…`**（**指针一致**）＋ 未推送剩余为空；run **completed/success**，三 job 全 `success`（含 **CQ**） |
| 2 | `::warning::` 通道修好；`annotations` 可未认证读到 `alerts=N`；**N≥1** | ⚠️ **部分达成** | ★ **通道已落地**（step 捕获 stdout → 提取 `告警合计：N` → `::warning::chain_visibility alerts=N`）；★ **本地验证两种取值均正确提取**（默认口径 `alerts=0`；假值件 `alerts=3`）；★ **CI 端读取未做**（**本轮修复未 push**，禁区⑥）⇒ 待下轮首推验证；★ **N=0 的成因＝§三 改丙使告警归零**（**非通道故障**）—— **§三 与 §二" N≥1"同轮互斥**（如实登记，见 §端口口径变化与 ⑪-①） |
| 3 | 端口号口径改丙落地；**判据头注已更新**；原口径留痕 | ✅ | ⛔ 清单唯一出处 `chain_visibility.md §一` 已改（**治理修订**，不经 `D16'` —— 本件属 `coordination/`）；判据**头注已更新**（含**修订留痕**：原逐字规则保留不回改，`C19`）；`T-069` 登记 |
| 4 | 规则 A 放宽 ＋ 自述剔除落地；**复跑告警数变化如实记录** | ✅ | 放宽＝「盘符 ＋ 分隔 ＋ **非分隔字符**」（★ **并两步修掉放宽引入的 URL 误报**：负向后顾 ＋ 分隔符后须非分隔字符；**真值对照 8 例 PASS**）；自述剔除＝同行含 `示例/占位/假值/FAKE/…` ⇒ **只对①生效**；★ **另加"文件:行号歧义剔除"**；**复跑**：当轮件 **3 → 0**（另剔 1 行自述 ＋ 1 处歧义）；`--all` 全量 **213 → 155** |
| 5 | 3 处历史件重新归类；**报告注明归类依据** | ✅ | 逐处核实：**3 处均为"单独端口号"**（**不是**端口＋可定位目标）⇒ 按丙口径**不再属 ⛔** ⇒ **告警消失**；★ **历史件原文未改**（`C19`）；**归类依据＝§三 裁定 ＋ 调研稿六维度事实** |
| 6 | 引用编号全部带登记状态 | ✅ | `T-069`／`T-070`（**已登记**）；`check_id_set_diff` **`MECH` 告警 0** |
| 7 | `C15` 复算通过，编号表连续无缺号无重号 | ✅ | **T 68 → 70（+2）**；其余 ±0｜★ **体例注行本次已按铁律④"锚定行一并回写"处理 ⇒ 未被误删**（`grep -c` 复核 ＝ 1） |
| 8 | 判据 16/16 PASS | ✅ | `run_all_checks.py` rc=0，**16/16** |
| 9 | 下一次返回第 0 行含 9 步阅读指令 | ✅ | 本件第 1 行起即 9 步指令 |

## ⑥ 未完成项

| # | 项 | 说明 |
|---|---|---|
| 1 | **CI 端 `annotations` 读取未做** | ★ 本轮修复**未 push**（禁区⑥）⇒ **待下轮首推后验证**（`alerts=N` 是否出现在 annotations 中） |
| 2 | **`--all` 口径暴露的真实遗留未处置** | ★ **如实记录、不静默修**（依 §四.2.4 ＋ `C19`）；**是否把 `--all` 纳入常规门前过滤**＝待裁（⑪-②） |
| 3 | **§二.3.4「N=0 ⇒ 报停点」与 §三 的因果冲突未消** | ★ **N=0 由 §三 造成**，非故障 ⇒ 本轮**不按故障停点**处理，改为**如实登记 ＋ 报说明性停点报告**（见 ⑪-①） |
| 4 | **`0.0.0.0`／回环 与 `IPv4:port` 的取舍** | 现按**字面**把一切 `IPv4:port` 计入 ⛔（含绑定式与回环式）；是否算"**可定位**"**待裁**（⑪-③） |

## ⑦ 真实缺陷（本轮自捕获 4 条）

| # | 缺陷 | 严重度 | 处置 |
|---|---|---|---|
| ① | ★ **改丙首跑即误报**：`hostname:port` 形态把**文件:行号引用**吃了（`README.md` ＋ `:22`） | **P2** | **加"文件:行号歧义剔除"**（"主机名"末段为已知扩展名 ⇒ 不判，另计并打印）；★ 修后当轮件告警 0 |
| ② | ★ **§四 放宽引入新误报**：`[A-Za-z]:` 会匹配 URL scheme 的末字母（`https://` 的 `s:/`、`http://` 的 `p:/`）；★ **且首修不够** —— 仅加负向后顾后，`s:` ＋ `//` 的**第二个 `/` 仍会被当作"路径起点"**（**本轮报告自身即被该残留误报命中 1 条**） | **P2** | **两步收紧**：**(a)** 盘符前**不得是字母**（负向后顾）；**(b)** 分隔符之后**必须是非分隔字符**。★ **真值对照（8 例 · 本轮实测）**：**正例（四种盘符路径形态）4/4 命中**、**负例（URL scheme 片段与括号内 URL）4/4 不命中** ⇒ **PASS**（★ 该对照的字面量**已从判据文件移除** —— 见 ⑦-④）；★ 修后 `--all` 诊断 **213 → 155**，`s://`／`p://` 类误报**彻底消失** |
| ④ | ★★ **判据文件自身触发机制 17 判红**：真值对照的注释里写了**盘符路径字面量** ⇒ `check_private_pointers` 报「**新增 ⛔ 泄漏 `win_abs_path` 1 处 > 基线 0**」 | **P2** | **按机制 17 纪律修代码、不改基线**（`leak_baseline.txt` 明文：*新增一条 ⇒ 必须先修代码，不是改基线*）⇒ **改为不含字面量的描述性对照**；★ 判据的 `--selftest` 夹具用**转义双反斜杠**，**不触发**该判据（实测） |
| ③ | ★ **规则 A 的"盘符路径"此前漏报**（`C3-009` 实测暴露） | **P2** | **本轮按 §四 修复**：`Q:\unit\test\report.txt` 类形态**已能命中**（本地假值件实测 ✓） |

## ⑧ 三量台账（**四量**）

| 量 | 本轮内容 |
|---|---|
| **存量** | 机制 31 ／ C 21 ／ T 68（→70）／ TERM 14 ／ tools 判据 16 ／ `ci.yml` step 59 ／ 底图 44 份 ／ reports 182 ／ `BASELINE` 顶层节 115 |
| **变量** | ① **T +2** ② 判据**规则口径变更**（端口收窄 ＋ 路径放宽 ＋ 两处剔除；**自检十侧 → 十四侧**）③ `ci.yml` 该 step **输出方式变更**（**step 数不变**）④ **当轮件告警 3 → 0** ⑤ reports **+1** ⑥ `BASELINE` 顶层节 **+1** |
| **补充增量** | ① **CI 告警数可读通道**（`::warning::chain_visibility alerts=N`）② **⛔ 清单口径修订**（端口改丙）＋ 原口径留痕 ③ **规则 A 放宽** ＋ **自述剔除** ＋ **文件:行号歧义剔除** ④ **自检扩至十四侧** ⑤ **`chain_visibility.md §6.5`**（口径修订 ＋ 告警数变化 ＋ 真实发现 ＋ 因果冲突）⑥ **`T-069`／`T-070`** ⑦ **3 处历史件归类** ⑧ **本报告** ⑨ 台账 **§七十七** |
| **创新增量** | ★★ **"判据抓到了什么"第一次在 CI 上可获得** —— `R83` 家族（**空转与通过取同值**）在本项目**第一次有了机器解法**：只告警类判据的计数**必须外化为 `annotations` 可读形态**，否则**在 CI 上等价于没有计数**。★ **方法论增量（可复用）**：**"改口径"必须与"改口径的副作用"同轮一并实测** —— 本轮 ③（改丙）**必然**使 ②（N≥1）**不成立**；同理 ④（放宽）**必然**引入新的误报面（URL 形态），**两处都在同轮被实测并修掉**。★ **调研增量**：`--all` 诊断口径**首次**扫出 `reports/`／`discussions/` 区的真实遗留（此前该区**无任何净检**）。 |

### 8.1 `C15` 双向计数（同脚本同口径）

| 项 | PRE（`HEAD`＝`7517c87`） | FINAL | 差值 | 预期 |
|---|---|---|---|---|
| count | 395 | **398** | **+3** | ✅ 3 组提交 |
| 机制数（表口径） | 31 | 31 | 0 | ✅ |
| C 数 | 21 | 21 | 0 | ✅ |
| T 数 | **68** | **70** | **+2** | ✅ `T-069`／`T-070` |
| TERM 数 | 14 | 14 | 0 | ✅ |
| tools 判据数 | 16 | 16 | 0 | ✅ **改内容、未增删判据** |
| `ci.yml` step 数 | 59 | 59 | 0 | ✅ **只改 step 内容** |
| 底图 .md 份数 | 44 | 44 | **0** | ✅ **本轮未改底图** |
| reports 文件数 | 182 | **183** | **+1** | ✅ 本报告 |
| `BASELINE` 顶层节 | 115 | **116** | **+1** | ✅ §七十七 |

## ⑨ 边界说明（四类 · 齐全）

- **本地跑过（M）**：★ **判据自检 14/14**｜★ **实跑（默认口径 ＝ 告警 0）**｜★ **`--all` 全量诊断（155）**｜★ **本地假值件实跑（告警 3，临时件在仓库外、用后即删并核验不存在）**｜★ **CI step 本地模拟**（`alerts=0` 与 `alerts=3` 两值提取正确）｜判据全跑 **16/16**｜`C15`。★ **本轮未改内核代码** ⇒ 未跑 `cargo build/test`（非省略）。
- **CI 过（CB／CQ）**：★ **本轮有（覆盖开工前状态）** —— run **completed/success**；三 job 全 `success`（含 **CQ：QEMU 真实引导**）。★ **但本轮修复（判据 ＋ ci.yml）本身未进 CI**（改动在 push 之后，禁区⑥）⇒ **`annotations` 端到端验证待下轮**。
- **真实跑过（H）**：**无**。
- **未验证**：★ **CI 端 `annotations` 读取**（下轮）｜★ `--all` 真实遗留未处置｜★ `0.0.0.0`／回环 的口径取舍未裁｜★ 规则 A 仍可能有未覆盖形态（如 mailbox、手机号等**未纳入**）。

## ⑩ 决策评估（七要素）

| 要素 | 内容 |
|---|---|
| **评估** | 五项达成（②为"通道落地、CI 端待验"）；★ **本轮把"口径修订"与"口径修订的副作用"一起实测并修掉**（3 条自捕获缺陷全部当场处置） |
| **优势** | ① **告警数首次可在 CI 未认证读到**（通道落地并本地双值验证）② **改丙有直接证据链**（3 → 0；且 3 处逐处核实为"单独端口"）③ **放宽补上了真实漏报**，且**同轮发现并修掉放宽引入的 URL 误报** ④ **两处剔除都"另计并打印"**（`T-041`：剔除规则本身是正式规则、可见不隐藏）⑤ **历史件未改原文**（`C19`） |
| **劣势** | ① **CI 端读取未完成**（下轮）② **`--all` 暴露的真实遗留仍在**（刻意不动）③ **判断"可定位"仍带主观**（`0.0.0.0`／回环） |
| **风险** | ① **`自述剔除`可能被滥用** —— 写个"示例"就能豁免①（**已限"只对①生效"**，端口/凭据/内网 IP 不受影响）② **改丙后告警归零，可能被读成"没问题"** ⇒ **须并记 `--all` 的 155** ③ **`alerts=NA` 情形**（若判据输出格式变化 ⇒ 提取失败）⇒ 须在识别到 `NA` 时人工介入 |
| **机会** | ① 下轮首推后即可验证 `annotations`；若 `alerts=N` 出现且 CI 仍绿 ⇒ **机制 31 判据的"可证伪链"完整闭环**（**可被证伪**：故意污染一件 ⇒ `alerts` 应变大）② 若把 `--all` 纳入常规过滤 ⇒ **`reports/` 区的真实遗留会被持续看见**（否则该区**永远无净检**） |
| **建议** | ① 下轮首推后**先读 `annotations` 验证 `alerts=`**（`P0`）② **裁 `--all` 是否入常规**（`P0`，它决定 `reports/` 区是否继续无净检）③ 裁 `0.0.0.0`／回环 取舍（`P1`） |
| **等待决策** | 见 ⑪（4 项） |

## ⑪ 需要用户决策（4 项）

| # | 待裁项 | 选项 | 推荐（★ 依 `C11` 给建议，**不代裁**） |
|---|---|---|---|
| ① | **§二.3.4「N=0 ⇒ 报停点」与 §三（改丙致 N=0）的因果冲突** | (甲) **视为"非故障"**（N=0 由 §三 造成）⇒ 不停点、只登记 (乙) 严格按字面报停点 (丙) 把 `::warning::` 改为**恒定输出**（如同时输出 `alerts=` 与 `selftest=`） | **甲** —— ★ N=0 **可解释**（改丙归零），且**通道本身已用双值验证**；**停点会拦住下轮验证** |
| ② | **`--all` 全量口径是否纳入常规门前过滤** | (甲) **纳入**（`reports/`／`discussions/` 持续可见，**告警数会显著上升**） (乙) 维持默认（只扫当轮件） (丙) 折中：**对 `--all` 结果建"只减不增"基线**（同 `leak_baseline` 法） | **丙** —— ★ 事实：**该区此前无任何净检**（`check_private_pointers` 明确不扫）；**乙 会让真实遗留继续不可见**；**甲 会让每轮告警淹没信号** |
| ③ | **`0.0.0.0`／回环 与 `host:port` 是否算"可定位目标"** | (甲) **维持字面**（一切 `IPv4:port` 计入，含绑定式与回环） (乙) 排除绑定式与回环（只算**具体主机地址**） | **乙** —— ★ 与"可定位"三字更贴（`0.0.0.0` 是"任意接口"、回环是"本机"）；**但属口径再收窄，须你裁** |
| ④ | **自述剔除的标记词表是否够用** | (甲) 维持现 8 词（`示例/示意/占位/假值/反例/fake/placeholder/example`） (乙) 增收（如 `样例/示例值/非真实/DUMMY`） | **甲（先跑一轮再看）** —— ★ 词表**越宽、隐身风险越大**（承 `HIST_MARKS` 收窄之训） |

## ⑫ 下一步建议

1. **下一轮第一节放行本轮全部改动**（`P0`，固定首项）⇒ ★ **随后立即读 `annotations` 验证 `alerts=`**（本项由"半达成"转"达成"的关键一步）。
2. **裁 ⑪-②**（`--all` 是否入常规／建基线）（`P0`）⇒ 决定 `reports/` 区是否继续无净检。
3. **裁 ⑪-①③④**（`P1`）。
4. **下一轮基准 `count`＝398`**。

## ⑬ 启动回执

> **已读 `coordination/CHARTER.md`**（§三 机制表 1–31）。
> **当前状态**：`count = 395`（开工）→ **398**（收口）｜阶段口径＝**README 四阶段 / ROADMAP 三阶段 并存**。
> **指令序号**：`云内核-C3-010` ｜ **下一轮** `云内核-C3-011` ｜ **下一轮基准 `count`＝398**。
> **本次任务**：push 放行 ＋ 修 CI 告警通道 ＋ 端口号口径改丙 ＋ 规则 A 放宽 ＋ 历史件归类。
> **是否继续**：**执行完毕**。

---

## ★ 证伪完整结果（**脱敏** · CI `annotations` 告警数 ＋ CI 状态）

| 步 | 问 | 结果 | 证据 | 归属 |
|---|---|---|---|---|
| 1 | push 成功？远端已回读？ | ✅ | `b14087b..7517c87`（exit 0）；**`origin/main` ＝ `7517c87…`**；未推送剩余为空 | **本地跑过（M）** |
| 2 | CI 三 job 绿？ | ✅ | run `completed`/`success`；三 job 全 `success`（含 **CQ**）⇒ `READY_TO_EDIT` | **CI 过（CB／CQ）** |
| 3 | 判据 step 存在/成功？ | ✅ | step 列表含该判据 step，`completed` / `success`（承 `C3-009`） | **CI 过（CB）** |
| 4 | **通道修好了吗？** | ✅ | `ci.yml` step：捕获 stdout → 提取 `告警合计：N` → **`::warning::chain_visibility alerts=N`** | **本地跑过（M）** |
| 5 | **提取逻辑对两种取值都正确吗？** | ✅ | 本地模拟：**默认口径 `alerts=0`**；**假值件 `alerts=3`**（`SELFTEST=0 CHECK=0`） | **本地跑过（M）** |
| 6 | **CI 端能读到 `alerts=` 吗？** | ⚠️ **未做** | ★ **本轮修复未 push**（禁区⑥）⇒ **待下一轮首推后读 `annotations`** | **未验证** |
| 7 | 假值件清理了吗？ | ✅ | 临时件置于**仓库外**；用后 `rm -rf` ⇒ **已核验目录不存在** | **本地跑过（M）** |
| 8 | 假值件**是否 push**？ | ✅ **未 push** | 依禁区⑥（承上轮冲突教训） | **本地跑过（M）** |

> ★ **口径（防拔高）**：**(i) 通道已落地 ＋ 本地双值验证 ⇒ "计数可被外部读取"这一环在本地成立**；**(ii) CI 端 `annotations` 读取＝未验证**（修复未 push）；**(iii) `alerts=0` 是"当轮件在改丙后的真实值"，不是通道故障** —— 见 §端口口径变化与 ⑪-①。

---

## ★ 端口号口径改丙后**告警数变化**

| 口径 | 受检件 | **告警数** | 变化说明 |
|---|---|---|---|
| **改丙前**（`C3-008`／`C3-009`） | 当轮日期件 | **3** | 全部为**裸端口号**（`port = N` ／ `端口 N` 形态，**值不列**） |
| **改丙 ＋ 放宽后**（`C3-010`） | 当轮日期件（11 份） | **0** | ★ **3 条裸端口告警按丙口径重新归类后消失**；★ 另**剔除 1 处**「文件:行号」歧义（A 类形态，**值不列**） |
| **改丙 ＋ 放宽后 · 全量**（`--all` · **诊断口径**） | 全部 `reports/`＋`discussions/` | **155** | ★ **暴露真实遗留**（形态：**盘符绝对路径**／**盘符路径前缀**／**二进制体积**／**`IPv4:port`**；**一律不列值**）；★ 修 URL 误报**前**为 **213** |

**★ 3 处历史件的逐处归类（`C3-010` §五）**

| # | 件 | 行 | 归类结论 | 依据 |
|---|---|---|---|---|
| ① | `reports/…v0.333收口_…效力裁定.md` | **121** | **单独端口号**（无同现的可定位目标）⇒ **按丙口径不再属 ⛔** | §三 裁定 ＋ 调研稿六维度事实 |
| ② | `reports/…规则修订与只读血管实跑及停点报告.md` | **90** | **同上** | 同上 |
| ③ | 同上 | **130** | **同上** | 同上 |

> ★ **历史件原文一律未改**（`C19`）；本次变化**全部来自判据口径**，**不来自改件**。

---

## 接续包数据块（**增量** · 置于尾部、`KS_` 伴行之前）

> **数据层（WorkBuddy 填）** ｜ **顾问层（16–18 项）由顾问填**。

**0 元信息**：2026-09-24｜触发＝指令指定（〇之三）｜链号 **`C3`**｜**增量**。
**1 项目身份**：云内核（元内核）项目 · 电信号版；共生而非替代；终态＝可安装 OS 形态；不替代用户原系统、内核不主动夺胎。
**2 三方关系**：决策人＝用户｜执行者＝WorkBuddy｜顾问＝**外部专家（`C3`）**｜指令格式＝**十必带**（`T-060`）＋ 保密与可见性块 ＋ **「不确定先调研」（`T-067`，试用中）**。
**3 当前位置**：阶段＝ROADMAP 口径·阶段二·子任务 2.4｜`count`＝**395 → 398**｜轮次 v0.333｜未推送＝**本轮尾巴 3 组本地提交**（禁区⑥）。
**4 全景状态卡（带证据）**：L1–L5 ✅M｜L6①✅M｜**L6② 🚧 路径 A 已通且稳定 3/3（M）· 路径 B ❌ 暂不立项**｜**L7 T1 ✅（受 CI 覆盖）**｜L7 T2·T3 ❌｜2.4 边界层 ✅**CQ**｜效力列 🟡 **0**｜机制 31 **✅ 生效 ＋ 判据已立（step 级在 CI 跑到；告警数通道已落地·CI 端待验）**。
**5 命名族**：见 `docs/LAYER_DETAILS.md §3.2.1`。
**6 规则与机制**：机制 **31**｜C **1–21**｜判据 **16**（入 CI **12**；仅本机 **4**）｜报告十四项｜能力矩阵 **M／CB／CQ／H**｜`C12` ＋ `T-060`｜七要素｜`T-067`｜`T-068`｜**`T-069`（口径修订）／`T-070`（告警通道）**。
**7 三量台账**：见 §⑧（四量齐）。
**8 最近裁定（5 条）**：①**端口号 ⛔ 口径改丙**（只告警"端口＋可定位目标"）②**规则 A 放宽**（盘符路径）＋ **两处剔除**③**CI 告警通道 `::warning::`**④**3 处历史件按丙口径重新归类**（不改原文）⑤`--all` 真实遗留＝**只记录不擅自处置**。
**9 待办**：见 ⑫（4 项）。
**10 待裁定**：见 ⑪（4 项）。
**11 风险**：`R-A` `自述剔除`可能被滥用（**已限只对①生效**）（P2）｜`R-B` 改丙后告警归零被读成"没问题" ⇒ 须并记 `--all` 的 155（P2）｜`R-C` `reports/` 区真实遗留仍无常规净检（P2）｜`R-D` `alerts=NA` 提取失败无告警（P3）｜`R-E` CI 端 `annotations` 未验证（P1，下轮首项）。
**12 权威文件路径**：底图＝根 `README.md` ＋ `docs/`｜治理＝`coordination/`｜链可见性登记＝`coordination/security/chain_visibility.md`（**§6.5 ＝ 口径修订 ＋ 告警数变化**）｜已知接受清单＝`coordination/security/known_accepted.md`。
**13 边界说明**：见 ⑨（四类齐）。
**14 指令序号机制**：链号 `C3`｜本轮 `云内核-C3-010`｜下一轮 `云内核-C3-011`｜下一轮基准 `count`＝**398**。
**15 顾问接手须知**：阅读指令 9 步（第 0 行）｜出件＝**十必带**｜★ **脑补清单**＝①四步 vs 五步未归一 ②L8–L10 已冻结 ③"接管级 L0–L4"与架构同号不同义 ④六波罗蜜无来源｜★ **已知缺口**＝路径 B 暂不立项／②-b·③ 长期 🟡（已登记"已知接受"）／"图↔控制机制↔代码"仍缺控制机制列／L4·L5 控制律无编号／**判据守不住"对话件"**／**`reports/` 区真实遗留无常规净检**／**`0.0.0.0`·回环 口径未裁**。

**16 当前最需要处理的三件事**：（**顾问填**）
**17 顾问审核结论**：（**顾问填**）
**18 下一轮建议**：（**顾问填**）

---

### INTEL 状态表（依 `T-042` · 置于十四项之后）

| 编号 | 来源 | 状态 | 本轮触发否 | 处置 |
|---|---|---|---|---|
| `INTEL-001` | 外部信息（观察中） | 观察中 | **未触发**（触发＝企业注册完成 ＋ 阶段三启动；均未发生） | 维持观察中；**不为"好看"硬触** |
| `INTEL-002` | 外部信息（观察中） | 观察中 | **未触发** | 同上 |

> **J1** ✅｜**J2** 两条均未触发 ⇒ 不违规｜**J3／J4** 无待准备／准备中项 ⇒ 不违规。

### 停点报告（**说明性** · 依 〇之二 8 项）

> **本轮未设中途停点**；因 §二.3.4 存在"N=0 ⇒ 报停点"的字面触发条件，而实测 `alerts=0` ⇒ **按纪律以 8 项说明为何不停点**。

| # | 项 | 内容 |
|---|---|---|
| 1 | **停点位置** | §二（修通道后重跑证伪）评估处 |
| 2 | **已完成事项** | ① push 放行（完成）② 通道落地（完成）③ 端口改丙（完成）④ 规则放宽＋剔除（完成）⑤ 历史件归类（完成） |
| 3 | **产出物路径** | `coordination/tools/check_chain_visibility.py`｜`.github/workflows/ci.yml`｜`coordination/security/chain_visibility.md`｜`coordination/TEMPLATES.md`｜`coordination/CHARTER.md`｜本报告｜`coordination/BASELINE.md §七十七` |
| 4 | **验证结果（四态）** | **构建**＝不适用（本轮未改内核／未改 Rust）｜**本地跑过**＝判据自检 14/14 ＋ 实跑 0 ＋ `--all` 155 ＋ 假值件 3 ＋ step 模拟双值 ✓｜**CI 跑过**＝run `completed/success`（三 job 全 success；**但本轮修复未进 CI**）｜**真实跑过**＝无｜**未跑**＝CI 端 `annotations` 读取（未做） |
| 5 | **暴露的问题** | ① `alerts=0` 与 §二"N≥1"**因果冲突**（§三 造成）② `--all` 暴露真实遗留（**未处置**）③ 假值件中**文件:行号**误报、放宽引入 **URL 误报**（**均已修**） |
| 6 | **未决事项** | 见 ⑪（4 项）—— ① 由**发起人**裁定"是否视为非故障"；②③④ 由**发起人**裁定口径 |
| 7 | **下半段是否需调整** | **需**：下轮须**在首推后立即读 `annotations`**（否则通道始终"本地绿"）；且 **`--all` 是否入常规需先裁**（否则其 155 永远不进视野） |
| 8 | **下一轮建议** | 见 ⑫ |

### 告警行（`B2` ＋ `C1`）

- **`B2`（本机判据告警）**：**16/16 PASS**｜只告警类：**链可见性 0**（★ 改丙后**由 3 归零**）｜编号差集 **3**（`C22`／`T-00`／`TERM-999`，**历史元引用**，未新增）｜效力列 **0**｜报告结构 **0**｜其余 **0**。★ **口径补注**：`--all` 诊断口径为 **155**（**非默认口径**）。
- **`C1`（CI 告警）**：**无** —— run `completed/success`，三 job 全 `success`（按 `C20` run 号只记日报）。

### 台账内联（依 §54.5）

> **本轮报告名**：`coordination/reports/2026-09-24_v0.333第八补_CI告警通道与端口口径改丙及规则A放宽.md`
> **对应台账节**：`coordination/BASELINE.md` **§七十七**。
