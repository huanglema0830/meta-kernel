【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-23 夜间 P3-4 · 🟡「待裁龄」全量计龄表

> **来源**：`coordination/reports/2026-09-23_今夜P3清单.md` · **§一 P3-4**；依据 `TEMPLATES.md` **`T-052`／`T-053`** ＋ `BASELINE.md` **§68.8-6／§68.9-⑥** ＋ 本日《补全表》**附件 B**。
> **性质**：**只读出表** —— **裁定本身属 P1，夜间不得代行**；**不改判据**（清单 P3-5 明写）。
> **边界**：本件**未改**任何文件（含 `check_effect_column.py`）。

## ① 保密标记（C12）

- ⛔ 机密（绝不拷）：**无**（无本机路径／凭证／网络细节／参数库取值）。
- 🔐 保密（屏蔽后拷）：**无**。
- 🔒 半公开（可拷）：仓库内相对路径与行号、编号体系、计数口径。
- 🔓 公开（直接拷）：版本号、轮次、🟡 计数、三档分布、结论。

> 【给用户的提示】本件含 **0** 处机密、**0** 处保密，**无需屏蔽即可整份拷给顾问**。

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡（本件时点）────┐
│ 版本   v0.364（本机 rev-list --count）  │
│ 轮次   v0.332（BASELINE 末顶层节）      │
│        上限 = 3 轮（T-053 · AGING_LIMIT）│
│ 阶段   ROADMAP 阶段二 · 子任务2         │
│ CI     本夜未触发（按 C20 不入库）      │
│ 底图   44 份（README + docs/ 43）       │
│ 判据   effect_column 实报告警 4 条      │
└─────────────────────────────────────────┘
```

## ③ 任务 · 状态 · 优先级

| 项 | 内容 |
|---|---|
| 任务 | ① 全仓 `🟡` 逐处抽取"首次标记轮次"，按 **`T-053` 上限 3 轮**计龄，分 **已挂满／临近上限／余量** 三档 ② 与 `check_effect_column` 本轮实测 **4 条**告警对账，并**查明口径差异** ③ 出**建议裁定顺序**（挂满轮数 × 阻塞下游） |
| 基准 | **v0.364**（**轮次 v0.332**） |
| 定位 | ROADMAP **阶段二 · 子任务2** |
| 优先级 | **P3** |
| 状态 | **完成** —— ② 的**口径差异已查明**（**非"待核"**） |

---

## 一、① 全仓 🟡 计龄表

### 1-1 抽取口径（本件写明，可复现）

| 项 | 口径 |
|---|---|
| **范围** | **全仓 `*.md`**（`docs/` ＋ `coordination/`（含 `discussions/`、`reports/`）＋ 根 `README.md`） |
| **"效力语境"**（＝判据同源口径） | **(a) 效力表数据行内的 🟡**（表头含「效力」的表格）**或 (b) 同行含「待复核」的 🟡** |
| **排除** | **进度／完成度类**（`🟡 部分`／`🟡 N%`）；**`<!-- eff-col-skip -->`** 行 |
| **计龄** | `挂轮 = 当前轮次(332) − 首次标记轮次` |
| **两套分档**（**因为条文与判据不一致，必须两算**） | **条文模型**（`T-053` 22.5「满 3 轮·**含起点轮为第 1 轮**」）⇒ **挂满 = 挂轮 ≥ 2**｜**判据模型**（`check_effect_column.py`：`cur − first > 3`）⇒ **挂满 = 挂轮 ≥ 4** |

### 1-2 总账

| 口径 | 范围内 🟡 | 已挂满 | 临近上限 | 余量 | 无标记 |
|---|---|---|---|---|---|
| **全仓 · 条文模型** | **209** | **194** | 0 | 0 | **15** |
| **全仓 · 判据模型** | **209** | **8** | 2 | 184 | 15 |
| 仅 `docs/` · 条文模型 | 139 | 139 | 0 | 0 | 0 |
| **仅 `docs/` · 判据模型** | 139 | **4** | **1** | 134 | 0 |
| 仅 `coordination/` · 条文模型 | 70 | 55 | 0 | 0 | 15 |
| 仅 `coordination/` · 判据模型 | 70 | 4 | 1 | 50 | 15 |

**★ 阳性对照（口径可复现）**：**仅 `docs/` + 判据模型 ⇒ 已挂满 = 4 条**，与 `check_effect_column` **本轮实报的 4 条逐数相符** ⇒ **本件复算与判据同源** ✅。

**首标轮次分布（全仓 · 范围内）**：`v0.330` **184** ／ `v0.328` **8** ／ `v0.329` **2** ／ **无标记 15**。
⇒ **含义**：**🟡 全部集中在 v0.328–v0.330 这三轮**，**没有更早的历史积压**（**这是好消息**）；但 **v0.330 一轮就积了 184 处**（**占 88%**）。

**「无标记 15」的定性（本件逐条抽查）**：**15/15 均为"叙述性 🟡"**（出现在**条文正文、报告叙述、表头说明**里，**不是待裁项**），例如 `BASELINE.md:5372`（`🟡 待复核`用于编者连缀的**规则说明**）、`TEMPLATES.md:386`（`T-053` **条文原文**里的 🟡）、★ **本夜 `P3-3` 报告 L167**（**自指**：我写"被判据的效力语境含…"时引用了 🟡）。⇒ **不构成"待裁项缺标记"**（判据亦未扫 `reports/`，**不影响判据**）。

### 1-3 逐文件台账（**仅 `docs/` 底图侧** · 这是真正要裁的面）

| # | 文件 | 范围内 🟡 | 首标 | 条文口径已挂满 | 身份 |
|---|---|---|---|---|---|
| 1 | `docs/QUANTUM_CLOCK_DESIGN_DETAIL.md` | **34** | v0.330 | **34** | 出稿（未升格） |
| 2 | `docs/INCARNATION_DESIGN.md` | **20** | v0.330 | **20** | 出稿（未升格，🟡 中） |
| 3 | `docs/DEFENSE_DESIGN.md` | **19** | v0.330 | **19** | **底图**（v0.332 升格） |
| 4 | `docs/SELF_DIAGNOSIS_FRAMEWORK.md` | **19** | v0.330 | **19** | 出稿（未升格，🟢 低） |
| 5 | `docs/DNA_KERNEL_DESIGN.md` | **16** | v0.330 | **16** | 出稿（未升格，🟡 中） |
| 6 | `docs/ORIGIN_BROWSER_DESIGN.md` | **15** | v0.330 | **15** | **底图**（v0.332 升格） |
| 7 | `docs/ORIGIN_OS_DESIGN.md` | **11** | v0.330 | **11** | **底图**（v0.332 升格） |
| 8 | `docs/LAYER_ARCHITECTURE.md` | **5** | **v0.328×4 ／ v0.329×1** | **5** | **底图（骨架 · 命名唯一权威）** |
| — | **合计** | **139** | — | **139** | **其中底图 50 处 ／ 出稿 89 处** |

**`coordination/` 侧（70 处 · 判据不扫、但属"待裁项"需登记）**：`discussions/2026-09-19_汇编合订本.md` **52**（**副本** —— 与底图重复，**随底图重生成而消解，不单独裁**）｜`BASELINE.md` **8**｜`TEMPLATES.md` **8**｜`reports/2026-09-23_*` 共 **6**（5 份报告）。

---

## 二、② 与 `check_effect_column` 对账 —— **口径差异已查明**（**非"待核"**）

**判据本轮实报（本件复跑同结果）**：**告警 4 条**，全在 `docs/LAYER_ARCHITECTURE.md`，行 **177／274／297／308**，文本均为「**🟡 挂满 4 轮未裁**（首次标记 `v0.328` ／ 当前 `v0.332` ＞ 上限 3 轮）」。

**清单侧提出的问题**：该文件内 `🟡` 共 **12** 处，判据只报 **4** 条 ⇒ 差异原因？

### 2-1 答案：**差异分两段，两段均已查明**（`12 → 5 → 4`）

| 段 | 从 → 到 | 原因 | 定性 |
|---|---|---|---|
| **第 1 段** | **12 → 5** | **判据的"效力语境"边界**：只算 **(a) 效力表数据行** 或 **(b) 同行含「待复核」**；该文件 12 处中有 **7 处属"叙述型 🟡"**（条文正文／表头／规范说明），**被边界排除** | ✅ **符合判据自陈边界**（`check_effect_column.py` docstring 明写 **"进度类用法不参与"**）⇒ **非漏报** |
| **第 2 段** | **5 → 4** | **阈值差**：5 处中 **行 246 首标 `v0.329`**（`挂轮 = 3`）⇒ 判据式 `cur − first > 3` **不成立** ⇒ **不告警** | ⚠️ **与 `T-053` 条文相抵** ⇒ **见 §三** |

### 2-2 该文件 12 处 🟡 的逐处定性（本件实测）

| 行 | 是否在判据范围 | 首标 | 判据是否告警 | 片段（节录） |
|---|---|---|---|---|
| 101 | 否 | — | — | `> **全表 11 行 ＝ 🟢 已裁定·已回写**（**本表已无 🟡 行**）` —— **叙述** |
| 104（×2） | 否 | — | — | `> **★ 效力列取值只有两种（T-052 规范件）**：🟢… ／ 🟡 **待裁定**…` —— **规则说明** |
| 163 | 否 | — | — | `> **L0–L6 中标注 🟡 待裁定的五行**，**冻结口径不覆盖**…` —— **叙述** |
| 167 | 否 | — | — | `> ⇒ **本文件 §2.2.1 表已无 🟡 行**。上段…（C19）` —— **留痕叙述** |
| **177** | **是** | **v0.328** | ★ **告警** | `> **🟡 待复核**（**首次标记：v0.328 轮**） —— 与上下层关系／实现状态两列＝编者连缀` |
| **246** | **是** | **v0.329** | **不告警** | `（**编者连缀 · 🟡 待复核**；**首次标记：v0.329 轮**）` |
| **274** | **是** | **v0.328** | ★ **告警** | `> **🟡 待复核**（**首次标记：v0.328 轮**） —— 与 L0–L10 的关系列＝编者连缀` |
| 278 | 否 | — | — | **表头列名**：`| 步 | … | 与 L0–L10 的关系（🟡 编者连缀） |` |
| **297** | **是** | **v0.328** | ★ **告警** | `> **🟡 待复核**（**首次标记：v0.328 轮**） —— "与 L0–L6 的一致性"列＝编者连缀` |
| 299 | 否 | — | — | **表头列名**：`| 机制 | … | 与 L0–L6 的一致性（🟡 编者连缀） |` |
| **308** | **是** | **v0.328** | ★ **告警** | `（**编者连缀 · 🟡 待复核**；**首次标记：v0.328 轮**）` |

⇒ `12 = 7（叙述型）＋ 5（判定型）`；`5 = 4（告警）＋ 1（行 246 · 被阈值挡住）` ⇒ **`12 → 5 → 4` 全部有落点，无未解释项**。

**附（口径可复现的第二重证据）**：判据自身对 `挂轮=4` 的表述是 **"挂满 4 轮"**（把 `挂轮` 直接当轮数）；而 `T-053` 明文 **"包含起点轮为第 1 轮"** ⇒ **判据的内部模型把起点轮漏算了 1 轮**，再加判定式用的 `> 3`（而非 `≥ 3`），**合计造成 2 轮差**（详见 §三）。

---

## 三、★ 口径差（与 P3-3 发现 C 同源 · 本件给出完整差额）

| # | 侧 | 触发条件 | 对「首标 v0.330 ／ 当前 v0.332」 | 对「首标 v0.329 ／ 当前 v0.332」 |
|---|---|---|---|---|
| 1 | **条文** `T-053`／`§22.5` | **满 3 轮（含起点轮为第 1 轮）** ⇒ `挂轮 ≥ 2` | **触发** | **触发** |
| 2 | **判据** `check_effect_column.py` | `AGING_LIMIT = 3` ＋ 判定式 **`cur − first > 3`** ⇒ `挂轮 ≥ 4` | **不触发** | **不触发** |

**差额 = 2 轮** ⇒ **本夜两批 🟡（`v0.328`／`v0.329`／`v0.330` 共 209 处中的 201 处）在条文口径下均已"挂满"，而判据只报 4 条**。

**代码自陈与实现互斥**（同文件可复现）：
- `check_effect_column.py:68`：`AGING_LIMIT = 3   # 满 3 轮（含起点轮为第 1 轮）；★ 与 T-053 22.5 同源`
- 判定式（`:153`／`:167`）：**`(cur - first) > AGING_LIMIT`**
⇒ **注释按条文读法（含起点），实现按"再挂 3 轮"读法** ⇒ **无论取哪一读法，注释与实现都不自洽**。

**自检为何测不出**（`R64` 家族 · 夹具与常量同错，本件实跑）：
| 自检侧 | 夹具 | 挂轮 | 实跑 | 问题 |
|---|---|---|---|---|
| 侧⑨ | `v0.100` → `v0.328` | **228** | 告警（"挂满 228 轮"） | **远离边界** ⇒ 两种口径结果相同 ⇒ **测不出边界错** |
| 侧⑩ | `v0.326` → `v0.328` | **2** | **不告警** | ★ **把 `挂轮=2` 钉为"不告警"，恰与条文读法相抵** ⇒ **自检固化了错误边界** |

---

## 四、③ 建议裁定顺序（**挂满轮数 × 阻塞下游**）

**排序原则（本件写明）**：① **所在层**（底图 > 出稿 > 治理／台账 > 报告 > 副本）；② **挂满轮数**；③ **是否被其他件显式引为阻塞**；④ **本件已获机器告警者优先**（有既有证据链）。

| 顺位 | 对象 | 处数 | 挂满 | 阻塞下游（实测依据） | 建议动作 |
|---|---|---|---|---|---|
| **1** | **`docs/LAYER_ARCHITECTURE.md` 5 处**（行 177／246／274／297／308） | **5** | **v0.328×4（挂轮 4）／v0.329×1（挂轮 3）** | **该件＝全库「命名唯一权威 ＋ 分层总纲」**；其 5 处均为**「与上下层关系／实现状态」列**的编者连缀 ⇒ **7 份设计稿的对应列均以它为参照** | **先裁**（**唯一已有机器告警**；**5 处一次裁完**，可 4 条告警随之清零） |
| **2** | **3 份已升格底图**：`DEFENSE_DESIGN` 19 ＋ `ORIGIN_BROWSER_DESIGN` 15 ＋ `ORIGIN_OS_DESIGN` 11 | **45** | **全 v0.330（挂轮 2）** | 已具**底图身份** ⇒ **改须走 `D16'`**；且是 `LAYER_DETAILS` 内多表的来源 | **同批一次 `D16'` 处理**（与 P3-3 的 `LAYER_DETAILS:120` 合并） |
| **3** | **4 份未升格出稿**：`QUANTUM_CLOCK_DESIGN_DETAIL` 34 ＋ `INCARNATION_DESIGN` 20 ＋ `SELF_DIAGNOSIS_FRAMEWORK` 19 ＋ `DNA_KERNEL_DESIGN` 16 | **89** | **全 v0.330（挂轮 2）** | 出稿 ⇒ **改无须 `D16'`**；但**升格候选**（§68.5「下一批审阅」）⇒ **不裁则不能升格** | **与"是否升格"合并裁**（一次决定：裁定后升格 or 维持出稿） |
| **4** | `coordination/` 治理·台账 16 处（`BASELINE` 8 ＋ `TEMPLATES` 8） | 16 | v0.330 | **治理文件** ⇒ 改须用户点头；`TEMPLATES` 的 8 处**多在条文原文内**（属 `C19` 保护的历史记载） | **先辨"是否属历史留痕"** ⇒ 仅**非留痕**者需补标记 |
| **5** | `reports/2026-09-23_*` 6 处 | 6 | v0.330 | **报告为当轮快照** ⇒ **不受"时效"约束**（快照天然定格） | **不裁**（仅登记） |
| **6** | `discussions/…汇编合订本.md` **52 处** | 52 | v0.328–v0.330 | **历史快照 ＋ 底图副本** ⇒ **随底图重生成自动消解** | **不单独裁**（**并入 P3-2 的"合订本重生成"一并处理**） |

**一句话**：**先裁顺位 1（5 处 · 已有告警 · 一裁即清）⇒ 再把顺位 2 与 `LAYER_DETAILS:120` 合并同批 `D16'` ⇒ 顺位 3 与"是否升格"合并 ⇒ 顺位 4 先辨留痕 ⇒ 顺位 5／6 不单独裁。**
**⚠️ 前置**：**顺位 1–3 的处理量级取决于先裁"计龄口径"（§三）** —— 若采条文口径，**需处理 134 处（`docs/` 侧）**；若采判据口径，**仅需处理 4 处**。**该口径本身属 P1**。

---

## 五、验收对照

| # | 验收标准（清单 P3-4 原文拆解） | 实测 | 结论 |
|---|---|---|---|
| 1 | ① 全仓 `🟡` 逐处抽取"首次标记轮次"，按 `T-053` 上限 **3 轮**计龄，分**已挂满／临近上限／余量**三档 | **全仓范围内 209 处**逐处抽取；**两套分档并列**（条文模型 194／0／0；判据模型 8／2／184）；**`docs/` 侧 139 处** | ✅ |
| 2 | ② 与判据 **4 条**对账，并**查明口径差异**（`LAYER_ARCHITECTURE` 内 🟡 **12** 处 vs 判据 **4** 条） | **已查明**（`12 → 5 → 4` 两段均有落点，**无未解释项**）；**阳性对照**：本件复算 `docs/` + 判据模型 = **4** ＝ 判据实报 4 | ✅ |
| 3 | ③ 出**建议裁定顺序**（按"**挂满轮数 × 阻塞下游**"排序） | §四 六顺位表，逐位给处数／挂满／下游依据／动作 | ✅ |
| 4 | **不改判据** | 未改 `check_effect_column.py`（只读源码与实跑） | ✅ |
| 5 | 裁定本身属 P1，夜间不得代行 | 本件**只出表**，无任何裁定动作 | ✅ |
| 6 | 报告落 `coordination/reports/` | 本件 | ✅ |

---

## 六、未完成项

| # | 未完成 | 原因 |
|---|---|---|
| 1 | **未裁定任何 🟡** | **P1**（清单明写"裁定本身属 P1，夜间不得代行"） |
| 2 | **未改判据阈值／夹具** | **判据改动留白天** |
| 3 | **未逐处读 139 处 `docs/` 的 🟡 语义**（是否值得裁、可否批量转 🟢） | 属**裁定准备**，非本件范围；本件只出**计龄与排序** |
| 4 | **`coordination/` 侧 16 处"是否属历史留痕"未逐处定性** | 涉 `C19` 边界，属待裁（顺位 4 的前置） |
| 5 | **未核 `BASELINE §68.9-⑥` 所记"`T-053` 4 处"与我实测的 5 处（`LAYER_ARCHITECTURE`）之差** | 疑为"4 处＝已告警者／5 处＝含行 246"的口径差 ⇒ **本件已给数字，未追台账原句**（`R32` 不判定） |

---

## 七、真实缺陷

> 口径：**只登记不改**。

1. **计龄口径条文与实现相差 2 轮**（`T-053` vs `check_effect_column.py`）⇒ **201 处 🟡 在条文口径下"已挂满"却在判据下"零告警"**。
2. **判据注释与判据实现自相矛盾**（同文件内：注释"含起点轮为第 1 轮"／实现 `> 3`）。
3. **自检夹具与常量同错**（`R64` 家族）：侧⑩（挂轮 = 2 钉为"不告警"）**使该口径差不可能被自检发现**。
4. **🟡 积压高度集中**：**v0.330 一轮积 184 处（占 88%）**，且 **139 处在 `docs/` 底图侧** —— 其中 **50 处已在底图内**（3 份已升格件 ＋ `LAYER_ARCHITECTURE`）。
5. **"底图内出现待裁标记"与"已裁仍标待裁"两种形态并存**（后者见 P3-3 发现 A）⇒ **机制 25 四条判据均不覆盖**。

---

## 八、三量台账（C6）

| 量 | 内容 |
|---|---|
| **存量** | **全仓 `*.md`** 的 🟡 逐处扫描（**范围内 209 处**）；`docs/` 8 件逐行定性；`LAYER_ARCHITECTURE.md` 12 处逐处定性；`check_effect_column.py` 全文（阈值／范围／自检夹具）；`TEMPLATES.md` `T-052`／`T-053`／`T-058` 条文；`BASELINE` 末顶层节轮次锚 |
| **变量** | **0** —— 未改任何既有文件（判据／底图／治理文件一律未动） |
| **补充增量** | **1 份**：本报告 |
| **创新增量** | **3 条**：① ★ **"`12 → 5 → 4`"两段式归因法**（判据边界段 ＋ 阈值段），把"判据报得少"从"疑似漏报"变成**可逐处对上的账**；② ★ **两套分档并列呈现**（条文模型／判据模型）⇒ 让"该处理 134 处还是 4 处"变成**可由一句话裁定切换的开关**；③ ★ **按"层 × 挂轮 × 下游引用 × 是否已有告警"四因子排序**的裁定顺序表，并指出**顺位 5／6 不应单独裁**（报告＝快照／合订本＝副本） |

---

## 九、边界说明（C5；四类齐全）

- **本地跑过** ✅：**Python 全仓逐处扫描与计龄**（3 种范围 × 2 种模型 ⇒ 6 组数）；**`docs/LAYER_ARCHITECTURE.md` 12 处逐处定性**（含效力表数据行识别＝`[88–98]`）；**`check_effect_column.py` 实跑（告警 4 条）与 `--selftest`（PASS 十侧）**；**阳性对照**（本件复算 `docs/`＋判据模型 = 4 ＝ 判据实报）；`grep -n`／`wc -l`。
- **CI 过** ➖：**无** —— 只读任务**未触发 CI**；按 `C20` **CI 结论不入库**。
- **真实跑过** ❌：**无** —— 未跑 QEMU、未上真机、未构建。
- **未验证** ❌（逐条）：
  1. **哪一读法是正解未裁定**（本件并列两算，**不判定**；`R32`）。
  2. **139 处 `docs/` 🟡 的逐处语义未读** ⇒ **"能不能批量转 🟢"未验证**。
  3. **`coordination/` 16 处是否属历史留痕未定性** ⇒ 顺位 4 的动作**未定**。
  4. **`BASELINE §68.9-⑥` 记的"`T-053` 4 处"与我实测 5 处之差**：**未追台账原句** ⇒ **未定**。
  5. **合订本 52 处是否为"纯副本"未逐处比对**（若其中有合订本独有的 🟡，则"随重生成消解"不成立）⇒ 属 P3-2 边界。

---

## 十、决策评估（C11 七要素）

**本轮无新决策。** 本件为**只读出表**；"口径以谁为准""哪些 🟡 该裁"**均属 P1**，**不由本件代行**。

---

## 十一、需要用户决策（白天；本件只列，不催不动）

| 级别 | 事项 | 来源 |
|---|---|---|
| P1 | ★ **计龄口径以谁为准**（条文"满 3 轮含起点" ⇒ 处理 134 处／判据"再挂 3 轮" ⇒ 处理 4 处）；**改条文 or 改判据** | 本件 §三 |
| P1 | ★ **`LAYER_ARCHITECTURE.md` 5 处 🟡 逐处裁定／补原话**（已有 4 条机器告警） | 本件 §四 顺位 1＋`BASELINE §68.9-⑥` |
| P1 | **3 份已升格底图的 45 处 🟡** 是否随 `D16'` 同批处理 | 本件 §四 顺位 2 |
| P1 | **4 份未升格出稿的 89 处 🟡** 与"是否升格"是否合并裁 | 本件 §四 顺位 3 |
| P1 | **`TEMPLATES.md` 8 处是否属 `C19` 历史留痕**（若属则无须补标记） | 本件 §四 顺位 4 |
| P1 | **自检侧⑩ 夹具是否改到边界值两侧**（`挂轮 = 2`／`3`），以暴露口径差 | 本件 §三 |
| P1 | **"底图内出现待裁标记"是否纳入机制 25** | 本件 §七-5 |

---

## 十二、下一步建议

1. **白天第一句先裁计龄口径**（§十一 第 1 行）—— 它把后面的处理量从 **134 处** 与 **4 处** 之间一次性定下来，**其余四条的优先级随之确定**。
2. **顺位 1 建议单独成一轮**（范围小、证据齐、可立刻清零 4 条告警）。
3. 顺位 2／3 建议与 **P3-2 的"合订本重生成"**、**P3-3 的 `LAYER_DETAILS:120`** **合成一个 `D16'` 批**（同为底图改动，一次留痕）。

---

**启动回执**：已读 `CHARTER.md`（机制 **1–30**／§四 速查 **C1–C21**／§五 启动回执／§六 C10）＋ `CONSTRAINTS.md`（**C10／C12／C13／C14 全文**）＋ `TEMPLATES.md`（§二 第〇项／§三 优先级／§十一 夜间自动化／**§二十二 效力列**）。
当前状态：**v0.364**（`HEAD 494aaba`＝本夜 P3-1／P3-2／P3-3 后）；**轮次 v0.332**。
本次任务：**夜间 P3-4 · 🟡「待裁龄」全量计龄表**（**P3**，只读出表 ＋ 只写本报告）。
是否继续：**P3-5 起继续**（本夜不推送、不与远端同步）。
