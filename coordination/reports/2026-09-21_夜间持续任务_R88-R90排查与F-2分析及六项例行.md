【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 夜间持续任务 · `R88`/`R90` 同型点排查 ＋ `F-2` 分析 ＋ 6 项例行（2026-09-21 夜 · v0.312）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。
（★ **说明**：本报告**不含**本机路径／内网 IP／用户名 —— 相关行在汇编稿中已由**泄漏掩码**覆盖，本报告只引其**位置与计数**。）

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 311** ⇒ 收尾 **312**（本轮 **3 个本地提交**）｜**label v0.312**（`label = count`，C21） |
| **HEAD／远端** | `origin/main = 5abdc70`（**已 push**）；★ **本轮全部 local-only**（**夜间默认不 push**） |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 ＝ 执行层（设计完成）｜L8–L10 🔒 冻结 |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–43**｜**R 1–90**｜TERM **1–13**｜INTEL **1–2**（试用中） |
| **判据** | `tools/check_*.py` **10 个**｜`ci.yml` test job **32 步**｜`reports/` **136** 份（报告体 **72**） |
| **本轮性质** | **夜间例行动作**（**零决策、零验证、零批准**）＋ **2 份只出稿** |

**③ 任务与约束**（含**基准**与**总规划定位**，含**优先级**）
- 基准 **count = 311**（指令填值，实测一致）｜定位：**夜间持续任务（跑完所有不需要决策的活）**。
- **优先级**：**P3**（全部 —— **不需决策、不需验证、不需批准**）。
- 约束：按 `CONSTRAINTS.md`｜**夜间默认不 push**｜**不改底图**｜**不改治理文件**（`CHARTER`／`TEMPLATES`／`CONSTRAINTS`／`BASELINE` 除授权项外）｜**不改 `ci.yml`**（`R90` 待授权）｜**不对外动作**｜不 `taskkill`、不让用户双击任何 bat。
- ★ **本轮的两处"不动手"决定（写在前面，防越界）**：
  ① **不改 `BASELINE.md`** —— 本轮指令只授权"记录到 `reports/`"，**未逐项授权台账条目** ⇒ **台账的夜间并入留待次日**（**已在 ⑪ 列为待裁**）；
  ② **不改任何机制文件**（含 `ROADMAP`）—— 两稿均**只出稿**。

**④ 状态：完成**（**8 项任务全部执行**：**2 项出稿** ＋ **6 项例行**；见 ⑤）。

---

## ⑤ 验收对照（用户 §十 · 8 条）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | **`R88`/`R90` 同型点排查稿出** | ✅ `reports/2026-09-21_R88-R90同型点全仓排查稿.md`（**甲/乙/丙/丁 四类**；**扩扫查见丁类 3 处**；**B5 playwright 中高**） |
| 2 | **`F-2` 分析稿出** | ✅ `reports/2026-09-21_F-2分析稿_夜间P3清单连续3夜0项.md`（**来源单点**归因；**12 行逐行实测**；**与 F-1 同病根**；**甲/乙/丙 三案**） |
| 3 | **INTEL 状态表出** | ✅ 本报告**末尾附加表**（依 `T-042` 置于十四项之后）；**逐条检查触发条件**（§三） |
| 4 | **判据自检全 PASS** | ✅ **10/10 rc=0**（逐条侧数见 §四） |
| 5 | **汇编幂等性检查完成** | ✅ **幂等 PASS**（重生成后 `git diff` **空**）＋ **反向自检 PASS**（34/34）＋ **汇编↔底图 35/35 可解释**（32 精确 ＋ 3 掩码；**ABSENT 真缺口 0**） |
| 6 | **文档时效核对（触发式）完成** | ✅ **未触发**（如实记录：`docs/` 仍 35 份无新增、`advisor_brief.md` 未改、阶段/机制编号未变） |
| 7 | **平台任务清理完成** | ✅ **只读检查**（列出 **6 条过期候选**；★ **不擅自下线**） |
| 8 | **记忆维护完成** | ✅ `MEMORY.md` **12 278 B → 7 586 B（−38%）**（**规范条文 verbatim 保留**；备份留痕） |
| 9 | 本地 commit、不 push | ✅ **3 个本地提交**（`26dc735`／`5a8faa8`／**本提交**）；**未 push** |
| 10 | 不改底图／不改治理文件／不改 `ci.yml` | ✅ **实测**：`git status` 仅本报告 1 份为新增；**无 `docs/`／`README`／`CHARTER`／`TEMPLATES`／`CONSTRAINTS`／`BASELINE`／`.github/` 改动** |
| 11 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行 |

---

## ⑥ 未完成项（清单 ＋ 原因）

| # | 未完成项 | 原因 |
|---|---|---|
| 1 | **`BASELINE.md` 的夜间并入（本轮成果入台账）** | 本轮指令**只授权"记录到 `reports/`"**，**未逐项授权台账条目** ⇒ **从紧执行：不动 `BASELINE`** ⇒ **建议次日并入**（`§五十二`） |
| 2 | **`R90` 修法（`ci.yml` 加重试）** | ★ **`ci.yml` 明确待授权**（本轮 §〇） ⇒ **只出稿（排查稿）** |
| 3 | **`F-2` 的"修列"（`ROADMAP` 2.5／2.6 改标 P1 ＋ 加复核标记）** | **`ROADMAP` 属治理文件** ⇒ **只出稿**（需授权） |
| 4 | **过期平台任务下线** | 指令明示"**不擅自下线**" ⇒ **只记录** |
| 5 | **`INTEL` 正式登记为机制 28** | **3 轮试用后**（本轮为**第 3 轮**，见末尾附加表 ⇒ **下轮可议登记**） |
| 6 | **丁类（`deploy/` 3 处下载无重试）的处置** | 本轮**只登记**（非 CI 门禁，优先级低） |

---

## ⑦ 真实缺陷（含 **`B2`** 要求：**本轮新增告警 ＋ 来源溯源**）

| 级 | 缺陷 | 来源／证据 | 性质 |
|---|---|---|---|
| **P1** | ★ **`R90` 未修**（`ci.yml:451` wasm-bindgen 下载无重试） | 上轮 CI 判红 ＋ 本轮排查稿 | **`R88` 家族第 2 实例**（**"修一处 ≠ 修一类"**）⇒ **修法待授权** |
| **P1** | ★★ **同型点"最危险的一处"未被注意**：**`npx playwright install --with-deps chromium`（`ci.yml:513`）** 体积最大、最易抖、**且无重试** | 本轮**扩扫发现**（甲类之外） | **预防性**（尚未失败）⇒ **建议与 `R90` 同批处置** |
| **P2** | ★ **丁类 3 处**：`deploy/win64/` 的下载点**无重试**，其中 **2 处用 `2>nul` 把失败变成静默** | `net-check-one.bat:16`／`net-repair-one.bat:27`（**静默**）｜`autoprobe.ps1:74`（有降级） | **同族不同型**（**不判红 CI**）⇒ 只登记 |
| **P2** | ★★ **`F-2`：`ROADMAP`「可夜间跑」列静默过期** —— **连续 3 夜"✅ 可跑"却 0 项可执行** | `F-2` 分析稿（**12 行逐行实测**；根因＝**该列无"可执行性判据"、无"复核时点"**） | **R69／R72 家族**（**与 `F-1` 同病根**：**标注缺复核机制**） |
| — | `MECH 28`／`C22`／`T-00` | 见 `BASELINE §47.3` 登记表 | **元引用**（**A2 已登记**，**非真缺口**） |

**★ 六族告警（`C1` 载体 · 固定一行）**：
> **六族告警：本轮 3 条**（MECH `28`｜C `C22`｜T `T-00`）**｜对比上轮 ±0**｜**真缺口 0 条**。
> ★ **口径**：告警数＝**"待复核清单"**，**不是质量指标**；**连续 N=5 轮为 0 才议转判红**。

---

## §一 · `R88`/`R90` 同型点全仓排查（**只出稿** · 稿见 `reports/2026-09-21_R88-R90同型点全仓排查稿.md`）

### 1.1 排查口径与**四类分级**
| 类 | 处数 | 无重试 | 有重试/降级 | 风险 |
|---|---|---|---|---|
| **甲 外部下载（CI）** | **2** | **1**（**A1＝`R90`**，`ci.yml:451`） | 1（**A2＝`R88`**，`ci.yml:745–754`） | **高 1** ／ 低 1 |
| **乙 工具链/依赖（CI）** | **7 类** | **7** | 0 | 中 6 ／★ **中高 1（B5 playwright）** |
| **丙 本地探测（CI）** | **3** | 2 | 1 | **全低**（**非外部依赖**） |
| **丁 非 CI 下载点（`deploy/`）** | **3** | **2（且静默）** | 1（有降级） | 中 2 ／ 中低 1 |

### 1.2 ★ 三条发现
1. **甲类 2 处、处置不一致** ⇒ **`R90` 未修、`R88` 已修** ⇒ **"修一处 ≠ 修一类"实证**。
2. ★ **最危险处不在甲类** —— **`B5`（`ci.yml:513`，`npx playwright install --with-deps chromium`）**：**体积最大／最易抖／无重试**，且**就在 `A1` 下游同一 job** ⇒ **修好 `A1` 后它仍可能是下一个红点**。
3. ★ **扩扫发现丁类**（`deploy/win64/` **3 处**）—— **`D2`／`D3` 以 `2>nul` 使失败静默**（与 **R83／R89** 同族）；**与甲类"同族不同型"**（**不判红**）。

### 1.3 建议（**不改机制**）
① **A1 加重试**（照 `A2` 同源做法）② **B5 同批加重试**（预防）③ 乙类其余**靠"四证法"判读**（文本未变／前轮通过／源可达／错误形态）④ 丙类**不处置**（防范围虚增）。

---

## §二 · `F-2` 分析：夜间 P3 清单**连续 3 夜 0 项可执行**（**只出稿** · 稿见 `reports/2026-09-21_F-2分析稿_夜间P3清单连续3夜0项.md`）

### 2.1 一句话结论
> **既非"任务池空了"，也非"筛选太严"** ⇒ **真因＝"来源单点"**：
> 清单**只依赖 `ROADMAP.md`「可夜间跑」一列**，而**该列存量已被"完成／升格 P1／依赖真机"吃光**；
> **夜间实际产能一直来自另一批来源**（`docs` 时效／判据加固／汇编同步／编号核对），**但它们从未被写进"清单来源"**。
> ★ **根因**：**该列无"可执行性判据"、无"复核时点"** ⇒ **可连续多夜声称"✅ 可跑"却一项也跑不了**（**标注静默过期**）。

### 2.2 逐行实测（**12 行**，含 `ROADMAP.md` 行号）
「✅ 可跑」**10 行** ＋「⚠️ 部分」**2 行** ⇒ **已完成 4 ／ P1 需决策 4 ／ 依赖真机 1 ／ P2 需验证 2 ／ 里程碑重复 1** ⇒ **可执行 0 行**。
★ **口径校正（如实）**：**"0 项可跑"的字面**只在 **`…09-20_今夜P3清单.md:150`（首次）** 与 **`…09-21_今夜P3清单.md:158`（"连续第 3 夜"）** 出现；
**09-19 夜清单未含该表述**（**当夜未识别**）⇒ ★ **本稿的"0 项"由本轮当场逐行实测独立成立**（**第一手证据**），**"连续 3 夜"则标注来源**。

### 2.3 ★ 与 `F-1` **同一病根**
| 缺陷 | 标注载体 | 症状 | 共同根因 |
|---|---|---|---|
| `F-1` | `TEMPLATES §7.2` **状态列** | 14 条长期挂"🔬试用中"（**已于 v0.311 修复 ＋ 补程序**） | **标注无"起点/到期"** |
| `F-2` | `ROADMAP` **「可夜间跑」列** | 连续 3 夜"✅ 可跑"却 0 项 | **标注无"复核时点"** |

### 2.4 三案（**不改机制**）
**甲 扩源**（**零治理改动**，**推荐先做**）：清单来源写明**六源**（`ROADMAP` 列／台账未完成项／判据盲区／`docs` 时效／汇编同步／编号残余）。
**乙 修列**（**需授权**）：`2.5`／`2.6` **改标 P1** ＋ 该列加**「最后复核」标记**。
**丙 改触发式**（**需授权**，兜底）：同 `T-037` 精神，**有触发才跑**。

---

## §三 · INTEL 状态例行检查（`DESIGN.md §2.1` 动作 ①–③）

- **已读** `coordination/intel/INDEX.md`（**全表**）⇒ 逐条检查触发条件：**两条均未满足**（**阶段仍为阶段二 · 2.4**、**L7 设计完成／L8–L10 冻结**）。
- **本轮触发的 INTEL ＝ 0**；**状态未变（均"观察中"）** ⇒ ★ **按 `J2`，状态保持不变是"正确"而非违规**（**不为好看而硬触**）。
- **`J3`／`J4`**：无"待准备"／"准备中"条目 ⇒ 不适用（**不构成违规**）。
- ★ **试用进度**：**本轮为第 3 轮**（v0.308／v0.310 报告 ＋ 本报告）⇒ **3 轮期满 ⇒ 下轮可议"正式登记为机制 28"**。
- **完整状态表见本报告末尾附加表**（依 `T-042`）。

---

## §四 · 判据自检（**10/10 PASS**）

| 判据 | 自检结论 | 侧数 |
|---|---|---|
| `check_doc_consistency` | ✅ 十二侧全部符合预期 | **12** |
| `check_endpoint_table` | ✅ 五侧均符合预期 | 5 |
| `check_id_set_diff` | ✅ PASS（含 v0.309 五处同批修订） | **22** |
| `check_kernel_purity` | ✅ 四侧均符合预期 | 4 |
| `check_migration_closure` | ✅ 正反两侧均符合预期 | 2 |
| `check_migration_fidelity` | ✅ 两侧都符合预期（**需传参**；`--selftest` rc=0） | 2 |
| `check_private_pointers` | ✅ 十一侧均符合预期（通过 12/12 侧） | 12 |
| `check_reading_instruction` | ✅ 通过=5 失败=0 | 5 |
| `check_report_structure` | ✅ PASS（十侧） | **10** |
| `check_source_of_truth` | ✅ PASS（阳性 3 ＋ 阴性 4） | 7 |
- **实跑（非自检）**：`check_id_set_diff` ⇒ **告警 3**（扫描 **246** 份／**59 162** 行）｜`check_report_structure` ⇒ **告警 0**（**136** 份／报告体 **72**／非报告体 **64**／冻结 **33**）｜判据 F **PASS**（36 份）。
- ★ **一条命令口径订正（如实）**：`check_migration_closure.py` **不再接受 `--mode=check`**（旧记忆里的写法已过期）⇒ **正确用法＝直接运行 / `--selftest`**（**本报告已按正确用法执行**）。

---

## §五 · 汇编**幂等性**检查（**三项合验**）

| 检项 | 方法 | 结果 |
|---|---|---|
| **① 幂等性** | `asm_regen_0920.sh`：**重生成** 4 批 ＋ 合订本 ⇒ 比 `git diff --numstat` | ✅ **空**（**逐字零差异**）⇒ **幂等成立** |
| **② 反向自检（逐字保真）** | `asm_selfcheck_0920.py`：嵌入段是否为合订本**子串** | ✅ 全 True（结构稿 HEAD/BODY/APDX ＋ **4 批**） |
| **③ 覆盖清单一致** | 合订本 §五 名录 vs 脚本实测 | ✅ **34 ＝ 34**，集合相等；**未收录 `REFERENCE_APP_DESIGN.md`**；**断言"不含根 `README.md`"在 §五** |
| **④ 汇编 ↔ 底图 逐份可定位**（**本轮新做**） | 逐份 `docs/*.md` 与汇编比对（**三态：EXACT／LINEWISE／ABSENT**） | ✅ **EXACT 32 ＋ LINEWISE（掩码）3 ＝ 35／35 可解释**；**ABSENT 真缺口 0** |
- ★ **④ 的判读细节（如实）**：3 份"ABSENT"（`FULL_AUDIT_REPORT`／`L5_VALIDATION_REPORT`／`PERFORMANCE_REPORT`）**共 20 行未命中**，
  **20／20 全部含"掩码触发特征"**（本机路径／内网 IP／用户名），**其他 0 行** ⇒ **ABSENT 完全由泄漏掩码解释**，**非缺口**。
  ★ **脚本局限如实写**：该比对脚本**不识别掩码** ⇒ 会把掩码行记为 ABSENT（**故"ABSENT=3"须经人工判读才成立**）。

---

## §六 · 文档时效核对（**触发式 · 本轮未触发**）

| 触发条件（`T-037`） | 实测 | 结论 |
|---|---|---|
| ① `advisor_brief.md` **哈希变化** | 最后一次改动 ＝ `03e3178`（v0.271）；其后无改动 | **未触发** |
| ② `docs/` **有新文档加入** | `docs/*.md` 仍 **35** 份；最后改动 ＝ `d015fce`（v0.302） | **未触发** |
| ③ **重大状态变化**（阶段／机制编号） | 阶段仍 **阶段二 · 2.4**；机制仍 **27** | **未触发** |
- ⇒ **按 `T-037`：未触发即不跑**（**如实记录"未触发"**，不空跑、不伪造批次）。

---

## §七 · 平台任务清理（**只读检查 · 不擅自下线**）

**★ 过期候选（`once` 已过时点，仍然 ACTIVE／或停留 PAUSED）—— 共 6 条**：

| # | 任务 | 计划 | 状态 | 判断 |
|---|---|---|---|---|
| 1 | `云内核·夜间任务晨报（09-21 08:00）` | once @ 2026-09-21 08:00 | **ACTIVE** | ★ **已过期且仍 ACTIVE** ⇒ **建议下线** |
| 2 | `云内核·夜间任务晨报（一次性）` | once @ 2026-09-20 08:00 | PAUSED | **已过期遗留** ⇒ 可删 |
| 3 | `云内核·首夜晨报**独立复核**（09-18 08:30）` | once @ 2026-09-18 08:30 | PAUSED | **已过期遗留** ⇒ 可删 |
| 4 | `云内核·首夜结果晨报（09-18 08:00）` | once @ 2026-09-18 08:00 | PAUSED | **已过期遗留** ⇒ 可删 |
| 5 | `资源巡检·首次验证运行（今晚01:30）` | once @ 2026-08-26 01:30 | PAUSED | **已过期遗留** ⇒ 可删 |
| 6 | `18:00 平台建设进展输出（2026-08-25）` | once @ 2026-08-25 18:00 | PAUSED | **已过期遗留** ⇒ 可删 |

**★ 云内核在跑的活跃任务（4 条，供核对）**：`今夜 P3 清单（每日 21:00）`｜`夜间 P3 自动开工（每日 23:00）`｜`夜间批量调度（每日 21:07 · 12 项不决策任务）`｜`meta-kernel 每日快照蒸馏（每日 21:05）`。

> ★★ **一条观察（值得单独留意）**：**云端"23:00 夜间自动开工"与"21:07 夜间批量调度"今晚均已触发**，
> 而**本轮人工夜间指令在 23:42 下达** ⇒ **存在"自动化通道 ＋ 人工通道"并发/重叠的可能**（机制 19 的双通道设计**未规定二者互斥**）。
> **本报告只登记该观察**（**不改任务、不改机制**）。

---

## §八 · 记忆维护（`MEMORY.md` 压缩）

| 项 | 值 |
|---|---|
| **压缩前** | **12 278 B** |
| **压缩后** | **7 586 B**（**−4 692 B，−38%**） |
| **手法** | ★ **只压"轮次明细"**（v0.306–v0.311 长文 ⇒ **一行一轮的"结论 ＋ 台账指针"**）；**规范条文一律 verbatim 保留** |
| **verbatim 保留** | 标题 ＋ 版本锚（317 B）｜**`★ 铁律` ＋ §一–§五（4 133 B）** ⇒ **断言实测 `True`** |
| **同轮订正** | 「编号」行 **T 1–41 → 1–43**、**R 1–89 → 1–90**（**原值已过期** ⇒ 一并修正） |
| **备份留痕** | `.workbuddy/tmp/MEMORY.bak.20260921night.md`（**仓库外**，可回滚） |
| ★ **口径说明** | 系统层限额是 **"每次会话写入 ≤3 000 字符"**（**写入预算**）而非**文件总上限**；本次为**净压缩**（**减 4 692 B**），**未新增信息体量** ⇒ 属**合规操作**；★ **但"文件总上限"在项目内无成文数值** ⇒ 已在 ⑪ 列为待裁（**是否给 MEMORY 定容量上限 ＋ 复核节奏**） |

---

## ⑧ 三量台账（四量）

| 量 | 内容 |
|---|---|
| **存量** | 机制 **27**｜C **21**｜T **43**｜R **90**｜TERM **13**｜判据脚本 **10**｜`ci.yml` 32 步｜`reports/` **136**｜底图 **36 份**｜INTEL **2** |
| **变量** | **新增 3 份文件**（**2 稿 ＋ 本报告**）；★ **零改动**：`docs/`／`README`／`CHARTER`／`TEMPLATES`／`CONSTRAINTS`／`BASELINE`／`.github/`／`coordination/tools/`（**治理文件与 `ci.yml` 本轮一字未动**）；★ **仓库外**：`MEMORY.md` 压缩（**−4 692 B**） |
| **补充增量** | ① **`R88`/`R90` 同型点全仓排查**（**甲/乙/丙/丁 四类**；**扩扫查见丁类 3 处**；★ **发现 B5 playwright 为"最危险未处置点"**）｜② **`F-2` 分析**（**来源单点**归因 ＋ **12 行逐行实测** ＋ **与 F-1 同病根**）｜③ **汇编 ↔ 底图 逐份可定位检查**（**新做**：EXACT 32 ＋ 掩码 3 ＝ **35/35**）｜④ **判据自检 10/10**（含侧数实录）｜⑤ **平台任务过期候选 6 条**（**含"自动化＋人工双通道并发"观察**）｜⑥ **`MEMORY.md` −38%** |
| **创新增量** | ① ★ **"同型点"分级法**：**判据＝外部依赖 × 有无门禁 × 失败是否静默** ⇒ **四类可分**（甲/乙/丙/丁），**防"范围虚增"**（丙类本地探测**刻意不计入**）｜② ★ **"扩扫"动作**：只扫 `.github/workflows/` **会漏** ⇒ **扩到全仓脚本类**（**查见 `deploy/` 3 处**）｜③ ★ **"汇编↔底图"三态比对（EXACT／LINEWISE／ABSENT）** —— **"ABSENT"须再经"掩码判读"才成立**（**脚本局限如实写**）｜④ ★ **"同病根"识别**：**F-1 与 F-2 同族**（**标注缺复核机制**）⇒ **同一条通则可治两处**｜⑤ ★ **"口径校正"动作**：**发现 `F-2` 的"连续 3 夜"字面只在 2 份清单中出现** ⇒ **本稿不依赖历轮表述、当场实测** |

---

## ⑨ 边界说明（四类）

- **本地跑过**：
  - **两点稿**的**全部依据**：静态扫描（`.github/workflows/` **2/2** ＋ 全仓脚本类 **22 份／31 行**）｜`ROADMAP.md` **逐行解析**（12 行）｜三夜清单**检索**。
  - **判据**：**10/10 自检 rc=0**；实跑 `check_id_set_diff` **告警 3**、`check_report_structure` **告警 0**、判据 F **PASS**。
  - **汇编**：`asm_regen_0920.sh`（**重生成后 `git diff` 空**）＋ `asm_selfcheck_0920.py`（**PASS**）＋ **本轮新做的逐份比对**。
  - **记忆**：压缩脚本执行 ＋ **断言 verbatim 段保留 = True**。
- **CI 过**：❌ **无** —— 本轮**只读／只出稿**，**未触发 CI**（★ 按 **C20**：**CI 结论不入库**；如需 CI 事实，见当日日报）。
- **真实跑过**：❌ 无（**零代码、零 boot 层改动**）⇒ 不适用 **CQ／H**（**机制 26：跨层引用＝无效**）。
- **未验证**：① **`R90` 修法**（待授权）｜② **`B5`／丁类的实际失败率**（**未失败过**，属预防性）｜③ **`F-2` 的"2.5／2.6 实为 P1"**（**引自历夜清单既有判读**，本轮**未重新论证**）｜④ **"自动化＋人工双通道并发"**（**仅观察，未验证是否有实际冲突**）｜⑤ **汇编比对脚本的掩码感知**（**未实现**，靠人工判读）。

---

## ⑩ 决策评估（**本轮无新决策** —— 三处均属"只出稿/待授权"，逐条给出选项与推荐）

| 待决项 | 选项 | 成本 | 风险 | 推荐 |
|---|---|---|---|---|
| **`R90` 修法** | 甲 加重试／乙 只重跑／丙 先甲后乙 | 低 | ★ **只重跑＝不根治**（下次仍红，且"重跑即绿"会掩盖问题） | **丙**（＋**保留"重试耗尽仍判红"**） |
| **是否同批处置 `B5`** | 是／否 | 低 | 否 ⇒ **下一个红点大概率是它** | **是**（**同一 job、同一失败模式**） |
| **`F-2` 先做哪案** | 甲 扩源／乙 修列／丙 触发式 | 甲最低 | 甲不需授权 ⇒ **零等待** | **先甲（本轮/下轮即可）→ 乙（需授权）→ 丙（兜底）** |
| **过期平台任务** | 下线／保留 | 低 | 保留 ⇒ **晨报类任务可能重复触发** | **下线 6 条**（**须你点头**：本轮指令明确"不擅自下线"） |
| **`MEMORY.md` 是否定容量上限** | 定（如 8 KB）／不定 | 低 | 不定 ⇒ **下次仍可能膨胀** | **定"软上限 8 KB ＋ 每 30 天复核"**（**待裁**） |

---

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | **push 放行**（**本轮 3 个本地提交**） | **放行**（机制 23：push 须当轮明示） |
| ② | ★★ **`R90` 修法 ＋ 是否同批处置 `B5`**（`ci.yml` 改动） | **丙 ＋ 同批 B5** |
| ③ | **`F-2` 乙案**（`ROADMAP`：2.5／2.6 改标 P1 ＋ 加"最后复核"标记） | **授权**（**治静默过期根因**） |
| ④ | **本轮成果并入 `BASELINE`（`§五十二`）** | **授权**（本轮从紧未动台账） |
| ⑤ | **6 条过期平台任务是否下线** | **下线**（**本轮未擅自动**） |
| ⑥ | **`MEMORY.md` 是否定"软上限 8 KB ＋ 30 天复核"** | **定** |
| ⑦ | **INTEL 是否正式登记为机制 28** | **下轮议**（**3 轮试用期满**） |

---

## ⑫ 下一步建议

1. **放行 ① 后按 T-038 门控**（等 `completed` 再落笔任何 CI 结论；**C20：只记日报**）。
2. **下轮批次（推荐合并）**：② `R90`＋`B5` 同批加重试 → 跑一次 → ③ `F-2` 乙案 → ④ 台账 `§五十二` → ⑤ 下线过期任务 → ⑦ INTEL 登记议。
3. ★ **`F-1`／`F-2` 的通则**（**建议成文**）：**凡"标注型列"（状态列／优先级列／可跑列）一律带"最后复核时点"** —— 与 **`T-043`** 同一手法。
4. ★ **下轮开工第 1 步＝读 `coordination/intel/INDEX.md`**（**INTEL 机制动作 ①**）。

---

## ⑬ 启动回执
已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD` 的输出 ⇒ 312**。

---

## ⑭ 本轮读了底图哪几份、哪几行

| 文件 | 读取范围 | 用途 |
|---|---|---|
| 根 `README.md` | **未读**（本轮**不改底图**） | — |
| `docs/`（35 份） | ★ **全份参与"汇编↔底图"逐份比对**（**按文件名逐份读取**，见 §五④）—— 但**未逐行人读**（机器比对） | **汇编一致性** |
| `coordination/ROADMAP.md` | **L55–63**（子步表「可夜间跑」列）｜**L90–108**（子任务 4/5/6）｜**L130–140**（里程碑表）｜**L66**（口径定义） | **`F-2` 的 12 行逐行实测** |
| `coordination/TEMPLATES.md` | **§十一（机制 19）**｜**§7.1／§7.2（变更程序 ＋ 43 行表）** | 夜间机制口径 ＋ `T-037`／`T-043` 取证 |
| `.github/workflows/ci.yml` | **全文逐行检索**（1157 行）＋ `.github/workflows/pages.yml`（52 行） | **`R88`/`R90` 同型点排查** |
| `deploy/win64/*.ps1|*.bat` | **三处下载点**逐行 | **扩扫（丁类）** |
| `coordination/reports/2026-09-19|20|21_今夜P3清单.md` | **关键词检索 ＋ §二/§三 逐行判读** | **`F-2` 取证（含"口径校正"）** |
| `coordination/tools/`（10 判据） | **逐个跑自检** | **§四** |

> **★ 说明**：本轮**未修改**根 `README.md`／`docs/`／任何治理文件（`git status` 可验）⇒ **按机制 22 判据 F 无需更新**。**不以此冒充"已核底图"**（C16 口径）。

---

## 附 · INTEL 状态表（**附加表 · 依 `T-042` 置于十四项之后** · `J1` 载体）

| 编号 | 状态 | 触发条件 | 本轮是否触发 | 本轮动作 |
|---|---|---|---|---|
| **INTEL-001** | **观察中** | **组合触发**：企业注册完成 ＋ 阶段三启动 | **✗ 未触发**（阶段定位＝**阶段二 · 2.4**） | 无（下一轮重新检查） |
| **INTEL-002** | **观察中** | **事件触发**：**L7–L10 落地后** | **✗ 未触发**（L7 设计完成／L8–L10 🔒 冻结中） | 无（下一轮重新检查） |

- **合计**：观察中 **2**｜待准备 **0**｜准备中 **0**｜已实施 **0**｜已归档 **0**。
- **`J1–J4` 自查**：J1 ✅（本表存在）｜J2 ✅（**两条未触发 ⇒ 状态保持"观察中"正确**）｜J3 ✅（无"待准备"）｜J4 ✅（无"准备中"）。
- ★ **试用进度＝第 3 轮**（v0.308／v0.310 报告 ＋ 本报告）⇒ **期满 ⇒ 下轮可议"正式登记为机制 28"**（`BASELINE §49.2`）。
- ★ **位置合规**：**置于十四项之后，未占用任何编号位**（依 `T-042`）。
