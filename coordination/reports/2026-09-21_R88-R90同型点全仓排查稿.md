【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# `R88`／`R90` 同型点全仓排查稿（**只出稿，不改文件**）

> **轮次**：2026-09-21 夜间持续任务 · **v0.311 后**
> **起因**：`R90`（`test` job 的 wasm-bindgen 下载无重试）暴露 **"修一处 ≠ 修一类"** —— `R88` 当时只修了 `host-windows`，`test` job 未一并处置。
> **本稿性质**：**排查 ＋ 分级 ＋ 建议**；**不改任何文件**（`ci.yml` 修改须单独授权）。

---

## 〇、排查口径（**先写清，避免"扫了什么"事后说不清**）

| 项 | 内容 |
|---|---|
| **扫描范围** | `.github/workflows/*.yml`（实测 **2 份**：`ci.yml` 1157 行／`pages.yml` 52 行） |
| **检索式** | `curl`／`wget`／`Invoke-WebRequest`／`DownloadFile`／`download`／`git clone`／`uses:`／`rustup (target\|toolchain)`／`cargo (install\|build\|test)`／`npm (ci\|install)`／`npx`／`pip install`／`playwright` |
| **判定维度** | ① **是否外部网络依赖**（区分"本地 HTTP 探测"）② **是否有重试** ③ **是否门禁（失败是否判红）** ④ **失败形态是否可区分（环境偶发 vs 真回归）** |
| **风险分级** | **高**＝外部下载 ＋ 无重试 ＋ 门禁 ＋ 体积/波动大｜**中**＝外部下载 ＋ 无重试（但失败报错明确、易定位）｜**低**＝本地探测 或 已有重试 或 平台托管 action |
| **★ 口径说明** | `curl` 出现在脚本里**不等于**"外部下载" —— 本项目多数 `curl` 是 **`127.0.0.1` 本地服务探测**（L3 网关活体验证／UI 目验），**风险性质完全不同**，故**单列一类**。 |

---

## 一、★ 甲类：**外部下载**（判据要求的核心 —— 共 **2** 处）

| # | 文件:行 | step | 下载内容 | **有重试？** | 门禁 | **风险** |
|---|---|---|---|---|---|---|
| **A1** | `wasm-bindgen 下载（ci.yml:451）` | `Manifest UI wasm 前端构建（wasm-bindgen，产物=dist/）` | `wasm-bindgen-0.2.128-x86_64-unknown-linux-musl.tar.gz`（**GitHub Releases**） | ❌ **无** | ✅ **门禁**（失败判红） | ★★ **高**（**＝`R90`**：本轮已实际判红） |
| **A2** | `llvm-mingw 下载（ci.yml:745–754）` | `修复 R1 —— 安装 llvm-mingw 并把 bin 前置到 PATH` | `llvm-mingw-${VER}-ucrt-x86_64.zip`（**GitHub Releases**） | ✅ **有**：`until curl -sSL … ; do` **shell 循环**（1 首试 ＋ 最多 3 重试／间隔 5s） | ✅ 门禁 | **低**（**＝`R88` 已处置**；★ 且**注释逐字写明**"curl 自带重试只覆盖瞬时错误、不覆盖 TLS/吊销类"） |

> **★ 甲类结论**：**2 处外部下载中，1 处无重试（A1＝`R90`）、1 处已有重试（A2＝`R88` 已修）**
> ⇒ **同类缺陷的处置状态不一致** —— 这正是"修一处 ≠ 修一类"的直接体现。

---

## 二、乙类：**工具链／依赖拉取**（外部网络，但机制不同 —— 共 **7** 类）

| # | 文件:行 | 步骤 | 拉取内容 | **有重试？** | **风险** | 说明 |
|---|---|---|---|---|---|---|
| **B1** | `rust-toolchain 安装（ci.yml:16／733／pages.yml:20）` | `Install Rust (stable)` | `dtolnay/rust-toolchain@stable`（**平台 action**） | ❌（交由 action／GitHub 托管） | 中 | 失败报错明确（toolchain 拉取失败）⇒ 易判读 |
| **B2** | `nightly 安装（ci.yml:581）` | `安装 nightly（boot 层专用）+ llvm-tools-preview + rust-src + 裸机 target` | nightly 工具链（**官方源**） | ❌ | 中 | **该 step 已有显式断言**（`rustup target list --installed` 必须含目标）⇒ **失败可定位** |
| **B3** | `rustup target add（ci.yml:32／431／450／587）` | 各构建步 | 编译目标组件（**官方源**） | ❌ | 中 | 组件小、失败报错明确 |
| **B4** | `npm install（ci.yml:512）` | `UI 自动目验（真实浏览器断言诊断结论可读）` | npm 包（**registry**） | ❌ | 中 | 有 `package-lock`（`npm ci` 语义更稳；此处用 `install`） |
| **B5** | ★ **`playwright install（ci.yml:513）`** | 同上 | **`npx playwright install --with-deps chromium`** ⇒ **下载 Chromium 二进制（约 100+ MB）＋ 系统依赖** | ❌ **无** | ★ **中高** | **体积最大、波动最可能** ⇒ **甲类之外最值得优先处置的一处** |
| **B6** | `cargo build/test（ci.yml 多处）` | 各构建/测试步 | crates.io 依赖 | ❌ | 中 | 失败报 crate 名＋版本 ⇒ **易区分"真回归"** |
| **B7** | `wasm 构建（pages.yml:25–26）` | `Build wasm (same kernel)` | `rustup target` ＋ crates | ❌ | 中 | `pages.yml` **仅发布用**，不参与"三 job 门禁" |

---

## 三、丙类：**本地 HTTP 探测**（**非外部依赖** —— 风险低，如实列出以免误判）

| # | 文件:行 | 步骤 | 内容 | 重试 | 风险 |
|---|---|---|---|---|---|
| **C1** | `网关五端点探测（ci.yml:141–166）` | `L3 网关活体验证` | `curl` **`127.0.0.1:3000`** 五端点 ＋ SSE | 无（**服务在同 job 内先起**） | **低**（**本地**，非网络） |
| **C2** | `UI 目验探测（ci.yml:496–507）` | `UI 自动目验` | `curl -sf 127.0.0.1:$PORT` 健康/首页/probe | ✅ **有**（`… && break` **轮询式**） | **低** |
| **C3** | `宿主取源码（ci.yml:1073）` | `验收 ④ 真实开窗 + URL 取源码 + 上屏回读（门禁）` | `curl 127.0.0.1:18124/page.html` | 无 | **低** |

> ★ **口径提醒**：丙类若被误列进"外部下载无重试"，会**虚增**同型点数量 ⇒ **本稿刻意单列**（**分级必须能区分**，R83 精神）。

---

## 三之二、★ 丁类：**仓库内非 CI 的下载点**（**扩扫发现 · 如实补录**）

> **扩扫动机**：判据说的是"**所有含 `curl`/`wget`/`download` 的步骤**" ⇒ 只扫 `.github/workflows/` **会漏**。
> **扩扫范围**：全仓**脚本类文件**（`*.sh/*.ps1/*.bat/*.cmd/*.yml/*.yaml` ＋ `Dockerfile*`）—— 实测 **22 份**、含下载命令 **31 行**；★ **`Dockerfile` 实测不存在**（`glob('**/Dockerfile')` = 空）。

| # | 文件:行 | 内容 | **有重试？** | 门禁 | **风险** |
|---|---|---|---|---|---|
| **D1** | `probe 下载（deploy/win64/autoprobe.ps1:74）` | `& $curl -s -f -o "$probe" "$base/cloud-probe.exe"`（**LAN origin**） | ⚠️ **无显式循环**，但有**降级路径**（失败后走 .NET `WebClient`，见同文件 L89 分支） | ❌ **不在 CI** | **中低**（**双重降级 ＋ 用户在场可见**） |
| **D2** | `诊断脚本下载（deploy/win64/net-check-one.bat:16）` | `curl.exe -s -f -o "%D%\net-diagnose.ps1" "%ORIGIN%/net-diagnose.ps1" 2>nul` | ❌ **无**（单次尝试） | ❌ 不在 CI | **中**（**失败即静默**：`2>nul` 吞掉错误输出） |
| **D3** | `修复脚本下载（deploy/win64/net-repair-one.bat:27）` | `curl.exe -s -f -o "%D%\net-repair.ps1" "%ORIGIN%/net-repair.ps1" 2>nul` | ❌ **无**（单次尝试） | ❌ 不在 CI | **中**（同上） |

**★ 丁类与甲类的区别（口径写清，防"同型点"范围虚增）**：
- **甲类**＝**外部下载 ＋ CI 门禁 ＋ 无重试** ⇒ 失败**判红**、**必须**处置（`R88`／`R90`）。
- **丁类**＝**外部下载 ＋ 不在 CI** ⇒ 失败**不判红**，但**影响用户在真机上的诊断/修复流程**（D2/D3 还**吞掉错误输出**）。
- ⇒ **丁类与甲类"同族但不同型"**：**同**＝外部下载无重试；**不同**＝**有无门禁** ＋ **失败是否静默**。
- ★ **D2/D3 的 `2>nul` 值得单独记一笔**：**它把"下载失败"变成"静默"** ⇒ 与 **R83（退化须与通过取不同值）**、**R89（声明 ≠ 发生）**同族 —— **本稿只登记，不改文件**。

---

## 四、★ 汇总与分级结论

| 类 | 处数 | **无重试** | **有重试/降级** | 风险分布 |
|---|---|---|---|---|
| **甲 外部下载（CI）** | **2** | **1**（A1＝`R90`） | **1**（A2＝`R88`） | 高 1 ／ 低 1 |
| **乙 工具链/依赖（CI）** | **7 类** | **7** | 0 | 中 6 ／ **中高 1（B5 playwright/Chromium）** |
| **丙 本地探测（CI）** | **3** | 2 | 1 | **全低** |
| **丁 非 CI 下载点（deploy/）** | **3** | **2**（D2／D3，**且静默**） | 1（D1，**降级**） | 中 2 ／ 中低 1 |

**★ 四条关键结论**：
1. **甲类 2 处、处置不一致** —— **`R90`（A1）未处置**、`R88`（A2）已处置 ⇒ **"修一处 ≠ 修一类"已实证**。
2. ★ **同型点的"最危险"不在甲类之外** —— **`B5`（`npx playwright install --with-deps chromium`，`ci.yml:513`）** 是**体积最大、最易抖、且没有重试**的一处；
   它**就在 A1 的下游同一 job**（`test`）⇒ **A1 修好后，B5 仍可能是下一个"红点"**。
3. **丙类不是同型点** —— 本地探测（`127.0.0.1`）**不属外部依赖** ⇒ **不纳入处置范围**（否则范围虚增）。
4. ★ **扩扫发现丁类 3 处**（`deploy/win64/`）—— **其中 D2／D3 用 `2>nul` 把失败变成静默**；★ **它们不判红 CI，故与 `R88`／`R90` "同族不同型"**（见 三之二 口径）。

---

## 五、建议（**不改机制，只提处置顺序**）

| 优先 | 对象 | 做法 | 成本 | 备注 |
|---|---|---|---|---|
| **1** | **A1**（`ci.yml:451`） | **加重试**（**照 A2 的既有 shell 循环同源复用**；保留"重试耗尽仍失败 ⇒ 判红"） | 低 | **＝`R90` 修法（待授权）** |
| **2** | ★ **B5**（`ci.yml:513`） | 同上加重试（Chromium 下载最易抖） | 低 | **本稿新发现**；尚未失败过，属**预防性** |
| **3** | **B1／B2／B3／B4／B6** | **暂不逐一加重试**；改为**在报告里区分"环境偶发 vs 真回归"**（已有四证法） | 极低 | 失败报错明确 ⇒ **优先靠"判读"而非"重试"** |
| — | **C1–C3** | **不处置**（本地，非同类） | — | 防范围虚增 |

> ★ **一条通则（建议成文，待裁）**：**"外部依赖 ⇒ 显式处理瞬时失败；且处置须按"缺陷类型"全仓一次做完，不许只修触发点"**。

---

## 六、边界说明（本节）

- **本地跑过**：✅ **静态扫描**（python 逐行检索）＋ **人工语义分级**；扫描覆盖 `.github/workflows/` **2/2** 份。
- **CI 过**：❌ **无**（本稿**只读**，未触发 CI）。
- **真实跑过**：❌ 无。
- **未验证**：① **A2 的重试是否真在 CI 起过作用**（其注释称"真实重试由 CI 验证"）；② **B5 的实际失败率**（未失败过，属预防性建议）；③ 扫描**未含** `Dockerfile`／脚本内联 `curl`（**本仓库无 Dockerfile**，实测）。
- **★ 本稿未改动任何文件**（`git status` 未受影响）。
