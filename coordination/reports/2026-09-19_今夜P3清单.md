【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 今夜 P3 清单 · 2026-09-19（机制 19 夜间自动化 · 开工前置）

**保密标记**（对应 C12；**拷贝给顾问前先看这一段**）：
- ⛔ 机密（**绝不拷**）：**无** —— 本清单按 C12 已预屏蔽：不含凭证、账号、本机绝对路径、网络细节、参数库取值（阈值一律只写"见编号"，不复述数值）
- 🔐 保密（**屏蔽后拷**）：**无**
- 🔒 半公开（**可拷**）：仓库内相对路径与行号、编号体系（R／D／C／T／机制号）、模块名与计数口径
- 🔓 公开（**直接拷**）：版本号、阶段定位、清单条目、验收结论

> 【给用户的提示】本次报告包含 **0** 处机密、**0** 处保密。无需屏蔽即可整份拷给顾问。

**全景状态卡**（自动生成；含版本号／阶段定位／待决策项／CI 状态）：

```
┌─ 云内核项目 · 全景状态卡 ─────────────┐
│ 版本   v0.218（rev-list --count HEAD） │
│        已在远端同步（ahead 0/behind 0）│
│ 阶段   ROADMAP 阶段二 · 子任务2        │
│        2.1 ✅ / 2.2 ✅ / 2.3 ✅ /      │
│        2.3b ✅ 已收口（全源：源 56 /   │
│        目标 58，未迁 0）                │
│        后续：2.4（P1 需决策）／2.5／2.6 │
│        ／2.7（P2 需验证）               │
│ CI     最近＝v0.218 补记 run 35435254790│
│        （按 BASELINE 快照；本次未触发） │
│ 漏洞   R1–R82（本轮无新增 R）          │
│ 治理   机制 25 / C 19 / T 34（C15 复算 │
│        无缺号无重号）                   │
│ 决策   待确认 D42–D49（8 项）＋ 待裁    │
│        6 项（见 §七）                   │
└────────────────────────────────────────┘
```

**任务**（含**基准**与**总规划定位**，含**优先级 P0–P4**）：生成「今夜 P3 清单」（机制 19／C14 开工前置）｜基准 v0.218｜定位：阶段二 · 子任务2（2.3b 已收口，2.4 路线今日刚裁定）｜**优先级 P3**（无需决策、可自主推进）

**状态**：完成

**验收对照**（标准 vs 实测，逐条）：

| # | 验收标准（指令原文拆解） | 实测 | 结论 |
|---|---|---|---|
| 1 | 输出**第 0 行**＝「顾问阅读指令」固定文本（C13） | 本文件字面第 1 行即为该段（起首无前置空行）；**按权威源 9 步**（见 §三 缺陷-6） | ✅ |
| 2 | 清单**只取 P3 且「可夜间跑＝✅」** | 5 项，逐项标 P3 ＋ 可夜间跑 ✅（见 §一） | ✅ |
| 3 | 每项写清【任务／依据／预计时长／可夜间跑】 | 5 项四字段齐全；依据逐项给行定位（**4 项依据不在 ROADMAP**，已如实标明，见 §三 缺陷-5） | ✅ |
| 4 | **排除**需用户决策（P1）项 | 2.4（P1）／子任务6（P1）／D42–D49 ／R80 四问 ／判据 F 口径 已排除并联原因（§二） | ✅ |
| 5 | **排除**需真实硬件／真机验证项 | 子任务4 ❌／子任务5（依赖子任务4）／2.7（P2）／老笔记本实测 已排除（§二） | ✅ |
| 6 | **排除**「对外可见／不可逆」动作 | 子任务6（镜像分发）／远端推送／C10 第 1 项动作／切模型 已排除（§二） | ✅ |
| 7 | **排除治理文件类改动**（CONSTRAINTS／CHARTER／ROADMAP／BASELINE／TEMPLATES／`.github/workflows/`） | 清单 5 项**无一**含治理文件或 CI 配置改动；「CI 优化」**整类排除**（含 closure／fidelity 入 CI 的待裁项）；§三 发现的观测缺陷**只登记、不改** | ✅ |
| 8 | 写入 `coordination/reports/<今日日期>_今夜P3清单.md`（同日只保留一份） | 本文件（2026-09-19）；核查：同目录**无同名文件**（检索式见 §三 缺陷-4 四要素），为**新写**非覆盖 | ✅ |
| 9 | 清单**不含 ⛔ 内容** | 实测：无凭证／账号／本机绝对路径／网络细节／参数库取值；行号与计数属 🔒，已标级 | ✅ |
| 10 | **不在远端同步、不提交**、不改代码 | 全流程只读 ＋ 只新建本 1 份清单；`git status` 未产生新提交（仅 2 个既有未跟踪设计稿）；未改任何源文件 | ✅ |
| 11 | 汇报 ≤6 行 | 对话侧汇报已按要求压缩 | ✅ |

---

## 一、今夜 P3 清单（5 项，按建议执行顺序）

> **执行口径**：① **一律只读 ＋ 只写 `reports/` 新文件**；**不改治理文件、不改代码、不提交**（本清单自身即"只写清单"）。② 若时间不足，**按序执行、可截断**，截断处须在报告中写明。③ 每项完成→**独立**报告落 `coordination/reports/`。

### P3-1｜2.5 前置调研：`field-render` 作为**第一个用户空间进程**——前置条件清单 ＋ 执行体归属判定

- **任务**：只做**调研 ＋ 归属判定**（不出实现方案、不写码）。① 读 `ROADMAP.md` 2.5 行与 `docs/` 相关底图条目，列出"内核需先具备什么"（进程／地址空间／系统调用面／与 boot 层的边界）；② 用 **机制 21**（`TEMPLATES.md` §十三 ＋ `coordination/tools/check_kernel_purity.py`）逐项判定"感知／动作 vs 纯算"，产出**该落在内核／宿主**的归属表；③ 与今日新裁定的 **2.4 新路径（A1 CPU-SIMD 多重网格场方程求解 ＋ B 低维场演化，共用场方程求解器）** 做一次**接口衔接核对**（只比对边界，不评价方案）。
- **依据**：`ROADMAP.md:61`（**2.5 行**：`field-render` 作为第一个用户空间进程｜优先级 **P3**｜可夜间跑 **✅（真机验证环节除外）**）。⭐ **首夜 4 项未覆盖 2.5**，`reports/` 检索"2.5"命中 **0**（对照：检索"2.6"命中 1）⇒ 本项为**真空白**，非重复。
- **预计时长**：约 50 分钟
- **可夜间跑**：✅（纯阅读 ＋ 出稿；无需真机、无需决策）

### P3-2｜f64 `sin`／`cos` **影响面普查 ＋ 处置方案包**（只读出稿，**不改码**）

- **任务**：① **调用方普查**（R31 教训：改前先问谁还在用）——在**全仓**（含 nostd／core／boot／host／gateway）检索 `FloatOps::sin`／`cos`／`sin_cos_f64` 及**间接依赖**（FFT／频域／`fourier`／NCA）；② 评估**未来承重**：今日已明确 **Fourier-NCA 需 FFT ⇒ 需 `sin`/`cos`**，故须回答"若走该路，精度缺陷是否会在频率域放大"；③ 出**三选一处置包**（修／明示支持范围／暂不动），每选项给**可证伪条件**与改动面（哪些文件、是否改变既有数值结果），**不代行决策**（C11）。
- **依据**：`coordination/reports/2026-09-17_fmath精度回归补缺.md` **§四 缺陷-1／缺陷-2**（f64 `sin`/`cos` 全域误差 ≈ 3.9e-13 **≈ 3500 ULP**，超 **D29 门线（≤4 ULP）约 875×**；根因＝`cos` 级数缺 `r¹⁴/14!`、归约无 Payne-Hanek ⇒ `|x| ≥ 1e8` 退化、`≥1e15` 灾难性错误且**输出越界**）＋ `coordination/discussions/2026-09-19_场方程求解器设计稿.md` 的**防误用口径①**。⚠️ **依据不在 ROADMAP**（ROADMAP 无对应行）——见 §三 缺陷-5。
- **预计时长**：约 60 分钟
- **可夜间跑**：✅（纯阅读 ＋ 出稿；**修复本体**属数值算法改造，须走 **C4** 且牵动 **D29** 门线口径 ⇒ **今夜禁改码**）

### P3-3｜**C19「判据↔判据同源」全仓审计**（只读出稿，**不落码**）

- **任务**：C19 系 **2026-09-19 新立**（同日由 R79 触发）。① 对现存 **8 类判据**（`coordination/tools/`）逐对审计：**同一机器事实是否存在两份口径实现**（R79 原型＝`glob("*.rs")` vs `modules()` 差 9 个模块）；② 对**有 CI 侧副本的判据**（机制 17 syntax／21 purity／22 source_of_truth／24 endpoint_table／25 doc_consistency）额外核查"CI 内联命令"与"工具本体"是否**口径一致**；③ 产出**同源审计表**（事实／实现处／是否同源／建议），**只列不改**；④ 顺带如实标注**「已入 CI 的判据集合」这一状态值本身会漂移**（CI 只跑 5 类，`closure`／`fidelity` 仅本机）。
- **依据**：`CONSTRAINTS.md` **C19**（`BASELINE.md` **§17.3** 单列）＋ `CHARTER.md:95/100`（机制 22／24／25 的"判据设计模式"）。⚠️ **依据不在 ROADMAP**——见 §三 缺陷-5。
- **预计时长**：约 60 分钟
- **可夜间跑**：✅（判据工具只读执行本就允许；**改动判据会制造假绿风险** ⇒ 今夜**只出审计表**，落码留白天）

### P3-4｜**活跃状态型陈述「过期点」普查**（只读出清单，**不改治理文件**）

- **任务**：机制 25（文档一致性）的三条判据**只覆盖**「段号／收口／内部自洽」；本轮实测发现**「决策表状态」与「编号范围」两类活跃陈述不在覆盖内**（R82／R41／R80 家族）。① 逐文件（`BASELINE.md` §五／§六／§九、`advisor_brief.md` 决策表与术语速查表、`ROADMAP.md` 观察相、两份设计稿）**取证式**扫描"活跃状态陈述"；② 输出**过期点清单**（文件:行／现文／实测事实／依据／是否治理文件）；③ **治理文件内的过期点只登记不修改**（供白天一处修、过 **C15** 双向计数）。
- **依据**：`BASELINE.md` **§18.4**（R82 处置范式）＋ **§五／§九**；已实测 3 处候选见 §三 缺陷-1／2／3。⚠️ **依据不在 ROADMAP**——见 §三 缺陷-5。
- **预计时长**：约 45 分钟
- **可夜间跑**：✅（纯只读；**治理文件整类夜间禁改**，故只出清单）

### P3-5｜**R27 编码／解码不对称**：调用方普查 ＋ 修复清单（只读出稿，**不改码**）

- **任务**：R27＝`l5_baseline` 序列化**不保真**（`to_json` 写 `object`／`established`，`from_json` 固定回填占位 `"obj"`／`"loaded"`）。① 普查该序列化对的**全部调用方**（内核侧写／宿主侧读）与**当前是否靠宿主另行注入**兜住；② 给出**修复清单**（改哪些函数、是否影响既有持久化文件格式、是否需迁移档）＋ **C4 前置清单**；③ 附**阳性／阴性对照**设计（写入后读回必须逐字段相等；错档必须被拒），**不改码**。
- **依据**：`BASELINE.md` **§六 R27**（状态 **⏳ 未处置**；原文注明"**属行为修复，须走 C4：先列清单**"）。⚠️ **依据不在 ROADMAP**——见 §三 缺陷-5。
- **预计时长**：约 45 分钟
- **可夜间跑**：✅（普查＋出清单；**落码须白天过 C4**）

> **总计**：约 **4 小时 20 分钟**（21:00 清单 → 23:00 开工 → 约 03:20 完成；余量留给报告撰写与自检）。

---

## 二、本期明确排除项（附原因 —— 排除也是结论，一并留痕）

| # | 排除对象 | 排除原因 |
|---|---|---|
| 1 | **2.4 实现（DRM/KMS／轻量帧缓冲）** | **P1 需决策**：C9 unsafe 白名单须逐条放行；且今日刚裁定路线 **A1＋B**、设计稿 **Q1–Q10** 待裁 ⇒ 夜间不得代行决策（C11） |
| 2 | **子任务6 镜像分发** | **P1 ＋ 对外可见**（C10 第 1 项明列动作为"对外可见类"） |
| 3 | **子任务5 安装程序** | ROADMAP 虽标 P3 ✅，但**依赖子任务4**，而子任务4 **❌ 需真机** ⇒ **前置未达成**，不得以"P3"名义抢跑 |
| 4 | **子任务4 硬件适配／老笔记本实测** | **❌ 需真机**（ROADMAP:82、`BASELINE.md:72`） |
| 5 | **2.7 生成可引导 ISO ＋ QEMU 验证** | **P2 需验证**（且真机引导不可夜间跑，ROADMAP:63） |
| 6 | **D42–D49（8 项待确认）** | **P1 需决策**；其中 D42（签到＋派猫猫定时任务）涉登录态＝**对外动作**（C10） |
| 7 | **R80 四问（测试计数类过期的规则）** | **待发起人确认**（`BASELINE.md:822`）⇒ 规则未定，夜间不得自定 |
| 8 | **机制 25 扩展以覆盖"测试计数"类** | 同 #7（规则未定 ⇒ 先裁后建） |
| 9 | **判据 F 是否纳入根 `README.md`**（甲／乙／丙） | **待裁 P1**（09-19 日志 §三） |
| 10 | **C13 形态裁定（是否回填历史报告／升为机器判据）** | 属**决策**；且"升为机器判据"要动 `.github/workflows/` ⇒ 双重排除 |
| 11 | **ROADMAP 口径同步**（2.4＝轻量帧缓冲／DRM 降远期） | **ROADMAP 属治理文件** ⇒ 整类排除（白天一处修） |
| 12 | **两份设计稿的 Q 清单（Q1–Q10／Q1–Q11）** | **待裁 P1**，夜间不得擅自选边 |
| 13 | **远端同步 / C10 第 1 项动作 / 切模型** | **C10 ＋ D33 ＋ `TEMPLATES.md` §11.8**（夜间默认不与远端同步） |
| 14 | **`CONSTRAINTS.md`／`CHARTER.md`／`ROADMAP.md`／`BASELINE.md`／`TEMPLATES.md`／`.github/workflows/`** | **任务书明令整类排除**；连带排除 P3 白名单里的「**CI 优化**」类（含 `closure`／`fidelity` 入 CI） |
| 15 | **f64 `sin`/`cos` 精度修复本体** | **数值算法改造 ＋ 会改既有数值结果** ⇒ 须走 **C4**；今夜**只出方案包**（P3-2） |
| 16 | **身份档案建立（`SOUL.md`／`IDENTITY.md`／`USER.md`）** | **需用户在场对话 ＋ 写用户级文件** ⇒ 不属无人监督夜间任务（09-19 日志已记"等用户单独放行一轮"） |
| 17 | **历史报告 C13 回填** | 建议"不回填"已登记但**未裁定**；且会改历史快照 ⇒ 排除 |

---

## 三、真实缺陷（本次发现的，**只登记不改**）

**缺陷-1：决策表状态型过期 —— `BASELINE.md:228`（D41）仍写「⏳ 待确认」，而片6／片7／片8 已执行、2.3b 已收口**
- 证据：`BASELINE.md:228` D41 行现文＝"⏳ **待确认**"；`ROADMAP.md:59` 与 `BASELINE.md:154` 均已记 **片6 按「方式 A」换 `BTreeMap` 并收口**、**片9 补迁**、**未迁 0**。
- 性质：**活跃状态陈述过期**（**R82 家族**；机制 25 **不覆盖**决策表状态）。

**缺陷-2：同一事实两处口径相反（R79 家族）——`advisor_brief.md:194` 与 `BASELINE.md:239` 对 D37 的结论不一致**
- `advisor_brief.md:194`：D37「✅ 本轮已执行并通过全部判据…**待你追认**」；`BASELINE.md:239`：D37「✅ **用户已追认**」。
- 即：**"同一机器事实，两处给出相反结论"** 的文档侧同族。

**缺陷-3：摘要／速查类内容未随正文同步（R41 家族）——`advisor_brief.md:249–251` 编号范围过期**
- 现文：`C1–C14`／`R1–R23`／`D1–D33`；实测：`CONSTRAINTS.md` 已到 **C19**、`BASELINE.md` §六 已到 **R82**、§九 已到 **D49**。

**缺陷-4（机制链）：09-18 的 21:00「今夜 P3 清单」未生成，23:00 仍自择 3 项开工**
- ① **检索式**：`git ls-files -z | tr '\0' '\n' | grep -i 今夜`（**用 `-z`，遵 R81**）＋ `ls coordination/reports/ | grep -i 今夜`；
- ② **范围**：全仓**跟踪文件**（`git ls-files -z`）＋ `coordination/reports/`（**60 份**）；
- ③ **命中数**：**1**（仅 `2026-09-17_今夜P3清单.md`）⇒ **`2026-09-18_今夜P3清单.md` 不存在**；
- ④ **阳性对照**：同一检索式**命中 09-17 那份**（证明检索式有效）；**阴性对照**：检索不存在的串 ⇒ 命中 0。
- ⇒ **机制 19 前置链在 09-18 断过一次**（n=3 采样中 2 次齐备）；09-18 日志已把"清单为何未生成"登记为待放行第 ⑥ 项，**根因至今未定**。⚠️ 本项**只登记**，不在夜间深挖（可能牵出平台侧）。

**缺陷-5（依据缺行·规划缺口）：任务书要求"依据（ROADMAP 哪一行）"，但 ROADMAP 中「P3 ＋ 可夜间跑 ✅」的行**只剩 2.5／2.6**（2.6 首夜已调研），**不足以支撑 5 项清单**
- ⇒ P3-2／3／4／5 的**依据不在 ROADMAP**，改引 `BASELINE.md`（§六 R27／§18.4）与 `reports/`（已逐项标明）。**建议（白天）**：ROADMAP 的"可夜间跑"列补「**遗留缺陷修复**」与「**判据维护**」两个类别，否则每晚清单都会被迫"越出 ROADMAP"。

**缺陷-6（口径冲突·与首夜同款）：任务书写"阅读指令 8 步"，权威源是 **9 步**
- 权威源：`TEMPLATES.md` §二 第〇项（**9 步**，T-018 生效）／`CONSTRAINTS.md` C13（"9 步"）／`CHARTER.md` §四 C13 行（"9 步"）。
- 处置：**按权威源 9 步全文粘贴**（本文件第 0 行）。
- ⚠️ **仍有 1 处活文件不同步（属治理文件 ⇒ 今夜只登记）**：`CHARTER.md:209` 写「顾问收到返回后该做什么（**8 步**）」。首夜登记的 3 处（`BASELINE.md:88`／`:171`／`CHARTER.md:181`）**已修**（`BASELINE.md:174` 现为 9 步、第 9 步说明齐备）⇒ **本轮残 1 处**。（`CHARTER.md:80/212` 的"八步"指**通用工作流八步场域演化**，**不是**阅读指令，**不属缺陷**。）

---

## 四、三量台账（C6）

| 量 | 内容 |
|---|---|
| **存量** | 读 `CHARTER.md`／`CONSTRAINTS.md`／`TEMPLATES.md` §三·§十一／`BASELINE.md` §五·§六·§九／`ROADMAP.md` 全文；**5 类判据只读实跑**（均 exit=0）；`reports/` 60 份目录清点 |
| **变量** | **0** —— 本清单**未改动任何既有文件**（治理文件、代码、CI 配置、判据工具一律未动） |
| **补充增量** | **1 份**：本清单（`coordination/reports/2026-09-19_今夜P3清单.md`，新写非覆盖） |
| **创新增量** | **0** |

---

## 五、边界说明（C5；四类齐全）

- **本地跑过**：✅ 有 —— `check_private_pointers.py --mode=syntax`（exit=0，含 [5] 密钥（⛔ 机密）级别判据／[6] 指纹（🔒 半公开）字面量判据，两项均 PASS）｜`check_kernel_purity.py --selftest`（exit=0，四侧符合预期）｜`check_doc_consistency.py`（exit=0，P1／P2a＋P2b／P3 全过）｜`check_migration_closure.py`（exit=0，源 ⊆ 目标）｜`check_endpoint_table.py`（exit=0，差集 0／0）。
- **CI 过**：❌ **无** —— 本任务为只读，**未触发 CI**；状态卡中的 CI 信息取自 `BASELINE.md` **快照**（**本次未独立核实**，不得当 CI 结论用）。
- **真实跑过**：❌ **无** —— 未跑 QEMU、未上真机。
- **未验证**：① 清单 **5 项均未执行**（属 23:00 开工范围）；② 各项**预计时长均为估计值**，未实测；③ 「`closure`／`fidelity` 不在 CI 内」为**读 `ci.yml` 所得**（`grep -c closure`＝0），**未在 CI 侧实跑验证**；④ 判定「`2026-09-18_今夜P3清单.md` 不存在」基于本地克隆（**未查远端／平台侧**）。

---

## 六、决策评估（如有新决策，按 §九 七要素）

**本轮无新决策。** 本清单为"只读 ＋ 只写清单"，**不含需要拍板的事项**；D42–D49、R80 四问、判据 F 口径、C13 形态等**一律保持待裁状态**，不由本清单代行（C11）。

---

## 七、需要用户决策（**白天**；本清单只列，不催不动）

| 级别 | 事项 | 来源 |
|---|---|---|
| P1 | D42–D49（8 项） | `BASELINE.md` §九 |
| P1 | R80 四问（测试计数类过期规则） | `BASELINE.md:822`／`discussions/2026-09-19_R80侦察稿.md` |
| P1 | 判据 F 是否纳入根 `README.md`（甲／乙／丙） | 09-19 日志 §三 |
| P1 | ROADMAP 口径同步（2.4＝轻量帧缓冲；DRM 降远期） | 帧缓冲稿 Q1 |
| P1 | 帧缓冲稿 Q1–Q10 ＋ 场方程稿 Q1–Q11 | 两份设计稿 |
| P2 | C13 形态裁定（历史回填？升机器判据？） | 09-19 日志 §九 |
| P3（建议项） | ROADMAP 补「遗留缺陷修复／判据维护」两个可夜间跑类别 | **§三 缺陷-5** |

---

## 八、下一步建议

1. **21:00–23:00**：用户可对清单增删改；**无异议则 23:00 自动开工**。
2. **23:00 起**：按 P3-1 → P3-5 顺序执行；每项**独立 commit**（消息前缀 **`夜间 P3-n：`**，遵机制 23 来源判据）＋**独立报告**；**不与远端同步**（D33）。
3. **08:00 前**：出「夜间结果」汇总（按报告模板十四项）。
4. **白天**：一处修治理文件（`CHARTER.md:209` 8→9 步；`BASELINE.md:228` D41 状态；**须过 C15 双向计数**）＋ 处理 §七 待裁项。

---

**启动回执**（`CHARTER.md` §五）：已读 `CHARTER.md`（机制 1–25／C1–C19／边界）＋ `CONSTRAINTS.md`（C10／C12／C13／C14 全文）＋ `TEMPLATES.md` §三（优先级）·§十一（夜间自动化）＋ `BASELINE.md`（§五 未完成／§六 已知漏洞／§九 待决策）＋ `ROADMAP.md`（可夜间跑列）。
当前状态：**v0.218**（`git rev-list --count HEAD`，已在远端同步）｜ROADMAP **阶段二 · 子任务2**（2.1 ✅／2.2 ✅／2.3 ✅／**2.3b ✅ 已收口**；2.4 路线今日刚裁定）｜本阶段目标：元内核成为**可引导内核**且能**直接输出场域画面**｜本次任务：生成今夜 P3 清单（**P3**，只读 ＋ 只写清单）｜是否继续：**等待用户 21:00–23:00 异议；无异议则 23:00 自主开工**
