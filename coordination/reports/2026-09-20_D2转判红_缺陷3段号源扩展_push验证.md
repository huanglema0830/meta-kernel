【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# v0.234 · D-9 push ＋ D-2 转判红 ＋ 缺陷③ 段号源扩展 · 报告

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**（无密钥/账号/本机绝对路径/内网 IP）。**可整份转发**。

**② 全景状态卡**（★ 数字为**生成时快照**；提交/推送动作本身会推高版本号 ⇒ **现值一律以复核命令为准**，R82）
```
┌─ 云内核项目 · 全景状态卡 ─────────────────────────────────────┐
│ 版本   以 git rev-list --count HEAD 为准（不写死，C2）        │
│        本轮起点 v0.233（238）→ 本轮末 239（v0.234，提交即变）  │
│ 阶段   阶段二 · 2.4 纯算层 ✅ ｜ 2.4 边界层（boot ⑫ 段）✅     │
│ 本轮   push 2 次（上一轮遗留 6 个 ＋ 本轮 1 个）；本地新增 1 提交│
│ 未推送 快照 0（已全部推送）                                    │
│ CI     ① run 35475813727（上一轮 6 个 commit）→ 三 job 全绿    │
│        ② run（v0.234）→ 见 ⑨ 边界说明                          │
│ 决策   待裁：D-1（暂停）／D-7（暂缓）＋ 本轮新增「授权同步 3 文件」│
└───────────────────────────────────────────────────────────────┘
```
复核命令（R71：数值不抄进文档）：`git rev-list --count HEAD`｜`git rev-list --count origin/main..HEAD`｜`gh run list --limit 3`

**③ 任务**：基准 **v0.233**｜总规划定位：push 7 个 commit ＋ 4 项裁定落地｜类型：裁定落地｜优先级 **P1**。
约束：禁止内联含反引号/特殊字符命令（全程脚本文件）｜不 taskkill｜**不改底图**｜**不改治理文件**（除授权项）。

**④ 状态：完成**（含 **1 处如实偏差**：指令写"7 个 commit"，**实测未推送为 6 个** —— 以实测为准，见 ⑥）。

**⑤ 验收对照**

| 标准 | 实测 |
|---|---|
| push 完成，CI 三 job 全绿 | ✅ **两次 push**（上一轮遗留 6 个 `52f12e3..9e162f7` ＋ 本轮 1 个 `9e162f7..fe40c9c`）；`run 35475813727` **test 1m21s / boot-image 2m14s / 宿主 4m24s 全绿** |
| **1.4 边界层在 QEMU 上真实验证** | ✅ ★ **「真验到」，非"静默跳过"** —— 证据链见 ⑦① |
| D-2 转判红，自检全过 | ✅ `p4_obs` 并入 `errors`；侧⑦b 改"命中即判红"；**自检八侧全过** |
| 缺陷③ 段号源扩展，自检全过 | ✅ 机器最大段号 **⑪ → ⑫**（处数 11 → 16）；**新增自检侧⑧**（防"只看 `verify.rs`"回归）；自检八侧全过 |
| **C15 复算通过，编号表连续无缺号无重号** | ✅ **编号表条目 0 处变动**（`git diff` 实证；唯一触碰为 R80-1 行**追加**）；C **1–19** / T **1–34** / 机制 **1–25** 全 **PASS**（缺号=无、重号=无） |
| 不改底图 / boot 层 / 治理文件 | ✅ 仅改 **`tools/`（非治理）** ＋ **`BASELINE.md`（指令授权"记录到 BASELINE"）** ＋ **`advisor_brief.md`（非五件套）**；`docs/` **零改动** |
| 第 0 行含 9 步阅读指令 | ✅ 自 `TEMPLATES.md` **逐字抽取**（D40，不手写）并回读全等自检 **PASS** |

**⑥ 未完成项**：① **指令写"push 7 个 commit"，实测为 6 个**（`origin/main..HEAD` 实测 6 ⇒ 已全部推送；**R35 报数写口径**）；② **3 处机制描述待授权同步**（`CHARTER.md` 机制 25 行／`TEMPLATES.md` T-032／`ROADMAP.md:59`）—— 本轮明示"不改治理文件"，**一律未改**，登记于 `BASELINE.md` §20.3；③ `present_field`（边界层 **product 入口**）**尚未被调用**；④ D-1（按指令**暂停**）、D-7（按指令**暂缓**）；⑤ **`fmath.rs` 的 `sin_cos_f64_regression` doc 数值已过期**（写"≈3500 ULP／≈3.9e-13／差 875×"，但本轮已补 `cos r¹⁴/14!` 与 `sin r¹⁵/15!`）—— **本轮不动代码**（D-1 按指令暂停）⇒ **登记待授权**。

**⑦ 真实缺陷（本轮 6 条）**

① ★ **1.4 的"真验到"判定链**（**本轮最重要的新证据**）—— 为什么可以断定"真验到"而非"静默跳过"：
   - **接线完整**（test job 机器断言）：`PRESENT_MIRROR_AND_WIRING_OK` ＋ `[诊断] ⑫ 投影灰度 = [0, 85, 170, 255]` ＋ 镜像 `5 passed` ⇒ **present.rs 存在 ＋ 已声明模块 ＋ 真被 `run()` 调用 ＋ 121–125 五码齐备**；
   - **不落入任何退化分支**（boot job 实测）：QEMU 帧缓冲 = **1280×720**、`截屏 1280x720 maxval=255`、`整屏采样 25 点，颜色分布（前 5）：[((0,255,0),25)]` ⇒ ①`desc_from_info` 不返 `None`（宽高/bpp 合法）；②**宽高 ≥ 2**；③`min_len ≤ buf.len()`（否则返 125 ⇒ 红）；
   - **推理**：`present_selfcheck` 的所有**退化出口**（None / 宽高<2 / 缓冲不足）在此环境下**要么不成立、要么返回非 0 ⇒ 红屏**；实测**绿屏** ⇒ **只可能是"写入 4 像素 → 回读 → 逐字节一致"走通后返回 0** ⇒ **真验到** ✅
   - **诚实边界**：此判定**仅对 QEMU 成立**（`present.rs` 头已写明：真实硬件 WC 显存回读可能拿陈旧值）。

② **缺陷③（本轮已修）**：机制 25 的段号源**只读 `verify.rs`** ⇒ `main.rs` 的 ⑫ 段接线**对判据不可见**（实得 ⑪，真值 ⑫）—— R80 家族（判据覆盖盲区）。

③ **R80 家族新实例**：`ROADMAP.md:59` 的**活跃**段号「⑪」因该行含"**历史快照**"词而被**整行豁免** ⇒ P1 对它**漏报**（机制 25 的"历史标记词"豁免**可能过宽**，与上轮 `§18.4`「表格级历史豁免过宽」**同族**）。

④ **自指陷阱（判据不空转的活证据）**：`BASELINE.md` §20.1 初稿把**夹具数字原样写进正文**（"源 NN ／ 目标 NN"）⇒ **当场被 P4 判红 2 条**；已改**描述式**（**不改判据、不调参** —— 判据表现正确）。

⑤ ★ **上轮报告的误述（本轮纠正·须留痕）**：上轮报告 ⑪④ 称"**主区间 ≤4 ULP 已达标**" —— **不实**。实测/原文：
   - `meta-kernel-core-nostd/src/fmath.rs` **行 1436–1452**（`sin_cos_f64_regression` doc）自述「门线（**D29**）**≤ 4 ULP** ⇒ **未达标**」；
   - 而**那条 `≤ 4 ULP` 是 `f32` 口径**（`fmath.rs` **行 18–20**："内部用 `f64` 运算、**返回 `f32`**"）；
   - **f64 侧从来没有 ≤4 ULP 判据**；本轮实测 `cos` 主区间 **maxULP = 10**（> 4）⇒ **未达**（已到**截断误差理论界内**：maxAbs 1.110e-15 < 2.0038e-15，但**这不是 ULP 达标**）。
   - **教训（C19 家族：判据↔判据同源）**：**同名断言可能属于不同精度域**（f32 vs f64）；**引用"某判据已达标"前，必须回读该断言的作用对象**。

⑥ **新盲区（R80 家族）**：**源码注释里的数值型陈述无任何判据覆盖** —— 判据 F 只覆盖 `docs/` 的散列文件，机制 25 只扫 `coordination/*.md` ＋ `README.md` ＋ `docs/*.md` ⇒ **`meta-kernel-core-nostd/src/*.rs` 的注释数值（如上面那处"875×"／"≈3500 ULP"）无人检查**。

**⑧ 三量台账**（四量）
- **存量**：D-2（P4 判红）｜缺陷③（段号源多文件）｜1.4 的 QEMU 实证｜14 项判据本机回归｜C15 复算
- **变量**：`coordination/tools/check_doc_consistency.py`（**+155 / −46**）｜`coordination/BASELINE.md`（**新增 §二十**，+47 行）｜`coordination/advisor_brief.md`（2 处段号）｜判据自检 **七侧 → 八侧**｜机器最大段号 **⑪ → ⑫**（出现处数 **11 → 16**）
- **补充增量**：`boot_seg_sources()`（**递归扫 boot kernel 全部 `.rs`，不写死文件名** —— 防"新增文件被漏"）｜自检**侧⑧**（段号源多文件）｜P4 转判红的**三项前置闭环**（处置 / `_EM` 粗体容忍 / 表格级历史豁免）
- **创新增量**：★ **"段号源必须多文件且不写死"成为可判据形态**（侧⑧：夹具只在 `main.rs` 写 ⑫，机器须取到 ⑫）；★ **P4 判红闭环当天即抓出自指文本**（判据有效性自证）；★ **1.4 的"真验到 vs 静默跳过"判定链**（用"退化出口的返回码 ⇒ 红/绿"反推，而非只看绿屏）

**⑨ 边界说明**（四类）
- **本地跑过** ✅：14 项判据全 **exit=0**（机制 18/21/22/24/25 各自 `--selftest` ＋ 实跑、私有指针 syntax、迁移闭包、迁移保真度 `--selftest`、屏幕转储 `--selftest`）；机制 25 自检 **八侧全过**；机器段号实测 **⑫（来源 3 份 `.rs`）**；C15 复算；`py_compile` 通过。
- **CI 过** ✅：**三次 push 各自三 job 全绿** —— ① `run 35475813727`（上一轮 6 个 commit）：test ✓1m21s｜boot-image ✓2m14s｜宿主 field-render ✓4m24s；② `run 35480973651`（**v0.234 判据加固**）：test ✓1m48s｜元内核引导镜像 ✓2m3s｜宿主 field-render ✓5m10s；③ `run 35481288838`（**v0.235 本轮报告**）：test ✓2m2s｜元内核引导镜像 ✓2m5s｜宿主 field-render ✓5m13s。
- **真实跑过** ✅（**本项目首次把"边界层写入帧缓冲"跑到真实引导**）：QEMU 8.2.2 真实引导，`BOOT_QEMU_SCREENSHOT_OK`、`R24_UPLOAD_NO_SOFT_FAILURE`、镜像 MBR 签名 `55aa` 通过。
- **未验证** ❌：① `present_field`（product 路径）**未被调用**；② **真实硬件**（非 QEMU）帧缓冲回读（WC 显存）；③ 3 处待授权同步的机制描述；④ 缺陷②的接线是否被 CI **持续**覆盖（当前仅 ⑫ 段 selfcheck 被断言）。

**CI 补记**：① v0.234（`fe40c9c`）`run 35480973651` **三 job 全绿** ⇒ **本轮新判据（P4 判红 ＋ 段号源扩到 boot 全部 `.rs`）已在 CI 上通过**；② v0.235（`ce77cc9`）`run 35481288838` **三 job 全绿**（test ✓2m2s｜元内核引导镜像 ✓2m5s｜宿主 field-render ✓5m13s）⇒ **报告本身亦过 CI**。复核命令（**run 号属环境派生值，R71：以命令实取为准**）：`gh run list --limit 3`｜`gh run view <id> --json jobs`。

**⑩ 决策评估（七要素）** —— 本轮新增 1 个决策点：**是否授权同步 CHARTER/TEMPLATES/ROADMAP 的机制描述与段号？**
| 要素 | 内容 |
|---|---|
| **评估** | 3 处与实现不一致：`CHARTER.md`（机制 25 行）写「**三条**／**六侧**／段号源 `verify.rs`」，实为「**四条**（含 P4）／**八侧**／**boot kernel 全部 `.rs`**」；`TEMPLATES.md`（T-032）写「六侧」且未含 P4；`ROADMAP.md:59` 活跃段号「⑪」（真值 ⑫） |
| **优势** | 消除**机制描述漂移**（R69／R72 家族）；机制 25 的"判据 ↔ 机制描述"重新一致 |
| **劣势** | 不改则留 3 处不一致（其中 ROADMAP 那处**当前被豁免 ⇒ 不会判红**，属隐性） |
| **风险** | 改治理文件需授权（**防擅自改框架** —— 本轮约束的立法意图）；改动面 = **3 处描述文本 ＋ 1 处段号**，**不动任何编号表** |
| **机会** | 把"**机制描述须随判据演进而同步**"固化为常规连带（与 §16.4「加段号 ⇒ 同步活跃段号陈述」同一先例） |
| **建议** | **授权一并同步**（成本 ≈ 5 分钟，可回滚，零编号表影响） |
| **等待决策** | 用户一句话（见 ⑪①） |

**⑪ 需要你决策**
① **是否授权同步 3 处机制描述**（`CHARTER.md` 机制 25 行 / `TEMPLATES.md` T-032 / `ROADMAP.md:59`）？
② **D-2 已转判红**：今后若判红，**优先"改文档"还是"改判据"**？（先定口径，免得现场争论）
③ **D-1 是否正式结项**？⚠️ **须先纠正一处上轮误述**（见 ⑦⑤）：**f64 侧「≤4 ULP」并未达标** —— 那条 `≤4 ULP` 是 **`f32` 口径**（`fmath.rs:18–20`），而 `sin_cos_f64_regression` 自述「门线 D29 ≤4 ULP ⇒ **未达标**」（`fmath.rs:1436–1452`）；本轮实测 `cos` 主区间 **maxULP = 10（> 4）**。**当前真正成立的只是**："**截断误差已到理论界内**"（maxAbs 1.110e-15 < 2.0038e-15）。⇒ 请裁：记为"**截断到界、ULP 未达标、暂不追**"？
④ **D-7 身份档案**：等用户给**名字/称呼**再办（本轮按指令暂缓）。

**⑫ 下一步建议**
1. **授权同步 3 文件**（最小、可回滚、消除漂移）；
2. **2.4 product 路径接线**：让 `present_field` 真正被调用（当前只有 selfcheck 被调，编译有 `never used` 警告）；
3. **2.4 × L6 集成**：呈现侧接入（底图 L6 呈现子功能 + `Project(S)`）；
4. 把"**边界层退化出口 ⇒ 判红/绿**"写成常驻自检（本轮是**人工反推**，尚未机器化）。

**⑬ 启动回执**：已读 `CHARTER.md`。版本 = `git rev-list --count HEAD`（**不手写**，C2）｜本次任务：push ＋ D-2 转判红 ＋ 缺陷③ 扩项｜**是否继续：等待裁定（⑪ 四项）**。

**⑭ 本轮读了底图哪几份、哪几行**（D55／D52：报告须写明）
- **根 `README.md` 全文 132 行**（状态/命名/版本规则/阶段路线/仓库结构）；
- **`docs/LAYER_BASEMAP_L0_L6.md`**：**行 34／37／47／528–542（L6 呈现＋对齐）／562／570–582／656**（呈现子功能、"呈现不增义"、L6-V1 依赖）；
- **`docs/LAYER_ARCHITECTURE.md`**：**行 92（§3.2 命名体系权威）／182–194（§4.L3 端点表 23 路径）／228（§4.L3.1 层归属）**；
- 脚手架 **`CHARTER.md` 全文 229 行**（**机制 25 = 行 100**；机制关系 = 行 102–109）；
- **`CONSTRAINTS.md` 全文 412 行**（**C18/C19 = 行 387–410 区域**）；
- **`TEMPLATES.md` 行 30–99**（十四项必带清单 = 行 32–60；表格编辑纪律/C15 = 行 62–76；第〇项 9 步块 = 行 97–118）；
- 台账 **`BASELINE.md` §十九（行 913–953）＋ §十五/§十六 段号落点（行 426／764／770）**；
- **判据工具 `coordination/tools/check_doc_consistency.py`**：全文（**改前 494 行**；本轮 **+155 / −46**）—— 重点 `machine_max_seg`／P4 段／`selftest` 六侧；
- **被测物**：`meta-kernel-boot/kernel/src/main.rs`（77 行，**⑫ 段接线 = 行 52–71**）／`present.rs`（222 行）／`verify.rs`（段号扫描）／`mem/{mod,global}.rs`（段号扫描，**0 处段号**）。

---

**一句话**：上一轮遗留 **6 个 commit 已 push**（`run 35475813727` 三 job 全绿）；**1.4 边界层在 QEMU 上"真验到"**（1280×720 不退化 ＋ 接线断言 ＋ 绿屏 ⇒ 只可能是回读一致）；**D-2 把机制 25 的 P4 转为判红**（自检八侧全过）；**缺陷③ 把段号源从单文件扩到 boot kernel 全部 `.rs`**（机器值 ⑪ → ⑫，判据当场抓出文档落后并同步）。**唯一待你点头的：要不要授权同步那 3 处机制描述？**
