【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 边界门控落地（`L77`→T-040／`L78`→T-041）＋ `ci.yml` 接线 ＋ 讨论稿入库（2026-09-21 · v0.306）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 305** ⇒ 收尾 **306**（本轮 **1 个本地提交** `b951229`）｜**label v0.306**（`label = count`，C21） |
| **HEAD／远端** | **已推 `f8adfd2..b951229`**（v0.306）⇒ 生成时 `HEAD = origin/main = b951229` |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 = 执行层 |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–41**（**新增 T-040／T-041**）｜D **1–44**（＋待核）｜R **1–89**（首格 50／唯一 53）｜TERM **1–13** |
| **本轮性质** | **裁定落地 ＋ 机制建设**（边界门控 2 条 ＋ A2 兜底 ＋ B2/C1 ＋ `ci.yml` 接线 ＋ 讨论稿入库） |

**③ 任务与约束**
- 基准 **count = 305**（指令填值，实测一致）｜总规划定位：**边界门控落地 ＋ `ci.yml` 接线 ＋ 讨论稿入库**。
- 约束：按 `CONSTRAINTS.md`｜**禁止内联含反引号／特殊字符命令**（全程脚本文件，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**按 T-038 门控**｜**按 C20** CI 结论不入库。
- ★ **顺序**：本轮起点 **ahead 0**（无待推 commit）⇒ 按 §〇「**本轮有新 commit 后**」执行 ⇒ **先做改动 → commit → push ＋ 门控**。

**④ 状态：完成**（**11 条验收全 ✅**，见 ⑤）。

---

## ⑤ 验收对照（用户 §九 · 11 条）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（T-038 门控） | ✅ `f8adfd2..b951229`；**按 T-038 门控**执行（阻塞至 `completed` 后取值）｜★ **job 级三态按 C20 只记当日日报、不入库** |
| 2 | **L77 引用完整性门控落地** | ✅ `T-040` ＋ `TEMPLATES §十七` ＋ **引用缺失首例自证**（见 §一） |
| 3 | **L78 落地，含关键约束三条** | ✅ `T-041` ＋ `TEMPLATES §十八` ＋ `scan_exclude.json`（同源／可见／成文） |
| 4 | **A2 豁免登记表落地**（兜底） | ✅ `BASELINE §47.3`（实表，含当前 1 条 `机制 28`） |
| 5 | **A4 明确反对** | ✅ `BASELINE §47.4`（**隐藏规则**，违 R73） |
| 6 | **B2 ＋ C1 落地** | ✅ `BASELINE §47.5`（执笔方列 ＋ 发起人复核 ＋ 每轮差集） |
| 7 | **`ci.yml` 接线（只告警）** | ✅ step 17 ＋ ★ **实测已执行**（见 §五） |
| 8 | **讨论稿入库** | ✅ `b951229` 含 `…_六族告警转正流程讨论稿.md` |
| 9 | **台账 §四十七 补记（含 v0.305）** | ✅ `BASELINE §四十七`（47.0 补记 v0.305 轮） |
| 10 | C15 复算通过、编号表连续无缺重 | ✅ **`T` 39 → 41**（+2）；机制／C／T 连续无缺重 |
| 11 | CI 全绿 | ✅（**C20 只记日报**） |

---

## §一 · **`L77` → `T-040`「引用完整性门控」**

### 1.1 规则（逐字落地于 `TEMPLATES.md §十七`）
> **凡一份文本中出现「选项标签」（如 `A/B/C`、`A1–A4`、`B1–B3`、`C1–C3` 等），必须在**同一份文本内**给出每个标签的**定义**。未定义 ⇒ **报「引用缺失」、列出缺失标签；不执行、不推测。**

- **指令侧**：模板新增「**引用定义表**」栏（凡出现选项标签须在此定义）。
- **返回侧**：**落笔前先检查**；有缺失 ⇒ **报告开头单列「引用缺失」清单**（标签 ＋ 位置）＋ 说明按何口径继续。

### 1.2 ★ **首例自证（本轮的引用缺失 —— 如实报，不推测）**
| 项 | 内容 |
|---|---|
| **缺失标签** | **`L77`**、**`L78`**（本轮指令**通篇使用**） |
| **为何缺失** | 本仓库**无「L 编号表」**；且 **`L0–L10` 已被「层级」占用**（`docs/LAYER_ARCHITECTURE.md`）⇒ 二者**无法与既有 L 体系并存** ⇒ **属未定义** |
| **处置** | ① **如实报缺失**；② **不推测其编号归属**；③ 按指令**自己给出的载体**（"写入 `TEMPLATES.md`、补 T 编号"）落地为 **`T-040`／`T-041`**；④ **标注"编号归属待确认"**（⑪-①） |
| **意义** | **本条规则"上线即自证"** —— 首个适用案例就是"上线它的那条指令" |

---

## §二 · **`L78` → `T-041`「告警扫描范围限定」**

### 2.1 规则与三条关键约束（逐字落地于 `TEMPLATES.md §十八`）
**告警检测前，先剔除 5 层文本**：

| 层 | 剔除内容（标记词） | 状态 |
|---|---|---|
| 1 | 元标注块（`【可信度】`／`【全景摘要】`／`【顾问阅读指令】`） | 已有 |
| 2 | 待裁定段（`待裁定`／`待裁项`／`待你裁定`／`待判`） | 已有 |
| 3 | 问答段（`问答段`／`Q&A`／`问：`／`答：`） | 已有 |
| 4 | **举例/引用段**（`例如`／`举例`／`示例`／`可约定`／`之类`／`比如`） | **新增** |
| 5 | **讨论段**（`讨论稿`／`讨论`／`议题`／`待确认`） | **新增** |

**★ 三条关键约束（防"隐藏规则" · R73 教训）**：
1. **清单入机器可读 json** ⇒ **新建 `coordination/tools/scan_exclude.json`**（**与元标注块同源**；**不写死在脚本里**）。
2. **每次扫描打印「已剔除段落」** ⇒ 逐层输出**行数 ＋ 示例行号**（**可见**）。
3. **剔除规则本身是正式规则** ⇒ `TEMPLATES.md §十八`（**可见、可审**）。

### 2.2 ★ 实测（v0.306 轮）
| 项 | 值 |
|---|---|
| **剔除行数** | **782 行**（L2 待裁定 **226**｜L3 问答 **14**｜L4 举例引用 **130**｜L5 讨论 **412**） |
| **引用集** | 56 129 → **55 347 行**（★ **登记集不剔除**） |
| **告警变化** | **2 → 1**：`99` 被剔（举例文字）；**`28` 仍在** ⇒ **交 A2 兜底**（§三） |
| **自检** | `--selftest` **十一侧全 PASS**（原八侧 ＋ ⑨ 举例段被剔 ⇒ 不告警／⑩ **真引用不被误剔** ⇒ 仍告警／⑪ 清单可读 ≥5 层） |

> ⇒ **完整演示"治本（T-041）＋ 兜底（A2）"的分工**：T-041 消掉 1 条，剩余 1 条由 A2 登记。

---

## §三 · **A2 豁免登记表（兜底）** ＋ **A4 明确反对**

### 3.1 A2 登记表（落点 `BASELINE §47.3`）
| 编号 | 告警内容 | 来源（文件:行号） | 类型 | 登记日期 | 复核条件 |
|---|---|---|---|---|---|
| **`机制 28`** | 「机制 28」被引用，而 `CHARTER.md`「## 三、全部机制」区段内无 `\| 28 \|` 登记行 | `coordination/reports/2026-09-21_MECH族专用解析评估稿.md` **L103**（举例文字） | **自指污染**（非真缺口） | 2026-09-21 | ① 该报告被删／改写；② T-041 剔除层扩容覆盖该写法；③ 告警**连续 N=5 轮为 0** |

- **铁律**：本表**仅登记"经复核确认为自指污染"的告警**；**真缺口一律不得入表**（入表＝放过）。

### 3.2 A4 明确反对（`BASELINE §47.4`）
- **A4 ＝「判据侧自动识别示例」** ⇒ **典型隐藏规则**，**违背 R73**（"隐藏规则削弱可信度"）⇒ **不引入**。
- **替代**：**T-041 的"可见剔除"**（三约束齐备）＋ **A2 的"显式登记"**。

---

## §四 · **B2 ＋ C1**（落点 `BASELINE §47.5`）

| 条 | 内容 | 载体 |
|---|---|---|
| **B2** | **执笔方每轮报告列出「本轮新增告警」**（含来源溯源）；**发起人复核**确认是否真缺口 | **报告 ⑦ 节既有载体**（不新增栏目） |
| **C1** | **每轮报告必须含「本轮告警列表 ＋ 与上轮的差集」** | 报告 ⑦ 节固定一行「六族告警：本轮 X 条｜对比上轮 ±N」 |

> ★ **本节即首次执行**：见 **⑦ 真实缺陷** 的「六族告警」行。

---

## §五 · **`ci.yml` 接线（只告警）** —— 逐字对照 ＋ **实测执行**

### 5.1 逐字对照
| | 内容 |
|---|---|
| **改前** | 机制 25 step（`test` job，原 **L331–344**）之后**直接**接「机制 18 / C13」注释块（原 **L346**） |
| **改后** | 二者之间**插入**新 step（现为 `test` job 的 **step 17**）：<br>`- name: 编号集差集门禁（六族 · 只告警）`<br>`run:` ⇒ `set +e` → `check_id_set_diff.py --selftest`（`SELFTEST=$?`）→ `check_id_set_diff.py`（`CHECK=$?`）→ `echo` → `set -e` → `test "$SELFTEST" -eq 0 && test "$CHECK" -eq 0` |
| **"只告警"如何实现** | **不传 `--strict`** ⇒ 判据退出码**恒 0** ⇒ `CHECK` 恒 0（**不阻断 CI**）；`SELFTEST` **仍当门**（工具自身坏了须判红） |
| **YAML 校验** | **pyyaml 解析通过**（`jobs = test／boot-image／host-windows`） |

### 5.2 ★ 实测 —「接线生效」的结构性证据（R89：「声明」≠「发生」）
- 项目范式：**「函数存在 ≠ 被调用」** ⇒ 仅"写进 `ci.yml`"**不足以**证明它跑了。
- **实测（按 step 序列，非"结论"）**：`test` job 中 —— step 18＝**机制 25 文档一致性门禁** → **step 19＝「编号集差集门禁（六族 · 只告警）」** → step 20＝机制 18 顾问阅读指令门禁。
- ⇒ **新 step 已实际执行**（**不是"写了没跑"**）。
- **CI 三 job 三态按 C20 只记当日日报、不入库。**

---

## §六 · **讨论稿入库**
- `coordination/reports/2026-09-21_六族告警转正流程讨论稿.md`（226 行；**v0.305 轮只出稿产物**）随 `b951229` 入库。

---

## ⑥ 未完成项
- **无本轮遗留**。**三项待裁**见 ⑪。

## ⑦ 真实缺陷
- **0 缺陷**。**2 条过程观察**：
  1. **★ 引用缺失首例（正向）**：见 §一 1.2 —— **新规则上线即自证**（首个案例即"上线它的指令"）。
  2. **★ B2／C1 首次执行（本行为首次）**：

| 六族告警（本轮） | 对比上轮 |
|---|---|
| **1 条**：`MECH` 族 **`28`**（自指污染｜来源 `MECH族专用解析评估稿.md:103`） | **± −1**（上轮 2 条：`28`／`99`；**`99` 已由 T-041 剔除**） |

> 其余五族 **R／D／C／T／TERM 全 0**（无新增）。**A2 表**已登记 `28`（§三 3.1）。

## ⑧ 三量台账（四量）
| 量 | 内容 |
|---|---|
| **存量** | 六族判据（301 行）＋ 观察窗口口径（N=5）＋ 台账 §四十四–§四十六＋ 讨论稿（讨论稿已入库） |
| **变量1**（需求） | 本轮指令：**边界门控 2 条**（引用完整性／扫描范围限定）＋ **A2 兜底**＋ **B2+C1**＋ **`ci.yml` 接线**＋ **讨论稿入库** |
| **补充增量1**（答案1） | **T-040／T-041**（规则成文）＋ **`scan_exclude.json`**（可见剔除清单）＋ **判据剔除层**（十一侧自检）＋ **`ci.yml` step 17** ＋ **A2 实表** |
| **创新增量** | 存量 ＋ 变量1 ＋ 补充增量1 **融化融合化合** ⇒ **"判据被自己的结论污染"这一同族问题（R82／C20／R89）第一次有了"可见＋可审＋有兜底"的完整处置链**：**T-041 治本 → A2 兜底 → B2/C1 每轮复核 → CI 长期采集数据** |

## ⑨ 边界说明（四类）
| 类 | 内容 |
|---|---|
| **本地跑过** | 判据 `--selftest` **十一侧全 PASS**｜**实跑**（剔除 782 行；告警 1）｜**C15 双向**（`T` 39→41，依据 `git show HEAD` 版 vs 工作区）｜**pyyaml 语法校验**（`ci.yml`）｜全量判据 **9/9 rc=0**｜判据 F **PASS** |
| **CI 过** | ✅ **已按 T-038 门控**等到 `completed`；★ **新 step 已实测执行**（step 序列见 §五 5.2）｜**三态按 C20 只记当日日报** |
| **真实跑过** | —（本轮为规则／判据／CI 接线，无真机或硬件面） |
| **未验证** | **编号归属**（`L77`／`L78` → `T-040`／`T-041`）**待你确认**（⑪-①）；**转判红**（`--strict`）**未启用** |

## ⑩ 决策评估（C11 七要素）—— 针对「**编号归属：维持 T 族 vs 新建 L 族**」
| 要素 | 内容 |
|---|---|
| **评估** | 本轮两条规则的原始标签为 `L77`／`L78`，仓库无 L 表 ⇒ 已**暂记** `T-040`／`T-041` |
| **优势** | **维持 T 族**：零新体系、与 §7.2 变更程序一致、`T-039` 之后自然续号；**新建 L 族**：与指令原标签字面对齐 |
| **劣势** | **维持 T 族**：与指令字面标签不同名（需在台账留"原名"备注）；**新建 L 族**：须**新定义一族**（范围／维护责任／编号规则），且 **`L0–L10` 已被层级占用** ⇒ **须避让**，回归面大 |
| **风险** | **新建 L 族** ⇒ 与既有 **L0–L10「层级」**混淆风险高（本仓库"L"已是层级语义）；若命名不当，后续引用会**歧义** |
| **机会** | 借此**明确编号族的边界规则**（`L` 保留给"层级"、流程规则归 `T`）⇒ 可写入 `TEMPLATES §7.2` 备注 |
| **建议** | **维持 T 族**（`T-040`／`T-041`）；**并在台账保留"原文标签 `L77`／`L78`"**（已做）⇒ 两边可检索 |
| **等待决策** | 见 ⑪-① |

## ⑪ 本轮待判
| # | 项 | 推荐 |
|---|---|---|
| **①** | ★ **编号归属**：`L77`／`L78` **维持 T 族**（`T-040`／`T-041`）还是**新建 L 族** | **维持 T 族**（`L0–L10` 已占层级语义；新建 L 族回归面大） |
| **②** | **`ci.yml` 的"只告警"是否维持** | **维持**（不传 `--strict`；待告警稳定后再议转红） |
| **③** | **A2 表的维护责任**是否即按 **B2＋C1** | **是** |

## ⑫ 下一步建议
1. **首选**：确认 ⑪-①（编号归属）—— 其余两项可一并给。
2. **观察**：**CI 已接线**（只告警）⇒ 观察窗口**开始连续采集**；每轮报告 ⑦ 节给出**告警 ＋ 差集**（**B2/C1 已生效**）。
3. **暂缓**：`docs` 批次与 `advisor_brief` 复扫**均为触发式**（本轮未触发 ⇒ 未跑，合规）。

## ⑬ 启动回执
> 已读 `CHARTER.md`。当前状态：**count = 305**（基准）⇒ 收尾 **306**｜阶段二 · **2.4 边界层已验**｜本次任务：**边界门控落地（`L77`／`L78`）＋ `ci.yml` 接线 ＋ 讨论稿入库**｜是否继续：**执行完毕**。

## ⑭ 本轮读了底图哪几份、哪几行
| 文件 | 行 | 用途 |
|---|---|---|
| `.github/workflows/ci.yml` | **L328–352**（机制 25 step 区） | 确认**接线位置**（紧随机制 25）与既有编排习惯；插入后**pyyaml 校验**、`test` job step 序列实测 |
| `coordination/TEMPLATES.md` | **L884–918**（§十六 行号引用格式 · T-039 节）＋ **L355**（§7.2 `T-039` 行） | 对齐新节写法与 §7.2 变更记录格式（新增 §十七／§十八 ＋ `T-040`／`T-041`） |
| `coordination/tools/check_id_set_diff.py` | **全 301 行**（改前）→ **393 行**（改后） | 落地剔除层（`load_exclude`／`apply_exclude` ＋ main 集成 ＋ 自检 ⑨⑩⑪） |
| `coordination/BASELINE.md` | **L3284–3290**（§46.7 尾部锚点）；**L3236–3239**（§45.6 待裁 ③ · `99` 的来源） | 台账续写定位；**告警 `28`／`99` 的来源溯源** |
| `coordination/reports/2026-09-21_MECH族专用解析评估稿.md` | **L103** | **A2 表首个登记条目的来源**（`机制 28` 举例文字） |
| `coordination/reports/2026-09-21_六族告警转正流程讨论稿.md` | 全篇（226 行） | 本轮落地对象（其 §六 待讨论项 → A2／B2／C1 的裁定依据） |

> ★ **本稿未改 `docs/` 任何文件**（**无底图改动**）⇒ **判据 F 无需更新、汇编无需重生成**（实测判据 F **PASS**，仍 36 份）。
