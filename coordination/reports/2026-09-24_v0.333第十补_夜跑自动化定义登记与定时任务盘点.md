【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

> **版本口径（★ 依 `T-045`：报告内版本号 ＝ 报告「首次提交」时的 `count`）**：本报告正文内的 **`410`** 为**首次提交时点值**，**后续订正提交不回改此处**。
> **后续任一轮若要取本轮「最终」的 `count`**：请以 `git rev-list --count <本轮最后一个 commit>` **实跑为准**，**不要引用本报告正文的静态数字**（因其为时点值）。

# v0.333 第十补轮 · 夜跑／自动化**定义登记（机制 32）** ＋ 3 条定时任务**盘点** ＋ 6 项待裁落地 ＋ push **受阻**（父链结构冲突）

> **轮次**：`v0.333`（两口径并存：版本＝`count`）｜**版本 `count = 407`（开工）→ `410`（提交后实时值）**
> **指令序号**：`云内核-C3-012`（下一轮 `云内核-C3-013`）
> **本轮报告落点**：`coordination/reports/2026-09-24_v0.333第十补_夜跑自动化定义登记与定时任务盘点.md`
> **任务类型**：**定义登记 ＋ 盘点**（单一主类型）

---

## ① 保密标记（`C12`）

- **本链**：`C3` ｜ **接收方＝外部顾问** ｜ **可见级别上限 🔒 半公开**（唯一权威：`coordination/security/chain_visibility.md §一`）
- **本轮 ⛔ 清单（六类）**：本机绝对路径 ／ 账号·口令 ／ 内网 IP ／ 端口＋可定位目标 ／ 密钥与 token ／ 二进制路径与体积
- **拷前屏蔽提示**：本报告已按上表过滤 —— **无**本机绝对路径、**无**端口号＋可定位目标组合、**无**内网 IP、**无**密钥与路径具值；出现的均为**相对路径**（🔒 可拷）与**机制编号**（🔓 可直接拷）。
- **本报告自检**：以上六类在本报告中均 **0 命中**（已通读核对；`check_chain_visibility` 默认口径实跑结果＝**告警 0**）。

## ② 全景状态卡

| 字段 | 值 |
|---|---|
| **版本 `count`** | **407**（开工）→ **410**（本轮 3 组提交后） |
| **HEAD** | `eeea4dd`（开工）→ **`57edbc2`**（本轮提交后） |
| **`origin/main`** | **`e0fa133`**（**本轮未推送**；原因见 §④ 与 §⑩/⑪ —— **push 指令不可执行**） |
| **未推送 commit** | **共 11 个** ＝ **夜跑 4 个**（`ffae676`／`b30cc6a`／`236775f`／`d197acd`）＋ `C3-011` 的 4 个（`fa53223`／`39cc9f1`／`5ef2003`／`eeea4dd`）＋ **本轮 3 组**（`82cc3e3`／`bd04e3d`／`57edbc2`） |
| **CI** | **本轮无新 run**（未 push）。最近一轮绿＝`e0fa133`（=`origin/main`），三 job 全 success；**结论只记日报，不入库**（`C20`） |
| **阶段定位（两口径并存）** | **README 四阶段**（能力刻度）／**ROADMAP 三阶段**（交付刻度）—— **本轮未推进任何阶段刻度**，属**治理/定义层**改动 |
| **`T` 编号** | **72 → 74**（`＋2`：`T-073`／`T-074`） |
| **机制编号** | **31 → 32**（`＋1`：机制 32 夜跑与自动化区分机制） |
| **判据脚本** | **16 个**（总数不变；本轮**改内部**：`check_rewrite_pipeline`／`check_chain_visibility`） |
| **判据自检侧数** | `check_chain_visibility` **17 → 21**；`check_rewrite_pipeline` **8 → 9** |
| **待决策** | **2 组**：**A 组 push 父链冲突**（§⑩）／**B 组定时任务是否按新定义调整**（§⑪） |

## ③ 任务（含基准与总规划定位）

- **基准**：上一轮 `云内核-C3-011`；`count = 407`（核实一致）。
- **总规划定位（两口径）**：本轮属**治理层的定义登记与盘点**，**不改变**任何工程阶段刻度（README 四阶段／ROADMAP 三阶段均不动）。
- **优先级拆分**：
  - **① 定义登记（P0）** —— 把「夜跑／自动化」写入治理（机制 32 ＋ `T-073`）。
  - **② 按新定义盘点 3 条定时任务（P0）** —— 21:00 清单／21:05 蒸馏／23:00 夜间 P3（**只盘点、不调整**）。
  - **③ 6 项待裁落地（P1）** —— `BASELINE §78.8` 六项逐项落地。
  - **④ push 本轮 `C3-011` 的 4 个 commit（P1）** —— **不可执行**（父链结构冲突，见 §④／§⑩）。

## ④ 状态（**部分完成** —— ①②③ 完成 ／ ④ push 受阻）

1. **① 定义登记 —— ✅ 完成**（`T-073` ＋ `TEMPLATES.md §二十四` ＋ `CHARTER.md` 机制 32 ＋ 记忆内容层 `ironlaws.md` 铁律⑪）。
2. **② 3 条定时任务盘点 —— ✅ 完成**（表见文末附加表三；**未调整任何任务配置**）。
3. **③ 6 项待裁 —— ✅ 完成**（逐项见 §⑤-③；其中 ① 带来**实测效果**：`check_rewrite_pipeline` 告警 **2 → 0**）。
4. **④ push —— ❌ 未执行且不可执行**：**`fa53223` 的父提交就是夜跑最后一个 commit `d197acd`**（`git rev-parse fa53223^` 实测），**夜跑 4 个 commit 是本轮 4 个的祖先** ⇒ **push 任一个后代都必然连带推送夜跑 4 个**；指令禁区⑥明文禁推夜跑 4 个 ⇒ **两者互斥，无法同时满足**（详见 §⑩ 决策评估）。**我没有擅自采取任何绕过手段**（不 rebase、不 cherry-pick、不改历史）。

## ⑤ 验收对照（标准 vs 实测）

| # | 验收标准 | 实测 | 结论 |
|---|---|---|---|
| 1 | 定义登记完成，走 `T-049`，机制编号已登记 | `T-073` 已入 `TEMPLATES §7.2`（先登记后引用）→ 机制 **32** 已入 `CHARTER §三`（含定义逐字／特征／分界线／红线）；`TEMPLATES §二十四` 为条文正文 | ✅ |
| 2 | 3 条定时任务盘点表出，逐条归类，不符者列调整建议 | 见附加表三；**三条均归类**（夜跑×2 ／ 自动化×1）；**不符者（23:00）列了三选一建议** | ✅ |
| 3 | 6 项待裁逐项落地并登记 | `T-074` ＋ §⑤-③ 逐项；①⑥ 落判据 ＋ 自检扩侧；② 落 `'已减'阈值`；③ 落 `known_accepted A-003`；④ 落 `CHARTER` 机制 19 加注；⑤ 登记保留 | ✅ |
| 4 | push 完成，CI 三 job 全绿；`origin/main` 回读 | **未达成（受阻）**：父链结构冲突（见 ④·4 与 §⑩）；**未推送 ⇒ 无新 CI run**；`origin/main` 已回读仍为 `e0fa133` | ❌ **受阻（待裁定）** |
| 5 | 夜跑 4 个 commit 未 push，登记待处置 | **未 push**（事实上也无法在不推本轮 4 个的前提下单独推）；待处置登记见 §⑥-① | ✅（按当前状态成立） |
| 6 | 引用编号全部带登记状态 | `T-073`／`T-074` 均已登记并标状态；`机制 32` 标 ✅ 生效；`A-003` 已登记 | ✅ |
| 7 | `C15` 复算通过；判据 **16/16 PASS** | `T` 72→74（＋2）｜机制 31→32（＋1）／`C` 21／`TERM` 14／tools 16／底图 44 均 ±0｜`reports` 187→188（＋1）／`BASELINE` 顶层节 117→118（＋1）｜`known_accepted` 2→3 条（＋1）；**判据 16/16 rc=0** | ✅ |

## ⑥ 未完成项

| # | 未完成 | 原因 | 下一步 |
|---|---|---|---|
| ① | **push（`P1④`）** | **父链结构冲突**：本轮 4 个 commit 的祖先链里**包含**夜跑 4 个 ⇒ "只推 4 个、不推 4 个"在 git 语义上不可实现（除非重写历史，被 `C19`／多条禁令排除） | **等用户裁定**（§⑩ 三选项；荐**甲·暂不推**） |
| ② | **夜跑 4 个 commit 的最终处置** | 同上；且**须先立新定义后的处置规则**（机制 32 已登记，**适用面待裁**） | 与 ① 合并裁定（同一道题的两半） |
| ③ | **3 条定时任务是否按新定义调整** | 指令禁区⑧：**只盘点、不调整** | 调整建议已出（附加表三），**待用户裁定后执行** |
| ④ | **基线重写阈值（`DECREASE_REWRITE_THRESHOLD`）现值 10** | 用户裁定②**只给了原则（"已减"累积超阈值才重写）、未给数** ⇒ 我**不自定为准**（`T-067`） | 标 🟡 **待用户确认**（10 为临时占位，可一句话改） |
| ⑤ | **「已减提示」是否也要进 CI `annotations`** | 本轮未改 `ci.yml`（无可验证入口：未 push ⇒ 改动无法在 CI 上取证） | 并入下轮 push 一并处理 |

## ⑦ 真实缺陷（本轮自捕获 3 条）

| # | 缺陷 | 严重度 | 处置 |
|---|---|---|---|
| ① | ★★ **push 指令不可执行（父链冲突）** —— 与本轮禁区⑥互斥：`fa53223^ = d197acd`，夜跑 4 commit 是其祖先；**不含 history rewrite 的手段都不可能"只推后代不推祖先"** | **P1**（抬高为待裁，**非本轮遗漏**） | **未擅自动手**；**登记 ＋ 出三选项 ＋ 荐甲**（§⑩） |
| ② | ★ **`check_rewrite_pipeline` 近似面**（承 `§78.6-②`）：`D16'` 单句引述即被判「半冻」 | **P2** | **已修**（裁定①收窄**：只认本节含「解冻」**）⇒ 告警 **2 → 0**；**自检 8→9 侧**（侧⑨ 反例守住该口径） |
| ③ | ★ **`0.0.0.0` 在「内网 IP」规则上无明文、无对照**（只在 port 规则里排除过） | **P2** | **已修**（裁定⑥：IP 规则显式排除 ＋ 补正/反两侧对照 ⑳㉑）;属"**未验证的绿**"修补（`R83`） |

## ⑧ 三量台账（**四量**）

- **存量（已在治理内）**：机制 1–31；`T-001`–`T-072`；`C1`–`C21`；`TERM-001`–`014`；判据脚本 16 个；基线件 4 份（`leak_baseline.txt`／`judge_gaps_baseline.txt`／`chain_visibility_baseline.txt`／`reading_directive_exempt.txt`）。
- **变量（本轮被改）**：`TEMPLATES.md`（`T-073`／`T-074` ＋ **§二十四** 新增）／`CHARTER.md`（**机制 32** 新增 ＋ **关系块** ＋ **机制 19 `D32` 加注**）／`tools/check_rewrite_pipeline.py`（近似面收窄，自检 8→9）／`tools/check_chain_visibility.py`（IP 规则明文 ＋ 基线阈值 ＋ 自检 17→21）／`security/known_accepted.md`（`A-003`）。
- **补充增量（既有能力的说服力延伸）**：`T-072` 的"只减不增基线"从「**只能看新增**」扩到「**也能说什么时候该重写**」（"已减"阈值）—— 这是同一机制上的**第二个改进点**。
- **创新增量（本轮新增）**：**机制 32「夜跑与自动化区分机制」** ＝ **首次把"需不需要人确认"写成条文（定义／关键特征／分界线）并落到**红线****（填补机制 19 从未定义"要不要人点头"的空洞）＋ **「改仓库状态 ⇒ 恒为夜跑」红线**（把 `C10` 落到定时任务场景的可执行判别式）。

## ⑨ 边界说明（**四类**）

- **本地跑过（M 层）**：全部 16 判据在本机实跑（`rc=0`）；两个改动脚本的 `--selftest`（9 侧／21 侧）均 PASS；`--baseline` 比对（**新增 0 ／ 已减 0 ／ 不达阈值不重写**）；编号连续性人工复核（`T-001`–`T-074` 无缺号、无重号）。
- **CI 过**：**无**（本轮未 push ⇒ **无任何新的 CI run**）；最近一轮绿仍覆盖 `e0fa133`。
- **真实跑过**：**无**（本轮无代码/内核/引导改动，无可真跑对象）。
- **未验证**：① push 相关的所有结论（**无 CI 取证**，因本轮未推送）；② `ci.yml` 是否要加"已减"告警行（未改）；③ 阈值 10 的适用性（未经用户确认）；④ **新定义对历史 8 夜夜跑的追溯归类**（本轮只盘点当前 3 条配置，历史不追改，`C19`）。

## ⑩ 决策评估（**七要素** · 因 §④ push 受阻而触发）

> **议题**：本轮 4 个 commit 与夜跑 4 个 commit **在同一条祖先链上**，"只推前者不推后者"无法实现，怎么办？

- **评估**：`git rev-parse fa53223^` ＝ `d197acd`（夜跑 P3-3 订正）⇒ **本轮 4 个全部位于夜跑 4 个之上**；git 推送必然包含全部缺失祖先，且**不容许"跳过祖先"**。
- **优势（甲·暂不推）**：完全合规（不动历史、不越禁区⑥）；把"夜跑产物要不要进公开仓库"这道**真正的问题**留给用户裁，**不被技术惯性带过**。
- **劣势（甲）**：本轮 C3-011／C3-012 的改动**继续留在本地**（**已达 11 个未推 commit**），**CI 杠铃失效**（本机绿 ≠ CI 绿，机制 26）。
- **风险（甲）**：未推积压越多，将来一次性 push 越难定位问题；**风险（乙·全推）**＝夜跑产物未经 expense 确认即进公开仓库，违反 `D33` 精神与机制 19「默认不推送」；**风险（丙·历史重写）**＝产生重复 commit、破坏 `C19` 与溯源链，**排除**。
- **机会**：借这次冲突，正好把**机制 32 的"夜晚产物处置规则"一次性定死**（草稿属性 ⇒ 默认何去何从），后续夜跑**自动合规**。
- **建议**：**甲 · 暂不 push**，同一句一并裁定"夜跑 4 个要不要放行"（我荐：若用户认其产物可接受 ⇒ 一句话放行**全部 8 个**；若不可接受 ⇒ 永久不推、仅在本地作夜跑存档）。
- **等待决策**：**用户三选一（甲／乙）；丙已排除。**

## ⑪ 需要用户决策（2 组）

| 组 | 事项 | 选项 | 推荐 |
|---|---|---|---|
| **A** | **push 父链冲突**（§⑩） | **甲** 暂不推（合规）／**乙** 放行推全部（含夜跑 4 个）／~~丙 历史重写~~（已排除） | **甲**（除非用户认可夜跑产物 ⇒ 可立刻转乙） |
| **B** | **3 条定时任务是否按新定义调整**（附加表三） | 维持现状 ／ 采纳建议②／③ ／ 只调整其中一条 | **只调整 23:00 那条（建议②：改为"出补丁不 commit"或③：标注草稿待确认）**，21:00 与 21:05 维持 |

## ⑫ 下一步建议（本轮不执行）

1. **先裁 A 组**（push 与否）—— 阻塞所有后续 CI 取证。
2. **再裁 B 组**（定时任务）—— 定完即可按机制 32 改写任务配置（届时**走应急 Marche ⇒ 单独一轮执行**，不在本轮）。
3. **确认阈值 10**（`T-074` ②）—— 一句话即可改。
4. 下轮若 push 放行 ⇒ 一并处置"已减提示是否入 `annotations`"＋读回验证（沿用 `C3-011` 已通的未认证读 `annotations` 通道）。

## ⑬ 启动回执

> 已读 `CHARTER.md`；版本 `count`：开工 **407** ／ 本轮提交后 **410**（取自 `git rev-list --count HEAD`，**非手写**）；阶段口径＝**README 四阶段 ／ ROADMAP 三阶段 并存**；指令序号 `云内核-C3-012`（下一轮 `云内核-C3-013`）。

---

# 附加表（依 `T-042`：位置在十四项之后）

## 附加表一 · **定时任务盘点表（P0② · 主交付）**

> **判据口径（机制 32）**：**① 先判"改不改仓库状态"（改 ⇒ 恒为夜跑·红线优先）→ ② 不改 ⇒ 再判"需不需要人确认"（需 ⇒ 夜跑／不需 ⇒ 自动化）**。
> **边界**：**只读盘点**（读取平台任务配置 ＋ 其 prompt 原文）；**未新增／未修改／未暂停／未删除任何定时任务**（禁区⑧ ＋ `C10`）。

| 项目 | **A · 21:00 清单** | **B · 21:05 蒸馏** | **C · 23:00 夜间 P3** |
|---|---|---|---|
| **① 任务名／触发时间** | 云内核·今夜 P3 清单｜每日 **21:00** | meta-kernel 每日快照蒸馏｜每日 **21:05** | 云内核·夜间 P3 自动开工｜每日 **23:00** |
| **② 执行内容（做什么）** | 读 `CHARTER`／`CONSTRAINTS`／`TEMPLATES §三·§十一`／`BASELINE` 未完成项／`ROADMAP`「可夜间跑」列 → 取 P3 且可夜间跑项 → **写入 `coordination/reports/<日期>_今夜P3清单.md`** → 汇报 6 行 | 读记忆目录最新日志末 30 行 → 在权威仓库执行 3 条只读命令取 `count`／`HEAD`／当日提交 → **更新 `MEMORY.md` 与 `CONTINUITY.md`** → 无进展则只输出 `no-change` | 读**今日**清单（无则从 `ROADMAP`「可夜间跑」中自择 1–3 项）→ 逐项执行 P3 白名单 → **每项单独 git commit**（前缀 `夜间 P3-n：`）→ 每项写 `reports/` 证据记录 → 出当夜 6 行汇报 |
| **③ 是否改仓库状态** | **是** —— 写入 `reports/`（**在 `git` 跟踪范围内**；同日覆盖） | **否** —— 只写**仓库外**记忆文件（`.workbuddy/memory/`／`CONTINUITY.md` 均在仓库外的记忆目录），**不 commit、不改任何被跟踪文件** | **是** —— **本地 commit（多篇：昨夜实际 4 个）** ＋ 写 `reports/` |
| **④ 是否出草稿** | **是** —— prompt 明文写「**你可在 21:00–23:00 内调整；无异议则 23:00 自动开工**」＝ **草稿待调整** | **否** —— 直接改定，无草稿态 | **部分** —— 产出物是**已落地的 commit**（非草稿），但**自陈"未推送、待用户次日一句话放行"** ＝ **确认环节被后置** |
| **⑤ 是否需要人确认** | **是**（明确请人调整） | **否**（prompt 无任何确认环节） | **是**（明写"需用户目验／放行项"） |
| **⑥ 按新定义归类** | **夜跑** ✅（**双理由**：改仓库状态触红线 ＋ 出草稿等人确认） | **自动化** ✅（三特征全中：无指令触发／无需确认／直接完成；**且不触红线**：只写仓库外） | **夜跑** ✅（**红线理由**：改仓库状态 ⇒ 恒为夜跑；**且确实要人放行**） |
| **⑦ 若不符 ⇒ 调整建议** | 符合夜跑，**唯一缝隙**：文件虽是草稿、却**未自我标注"草稿待确认"** ⇒ 建议**在清单头部加一行「草稿待确认（21:00–23:00 可调整）」**（或不改，由用户裁） | **唯一完全符合「自动化」定义者** ⇒ **建议维持现状** | ★ **与夜跑的定义存在真实落差**：夜跑＝「先出草稿、白天再确认」，而本任务是「**先落 commit、再等人放行 push**」⇒ **确认被后置**。三条建议（荐②或③）：**① 维持现状**（保留"便于回退"的本地 commit，确认后置）／**② 改草稿化**（改为产出 diff／补丁件、**不 commit**，白天确认后再落地）／**③ 折中**（保留本地 commit，但在 commit 消息与报告明确标注「**草稿待确认**」，使属性可见） |

> **盘点口径说明（如实）**：归类依据＝**各任务的 prompt 原文**（平台任务配置，已逐字读取）+ **产出事实**（昨夜 4 个 commit 已实测）；**未改任何配置、未调整任何任务**。
> **连带事实**：昨夜（09-24）21:00 清单缺件 ⇒ 23:00 任务**按回退路径**自择 P3 项运行（与 09-18 首夜先例一致）；该缺件现象已由 `T-074` ③登记为 **`A-003` 已知·已裁定接受（偶发）**。

## 附加表二 · **6 项待裁落地逐项（P1③）**

| # | 裁定 | 落地位置（可查） | 机器证据 |
|---|---|---|---|
| ① | 回写流水线近似面：**`D16'` 仅同段有「解冻」字样才受检** | `tools/check_rewrite_pipeline.py`（`pick_target` ＋ 半冻检测 ＋ 头注口径 2 留痕） | 自检 **8→9 侧**（侧⑨ 反例）；**实跑告警 2 → 0** |
| ② | 基线重写周期：**仅"已减"累积超阈值时重写**（**不定周期**） | `tools/check_chain_visibility.py`（`DECREASE_REWRITE_THRESHOLD` ＋ `decrease_advice()` ＋ `--baseline` 输出新增两行） | 自检新增 **⑱⑲ 两侧**；`--baseline` 实测：**新增 0 ／ 已减 0 ／ 不达阈值 ⇒ 不提示重写**（阈值现值 **10** ＝ 🟡 待确认） |
| ③ | 夜跑缺件：**接受偶发**（三解已结清） | `security/known_accepted.md` **`A-003`** 行（按 `#` 递增插入，原 `A-001`／`A-002` 不动） | 登记表 2→3 条；**此前三解证据件已在 `reports/`** |
| ④ | 机制 19 加 `D32` 注：**最小改动加注** | `CHARTER.md` 机制 19 行**末追加一句**（指向 `D32`「暂不创建·非漏做」＋ 再评估条件＋`BASELINE §五` 出处） | **只增行、不改原句**（`C19`）；`check_source_of_truth` PASS |
| ⑤ | 4 个已消失任务记忆残件：**保留** | `TEMPLATES.md` `T-074` ⑤ ＋ 本报告 §⑧ 存量说明；**不删除任何目录** | 历史登记的 4 个 ID 目录**原样保留**（无删除动作） |
| ⑥ | `0.0.0.0`：**与回环同逻辑 ⇒ 排除** | `tools/check_chain_visibility.py` 的 `LAN_IP` 规则（负向前看排除）＋ 头注明文理由 | 自检新增 **⑳（反例）／㉑（正例对照）**；**动态拼接夹具** ⇒ `check_private_pointers` 仍 PASS（未触铁律⑦） |

## 附加表三 · `INTEL` 状态表（机制 29 动作①）

| 编号 | 状态 | 本轮是否触发 | 结论 |
|---|---|---|---|
| `INTEL-001` | 观察中 | 否（企业注册 ＋ 阶段三 均未达） | 维持，不虚触 |
| `INTEL-002` | 观察中 | 否（L7–L10 未落地） | 维持，不虚触 |

## 附加表四 · 本轮读底图／治理件（交代"读了哪几份、哪几行"）

| 文件 | 读了什么（节／行） |
|---|---|
| `.workbuddy/memory/MEMORY.md`＋`MEMORY_INDEX.md`＋`{ironlaws,env,state}.md` | 调用层 6 条铁律 ＋ 环境铁律（权威根／MSYS `C:/` 纪律）＋ 状态尾段 |
| `coordination/BASELINE.md` | **§七十八 全文（行 6327–6417）**、§五 机制 18/19 行（`D32` 出处）、决策表 `D32` 行 |
| `coordination/CHARTER.md` | **机制 19 行（97）／机制 31 行（120）／关系块（122–141）** |
| `coordination/TEMPLATES.md` | **§七变更记录（297–414）**、`T-049`／`T-060`–`T-072` 行、**§二十三 末尾（1230–1240）**、第〇项固定文本（110–132） |
| `coordination/intel/INDEX.md` | 总表 2 条（机制 29 动作①） |
| `coordination/tools/check_rewrite_pipeline.py` | 全文（`pick_target`／半冻检测／自检八侧） |
| `coordination/tools/check_chain_visibility.py` | 规则定义区（104–160）／自检区（355–455）／基线读写比对（455–530） |
| `coordination/security/known_accepted.md` | 全文（登记表 ＋ 处置口径） |
| `.github/workflows/ci.yml` | 机制 31 step（行 435–456）的告警提取逻辑 |

## 附加表五 · `B2`＋`C1` 告警行

- **`B2`（Report 主线）**：本轮**未推进任何主线项**（定义/盘点/待裁均属治理层）⇒ 主线露出瞄准口不变（`H` 层判据仍为 **0**）。
- **`C1`（canon 一致性）**：本轮编号增量 **`T`＋2 ／ 机制＋1** 已由 `C15` 复算；`check_id_set_diff` 告警 **3**（历史元引用 `C22`／`T-00`／`TERM-999`，**非本轮新增**）；编号连续性 `T-001`–`T-074` 无缺号无重号。

## 附加表六 · 接续包数据块（**增量**）

> 供下一轮（`云内核-C3-013`）开工直接取用；**已省略本轮之前的历史**，只给增量。

```yaml
round: 云内核-C3-012
next: 云内核-C3-013
count_open: 407
count_after_commits: 410
head_open: eeea4dd
head_after_commits: 57edbc2
origin_main: e0fa133
unpushed_total: 11
unpushed_struct:
  note: 本轮 4 个 commit 的祖先链含夜跑 4 个 ⇒ 无法只推其一
  chain: e0fa133 -> ffae676 -> b30cc6a -> 236775f -> d197acd -> fa53223 -> 39cc9f1 -> 5ef2003 -> eeea4dd
  night_run: [ffae676, b30cc6a, 236775f, d197acd]
new_ids:
  T: [T-073 机制32定义登记, T-074 6项待裁落地]
  MECH: [32 夜跑与自动化区分机制]
  known_accepted: [A-003 夜跑缺件偶发]
pending_decisions:
  A: push 父链冲突（甲暂不推 / 乙全推 / 丙已排除）
  B: 3 条定时任务是否按新定义调整（荐只调 23:00）
  C: 基线重写阈值现值 10（🟡 待确认）
judges:
  total: 16
  pass: 16
  alerts: {chain_visibility: 0, baseline_new: 0, id_set_diff: 3, rewrite_pipeline: 0, effect_column: 0, report_structure: 0}
  selftest: {check_chain_visibility: 21, check_rewrite_pipeline: 9}
```

## 附加表七 · 本轮指令原文（归档 · 逐字）

> 用户 2026-09-24 指令 `云内核-C3-012` **原文**（依惯例入havi reports 归档）：

**【指令】指令序号：云内核-C3-012 基准：count = 【先读 BASELINE.md，自动核实；预生成值＝407】约束：按 CONSTRAINTS.md；环境铁律按 .workbuddy/memory/env.md；禁内联反引号命令；不 taskkill、不让用户双击任何 bat**
**总规划定位：阶段口径两者并存——README 四阶段｜ROADMAP 三阶段。引用阶段号必须写明口径。任务类型：定义登记 + 盘点（单一主类型）**
**优先级：① 定义登记：夜跑 vs 自动化 —— P0 ② 按新定义盘点 3 条定时任务 —— P0 ③ 6 项待裁落地 —— P1 ④ push 放行本轮 C3-011 的 4 个 commit（不含夜跑 4 个）—— P1**
**本阶段目标：把"夜跑/自动化"定义写入治理；按新定义盘点现有定时任务；6 项待裁落地**

- **〇 沿用机制**：指令序号（本轮 `C3-012`／下轮 `C3-013`）；停点报告 8 项；接续包机制；不确定先调研（`T-067`）；三号关系写死（`count`｜`v0.NNN`｜`云内核-C3-00N`）。
- **一、定义登记（P0）**：**1.1 用户定义（逐字）** —— **夜跑**：下指令之后的运行，可以先做草稿，白天再来确认。**自动化**：完全不需要决策、不需要认可的，才做自动化。**分界线＝需不需要人确认。**
  **1.2 动作**：① 写入治理件（机制 32 或现有机制扩展，走 `T-049` 登记）；② 定义须含夜跑定义＋关键特征（有指令、出草稿、等人确认）／自动化定义＋关键特征（无指令、无需确认、直接完成）／分界线／**红线：改仓库状态的，必须走夜跑（出草稿等确认），不得自动化**；③ 建议编号＝机制 32「夜跑与自动化区分机制」；④ 同时登记到 `ironlaws.md`。**1.3 边界**：只登记定义，不改现有任务；走 `T-049`，先登记后引用。
- **二、按新定义盘点 3 条定时任务（P0）**：**2.1 对象**＝21:00 清单／21:05 蒸馏／23:00 夜间 P3。**2.2 逐条填 7 项**（任务名·触发时间／执行内容／是否改仓库状态／是否出草稿／是否需要人确认／按新定义归类／若都不符→调整建议）。**2.3 输出**＝一张盘点表＋逐条归类＋不符者列调整建议（**只出建议，不擅自调整**）。**2.4 边界**：**只盘点，不调整**；调整建议等用户确认后再执行。
- **三、6 项待裁落地（P1）**：① 回写流水线近似面：`D16'` 仅同段有"解冻"字样才受检；② 基线重写周期：仅"已减"累积超阈值时重写；③ 夜跑缺件：接受偶发（三解已结清）；④ 机制 19 加 `D32` 注：最小改动加注；⑤ 4 个已消失任务记忆残件：保留；⑥ `0.0.0.0` 排除：与回环同逻辑。逐项落地，逐项登记。
- **四、push 放行（P1）**：**4.1** 推送对象＝本轮 `C3-011` 的 4 个 commit（`fa53223`／`39cc9f1`／`5ef2003`／`eeea4dd`）；**夜跑 4 个（`ffae676`／`b30cc6a`／`236775f`／`d197acd`）暂不 push**，等新定义落地后按新规则处置；先核实远端；按 `T-038` 门控；push 后必回读 `origin/main`。**4.2 边界**：只 push 本轮 4 个。
- **验收标准 7 条**（定义登记／盘点表／6 项落地／push＋CI／夜跑 4 个未 push／编号带登记状态／`C15`＋判据 16 16 PASS）＋**下一次返回第 0 行含 9 步阅读指令**。
- **汇报格式**：十四项＋第 0 行 9 步；三量台账四量齐全；边界四类齐全；含"读了底图哪几份哪几行"；含 INTEL 表、`B2`+`C1` 告警行、台账内联报告名；含接续包数据块（增量）；★ 含"定时任务盘点表＋逐条归类＋调整建议"。
- **保密与可见性**：本链 `C3` ｜接收方＝外部顾问（🔓 版本号/缺陷编号/机制条文｜🔒 crate 名/相对路径/测试计数/机制编号｜🔐 函数签名/阈值常量｜⛔ 绝对路径/账号/IP/密钥/二进制路径与体积/端口+可定位目标）。
- **本轮禁区**：① 不改底图（6 项待裁落地除外）② 不改治理（定义登记＋待裁登记除外）③ 不直接升格 ④ 不新增端点 ⑤ 不迁 `tmp` ⑥ **push 只 push 本轮 4 个；夜跑 4 个不 push** ⑦ 不改内核公共类型 ⑧ **不擅自调整定时任务**（只盘点、只出建议）。
- **中断续跑**：中途被中断，恢复后先出《续跑状态表》，再继续。
- **启动回执**：已读 `CHARTER.md`；当前状态 `count`＝自动生成；阶段口径两口径并存；任务＝定义登记＋定时任务盘点＋6 项待裁＋push 本轮 4 个；**是否继续：执行**。
