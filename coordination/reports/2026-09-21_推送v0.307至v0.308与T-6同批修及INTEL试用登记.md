【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# push v0.307–v0.308 ＋ `T-6` 假阳性与假阴性同批修 ＋ INTEL 试用登记 ＋ 报告结构判据立项（2026-09-21 · v0.309）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。
（**唯一涉网项**：v0.308 轮 `INTEL-001` 引用的 `miit.gov.cn` 公开政策页 —— 属公开政府信息。）

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 308** ⇒ 收尾 **309**（本轮 **1 个本地提交**）｜**label v0.309**（`label = count`，C21） |
| **HEAD／远端** | **已推 `b951229..0d1caf1`**（v0.307＋v0.308 两个 commit）⇒ 生成时 `origin/main = 0d1caf1`；**本轮新 commit 未推** |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 ＝ 执行层（设计完成）｜L8–L10 🔒 冻结 |
| **编号** | 机制 **1–27**｜C **1–21**｜T **1–41**｜R **1–89**｜TERM **1–13**｜**INTEL 1–2**（**试用中**） |
| **判据** | `tools/check_*.py` **10 个**（**新增 `check_report_structure.py`**）｜`scan_exclude.json` **6 层**｜**A2 表 3 行** |
| **本轮性质** | **放行 ＋ 裁定落地**（push ×2｜判据双向修｜试用登记｜判据立项） |

**③ 任务与约束**（含**基准**与**总规划定位**，含**优先级**）
- 基准 **count = 308**（指令填值，实测一致）｜定位：**push v0.307–v0.308 ＋ 4 项裁定落地**。
- **优先级**：**P0**（push 放行 —— 不做则后续全卡）｜**P1**（`T-6` 双向修 —— 判据正确性）｜**P2**（INTEL 试用登记／报告结构判据立项 —— 需发起人复核）｜**P3**（`C13` 范围追认／`§7.2` 待办登记 —— 可自主推进）。
- 约束：按 `CONSTRAINTS.md`｜**禁止内联含反引号／特殊字符命令**（脚本文件，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**按 T-038 门控**｜**按 C20**：CI 结论**只记日报、不入库**。
- ★ **顺序铁律**：**先 push、再动文件**（本轮已按此执行）。

**④ 状态：完成**（**9 条验收全 ✅**，见 ⑤）。

---

## ⑤ 验收对照（用户 §八 · 9 条）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（按 T-038 门控） | ✅ `b951229..0d1caf1`（exit 0）｜**门控已执行**：阻塞至 `status == completed` ＋ 取**逐 job 三态**，**门控证据 `READY_TO_EDIT`**｜★ **结论按 C20 只记日报**（仓库侧只引命令） |
| 2 | **`T-6` 假阳性与假阴性同批修**，自检正反对照 | ✅ §一（**五处同批**：左边界 ＋ C/D 收紧 ＋ 第 6 层 ＋ **登记侧区段排除** ＋ 自检扩容）；**`--selftest` 二十二侧全 PASS** |
| 3 | **INTEL 试用登记到 BASELINE** | ✅ `BASELINE §49.2`（**三条观察项** ＋ **起点 2026-09-21** ＋ **3 轮后复核**）；**`CHARTER` 一字不动** |
| 4 | **报告结构判据立项（先只告警）**，自检正反对照 | ✅ §三（新脚本 ＋ **自检十侧全 PASS** ＋ **冻结 33 条** ＋ **首版告警 0**） |
| 5 | **`C13` 范围暂不扩，记录到 BASELINE** | ✅ `BASELINE §49.4`（追认 ＋ **留痕不静默**） |
| 6 | **`§7.2` 14 条状态纠正记录待办** | ✅ `BASELINE §49.5①`（下轮单独授权；含 **F-1 修法指针**） |
| 7 | C15 复算通过、编号表连续无缺重 | ✅ 节数 **43 → 44**｜脚本 **9 → 10**｜A2 **1 → 3**｜层 **5 → 6**；**机制／C／T／TERM 全 ±0**，**连续无缺重** |
| 8 | CI 全绿 | ✅（**C20：只记日报**） |
| 9 | 下一次返回第 0 行 9 步 ＋ 含 INTEL 状态表 | ✅ 本报告第 0 行 ＋ 下方 **INTEL 状态表** |

---

## ⑥ 未完成项（清单 ＋ 原因）

| # | 未完成项 | 原因 |
|---|---|---|
| 1 | **`§7.2` 14 条「试用中」状态纠正** | 用户裁定**下轮单独授权** ⇒ 本轮**不做**（`BASELINE §49.5①`） |
| 2 | **`check_report_structure.py` 入 CI** | 未在本轮授权范围（**建议下轮接线**，照 `check_id_set_diff.py` 先例） |
| 3 | **`F-4`（v0.308 报告缺第 6 项）的回填** | **不回填**（丁案：改历史快照＝篡改留痕；该报告**已 push**）⇒ 计**冻结豁免** ＋ 留痕 |
| 4 | **`INTEL` 正式登记为机制 28** | **须 3 轮试用后**（`BASELINE §49.2`） |
| 5 | **`C13` 判据范围扩到 `intel/`** | 用户追认**暂不扩**（`BASELINE §49.4`） |
| 6 | **`INTEL-002` 来源核验** | 事件触发未满足（L7–L10 未落地）⇒ 保持**未核验** |

---

## ⑦ 真实缺陷（含 **`B2`** 要求：**本轮新增告警 ＋ 来源溯源**）

| 级 | 缺陷 | 来源／证据 | 性质 |
|---|---|---|---|
| **P1** | **`check_id_set_diff.py` 族正则缺左边界** ⇒ `GPT-6` 被读成编号 `T-6` | `coordination/intel/INTEL-002.md`（L14）、`coordination/intel/INDEX.md`（L26） | **判据缺陷（假阳性）** ⇒ **已修**（§一.1） |
| **P1** | **登记形态 C／D 过宽** ⇒ 报告"谈论某编号"的行被判为「登记」⇒ **告警自行消失** | 实测 offending 行为 **分条行／节标题**；**表格行 `forms=NONE`** | **判据缺陷（假阴性）** ⇒ **已修**（§一.2） |
| **P1** | ★★ **我自己给出的归因是错的**：v0.308 轮台账／报告写「**登记形态 B（表格第 2 列）命中**」 | 本轮诊断：**表格行全部 `forms=NONE`**，真实成因是 **C／D** | **未验证的推断被写成登记**（**R82 家族第 2 例**，与 **T-034** 同族）⇒ **原句留痕 ＋ 后记指针**（`BASELINE §49.1`） |
| **P2** | ★★ **`F-5`（本轮执行中新发现）**：**把告警追加进 `A2` 豁免登记表 ⇒ 该告警立刻消失**（`A2` 表首格＝编号本身 ⇒ 被 A 形态判为"已登记"） | 实测：追加后 C 引用 23／登记 23、T 43／44 ⇒ **差集 0**；`MECH 28` **不灭灯**（**口径不一致**） | **判据缺陷（假阴性 · 又一形态）** ⇒ **已修**（§一.6 **登记侧区段排除**）；★ **若未发现，`B2`＋`C1` 的"每轮差集"会永久恒 0** |
| **P2** | **`F-4`：v0.308 轮报告缺第 6 项（未完成项）** —— 把"INTEL 状态表"排在 **⑥** 位置，**顶掉固定相 B 第 6 项** | 本轮**新立的报告结构判据在建立过程中即抓出** | **自指类**（我自己的输出被我自己新立的判据抓出）⇒ **判据立项的即时实证** |
| **P2** | **`T-041` 剔除层覆盖不到"报告体表格行"**（本轮扩容前） | 见 §一.3 | **判据覆盖缺口** ⇒ **已扩容第 6 层**；★ **形态 B 仍属 KNOWN-LIMIT**（**现状无实例**，**如实声明不假装消除**） |
| — | `MECH 28`／`C22`／`T-00` | 均为**元引用**（详见 `BASELINE §47.3` 登记表） | **自指污染**（**A2 已登记**，**非真缺口**） |

**★ 六族告警（`C1` 载体 · 固定一行）**：
> **六族告警：本轮 3 条**（MECH `28`｜C `C22`｜T `T-00`）**｜对比上轮 ±+2**（上轮 **1 条**：`MECH 28`）｜**真缺口 0 条**。
> ★★ **必须并读（口径说明）**：**"1 → 3"不是变差**。上轮的"1"是**假阴性掩盖后的读数**（"谈论 `T-6` 的表格行"被当成登记 ⇒ 告警消失）；
> 本轮**假阳性修掉、假阴性掀开**后，才**首次看到真实面貌**：**新增 2 条（`C22`／`T-00`）全是元引用**，**已按 `A2` 兜底登记**。
> ⇒ **"3"这个数仍不宜单独引用**（**3 条里 0 条是真缺口**）；**本判据的告警数只作"待复核清单"用，不作质量指标**。

---

## §一 · `T-6` 假阳性与假阴性「同批修」（三处同批 ＋ 自检扩容）

### 1.1 假阳性 —— **族正则加左边界**
- **改前**：六族正则**仅有尾断言** `(?![A-Za-z0-9])` ⇒ `GPT-6` 的子串 `T-6` 被当成编号引用。
- **改后**：**统一加前断言** `(?<![A-Za-z0-9])`（**一处改动，引用侧／登记侧同时受益** —— **C19 同源**）。
- **对照**：**`GPT-6` 不命中**／**`T-6` 命中**（自检侧⑫ **双侧对照**）。

### 1.2 假阴性 —— **登记形态 C／D 收紧**
| 形态 | 改前 | **改后** |
|---|---|---|
| **C 分条列表** | `- **…任意文字…T-6…**` 即算登记 | **编号须紧邻行首分条标记**（`- **T-041 ★（…）**：…` 仍算 ✅） |
| **D 节标题** | `### …任意文字…T-6…` 即算登记 | **编号须在标题前段**（允许 `12.1 `／`★` 前缀；`### 12.1 T-041（本轮新增）` 仍算 ✅） |
- **代价实测（可复现）**：**R 90/90、D 55/55、TERM 14/14 三族无回归**；C 23→22（露 `C22`）、T 44→43（**假登记消失**，露 `T-00`）。
  ⇒ **收紧不过头**（自检侧⑮⑯ **专测"真登记不被误伤"**）。

### 1.3 剔除层扩容 —— **第 6 层「报告自述告警段」**
- `scan_exclude.json` **新增第 6 层**，`apply_exclude` **新增 `line_prefixes` 支持**（层可限定 marks **只在行以上述前缀开头时生效**）。
- ★ **刻意收窄**：只剔「**报告／台账自述告警数值**」的**表格行／分条行**，
  **不是**「一切谈论编号的行」—— **过宽剔除＝变相把真引用藏起来**（R73）。
- **实测**：剔除 **854 → 881 行**（**第 6 层 27 行**）。

### 1.4 ★ KNOWN-LIMIT（**如实声明，不假装消除**）
- **形态 B（表格第 2 列）** 无法按**形状**区分「登记」与「谈论」：`| 7 | R99 说明 |` 与 `| x | T-6 假阳性 |` **同形**。
- **现状**：**实测无实例**（所有 offending 行为 C／D，**表格行全部 `forms=NONE`**）⇒
  **本次不修 B**，以**自检侧⑲ 把该口径写死**（**断言"口径如此"并打印**），**风险如实留痕**。

### 1.5 自检扩容：**十一侧 → 二十二侧**
新增 ⑫假阳性（双侧）｜⑬⑭假阴性（C／D）｜⑮⑯收紧不过头｜⑰第 6 层生效｜⑱第 6 层不误剔｜⑲KNOWN-LIMIT｜⑳㉑**登记侧区段排除**（生效／不过宽）｜㉒排除清单非空 ⇒ **实测 22/22 PASS**。

### 1.6 ★★ **同批第 5 处：登记侧「区段排除」**（**本轮执行中新发现 · `F-5`**）
- **现象**：把 `C22`／`T-00` 追加进 **`A2` 豁免登记表**后，**两条告警立刻消失**（C 23／23、T 43／44 ⇒ 差集 0）。
- **根因**：**`A2` 表首格就是编号本身**（形如 `| **C22** | … |`）⇒ 被 **A 形态判为"该编号已登记"** ⇒ **告警一登记就灭灯**。
- **危害**：**`A2`（处置记录）变成了登记源** ⇒ **`B2`＋`C1` 的"每轮差集"失去意义**；且**与 MECH 族"区段限定"口径不一致**（`MECH 28` **不会**灭灯，因它只在 `CHARTER` 区段内解析）。
- **修法**：`scan_exclude.json` 新增 **`reg_section_excludes`**（**登记侧**排除区段标题关键词 ＝ `["A2 豁免登记表"]`）；判据新增 `reg_section_excludes()`／`reg_excluded_indexes()`，**登记集改用「全部行 − 排除区段」**。★ **方向是更严**（**让告警保留可见**，**不是放宽**）；**机器可读 ＋ 每次打印**（**不静默**）。**实测排除 15 行**。

### 1.7 同批执行中的一次失败留痕（**如实记**）
- 首次写 `scan_exclude.json` 时，说明文字里**混入 ASCII 双引号** ⇒ **JSON 解析失败** ⇒ **自检 ⑨／⑪／⑰／⑳／㉒ 五项 FAIL**。
- ★ **意义**：这正是 **T-041「剔除清单读取失败 ⇒ 不静默」**的**实战验证** —— **配置坏掉不会被当成"没有可剔的"**，而是**直接判 FAIL**。⇒ **改全角引号后通过**。

---

## §二 · INTEL 状态表（**机制 `J1` 载体 · 第 2 个执行案例**）

| 编号 | 状态 | 触发条件 | 本轮是否触发 | 本轮动作 |
|---|---|---|---|---|
| **INTEL-001** | **观察中** | **组合触发**：企业注册完成 ＋ 阶段三启动 | **✗ 未触发**（阶段定位＝**阶段二 · 2.4**，两条件均未发生） | 无（下一轮重新检查） |
| **INTEL-002** | **观察中** | **事件触发**：**L7–L10 落地后** | **✗ 未触发**（L7 设计完成／L8–L10 🔒 冻结中） | 无（下一轮重新检查） |

- **合计**：观察中 **2**｜待准备 **0**｜准备中 **0**｜已实施 **0**｜已归档 **0**。
- **J1–J4 自查**：J1 ✅（本表存在）｜J2 ✅（**两条均未触发，状态保持"观察中"正确**）｜J3 ✅（无"待准备"条目）｜J4 ✅（无"准备中"条目）。
- ★ **"全部未触发"是合法结果** —— **不为"好看"而硬触**（**空转 ＝ 机制失效**，R83 同族）。
- ★ **本轮起 INTEL 进入常规循环**：**下一轮开工第 1 步＝读 `coordination/intel/INDEX.md`**（`DESIGN.md §2.1` 动作 ①）。

---

## §三 · 报告结构判据「立项」（**先只告警**）

### 3.1 口径（四条，写清以免误用）
1. **范围**＝`coordination/reports/*.md`（与阅读指令判据**同范围**，便于对照）。
2. **"报告体"判定**＝含「**启动回执**」—— 十四项里**只有它是轮次报告独有**；`reports/` 下还混有**评估稿／核对稿／清单／讨论稿**（**不是**十四项报告）⇒ **不判定会大面积假告警**。**非报告体跳过但如实计数**（**不静默**）。
3. **第 0 项 `import` 复用 `check_reading_instruction.py`**（**C19 同源**，**不另写第二份**）。
4. **冻结豁免（丁案 · 不回填）**＝`coordination/security/report_structure_exempt.txt`，**只减不增**（新增条目＝自检判红）。

### 3.2 检查项＝十四项 ＋ **同义词表可见**
★ **关键设计**：第 **⑧（三量台账）** 与 **⑨（边界说明）** 用**合取**（**四量齐／四类齐**）——
**防"关键词命中即通过"**（自检侧④⑤ 专测"缺一即报缺"）。

### 3.3 首版实测
| 项 | 值 |
|---|---|
| 报告总数 | **131** |
| **报告体** | **69** |
| 非报告体（跳过） | **62**（如实计数） |
| **冻结豁免** | **33 条** |
| **告警** | **0** ✅ |

### 3.4 触发再评估
**告警命中连续 N=5 轮为 0** ⇒ 再议转判红（与 `check_id_set_diff.py` **同口径**）。

---

## §四 · `C13` 范围追认 ＋ `§7.2` 待办

- **`C13` 范围「暂不扩」**（追认）：`check_reading_instruction` 的窄范围维持 ⇒ **`coordination/intel/` 五份文件在覆盖外**（**不放指令块、亦不判红**）—— **留痕，不静默**（`BASELINE §49.4`）。
- **`§7.2` 14 条状态纠正**：**下轮单独授权**（`BASELINE §49.5①`）；★ **本轮把 INTEL 按新写法登记**（明写**起点 ＋ 预计复核轮次**），**未回改既有 14 条** —— **不重复 F-1 的缺陷**。

---

## ⑧ 三量台账（四量）

| 量 | 内容 |
|---|---|
| **存量** | 机制 **27**｜C **21**｜T **41**｜R **89**｜TERM **13**｜判据脚本 **10 个**｜剔除层 **6 层**｜底图 **36 份** |
| **变量** | **修改**：`check_id_set_diff.py`（三处同批 ＋ 自检十九侧）｜`scan_exclude.json`（第 6 层）｜`BASELINE.md`（§四十九 ＋ §47.3 追加 2 行）｜**新增**：`check_report_structure.py`｜`security/report_structure_exempt.txt`（33 条）｜**本轮报告 1 份**；**编号表变动：0**（机制／C／T／TERM 全 ±0） |
| **补充增量** | ① **push v0.307–v0.308**（**T-038 门控第 10 次执行成功**）｜② **判据缺陷同批修（五处）**（假阳性＋假阴性＋剔除层扩容＋**登记侧区段排除**＋自检扩容）｜③ **INTEL 试用登记**（三条观察项 ＋ 起点 ＋ 3 轮复核）｜④ **报告结构判据立项**（十侧自检 ＋ 冻结 33 条 ＋ 告警 0）｜⑤ **`A2` 表 1 → 3 行**（`C22`／`T-00`） |
| **创新增量** | ① ★ **"同批修"方法论首次落地**（**只修一侧会留下另一侧的洞** ⇒ **五处同批**，含**执行中新发现的第 5 处**）｜② ★ **`line_prefixes` 泛化剔除层**（**层自带行前缀限定** ⇒ 剔除可"按行种"收窄）｜③ ★ **`reg_section_excludes`：登记侧区段排除**（**"处置记录表"不得成为"登记源"** —— 根治"**一登记就灭灯**"）｜④ ★ **KNOWN-LIMIT 自检侧**（把"**已知不可修**"写进自检并断言"口径如此"，**不假装消除**）｜⑤ ★ **"报告体"判定器**（用轮次报告独有标记区分报告与评估稿 ⇒ 避免大面积假告警）｜⑥ ★ **判据立项的即时自证**（新判据**建立过程中即抓出我自己的输出缺项**，见 **F-4**）｜⑦ ★ **配置损坏的 fail-closed 实证**（JSON 坏 ⇒ 自检 5 项 FAIL，**未被当成"无内容可剔"**） |

---

## ⑨ 边界说明（四类）

- **本地跑过**：
  - `python coordination/tools/check_id_set_diff.py --selftest` ⇒ **二十二侧全 PASS**；**实跑** ⇒ 六族 R **90/90**、D **55/55**、C **23/22**、T **43/43**、TERM **14/14**、MECH **26/27**；**告警合计 3**；剔除 **893 行**（6 层）＋ **登记侧排除 15 行**（扫描 **241** 份）。
  - `python coordination/tools/check_report_structure.py --selftest` ⇒ **十侧全 PASS**；**实跑** ⇒ 报告 **131**／报告体 **69**／非报告体 **62**／**冻结 33**／**告警 0**。
  - **C15 取数**：台账顶层节数 **43 → 44**｜判据脚本 **9 → 10**｜`A2` 表 **1 → 3**｜剔除层 **5 → 6**｜登记侧排除关键词 **0 → 1**｜机制／C／T／TERM **±0**。
  - **推送**：`git push` ⇒ `b951229..0d1caf1`（**exit 0**）。
- **CI 过**：**已按 T-038 门控**执行（**阻塞至 `completed` ＋ 取逐 job 三态**，**门控证据 `READY_TO_EDIT`**）；★ **按 C20：run 号与逐 job 三态只记当日日报、不入库**（本报告亦不写）。
- **真实跑过**：**无**（本轮**零代码、零 boot 层改动**）⇒ 不适用于 **CQ（CI QEMU）／H（真机）** 两层（**机制 26：跨层引用＝无效**）。
- **未验证**：① **`§7.2` 14 条状态纠正**（下轮授权）｜② **新判据入 CI**（本轮未接线）｜③ **形态 B 的 KNOWN-LIMIT**（无实例，**未修**）｜④ **`INTEL-002` 来源**（未检索）｜⑤ **`C22`／`T-00` 的发起人复核**（**待确认**）｜⑥ **`F-4` 不回填**的处理是否为最终口径（**待确认**）。

---

## ⑩ 决策评估（七要素 · 本轮**新决策 1 项**：`C22`／`T-00` 两条新露出告警如何处置）

| 要素 | 内容 |
|---|---|
| **评估** | 两条均为**元引用**（**讨论一个不存在的编号**），**非真缺口**；处置有三：**甲** 入 `A2` 兜底登记（**已执行**）｜**乙** 把 `T-041` 剔除层再扩容以覆盖"编号追认讨论"段｜**丙** 为其**真建编号**（C22／T-00） |
| **优势** | **甲**：**零新增剔除、零编号变动**，且**显式可见**（`A2` 表即"待复核清单"）｜**乙**：一次性消警｜**丙**：把"经常被讨论的编号"**变成正式登记** |
| **劣势** | **甲**：告警数**停留在 3**（**但 3 条全非真缺口**）｜**乙**：**剔除层继续膨胀**（第 7 层），**每加一层都要问"会不会把真引用也剔掉"**（**过剔＝把真缺口藏起来**，比告警更危险）｜**丙**：**C22 已在 §41.1 被明确裁定"暂不补"** ⇒ 建号＝**推翻既有裁定** |
| **风险** | **甲**：若将来出现**同名真缺口**，会**被既有登记行"顺带放过"**（**A2 表铁律已写"真缺口一律不得入表"**，但**人可能不看**）｜**乙**：**R73 家族**风险（规则越加越隐）｜**丙**：一次动两族编号（**回归面大**，§41.1 已评估过） |
| **机会** | ★ **甲 顺带获得一项"告警分类"能力** —— 本轮**首次把告警分成"真缺口／元引用／假阳性"三类**（此前只有"有／无"二值）⇒ **告警数从"质量指标"降级为"待复核清单"**（**更诚实**）；若持续，可把**分类**机器化（第 3 类现在只能人工判） |
| **建议** | **选甲**（维持 `A2` 登记）＋ **不扩剔除层**（**乙 暂缓**，**过剔风险 > 收益**）＋ **不为元引用建号**（**丙 不做**） |
| **等待用户决策** | ✅ **等**：**②** 复核 `C22`／`T-00` 是否为元引用（**`B2` 发起人复核**）；**③** `F-4` 不回填是否为最终口径 |

---

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★ **push 放行**（本轮 **1 个本地 commit**，v0.309） | **放行**（机制 23：push 须当轮明示） |
| ② | **`C22`／`T-00` 经复核确认为"元引用"并留表** | **确认留表**（**真缺口 0 条**） |
| ③ | **`F-4`（v0.308 报告缺第 6 项）维持"不回填＋计豁免"** | **维持不回填**（丁案口径） |
| ④ | **`check_report_structure.py` 入 CI（只告警）** | **下一轮接线**（照 `check_id_set_diff.py` 先例） |
| ⑤ | **`§7.2` 14 条状态纠正** | **下轮单独授权**（**本轮已登记为待办**） |
| ⑥ | **`C13` 范围是否在 INTEL 试用期满后扩到 `intel/`** | **试用期满再议**（**暂不扩**已追认） |

---

## ⑫ 下一步建议

1. **本 commit 放行后按 T-038 门控**：等 `gh run list` 显示 `completed` **再落笔任何 CI 结论**（**门控证据 `READY_TO_EDIT`**）。
2. **下轮批次（推荐合并）**：⑤ `§7.2` 14 条状态纠正 ＋ ④ 新判据入 CI ＋ **A2 表的"告警分类"机器化评估**。
3. ★ **下轮开工第 1 步＝读 `coordination/intel/INDEX.md`**（**INTEL 机制动作 ①**，`DESIGN.md §2.1`）。
4. **INTEL 试用进度**：已 **1 轮**（v0.308）＋ **本轮第 2 轮**（报告含状态表）；**预计 v0.311 轮满 3 轮** ⇒ 再议正式登记。

---

## ⑬ 启动回执
已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD` 的输出 ⇒ 309**。

---

## ⑭ 本轮读了底图哪几份、哪几行

| 文件 | 读取范围 | 用途 |
|---|---|---|
| 根 `README.md` | **未读**（本轮**不改底图**） | — |
| `docs/`（35 份） | **未读**（同上；**判据 F 实跑 PASS 即以机器方式确认底图未被扰动**） | — |
| `coordination/CHARTER.md` | **未读全文**（本轮**不动机制表**；仅按既有登记引用机制 4／18／23／26） | INTEL 与 F-1 的机制归属 |
| `coordination/TEMPLATES.md` | **§〇 固定相 B（L32–61）** ＋ **§7.2 变更记录（L307–360）** | **本报告十四项** ＋ **F-1（14 条"试用中"）取证** |
| `coordination/BASELINE.md` | **L3280–3383（§四十七 尾部）** ＋ **§47.3 A2 表**＋ **§四十八** | 追认记录落点 ＋ `A2` 表追加 ＋ C15 取数 |
| `coordination/CONSTRAINTS.md` | **`^## C` 行位** ＋ C13/C20/C21 条文 | 判据口径（**C19 同源**、**C20 不入库**） |
| `coordination/tools/` | **`check_id_set_diff.py` 全文（394 行）** ＋ `scan_exclude.json` ＋ `check_reading_instruction.py`（头部口径） | **本轮三处修改的依据**（**先读源码、再改** —— 正是"归因订正"的教训） |
| `coordination/security/` | 目录（`reading_directive_exempt.txt` 口径） | 冻结豁免名单的格式先例 |

> **★ 说明**：本轮**未读根 `README.md` 与 `docs/`** —— 因**零底图改动**（`git diff --stat` 可验）⇒ **按机制 22 判据 F 无需更新**。
> **不以此冒充"已核底图"**（C16 口径）。
