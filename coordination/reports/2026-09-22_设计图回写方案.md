【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 云内核 · 设计图回写方案（2026-09-22）

> **性质**：本文件是 **回写施工提案**（**只出稿**），不是执行记录。
> **上位稿**：《第一部分：技术与产品（详尽版）》／《第二部分：管理与运行（详尽版）》—— 本方案引用其盘点结论。
> **★ 本轮不改任何文件**：本轮只提出稿；**回写须等用户确认后执行**（`CONSTRAINTS.md` **C4**：改前先列清单）。
> **★ 底图回写须走 `D16'` 程序**：**发起人授权解冻 → 回写 → 重新冻结**。本方案所列一切涉 `docs/` 与根 `README.md` 的动作，**在未获授权前一律冻结**。
> **轮次口径**：版本 **count = 339**／轮次 **v0.325**。

---

## 〇 · 摘要（先读这里）

| 项 | 数 |
|---|---|
| 涉及**底图**文件件数 | **7** 件 |
| 涉及**脚手架**文件件数 | **3** 件 |
| 其中 **P0（须先做）** | **3** 项 |
| 其中 **P1** | **4** 项 |
| 其中 **P2** | **5** 项 |
| **无来源不回写** | **4** 项（第 4 类，见第七章） |
| **须先统一的口径冲突** | **3** 处（第三章，**阻塞项**） |

**一句话**：本项目的**缺口不在技术实现，而在"讨论结论没写回底图"**——几乎每一 library 侧 opacity 都有对应文件路径，但**几乎都没有回写底图**。本方案不追求"补完"，而追求**按优先级、按顺序、可回滚**地把它们写回去。

---

## 一 · 盘点结论：去向与优先级总表

| # | 结论/事实归纳 | 现有出处（卡 ✓） | 应写回何处 | 优先级 |
|---|---|---|---|---|
| 1 | **L0–L6 的 内外一体两面表**（完整 11 层） | `discussions/2026-09-19_生成式场规划.md:55-87`（自标"临时理解稿"） | 建议新建子文件：底图 `docs/LAYER_BASEMAP_L0_L6.md` §2【补表】 | **P1** |
| 2 | **L7–L10 内外一体两面**（**已裁定版**） | `discussions/2026-09-18_汇编正文_第1批_设计定位层.md:776-789`（同轴：始末 see `2026-09-19_汇编合订本.md:971-984`） | **已回写底图** `docs/LAYER_ARCHITECTURE.md` §4.1.2（行 385-392）✅ | **已完成** |
| 3 | **L4 内外一体两面裁定（R-5）** | `discussions/2026-09-19_汇编合订本.md:868`；`discussions/2026-09-18_汇编正文_第1批_设计定位层.md:673` | **已回写底图** `docs/LAYER_ARCHITECTURE.md` 行 282 ✅ | **已完成** |
| 4 | **戒律内化框架**（本征态归 L0–L6 之裁定） | `discussions/2026-09-18_汇编正文_第1批_设计定位层.md:778-789` | **已回写底图** `docs/LAYER_ARCHITECTURE.md` 行 385-396 ✅ | **已完成** |
| 5 | **四态/创新增量 权威定义（⟨补 6⟩）** | `discussions/2026-09-18_汇编正文_第1批_设计定位层.md:1086-1097`；`discussions/2026-09-19_汇编合订本.md:1286-1292` | 底图 `docs/LAYER_BASEMAP_L0_L6.md` 行 214-225 附近补 **⟨补 6⟩ 全文** | **P1** |
| 6 | **八步通用工作方法论（两层结构）** | `coordination/reports/2026-09-22_云内核整体规划_第一部分_技术与产品.md:128-157`（稿，非底图） | 底图 **无任何命中** ⇒ 建议 **docs 新建或 `README.md` 补`：两层＝通用层（任何系统可对上）＋云内核层（本项目物理名） | **P2** |
| 7 | **感知-认知-行动-反馈循环** | **see below：全库 0 命中** | **无来源 ⇒ 不回写**（第四类） | **—** |
| 8 | **界面就是它自己**（D8 裁定，2026-09-20） | `coordination/BASELINE.md:1078`；`discussions/2026-09-19_帧缓冲抽象层设计稿.md:751`；`discussions/2026-09-19_生成式场规划.md:47-50` | 底图：`docs/FIELD_PRESENTATION_DESIGN.md` 或 `docs/FIELD_RENDER_SOURCELESS_DESIGN.md` | **P1** |
| 9 | **自他无二 / 度众生** | `docs/LAYER_ARCHITECTURE.md` 行 392/390 | 已回写 ✅ | **已完成** |
| 10 | **猕猴桃葡萄比喻**（项目定位） | **全库 0 命中** | **无来源 ⇒ 不回写**（第四类） | **—** |
| 11 | **场景举例（大树 / 种子）** | **全库 0 命中**（`种子` 仅作为术语出现） | **无来源 ⇒ 不回写**（第四类） | **—** |
| 12 | **量子层位置** | `docs/QUANTUM_CLOCK_DESIGN.md:14-24,44-61`；`coordination/reports/2026-09-22_云内核整体规划_第一部分_技术与产品.md` | 底图 `docs/LAYER_ARCHITECTURE.md` （**不专列**仅适用同一 ⭐ 的描述） | **P2** |
| 13 | **L0–L6 的 M1–M6 裁定（含 M6/M8 状态）** | `discussions/2026-09-19_L0-L6修正说明.md:200-230` | `coordination/BASELINE.md`（**§59.3 口径冲突，先统一**） | **P0** |
| 14 | **⟨补 6⟩ 里 "创新增量是否为第四量"** | `discussions/2026-09-18_汇编正文_第1批_设计定位层.md:1097`（⟨待裁定⟩） | 先裁定，后回写 | **P0** |
| 15 | **L7–L10 两版本冲突** | 见 §1.1 | 先统一为准一回写版本 | **P0** |

### 1.1 重要：L7–L10 存在两个互相冲突的版本

| 版本 | 位置 | 状态 |
|---|---|---|
| **甲版（外推稿）** | `discussions/2026-09-19_生成式场规划.md:55-87` | 自标「临时理解稿，待修正」；第 87 行明示「**L7–L10 的"对外面"是本稿外推……效力＝推测**」 |
| **乙版（裁定版）** | `discussions/2026-09-18_汇编正文_第1批_设计定位层.md:776-789`（同轴：始末 `2026-09-19_汇编合订本.md:971-984`） | 明示「★ **2026-09-21 回写**，`D16'` 授权解冻→修改→**重新冻结**」 |

**风险**：若误把**甲版外推内容**当作"已裁定"写回底图，会把**推测写进权威**。
⇒ **本轮处置建议**：回写时**只准采用乙版**（裁定版），甲版仅可作为"待补其内容 which is："之子选项 referring to。

---

## 二 · 逐文件回写方案

### 2.1 `docs/LAYER_BASEMAP_L0_L6.md`（P1）

| 栏 | 内容 |
|---|---|
| **现状** | 行 187-188 自承缺口：**「L1-M3｜三量（存量/变量/共同增量）没有权威定义」**；已有行 214-225 的⟨补 6⟩表（**但并未标记"权威定义"**，且仍持"⟨待裁定⟩"）。另形 natura L0–L6 总表（行 36-44）列头无"外侧/内侧"两列。**"内外一体"全 docs 仅 1 处**（行 282，L4） |
| **应改为**（Proposal） | ① 补【补表】或补列：给 L0–L6 总表加 **内侧（对内核自身）** 与 **外侧（对世界/用户）** 两列；<br>② 将⟨补 6⟩（含"创新增量"行）提升标记为**权威定义**，清除 ⟨待裁定⟩ 标记（须先裁定，见 §3.2） |
| **依据** | `discussions/2026-09-18_汇编正文_第1批_设计定位层.md` 行 1086-1097；2026-09-17 裁定见 `discussions/2026-09-17_顾问接续包.md:191` |
| **卡的操作步骤** | 先按 **§3.2** 裁定；再执行 `python coordination/tools/run_all_checks.py`；末行更新 `security/basemap_hashes.txt`（见 §5.1） |
| **优先级** | **P1** |

### 2.2 `docs/LAYER_ARCHITECTURE.md`（P0 / P2）

| 栏 | 内容 |
|---|---|
| **现状** | §4.1.2（行 385-392）已完成 L7–L10 表，对 had been aligned 已冻结 🔒；但**量子层未在 L0–L10 总表专列**；"内外一体"仅 L4 一处 |
| **应改为**（Proposal） | ① 在 §4.1.2 起另起一段："量子层不专列"之********禁止性表达********，明示其 Location＝L0 的量子纠缠（依据 `TERM-011`；设计见 `QUANTUM_CLOCK_DESIGN.md`）；<br>② 行 36-44 总表补 L0–L6 的"内侧/外侧"两列（同 §2.1 ①） |
| **依据** | `coordination/reports/2026-09-22_云内核整体规划_第一部分_技术与产品.md` §2.3；`discussions/2026-09-19_生成式场规划.md:55-87` |
| **优先级** | ① P2 **（建议典型吊装）**；② P1 |

### 2.3 `docs/FIELD_PRESENTATION_DESIGN.md` 或 `FIELD_RENDER_SOURCELESS_DESIGN.md`（P1）

| 栏 | 内容 |
|---|---|
| **现状** | "界面就是它自己"：**底図 0 命中**；讨论稿 `discussions/2026-09-19_帧缓冲抽象层设计稿.md:751` 有 D8 裁定（2026-09-20）；`discussions/2026-09-19_生成式场规划.md:47-50` 有四条 presented pro |
| **应改为** | 在两者之一补一段"呈现＝内核状态直接投影（`Project(S)`，无渲染管线）"：① 不引入外部素材 ② 不引入美术规则 ③ 不引入中间表示（无场景图/图层/着色器） |
| **依据** | D8 裁定，2026-09-20（帧缓冲抽象层设计稿:751） |
| **优先级** | **P1** |

### 2.4 `README.md` / `docs/VISION.md`（P2）

| 栏 | 内容 |
|---|---|
| **现状** | 项目定位（三层产品形态）已完整；"猕猴桃葡萄比喻" **全库 0 命中**；"八步两层"及"感知-认知-行动-反馈"亦 0 |
| **应改为** | ① 若发起人能提供"猕猴桃葡萄"原文 ⇒ 补入 `README.md` 项目定位节；不能 ⇒ **不补**；<br>② "八步两层"建议在 `README.md`（而非 docs）作为**方法论指针**一句带过，详细内容仍在 `coordination/workflows/`（**不回写底图的理由**：八步属"怎么干活"，不属"这个系统是什么"） |
| **依据** | 动词性质分类；`discussions/2026-09-18_汇编正文_第1批_设计定位层.md:1086-1097` 未稿记此事 |
| **优先级** | **P2** |

### 2.5 `coordination/BASELINE.md`（P0）

| 栏 | 内容 |
|---|---|
| **现状** | §59.3（行 4530-4539）表第 6/7 项与源清单不符（源清单第 6 项＝`count` 数值时点化、第 7 项＝"本稿未做"；见 `coordination/reports/2026-09-22_规划稿7项待确认清单.md`）；另：`M6` 状态两处不一 |
| **应改为** | 先裁定（§3.1、§3.3）⇒ 统一后修改；并在本轮新增 **§六十一**（见文末"下一轮施工"） |
| **优先级** | **P0（阻塞项）** |

---

## 三 · 须先统一的口径冲突（**阻塞回写**，先裁再做）

### 3.1 `M6` 状态两处不一（`C19` 家族）

| 出处 | 记载 |
|---|---|
| `discussions/2026-09-19_L0-L6修正说明.md` §七（行 200-230） | 「**2026-09-19 用户裁定：选乙**」 |
| `coordination/reports/2026-09-22_L0-L6专题_规划稿7项涉底图部分.md` §5.1 ／ `BASELINE.md` §59.3 第 6 项 | 「**未裁**」 |

**建议**：由发起人裁定 **M6 是否已于 2026-09-19 裁定为选乙**。若已裁 ⇒ 改 `§59.3` 表；若未裁 ⇒ 改源稿 §七 的行文（补"待补 IgA 记录"标记）。

### 3.2 "创新增量是否为第四量"

⟨补 6⟩ 表自带 `⟨待裁定⟩`（行 1097）。**未裁之前，回写时须保留该 ⟨待裁定⟩ 标记，不得预填答案**。

### 3.3 `BASELINE.md` §59.3 表第 6/7 项

见 §2.5。建议：**第 6 项改为 `count` 数值时点化；第 7 项改为「本稿未做」**，并同步修正该表合计行（原文仍写"不涉 3 项（第 1/6/7）"，属自相矛盾）。

---

## 四 · 回写顺序（含 `D16'` 程序）

```
┌ 阶段〇  授权解锁（发起人）
│   ⚠️ 未解锁 ⇒ 一切涉 docs/ 与 README.md 的动作冻结
├ 阶段一  统一口径（P0，不动底图）
│   ① 裁定 M6 状态              ② 裁定"创新增量是否第四量"
│   ③ 修正 BASELINE §59.3 表
├ 阶段二  回写 A 类 · 戒律/创新增量权威定义
│   ④ LAYER_BASEMAP_L0_L6.md  ⑤ LAYER_ARCHITECTURE.md §4.1.2 附近
├ 阶段三  回写 B 类 · 呈现/界面
│   ⑥ FIELD_PRESENTATION_DESIGN.md（界面就是它自己）
├ 阶段四  回写 C 类（可选）
│   ⑦ 八步两层（README.md）     ⑧ 量子层不专列说明
├ 阶段五  同步（见第五章）
│   ⑨ 判据 F 更新  ⑩ 汇编重生成  ⑪ 全套判据跑
└ 阶段六  重新冻结 🔒（D16' 第三步）
```

**原则**：**一次一阶段、一阶段一确认**。任一阶段判据转红 ⇒ **当场停**，不继续下一阶段。

---

## 五 · 回写后必须同步的动作

### 5.1 判据 F 更新（**必做**）

底图内容变更后，`coordination/security/basemap_hashes.txt`（36 份快照）必须同步更新，否则 **`check_source_of_truth.py` 会判红**（这是它该做的）。

```
python coordination/security/update_basemap_hashes.py --update-baseline
```

> ⚠️ 该命令须在**回写完成后、判据跑之前**执行；**不得**为了"打绿"提前更新而实际未改。

### 5.2 汇编重生成（**回写底图后**）

`docs/` 若有结构性变化 ⇒ 需要重新生成 `2026-09-18_汇编正文_*` 系列（第 1–4 批）与 `2026-09-19_汇编合订本.md`。
⚠️ **注意**：汇编整𝑙批次集合重生成会 kont 同时改动 organ നടപ"::~:. 建议：**仅重生成被改文件所在的批次**。

### 5.3 全套判据跑（**每阶段必跑**）

```
python coordination/tools/run_all_checks.py
```

**期望**：**11/11 PASS**。原则上"只跑不改"：若转红，**优先怀疑是回写引入的真实不一致**，而非调判据。

### 5.4 记忆三层更新（**收口**）

按 **机制 30**：调用层 `MEMORY.md` <1 KB ／索引层 `MEMORY_INDEX.md` <4 KB ／内容层 `state.md`・`rounds.md` 更新；随后跑 `check_memory_layers.py`（只告警）。

---

## 六 · marks important detail Documentation

| Attention | 内容 |
|---|---|
| **©/⛔ 机密不入仓** | 回写涉及 `{PRIVATE_ASSETS}` 内容时，**只用占位符**（机制 17）；密钥本体/路径/指纹**一律不写**（C12・⛔） |
| **一次一文件** | 避免并行编辑 `BASELINE.md`  numbering 表（**C15**：编辑前取数→编辑后复算→差值须与预期一致） |
| **机动他与Influence** | 回写后每一)`git status --short` 必须迹 check 是否"只新增、零删除" |

---

## 七 · 第四类：无来源，不回写（**如实记录，不臆造**）

| # | 项目 | 核验证据 |
|---|---|---|
| 1 | **猕猴桃葡萄比喻** | 全库 262 份 md 检索：命中 **0**（阳性对照：同脚本同范围 `0锚点` 175、`阈值` 324 ⇒ 探针有效） |
| 2 | **感知-认知-行动-反馈循环** | 同上，检索式 `感知.{0,12}认知` 等 **0 命中** |
| 3 | **大树比喻场景** | 同上，`大树`/`果树` **0 命中** |
| 4 | **六波罗蜜**（SILA 相关） | 同上，`六波罗蜜`/`波罗蜜` **0 命中**（底图现行用"比丘五戒＋具体戒律表述"，非六波罗蜜体系） |

**★ 这四项不回写，亦不代拟。** 理由：**缺少原文出处** ⇒ 回写等于用猜测污染权威底图（`C16` / **C17**）。建议由发起人**提供原文**，或确认**以现行底图口径为准、不予补充**。

---

## 附 · 交叉引用

| 本方案 | 引用 |
|---|---|
| §1 总表 | **第一部分（详尽版）**第十二章「盘点结论」 |
| §3 口径冲突 | **第二部分（详尽版）**第二章 C19 家族；`coordination/reports/2026-09-22_L0-L6专题_规划稿7项涉底图部分.md` |
| §4 `D16'` | `discussions/2026-09-18_汇编正文_第1批_设计定位层.md` 行 776-789 的冻结辞 |
| §5.1 | `coordination/tools/check_source_of_truth.py` 判据 F |
| §5.3 | `coordination/tools/run_all_checks.py`（`T-048`） |

---

## 边界说明（四类）

- **本地跑过**：✅ git 实跑（`rev-list --count`＝339、push `a78acf9..684aa9a` exit 0）；判据 11 个本地全跑 **11/11 PASS**；C15 取数脚本实跑；关键词探针实跑（262 份文件）。
- **CI 过**：✅ run `35746939981` 三 job 全 success、`##[error]` = 0、`READY_TO_EDIT`（详见配套的 2026-09-22 十四项报告）。
- **真实跑过**：❌ **本方案为零执行稿** —— 一切回写动作**尚未执行**，故无真机/真跑验证。
- **未验证**：★ **各回写项落笔后的实际文案**未经验证（本方案为零执行稿，文案须等发起人批准后回写时才产生）；★ **§1.1 两版 L7–L10 的取舍**待发起人确认（本方案建议乙版＝裁定版）；★ **四项「全库 0 命中」结论的边界** —— 检索范围限于 **md 文件**（根 `README.md` ＋ `docs/**` ＋ `coordination/**`），**未覆盖源码非 md 文件与历史 git 对象**。
