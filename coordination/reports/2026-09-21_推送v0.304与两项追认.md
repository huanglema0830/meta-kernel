【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# push v0.304 ＋ 两项追认（MECH 先观察／告警 28 暂不做标记）（2026-09-21 · v0.305）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 304** ⇒ 收尾 **305**（本轮 **1 个本地提交**）｜**label v0.305**（`label = count`，C21） |
| **HEAD／远端** | 推前 `bf685d8`（= `origin/main`）｜**已推 `63edb1e..bf685d8`**（v0.304） |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 = 执行层 |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–39**｜D **1–44**（＋待核）｜R **1–52**（＋ R70 已补登）｜TERM **1–13** |
| **本轮性质** | **放行 ＋ 追认**（两项追认：§一 MECH 先观察／§二 告警 28 暂不做标记） |

**③ 任务与约束**
- 基准 **count = 304**（指令填值，实测一致）｜总规划定位：**push v0.304 ＋ 2 项追认**。
- 约束：按 `CONSTRAINTS.md`｜**禁止内联含反引号／特殊字符命令**（全程脚本文件，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**按 T-038 门控**｜**按 C20** CI 结论不入库。

**④ 状态：完成**（**2 项追认全落地**；**5 条验收全 ✅**，见 ⑤）。

---

## ⑤ 验收对照（用户 §五 · 5 条）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（按 T-038 门控） | ✅ `63edb1e..bf685d8`（exit 0）；✅ 按 **T-038 门控**执行（阻塞至 `completed` 后取值，门控证据 **`READY_TO_EDIT`**）—— ★ **job 级三态按 C20 只记当日日报、不入库** |
| 2 | MECH 族先观察，记录到 BASELINE | ✅ `BASELINE §四十六 · 46.1`（含"触发再评估 N=5"＋ 本轮实测基线） |
| 3 | 告警 `28` 暂不做，记录到 BASELINE | ✅ `BASELINE §四十六 · 46.2`（理由＝**避免新增隐藏规则**，R73 教训） |
| 4 | C15 复算通过，编号表**连续无缺号无重号** | ✅ **改前＝改后 全 ±0**（机制 27／C 21／T 39／D 行 45／R 首格 50／R 唯一 53／§四 21／TERM 13） |
| 5 | CI 全绿 | ✅ 门控取值＝`completed`（**按 C20 只记日报**） |

---

## §一 追认 · **MECH 族仍先观察、不入 CI**（用户 §一 · 无文件改动）

- **裁定**：`tools/check_id_set_diff.py` 的 **MECH 族**（与其余五族同处状态）**维持"先只告警、不入 CI"**。
- **触发再评估**：**告警命中连续 N 轮为 0**（**N = 5**）⇒ 再议「**入 CI ＋ 转判红**」**合并落地**（与 §44.2 同口径）。
- **本轮实测基线（供观察窗口比对）**：

| 族 | R | D | C | T | TERM | MECH | **合计** |
|---|---|---|---|---|---|---|---|
| 引用但无登记 | 0 | 0 | 0 | 0 | 0 | **1**（`28`） | **1** |

> **观察窗口起点**：本轮的 **告警合计 = 1**（即"连续 N 轮为 0"的**第一轮记数**从下一轮开始）。

## §二 追认 · **告警 `28`「示例标记」约定 —— 暂不做**（用户 §二 · 无文件改动）

- **裁定**：**不引入**"示例编号一律用保留号段"之类约定。
- **理由（逐字口径）**：**避免新增隐藏规则**（**R73 教训** —— 隐藏规则削弱判据可信度，并把"真实缺口"与"举例文字"混为一谈）。
- **现状保留**：告警 `28` **如实输出、不排除、不调参**；成因已溯源（§45.1：唯一出现处＝MECH 族评估稿 **L103 举例文字**，属**自指污染**，非真缺口）。
- **关联**：与 §45.2「自指污染先观察」**同一处置线** ⇒ 二者**共同等待**"连续 N=5 轮为 0"或"新污染类型"时**一并再评估**。

---

## ⑥ 未完成项
- **无本轮遗留**。两项追认**已落地**（均为口径登记，**无实现改动**）。

## ⑦ 真实缺陷
- **0 缺陷**。**1 条正向观察**：**追认轮零污染** —— 本轮**未动任何 `docs/`／代码／判据脚本**，故**不产生新的自指污染**；观察窗口**从干净状态起算**（利于"连续 N 轮为 0"的判定）。

## ⑧ 三量台账（四量）
| 量 | 内容 |
|---|---|
| **存量** | v0.304 前的全部资产：**六族齐备**的编号集差集判据（301 行，含 MECH 区段限定）＋ 登记四形态 A/B/C/D ＋ 台账 §一–§四十五 |
| **变量1**（需求） | 本轮指令：push v0.304 ＋ **MECH 仍先观察** ＋ **告警 `28` 暂不做示例标记** |
| **补充增量1**（答案1） | 两项**追认留痕**（`BASELINE §四十六`）—— 把"**不做**"也**可追溯**（附**触发条件**，避免日后被当"遗漏"重复提问） |
| **创新增量** | 存量 ＋ 变量1 ＋ 补充增量1 **融化融合化合** ⇒ **判据观察窗口的"干净起点"**（告警合计 1，成因已知、口径已定、再评估条件已写明） |

## ⑨ 边界说明（四类）
| 类 | 内容 |
|---|---|
| **本地跑过** | **C15 双向计数**（全 ±0）｜全量判据 **9/9 rc=0**｜（本轮无代码/脚本改动，故**未重跑**判据自身 selftest —— 沿 §四十五 结果） |
| **CI 过** | ✅ 已跑并按 **T-038 门控**等到 `completed`（**job 级三态按 C20 只记当日日报、不入库**） |
| **真实跑过** | —（追认轮，无真机/硬件面） |
| **未验证** | **"六族告警转正流程"**（用户 §三 明示本轮不派、**待讨论**） |

## ⑩ 决策评估（C11 七要素）—— 针对「**是否现在给告警 `28` 加"示例标记"约定**」
| 要素 | 内容 |
|---|---|
| **评估** | 告警 `28` 来自"举例文字"（自指污染），是否用"保留号段"等约定让机器区分"示例"与"真实引用" |
| **优势** | 若加约定 ⇒ 告警可回到 0，判据看起来"干净" |
| **劣势** | 需在**写作侧**新增一条**不成文约束**（"示例编号必须用保留号段"）⇒ **规则藏进约定、不进判据** |
| **风险** | ★ **可能掩盖真实缺口** —— 若某次真的引用了"机制 28"，也会被当"示例"而**静默放过**（**与 §45.2 的"排除法掩盖假阴性"同族**） |
| **机会** | 不引入约定 ⇒ 保留**如实输出**的判据，观察窗口内可**量化"自指污染占比"**（现有数据：1/1 = 100%） |
| **建议** | **暂不做**（用户已裁定，本表为**理由留痕**）；改由"连续 N=5 轮为 0"作为**客观转正前置** |
| **等待决策** | 见 ⑪-②（六族转正流程） |

## ⑪ 本轮待判
| # | 项 | 推荐 |
|---|---|---|
| **①** | ★ **push 本轮 1 个 commit（v0.305）是否放行** | **放行**（机制 23 须当轮明示） |
| **②** | **"六族告警转正流程"是否开议**（用户 §三 明示本轮不派、待讨论） | **下轮单独出讨论稿**（议题：**入 CI 时机**／**N 的取值**／**转判红后的误红处置**） |

## ⑫ 下一步建议
1. **首选**：放行 **v0.305**（本轮唯一 commit）。
2. **次选**：下轮开 "**六族告警转正流程**" 讨论稿（§三 已预留）。
3. **观察**：每轮报告后跑一次 `check_id_set_diff.py`，记 **告警合计**；**连续 5 轮为 0** ⇒ 提请转正。**本轮起点 = 1**。
4. **暂缓**：`docs` 批次与 `advisor_brief` 复扫**均为触发式**（本轮未触发 ⇒ 未跑，合规）。

## ⑬ 启动回执
> 已读 `CHARTER.md`。当前状态：**count = 304**（基准）⇒ 收尾 **305**｜阶段二 · **2.4 边界层已验**｜本次任务：**push v0.304 ＋ 2 项追认**｜是否继续：**执行完毕**。

## ⑭ 本轮读了底图哪几份、哪几行
| 文件 | 行 | 用途 |
|---|---|---|
| `coordination/BASELINE.md` | **§四十五（L3195–3243）** 与 **L3236–3243**（§45.6 待裁 ＋ §45.7 改动清单） | 台账续写定位（新增 §四十六）；两处待裁与 §四十六 的**追认对应** |
| `coordination/tools/check_id_set_diff.py` | **MECH 族段**（族定义 ＋ 专用解析四函数） | 确认追认对象**已在库**、口径与 §四十五 一致（**本轮不改动**） |
| `coordination/reports/2026-09-21_推送v0.303与MECH族解析落地.md` | **L1–L40**（第 0 行块 ＋ ①–④） | 十四项格式对齐 |

> 说明：本轮为**追认轮**，**未读 `docs/` 任何文件**（无底图改动）；上表所列均为**仓库内治理/工具/报告文件**（非 `docs/` 底图本身），如实标注以免与"读了 `docs/` 哪几份"混淆。
