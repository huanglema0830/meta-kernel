【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# 夜间 P1 记录 · 核对稿计数订正 ＋ ROADMAP 口径核对（2026-09-20 · v0.253）

> **性质**：夜间持续任务（不需要决策）第 1 项记录。**1.1 改文件（已授权）**；**1.2 只核对、不改文件**（见 §2.4 判断依据）。
> **边界**：本轮**不改底图**（`README.md` ＋ `docs/`）；**不改治理文件**（`CONSTRAINTS`／`CHARTER`／`ROADMAP`／`BASELINE`／`TEMPLATES`）——1.2 虽获授权，但**经核实无需改**（详见 §2.3）。
> **纪律**：结论一律附**复核命令或 blame 证据**（C18／R71／R82）。

---

## 1. 任务 1.1 · 核对稿 §0 汇总计数订正（7 → 8）

### 1.1 问题

`coordination/reports/2026-09-20_advisor_brief时效核对稿.md` 的 **§0 汇总**与 **§1 实列**口径不一致：

| 位 | 记 | 实 |
|---|---|---|
| §0 表「状态型」行 | **6** 项 | **§1 实列 S1–S7 ＝ 7 行** |
| §0「一句话」 | 活跃段还剩 **7** 处 | 数值型 1 ＋ 状态型 7 ＝ **8 项** |

同文件三处口径互不一致（`§0 表` 记 6／`§0 一句话` 记 7／`§1–§2` 实列 8）——**R35／R76 同族**（计数口径不稳）。

### 1.2 订正前后逐字对照

**（a）§0 表 · 状态型行**

改前：
```
| **状态型**（R87 家族） | **6** 项（含 1 处**同文件自相矛盾**、1 处**同文件内部不一致**） | — | **1** 项（§4） |
```
改后：
```
| **状态型**（R87 家族） | **7** 项（含 1 处**同文件自相矛盾**、1 处**同文件内部不一致**） | — | **1** 项（§4） |
```

**（b）§0 表后 · 新增口径说明（3 行）**

改前：（无此题注，直接接「一句话」）
改后：
```
> **★ 2026-09-20 事后订正（v0.253）**：上一行「状态型」原记 **6** 项，与 §1 实列 **S1–S7 ＝ 7 行**不符 ⇒ **已订正为 7**。
> 口径说明：**「活跃段需订正」＝ 数值型 ＋ 状态型 ＝ 1 ＋ 7 ＝ 8 项**（同源镜像 M1 **改台账源头、不改本文件** ⇒ **不计入本文件的「需订正」**，故汇总为 **8 项**）。
```

**（c）§0 · 一句话**

改前：
```
**一句话**：`advisor_brief.md` **整体时效尚可**（本轮前次已同步 2 处）；**活跃段**还剩 **7 处**需订正，其中 **§四 表 2 处（D35／D36）** 与 **§二 表 1 处（"下一步 2.3"）** 是**实质性过期**（会让顾问读到错的阶段与错的待决状态）。
```
改后：
```
**一句话**：`advisor_brief.md` **整体时效尚可**（本轮前次已同步 2 处）；**活跃段**还剩 **8 处**需订正（**数值型 1 ＋ 状态型 7**），其中 **§四 表 2 处（D35／D36）** 与 **§二 表 1 处（"下一步 2.3"）** 是**实质性过期**（会让顾问读到错的阶段与错的待决状态）。
```

**diff 体量**：`1 file changed, 5 insertions(+), 2 deletions(-)`（`git diff --stat` 实测）。
**复核命令**：`git diff -- "<仓库>/coordination/reports/2026-09-20_advisor_brief时效核对稿.md"`。

### 1.3 口径裁定（本轮确立）

> **「活跃段需订正项数」＝ 数值型（N）＋ 状态型（S）；同源镜像（M）不计入**——
> 因为 M 的**源头在台账**，本文件只是镜像，**改的是台账**（C19：同一事实只许一份口径）。

| 类别 | 计数 | 是否计入「需订正」 |
|---|---|---|
| 数值型（R82 家族） | 1（N1） | ✅ 计入 |
| 状态型（R87 家族） | 7（S1–S7） | ✅ 计入 |
| 同源镜像 | 1（M1） | ❌ 不计（改台账源头） |
| **合计** | **10 项发现** | **需订正 ＝ 8** |

---

## 2. 任务 1.2 · `ROADMAP.md` 60／72 口径核对

### 2.1 指令前提与实测不符（重要）

**指令原文**：「`ROADMAP.md:60/72` **仍写**"2.4 = DRM/KMS""2.4 需要 unsafe"」。

**实测（2026-09-20，`HEAD = bad7881`）**：**L60／L72 早已订正**，且**再修正列 L136 亦已订正**。

### 2.2 逐字现状（含 blame 证据）

**L60（2.4 行）** —— 现文逐字：
```
| **2.4** | ★ **口径修正（2026-09-19）**：**轻量帧缓冲 ＋ L6 呈现层接入**（**不引入 DRM/KMS** ⇒ 降为**远期可选**）。~~实现 DRM/KMS 驱动，直接输出场域画面~~ | **P1**（★ 口径修正后 **C9 放行非必需** —— 帧缓冲写入经 bootloader **安全 API**；SIMD 优先走**自动向量化 ／ `core::simd`（均零 unsafe）**） | ✅（编码/测试可夜间跑；**无白名单待决**） | `bootloader` 0.11 帧缓冲 |
```
**L72** —— 现文逐字：
```
- ⚠️ **2.4 的 unsafe 口径（★ 2026-09-19 修正）**：**原口径**（DRM/KMS 直渲）认为需要 unsafe；**修正后不再必需** ——
```
**L136（M3 里程碑行）** —— 现文逐字（摘）：
```
| M3 | **帧缓冲输出场域画面**（★ 2026-09-19 口径修正：**轻量帧缓冲 ＋ L6 呈现层接入**；~~DRM/KMS 输出~~，**C9 放行非必需**） | **P1** | ✅ | 3周 | |
```

**blame 证据**（复核命令：`git blame -L 60,60 --date=short coordination/ROADMAP.md`）：
```
bb72eec9 (huanglema0830 2026-09-19 60) | **2.4** | ★ **口径修正（2026-09-19）**：...
bb72eec9 (huanglema0830 2026-09-19 72) - ⚠️ **2.4 的 unsafe 口径（★ 2026-09-19 修正）**：...
```
⇒ **L60／L72 的订正发生在 2026-09-19 的 `bb72eec9`（v0.219）**，早于本轮。

### 2.3 结论（1.2）

| 指令项 | 期望 | 实测 | 处置 |
|---|---|---|---|
| `ROADMAP.md:60` 口径同步 | 需订正 | **已是修正后口径**（2026-09-19 · `bb72eec9`） | **无需改** |
| `ROADMAP.md:72` unsafe 口径同步 | 需订正 | **已是修正后口径**（同上） | **无需改** |
| （连带）`L136` M3 行 | — | **已同步**（`~~DRM/KMS 输出~~` 已划删） | **无需改** |

⇒ **1.2 的授权项「已达成」，本轮对 `ROADMAP.md` 零改动**。**故「逐字订正前后对照」不存在（无订正）**，代之以**逐字现状 ＋ blame 证据**。

### 2.4 为什么不去订正 L53（同一族残留）

`ROADMAP.md` **L53** 仍写旧口径（逐字）：
```
53:> **新路线：元内核自举**——无 Linux 依赖，DRM/KMS 直接渲染。
```
**为什么本轮不动它**：
1. **越权风险**：1.2 的授权范围明写 `ROADMAP.md:60/72`；L53 位于 **§三「子任务2 修正记录」块引用**（2026-09-16 写入，`c84afc0a`），**不在授权行内**。
2. **历史块性质**：L51–L53 是**带日期的历史修正记录**（"修正记录（2026-09-16，用户决策）"），按 **D52／D54 纪律＝历史段只加标注、不改写**——**加标注亦属改治理文件**，须用户点头。
3. **约束优先**：「不改治理文件（除 1.2 授权项）」——**从严**（机制 23 同源：无法区分时从严）。

⇒ **升格为本轮待裁定项**（见 §3）。

### 2.5 ★ 系统性发现：旧口径在「叙述句」存续（R82／R87 同族）

**2.4 口径修正（2026-09-19）只改到「表格行」，未清「叙述句」**。全库实测（复核命令：`grep -rn "DRM/KMS\|DRM/KMS 直渲" coordination/ROADMAP.md coordination/advisor_brief.md`）：

| # | 位置 | 逐字 | 性质 | 归属任务 |
|---|---|---|---|---|
| P1 | `ROADMAP.md:53` | `**新路线：元内核自举**——无 Linux 依赖，DRM/KMS 直接渲染。` | 叙述句·历史块 | **本轮待裁（不动）** |
| P2 | `advisor_brief.md:31` | `→ 改**元内核自举**：`no_std` → GRUB/Limine → 最简内存 → **DRM/KMS 直渲** → field-render 作首个用户态进程 → …` | 叙述句·历史块 | **2.1 只出稿** |
| P3 | `advisor_brief.md:235` | `第一里程碑是"**能在 QEMU 里跑起来并按 DRM/KMS 输出场域画面**"。` | 叙述句·**§五 活跃段** | **2.1 只出稿** |

> **同族第二步（仓外）**：`CONTINUITY.md`（**仓库外**）L16／L152 仍写「⚠️ `ROADMAP.md:60/72` 口径未同步，**待裁**」——
> 该**状态陈述**在本轮实测下**已过期**（60/72 已同步）⇒ **R82／R87 家族**（状态陈述静默过期）。
> **处置**：本轮**不动**（仓库外文件，且属"只记录处置动作"范畴）；**列入晨报待放行项**。

---

## 3. 本轮待裁定（1 项，附七要素）

**议题**：`ROADMAP.md:53`（＋连带 `advisor_brief.md:31/235`）的旧口径「DRM/KMS 直接渲染」如何处置？

| 要素 | 内容 |
|---|---|
| **评估** | 三处**均非"虚假"而是"落后"**（C17：实现在前、文档未同步＝正常演进差）——**不得用诚信类定性词**（`CONSTRAINTS.md:355` 已立此先例）。 |
| **优势** | ① 清掉后，**全库"DRM/KMS 直渲"旧口径归零**，顾问/新人不会再读到"第一里程碑＝DRM/KMS"；② 与 L60／L72／L136 的修正**口径合一**（同源）。 |
| **劣势** | ① L53 属**带日期的历史修正记录**，改写＝违 **D52／D54**（历史段只标注）；② 需**动治理文件**，越过本轮约束。 |
| **风险** | **不做**：顾问若从 `advisor_brief:235` 恢复上下文，会把 **DRM/KMS 当现行主线**（误导决策）；**做**：若方式选错（改写而非标注），损失历史留痕（C19）。 |
| **机会** | 可**一次建立"2.4 旧口径清扫"判据**（`grep -c "DRM/KMS 直渲"` 归零），其**可证伪**：改后该计数＝0 且 L60／L72／L136 修正文不动。 |
| **建议** | **①（推荐）**：`ROADMAP:53` 只**追加括注**（不改原文）——「（★ 2026-09-19 再修正：**2.4 口径＝轻量帧缓冲 ＋ L6 呈现层接入**，DRM/KMS 降为远期可选 —— 见本表 2.4 行）」；`advisor_brief:31/235` 按 2.1 核对稿**只加"当时快照"标注**。**②** 全部不动，只在台账留痕。**③** 改写（**不推荐**：违 D52／D54）。 |
| **等待决策** | 请你一句话：**L53 是否授权加标注**（①／②／③）。 |

---

## 4. 边界说明（四类）

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | 1.1 diff 实测；1.2 **全库 `grep` ＋ `git blame` 实测** |
| **CI 过** | **不适用**（本轮零代码、零 workflow 改动） |
| **真实跑过** | **不适用**（无 boot／内核侧改动） |
| **未验证** | ① `CONTINUITY.md` 属**仓库外**，本机可读但**不受仓库判据覆盖**；② L53 处置**未执行**（待裁） |

## 5. 三量台账

| 量 | 内容 |
|---|---|
| **存量** | `ROADMAP.md`（L53／L60／L72／L136）｜`advisor_brief.md`（L31／L235）｜`CONTINUITY.md`（仓外，L16／L152）｜核对稿 §0 |
| **变量** | 核对稿 §0：`状态型 6→7`、`一句话 7→8`（**＋5／−2**） |
| **补充增量** | **口径裁定**：活跃段需订正计数＝数值型＋状态型，同源镜像不计 |
| **创新增量** | **系统性发现**：口径修正只到"表格行"、"叙述句"存续 ⇒ 建议升为**清扫判据**（可证伪） |

> **保密**（C12）：本记录 **🔓 公开**；**⛔ 机密 0 处**（无密钥／账号／本机绝对路径／内网 IP）；**🔐 算法层 0 处**。
> **底图引用**（D50／D52）：本记录**读底图的地方** ＝ `README.md` L108–L120（仓库结构）——作为 1.2 对照的旁证；**结论主体依据为 `coordination/` 台账与 `git` 实测**。
