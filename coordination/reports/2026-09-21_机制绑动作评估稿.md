【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 机制绑动作评估稿（2026-09-21 · v0.307 轮）

> **本稿性质：只出稿**。**不改任何治理文件**（`CHARTER.md`／`TEMPLATES.md`／`CONSTRAINTS.md` 一字不动）。
> **评估对象**：机制 **1–27**（`CHARTER.md §三`）＋ **T-001–T-041**（`TEMPLATES.md §7.2`）＋ **C1–C21**（`CONSTRAINTS.md`，附表）。
> **产出依据**：`CHARTER.md §三` 逐条 ＋ `TEMPLATES.md §7.2` 逐条 ＋ `CONSTRAINTS.md` 逐条（**逐条回读真实文件，非凭记忆**）。

---

## 〇、背景：机制为什么"写了不生效"

**机制专家的核心认知**（本轮指令 §4.1 原话）：

> **机制不是靠"写规则"起作用，是靠"把规则绑到具体动作+触发+判定"起作用。**

本项目已有的实证支持这一判断 —— **三条最贵的教训全是"规则写了、没绑动作"**：

| 实证 | 规则写了 | 绑动作了吗 | 结果 |
|---|---|---|---|
| **R14**（本机绿是假绿） | "关键功能必须真实环境验证"（C7） | ❌ 未说明"在哪一层验" | 本机 PASS／CI FAIL，**得出相反结论** |
| **R89**（声明 ≠ 发生） | "push 后确认 CI" | ❌ 未说明"确认到什么状态才能落笔" | 两轮写"三 job 全绿"时 CI 仍 `in_progress` |
| **R84**（版本号 −5 漂移） | 底图"每次迭代累加 1" | ❌ 未说明"哪一次算一次" | 14 条提交**持续偏移 −5** |

⇒ **本稿要回答的就是一件事：现有 68 条规则里，哪些真的绑上了动作？**

---

## 一、三条判据与三档分类（**逐条对齐指令 §4.2**）

| 判据 | 内容 | 判定标准（**可检查**） |
|---|---|---|
| **J-动 绑动作？** | 规则是否指向**具体动作** | ✅＝写明"**必须做 X**"（可执行动词 ＋ 对象）；❌＝只在说"应当／可以／需注意" |
| **J-触 绑触发？** | 是否明确**什么时候做** | ✅＝**时点或事件可判定**（"每轮开工时"／"push 后"／"≥N 时"）；❌＝"条件满足时"／"必要时"／"按需" |
| **J-判 绑判定？** | 是否**可机器检查** | ✅＝有**脚本／CI 断言／可复现命令**；△＝**人工可核对但有固定载体**（如 commit 前缀、固定表格）；❌＝无任何检查点 |

| 类 | 定义 |
|---|---|
| **A（三有）** | 三条全 ✅ ⇒ **可直接依靠** |
| **B（部分有）** | 缺一条或两条（多为 **缺 J-判**）⇒ **靠人守，会静默失效** |
| **C（三无，纯规则）** | 三条皆 ❌ ⇒ **写在纸上，不产生行为** |

---

## 二、总览（**统计**）

| 族 | 条数 | **A** | **B** | **C** |
|---|---|---|---|---|
| **机制 1–27** | 27 | **11** | **14** | **2** |
| **T-001–T-041** | 41 | **23** | **16** | **2** |
| **合计** | **68** | **34**（50.0%） | **30**（44.1%） | **4**（5.9%） |

**三条总体判断**：
1. **A 类集中在"有脚本的那一族"** —— 机制 20–25／27（术语／执行体／源一致性／夜间例外／端点表／文档一致性／版本号）**全部 A**；**判据建设与"三有"高度重合**。
2. **B 类几乎全是"缺 J-判"** —— 30 条 B 中 **28 条**卡在"无机器判据"；**其中"必带项"类（十四项／优先级／七要素）占了 6 条**，是**最大的一块空白**。
3. **C 类只有 4 条**，但**全部是"流程性质"的规则**（机制 8 断裂接续／机制 11 通用工作流／T-003 文件合并／T-006 三层说明）⇒ **纯流程规则天然难绑判据**，可接受；**但须标注"此条不可自查"**。

---

## 三、机制 1–27 逐条评估

> **规则原文**栏为**摘录**（全文见 `CHARTER.md §三`）；**触犯/失效记录**栏只写**有据可查**的实例，无记录的写"**未见记录**"（**不编造**，C16／C17 精神）。

| # | 规则原文（摘） | J-动 | J-触 | J-判 | 类 | 触犯／失效记录 | 改造建议 |
|---|---|---|---|---|---|---|---|
| **1** | 基准机制：`BASELINE.md` 是项目当前状态的**唯一权威描述**，**每版本更新** | ✅ 更新 BASELINE | ✅ 每版本 | ❌（**判据 F 只管底图，不管台账是否更新**） | **B** | **R87**：台账状态与结案记录脱节（R84 表行仍写"待裁"而同文件 §23.1 已结案） | ① 报告固定一行「**台账更新：§__（本轮）**」；② 台账末新增「**最后更新于 HEAD**」行**已有**⇒补**机器可查**（本轮 HEAD 与台账行一致） |
| **2** | 约束机制：硬约束 **C1–C21** 不可破，破了须在报告说明 | ✅ 遵守 | ✅ 常驻 | △（**部分 C 有判据**：C15／C16／C18–C21；**部分纯原则**：C5／C7） | **B** | **R25**：历轮报告写「⛔ 已预屏蔽」而**事实不符**（C5/C7 破） | 把「C 族逐条是否可判」**列表化**（见 §五 附表）⇒ **无判据的 C 须在报告里显式承接** |
| **3** | 模板机制：指令**九必带**／报告**十四必带**，固定相不可破 | ✅ 按模板 | ✅ 每轮 | △（**只有第 0 项有脚本** `check_reading_instruction.py`；**其余 13 项无**） | **B** | 未见记录 | ★ **最有价值的一块**：十四项中**可结构检查的有 5 项**（①保密标记 ②状态卡 ③三量台账 ④边界说明 ⑨）⇒ **可机器化**（见 §六 P1-①） |
| **4** | 模板活相机制：**提议 → 记一行 → 试用 3 轮 → 生效或废弃** | ✅ 记入 §7.2 | ❌（"试用 3 轮"**无计时起点**） | ❌ | **B** | ★ **实测：14 条 T 条目仍标「🔬 试用中」**（T-003/004/005/006/007/008/011/012/013/014/015/016/020/021），而其承载机制**早已在 `CHARTER`／`CONSTRAINTS` 生效并逐轮执行** ⇒ **§7.2 状态列静默过期**（R69／R72 家族新实例） | ★ **补"复核时点"**：进入试用即记 **`试用起点（commit count）`**；**满 3 轮强制出一行结论**（生效／废弃），**不得留空** |
| **5** | 路线图机制：三阶段总路线图 ＋ 里程碑，每轮更新 | ✅ 更新 ROADMAP | ✅ 阶段变化时 | △（**判据 E 覆盖"README 四阶段↔ROADMAP 显式映射"**） | **B** | 连续 3 夜 **「P3＋可夜间跑 ✅」行全部不可执行（0 项）** | **"可夜间跑"列须给"可执行判据"**（不是标 ✅ 就算可跑）—— 否则夜间机制**空转** |
| **6** | 优先级机制：**每项任务必须标优先级**（P0–P4）；冲突排序 P0>P1>… | ✅ 标优先级 | ✅ 每轮 | ❌（**没有任何判据检查"报告是否每项带 P 级"**） | **B** | 未见记录 | 判据 J-判 可低成本补：**报告任务节必须匹配 `P[0-4]`**（正则可查） |
| **7** | 无响应推进机制：P1/P2 超 **4h** 无响应 → 转 P3 清单，不中断 | ✅ 转 P3 | ✅ 超 4h | ❌（**谁在计时、有没有转，无载体**） | **B** | 未见记录 | 报告固定一行「**超 4h 未响应项：N 条（编号）**」（**记"无"也算**，防省略） |
| **8** | 断裂接续机制：任何一方断裂后，读 `coordination/` 最新状态即可恢复 | ✅ 读最新状态 | ❌（"断裂后"**不可判定**） | ❌ | **C** | 未见记录 | 保留为**纯流程**；**但须显式标注"本条不可自查"**（避免误以为有保护） |
| **9** | 归档机制：指令／报告／讨论**全部归档**，不靠对话记忆存活 | ✅ 归档 | ✅ 每轮 | △（文件存在性可查，**无脚本**） | **B** | **v0.305 轮讨论稿"只出稿未入库"**，v0.306 才随提交入库 ⇒ **延迟一轮** | 判据：**报告的"本轮产物清单" vs `git status`** 对账（新增文件**当轮必须入库或显式标注"下轮入库"**） |
| **10** | unsafe 白名单机制：C9 门禁白名单（当前为空）＋ **CI 断言** | ✅ 登记白名单 | ✅ 出现 unsafe 时 | ✅ **CI 断言**（`transmute` 零出现 ＋ 白名单子集 ＋ `// SAFETY:` 覆盖数 ＋ 门禁自检） | **A** | **R10**（曾只有文字约定、无 CI 断言）⇒ 已修 | 保持 |
| **11** | 通用工作流机制：**八步场域演化**，**按需调用**、分层保密 | △（八步是动作序列） | ❌（"按需"） | ❌ | **C** | 未见记录 | 保留为**能力清单**（非门控）；**显式标注"不属开工必读"**（`CHARTER §九` 已如此写 ⇒ 一致） |
| **12** | 顾问简报机制：`advisor_brief.md` **滚动摘要**（结论 ≤5 条） | ✅ 维护 | ✅ 触发词 ＋ **T-037 触发式** | ✅ **机制 25 覆盖其时效**（`P1/P2` 检查 `advisor_brief.md`） | **A** | **机制 25 建立后立即抓出 2 处过期点**（行 107 段号落后 2 段） | 保持 |
| **13** | 顾问接续包机制：断裂时生成**接续包（9 节）**，**待决策必摆七要素** | ✅ 生成接续包 | ✅ 触发词「顾问对话断了」 | ❌（**9 节是否齐全，无检查**） | **B** | 未见记录 | 接续包落盘后 **`grep -c "^## "` 应 ≥9**（低成本可机器化） |
| **14** | 决策评估机制：凡涉及决策**必须给七要素**（顾问侧） | ✅ 给七要素 | ✅ 涉及决策时 | ❌（**报告是否含七要素无判据**） | **B** | 未见记录 | 报告 **⑩ 节存在性 ＋ 七要素关键词齐全**可机器化（低成本） |
| **15** | 保密机制：**四级别**标记 ＋ 机密**绝不拷顾问** | ✅ 标级别 | ✅ 每次输出 | △（`check_private_pointers.py --mode=syntax` **入 CI** ＋ `leak_baseline.txt` 只减不增；**但"报告是否标了级别"无判据**） | **B** | ★ **R25**：报告每轮写"⛔ 已预屏蔽"，**与事实不符**（`docs/` 13 处内网 IP ＋ 6 处用户名仍在公网） | **补"报告第 ① 节存在性"判据**（与 ③ 同一批） |
| **16** | 私有资产盘点机制：五类独创资产 × 四级别；触发词「资产盘点」 | ✅ 盘点 | ✅ 触发词 | △（`ASSETS_INVENTORY.md:48` 存在；`check_private_pointers --mode=resolve` 可核指针） | **B** | 未见记录 | 把「触发词 → 动作」写入**报告动作清单**（触发词是动作源，但**执行与否无载体**） |
| **17** | 拆分机制：**骨架层（公开仓库）／实现层（仓库外）物理分离** ＋ `{PRIVATE_ASSETS}` 占位符 | ✅ 分离 | ✅ 新增内容须判归属 | ✅ **`check_private_pointers.py`**（`syntax` 入 CI／`resolve` 本机）＋ 泄漏基线**只减不增** | **A** | **R25**（本条即 R25 的处置）；**T-020 过阴阳自检**（非法 3 类全检出／合法 0 误报） | 保持 |
| **18** | 顾问阅读指令机制：**任何输出的字面第 0 行**放固定文本（**9 步**） | ✅ 第 0 行放 | ✅ 任何输出 | ✅ **`check_reading_instruction.py`** | **A** | 未见记录 | 保持 |
| **19** | 夜间自动化机制：**21:00 前出 P3 清单**／23:00 开工／**只跑 P3**／双通道 | ✅ 出清单＋跑 | ✅ 时段 | △（**commit 前缀可查**；**P3 清单"可执行性"无判据**） | **B** | ★ **连续 3 夜「P3＋可夜间跑 ✅」行全部不可执行（0 项）** ⇒ **清单在跑、内容为空**（**空转**）；**v0.305 轮"零提交且未写台账"** | ★ **补"清单有效性"判据**：清单须写明**本轮可执行项数**；**连续 N 夜为 0 ⇒ 判违规**（与 R83「空转须与通过取不同值」同族） |
| **20** | 术语三层映射机制：公开／索引／内容三层 ＋ **4 条机器判据** | ✅ 三层分离 | ✅ 新增／改术语 | ✅ **4 条判据**（密钥不在仓库／仓库内无密钥／密文可解不含原名／**错密钥必须被拒**） | **A** | **T-021 → T-022** 口径被用户改判（不加密 → 加密）；**T-023** 收紧指纹不入库 | 保持 |
| **21** | 执行体归属机制：需感知/动作 ⇒ 宿主；纯算 ⇒ 内核 | ✅ 判归属 | ✅ 新增代码时 | ✅ **`check_kernel_purity.py`**（入 CI；7 类命中＝白名单，**只减不增**）＋ 4 侧自检 | **A** | **T-024 建立当场由 C15 抓到 `CHARTER` 机制 19 行丢失**（R36） | 保持 |
| **22** | 源一致性机制：**底图（根 `README` ＋ `docs/`）＝唯一权威** | ✅ 以底图为准 | ✅ 改底图／改入口 | ✅ **判据 A–F**（入 CI）＋ 底图哈希基线 36 份**只增不改** | **A** | ★ **v0.191 首次上 CI 即判红**（本机绿是假绿）；**T-034** 曾写"README 无机器保护"⇒ **被实测推翻**（R87(b)） | 保持 |
| **23** | 夜间时段例外机制：C14 只约束**无人监督的自动化任务** | ✅ 用前缀区分来源 | ✅ 夜间改治理文件／push | △（**`git log` 前缀可查** ＋ 报告记录义务） | **A** | **首判例（2026-09-18 00:37–06:03）**：字面越界但**归属豁免** | 保持（**△ 与 ✅ 的界线已由"可复现命令"满足**） |
| **24** | 端点表一致性机制：`http.rs` 每个 `/v1` ⊆ `LAYER §4.L3` 权威表 | ✅ 对表 | ✅ 新增路由时 | ✅ **`check_endpoint_table.py`** ＋ 白名单只减不增 ＋ **5 侧自检** | **A** | ★ **R64**：锚点常量多写一字、**夹具与常量同错** ⇒ **自检全绿、实跑判红** ⇒ 催生 **C18** | 保持 |
| **25** | 文档一致性机制：文档说的状态 **＝** 机器实测的状态 | ✅ 核对 | ✅ 每次改文档 | ✅ **`check_doc_consistency.py`**（P1–P4，**P4 已判红**；**8 侧自检**） | **A** | ★ **R69**（模块文档不实）／**R72**（4 处过期 ＋ 同文件自相矛盾）／**R73**（判据把自己刚登记的 T-032 判红）／**R79**（同源口径） | 保持 |
| **26** | 能力矩阵机制：**四层（M／CB／CQ／H）** 逐层登记 ①能做什么 ②判据 ③正反对照；**跨层引用＝无效** | ✅ 逐层登记 | ✅ 产生"绿"结论时 | ❌（**明写"不新增门禁脚本，先立矩阵"**，由**报告第 ⑨ 项人工承载**） | **B** | ★ **R14／R23**（本机绿是假绿）—— **本条正是其处置** | **3 轮后评估**：把"报告是否含四层边界说明"结构检查化（与 ③ 同批） |
| **27** | 版本号口径机制：**版本号 ＝ `git rev-list --count HEAD`**，一次提交一个号 | ✅ 取 count | ✅ 每次写版本号 | ✅（**`label == count` 可复现**；**禁止排除某类提交**） | **A** | ★ **R84**：自 count 230 起**一致偏移 −5**（14 条）；**四规则假设检验全否证** | 保持 |

---

## 四、T-001–T-041 逐条评估

> **口径说明（★ 重要，防"重复计分"）**：`TEMPLATES.md §7.2` 本质是**变更记录**（每条记"某次规则变更"），**不是机制表**。
> ⇒ 同一机制可能横跨多条 T（如机制 17 → T-012／T-020）；评估对象＝**该条所固化的规则**，**与机制族存在多对一映射**。
> ⇒ **统计时已把"与机制族重复"的条目按同一结果归类**（不人为抬高 A 类占比）。

| # | 规则原文（摘） | J-动 | J-触 | J-判 | 类 | 触犯／失效记录 | 改造建议 |
|---|---|---|---|---|---|---|---|
| **T-001** | 固定相：指令八必带／报告十一必带 | ✅ | ✅ | △（仅第 0 行有脚本） | **B** | 已被 **T-013** 升级（八→九、十一→十四） | 见 机制 3 |
| **T-002** | 新增 §〇 固定相；§三/§四/§五 拆为独立小节 | ✅ | ✅ | △ | **B** | 起因＝**报告缺项**（三量台账/边界说明）无人拦 | 见 机制 3 |
| **T-003** | 三文件（TEMPLATES＋CHANGELOG＋OBSERVATIONS）**合并为一个** | ✅（已合并） | ❌（一次性） | ❌ | **C** | ★ **状态仍标「🔬 试用中」**（本条本身即 §一-4 的实例） | **一次性变更不应挂"试用中"** ⇒ 改用「**已执行·不适用试用**」状态 |
| **T-004** | **启动回执**：读完 CHARTER 必回执，不回执＝未读 | ✅ 回执 | ✅ 每轮 | ❌（**回执无判据**） | **B** | ★ 状态仍标「🔬 试用中」 | 报告 **⑬ 节存在性**可机器化 |
| **T-005** | **C10 动作约束**：对外发布/切换/不可逆 ⇒ 须用户确认 | ✅ 报备 | ✅ 四类动作 | ❌（**"有没有偷偷做"不可机器检查**） | **B** | **机制 23 首判例**（夜间 push 字面越界）；状态仍标「🔬 试用中」 | 报告固定一行「**本轮对外动作：N 处（或"无"）**」 |
| **T-006** | `CONSTRAINTS.md`／`ROADMAP.md` 加"固定相-活相-观察相"说明 | ✅ | ❌ | ❌ | **C** | 状态仍标「🔬 试用中」 | 一次性；同 T-003 |
| **T-007** | **C11 决策评估机制** ＋ §九 模板 | ✅ | ✅ | ❌ | **B** | 状态仍标「🔬 试用中」 | 见 机制 14 |
| **T-008** | **C12 保密机制** ＋ 报告「保密标记」字段 | ✅ | ✅ | △ | **B** | ★ **R25**（声明与事实不符）；状态仍标「🔬 试用中」 | 见 机制 15 |
| **T-009** | C11 扩**七要素**（"优势≠机会"） | ✅ | ✅ | ❌ | **B** | ✅ 已生效 | 见 机制 14 |
| **T-010** | **优先级纳入规则**（指令 8→9／报告 12→13） | ✅ | ✅ | ❌ | **B** | ✅ 已生效 | 见 机制 6 |
| **T-011** | **私有资产盘点机制**（机制 16） | ✅ | ✅ | △ | **B** | 状态仍标「🔬 试用中」 | 见 机制 16 |
| **T-012** | **拆分机制**（机制 17）＋ `PRIVATE_ASSETS` | ✅ | ✅ | ✅ | **A** | 起因＝**R20 仓库 PUBLIC**；**T-020 补判据** | 保持 |
| **T-013** | **顾问阅读指令机制**（机制 18）→ 报告**十四项** | ✅ | ✅ | ✅ | **A** | 状态仍标「🔬 试用中」（**而本条已逐轮执行数十轮**） | 保持 ＋ **§一-4 状态复核** |
| **T-014** | **夜间自动化机制**（机制 19）＋ `ROADMAP`「可夜间跑」列 | ✅ | ✅ | △ | **B** | 状态仍标「🔬 试用中」；★ **连续 3 夜清单 0 项可执行** | 见 机制 19 |
| **T-015** | §11.7 **双通道** ＋ §11.8 **夜间安全边界**（默认不 push） | ✅ | ✅ | △（双通道可查；默认不 push 可查 commit） | **A** | 见 机制 19 | 保持 |
| **T-016** | **C13 扩为"任何输出的第 0 行"** | ✅ | ✅ | ✅ | **A** | 状态仍标「🔬 试用中」 | 保持 |
| **T-017** | **C1 界线举例**（`core`/`alloc` 属标准分发；用 `alloc` ≠ 放开 unsafe） | ✅ | ✅ | ❌（**"内核是否引第三方 crate"无独立判据**） | **B** | ✅ 已生效（D34-b） | 补 `Cargo.toml` **零第三方依赖**判据（低成本） |
| **T-018** | **阅读指令 8 步 → 9 步**（第 9 步＝对齐用户意图） | ✅ | ✅ | ✅ | **A** | 起因＝十四项齐全**但答偏问题**的报告会全过 | 保持 |
| **T-019** | **C15 编号表编辑纪律**（编辑前后 `grep -c` ＋ 连续性） | ✅ 双向取数 | ✅ 编辑编号表时 | ✅（**差值须与预期一致 ＋ 连续性**；**可复现**） | **A** | ★ **生效当场抓到** `CHARTER` 机制 19 行丢失（R36） | 保持 |
| **T-020** | 机制 17 第二实例 ＋ **`check_private_pointers.py`** ＋ **泄漏基线** | ✅ | ✅ | ✅ | **A** | 起因＝**R25 声明与事实不符**（C5/C7 意义上更严重） | 保持 |
| **T-021** | 术语双层映射（**不加密、靠物理隔离**） | △ | △ | △ | **B** | **T-022 被用户改判为加密**（"与 T-021 相反的一处"） | 已被取代；**原条保留留痕**（C19）即可 |
| **T-022** | 机制 20 升级：**三层 ＋ AES-256-GCM** ＋ 4 条判据 | ✅ | ✅ | ✅ | **A** | 引入**密钥管理**与**不可逆风险**（已拆 `purge`，默认不执行） | 保持 |
| **T-023** | **密钥指纹不入仓库**（文档一律写 `〈本机私记〉`） | ✅ | ✅ | ✅（检查器新增 **[6] 判据**） | **A** | 起因＝C12 定为"可拷"与本轮收紧"不入仓库"**冲突** ⇒ **以本轮为准** | 保持 |
| **T-024** | **执行体归属机制**（机制 21）＋ 判据 ＋ 白名单 | ✅ | ✅ | ✅ | **A** | ★ **登记当场抓到机制 19 行丢失**（R36） | 保持 |
| **T-025** | **C16**：断言"不存在"须给**四要素** | ✅ | ✅ | ✅（**四要素齐备可查，缺一即降级**） | **A** | 起因＝**一轮推翻 3 条结论**（同根：只在 1 份文件检索就下"全库"结论） | 保持 |
| **T-026** | **C17**：诚信类指控须**穷举 ＋ 逐字引用** | ✅ | ✅ | ✅ | **A** | 起因＝**L4-M1 曾被定性为"底图虚假声明"，实为未穷举** | 保持 |
| **T-027** | **源一致性机制**（机制 22，**补登记**） | ✅ | ✅ | ✅ | **A** | ★ **本条本身是"补登记"**：脚本与 CI 建于 v0.191，**机制表当时漏登** ⇒ **判据尚无"机制表登记"一条（已知缺口）** | ★ **补判据**：机制表中**每个机制须有登记行**（与 `check_id_set_diff` 的 MECH 族同源） |
| **T-028** | **夜间时段例外机制**（机制 23）＋ C14 例外条款 | ✅ | ✅ | △→✅ | **A** | 首判例（字面越界但归属豁免） | 保持 |
| **T-029** | **端点表一致性机制**（机制 24）＋ **附 R60 前置提示** | ✅ | ✅ | ✅ | **A** | ★ **第 ⑤ 侧来历**：锚点常量多写一字、**夹具与常量同错** | 保持 |
| **T-030** | 底图条文补充：**§4.L3 收敛为全库唯一权威端点表**（23 路径） | ✅ | ✅ | ✅（**由机制 24 覆盖**） | **B** | 起因＝**实测 28 条路由 / 23 路径，而最全口径只覆盖 8 个**（差集 15） | 保持 |
| **T-031** | **C18**：判据自检须与实跑**同源** ＋ **回读真实仓库** | ✅ | ✅ | ✅ | **A** | ★ **R64**：自检全绿、实跑判红 | 保持 |
| **T-032** | **文档一致性机制**（机制 25） | ✅ | ✅ | ✅ | **A** | ★ **R69／R72**；**首次实战立即抓出 `advisor_brief` 2 处过期点** | 保持 |
| **T-033** | **C19**：同一机器事实**只许一份口径实现** | ✅ | ✅ | ✅ | **A** | ★ **R79**：同一状态**两个数**（56 vs 47）并给出看似相反的结论 | 保持 |
| **T-034** | 底图裁定：**5 处「心脏/大脑」→「心 → 大脑」** | ✅ | ✅（裁定触发） | ✅（**判据 F ＋ 汇编反向自检 `TOTAL_BAD = 0`**） | **A** | ★ **本条行末曾挂"README 无机器保护"的登记，v0.247 实测已推翻**（R87(b)）⇒ **未读源码的推断被写成登记**（R82 家族） | 保持；**教训已固化**（先读源码再登记） |
| **T-035** | **能力矩阵模板**（机制 26 的载体） | ✅ | ✅ | ❌（**明写不新增门禁脚本**） | **B** | ★ **R14／R23**（本机绿是假绿） | 见 机制 26 |
| **T-036** | **版本号口径机制**（机制 27）＋ **C21** ＋底图行 89 修订 | ✅ | ✅ | ✅ | **A** | ★ **R84**（−5 偏移 14 条；四规则全否证） | 保持 |
| **T-037** | **§11.3 复扫类改触发式**：触发＝对象有改动 **或** 重大状态变化 | ✅ 触发才跑 | ✅ **可判定**（哈希变化／`docs/` 有新文档／阶段或编号变化） | ✅（**未触发即不跑 = 可核对**） | **A** | 起因＝**产出递减**（`advisor_brief` 复扫**连续两轮无新增过期项**） | 保持 |
| **T-038** | **§十五 CI 结论落笔检查点**：push 后**须等 `completed`** 才落笔 | ✅ push 后查 run | ✅ **push 动作完成**（时点明确） | ✅（**run 状态可查 ＋ 落笔时点可查**；门控证据 `READY_TO_EDIT`） | **A** | ★ **v0.294／v0.295 两轮均在 `in_progress` 时先写"三 job 全绿"**（事后实测一致，但**方法违规**） | 保持 |
| **T-039** | **§十六 行号引用格式**：须写 `符号名（文件:行号）` | ✅ 统一格式 | ✅ 引用行号时 | ❌（**明写"零成本、无需判据脚本"**） | **B** | **R75**（两处行号数错）；**v0.284 汇编 diff 232 行**（L4 锚另起一行） | ★ **可低成本补判据**：报告/台账中出现**裸数字行号**（无 `符号名（…）` 包裹）⇒ 告警 |
| **T-040** | **§十七 引用完整性门控**：选项标签须**在同一文本内定义** | ✅ 落笔前检查 | ✅ 出现选项标签时 | ❌（**人工**：报告开头单列缺失清单） | **B** | ★ **"上线即自证"**：该轮指令通篇用 `L77`／`L78`，**仓库无 L 编号表** | ★ **可机器化**：扫描形如 `A1–A4`／`L77` 的标签，**未见定义表即告警** |
| **T-041** | **§十八 告警扫描范围限定**：扫描前**剔除 5 层** | ✅ 扫描前剔除 | ✅ **每次扫描** | ✅（**打印已剔除段落 ＋ json 清单 ＋ 11 侧自检 ＋ 范围口径写死**） | **A** | ★ **自指污染三轮实证 3→2→1→2**；v0.306 实测**剔除 782 行、告警 2→1** | 保持 |

---

## 五、附表：C1–C21 逐条可判性（**机制 2 的细化**）

> **为什么单列**：**机制 2 是"约束机制"，但它把 21 条硬约束打包成一个 B**。打包会**掩盖差异** —— 其中 **9 条有明确判据、12 条靠人守**。

| C | 一句话 | 有机器判据？ | 判据载体 |
|---|---|---|---|
| C1 | 内核零依赖 | ❌ | 无（`check_kernel_purity` 管感知/动作，**不管依赖**） |
| C2 | 版本号自动生成 | △ | 可复现命令 `rev-list`；**无脚本拦截手写** |
| C3 | 不用 unsafe 取巧 | ✅ | C9 门禁 ＋ CI 断言 |
| C4 | 改前先列清单 | ❌ | 无 |
| C5 | 诚实标注未完成项 | ❌ | 无（**R25 即此条失守**） |
| C6 | 三量台账 | ❌ | 无（报告结构可查，未机器化） |
| C7 | 不用 CI 通过代替真实运行 | ❌ | 无（**机制 26 是其处置**） |
| C8 | 硬约束可扩充 | ✅ | 流程（`§7.2` 记一行） |
| C9 | unsafe 边界形态 | ✅ | `unsafe_whitelist.txt` ＋ CI 断言 |
| C10 | 动作约束 | ❌ | 无 |
| C11 | 决策评估（七要素） | ❌ | 无 |
| C12 | 保密机制 | △ | `check_private_pointers`（**只管指针/泄漏基线，不管报告标记**） |
| C13 | 顾问阅读指令（9 步） | ✅ | **`check_reading_instruction.py`** |
| C14 | 夜间自动化 | △ | commit 前缀可查 |
| C15 | 编号表编辑纪律 | ✅ | **可复现**（`grep -c` 双向 ＋ 连续性） |
| C16 | 断言"不存在"须四要素 | ✅ | 可核对（四要素齐备性） |
| C17 | 诚信指控须穷举＋逐字引用 | ✅ | 可核对 |
| C18 | 判据自检同源＋回读真实仓库 | ✅ | **各判据 `--selftest`** |
| C19 | 同一机器事实一份口径 | ✅ | **`import` 复用 ＋ 输出打印口径** |
| C20 | CI 结论不入库 | ✅ | 可查（跟踪文件 `grep`） |
| C21 | 版本号口径唯一 | ✅ | **`label == count` 可复现** |

**统计**：**✅ 9 条**／**△ 3 条**／**❌ 9 条** ⇒ **9 条硬约束完全靠人守**。

---

## 六、共性缺陷与改造建议（**按优先级**）

### ★ 真实缺陷（**本轮评估中新发现 2 条 ＋ 复核确认 1 条**）

| # | 缺陷 | 证据（可复现） | 性质 |
|---|---|---|---|
| **F-1** | **`§7.2` 状态列静默过期**：**14 条**仍标「🔬 试用中」，而其承载机制**已在 `CHARTER` 生效并被逐轮执行数十轮** | `grep -c "试用中" coordination/TEMPLATES.md` ＝ **16**（其中 T 表行 **14**）；逐条：`T-003/004/005/006/007/008/011/012/013/014/015/016/020/021` | **R69／R72 家族新实例**（状态型内容静默过期）；根因＝**"试用 3 轮"无计时起点、无强制结论时点** |
| **F-2** | **夜间机制空转**：**连续 3 夜** `ROADMAP`「P3＋可夜间跑 ✅」行**全部不可执行（0 项）** | `ROADMAP.md`「可夜间跑」列 ＋ 三夜 P3 清单实测 | **R83 同族**（**空转须与"通过"取不同值**）；根因＝**"可夜间跑"列无"可执行性判据"** |
| **F-3** | **"必带项"类规则整体缺机器载体**：报告 **14 项**中**仅第 0 项**有脚本；**优先级／七要素／三量／边界说明／回执**均无判据 | `coordination/tools/` 现有 9 个判据脚本，**无一**检查报告结构 | **判据覆盖盲区**（`CHARTER §四` 已登记"盲区：编号/状态无判据"） |

### 改造建议（**P0／P1／P2**）

| 级 | 改造项 | 成本 | 收益 | 可逆性 |
|---|---|---|---|---|
| **P0-①** | **`F-1` 修法**：`§7.2` 增「**试用起点（commit count）**」列；**满 3 轮强制出结论行**（生效／废弃），**不得留空** | 低（改表格结构） | 高（**14 条状态一次性纠正 ＋ 从此不再静默过期**） | 高（纯表格） |
| **P0-②** | **`F-3` 修法**：新增报告结构判据（**只告警**，照 `check_id_set_diff` 先例）：检查报告是否含 **①保密标记 ②状态卡 ③三量四量 ④边界四类 ⑤启动回执 ⑥INTEL 状态表** | 中（1 个脚本） | 高（**一次覆盖 6 项必带项**） | 高（只告警、不判红） |
| **P1-①** | **`F-2` 修法**：P3 清单**须写明本轮可执行项数**；**连续 N 夜为 0 ⇒ 违规**（同时给"可夜间跑"列补可执行判据） | 低（清单格式） | 中高（**止住机制空转**） | 高 |
| **P1-②** | **T-039／T-040 补判据**（两条新增规则目前**均无判据**）：① 裸数字行号告警；② 选项标签无定义表告警 | 中 | 中（**把刚立的规则真正绑上动作**） | 高 |
| **P1-③** | **T-027 缺口补判据**：**每个机制须有登记行**（与 MECH 族差集**同源**） | 低（复用现有） | 中（**止住"脚本建了、机制没登"复发**） | 高 |
| **P2-①** | **C 族无判据 9 条**（C1／C4／C5／C6／C7／C10／C11）逐步机器化（**从低成本者起**：C1 依赖检查、C6 三量结构） | 中 | 中 | 高 |
| **P2-②** | **一次性变更不得挂"试用中"**（T-003／T-006 类）：改用「**已执行·不适用试用**」 | 极低 | 低（**但能防 F-1 复发**） | 高 |

---

## 七、待裁定

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | **`F-1`（14 条试用中）是否本轮即纠正**（改 `§7.2` 状态列） | **推荐：是，但**列**次日授权**（本轮红线＝**只改 BASELINE**，`TEMPLATES` 不动） |
| ② | **P0-② 报告结构判据是否立项**（先只告警） | **推荐：立项**；照 **T-040 先例**（**先只告警、不入 CI、观察 N 轮**） |
| ③ | **`P2-②` 是否采用"已执行·不适用试用"这一新状态名** | **推荐：采用**（**新状态名须先定义再使用**，防"自造状态"） |
| ④ | **本评估稿是否入 `discussions/`**（属**评估稿**，非裁定记录） | **推荐：留 `reports/`**（与《抽象铁律评估稿》同处） |
