【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-23 夜间 P3-3 · `docs/` 时效核对批（触发式 · 本轮已触发）—— **核对稿**

> **来源**：`coordination/reports/2026-09-23_今夜P3清单.md` · **§一 P3-3**；触发条件已由清单侧实测成立（`git log --diff-filter=A -- docs/*.md` ⇒ **09-23 新增 8 份**）。
> **性质**：**只读出稿** —— 本件**不改底图正文**（底图改动须走 `D16'` 五段留痕，留白天）。**本件即"核对稿"**（清单 ④ 所要求）。
> **依据**：`TEMPLATES.md` §11.3「★ 复扫类（触发式）」（`T-037`）＋ `BASELINE.md` §34.4／§35.4 ＋ `T-052`／`T-053`／`T-058`。

## ① 保密标记（C12）

- ⛔ 机密（绝不拷）：**无** —— 本件不含本机绝对路径、不含凭证／账号／网络细节、不含参数库取值。
- 🔐 保密（屏蔽后拷）：**无**。
- 🔒 半公开（可拷）：仓库内相对路径与行号、区块编号、`R`／`D`／`T`／`C`／机制编号、计数口径。
- 🔓 公开（直接拷）：版本号、行数、🟡 计数、结论。

> 【给用户的提示】本件含 **0** 处机密、**0** 处保密，**无需屏蔽即可整份拷给顾问**。

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡（本件时点）────┐
│ 版本   v0.364（本机 rev-list --count）  │
│        HEAD 86b6c6c ＋本夜 P3-1/P3-2    │
│ 阶段   ROADMAP 阶段二 · 子任务2         │
│ 轮次   当前轮 = v0.332                  │
│        （BASELINE 末顶层节 §六十八）    │
│ CI     本夜未触发（按 C20 不入库）      │
│ 底图   44 份（README + docs/ 43）       │
│ 新增   docs/ 09-23 新增 8 份（本件对象）│
└─────────────────────────────────────────┘
```

## ③ 任务 · 状态 · 优先级

| 项 | 内容 |
|---|---|
| 任务 | 对 **09-23 新入 `docs/` 的 8 份稿**逐份做时效核对：① 内部"最新／当前／待裁／已结／性质"类陈述 vs `BASELINE` 最新节 ② **🟡 计龄** ③ 是否含 **`C12` 级别声明字段** ④ 出**核对稿** |
| 基准 | **v0.364**（**轮次 v0.332**） |
| 定位 | ROADMAP **阶段二 · 子任务2** |
| 优先级 | **P3** |
| 状态 | **完成** |

**受核对对象（8 份 · 实读行数）**

| # | 文件 | 行数 | 首次入库 | 性质（本件实读） |
|---|---|---|---|---|
| 1 | `LAYER_DETAILS.md` | 626 | 09-23（`e8aff8c` 拆文件） | **底图**（**无「性质」头** —— 见 §一-4） |
| 2 | `DEFENSE_DESIGN.md` | 281 | 09-23（`1cdce73` 出稿 → 升格） | **底图**（v0.332 裁定⑥ 升格） |
| 3 | `ORIGIN_BROWSER_DESIGN.md` | 232 | 09-23（同上） | **底图**（v0.332 裁定⑥ 升格） |
| 4 | `ORIGIN_OS_DESIGN.md` | 276 | 09-23（同上） | **底图**（v0.332 裁定⑥ 升格） |
| 5 | `DNA_KERNEL_DESIGN.md` | 246 | 09-23（同上） | **出稿**（非底图） |
| 6 | `INCARNATION_DESIGN.md` | 280 | 09-23（同上） | **出稿**（非底图） |
| 7 | `QUANTUM_CLOCK_DESIGN_DETAIL.md` | 291 | 09-23（同上） | **出稿**（非底图） |
| 8 | `SELF_DIAGNOSIS_FRAMEWORK.md` | 268 | 09-23（同上） | **出稿**（非底图） |

---

## 一、① 内部时效型陈述 vs `BASELINE` 最新节（v0.332）

### 1-1 「性质」行与 `§68.5` 的一致性 —— **8/8 一致** ✅

| 文件 | 本件实读「性质」 | `BASELINE §68.5` 口径 | 结论 |
|---|---|---|---|
| `DEFENSE_DESIGN` | ★ 底图（v0.332 裁定⑥ 升格） | 升格 3 件之一 | ✅ |
| `ORIGIN_BROWSER_DESIGN` | ★ 底图（同上） | 升格 3 件之一 | ✅ |
| `ORIGIN_OS_DESIGN` | ★ 底图（同上） | 升格 3 件之一 | ✅ |
| `DNA_KERNEL_DESIGN` | 出稿（非底图） | 「未升格（下一批审阅）」 | ✅ |
| `INCARNATION_DESIGN` | 出稿（非底图） | 同上（🟡 中） | ✅ |
| `QUANTUM_CLOCK_DESIGN_DETAIL` | 出稿（非底图） | 同上 | ✅ |
| `SELF_DIAGNOSIS_FRAMEWORK` | 出稿（非底图） | 同上（🟢 低） | ✅ |

### 1-2 ★ **发现 A：2 处活跃陈述与 v0.332「裁定⑤」相抵**（**1 处在底图**）

| 处 | 文件:行 | 本件实读原文（节录） | 应与之一致的裁 |
|---|---|---|---|
| 1 | **`LAYER_DETAILS.md:120`**（**底图**） | `\| **R4「元内核」歧义消除** \| ✅ 保留（内核层侧）／⚠️ **系统名侧待裁** \| … **系统名**：仍称 **元内核系统**（**待发起人裁定**是否改名）。\|` | **v0.332 裁定⑤**：「元内核」**只作系统名**、**沿用原名** |
| 2 | `QUANTUM_CLOCK_DESIGN_DETAIL.md:273`（出稿） | `…★ **系统名「元内核系统」是否改名 ⇒ 待裁定**。` | 同上 |

**依据（裁定⑤ 落点，可复现）**：
- `CHARTER.md` 命名体系行（**组2 `f8e0c08` 同步后**）：`- ⚠️「元内核」只作**系统名**（**裁定⑤ · 2026-09-23 沿用原名**）`。
- `BASELINE §68.4`：`**"元内核"（系统名）按裁定⑤ 保留**`。

⇒ **性质**：**同一行内自相矛盾**（前半句已写"**仍称** 元内核系统"，后半句又写"**待裁定**是否改名"）＋ **与已生效裁定⑤ 相抵** ⇒ `R69／R72` 家族（**活跃陈述静默过期**）。
⇒ **⚠️ 加重情形**：**第 1 处落在底图**（`LAYER_DETAILS.md` 是**唯一权威**层）⇒ **不只是文档滞后，而是底图内出现"已裁事项仍标待裁"**。**本件只登记，不改**（底图改动须走 `D16'`）。

### 1-3 「待裁与未决」小节齐备度

| 文件 | 小节 | 处/行 | 条目数 |
|---|---|---|---|
| `DEFENSE_DESIGN` | ✅ §5 待裁与未决 | L247 | **8** |
| `DNA_KERNEL_DESIGN` | ✅ §7 | L213 | **9** |
| `INCARNATION_DESIGN` | ✅ §10 | L250 | **9** |
| `ORIGIN_BROWSER_DESIGN` | ✅ §7 | L197 | **9** |
| `ORIGIN_OS_DESIGN` | ✅ §5 | L219 | **9** |
| `QUANTUM_CLOCK_DESIGN_DETAIL` | ✅ §9 | L267 | **9** |
| `SELF_DIAGNOSIS_FRAMEWORK` | ✅ §10 | L239 | **9** |
| `LAYER_DETAILS` | ❌ **无独立小节** | — | — |

**口径一致性**：6 件出稿／升格件**均在文首明写统一口径**（`冲突处理口径：正文按最新发起人原话陈述并标注依据；与底图不一致处不在正文调和，逐字登记到 §N「待裁与未决」`）⇒ **口径自洽** ✅。

### 1-4 ⚠️ **发现 B：`LAYER_DETAILS.md` 缺「性质」头**（`TIER` 类声明字段缺项）

- **实测**：`LAYER_DETAILS.md` 全文 **无** `> **性质**：` 头（其余 7 份**均有**）；其 `性质` 一词仅出现在 §4.1.1 附近的"**四性质**（个体→网络→时间→种子）"—— **是同形异义**，不是声明字段。
- **对照**：同批拆出的另一件（`LAYER_ARCHITECTURE.md` 保留骨架）**属既有底图**；`LAYER_DETAILS.md` 是 `T-057`「**骨架-详述分离**」的**新产物**，**出生即缺该头**。
- ⇒ **登记**（是否补 `性质` 头属 P1，涉底图改动）。

---

## 二、② 🟡 计龄（8 份 · 合计 **138** 处）

### 2-1 逐份计数与语境分离

| 文件 | 🟡 总数 | 其中**效力语境** | 其中**进度语境**（`🟡 部分`／`🟡 N%`） | 「首次标记」 |
|---|---|---|---|---|
| `LAYER_DETAILS.md` | **0** | 0 | 0 | — |
| `DEFENSE_DESIGN.md` | **20** | 19 | 0 | **v0.330** |
| `DNA_KERNEL_DESIGN.md` | **16** | 16 | 0 | **v0.330** |
| `INCARNATION_DESIGN.md` | **20** | 20 | 0 | **v0.330** |
| `ORIGIN_BROWSER_DESIGN.md` | **17** | 16 | 0 | **v0.330** |
| `ORIGIN_OS_DESIGN.md` | **12** | 12 | 0 | **v0.330** |
| `QUANTUM_CLOCK_DESIGN_DETAIL.md` | **34** | 34 | 0 | **v0.330** |
| `SELF_DIAGNOSIS_FRAMEWORK.md` | **19** | 19 | 0 | **v0.330** |
| **合计** | **138** | **136** | **0** | — |

**与清单侧交叉核对** ✅：清单 §一 P3-3 记「`DNA_KERNEL_DESIGN` 16 ／ `INCARNATION_DESIGN` 20 ／ `SELF_DIAGNOSIS_FRAMEWORK` 19 ／ `QUANTUM_CLOCK_DESIGN_DETAIL` 34」⇒ **本件实测 4 项逐一相符**（**阳性对照成立**）。

### 2-2 计龄结论

- **首次标记轮次**：8 份**全部为 `v0.330`**。
- **当前轮次**：`v0.332`（`BASELINE.md` **最后一个顶层节**＝`## 六十八、v0.332 轮 …` ⇒ 与 `check_effect_column.py` 的取数口径**同源**）。
- 按 **`T-053` 22.5** 逐字口径「以 🟡 **首次出现的轮次**为起点，**满 3 轮**（**含起点轮为第 1 轮**）」：`v0.330`（1）→ `v0.331`（2）→ `v0.332`（3）⇒ **本轮即"满 3 轮"** ⇒ **按条文应登记**。

### 2-3 ★ **发现 C：条文 `T-053` 与判据 `T-058` 的计龄口径相差 2 轮，且自检无法发现**

| 侧 | 口径 | 「首次 v0.330 ／ 当前 v0.332」判定 |
|---|---|---|
| **条文** `T-053`／`§22.5`（`TEMPLATES.md:1190` 段） | 满 3 轮，**含起点轮为第 1 轮** ⇒ 触发条件 **`当前 − 首次 ≥ 2`** | **触发**（应登记） |
| **判据** `check_effect_column.py` | `AGING_LIMIT = 3`；条件 **`cur - first > 3`**（即 **≥ 4**） | **不触发**（`332−330 = 2`） |

**代码自陈与实现互斥（同文件内可复现）**：
- `check_effect_column.py:68`：`AGING_LIMIT = 3   # 满 3 轮（含起点轮为第 1 轮）；★ 与 T-053 22.5 同源`
- `check_effect_column.py:153/167`：判定式为 **`cur - first > 3`**
⇒ **注释写"含起点轮为第 1 轮"，判据式却按"再挂 3 轮"实现** ⇒ **两读法相差 1–2 轮**；**无论取哪一读法，注释与实现均不自洽**。

**★ 实跑证据（本件已补做，`rc=0` 可复现）**

1. **判据实跑**（`python coordination/tools/check_effect_column.py`）：
   - 扫描范围 ＝ **`docs/**/*.md`**；**含效力表**的文件 ＝ **1 份**（`LAYER_ARCHITECTURE.md`，效力表 **1** 个）；
   - **当前轮次 v0.332 ／ 上限 3 轮**；**告警 4 条**，**全部**在 `docs/LAYER_ARCHITECTURE.md`（**行 177／274／297／308**），文本逐条为 **「🟡 挂满 4 轮未裁」（首次标记 `v0.328` ／ 当前 `v0.332` ＞ 上限 3 轮）**。
2. **判据自检**（`--selftest`）：**PASS（十侧：5 正例 ＋ 2 反例 ＋ 3 对照）**；其中 **侧⑨**（首次 `v0.100`／当前 `v0.328`）打印 **"挂满 228 轮"**；**侧⑩**（首次 `v0.326`／当前 `v0.328`）＝**不告警**；**侧⑦** 打印 **"含效力表文件 1 份 ／ 当前轮次 v0.332"**。
3. **本批 8 份的 🟡 是否在扫描范围内？** —— **在**（判据的"效力语境"含 **(b) 同行含「待复核」**，而 8 份的 🟡 **逐行均含「待复核」**）。⇒ 它们**不是被漏扫，而是被阈值挡住**：`332 − 330 = 2`，**不满足 `> 3`**。

**★ 判据自己的措辞也印证了口径差**：同一判据对 `diff = 4` 的表述是 **"挂满 4 轮"**（把 `diff` 直接当轮数）⇒ 其内部模型＝**起点轮不计作第 1 轮**；而条文 `T-053` 明文 **"含起点轮为第 1 轮"** ⇒ **两模型相差 2 轮**。

**自检为何测不出（`R64` 家族 · 夹具与常量同错）**：
| 自检侧 | 夹具 | `diff` | 实跑结果 | 在本件口径下的问题 |
|---|---|---|---|---|
| 侧⑨ | 首次 `v0.100` ／ 当前 `v0.328` | **228** | 告警（"挂满 228 轮"） | **远离边界** ⇒ 两种口径**均告警** ⇒ **测不出边界错** |
| 侧⑩ | 首次 `v0.326` ／ 当前 `v0.328` | **2** | **不告警**（标为"未满 3 轮·边界另一侧"） | ★ **该夹具把 `diff=2` 钉死为"不告警"** ⇒ **若按条文读法（`diff=2` 即满 3 轮），侧⑩ 本身就该判失败** ⇒ **自检把错误边界固化了** |

⇒ **后果（量级 · 实跑可核）**：**8 份稿的 136 处效力语境 🟡 已达条文阈值却零告警**；本轮 `check_effect_column` 报出的 **4 条**全为 `diff=4` 的另一批（首标 `v0.328`）。**完整的"4 条 vs `LAYER_ARCHITECTURE` 内 🟡 12 处"对账属 P3-4**，本件只登记该**口径差**。

### 2-4 方法附记（本件实测）

- ★ **bash `grep` 在本机对 emoji（`🟡`／`🔒` 等）不可靠** —— 同一批文件，`grep` 命中 **1** 行，Python（`encoding="utf-8"`）读命中 **20** 行 ⇒ **本类计数一律以 Python 读为准**（本件全部用 Python）。
- 本件**未改** `check_effect_column.py`（**判据改动留白天**，清单 P3-5 明写）。

---

## 三、③ `C12` 级别声明字段核对

### 3-1 全 `docs/`（43 份）三口径实测

| 口径 | 命中 | 说明 |
|---|---|---|
| **A 含密级符号**（`⛔／🔐／🔒／🔓` 任一） | **5 份** | `FULL_AUDIT_REPORT`／`INCARNATION_DESIGN`／`LAYER_ARCHITECTURE`／`LAYER_DETAILS`／`SELF_DIAGNOSIS_FRAMEWORK` |
| **B 真·级别声明字段**（含 `保密级别／密级／级别：／🔓公开／🔒半公开／🔐保密／⛔机密` 等字样） | **0 份** | ★ **全 43 份命中 0 行** |
| **C 符号的实际语义**（逐行定性） | — | 见 3-2 |

### 3-2 5 份命中件的符号语义逐行定性

| 文件 | 符号数 | 其中「**冻结**」语义 | 其中「**`C12`**」语义 | 结论 |
|---|---|---|---|---|
| `FULL_AUDIT_REPORT.md` | 1 | **1**（L60 `L8–L10=推演（🔒冻结）`） | 0 | **非 `C12`** |
| `INCARNATION_DESIGN.md` | 1 | **1**（L5 `L7–L10 处于 🔒 冻结口径`） | 0 | **非 `C12`** |
| `LAYER_ARCHITECTURE.md` | 3 | 2（L161／L174 冻结） | **1**（L290 `分层保密…（{PRIVATE_ASSETS}，C12 ⛔）`） | **含 1 处 `C12` 引用**（**是对"实现层/数据层"的说明，非本件级别声明**） |
| `LAYER_DETAILS.md` | 7 | **7**（L428／456／468／524／530／547／613） | **0** | **非 `C12`** |
| `SELF_DIAGNOSIS_FRAMEWORK.md` | 8 | **8** | 0 | **非 `C12`** |

### 3-3 ★ **发现 D：《补全表》「真声明级别仅 2 份」在本件口径下不可复现**

- **《补全表》原文**（`2026-09-23_接续包核验补全表…md` L97／L172／L261）：`43 份全量；命中 5 份（其中 2 份符号属"冻结点"语义，非 C12）⇒ 真声明级别的底图仅 2 份`，点名 **`LAYER_ARCHITECTURE.md`／`LAYER_DETAILS.md`**。
- **本件实测**：命中 **5 份** ✅ **与《补全表》相符**；但——
  - **`LAYER_DETAILS.md` 的 7 处符号 7/7 全为「冻结」语境**（**无一处 `C12`**）；
  - `FULL_AUDIT_REPORT.md`／`INCARNATION_DESIGN.md`／`SELF_DIAGNOSIS_FRAMEWORK.md` 亦**全为冻结**；
  - 全库**仅 1 处**真正带 `C12` 字样（`LAYER_ARCHITECTURE.md:290`），且**不是该文件的级别声明**。
- ⇒ **两种可能**：① 《补全表》的"真声明"判据**未写明**（**不可复现**）；② 其"冻结点语义"分类与逐行实测不符（**5 份中有 4 份是冻结点语义，非 2 份**）。**本件不判定谁对**（`R32`）⇒ **记为"待核"**，并如实给出上表三口径实测值。

### 3-4 对本批 8 份的直接结论

| 文件 | 含 `C12` 级别声明？ |
|---|---|
| `LAYER_DETAILS.md` | **否**（7 处符号全为冻结语义） |
| `DEFENSE_DESIGN.md` | **否**（0 处符号） |
| `ORIGIN_BROWSER_DESIGN.md` | **否**（0 处符号） |
| `ORIGIN_OS_DESIGN.md` | **否**（0 处符号） |
| `DNA_KERNEL_DESIGN.md` | **否**（0 处符号） |
| `INCARNATION_DESIGN.md` | **否**（1 处符号，冻结语义） |
| `QUANTUM_CLOCK_DESIGN_DETAIL.md` | **否**（0 处符号） |
| `SELF_DIAGNOSIS_FRAMEWORK.md` | **否**（8 处符号，全冻结语义） |

⇒ **8 份 0 份含 `C12` 级别声明**；★ **且 3 份已升格为底图**（`DEFENSE`／`ORIGIN_BROWSER`／`ORIGIN_OS`）⇒ **升格为底图并未同步补 `C12` 级别声明**。
⇒ 与《补全表》**⑪-③**（"4 份未升格稿是否补 `C12` 级别声明"）**同源，且实测范围更大**：**不止那 4 份，本批 8 份（含 3 份新底图）全缺**。

---

## 四、④ 核对稿 —— 逐份核对结论（本件即出稿，**未改底图正文**）

| # | 文件 | ① 时效型陈述 | ② 🟡 计龄 | ③ `C12` 级别 | 一句话 |
|---|---|---|---|---|---|
| 1 | `LAYER_DETAILS.md` | ⚠️ **L120 过期**（裁定⑤ 相抵） | 0 处 | 否 | **需修 L120**（底图 ⇒ 走 `D16'`）＋ **补性质头** |
| 2 | `DEFENSE_DESIGN.md` | ✅ 一致 | 20（首标 v0.330） | 否 | 达标但**效力🟡 挂满 3 轮** |
| 3 | `ORIGIN_BROWSER_DESIGN.md` | ✅ 一致 | 17（首标 v0.330） | 否 | 同上 |
| 4 | `ORIGIN_OS_DESIGN.md` | ✅ 一致 | 12（首标 v0.330） | 否 | 同上 |
| 5 | `DNA_KERNEL_DESIGN.md` | ✅ 一致 | 16（首标 v0.330） | 否 | 与 §68.5「未升格」口径一致 |
| 6 | `INCARNATION_DESIGN.md` | ✅ 一致 | 20（首标 v0.330） | 否 | 同上 |
| 7 | `QUANTUM_CLOCK_DESIGN_DETAIL.md` | ⚠️ **L273 过期**（裁定⑤ 相抵） | 34（首标 v0.330） | 否 | **需修 L273**（出稿 ⇒ 无须 `D16'`） |
| 8 | `SELF_DIAGNOSIS_FRAMEWORK.md` | ✅ 一致 | 19（首标 v0.330） | 否 | 同上 |

---

## 五、验收对照

| # | 验收标准（清单 P3-3 原文拆解） | 实测 | 结论 |
|---|---|---|---|
| 1 | ① 内部"最新／当前／待裁／已结／性质"类陈述与 `BASELINE.md` 最新节是否一致 | **逐份核对**：性质 **8/8 一致**；发现 **2 处过期**（发现 A）；待裁小节 **7/8 齐备**（发现 B） | ✅（**2 项发现**） |
| 2 | ② **🟡 计龄**（清单给定 4 项基线值） | **8 份 138 处**（效力 136）；**4 项基线值逐一相符**；首标**全为 v0.330** ⇒ 本轮**满 3 轮**；★ **发现 C**（条文 vs 判据差 2 轮） | ✅ |
| 3 | ③ 是否含 **`C12` 级别声明字段**（对照《补全表》"真声明 2 份"） | 三口径实测：**含符号 5 份**／**真声明字段 0 份**；★ **发现 D**（《补全表》数值不可复现，记"待核"） | ✅ |
| 4 | ④ 出**核对稿**（只出稿，**不改底图正文**） | 本件 §四 即核对稿；**底图一字未改** | ✅ |
| 5 | 触发条件（`T-037`：对象有改动 或 重大状态变化） | 清单侧已实测 09-23 新增 8 份 ⇒ **触发成立** | ✅ |
| 6 | 报告落 `coordination/reports/` | 本件 | ✅ |

---

## 六、未完成项

| # | 未完成 | 原因 |
|---|---|---|
| 1 | **未改任何底图正文**（`LAYER_DETAILS:120` 等 2 处过期） | 底图改动须走 **`D16'` 五段留痕** ⇒ **属白天** |
| 2 | **未改 `check_effect_column.py` 的阈值** | **判据改动留白天**（清单 P3-5 明写）；本件**只报口径差** |
| 3 | **未穷究《补全表》"真声明 2 份"的判据** | **原始判据未在其件内写明** ⇒ 无法复现 ⇒ 记"待核"（`R32` 不判定） |
| 4 | **未核对 8 份中"非时效型"技术陈述**（如数值、模块数） | **超出本件范围**（属机制 25／`check_doc_consistency` 的覆盖域） |
| 5 | **未做 `SELF_DIAGNOSIS_FRAMEWORK` 与 `SELF_DIAGNOSIS_REPORT` 的口径互校** | 超范围（属待裁与未决第 5 条本身） |

---

## 七、真实缺陷

> 口径：**只登记不改**。

1. **底图内出现"已裁事项仍标待裁"**（`LAYER_DETAILS.md:120`）：与 **v0.332 裁定⑤「沿用原名」**相抵，且**同一行内自相矛盾**（前句"仍称"／后句"待裁定"）⇒ `R69／R72` 家族，**且落在唯一权威层**。
2. **效力🟡 的计龄口径条文与判据相差 2 轮**（`T-053` vs `T-058`/`check_effect_column.py`）：**条文本轮应触发，判据不触发** ⇒ **136 处效力语境 🟡 零告警**。
3. **自检夹具与常量同错**（`R64` 家族）：侧⑩（`diff=2` 钉为"不告警"）**使该口径差不可被发现**。
4. **`LAYER_DETAILS.md` 缺「性质」头**（同批 7 份均有）⇒ 出生即缺的声明字段缺口。
5. **《补全表》"真声明级别 2 份"不可复现**（本件三口径实测 5／0；5 份中 4 份为冻结语义）。

---

## 八、三量台账（C6）

| 量 | 内容 |
|---|---|
| **存量** | 8 份新增稿**全文逐行**（626+281+232+276+246+280+291+268 ＝ **2,500 行**）；`BASELINE.md` §六十八（68.1–68.10）与末顶层节轮次锚；`TEMPLATES.md` `T-052`／`T-053`／`T-058` 条文与 §22.5／22.6；`check_effect_column.py` 阈值实现（`AGING_LIMIT`／自检侧⑧⑨⑩）；全 `docs/` **43 份**密级符逐行扫描 |
| **变量** | **0** —— 未改任何既有文件（底图、治理文件、判据脚本、CI 一律未动） |
| **补充增量** | **1 份**：本核对稿 |
| **创新增量** | **3 条**：① ★ **条文／判据"计龄差 2 轮"的实证＋自检为何测不出**（`R64` 家族第 N 例）；② ★ **"含密级符"／"真声明字段"／"符号语义"三口径分离实测**（5／0／逐行），把《补全表》的"2 份"变成**可复现的三组数**；③ ★ **"底图内出现已裁仍标待裁"** 这一**新形态**（此前过期均在 `coordination/`，**首次落在底图**） |

---

## 九、边界说明（C5；四类齐全）

- **本地跑过** ✅：**Python 逐行扫描**（8 份 × 全文；全 `docs/` 43 份密级符／语义分离）；**Python 抽取**（性质头／待裁与未决条目数／首次标记）；`git log --diff-filter=A`／`--numstat`；`wc -l`；**判据全跑 15/15 rc=0**（P3-1 已跑）＋ ★ **本件补跑 `check_effect_column.py` 实跑（告警 4 条）与 `--selftest`（PASS 十侧）**；`grep -n` 条文与判据实现（`TEMPLATES.md`／`check_effect_column.py` 的 `AGING_LIMIT`／`aging_alerts`／自检夹具）。
- **CI 过** ➖：**无** —— 只读任务**未触发 CI**；按 `C20` **CI 结论不入库**。
- **真实跑过** ❌：**无** —— 未跑 QEMU、未上真机、未构建。
- **未验证** ❌（逐条）：
  1. **判据阈值是否"错"**：本件**实测了两侧行为**（条文读法应告警／判据不告警），但**未裁定谁为准**（`R32`）⇒ "谁是正解"**未验证**。
  2. **未改判据** ⇒ "改阈值后 8 份是否即告警"**未验**。
  3. **《补全表》判据未写明** ⇒ 发现 D **记"待核"，不判定**。
  4. **`LAYER_DETAILS.md` 的 `性质` 语义**只作"缺字段"登记，**未追 `T-057` 是否本就不要求该头**。
  5. **未做跨件互校**（如 8 份之间的口径冲突）。
  6. **`docs/**/*.md` 之外的 `coordination/*.md` 是否也含效力语境 🟡**（及其计龄）**未扫**（判据自身范围也是 `docs/`）⇒ 属 P3-4 的全仓口径。

---

## 十、决策评估（C11 七要素）

**本轮无新决策。** 本件为**只读核对**；三处"该不该改"（底图 2 处过期／`LAYER_DETAILS` 补性质头／判据阈值）**均属 P1**，**不由本件代行**。§七 缺陷**只登记 ＋ 给建议**。

---

## 十一、需要用户决策（白天；本件只列，不催不动）

| 级别 | 事项 | 来源 |
|---|---|---|
| P1 | ★ **`LAYER_DETAILS.md:120` 一处修**（底图 ⇒ 须走 `D16'` 五段留痕 ＋ 更新判据 F 基线） | 本件 §一-2 |
| P1 | **`QUANTUM_CLOCK_DESIGN_DETAIL.md:273` 一处修**（出稿 ⇒ 无须 `D16'`） | 本件 §一-2 |
| P1 | ★ **效力🟡 计龄口径以谁为准**（条文"满 3 轮含起点"／判据"再挂 3 轮"）⇒ **改条文或改判据** | 本件 §二-3 |
| P1 | **`LAYER_DETAILS.md` 是否补「性质」头** | 本件 §一-4 |
| P1 | **本批 8 份（含 3 份新底图）是否补 `C12` 级别声明**（与《补全表》⑪-③ 合并裁） | 本件 §三-4 |
| P1 | **《补全表》"真声明级别 2 份"的判据是否补明** | 本件 §三-3 |

---

## 十二、下一步建议

1. **白天先裁 §十一 第 3 行**（计龄口径）—— 它决定**是否要为 136 处 🟡 立处置**，也决定 `T-058` 自检是否要改夹具（**边界值必须落在 `diff=2`／`diff=3` 两侧**，而非 v0.100）。
2. **`LAYER_DETAILS.md:120` 一处修**建议与 P3-4 的 `T-053` 逐处裁定**同批处理**（同为底图改动，可共用一次 `D16'`）。
3. 把 **"底图内出现已裁仍标待裁"** 列为**机制 25 扩项候选**（现四条判据只覆盖 段号／收口／内部自洽／模块数，**不覆盖"已裁事项仍标待裁"**）。

---

**启动回执**：已读 `CHARTER.md`（机制 **1–30**／§四 速查 **C1–C21**／§五 启动回执／§六 C10）＋ `CONSTRAINTS.md`（**C10／C12／C13／C14 全文**）＋ `TEMPLATES.md`（§二 第〇项／§三 优先级／§十一 夜间自动化／**§二十二 效力列**）。
当前状态：**v0.364**（`HEAD 86b6c6c`＝本夜 P3-1／P3-2 后）；**轮次 v0.332**。
本次任务：**夜间 P3-3 · `docs/` 时效核对批**（**P3**，只读出稿 ＋ 只写本核对稿）。
是否继续：**P3-4 起继续**（本夜不推送、不与远端同步）。
