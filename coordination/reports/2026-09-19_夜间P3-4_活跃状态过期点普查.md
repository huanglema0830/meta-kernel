【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 夜间 P3-4 · 活跃状态型陈述「过期点」普查

> 夜间自动化（机制 19 / C14）· 来源＝**自动化任务**（commit 前缀 `夜间 P3-4：`）
> 任务口径：**取证式扫描 ＋ 只出清单**；**治理文件内的过期点只登记不修改**
> 状态词：**未施工**（治理文件**本夜一字未改**）

**保密标记**（对应 C12）：
- ⛔ 机密（**绝不拷**）：**无**
- 🔐 保密（**屏蔽后拷**）：**无**
- 🔒 半公开（**可拷**）：文件名与行号、现文摘要、机器实测值、编号体系、**密钥级别标识行**（原行含级别标识，本报告**只引用其级别词**）
- 🔓 公开（**直接拷**）：结论、过期点分类、建议

> 【给用户的提示】本次报告包含 **0** 处机密、**0** 处保密。无需屏蔽即可整份拷给顾问。

**全景状态卡**：

```
┌─ 云内核项目 · 全景状态卡 ─────────────┐
│ 版本   v0.227（rev-list --count HEAD） │
│        未推送：ahead origin 9          │
│ 阶段   ROADMAP 阶段二 · 子任务2        │
│ CI     本夜未触发（无 CI 结论）        │
│ 漏洞   R1–R82（本项无新增 R；         │
│        登记 2 条判据盲区，见 §四）     │
│ 决策   见 §五（本项不代行）            │
└────────────────────────────────────────┘
```

**任务**：机制 25（文档一致性）只覆盖「段号／收口／内部自洽」；本轮实测发现**「决策表状态」与「编号范围」两类活跃陈述不在覆盖内**。① 逐文件取证式扫描活跃状态陈述；② 输出**过期点清单**（文件:行／现文／实测事实／依据／是否治理文件）；③ **治理文件内的只登记不修改**｜**优先级 P3**｜基准 v0.227

**状态**：完成

**验收对照**（标准 vs 实测）：

| # | 验收标准 | 实测 | 结论 |
|---|---|---|---|
| 1 | 逐文件取证式扫描 | 扫 `BASELINE.md`（§五/§六/§九）｜`advisor_brief.md`（决策表＋术语速查）｜`ROADMAP.md`（含 §七 活相）｜`CHARTER.md`｜`CONSTRAINTS.md`｜两份设计稿（**设计稿未发现活跃状态陈述**，见 §六 未验证③） | ✅ |
| 2 | 输出过期点清单（五字段齐全） | §二 **9 组过期点**，逐项给「文件:行／现文／实测／依据／是否治理」 | ✅ |
| 3 | 治理文件内只登记不修改 | `git status` 本项**仅 1 个新增报告**；治理文件 0 改动 | ✅ |
| 4 | 机器对照（不靠印象） | 全部过期点均以 `closure`／`doc_consistency`／`grep` 实测为对照（§三） | ✅ |
| 5 | 独立 commit | 见本项 commit（前缀 `夜间 P3-4：`） | ✅ |

---

## 一、结论摘要（四条）

1. **上一夜（09-19 21:04）清单 §三 登记的 3 处缺陷，本轮复核为"全部仍存在"**（D41 状态／D37 两处结论不一／`advisor_brief` 编号范围），**另新增 5 组**（§二 O-6～O-9＋O-2 的扩展）。

2. ★ **最有价值的一处**：`BASELINE.md:695` **口径描述与结论"双过期"** —— 该行既写「`machine_is_closed` 的机器事实＝**顶层 `src/*.rs` 文件差集**」（**R79 后该口径已废**：现与 closure 同源、递归），又写结论「⚠️ **未收口（源 47 / 目标 48 / 剩余 1）**」（**机器实测：已收口、源 56 / 目标 63 / 剩余 0**）。

3. ★ **并由此定位机制 25 的一处模式盲区**（§四）：`RE_REMAIN` 正则为 `剩余\s*片\s*[0-9①-⑳]`，**要求"剩余"后必须出现"片"字** ⇒ 对写法「**剩余 1**」（无"片"）**不命中** ⇒ **P2a 漏报**；而**同一行已被 P4（模块数）捕获** ⇒ **同一行、两个子判据覆盖不同**。
   **实测复现**（仓库外脚本）：`剩余片6`⇒True｜`剩余 片6–片8`⇒True｜`剩余片 6`⇒True｜**`剩余 1`⇒False**｜**`未收口（剩余 1）`⇒False**。

4. **"过期"的分布有结构性规律**：**决策表状态**（D 编号行的 ⏳/✅）与**编号范围**（"C1–C14"类）**不在机制 25 覆盖内** ⇒ 这两类**没有任何机器判据在看**（机制 25 只管段号/收口/内部自洽；P4 只管模块数）。本报告 §二 的 9 组里，**有 4 组属这两类**。

---

## 二、过期点清单（**只登记不修改**；治理文件已标注）

> "是否治理文件"口径：**治理文件** ＝ 任务书禁止清单所列 —— `CONSTRAINTS.md`／`CHARTER.md`／`ROADMAP.md`／`BASELINE.md`／`TEMPLATES.md`／`.github/workflows/`。

| # | 文件:行 | **现文**（摘要） | **机器／实测事实** | 依据 | **治理？** |
|---|---|---|---|---|---|
| **O-1** | `BASELINE.md:228`（D41） | 「⏳ **待确认**：`alloc` 里没有 `HashMap`…**建议**：先出用法论证清单，确认后再迁片6」 | 片6 **已按「方式 A」换 `BTreeMap`**；片6–片8 ✅ 完成；**片9 补迁**；**未迁 0 ⇒ 全源收口** | `closure` 实跑（源 56 / 目标 63 / 剩余 0）＋ `BASELINE.md:154` 自述 | **✅ 治理** |
| **O-2** | `advisor_brief.md:194`（D37） | 「✅ 本轮已执行并通过全部判据…**待你追认**：若确认，请回一句「D37 放行」」 | `BASELINE.md:239`：D37「✅ **用户已追认（2026-09-17）**」 ⇒ **同一事实两处结论相反**（一处"待追认"／一处"已追认"） | 两文档逐字对照 | ❌ 简报 |
| **O-3** | `advisor_brief.md:249` | 「**C1–C14**」 | `CONSTRAINTS.md` 活相已到 **C19** | `grep '^\| \*\*C'`／C19 全文 | ❌ 简报 |
| **O-4** | `advisor_brief.md:250` | 「**R1–R23**…R15 为空号…」 | `BASELINE.md` §六 已到 **R82** | `grep -c` §六 行 | ❌ 简报 |
| **O-5** | `advisor_brief.md:251` | 「**D1–D33**…**D1–D31 全部收口；D32 GHA nightly、D33 夜间是否 push 待你拍板**」 | §九 已到 **D49**；且 **D32／D33 均已 ✅ 确认**（`BASELINE.md:221/222`） | 逐行对照 | ❌ 简报 |
| **O-6** | `CHARTER.md:209`（§九 速查表） | 「**顾问收到返回后该做什么（8 步）** ＋ 报告必带十四项」 | **权威源为 9 步**（`TEMPLATES.md` §二 第〇项／`CONSTRAINTS.md` C13／`CHARTER.md:87/134` 均为 9 步） ⇒ **本文件内部不一致** | 4 处逐字对照 | **✅ 治理**（**§九 属本文件**） |
| **O-7** | `BASELINE.md:695` | 「`check_doc_consistency.py` 的 `machine_is_closed` ｜**顶层 `src/*.rs` 文件差集** ｜ ⚠️ **「未收口（源 47 / 目标 48 / 剩余 1）」**」 | **双过期**：① 口径已于 R79 改为**与 closure 同源（递归）**；② 结论应为**已收口**（源 56 / 目标 63 / 剩余 0） | 代码（`check_doc_consistency.py:54-62`）＋ `closure` 实跑 | **✅ 治理** |
| **O-8** | `BASELINE.md:216`（D29） | 「✅ 用户已确认：取 ULP 判据（≤4），**不做 `f64` 版**」 | `fmath.rs` **已有 f64 全套**：`sqrt_f64`／`exp_f64`／`ln_f64`／`log2_f64`／`atan2_f64`／`powi_f64`／`round_f64`／`rem_euclid_f64` ＋ `FloatOps for f64` 的 `sin`/`cos`；且 **v0.220 刚对 f64 `sin`/`cos` 打精度补丁**、**本夜 P3-2 又对 f64 侧做六档实测** | `fmath.rs` 公开面 `grep '^pub fn'`（21 个 `pub fn`，含 8 个 `_f64`） | **✅ 治理** |
| **O-9** | **模块数类 9 处**（见下） | 「目标 **57／58／48**」「源 **47／55**」 | 机器实测 **源 56 / 目标 63**（`closure` 同源） | `doc_consistency` **P4 提示**（R80-1）+ 本项复核 | **✅ 治理 6 处**（BASELINE×6、CONSTRAINTS:407、ROADMAP:59）＋ 简报 0 |

**O-9 明细（`doc_consistency --selftest` 之外的实跑输出，11 条 P4 提示，去重后 9 处）**：

| 文件:行 | 现文声称 | 机器实测 |
|---|---|---|
| `BASELINE.md:153` | **目标 crate 58 模块**（源 56） | **目标 63** |
| `BASELINE.md:423` | 源 crate **55** ／ 目标 crate **57** | 源 **56** / 目标 **63** |
| `BASELINE.md:647` | 源 **56** ／ 目标 **57** | 目标 **63** |
| `BASELINE.md:694` | 源 **56** ／ 目标 **57** | 目标 **63** |
| `BASELINE.md:695` | 源 **47** / 目标 **48** | 源 **56** / 目标 **63** |
| `BASELINE.md:717` | 源 **56** / 目标 **58** | 目标 **63** |
| `BASELINE.md:737` | 源 **56** ／ 目标 **58** | 目标 **63** |
| `BASELINE.md:756` | 源 **56** ／ 目标 **58** | 目标 **63** |
| `CONSTRAINTS.md:407` | 源 **56** ／ 目标 **58** | 目标 **63** |
| `ROADMAP.md:59` | 目标 **58** | 目标 **63** |

> ⚠️ **判据自身状态（如实标注）**：机制 25 的 **P4 首轮为"提示不判红"**（`doc_consistency.py:296-311` 有完整留痕：首跑**误报 3 项**＋**漏报 1 项**，故依 R60 先例降级）。⇒ O-9 **已被判据看见**，但**未阻塞 CI**。

---

## 三、机器对照（本项全部结论的"实测"一侧）

| 命令 | 输出要点 | exit |
|---|---|---|
| `python coordination/tools/check_migration_closure.py` | 源 crate **56**｜目标 crate **63**｜**[0] 收口：✅ 源 ⊆ 目标（未迁 0）**｜目标独有 7：`fb`/`field`/`field/nca`/`field/poisson`/`field/sdf`/`fmath`/`quad` | 0 |
| `python coordination/tools/check_doc_consistency.py` | 机器·最大段号 **⑪**｜机器·迁移收口 **已收口（源 56 / 目标 63 / 剩余 0）**｜枚举口径**与 closure 同源**｜**结果 ✅ 通过**（P1/P2 双侧/P3）｜**P4 提示 11 条待裁** | 0 |
| `grep -n "^| \*\*D" coordination/BASELINE.md` | D 表现文（D41 ⏳／D32 ✅／D33 ✅…） | — |
| `grep -rn "8 步\|9 步" coordination/*.md` | `CHARTER.md:209` 仍写「8 步」；其余为 9 步／"八步场域演化"（工作流名，非缺陷） | — |
| 仓库外正则验证脚本 | `剩余 1` ⇒ **RE_REMAIN 不命中**（§四） | — |

---

## 四、由 O-7 定位的**机制 25 模式盲区**（**只登记，不改**）

### 盲区-1：P2a 对「剩余 N」（无"片"字）不触发

| 项 | 内容 |
|---|---|
| **判据原文** | `RE_REMAIN = re.compile(r"剩余\s*片\s*[0-9①②③…⑳]")`（`check_doc_consistency.py:83`） |
| **注释所述设计** | 「剩余片陈述（P2 用）："剩余片6" / "剩余 片6–片8" / "剩余片 6"」—— **三个示例全部含"片"** |
| **实测** | `剩余片6`→**True**｜`剩余 片6–片8`→**True**｜`剩余片 6`→**True**｜`仍有剩余片 3 未迁`→**True**｜**`剩余 1`→False**｜**`未收口（剩余 1）`→False** |
| **后果** | `BASELINE.md:695` 的「⚠️ 未收口（… **剩余 1**）」**逃过 P2a**；而**同一行的模块数**（源 47/目标 48）**被 P4 捕获** ⇒ **同一行、两个子判据覆盖不同** |
| **为什么值得记** | 这不是"数字算错"，而是**判据的模式随口径演进掉队**：R79 之前"收口"以**片**为单位表述（剩余**片**N）；R79 之后改为**模块**口径（剩余 N 个模块）⇒ **旧模式漏新写法**。与 **C19 精神**（同一事实只许一份口径）同族：**P2a 与 P4 对"收口/模块数"这一族事实各有一套模式** |
| **建议（白天；须动判据工具 ⇒ 夜间禁改）** | ① 先**裁定** O-7 的 695 行如何处置（改口径描述＋改结论）；② 再决定 P2a 是否扩展模式（**不得调参打绿**：扩展须先造**正反样例**并过 **C18 三问**） |

### 盲区-2（已有留痕，本项只做交叉印证）：P4 首轮"既误报又漏报"

`doc_consistency.py:296-311` 已自述：首跑**误报 3 项**（`:695` 历史数字／`:884`「修订前」列）＋**漏报 1 项**（`:694`，根因＝markdown `**` 粗体阻断正则）。
⇒ 本项复核确认：**`:694` 现仍在 11 条 P4 列表中**（说明"粗体阻断"已在 v0.223 修好），而 **`:695` 仍在列表**（说明"历史豁免"尚未覆盖它）—— 与 O-7 的发现**互相印证**。

---

## 五、需要裁定（**白天**；本报告不代行）

| 级别 | 事项 | 依据 |
|---|---|---|
| P2 | **O-1／O-7／O-8／O-6**：4 处**治理文件**内的活跃状态过期（须过 **C15 双向计数**后一处修） | §二 |
| P2 | **O-9**：模块数类 9 处 —— 与 **R80 四问**（`BASELINE.md:822`）**同一决策**，应合并裁定 | §二／§四 |
| P2 | **O-2～O-5**：`advisor_brief.md` 「决策表状态 ＋ 编号范围」两处批量过期（建议**一次性对齐**，并考虑给该文件的"摘要类内容"加**生成式口径**） | §二／§一·4 |
| P3 | **§四 盲区-1**：P2a 模式是否扩展（**须先裁 695 行**） | §四 |

> ⚠️ 上述**全部须动治理文件或判据工具** ⇒ **夜间禁改**（前者属任务书明令整类排除；后者改动判据＝假绿风险）。
> ⚠️ **修治理文件前须先跑 `--update-baseline`**（R60 前置提示）＋ **C15**（改前取数→改后复算）。

---

## 六、边界说明（C5；四类齐全）

- **本地跑过**：✅ 有 —— `check_migration_closure.py`（exit=0）｜`check_doc_consistency.py`（exit=0，含 P4 提示 11 条）｜多处 `grep` 逐行取证｜**仓库外正则验证脚本**（§四 盲区-1 的假阳性/假阴性实测）。
- **CI 过**：❌ **无** —— 本项**未触发 CI**。
- **真实跑过**：❌ **无** —— 未跑 QEMU、未上真机。
- **未验证**：
  1. **扫描未穷举全仓**：本项聚焦 `BASELINE`／`advisor_brief`／`ROADMAP`／`CHARTER`／`CONSTRAINTS` 五份 ＋ 两份设计稿；**`DECISIONS_D50_D55.md`／`TERMS.md`／`ASSETS_INVENTORY.md`／`llm/`／`workflows/` 未逐行扫** ⇒ 本清单**不是**"全仓过期点全集"（C16：不得据此写"全库仅此 9 组"）。
  2. **O-8（D29）性质待核**：可解读为"当时决策＝不出 f64 版，后由 2.3b 片3 补 `f64` 全套"（`BASELINE.md:154` 有"`FloatOps` 初版只实现 `f32`…⇒ `fmath` 补 f64 全套"的记载）⇒ **属"决策后由执行补足"还是"状态陈述过期"，须发起人定性**（本报告**只报"现文与现状不符"**）。
  3. **两份设计稿未发现活跃状态陈述** —— 这一"未发现"**只基于快速扫读**，**未按 C16 四要素取证** ⇒ 应按"未检索到"对待，**不得**读作"设计稿无过期点"。
  4. **`advisor_brief.md:252` 的"field-render 11 个验收模式"未复核**（未去数验收模式个数）。
  5. **平台定时任务的 ACTIVE 状态**（`BASELINE.md:86`）**无法在仓库内验证** ⇒ 未列为过期点（不凭推测下结论）。

---

## 七、三量台账（C6）

| 量 | 内容 |
|---|---|
| **存量** | `BASELINE.md`（§五 未完成／§六 R 表／§九 D 表，重点 D29/D41/D32/D33/D37）｜`advisor_brief.md`（决策表 ＋ 术语速查表）｜`ROADMAP.md`（含 §七 活相）｜`CHARTER.md` §三·§九｜`CONSTRAINTS.md` C19｜`check_doc_consistency.py`（P1–P4 实现与正则常量）｜`check_migration_closure.py` 输出 |
| **变量** | **0** —— 未改任何治理文件、代码、CI、判据（脚本在**仓库外**） |
| **补充增量** | **2 份**：本报告 ＋ 仓库外正则验证脚本（**不入库**） |
| **创新增量** | **2 条**：① **O-7**（`BASELINE.md:695` 口径＋结论双过期）；② **§四 盲区-1**（机制 25 P2a 对"剩余 N"不命中，已实测复现） |

---

## 八、下一步建议（留给白天）

1. **先裁 §五 的"合并项"**：O-9（模块数）与 **R80 四问**同源 ⇒ 合并裁定可一次解决 9+ 处。
2. **O-1／O-6／O-7／O-8 四处治理文件**建议**同一轮**修（一次性过 C15 双向计数），避免反复编辑编号表（**T-019／C15 的根因是编辑动作本身**）。
3. **O-2～O-5（`advisor_brief.md`）**建议考虑"**摘要类内容生成式化**"（从 `BASELINE`／`CONSTRAINTS` 抽取编号范围，而非手写）—— 与 **D40**（清单不手写）同族。
4. **§四 盲区-1** 在 695 行裁定后处理；扩展模式时**必须先造正反样例**（C18）。
