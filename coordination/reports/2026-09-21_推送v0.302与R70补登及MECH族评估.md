【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# push v0.302 ＋ R70 补登 ＋ 两项追认 ＋ MECH 族评估稿（2026-09-21 · v0.303）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 302** ⇒ 收尾 **303**（本轮 **1 个本地提交**）｜**label v0.303**（`label = count`，C21） |
| **HEAD／远端** | 推前 `d015fce`（= `origin/main`）｜**已推 `78ceaf0..d015fce`**（v0.302） |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 = 执行层 |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–39**｜D **1–44**｜R **1–52**（**+ R70 本轮补登**）｜TERM **1–13** |
| **本轮性质** | **放行 ＋ 裁定落地**（§一 R70 补登／§二·§三 追认／§四 MECH 族评估稿） |

**③ 任务与约束**
- 基准 **count = 302**（指令填值）｜总规划定位：**push v0.302 ＋ 5 项裁定落地**（实为 **「1 放行 ＋ 1 补登 ＋ 2 追认 ＋ 1 只出稿」**）。
- 约束：按 `CONSTRAINTS.md`｜**禁止内联含反引号／特殊字符命令**（全程脚本文件，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**按 T-038 门控**｜**按 C20** CI 结论不入库。

**④ 状态：完成**（**5 项全落地**；**8 条验收全 ✅**，见 ⑤）。

---

## ⑤ 验收对照（用户 §七 · 8 条）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（**按 T-038 门控**） | ✅ push **`78ceaf0..d015fce`**（exit 0）；**门控第五次**＝阻塞等到 `completed` 取三态后才动文件；**CI 结论按 C20 只记当日日报** |
| 2 | **R70 补登**，逐字对照 | ✅ `BASELINE §六` 指针表 R70 行 ＋ **新增补登记块**；见下 §一 |
| 3 | 新判据**先观察**，记录到 BASELINE | ✅ `§44.2`（**N=5**） |
| 4 | 判据**暂不加排除**，记录到 BASELINE | ✅ `§44.3`（＋ **自指污染**新实证） |
| 5 | **MECH 族评估稿**出 | ✅ `2026-09-21_MECH族专用解析评估稿.md`（**149 行**；P1/P2 PASS、泄漏 0） |
| 6 | C15 复算通过，编号表**连续无缺号无重号** | ✅ 机制 27／C 21／T 39／D 45／R 首格 50／R 唯一 53／§四 21／TERM 13 |
| 7 | CI 全绿 | ✅（**结论按 C20 只记当日日报**） |
| 8 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行 |

---

## §一 裁定 · **R70 补登**（用户 §一 · 不新增编号）

**逐字前后对照（`BASELINE §六`）**：

| | 内容 |
|---|---|
| **改前（指针表 R70 行）** | `> \| **R70** \| — \| — \| ★ **无独立登记行**（仅被历轮引用：\`（全程脚本文件，R70）\`；语义＝**MSYS 路径改写／特殊字符命令走脚本**）⇒ **待补登** \|` |
| **改后（指针表 R70 行）** | `> \| **R70** \| **本表下方「★ 2026-09-21 补登记（R70）」块** \| **§六 末尾** \| ★ **2026-09-21 已补登**（**不新增编号**，沿用 R15 先例） \|` |
| **改前（§六 无此块）** | — |
| **改后（新增块，逐字）** | `> ★ **2026-09-21 补登记（R70）** —— **本编号是 R 族"唯一无独立登记行"者**；**不新增编号**（沿用 **R15 先例**）；**语义由历轮引用反推**（**非原始登记原文**，**如实标注**）：`<br>`> \| **R70** \| **Windows Git Bash（MSYS）环境差异**：**(a) 命令解析** —— 含**反引号／特殊字符**的**内联命令**经 MSYS 转义被改写 ⇒ **一律走脚本文件**（历轮引用原文：\`（全程脚本文件，R70）\`）；**(b) 路径改写** —— MSYS 把 \`/c/...\` 写成 \`C:\c\...\` 再传给 Windows 程序（\`gh\`／\`python\`／\`git -F\`）⇒ \`Errno 2\`／退出码 2；**★ 该坑也作用于「脚本路径参数本身」**（2026-09-20 实测）。 \| ✅ **已作为长期纪律执行**（`BASELINE §二` 环境铁律）。**同族参考**：\`R19\`／\`R32\`。 \|` |

**效果**：R 族**唯一无独立登记行**者**归位** ⇒ **R 族登记完整**。
**C15**：**本轮 R70 补登不新增编号** ⇒ **R 首格 50／R 唯一 53 不变**；其余全 ±0。

---

## §二 追认 · 新判据「**先观察**」，不入 CI（用户 §二 · 无文件改动）
`tools/check_id_set_diff.py` **保持"先只告警、不入 CI"**；**再评估条件**＝告警**连续 N 轮为 0**，**N 取 5**（用户建议值）。

---

## §三 追认 · 判据**暂不加「上下文排除」**（用户 §三 · 无文件改动）
理由（用户逐字）：**R73 教训 —— 隐藏规则削弱可信度**。**残余告警如实保留**（不掩盖、不调参）。

**★ 本轮新实证（支撑"暂不加"的观察）—— 判据的「自指污染」**
本轮实跑告警由 **3 → 2**，但**成分漂移**：

| 告警 | 上轮 | 本轮 | 原因 |
|---|---|---|---|
| `C05` | ⚠️ 有 | ✅ 无 | 上轮**报告里的"告警说明表"** `` \| **C05** \| `` 被四形态判为「**登记**」⇒ **假阴性** |
| `TERM-014` | ⚠️ 有 | ✅ 无 | 同上（判据稿的说明表） |
| `R99` | — | ⚠️ **新增** | 来自判据稿的**自检示例表**（`` `R99` ``） |
| `T-0` → **`T-00`** | ⚠️ 有 | ⚠️ 有（**变形**） | 来自"**讨论 T-0 告警**"的**元引用**（`grep -oE "^\| T-0"` ＋ 我的报告文字） |

⇒ **判据会被"讨论它自己的文字"污染**（**自指污染**，与 **C20 治的 R82 自指递归**同族）⇒ **告警数受"报告内容"影响而不稳定**。
⇒ **若加"上下文排除"，必须同时处理"判据自身输出"这一类**，否则排除规则会被自身的说明文字绕过。**建议列入后续评估（本轮不动）**。

---

## §四 裁定 · **MECH 族专用解析评估稿**（用户 §四 · **只出稿**）→ `…_MECH族专用解析评估稿.md`（149 行）

| 项 | 内容 |
|---|---|
| **格式现状（取证）** | `CHARTER.md` **`## 三、全部机制`**（L66 起）—— 首格**裸数字**（`\| 1 \|`／`\| 2 \|`…），**27 行**（与"机制 1–27"一致） |
| **与五族差异（本质）** | 其余五族**编号字面自带族名**（`R`／`D`／`C`／`T-`／`TERM-`）⇒ **全库无歧义识别**；**MECH 是裸数字** ⇒ **必须靠"所在区段"才能确定身份** |
| **选项** | **A** 不启用（零成本零风险，但放弃机器检查）／**B** ★ **区段限定专用解析**（~20 行独立函数，**零回归**）／**C** 全文件裸数字（**高误报**，R73 教训） |
| **推荐** | **B** —— 前置条件＝配「**解析到 0 行 ⇒ 告警**」（遵 **C-04／R83**，防"空转像通过"）；建议同时纳入 `机制 22/24` 的**多编号切分**（+3 行） |

---

## ⑥ 未完成项
**无。**（5 项全落地；用户 §五 明示"本轮不派其他任务"。）

---

## ⑦ 真实缺陷
- **0 缺陷**。**1 条过程观察（正向 · 判据自证）**：**判据的「自指污染」首次被量化**（见 §三表）—— 3→2 条告警的**成分漂移**证明：**在不加排除的前提下，告警数不是稳定指标**。**这恰好为用户"暂不加排除"的裁定提供了实测依据**（若当初加了排除，本轮会因"说明表被当登记"而**误报归零**）。
- **另 1 条（正向）**：**T-038 门控第五次执行成功**（阻塞等 `completed` 后才动文件）—— **连续五轮零违规**。

---

## ⑧ 三量台账（四量）

| 量 | 内容 |
|---|---|
| **存量** | R 表（R1–R42＋指针表）｜`tools/` 7 支判据｜`CHARTER` 机制表（27 行）｜L7–L10 底图回写区 |
| **变量** | **R70 由"无登记"→"已补登"**（**R 族登记完整**）｜告警 **3 → 2**（成分漂移）｜**MECH 族由"暂不启用"→"已评估（荐 B）"** |
| **补充增量** | **R70 补登记块**｜`MECH族专用解析评估稿`｜`BASELINE §四十四`｜**自指污染**实证 |
| **创新增量** | ① **R 族"唯一缺口"归位**（R15 先例复用于 R70）；② **「编号字面自带族名 vs 裸数字」是族可分性的分水岭**；③ **判据「自指污染」的首次量化** |

---

## ⑨ 边界说明（四类）

| 类 | 内容 |
|---|---|
| **本地跑过** | ✅ C15 双向计数；新判据自检四侧 ＋ 实跑（复核 R70 补登后）；`MECH族……` 稿第 0 行自检；`grep`／Python 同源溯源 |
| **CI 过** | ✅ push `78ceaf0..d015fce` 已按 **T-038 门控**确认 `completed`（**结论按 C20 只记当日日报**） |
| **真实跑过** | ❌ **不涉及**（本轮为文档／登记层；**未改代码、未改 boot、未改判据**） |
| **未验证** | ⚠️ ① **MECH 方案 B 未编码**（仅评估）；② **自指污染**未处置（如实保留 2 条告警）；③ **新判据仍未入 CI**（先观察，N=5） |

---

## ⑩ 决策评估（C11 七要素）—— 针对「**MECH 族是否做专用解析**」

| 要素 | 内容 |
|---|---|
| **评估** | 机制是本项目**最核心编号族**（27 条）且**引用极密**（全库数百处）；当前**无机器检查**（`CHARTER` 表靠人眼核）。 |
| **优势** | ① 可检出「**机制 N 被引用但未登记**」类不一致；② 与现有五族**同构**、**零回归**（独立函数）；③ 成本低（**~20 行**）。 |
| **劣势** | 需维护**区段锚**（`## 三、全部机制`）；`机制 22/24` 多编号须额外切分。 |
| **风险** | **区段改名 ⇒ 解析静默失效**（"空转像通过"，**R83／C-04 同族**）。**缓解**：**必须配「解析到 0 行 ⇒ 告警」**。 |
| **机会** | 该范式（**区段限定解析**）可**复用于其它"无前缀编号表"** ⇒ 成为通用能力。 |
| **建议** | **B（区段限定专用解析）**。 |
| **等待决策** | ⏳ 待你一句话（A／B／C）。 |

---

## ⑪ 本轮待判

| # | 项 | 推荐 |
|---|---|---|
| 1 | ★ **push 本轮 commit（v0.303）** | **放行**（机制 23 须当轮明示） |
| 2 | **MECH 族选项 A／B／C**（见 ⑩） | **B**（区段限定专用解析） |
| 3 | **"自指污染"是否专项处置**（判据排除"自身输出／元引用"） | **先观察**（当前 2 条告警**均属此类**，能量化占比后再定） |

---

## ⑫ 下一步建议
① 先裁 **⑪-1（push）** 与 **⑪-2（MECH 选 B）**；② 若同意 B，可与 **"自指污染"的评估**合并为一次 `tools/` 变更（**仍"先只告警"**）；③ `docs` 批次（批八）与 `advisor_brief` 复扫**均为触发式，本轮未触发 ⇒ 未跑**（合规）。

---

## ⑬ 启动回执
> 已读 `CHARTER.md`。
> 当前状态：**count = 302**（基准）⇒ 收尾 **303** ｜ 阶段二 · **2.4 边界层已验**。
> 本次任务：**push v0.302 ＋ 5 项裁定落地**。
> 是否继续：**执行完毕**。

---

## ⑭ 本轮读了底图哪几份、哪几行

| 文件 | 行 |
|---|---|
| `coordination/BASELINE.md` | **§六 R 表 L89–L134**｜**§六 指针表 ＋ R70 补登记块 L136–L162**｜**§二 环境铁律**（`C:/...` 形式）｜**§四十三 43.3**（判据落地） |
| `coordination/CHARTER.md` | **`## 三、全部机制` L66–L123**（机制表；L70–L76 抽样）｜**`## 四、硬规则速查` L124–L161**（表头 L126） |
| `coordination/tools/check_id_set_diff.py` | **FAMILIES**（五族正则）｜**`forms()`**（A/B/C/D）｜注释「MECH 暂不启用」 |
| `coordination/reports/2026-09-20_夜间持续任务_晨报与十四项报告.md` | **L107**（R70 家族 · 脚本路径参数） |
| `coordination/reports/2026-09-21_编号集差集判据_落地.md` | **L66／L81／L90／L102**（自检示例表 ＋ 告警说明表 ⇒ **自指污染源**） |

---

**（本轮 1 个本地提交：台账 §四十四 ＋ §六 R70 补登 ＋ MECH 族评估稿。）**
