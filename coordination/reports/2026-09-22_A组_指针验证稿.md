【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# A 组 · 指针验证稿（八步通用工作方法论 · 入口链逐跳实跑）

> 归口：**A 组 A.4**｜日期：2026-09-22｜性质：**指针验证**（只读实跑 ＋ 出稿）
> 验证对象链：**用户自定义指令 → `TERM-014` → `coordination/TERMS.md` → `coordination/workflows/ENTRY.md` → 按入口执行**
> 口径：**每一跳都实跑**（读真实文件、断言真实字符串/路径），**不写「应该能找到」**（**R89：声明≠发生**）。
> 复现命令（脚本**在仓库外**，未入库）：`python <仓库外>/.workbuddy/tmp/pointer_verify_0922.py`

---

## 一、逐跳结果（**运行期实测**）

| 跳 | 验证项 | 结果 | 运行期证据 |
|---|---|---|---|
| 1 | `ENTRY.md` **首行＝自定义指令锚** | ✅ PASS | 首行＝`【本入口对应的自定义指令】` |
| 2 | `ENTRY.md` **声明 `TERM-014`** | ✅ PASS | 命中 1 次 |
| 3 | `TERMS.md` 有 **`TERM-014` 登记行**（表格首格 · 行首锚） | ✅ PASS | `\| \`TERM-014\` \| **Eight-Step Universal Workflow Methodology** \| **八步通用工作方法论**——所有动作按此执行 \| \`coordination/workflows/ENTRY.md\` \|` |
| 4 | `TERM-014` **指针列 → `ENTRY.md`** | ✅ PASS | 指针列命中 = True |
| 5 | **骨架文件存在** | ✅ PASS | `universal_workflow.md` 存在 = True |
| 6 | **子步 `sub/NN_*.md` 齐（01–08）** | ✅ PASS | 实测 **8** 份（`01_field_sampling`…`08_phase_diagram`） |
| 7 | **实现层＝占位符**（非真实值 · C12） | ✅ PASS | `{PRIVATE_ASSETS}/workflows/sub/*_impl.md` 命中 ＋ 标「（占位，待填）」 |
| 8 | **`CHARTER` 机制 28 已登记** 且指向 `ENTRY.md` | ✅ PASS | `\| 28 \| **八步通用工作方法论机制**…**入口** \`coordination/workflows/ENTRY.md\`…` |
| 9 | **八步速览 ↔ 骨架 逐字一致** | ✅ PASS | 骨架 8 行／入口 8 行，**逐字一致 = True**（防入口自行发明步名） |
| **10** | **反向：骨架指回入口** | ❌ **FAIL** | `universal_workflow.md` 含 `ENTRY.md` = **False** |
| 对照 ⑪ | **阴性**：`TERM-999` **不得**命中 | ✅ PASS | 实测 False（证跳 3 不是「任意编号都算」） |
| 对照 ⑫ | **阴性**：不存在的路径**不得**存在 | ✅ PASS | 实测 False（证跳 5 的存在性判据有效） |

**结论：11/12 PASS；唯一 FAIL ＝ 跳 10（反向可达）**。

---

## 二、★ 跳 10 缺口（**如实登记 · 本轮不代改**）

- **现象**：**入口 → 骨架** 可达（跳 1–9 全通）；**骨架 → 入口** **不可达** —— `coordination/workflows/universal_workflow.md` 全文 **不含 `ENTRY.md` 字样**。
- **后果**：**若读者先读到骨架（该文件是 `CHARTER §九` 与 `coordination/README.md` 的现行指针目标），就找不到入口** ⇒ 机制 28 的「唯一入口」**只在"从入口出发"这一条路上成立**。
- **为什么不本轮修**：`A 组` 授权改动文件＝`ENTRY.md`（新建）／`TERMS.md`／`CHARTER.md`／`BASELINE.md`；**`workflows/universal_workflow.md` 不在授权范围** ⇒ 按 **C5／C8** 精神**不擅改**，登记待裁。
- **建议修法（一行，加性）**：在 `universal_workflow.md` 的 `## 定位` 节补一句：
  > **本文件是「骨架」；对外统一入口见 `coordination/workflows/ENTRY.md`（机制 28）。**
- **风险（不修的代价）**：**同族先例** —— `CHARTER §九` 与 `README.md` **各挂一行、口径散落**，正是机制 28 的**建立理由**；跳 10 说明**散落没有消除，只是补了新的一处**。

---

## 三、边界说明（四类）

| 类 | 内容 |
|---|---|
| **本地跑过** | 本验证 **全部在本机跑**（`python` 直跑，逐跳断言）＝**12 项**；本机结果为 **11 PASS / 1 FAIL**。 |
| **CI 过** | ❌ **无** —— 本验证**未入 CI**（无对应 step；脚本在仓库外）。 |
| **真实跑过** | **真实文件 ＋ 真实路径**（非 mock）：读的是仓库内实际文件，跳 5/6 是**真实文件系统存在性**，跳 9 是**真实文本比对**。 |
| **未验证** | ① **反向可达**（跳 10）**未通过**；② **「按入口执行」的执行质量未验**（本稿只验**指针可达**，不验"按八步跑出来的结果对不对" —— **那是另一个问题**）；③ **实现层未就位**（`*_impl.md` 为占位，**无法验证**）；④ 脚本**未入库** ⇒ **他人复现需先拿到脚本**；⑤ **CI 未接线** ⇒ 入口文件**未来被改动时无机器拦截**。 |

---

## 四、结论与待裁

1. **入口链正向 10 跳全部可达**，且 **`TERM-014` ↔ `ENTRY.md` ↔ 机制 28 三处互相一致**（跳 3/4/8）。
2. **唯一缺口＝反向可达**（跳 10），**修法一行、需授权**（`universal_workflow.md`）。
3. **待裁**：① 是否补跳 10 的反向指针；② 本验证脚本**是否入库为 `coordination/tools/check_pointer_chain.py`**（入库＝可复现、可入 CI；不入库＝**他人无法复现**）。
