【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# v0.238 · 4 项裁定落地 ＋ 2 项机制建设 · 报告

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**（无密钥／账号／本机绝对路径／内网 IP／用户名）。**可整份转发**。
（说明：本报告如实记录**本机构建环境的异常现象**，但**只描述现象与命令**，**不含**绝对路径。）

**② 全景状态卡**（★ 数字为**生成时快照**；提交／推送动作本身会推高版本号 ⇒ **现值一律以复核命令为准**，R82）
```
┌─ 云内核项目 · 全景状态卡 ─────────────────────────────────────┐
│ 版本   以 git rev-list --count HEAD 为准（不写死，C2）         │
│        本轮起点 count = 243（本仓库近期 label 惯用 count−5      │
│        ⇒ 记为 v0.238；⚠️ **口径分歧待裁 R84**）                │
│ 阶段   阶段二 · 2.4 纯算层 ✅ ｜ 2.4 边界层 ✅（自检 / product  │
│        / **本轮新增：场演化** 三条入口）；**待 CI 真实引导证成** │
│ 本轮   4 项裁定落地 ＋ 2 项机制建设；改动 **13 文件 ＋ 1 报告**  │
│ 未推送 快照 0（提交后按 gh 实取）                              │
│ CI     按 **C20**：结论**只进日报／对话返回**，**不入仓库**     │
│        ⇒ 复核命令 `gh run list --limit 3`                     │
│ 决策   待裁：① **R84 版本号口径** ② **R85 已自处置**            │
│        ③ 场源是否从 9×9 扩到全屏 ④ 底图 `README.md` 是否需订正  │
└───────────────────────────────────────────────────────────────┘
```
复核命令（R71：数值不抄进文档）：`git rev-list --count HEAD`｜`git rev-list --count origin/main..HEAD`｜`gh run list --limit 3`

**③ 任务**：基准 **count = 243（label v0.238）**｜总规划定位：**4 项裁定落地 ＋ 2 项机制建设**｜类型：**裁定落地 ＋ 机制建设**｜优先级 **P1**。
约束：按 `CONSTRAINTS.md`｜禁止内联含反引号／特殊字符命令（**全程脚本文件**）｜不 `taskkill`｜不让用户双击任何 bat｜**不改底图**。

**④ 状态：完成**（含 **3 处诚实边界**：① boot 层 **full build ＋ QEMU 真实引导本机不可做**；② 场演化验的是 **9×9 小范围**，**未扩到全屏**；③ **本轮自纠 2 条我自己写的错口径** —— 见 ⑦）。

**⑤ 验收对照**

| # | 标准（用户原文） | 实测 |
|---|---|---|
| 1 | 场源小范围验证完成，host 单测通过 | ✅ 纯算层 `nca::evolve`（`forward_const_weights` ＋ 多步演化，结果恒在 `a`；`steps==0` ⇒ 恒等）＋ 边界层 `present_evolve_selftest`（**129–132**）＋ `main.rs` **真调**（步数由 `field::rhythm_steps` 导出，**import 不重实现** ⇒ C19）。**网格 `9 = 2³+1`**（守多重网格铁律）。host 镜像 **8 → 13 passed**；`nostd` lib **474 → 477 passed** |
| 2 | CI 结论不入库，纪律条目写入 | ✅ `CONSTRAINTS.md` 新增 **C20**（三条要求 ＋ 三问判据）；`BASELINE §22.2` 记病与处置；★ **本轮自身遵守**：v0.237 的 CI 结论**只写日报**，仓库侧 `§21.7` **未删但已追认为历史留痕** |
| 3 | D-1 结项记录到 BASELINE | ✅ `§22.4`；口径＝**截断到界**（maxAbs 1.110e-15 < 界 2.0038e-15）／**ULP 未达标**（`cos` maxULP = 10；那条 ≤4 ULP 是 **f32** 口径）／**暂不追**（不再派 Payne-Hanek）；并**传播到** `ROADMAP.md` 子任务2 的 D-1 结项块 |
| 4 | 边界层退化出口机器化，自检正反对照 | ✅ 纯算层 `fb::classify_degenerate`（**判定属纯算**，机制 21）＋ 边界层映射为 **133／134**（**判红**）⇒ **三条入口全部改**，**不再有"退化成绿"路径**。**正反对照**：`fb.rs` 单测 **5 类阳性 ＋ 阴性 ＋ 顺序写死 ＋ 边界值 767/768 ＋ stride 959/960**；host 镜像 `mirror_present_133_134…`；**CI 负断言**（`present.rs` 不得再出现"不判红"＋ 直查两处 `return CODE_DEG_*`） |
| 5 | 能力矩阵立，写入治理文件 | ✅ `CHARTER.md` **机制 26**（四层 M／CB／CQ／H ＋ 每层三要素）＋ `TEMPLATES.md` **T-035（§十四）**（矩阵四要素 ＋ 判据三问 ＋ 报告第 ⑨ 项即人工承载）。★ **机制 26 首次实战即抓出我自己写错的能力条目**（**R85**），详见 ⑦① |
| 6 | C15 复算通过，编号表连续无缺号无重号 | ✅ 改前 → 改后：机制 **25→26（+1）**／C **19→20（+1）**／T **34→35（+1）**／D **44→44（±0）**／R **45→48（+3）**；**三张编号表（机制／C／T）范围 1–26／1–20／1–35，缺号 无、重号 无、连续 是**；对照（阳性＝表内最大号／阴性＝最大号+1）**6/6 PASS**。**同一脚本取两次数**（C18 同源） |
| 7 | CI 全绿 | ⏳ **按 C20：结论不进仓库** ⇒ 以命令实取（`gh run list --limit 3`），**结论写在当轮对话返回／日报**。仓库侧本报告**只留命令**，**不写结论** |
| 8 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行 ＝ **`TEMPLATES.md` 行 104–118 逐字抽取**（D40 不手写），并**回读全等自检 PASS**（见 ⑭） |

**⑥ 未完成项**
① **boot 层 full build ＋ QEMU 真实引导本机不可做**（缺 `dlltool.exe`／无 `qemu-system-x86_64`，**实测**）⇒ 交 CI `boot-image` job；
② **场演化只验到 9×9**（小范围，按裁定"先小范围再扩大"）；**扩到全屏未做**；
③ **真实硬件**（非 QEMU）帧缓冲回读**未验证**（真机 WC 显存可能读到陈旧值）；
④ **`fmath.rs` doc 注释**仍写旧值（"≈3500 ULP／875×"）—— **登记待办**（§22.4），**本轮不改代码注释**（避免动被测物）；
⑤ **D-7（身份档案）** —— 按本轮指令**继续暂缓**；
⑥ **H 层（真机）判据 0 条** —— 能力矩阵中如实标注"未建立"。

**⑦ 真实缺陷（本轮 3 条 ＋ 1 条环境异常）**

① ★★ **我自己写的"能力条目"不成立（＝能力矩阵的第一次实战抓错）** —— 上一稿 `BASELINE §22.5` 记「本机可做 boot 层类型检查」，命令是
   `cargo +nightly-x86_64-pc-windows-gnu check -p meta-kernel-boot-kernel --target x86_64-unknown-none`。**实测不成立**：
   - **从仓库根跑 ⇒ 必然失败**：`error: package ID specification 'meta-kernel-boot-kernel' did not match any packages`
     ⇒ 根因：**`meta-kernel-boot` 是独立 workspace**（其 `Cargo.toml` 显式 `[workspace]`，**不入父仓库**；且其 `.cargo/config.toml` 的 `bindeps = true` 是**必需**配置，靠 **cwd 向上查找**生效）；
   - **订正后**（`cd meta-kernel-boot` ＋ 显式 GNU ＋ `--target-dir` 指向系统 Temp）⇒ **`error = 0`**，**正反对照成立**（见下条）。
   **性质**：与旧记"boot 层**连 `cargo check` 都跑不起来**"**同病**（都是把**环境因**写成**项目结论**）。**新编号 R85**。

② ★ **我上一稿的"底图陈旧项"引用错了** —— `§22.6` 写「**底图 `README.md` 行 14／16** 写『机制清单（19 项）／硬规则 C1–C14』」。**实测**：`grep` 根 `README.md` **全 132 行**，**通篇无**该字样；**真出处＝`coordination/README.md` 行 14／15／16**（**脚手架，非底图**）。
   ⇒ **性质变了**：不是"底图待授权"而是"**脚手架可直接订正**" ⇒ **本轮已顺手订正**（3 行；零风险、可回退）。

③ **判据设计缺陷（当场修正）** —— 我给 boot 类型检查做**阴性对照**时，第一版脚本的"命中判据"是"**错误输出里必须出现我注入的**函数名**" ⇒ **假阴性（`grep` 得 0）**。
   实测 rustc 报错按 **`error[E0308] … --> kernel\src\present.rs:587:18`（行号 ＋ 原文）** 给位置，**不保证**出现函数名 ⇒ 判据改为**行号 ＋ 原文**才成立。

④ **环境异常（非项目缺陷，如实登记）** —— boot 类型检查若把构建产物写进 **workspace 内**（含 boot 默认 `target/`、仓库根 `target/` 子目录），本机**不稳定失败**（`os error 2`）：**实测 0/3 成功**；**系统 Temp 3/3 成功**。
   **已逐条排除**：包名不允许／目录权限（bash 建目录写文件**均成功**）／目录不存在（预建后**仍失败**）／cargo 配置／环境变量。**原因＝未定**，按「**阴性先怀疑工具链**」登记，**不写成"项目不可做"**；**误差如实登记**：11:19 曾有一段**窗口期**同一路径 **3 次成功**，11:21 起转 0/3，**该窗口未复现**。

**⑧ 三量台账（四量）**

| 量 | 内容 |
|---|---|
| **存量** | 带入的：crate 8 个（`meta-kernel-core`／`-nostd`／`-mem`／`-boot`／`npb-gateway`／`manifest-ui`／`host/field-render`／`host/sky-browser`）｜`nostd` lib **474** 测试｜host 镜像 **8**｜机制 **25**｜C **19**｜T **34**｜D **44**｜R **45**｜CI 三 job（`test`／`boot-image`／`field-render`）｜第 ⑫ 段（自检 121–125／product 126–128） |
| **变量** | 本轮**改动 13 文件**：代码 5（`fb.rs`／`field/nca.rs`／`mirror_bare_assertions.rs`／`present.rs`／`main.rs`）＋CI 1（`ci.yml` ⑫ 段扩项）＋治理 5（`CONSTRAINTS.md`／`CHARTER.md`／`TEMPLATES.md`／`ROADMAP.md`／`coordination/README.md`）＋台账 1（`BASELINE.md`）＋报告 1 |
| **补充增量** | **既有能力的补全与订正**（非新能力）：① `present.rs` 头注的 boot 命令**订正**（＋`cd` ＋`--target-dir` ＋环境说明）；② `TEMPLATES §14.2`／`T-035` 行的 M 层命令**订正**；③ `coordination/README.md` 3 行陈旧项**订正**；④ `§22.7` 补写 **D／R 只计数、不做连续性**的理由（防后人误加判据）；⑤ 项目记忆的 boot 能力条目**重写**（并记下三稿沿革） |
| **创新增量** | **全新能力（本轮首次出现）**：① **场演化入口** `present_evolve_selftest`（编号 **129–132**）＋ `nca::evolve`／`forward_const_weights`；② **退化分类器** `fb::Degenerate`／`classify_degenerate` ＋ 编号 **133／134**（**三条入口全部机器化**）；③ **C20**（CI 结论不入库）；④ **机制 26**（能力矩阵）；⑤ **T-035**；⑥ **R83／R84／R85** 三条新编号 |

**⑨ 边界说明（四类 · 与机制 26 四层对齐）**

| 类 | 层 | 本轮实测内容（**命令级**） |
|---|---|---|
| **本地跑过** | **M** | `cargo test -p meta-kernel-core-nostd --lib` ⇒ **477 passed / 0 failed**｜`--test mirror_bare_assertions mirror_present` ⇒ **13 passed**｜`cargo build -p meta-kernel-core-nostd --target x86_64-unknown-none` ⇒ **OK**｜**boot 类型检查**（订正命令）⇒ **error 0 / warning 3**（全属**既存** `fourier.rs`／`thinking_chain.rs`，**无一条涉及本轮新增代码**）｜判据脚本 **14 项全绿**（机制 17／18／21／22／24／25 ＋ 迁移闭包／保真 ＋ 屏幕转储，含 `--selftest` 与实跑）｜**C15** 双向计数通过 |
| **CI 过** | **CB** | ⏳ **待本轮 push 后实取**（`gh run list --limit 3`）。**按 C20：结论不进仓库** ⇒ 见**当轮对话返回／日报**。**不预写"全绿"** |
| **真实跑过** | **CQ** | ⏳ **待本轮 push 后**由 `boot-image` job（QEMU **真实引导** ＋ 整屏像素断言）证成。**本轮新增断言已预置**：CI ⑫ 段直查 `present::present_evolve_selftest`／`nca::evolve(`／`rhythm_steps`／`classify_degenerate(`／`return CODE_DEG_*` ＋ **R83 负断言** ＋ 码循环扩到 **121–134** |
| **未验证** | **H** ＋ 上述缺口 | ❌ **真机（H）**：0 判据（含 WC 显存回读）｜❌ **场演化扩到全屏**｜❌ **boot 层 full build**（缺 `dlltool.exe`）｜❌ **本机 QEMU 真实引导**（无 `qemu-system-x86_64`）｜❌ **D-7 身份档案**（按指令暂缓）｜❌ `fmath.rs` doc 旧值（登记待办） |

**⑩ 决策评估（七要素）** —— 本轮**新增一项需裁**（R84 版本号口径）

| 要素 | 内容 |
|---|---|
| **评估** | 本轮发现**底图与实现的口径分歧**：底图 `README.md` **行 90** 明写「**版本号 = `git rev-list --count HEAD`**（首提交 = v0.001）」。**逐条实证**（70 条提交）：**差值 = 0** 成立（`v0.078`↔78、`v0.079`↔79、`v0.076`↔`47341e6` = 76）；但 **count 115 起 +1**（1 条）、**count 230 起 +5**（**近 14 条**）⇒ 本项目近期 label = **count − 5** |
| **优势** | ① **改回底图口径**：与底图一致、**一个数**、无需记忆偏移、**新人不会算错**；② **修订底图**：把"本仓库 label = count − 5"写进底图，代价是**改底图须 `--update-baseline`** |
| **劣势** | ① 改回口径 ⇒ **历史 label 与未来 label 会出现 +5 的断裂**（v0.225–v0.238 一段）；② 修订底图 ⇒ 偏离"版本＝提交计数"的**原始简明定义**，且**漂移原因仍未查明**（不知那 5 条是什么） |
| **风险** | ★ **不做**的风险最大：**再漂一次就无人能判断哪个数对**；且**报告／BASELINE／ROADMAP 三处引用口径不一**，属 **R14／C19 家族**（同名概念两个口径）。**改为 count** 的风险＝一段历史 label 对不上账；**修订底图**的风险＝把"漂移"固化成"规则"（**恰恰是 R84 这个病本身**） |
| **机会** | 顺手可查清**那 5 条是什么**（`git log` 逐条看是否为**非实质性提交**，如 merge／空提交／chore），若查明 ⇒ 可精确决定"改口径"还是"改底图" |
| **建议** | **推荐：先查清那 5 条，再改口径**（**本轮不擅改**，版本口径属**元规则** ⇒ 按流水线须**先分析、再确认**）。**理由**：现在两种做法都可能是"把漂移当规则"；**查明来源后**，若那 5 条是 merge 提交 ⇒ **应改回 count**（口径回归底图，零歧义） |
| **等待决策** | 用户一句话：**R84 按哪条走**（见 ⑪①） |

**⑪ 需要你决策**
① **R84 版本号口径**：**(a)** 改回**底图口径**（label = `git rev-list --count HEAD`，接受历史段 +5 断裂）／**(b)** 修订**底图**把 `count − 5` 写成规则／**(c)** 先让我**查清那 5 条**再定（**推荐**）。**本轮未擅改**（元规则）。
② **场源是否从 9×9 扩到全屏**：本轮已按裁定"先小范围验证"跑通（129–132）。**扩到全屏**会撞上**多重网格 `2^k+1` 铁律**（1920×1080 不是 `2^k+1`）⇒ 需先定**网格尺寸策略**。**是现在定，还是等 CI 先证成 129–132？**
③ **底图 `README.md` 是否需要订正**：★ 更正一条 —— **上一稿我误报**「底图 `README.md` 行 14／16 陈旧」（见 ⑦②）：**实测底图 `README.md` 通篇无该内容，本轮底图零改动**。⇒ **底图侧当前无需授权订正**；**`coordination/README.md`（脚手架）已由我顺手订正 3 行**。**如你认为该订正超出授权，请示意回退**（`git checkout -- coordination/README.md`）。
④ **D-7（身份档案）**：按指令继续暂缓 —— **如无变化不再问**。

**⑫ 下一步建议**
1. **本轮 push 后**：取 `test` job ＋ `boot-image` job（**QEMU 真实引导**）结论 —— **按 C20 只记日报／对话返回**，**不改仓库**；
2. **R84 查清那 5 条**（`git log --oneline --reverse` 逐条比差）⇒ 出一页结论，再请你裁口径；
3. **场源扩大**：定**网格尺寸策略**（`2^k+1` 与屏幕分辨率的映射）后，把 9×9 换到全屏；
4. **H 层判据**：真机帧缓冲回读（WC 显存语义）——目前 **0 判据**，是能力矩阵**唯一空白层**；
5. **顺手可做**：`fmath.rs` doc 旧值订正（**登记待办**，本轮为不动被测物而未改）。

**⑬ 启动回执**：已读 `CHARTER.md`。版本 = `git rev-list --count HEAD`（**不手写**，C2；本轮起点 count = 243，label v0.238，**口径分歧待裁 R84**）｜本次任务：**4 项裁定落地 ＋ 2 项机制建设**｜**是否继续：等待裁定（⑪ ①②；③④ 已处置／无变化）**。

**⑭ 本轮读了底图哪几份、哪几行**（D55／D52：报告须写明；**行号均当轮实测**，R75）

- **根 `README.md` 全文 132 行**（实测 `wc -l` ＝ **132**）—— 本轮**实取引用**：**行 89–90**（版本规则：行 89 ＝ 格式固定 `v0.xxx`；**行 90 ＝ 「版本号 = git 提交计数」，即 R84 的底图出处**）｜**行 91–92**（权威统计点：**行 92 ＝ L4 交付 = v0.076，HEAD `47341e6`**，用于逐条核 count）｜**行 13–20**（三层次表）｜**grep 全文件**：确认**通篇无**"机制清单（19 项）"或"C1–C14"（⇒ 坐实 ⑦② 的引用订正）。
- **`docs/LAYER_ARCHITECTURE.md`** —— **行 307**（**呈现** ✅ 已实现，落点 `manifest-ui/`）｜**行 313**（**「呈现不增义」**＝ D8 依据）。全文 449 行（实测）。
- **`docs/FIELD_PRESENTATION_DESIGN.md`** 全文 **183 行**（实测 `wc -l`）—— 呈现层设计（**本轮只读**，未改）。
- **`docs/FIELD_RENDER_SOURCELESS_DESIGN.md`** 全文 **132 行**（实测）—— 无源场呈现设计（**本轮只读**，未改）。
- **`docs/` 目录**：`ls docs/*.md | wc -l` ＝ **35 份**（实测，与机制 22 基线 **36 份**＝ `README` ＋ `docs/` 35 一致）。
- 脚手架（**非底图**，改动落点）：`coordination/CHARTER.md`（机制表 ＋ 机制 26 行 ＋ 四层定义）｜`coordination/CONSTRAINTS.md`（C20 ＋ C1–C20 速查）｜`coordination/TEMPLATES.md`（**行 104–118** ＝ 阅读指令逐字源｜§14.2／T-035 行）｜`coordination/ROADMAP.md`（子任务2 D-1 结项块 ＋ ⑫ 段）｜`coordination/README.md`（行 14／15／16 陈旧项）｜`coordination/BASELINE.md`（§21.7／§22 全节）。
- **被测物**：`meta-kernel-boot/kernel/src/present.rs`（**全文**；头注订正 ＋ 129–134）｜`meta-kernel-boot/kernel/src/main.rs`（`run()` 接线）｜`meta-kernel-boot/kernel/Cargo.toml`＋`meta-kernel-boot/Cargo.toml`＋`meta-kernel-boot/.cargo/config.toml`（**证明"独立 workspace"与 `bindeps`**）｜`meta-kernel-core-nostd/src/fb.rs`／`src/field/nca.rs`／`tests/mirror_bare_assertions.rs`｜`.github/workflows/ci.yml`（⑫ 段）｜根 `Cargo.toml`（members 表，证 boot **不在**父 workspace）。

---

**一句话**：**4 项裁定全部落地**（场源换场演化**小范围跑通** 129–132／**C20** CI 结论不入库／**D-1 结项**／退化出口**机器化** 133–134）＋ **2 项机制建成**（**机制 26 能力矩阵**＋ **T-035**），并且 —— ★ **机制 26 立起来的第一分钟就抓出了我自己写错的能力条目**（`BASELINE §22.5` 那条 boot 命令**从仓库根跑不成立**：boot 是**独立 workspace**），同时**自纠**了另一条**引用错**（"底图 `README.md` 陈旧"实为 `coordination/README.md` 陈旧，**底图本轮零改动**）。**唯一需要你点头的：R84 版本号口径按哪条走（推荐先查清那 5 条）。**
