【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 今夜 P3 清单 · 2026-09-20（机制 19 夜间自动化 · 开工前置）

**保密标记**（对应 C12；**拷贝给顾问前先看这一段**）：
- ⛔ 机密（**绝不拷**）：**无** —— 本清单按 C12 已预屏蔽：不含凭证、账号、本机绝对路径、网络细节、参数库取值（阈值一律只写"见编号"，不复述数值）
- 🔐 保密（**屏蔽后拷**）：**无**
- 🔒 半公开（**可拷**）：仓库内相对路径与行号、编号体系（R／D／C／T／机制号）、模块名与计数口径
- 🔓 公开（**直接拷**）：版本号、阶段定位、清单条目、验收结论

> 【给用户的提示】本次报告包含 **0** 处机密、**0** 处保密。无需屏蔽即可整份拷给顾问。

**全景状态卡**（自动生成；含版本号／阶段定位／待决策项／CI 状态）：

```
┌─ 云内核项目 · 全景状态卡 ─────────────┐
│ 版本   v0.259（rev-list --count HEAD） │
│        HEAD 93393cd，未同步数 1        │
│        （ahead 1 / behind 0）          │
│ 阶段   ROADMAP 阶段二 · 子任务2        │
│        2.1 ✅ / 2.2 ✅ / 2.3 ✅／      │
│        2.3b ✅ 已收口（全源：未迁 0）  │
│        2.4 product 已接线（口径修正为  │
│        轻量帧缓冲 + L6 接入）           │
│        后续：2.5（P3，卡决策）／2.6    │
│        （P3，卡 C9）／2.7（P2）        │
│ CI     本夜未触发（只读任务）          │
│        按 C20：CI 结论不入库           │
│ 漏洞   R1–R89                          │
│        未解除：R5🟢／R8🟡／R20／R27⏳  │
│ 治理   机制 27 / C 21 / T 36 / D 44 /  │
│        R 52（C15 复算无缺号无重号）    │
│ 决策   待用户放行 4 项（F/G/H/I）＋    │
│        待确认 D42–D49（8 项）          │
└────────────────────────────────────────┘
```

**任务**（含**基准**与**总规划定位**，含**优先级 P0–P4**）：生成「今夜 P3 清单」（机制 19／C14 开工前置）｜基准 v0.259｜定位：阶段二 · 子任务2（2.4 product 已接线，2.5／2.6 均卡在前置决策）｜**优先级 P3**（无需决策、可自主推进）｜**边界：只读 ＋ 只写本清单，不改代码、不改治理文件、不与远端同步**

**状态**：完成

**验收对照**（标准 vs 实测，逐条）：

| # | 验收标准（指令原文拆解） | 实测 | 结论 |
|---|---|---|---|
| 1 | 输出**第 0 行**＝「顾问阅读指令」固定文本（C13） | 本文件字面第 1 行即为该段（起首无前置空行）；**按权威源 9 步**（见 §三 缺陷-5：指令写 8 步、权威源 9 步） | ✅ |
| 2 | 清单**只取 P3 且「可夜间跑＝✅」** | 5 项，逐项标 P3 ＋ 可夜间跑 ✅（见 §一） | ✅（**含一项结构性发现**，见 §三 缺陷-2） |
| 3 | 每项写清【任务／依据／预计时长／可夜间跑】 | 5 项四字段齐全；依据**逐项给行定位**；**5 项依据均不在 ROADMAP**（原因见 §三 缺陷-2，已如实标明） | ✅（**带保留**） |
| 4 | **排除**需用户决策（P1）项 | 2.5（卡甲／乙解读）／2.6（卡 C9 放行）／子任务6／D42–D49／R80 四问／F/G/H/I 已排除并联原因（§二） | ✅ |
| 5 | **排除**需真实硬件／真机验证项 | 子任务4 ❌／子任务5（依赖子任务4）／2.7（P2）／老笔记本实测 已排除（§二） | ✅ |
| 6 | **排除**「对外可见／不可逆」动作 | 子任务6（镜像对外分发）／与远端同步／C10 第 1 项所列动作／切换模型后端 已排除（§二） | ✅ |
| 7 | **排除治理文件类改动**（CONSTRAINTS／CHARTER／ROADMAP／BASELINE／TEMPLATES／`.github/workflows/`） | 5 项**无一**含治理文件或 CI 配置改动；「CI 优化」**整类排除**；§三 发现的观测缺陷**只登记、不改** | ✅ |
| 8 | 写入 `coordination/reports/<今日日期>_今夜P3清单.md`（同日只保留一份） | 本文件（2026-09-20）；**核查：同目录无 09-20 同名文件**（检索四要素见 §三 缺陷-4）⇒ **新写**非覆盖 | ✅ |
| 9 | 清单**不含 ⛔ 内容** | 实测：无凭证／账号／本机绝对路径／网络细节／参数库取值；仓外物一律只写"仓外"，不写路径 | ✅ |
| 10 | **不与远端同步**、不提交、不改代码 | 全流程只读 ＋ 只新建本 1 份清单；`git status --porcelain` **空**；未改任何源文件（`HEAD` 仍 93393cd） | ✅ |
| 11 | 汇报 ≤6 行 | 对话侧汇报已按要求压缩 | ✅ |

---

## 一、今夜 P3 清单（5 项，按建议执行顺序）

> **执行口径**：① **一律只读 ＋ 只写 `coordination/reports/` 新文件**；**不改治理文件、不改代码、不提交、不与远端同步**。② 若时间不足，**按序执行、可截断**，截断处须在报告中写明。③ 每项完成→**独立报告**落 `coordination/reports/`。④ 每项均属 **P3 白名单**（调研／文档／测试补充／重构／清理／基准维护／CI 优化／报告归档）中的可见类别，已在每项标明。

### P3-1｜`docs/` 时效核对稿「**第二批**」（只出稿，**不改 `docs/`、不改基线**）

- **任务**：对第一批**未覆盖**的 9 份文档的其余章节做**定向时效扫**（家族：设计-实现对齐／版本号族），逐项给【文件:行／原文／应改为／依据／活跃段 or 历史段】；**观测不到的写"无法观测"、不反推**（C17）。
- **依据**：`coordination/reports/2026-09-20_docs文档时效核对稿.md` **§6「本批未覆盖（诚实清单 —— 供下一批）」L138–144**（9 份文档其余章节）＋ `BASELINE.md` **§28.7 待裁③**（原文"**推荐：可夜间跑**"）。⚠️ **依据不在 ROADMAP** —— 见 §三 缺陷-2。**P3 白名单类别＝调研／文档**。
- **预计时长**：约 60 分钟
- **可夜间跑**：✅（**只出稿＝零副作用**；正本执行（改 `docs/`）须用户授权 ＋ 过判据 F `--update-baseline` ⇒ 留白天）

### P3-2｜判据**全覆盖回归** ＋「**CI 覆盖 ↔ 本机覆盖**」差集对账（只读，只出表）

- **任务**：① 对 `coordination/tools/` **全部 8 个判据脚本**逐一实跑并记三态；② 建**两张对账表**：「判据 ↔ 是否入 CI」（含 CI 行号）与「判据 ↔ **有／无正反对照**」；③ 对**无对照**的判据按 **R83**（空转须与"生效"取不同值）出**对照设计**（**只出设计，不改脚本**）；④ 明确标出**"本机专属"判据**，供每晚自检清单固化。
- **依据**：`.github/workflows/ci.yml` 实跑 **6 类**（`check_private_pointers` L212／`check_kernel_purity` L229／`check_source_of_truth` L254／`check_endpoint_table` L309／`check_doc_consistency` L335／`check_reading_instruction` L353）＋ **阴性对照实测**：`check_migration_closure`／`check_migration_fidelity` 在 `ci.yml` 命中 **0**（**阳性对照**：`check_doc_consistency` 命中 **2**）⇒ **closure／fidelity 属本机专属**（09-19 清单 §三 已记该口径，本夜**实测复核**）。⚠️ **依据不在 ROADMAP** —— 见 §三 缺陷-2。**P3 白名单类别＝测试补充／CI 优化（只做对账，不动 CI 配置）**。
- **预计时长**：约 40 分钟
- **可夜间跑**：✅（纯只读实跑；**改判据会制造假绿风险 ⇒ 今夜只出对账表**）

### P3-3｜**R5「二进制 banner 版本号口径」评估 ＋ 处置方案包**（只读出稿，**不改码**）

- **任务**：① 定位 banner 的取值点与注入链路（**读实现、不猜**，遵 R29：期望值必须先读实现）；② 给出**可证伪判据**："构建产物自报版本"与 **机制 27**（版本号＝count）口径差能否被一条命令检出；③ 出**三选项**（改注入 count／保留 ＋ 显式标注为包版本／双行并列）——每项给**改动面**（哪些文件、是否影响既有产物）、**回退方式**与**可证伪条件**；④ **不代行决策**（C11）。
- **依据**：`BASELINE.md:97` **R5**（状态 **🟢 待评估**，原文"二进制 banner 显示 `CARGO_PKG_VERSION` 而非 commit count"）＋ **机制 27**（版本号＝count）＋ `BASELINE.md` **§23.7**（2026-09-20 起 **label ＝ count**）。⚠️ **依据不在 ROADMAP** —— 见 §三 缺陷-2。**P3 白名单类别＝调研／清理（口径不一致类）**。
- **预计时长**：约 35 分钟
- **可夜间跑**：✅（纯阅读 ＋ 出稿；改注入链路会动构建产物口径 ⇒ **今夜禁改码**）

### P3-4｜「**活跃状态型／计数型陈述**」**第二轮**过期点普查（只读出清单，**治理文件禁改**）

- **任务**：① 逐文件**取证式**扫描活跃状态陈述与计数型陈述（`BASELINE.md` §五／§六／§九、`advisor_brief.md`、`ROADMAP.md`、`CHARTER.md`）；② 输出**过期点清单**（文件:行／现文／实测事实／依据／**是否治理文件**）；③ **治理文件内的只登记不修改**（供白天一处修、过 **C15** 双向计数）；④ 与**首轮**（09-19 P3-4）结果做**差集**，标出"首轮已登记但**至今仍在**"的点（**防"只登记不处置"变成静默积压** —— R87 家族）。
- **依据**：`BASELINE.md` **§18.4**（R82 处置范式：**保留留痕 ＋ 后记指针**）＋ **§28.7 待裁①**；**本夜已实测 1 处残留**：`CHARTER.md:225` 仍写「**8 步**」（**权威源** `TEMPLATES.md` §二 第〇项／C13／`CHARTER.md:87`·`:140` 均为 **9 步**）⇒ **R41／C13 家族**。⚠️ **依据不在 ROADMAP** —— 见 §三 缺陷-2。**P3 白名单类别＝文档／基准维护（只出清单）**。
- **预计时长**：约 45 分钟
- **可夜间跑**：✅（纯只读；**治理文件整类夜间禁改**）

### P3-5｜**汇编副本 ↔ 底图 同步性复核** ＋ 同步判据「**可复现性**」评估（只读，只出稿）

- **任务**：① 对 `coordination/discussions/` 的四批正文 ＋ 合订本，与**当前** `docs/`（35 份）＋ 根 `README.md` 做**逐字比对**，列出**差异份数／差异行**；② 评估**同步判据的落点**：R78 的"同步判据"当前是否**可被他人复现**；③ 给出「是否值得在仓内固化同一口径」的**评估结论**（**今夜只评估：不建文件、不入 CI**）。
- **依据**：`BASELINE.md` **§15.5 R78**（汇编副本**随底图变更而静默过期**）＋ **§16.6**（R78 处置：同步判据＝`verify_asm.py`／`verify_binder.py`，"逐字比对**当前**源"）＋ **§23.5**（汇编覆盖范围缺口 R86）。⚠️ **依据不在 ROADMAP** —— 见 §三 缺陷-2。**P3 白名单类别＝报告归档／基准维护（BASELINE 以外的归档）**。
- **回填证据（本夜已实测，见 §三 缺陷-3）**：三个同步脚本 `verify_asm.py`／`verify_binder.py`／`asm_selfcheck_0920.py` **均在仓库外**（`git ls-files` 无同名 → 阴性；`coordination/tools/` 内"汇编"命中 **0** → 阴性；**阳性对照**＝`git ls-files -- coordination/discussions/` 命中 **23** 份被跟踪产物）⇒ **R78 的"已生效"当前不可复现**。
- **预计时长**：约 50 分钟
- **可夜间跑**：✅（纯只读比对；**固化判据可选落点评估留白天**）

> **总计**：约 **3 小时 50 分钟**（21:00 清单 → 23:00 开工 → 约 02:50 完成；余量留给报告撰写与自检）。

---

## 二、本期明确排除项（附原因 —— 排除也是结论，一并留痕）

| # | 排除对象 | 排除原因 |
|---|---|---|
| 1 | **2.5 `field-render` 作首个用户空间进程**（ROADMAP **标 P3 ✅**） | ★ **实质是 P1**：09-19 P3-1 已定论"字面存在**甲／乙两种解读**（搬 wgpu 栈 vs 另写最小用户进程）"⇒ 报告原文"**这属决策（C11），夜间不得代行**" ⇒ **今夜排除**（缺陷-2 实例）。 |
| 2 | **2.6 ACPI/CPUID 读取硬件信息**（ROADMAP **标 P3 ✅**） | ★ **实质是 P1**：实施须**新增 1 个 unsafe 边界模块**（`CPUID` 内联汇编）⇒ **C9 白名单放行须用户确认**（2.6 报告 §十 第 3 项："本夜只报备、不申请放行"）⇒ **今夜排除**（缺陷-2 实例）。 |
| 3 | **子任务5 安装程序**（ROADMAP 标 P3 ✅） | **依赖子任务4**，而子任务4 **❌ 需真机** ⇒ **前置未达成**，不得以"P3"名义抢跑 |
| 4 | **子任务4 硬件适配／老笔记本实测** | **❌ 需真机**（`ROADMAP.md:92`、`BASELINE.md:72`） |
| 5 | **2.7 生成可引导 ISO ＋ QEMU 验证** | **P2 需验证**（真机引导环节不可夜间跑，`ROADMAP.md:63`） |
| 6 | **2.4 实现推进（帧缓冲／呈现层）** | **P1**：2.4 相关**设计稿 Q 表**仍在待裁；本夜不代行决策（C11） |
| 7 | **R80 四问**（测试计数类过期规则） | **待发起人确认**（规则未定 ⇒ 夜间不得自定） |
| 8 | **机制 25 扩展以覆盖"测试计数"类** | 同 #7（**先裁后建**） |
| 9 | **D42–D49（8 项待确认）** | **P1 需决策**；其中 D42（签到类定时任务）涉登录态 ⇒ **对外动作**（C10） |
| 10 | **待用户放行 4 项（F/G/H/I）** | **P1**：F＝本轮版本是否与远端同步｜G＝第三条过期一次性平台任务是否下线｜H＝`advisor_brief:35`（P2）是否加限定｜I＝`docs/` 时效第二批**开跑与否**（**本清单 P3-1 只出稿不改文件**，故不致越权） |
| 11 | **治理文件改动**（`CONSTRAINTS.md`／`CHARTER.md`／`ROADMAP.md`／`BASELINE.md`／`TEMPLATES.md`／`.github/workflows/`） | **任务书明令整类排除**；连带排除 P3 白名单里的「**CI 优化**」类（含把 `closure`／`fidelity` 入 CI） |
| 12 | **R27 修复本体**（`l5_baseline` 编码/解码不对称） | **⏳ 未处置**：原文注明"属**行为修复**，须走 **C4：先列清单**" ⇒ 首轮已出清单、**尚未获确认** ⇒ 今夜不落码（避免重复出稿） |
| 13 | **R8（quad-check 喜欢维）** | **P2**（`BASELINE.md:100`）；且须在**内核映射层**改，"禁止宿主调参打绿" |
| 14 | **历史报告 C13 回填** | "不回填"建议已登记但**未裁定**；且会改历史快照 ⇒ 排除 |
| 15 | **身份档案建立（`SOUL.md`／`IDENTITY.md`／`USER.md`）** | **需用户在场 ＋ 写用户级文件** ⇒ 不属无人监督夜间任务 |
| 16 | **对外可见或不可逆动作 / 与远端同步 / 切换模型后端** | **C10 第 1 项 ＋ D33 ＋ `TEMPLATES.md` §11.8**（夜间默认） |
| 17 | **根 `README.md`／`docs/` 正文改动** | 属**底图**，改动须授权 ＋ 过 **判据 F**（`basemap_hashes.txt` 36 份）⇒ 本清单只出**核对稿**，不动正文 |

---

## 三、真实缺陷（本次发现的，**只登记不改**）

**缺陷-1（状态型残留 · R41／C13 家族）：`CHARTER.md:225` 仍写「8 步」，权威源为 9 步**
- 证据：`CHARTER.md:225` 现文＝"**顾问收到返回后该做什么（8 步）** + 报告必带十四项"；同文件 `:87`／`:140` 均写 **9 步**，`TEMPLATES.md` §二 第〇项（固定文本）为 **9 步**。
- 性质：**活跃状态陈述过期**（`CHARTER.md` 属**治理文件** ⇒ **今夜只登记**；白天一处修 ＋ 过 **C15** 双向计数）。

**缺陷-2（规划缺口 · 本夜的关键发现）：ROADMAP 中「P3 ＋ 可夜间跑 ✅」的行**全部不可执行（**0 项可跑**）
- 证据（逐行）：`ROADMAP.md:61` **2.5**（P3 ✅）⇒ 卡**甲／乙决策**（见 §二 #1）；`ROADMAP.md:62` **2.6**（P3 ✅）⇒ 卡 **C9 白名单放行**（见 §二 #2）；`ROADMAP.md:99–100` **子任务5**（P3 ✅）⇒ **依赖子任务4**（❌ 需真机，见 §二 #3）；`ROADMAP.md:136` **M3** 标 **P1**。
- ⇒ 任务书要求"依据（ROADMAP **哪一行**）"在本夜**结构性无法满足**：不只是 09-19 记的"只剩 2.5／2.6"，而是"**即便只剩的两项，也各自卡在决策／放行上**"。
- **建议（白天 · 治理文件）**：`ROADMAP.md` 的「可夜间跑」列补三个类别 ——**① 遗留缺陷修复（R5／R27 一类）② 判据维护与回归 ③ 归档同步**，否则每晚清单都会被迫"越出 ROADMAP"（**连续两夜同款**）。

**缺陷-3（声明 ≠ 可复现 · R89 家族）：R78 的"汇编同步判据已生效"**在仓内**不可复现**
- 证据：`BASELINE.md` §16.6 记同步判据＝`verify_asm.py`／`verify_binder.py`；**本夜实测**：三个相关脚本（含 `asm_selfcheck_0920.py`）**均在仓库外**（`git ls-files` **无同名**；`coordination/tools/` 内"汇编"命中 **0**）。
- **阳性对照**：`git ls-files -- coordination/discussions/` 命中 **23** 份被跟踪产物（四批正文 ＋ 合订本在仓内）⇒ 检索式有效、产物确在仓内、**判据不在**。
- 性质：**判据在仓外 ⇒ 他人无法复现"汇编与底图同步"这一结论**（与 **R89「声明 ≠ 发生」**同族；亦与 **R32**"判据不可复现"同族）。**今夜只登记**（P3-5 只做比对与评估）。

**缺陷-4（机制链 · 采样第 4 夜）：`2026-09-18_今夜P3清单.md` 确未生成；本夜为**第 3 份**
- ① **检索式**：`git ls-files -z | tr '\0' '\n' | grep -i 今夜`（**用 `-z`，遵 R81**）＋ `ls coordination/reports/ | grep -i 今夜`；
- ② **范围**：全仓**跟踪文件** ＋ `coordination/reports/`（**83 份**）；
- ③ **命中数**：**2** —— `2026-09-17_今夜P3清单.md`｜`2026-09-19_今夜P3清单.md` ⇒ **`09-18` 缺**；
- ④ **阳性对照**：检索式**命中已有两份**（有效）；**阴性对照**：`zzz_not_exist_zzz` ⇒ **0**。
- ⇒ 机制 19 前置链 **n=4 采样中 3 次齐备**（09-17 ✅／09-18 ❌／09-19 ✅／**09-20 ✅＝本清单**）。**根因未定**（可能牵出平台侧），**今夜只登记**。

**缺陷-5（口径冲突 · 连续第 3 夜）：任务书写"阅读指令 8 步"，权威源是 **9 步**
- 权威源：`TEMPLATES.md` §二 第〇项（**9 步**，T-018 生效）／`CONSTRAINTS.md` C13／`CHARTER.md:87`·`:140`。
- 处置：**按权威源 9 步全文粘贴**（本文件第 0 行）。
- ⚠️ **建议（需用户 · 平台侧，不入库）**：本差异**连续三夜出现** ⇒ 疑为 **21:00 定时任务的任务书文本本身未更新**（与 §二 #10 的 G 同族：**平台侧过期配置**）。**建议白天把该定时任务的 prompt 里的步数改为 9 步**（一句话即可，可逆）。

---

## 四、三量台账（C6）

| 量 | 内容 |
|---|---|
| **存量** | 读 `CHARTER.md`／`CONSTRAINTS.md`（C10／C12／C13／C14）／`TEMPLATES.md` §二 第〇项·§三·§十一／`BASELINE.md`（§五／§六／§九／§十五–§二十八）／`ROADMAP.md` 全文；`coordination/tools/` **8 个判据**实跑；`reports/` **83 份**目录清点；`ci.yml` 判据接线核对 |
| **变量** | **0** —— 本清单**未改动任何既有文件**（治理文件、代码、CI 配置、判据工具一律未动） |
| **补充增量** | **1 份**：本清单（`coordination/reports/2026-09-20_今夜P3清单.md`，**新写非覆盖**） |
| **创新增量** | **0**（**缺陷-2／缺陷-3 两条新观测**，属"存量缺陷的显式化"，不计增量） |

---

## 五、边界说明（C5；四类齐全）

- **本地跑过**：✅ 有，**判据 8／8 全 PASS（exit=0）** —— 自检类：`check_kernel_purity --selftest`｜`check_doc_consistency --selftest`｜`check_migration_closure --selftest`｜`check_migration_fidelity --selftest`｜`check_endpoint_table --selftest`｜`check_source_of_truth --selftest`｜`check_reading_instruction --selftest`；实跑类：`check_private_pointers --mode=syntax`｜`check_kernel_purity`｜`check_doc_consistency`｜`check_migration_closure`（结果"**封闭**"）｜`check_endpoint_table`（结果"**一致**"）｜`check_source_of_truth --ci`（**底图 36 份，PASS**）｜`check_reading_instruction`（**报告 83／豁免 41／受检 42，PASS**）。注：`check_migration_fidelity` 为**逐文件对**用法（无全仓入口），本夜只跑其 `--selftest`。
- **CI 过**：❌ **无** —— 本任务只读，**未触发 CI**。按 **C20**：**CI 结论不入库**（状态卡不写 CI 结论；如需 CI 事实，见当日日报）。
- **真实跑过**：❌ **无** —— 未跑 QEMU、未上真机。
- **未验证**：① 清单 **5 项均未执行**（属 23:00 开工范围）；② 各项**预计时长均为估计值**，未实测；③ 缺陷-2 的"ROADMAP 中 P3＋✅ 行全部不可执行"基于**逐行实读**，但**未穷举 ROADMAP 全表语义**（只取 `| **P3**` 形态的行 ＋ 里程碑表）；④ 缺陷-4 的"09-18 缺"基于**本地克隆**，**未查平台侧任务日志**；⑤ 缺陷-3 的"判据在仓外"基于 `git ls-files`／目录检索，**未逐脚本实跑比对**（属 P3-5 范围）。

---

## 六、决策评估（如有新决策，按 §九 七要素）

**本轮无新决策。** 本清单为"只读 ＋ 只写清单"，**不含需要拍板的事项**；F／G／H／I 与 D42–D49、R80 四问**一律保持待裁状态**，不由本清单代行（C11）。§三 四条缺陷均为**登记**，**处置建议已写明、动作留给白天**。

---

## 七、需要用户决策（**白天**；本清单只列，不催不动）

| 级别 | 事项 | 来源 |
|---|---|---|
| P1 | **F** 本轮版本是否与远端同步（机制 23：上轮仅授权至 v0.258，v0.259 未授权） | `BASELINE.md` §28.0／§28.7 |
| P1 | **G** 第三条过期一次性平台任务是否下线（**推荐：下线**） | `BASELINE.md` §28.5 观察项 |
| P1 | **H** `advisor_brief:35`（P2「不做 f64 版」）是否加限定（**推荐：按稿加括注**） | `BASELINE.md` §28.7 待裁② |
| P1 | **I** `docs/` 时效第二批**开跑授权**（本清单 P3-1 只出稿；改 `docs/` 须授权 ＋ 过判据 F） | `BASELINE.md` §28.7 待裁③ |
| P1 | **平台侧**：21:00 定时任务 prompt 的"阅读指令步数"由 8 → 9（**连续三夜同款冲突**） | §三 缺陷-5 |
| P1 | D42–D49（8 项）｜R80 四问 | `BASELINE.md` §九 |
| P2 | **2.5 甲／乙解读**（在裸机搬 wgpu 栈 vs 另写最小用户进程） | 09-19 P3-1 报告 §二 |
| P2 | **2.6 是否新开 C9 白名单条目**（`CPUID` 边界模块） | 2.6 报告 §十 第 3 项 |
| P3（建议项） | ROADMAP「可夜间跑」列补三类别（遗留缺陷修复／判据维护／归档同步） | §三 缺陷-2 |

---

## 八、下一步建议

1. **21:00–23:00**：用户可对清单增删改；**无异议则 23:00 自动开工**。
2. **23:00 起**：按 P3-1 → P3-5 顺序执行；每项**独立 commit**（消息前缀 **`夜间 P3-n：`**，遵机制 23 来源判据）＋**独立报告**；**不与远端同步**（D33）。
3. **08:00 前**：出「夜间结果」汇总（按报告模板十四项，第 0 行仍为阅读指令）。
4. **白天**：一处修治理文件（`CHARTER.md:225` 8→9 步；**须过 C15 双向计数**）＋ 处理 §七 待裁项 ＋ 更新平台侧任务书步数。

---

**启动回执**（`CHARTER.md` §五）：已读 `CHARTER.md`（机制 1–27／C1–C21／边界）＋ `CONSTRAINTS.md`（C10／C12／C13／C14 全文）＋ `TEMPLATES.md` §二 第〇项·§三（优先级）·§十一（夜间自动化）＋ `BASELINE.md`（§五 未完成／§六 已知漏洞／§九 待决策／§十五–§二十八）＋ `ROADMAP.md`（可夜间跑列）。
当前状态：**v0.259**（`git rev-list --count HEAD`，未同步数 **1**）｜ROADMAP **阶段二 · 子任务2**（2.1 ✅／2.2 ✅／2.3 ✅／**2.3b ✅ 已收口**；2.4 product 已接线）｜本阶段目标：元内核成为**可引导内核**并**输出场域画面**｜本次任务：生成今夜 P3 清单（**P3**，只读 ＋ 只写清单）｜是否继续：**等待用户 21:00–23:00 异议；无异议则 23:00 自主开工**
