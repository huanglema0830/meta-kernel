【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-17 · 2.3b 分片启动 + alloc 裸机门禁 + 大模型接入骨架化 + C13/C15

## 【保密标记】（C12）

- ⛔ **机密（绝不拷）**：本机绝对路径／`PRIVATE_ASSETS` 真实值／密钥指纹／内网 IP／具体阈值权重 → **本报告已预屏蔽**（只写类别与相对路径）
- 🔐 保密：判据方法形态的实现细节 ｜ 🔒 半公开：模块结构／门禁口径／统计数／判定编号 ｜ 🔓 公开：版本号／CI／R-D 编号／C1–C15

> 【给用户的提示】本次含 **1 类机密（⛔，已预屏蔽）**。**删掉上面 ⛔ 那一行即可整篇外发**。
> ⚠️ **本轮的一项发现**：历轮报告一直写"已预屏蔽"，但**仓库内实际存在 ⛔ 内容**（见 **R25**）——
> 本轮已把 `coordination/` 内清零并建机器判据，`docs/` 部分待 **D35** 定范围。

## 全景状态卡

```
┌─ 全景状态卡 · v0.168 ───────────────────────────────────────┐
│ 阶段  阶段二 ｜ 1 ✅ ｜ 2.1 ✅ ｜ 2.2 ✅ ｜ 2.3 ✅             │
│ 2.3b  🚧 分片执行中：**片1 ✅**（l7::grade）→ **片2–5 待确认** │
│       ★ alloc 裸机门禁 ✅ 通过（可编译+可链接+运行断言）        │
│ 机制  19 项 ＋ **17 第二实例（大模型接入骨架）**               │
│ 约束  C1–C15（+**C13 扩为 9 步**／+**C15 编号表编辑纪律**）    │
│ CI    ⏳ 见 §五（本轮提交后触发；含**新指针门禁**首次真跑）      │
│ 漏洞  R1–R25（**+R25 泄漏台账**）                              │
│ 决策  D1–D34-b 收口 ｜ **新增待拍板 D35（docs 换壳范围）**      │
│ 夜间  **首夜尚未发生**（任务建于 09-17 05:20 → 首次触发 今夜 21:00）│
└──────────────────────────────────────────────────────────────┘
```

## 一、任务与状态

| 项 | 内容 |
|---|---|
| **基准** | v0.168（`git rev-list --count HEAD`） |
| **总规划定位** | 阶段二 · 2.3b（alloc 迁移）＋ 机制建设（机制 17 第二实例） |
| **任务** | ① 2.3b（**首步 alloc 裸机门禁**）② 大模型接入骨架化 ③ C13 加第 9 步 ④ BASELINE 编辑纪律加固（C15）⑤ 夜间首夜 ⑥ 报告补全 |
| **状态** | **部分完成**：②③④ **完成**；① **门禁通过＋片1 已执行**，片2–5 **按 C4 待确认**；⑤ **首夜未发生**（如实）；⑥ 完成 |

## 二、验收对照（逐条）

| # | 标准（你的第七部分） | 实测 | 结论 |
|---|---|---|---|
| 1 | 2.3b 有改动清单并确认后执行；`alloc` 裸机链接验证通过 | **门禁 ✅ 通过**（详见 §三）｜**片1 ✅ 已执行**（在已确认的 10 文件清单内）｜⚠️ **片2–5 未执行**：因为**真实规模与已确认清单不符**（3,098 → **6,853 行**），按 **C4** 先出**订正清单**待你确认 | **部分** |
| 2 | 大模型接入：骨架在公开仓库、实现在仓库外、指针可机器校验 | ✅ `coordination/llm/` **4 文件**（只写"做什么"）｜实现层 `{PRIVATE_ASSETS}/llm/` **4 文件**（⛔）｜**检查器双模**（syntax 入 CI／resolve 本机）**通过阴阳自检** | ✅ |
| 3 | 第 9 步写入 TEMPLATES 与 CONSTRAINTS | ✅ `TEMPLATES.md` §二 **第〇项**（**9 步**，逐字固定文本）＋ `CONSTRAINTS.md` **C13**（含"为什么不能省"）＋ `CHARTER.md` 机制 18 与 §四 C13 行 | ✅ |
| 4 | BASELINE 编辑纪律加固写入 | ✅ 新增 **C15**（`CONSTRAINTS.md`）＋ `TEMPLATES.md` §〇 表增 **R-e**；**判据＝编辑前后双向计数 + 编号连续性** | ✅ |
| 5 | 夜间任务未自动 push、未改治理文件 | ✅ 两个定时任务 prompt 已含**白名单 + 禁止清单 6 条**（禁改治理文件／禁对外动作／禁切模型／禁不可逆／**禁 push**／不碰 P0–P2）。**今夜尚未到点，故"未触发"无法证伪"不 push"**——如实标注 | ✅（就绪） |
| 6 | R24 加固生效且门禁未放宽 | ✅ 沿用上轮逐行核实结论（3 job 均"吸收数 **0**"＋`R24_UPLOAD_NO_SOFT_FAILURE`）；本轮**未再新增**软失败步骤（test job 软失败仍仅 4 个 upload） | ✅ |
| 7 | CI 全绿 | ✅ **run `35167675072`（@`947bf1b`）三 job 全 success**——**但过程是"红→定位→修→绿"**（见 §十一，含一个真实根因与一个新判据缺陷） | ✅ |
| 8 | 下一次返回第 0 行是阅读指令且含第 9 步 | ✅ 本回复第 0 行即 9 步固定文本 | ✅ |

## 三、★ 2.3b 首步门禁：`alloc` 能否在裸机目标上链接？——**通过**

**做了三件事**（不靠宣称，靠可复现证据）：

1. **必要条件（零改动）**：裸机 target 的 sysroot **确含 `liballoc-*.rlib`**（与 `libcore` / `libcompiler_builtins` 并列）。
2. **充分性探针（代码）**：内核 `mem/mod.rs` 新增 **`alloc_probe`** —— 逼 `Vec`（渐进增长→realloc／`with_capacity`）、`String`（拼接）、`format!` **真实走一遍 `GlobalAlloc`**，
   并由外层**净零断言**兜底 ⇒ 顺带证 **无泄漏**。判定编号 **9/10/11**。
   > **为什么必须单独设这一步**：`extern crate alloc;` 若**没有任何代码真正使用**，链接器**不会去解析 `liballoc`** —— "用了 alloc"这件事**从未被验证**，只会得到"看起来能编"的假象。
3. **产物复核**（防缓存假绿）：镜像 **3,638,272 B**／MBR **`55aa`**；**内核 ELF 1,416,536 B，较 2.3 版 +155,816 B**；
   且用 `grep -a` 在**镜像本体**内命中本轮新增字符串（`touch-user-data`／`meta-kernel/boot`）。
   > ⚠️ **镜像总大小不变**（内核区按固定布局放置）⇒ **不能只看镜像大小判断"有没有编进去"**，必须看 ELF 增量或产物内字符串。

**片1（`l7::grade`，294 行）已执行**：它是闭包中**唯一零依赖、零 std、零浮点**的模块 ⇒ 最安全首片。
- **保真度判据**：剔除插入行与空行后，**270 有效行逐行逐字相同** ✓
- **实际改动**：**+1 行 `use alloc::vec::Vec;`**；`meta-kernel-core` **一字未改** ✓
- **测试**：nostd crate **60 → 71 全绿** ｜ **裸机目标编译通过** ✓
- **内核侧**：`verify.rs` 增 ④ 段 —— **迁移后的 `l7::grade` 在裸机上算对**（正 1 ＋ **反 3**：触及用户数据⇒T3／网络配置⇒T2／不可逆⇒T3），含 `Vec` 路径。

## 四、★ 规模订正：3,098 → **6,853 行**（上轮口径漏了传递依赖）

| 项 | 上轮清单 | 实测 | 
|---|---|---|
| 待迁模块 | 10 个 | **22 个**（10 种子 3,098 ＋ **追加 12 个 3,755**） |
| std 硬门槛 | **未统计** | **151 处** std 专有浮点（`.abs()` **100**／`.sqrt()` 8／`.sin()` 4／`.cos()` 4／`.round()` 4／`.exp()` 3／`.powi()` 2／`.log2()` 1／`.atan2()` 1）＋`std::` 路径 9 处 |

**两个关键结论**：
1. **估算迁移量必须以"依赖闭包"为准**，不是"文件清单"——否则**低估 2.2 倍**。
2. **2.1 自实现的 `fmath` 正好覆盖 7 类**（缺 `round`/`log2`）⇒ 若再加一个 **`FloatOps` trait**（为 `f32` 实现这些方法），
   则 `no_std` 下 `x.abs()` **解析到 trait 方法**、host 测试下**内在方法优先** ⇒ **151 处调用点一行不改**，
   迁移动作退化为「加 1 行 `use`」。**这是"分片可行"的技术前提。**

## 五、边界说明（四类）

| 类 | 内容 |
|---|---|
| **本地跑过** | nostd 测试 **71/0**｜mem crate 25/0｜`l7::grade` 保真度判据｜nostd **裸机目标构建**｜boot 镜像构建＋**镜像内含新代码的字节校验**｜**C9 同口径预演**（1 文件／unsafe **21**／SAFETY **22**／transmute **0**／白名单 1）｜**指针检查器三模式**（syntax PASS／resolve PASS／阴阳自检：非法 3 类全检出、合法 0 误报）｜**⛔ 泄漏扫描**（全仓 299 文件／非 UTF-8 **0**／乱码 **0**）｜YAML 校验（+16/−0） |
| **CI 过** | ⏳ 本轮提交后触发（含 **boot-image 的 QEMU 绿屏** 与 **新指针门禁首次真跑**） |
| **真实跑过** | ✅ **QEMU 真实引导**（上轮由 CI 证成：1280×720、25/25 绿）。⚠️ 本机**无 QEMU** ⇒ 只由 CI 证成，**不冒充**（C5/C7）。本轮**新增的 `alloc` 探针与 ④ 段断言尚未在真实 QEMU 上跑过**（等本轮 CI） |
| **未验证** | ① **夜间首夜实际触发**（**尚未发生**，见 §八）② 片2–5 未执行 ③ `alloc` 在片3+（大量 `String`/`format!`）下的**堆占用峰值**未测 ④ `docs/` 14 条 ⛔ 的换壳（待 D35） |

## 六、真实缺陷（本轮自查与自纠）

| # | 缺陷 | 如何抓到 | 处置 |
|---|---|---|---|
| 1 | **规模口径错**（3,098 vs 6,853） | 写清单前做**依赖闭包**计算 | 出订正清单；已写进 `lib.rs` 与 BASELINE |
| 2 | **R25：仓库内实际存在 ⛔ 内容**（本机路径/用户名/内网 IP），而历轮报告写"已预屏蔽" | 为建"泄漏门禁"而写的扫描器**首次运行即命中** | `coordination/` 清零；`docs/`+`deploy/` 14 条入基线；**入 CI 防新增** |
| 3 | **判据第 1 次失效**：核对迁移保真度的管道**把空行也过滤**，diff 结果失真 | 对同一文件用**两种核对方式**比对 | 改用"剔除插入行后逐行比较" |
| 4 | **判据第 2 次失效**：Python `replace(""+"\n")` **吃掉首个换行**，生成假 diff | 输出与预期不符，追进脚本 | 换实现 |
| 5 | **判据第 3 次失效**：用 `strings` 判断产物是否含新代码，而**本机没有 `strings`** ⇒ 5 个"❌ 未见"**全是假阴性** | **加阳性对照**（已知必存在的字符串也查不到） | 改用 `grep -a` 字节匹配 |
| 6 | **判据第 4 次失效**：`sed \| cut` 显示某报告为**乱码** ⇒ 疑似非 UTF-8 文件 | 用 Python 直读＋**全仓编码审计**（299 文件／非 UTF-8 = **0**） | 确认是**显示 artifact**，非文件问题 |
| 7 | **判据第 5 次失效**：`grep -oE "^\| T-0"` 里 `^\|` 被当成"空串或"分支 ⇒ **失去行首锚定**，把行内引用也算作表行，报出 4 个**假重复** | 逐条比对实际行号 | 改 `'^\| T-0[0-9]+'`；重核 T-001..T-020 **无缺无重** |
| 8 | **门禁漏报（自检抓到）**：指针检查器初版**只认 `/` 开头**，于是「`{PRIVATE_ASSETS}\x.md`（反斜杠写法）」—— **示例**，故本行已标"示例"豁免——**绕过了检查** | **阴阳自检**（注入非法样例） | 尾类改为 `[/\\]` |
| 9 | **门禁假阳性 ×3**：① 裸 `{PRIVATE_ASSETS}`（合法指代整体）被判"缺路径" ② `{PRIVATE_ASSETS}/...`（占位符）被判"绝对路径" ③ 掩码示例 `C:\Users\...`／`/c/Users/<乱码>` 被判泄漏 | 检查器首跑输出 | 判据三处收紧/放行；**原则：判据宁可窄而准，假阳性会让人关掉门禁（R16 同族）** |
| 10 | **门禁自命中**：检查器**把自己注释里的反例字面量**当成违规 | 首跑输出指向自身第 41 行 | 脚本自身从**指针检查**中也排除 |
| 11 | **★ 我第 7 次犯"追加变替换"**：给 `ci.yml` 插入新步骤时**误删** `test -f examples/dos_concept/main.c` | **C15 双向计数**（9 ≠ 预期 10，恰好丢 1 行） | 按 R-a 恢复（净增删 **+16/−0**） |
| 12 | **同一文件并行编辑致覆盖**（本轮再次发生）：`mem/mod.rs` 的两处编辑并行发 ⇒ 第二个（加 `use`）被覆盖，构建报 `cannot find type Vec` | 构建报错→查文件发现 `use` 不在 | 串行重做；**R-d 的存在被实证** |

> **本轮最值得记的一条**：**11 与 12 都发生在我"刚刚"写下 C15 与 R-d 之后** ⇒
> **纪律写在纸上不会自动生效，必须在"每一次编辑动作"上执行**（C15 生效的唯一证据是**动作前先取数**，而不是条文存在）。

## 七、三量台账（四量）

- **存量**：core 411 测试｜nostd **71**｜mem 25｜boot 层零 unsafe（新代码）｜机制 19｜C1–C15｜报告 14 项｜CI 三 job ＋ R24｜夜间双通道｜C9 白名单 1 条｜**R1–R25**｜**D1–D34-b**
- **变量**：`CONSTRAINTS`（C13 扩 9 步／+C15）｜`TEMPLATES`（第〇项 9 步／R-e／+T-018/019/020）｜`CHARTER`（机制 18 步数／§四 +C15）｜`BASELINE`（+R25、+D35、指标 4 行、快照头）｜`ci.yml`（+1 步：指针门禁）｜`Cargo.toml`（nostd，注释）｜`meta-kernel-core-nostd/src/lib.rs`｜`meta-kernel-boot/kernel/src/{mem/mod.rs,verify.rs}`｜`coordination/ASSETS_INVENTORY.md`、`instructions/…`（+豁免标注）｜`reports/2026-09-16_R1…`（路径换壳）
- **补充增量**：**`coordination/llm/`（4 文件）**｜**`coordination/tools/check_private_pointers.py`**｜**`coordination/security/leak_baseline.txt`**｜**`meta-kernel-core-nostd/src/l7.rs` + `src/l7/grade.rs`**｜`私有资产/llm/`（4 文件，仓库外）｜2.3b 订正清单｜本报告｜指令归档
- **创新增量**：
  1. **`alloc` 门禁的形态**：不满足于"能编"，而用 **`alloc_probe`（真实分配 + 净零）＋ 产物字节校验** 证"真链接、真运行、无泄漏"
  2. **"判据必须自带阳性对照"**（本轮 `strings` 假阴性即此教训）——**阴性结果先怀疑工具链**
  3. **指针门禁的"双模分离"**：CI 能跑的（格式＋泄漏）与只有本机能跑的（存在性）**分开**，避免"CI 上必然失败的门禁"
  4. **泄漏基线"只减不增"**：让 legacy 泄漏**不阻塞**当前工作，同时**锁死新增**
  5. **`FloatOps` trait ⇒ 迁移改动面从 151 处降到 1 行 `use`**（"用语言机制把机械替换吸收掉"）
  6. **C15 的形态**：把"编辑后复核"升级为"**编辑前取数 + 后复算 + 差值预期**"，补上"少了没有"这一侧

## 八、夜间首夜：**如实纠正——首夜尚未发生**

你的指令假设"首夜已过、明早汇报"。**实测时间线**：两个定时任务创建于 **09-17 05:20**，而当前 **09-17 06:45**
⇒ **首次触发是今夜 21:00（清单）与 23:00（开工）**，**首夜结果要到 09-18 早上才有**。
⇒ 本轮**不做"首夜已完成"的任何汇报**（那会是编造）。已就绪项：白名单／禁止清单／固定汇报格式／默认不推送。

## 九、决策评估（D35，七要素）

| 要素 | 内容 |
|---|---|
| **评估** | `docs/`（6 文件）与 `deploy/win64/README.md` 共 **14 条** ⛔ 命中**已在公网**（R20/R25）。**git 历史撤不回**（D30-1 已确认）。选项：① 全量换壳 ② 保持现状（只靠基线锁新增）③ 只换 `deploy/` |
| **优势（已确定的）** | ① 换壳后**当前文件零泄漏**，且手段已就绪（脚本化、30 分钟内）② 保持现状**成本 0**，且**新文件已被门禁锁死**③ 只换 `deploy/` 能覆盖"最可能被外部读到"的那一份 |
| **劣势** | ① 会让历史报告与当时事实**不完全一致**（须逐处留痕，成本与噪声）② 14 条**继续公开**③ 折中同上，仍留 12 条 |
| **风险** | ① **假安全**：历史仍可检出 ⇒ 换壳**不等于**消除（真风险是"以为清了"）② 保持现状的风险＝**暴露面不再缩小**；③ 漏掉某文件 |
| **机会**（可能的，须可证伪） | ① 借这次把"**对外文档的泄漏判据**"沉淀成**可复用的检查**（已部分完成）② 若 `deploy/` 换壳后仍被发现泄漏 ⇒ **证伪**"换壳足以覆盖对外面"，须改为"**对外材料独立生成**"流程 |
| **建议** | **选③（只换 `deploy/`）+ 保持 `docs/` 现状**：投入最小、覆盖"对外可见面"，且 `docs/` 的历史价值高于其路径敏感性；同时**依赖已入 CI 的门禁**保证不再新增 |
| **等待决策** | 你一句话：**①/②/③**（我按选择执行；选①时我会**逐处加"已换壳"留痕**） |

## 十、未完成项与下一步

| 项 | 原因 | 下一步 |
|---|---|---|
| 2.3b 片2–5 | 真实规模与已确认清单**不符**（3,098→6,853）⇒ 按 **C4** 先出订正清单 | 你确认后按**分片**执行（每片独立 commit）；片2 **可夜间跑 ✅** |
| `alloc` 探针的**真实 QEMU 验证** | 需 CI 真跑 | 本轮提交后由 `boot-image` job 证成 |
| 夜间首夜 | 未到点 | 今夜 21:00／23:00；09-18 早上汇报 |
| `docs/` 换壳 | 待 **D35** | 你选①/②/③ |

**下一步（无需你决策即可做）**：`fmath` 补 `round`/`log2` ＋ `FloatOps` trait（纯函数、host 可测、零风险），为片2 铺路。

—— 报告结束。CI 结论与首夜结果将在下一轮如实回填。

## 十一、★ CI 从红到绿：一个真根因 + 一个新判据缺陷（本轮最有价值的一段）

| 阶段 | 内容 |
|---|---|
| **首跑（@af1c0d1）** | `test` ✅（**新指针门禁 CI 首跑通过**：`检查器通过`、exit=0）｜`host-windows` ✅｜**`boot-image` ❌** 于「断言截屏像素 == 绿」 |
| **第 1 步：读日志（不猜）** | 采样点**全为 `(255,0,0)`** ⇒ 整屏红 ⇒ `Verdict::Fail(code)`。**排除 panic**：`panic.rs` 明确**不刷屏**（只停机），若 panic 发生在 `render` 之前屏幕不会是红 |
| **第 2 步：发现判据缺陷（→ 新 R26）** | 红屏**不含判定编号** ⇒ "内存层 1–13"与"算法层 101–113"**无法区分**，每轮诊断只能重新猜。<br>**修法**：`verify.rs` 新增 **判定编号诊断条**（`COLOR_DIAG` 白；失败时把编号写成第 0 行最左 N 个白像素）。<br>**判定不受影响**：像素断言只采 25 点（y=120/240/…），**不含 y=0** ⇒ 红屏仍是红屏、门禁照样判红（与 R24 的"诊断/门禁分离"同族思路） |
| **第 3 步：一轮定位** | @e2759e6 的截屏解码 ⇒ **第 0 行连续白像素 = 10** ⇒ **判定编号 = 10** = 内存自检第 ③ 步（`String`/`format!` 内容不符） |
| **第 4 步：真根因＝我自己的期望值算错** | `"meta-kernel/boot".len()` 实际 = **16**（meta4+`-`1+kernel6+`/`1+boot4），我写成 **14**；连带的 `format!` 期望 `"14-64"` 应为 `"16-64"`。**分配器工作正常**（前两步全过）。<br>⇒ **与 2.3 那轮同族**（那次也是"我写的测试期望值算错、守卫是对的"）——**凡"断言失败"，先自查期望值** |
| **第 5 步：修法＝把"记性"换成"编译器"** | 期望串抽成 `EXPECT_S`/`EXPECT_T` 常量，长度用 `.len()` 取；再加 **编译期断言** `const _: () = assert!(EXPECT_S.len()==16, …)` ⇒ **改一处忘改另一处就编不过**（同类错误不可能再溜过 CI） |
| **终态（@947bf1b）** | ✅ **三 job 全 success**；逐行核实：截屏 **1280×720、25/25 全绿**、`[PASS] 全部 25 个采样点 == (0,255,0)`；指针门禁 `[PASS] 检查器通过`（`基线项 14 条，本次命中 11 条`——CI 上按设计**不启用用户名模式**，故少 3 条，且不误判） |

**新增风险 R26**：**判定编号不可观测** —— "失败"可机读、"**为什么失败**"不可机读。
**通用法**：**判据的判据**——"断言失败"必须能**一眼看出是哪一号**，否则每次诊断都要重新猜（本轮实测代价＝1 轮 CI）。

**边界（如实）**：本轮**先红了两次**（@af1c0d1 首次、@e2759e6 诊断轮），第 3 次才绿；
诊断条这一改动**自身也经过一次 CI 验证**（@e2759e6 读出编号 10 即其证据）。

## 十二、边界补充：本轮 CI 的"真实跑过"更新

| 类 | 更新后 |
|---|---|
| **CI 过** | ✅ run `35167675072`（@`947bf1b`）三 job 全绿（含 **boot-image 的 QEMU 真实引导 + 像素断言**、**指针门禁首跑**） |
| **真实跑过** | ✅ **QEMU 真实引导** —— 本轮**新增**的三件事**已由 CI 真验**：`alloc` 探针（Vec/String/format!）＋ **净零无泄漏** ＋ ④ 段 `l7::grade` 裸机断言 ⇒ 与 2.1 算法自检**共用同一条绿屏断言** |
| **未验证（仍）** | 夜间首夜（**尚未发生**）｜2.3b 片2–5｜`alloc` 在片3+ 的堆占用峰值｜`docs/` 换壳（待 D35） |
