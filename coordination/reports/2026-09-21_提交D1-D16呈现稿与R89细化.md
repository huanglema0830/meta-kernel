【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 提交 D1–D16 呈现稿 ＋ R89 细化为可执行检查点（2026-09-21 · v0.298）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。
（本稿新增行扫描：本机用户名 **0**、绝对路径 **0**、端口数字 **0**、密钥字样 **0**。）

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 295** ⇒ 收尾 **298**（本轮 **3 个本地提交**：v0.296 `98b80f6` 呈现稿／v0.297 `1ba1f81` R89 细化／v0.298＝台账 §四十 ＋ 本报告）⇒ label **v0.298**（`label = count`，C21） |
| **推送** | **已推 `4d3d8d0..98b80f6`（v0.296，exit 0）**；**本轮后 2 个 commit 未推**（机制 23 须当轮明示） |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 = 执行层（**判定与记账（T1 闭环）已实现、执行侧待确认**） |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–38（本轮 +1：T-038）**｜D **1–44**｜R **1–52**｜TERM **1–13** |
| **三表** | **连续、无缺号、无重号**（C15 实测；**T 37 → 38**） |
| **新口径** | **R89 已细化为可执行检查点**：`TEMPLATES §十五`（**CI 结论落笔门控**）＋ **T-038** |
| **工作树** | 见 ⑬（本报告提交前为「已改待提交」） |

**③ 任务**：基准 **count = 295**｜总规划定位：**提交 D1–D16 呈现稿 ＋ R89 细化**｜类型：**放行 ＋ 机制加固**。

**④ 状态：完成**（含 **4 处诚实边界**：① **CI 三态按 C20 只记当日日报**；② **本轮零 `docs/`／`README.md`／代码改动** ⇒ 判据 F 无需更新、汇编无需重生成；③ **改治理文件仅 1 份**（`TEMPLATES.md`），未动 C 编号族；④ **R89 检查点的"防复发有效性"属未来事件** ⇒ 只能由后续轮次检验）。

---

## ★ 主体

### 〇 · 提交 ＋ Push（用户 §〇）· **先提交、再 push、后动文件**

#### 0.1 提交 D1–D16 呈现稿
- commit **`98b80f6`**（v0.296；**1 file changed, +303**）。
- **严格按 §〇.0.1「只提交这一份」** ⇒ `git add` **只 add 该文件**；`git status` 复核确认 `2026-09-20_今夜P3清单.md` **仍为未跟踪**（未误纳）。

#### 0.2 Push ＋ **等 CI 收束（关键）**
- `git push origin main` ⇒ **`4d3d8d0..98b80f6`（exit 0）**；`ahead 1 → 0`。
- **★ 按用户 §〇.0.2 明示「push 后先等 CI `completed`，再动文件」** —— 本轮**不采用"push 完立刻出稿"的做法**，改为**阻塞式脚本** `.workbuddy/tmp/push_and_wait_n22.sh`：push → 定位 run → **循环轮询直到 `status == "completed"`** → 输出逐 job 三态 → 尾行打印 **`READY_TO_EDIT`** 作为**门控证据**；**此后才开始任何文件编辑**。
- **CI 三 job 全绿**（`test`｜元内核引导镜像（QEMU 真实引导）｜宿主 field-render）—— **一次通过、无判红、无重跑**。
- **⚠️ 按 C20，run 号与逐 job 结论只记当日日报**（`.workbuddy/memory/2026-09-21.md`），**本报告不内联**。
- ⇒ **可执行检查点的"首次执行"成功**（**本轮未再触犯** R89）。

### 一 · R89 细化为**可执行检查点**（用户 §一 · 改治理文件 · 获授权）

#### 1.1 逐字前后对照

| | 改动前 | 改动后 |
|---|---|---|
| **规则文本** | **不存在** —— R89 仅在 `BASELINE §26.9`（L1739）**登记为教训**；`§27.3` 曾裁定「**只登记、不固化**」 | `TEMPLATES.md §15.1` **新增，逐字**：<br>「**push 动作完成后，任何含 CI 结论的文本，必须等 `gh run list` 显示 `completed` 后再落笔。**」 |
| **文件结构** | `TEMPLATES.md` **无 §十五**（§十四 能力矩阵为末节，止于 §14.6） | **新增 §十五**（**六小节**）：15.1 规则逐字｜15.2 **为什么需要**（两次触犯实证表）｜15.3 与 R89／C20／机制 26 的关系｜15.4 **执行三步**＋**禁止／允许条款**｜15.5 与 **§11.3 复扫类触发式**的**类比**｜15.6 **触发词** |
| **变更记录** | `§7.2` 末条＝ **T-037** | `§7.2` **补记 `T-038`**（走 §7.2 程序） |

#### 1.2 新增的关键**可执行**条款（逐字）
- **执行三步**：① `gh run list …` **按 sha 前缀定位 run** → ② `gh run view <id> --json status,conclusion` **循环直到 `completed`** → ③ `gh run view <id> --json jobs` **取 job 级三态后方可落笔**。
- **禁止**：在 ② 仍返回 `in_progress` 时写下"全绿／success／通过"等**任何**结论性字样（含"预计""应当"式表述）。
- **允许**：写"**CI 已触发，结论待 `completed` 后补**"（**如实标注未完成**，符合 C5 诚实标注）。

#### 1.3 落点选择（用户给**二选一**）
**裁定落点＝`TEMPLATES.md`**（非 `CONSTRAINTS.md`）。**理由**：① 用户要求**补记 T 编号** ⇒ 需落 T 族载体；② **单文件闭环** ⇒ **不动 C 编号族**（若新增 C22 须连带改 `CHARTER §四` 速查，**超出本轮明示授权**）；③ 与 §11.3（T-037 所在）**同族** ⇒ 两条可**并列对照**（"触发式" vs "门控式"）。

#### 1.4 与 `§27.3`「R89 只登记不固化」的关系（**口径变更 · 已留痕**）
- `§27.3`（v0.251）曾裁定 R89 **只登记、不固化**。**本轮用户裁定细化并固化** ⇒ **口径由"只登记"变更为"细化＋固化"**，**原裁定留痕保留**（C19），台账 §40.3 为**后记指针**。
- **变更依据**：**v0.294／v0.295 同一违规两次触犯** ⇒ 证明"抽象铁律"**不足以防复发**。

#### 1.5 C15 双向计数
- 机制 27｜C 21｜**T 37 → 38（+1）**｜D 行 45／R 首格 46／R 唯一 47｜§四 21｜TERM 13 —— **除 T 外全 ±0**；机制/C/T **连续、无缺号、无重号**。
- **机制 25 实跑**：✅ **通过**；`--selftest` **十二侧全绿**（`TEMPLATES.md` 属其扫描范围）。
- **判据 F**：**PASS**（`TEMPLATES.md` **不在基线 36 份内** ⇒ 无需 `--update-baseline`、**无需汇编重生成**）。

---

## ⑤ 验收对照（用户 §四）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | D1–D16 呈现稿提交并 push，CI 三 job 全绿 | ✅ `98b80f6`；推 `4d3d8d0..98b80f6`（exit 0）；三 job **全 success**（**阻塞等 `completed` 后取值**；C20 只记日报） |
| 2 | R89 细化写入治理文件，逐字对照 | ✅ §40.1／本稿一.1（**逐字前后对照**） |
| 3 | C15 复算通过，编号表连续无缺号无重号 | ✅ **T 37→38**；机制/C/T 连续 |
| 4 | 下一次返回第 0 行含 9 步阅读指令 | ✅（本报告第 0 行即 9 步指令） |

## ⑥ 未完成项（如实列出）

1. **未在 `CONSTRAINTS.md` 补 C 编号**（用户给二选一 ⇒ 本轮落 `TEMPLATES.md`）⇒ 见 ⑪-2。
2. **`coordination/reports/2026-09-20_今夜P3清单.md`** 仍为未跟踪（历史未纳管件；按 §〇.0.1 **不含**）。
3. **D1–D16 待讨论**（用户明示本轮不派）。
4. **R89 检查点的防复发有效性未验证**（属未来事件）。

## ⑦ 真实缺陷与过程观察

- **真实缺陷 0**。
- **★ 过程观察 1（正向 · 可执行检查点首次执行成功）**：本轮**不再"先写后验"** —— 改用**阻塞式门控脚本**（`push_and_wait_n22.sh`），**在 `completed` 之前未做任何编辑**，尾行 `READY_TO_EDIT` 为门控证据 ⇒ **承接两轮触犯后的规则落地见效**。
- **过程观察 2（正向 · 提交边界）**：`git add` **只 add 指定一份**，`git status` 复核 P3 清单仍未跟踪 ⇒ 未误纳历史件。
- **过程观察 3（结构性观察）**：本轮把 R89 从"**教训**"转为"**门控条款**" —— 与 T-037 把"固定复扫"改为"触发式"**同构**：**都是把"应当记得"替换为"由可观测条件决定"**。⇒ 建议后续把同类"抽象铁律"按此模式**逐条绑定动作**（见 ⑫-3）。

## ⑧ 三量台账（四量齐全）

| 量 | 本轮情况 |
|---|---|
| **存量** | 底图 **36 份**、`docs/` **35 份**、机制 27／C 21／**T 37**（**本轮前**）／D 44／R 52／TERM 13；呈现稿**由未跟踪转为入库**（`98b80f6`） |
| **变量1** | 用户两项指令：① 提交呈现稿并 push；② **把 R89 细化为可执行检查点**并写入治理文件 |
| **补充增量（答案1）** | 本轮产出：**`TEMPLATES §十五`（六小节 ＋ 门控三步 ＋ 禁止/允许条款）＋ `T-038`** ＋ 台账 §四十 ＋ 本报告 |
| **创新增量** | ① **规则形态升级**：R89 从"抽象铁律/教训"升级为**可执行检查点**（绑定到"**落笔前的那一秒**"）；② **门控首次执行成功**（阻塞式脚本 `READY_TO_EDIT` 证据）；③ **建立"触发式／门控式"类比**（T-037 ↔ T-038），为后续"抽象铁律→可观测条件"的**批量改造**提供范式。**（一经产出即降为新的存量）** |

## ⑨ 边界说明（四类齐全）

| 类 | 本轮内容 |
|---|---|
| **本地跑过** | C15 双向计数（**T 37→38**）｜机制 25 `--selftest` **十二侧全绿** ＋ 实跑 ✅ 通过｜判据 F **PASS**｜本稿第 0 行自检（P1 位置 ＋ P2 逐字 15 行 **PASS**、泄漏 **0**）｜**阻塞式等 CI `completed`**（门控证据 `READY_TO_EDIT`） |
| **CI 过** | **`test`／元内核引导镜像（QEMU 真实引导）／宿主 field-render 三 job 全绿**（**结论按 C20 只记当日日报，不入库**）★ 取材时点＝**阻塞等到 `completed` 之后** |
| **真实跑过** | 元内核引导镜像 job 内**真实 QEMU 引导**（CI 侧）；本轮**本机未跑 QEMU**（本机能力边界，见 §十四 能力矩阵 M 层） |
| **未验证** | ① 本机 **不可 full build／无 qemu**；② **R89 门控条款的"防复发有效性"未验证**（需后续轮次实测）；③ **`§27.3`→本轮的口径变更**是否有下游文档仍在引用旧口径 —— **本轮未做跨文档复扫**（复扫类已改**触发式**，见 §11.3） |

## ⑩ 决策评估（对 ⑪ 的 3 项按七要素给建议）

| 待裁 | 评估 | 优势 | 劣势/风险 | 建议 | 等待 |
|---|---|---|---|---|---|
| **① push 本轮 2 commit** | 远端落后 2（v0.297–v0.298） | 远端与本地对齐；门控条款随仓库固化 | 无实质风险；仅多一次 CI | **放行** | 你一句话 |
| **② 是否再补 C 编号** | 现仅 T 族（T-038）；C 族无对应条 | C 族与判据族并列、检索直观 | **须连带改 `CHARTER §四`**（一次动两族编号，回归面变大） | **暂不补**（T 族已足） | 你一句话 |
| **③ 其余抽象铁律是否按此模式改造** | 现有铁律多为"应当如此"型（如 C19／R75／R82 的执行面） | 可把"应当记得"换成"由条件决定" | 逐条改造需多轮授权；滥用会致条款膨胀 | **逐条评估、分批授权** | 你一句话 |

## ⑪ 需要你一句话的裁定（3 项）

| # | 项 | 推荐 |
|---|---|---|
| 1 | **push 本轮 2 个 commit（v0.297–v0.298）** | **放行**（机制 23 须当轮明示） |
| 2 | **是否再补 C 编号**（`CONSTRAINTS.md` C22 ＋ `CHARTER §四` 同步） | **暂不补** |
| 3 | **其余"抽象铁律"是否按 T-038 模式逐条改造** | **逐条评估、分批授权** |

## ⑫ 下一步建议

1. **D1–D16 讨论**（用户已明示待讨论）：建议 **D15 先裁**（方法层）→ 口径项 D1／D6／D8／D11 逐条 → 工程项 11 条批量（D13 单列）。
2. **R89 门控条款的"首次实战验证"** 已完成（本轮）；后续每轮**沿用阻塞式脚本**即可自动合规。
3. **可考虑**（待授权）：把 **R75（行号漂移）／R82（状态陈述静默过期）** 等同类铁律也按 **T-038 模式**绑定到具体动作（如"汇编 diff 超阈值时**自动**提示行号漂移检查"）。

## ⑬ 启动回执

> 已读 `CHARTER.md`。
> 当前状态：**count = 295** ｜ 阶段二 · **2.4 边界层已验**
> 本次任务：**提交 D1–D16 呈现稿 ＋ R89 细化**
> 是否继续：**执行完毕**

**工作树**（本报告提交前）：`M coordination/BASELINE.md`（§四十，**待提交**）＋ `?? 本报告`（**待提交**）；`?? coordination/reports/2026-09-20_今夜P3清单.md`（**历史未纳管**）。
**提交状态**：`count = 298`｜`origin/main = 98b80f6`｜**`ahead 2`**（v0.297 `1ba1f81` ＋ v0.298 收尾）。

## ⑭ 本轮读了底图哪几份、哪几行

| 底图/文件 | 读的区间 | 用途 |
|---|---|---|
| `coordination/CONSTRAINTS.md` | **L7**（C 族"活相"清单一览）｜**§C20 L415–443**（CI 结论不入库：底线／为什么／三条要求） | **落点二选一的评估**（终未采用）；确认 C20 与本条**正交** |
| `coordination/TEMPLATES.md` | **§7.2 L296–353**（变更记录表，T-035／T-036／T-037 格式）｜**§11.3 L577**（复扫类触发式行）｜**§十四 L783–831**（能力矩阵，**文件末节**） | **T-038 行格式对齐**＋**新增 §十五 的插入位置**＋**§15.5 类比的引据** |
| `coordination/BASELINE.md` | **§26.9 L1726–1742**（**R89 原登记**）｜**§27.3 L1865–1867**（"R89 只登记不固化"原裁定）｜**尾部 L2852** | R89 原口径 ＋ **本轮口径变更的对照基线** ＋ §四十 插入点 |
| `.workbuddy/memory/2026-09-21.md` | 尾部（本轮追加段） | CI 结论（C20：只在此处） |
| `gh run list`／`gh run view`（运行） | — | **阻塞等 `completed` ＋ 取 job 三态（T-038 §15.4 三步）** |

---

## 附 · 晨报要点（供 08:00 交付复用）

- **夜间触发结果**：本轮为**用户指令轮**（放行 ＋ 机制加固）；**复扫类均未触发 ⇒ 未跑**（合规）。
- **完成项**：提交并 push D1–D16 呈现稿（`98b80f6`；CI 三 job 全绿）｜**R89 细化为可执行检查点**（`TEMPLATES §十五` ＋ `T-038`；C15：T 37→38）｜台账 §四十。
- **失败项**：**无**。
- **未推送 commit（2）**：`1ba1f81` v0.297 R89 细化｜**v0.298** 台账 §四十 ＋ 本报告。
- **需你目验/放行**：① push 本轮 2 个 commit；② 是否再补 C 编号；③ 其余铁律是否按 T-038 模式改造。
