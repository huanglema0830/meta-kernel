【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · **MEMORY 三层指针机制（`T-046`／机制 30）** ＋ 4 项待裁落地

> 归口：**本轮指令 §〇–§五**｜日期：2026-09-22｜性质：**机制建设 ＋ 裁定落地**
> ★ **版本口径（`T-045`）**：**报告内版本号 ＝ 本报告「首次提交」时的 `count`** ⇒ 见 ⑬ 启动回执。

---

## ① 保密标记（C12）

| 项 | 内容 |
|---|---|
| **保密级别** | 🔓 **公开**（本报告**只含仓库内公开信息**） |
| **四级清单** | 🔓 公开／🔒 半公开（顾问可读）／🔐 保密（仅本机）／⛔ 机密（绝不外发） |
| **屏蔽提示** | **无屏蔽项** —— 不含 `{PRIVATE_ASSETS}` 真实值、**本机绝对路径**、用户名、密钥；记忆文件一律写作**工作区相对路径** `.workbuddy/memory/…`。 |

---

## ② 全景状态卡（自动生成）

| 项 | 值 |
|---|---|
| **版本** | **起点 `count 325`**（指令基准）⇒ **本轮收口 `count 326`**（`81edf8e`）；**本报告 ＝ 327** |
| **阶段定位** | 阶段二（可引导 ISO）· **2.4 边界层已验** |
| **待决策项** | **2 项**（见 ⑪）★ 上轮 4 项**已全部落地** |
| **CI 状态** | **全绿**（三 job 全 success；★ **run 号与逐 job 三态按 C20 只记日报、不入库**） |
| **仓库** | `ahead 1`（本轮 1 个新 commit **未 push**）；工作树**待提交本报告** |

---

## ③ 任务（含基准与总规划定位）

- **基准**：`count = 325`（**实测一致**）。
- **总规划定位**：**MEMORY 三层指针机制** ＋ **4 项待裁落地**。
- **★ 本轮读了底图哪几份、哪几行**（如实）：

| 文件 | 读的位置 |
|---|---|
| `coordination/CHARTER.md` | **§三 机制表（机制 29 行／「机制之间的关系」块）**｜**§九 按需深入** |
| `coordination/TEMPLATES.md` | **§7.2 表尾（`T-045` 行）**｜**§11.3「夜间任务清单（P3）」全表**｜§11.4–§11.8 |
| `coordination/BASELINE.md` | **§47.3 A2 表**｜**§五十三 末节**｜**文件尾**（追加点） |
| `coordination/CONSTRAINTS.md` | **§C1–C21 节标题**（含 **C10／C16／C19／C20**） |
| `coordination/tools/check_source_of_truth.py` | **判据 B 实现**（核它读**哪个** `MEMORY.md`） |
| **工作区记忆** `.workbuddy/memory/MEMORY.md` | **全文 35 行**（拆分源）｜`MEMORY_INDEX.md`／`ironlaws.md`／`env.md`／`state.md`／`rounds.md`（本轮新建，回读核对） |
| **底图（根 `README.md` ＋ `docs/` 35 份）** | ★ **本轮未读**（**零改动底图**；**但判据 B 要求 `MEMORY.md` 含底图硬指针** ⇒ 该指针**已补回并复跑 PASS**） |

---

## ④ 状态

**状态**：**完成**（§〇 push ＋ **§一 三层建立** ＋ **§二–§五 4 项裁定**全部落地）。

---

## ⑤ 验收对照（指令「验收标准」逐条）

| # | 验收标准 | 实测 |
|---|---|---|
| 1 | **push 完成，CI 三 job 全绿（T-038 门控）** | ✅ `32b4042..6b8afd5`（**exit 0**）；**三 job 全 success**；门控证据 **`READY_TO_EDIT`**（`##[error]` **0 行**） |
| 2 | **MEMORY 三层建立：调用层 < 1 KB、索引层 < 4 KB、内容层分文件** | ✅ **调用层 977 B**（＜1024）／**索引层 4016 B**（＜4096，**初版 4398 超限 ⇒ 已压**）／**内容层 4 文件**（`ironlaws`／`env`／`state`／`rounds`） |
| 3 | **现有内容 verbatim 迁移到内容层** | ✅ **按标题整段搬运**；**正文核心 8435 B ≤ 原文 8191 ＋ 400**（**无丢、无重复**）；**写后回读断言 6/6 一致**；**迁移前备份**（仓库外，8191 B） |
| 4 | **`T-044` 升级为三层口径** | ✅ `TEMPLATES §7.2` 新增 **`T-046`**；★ **`T-044` 原句保留不回改**（C19）—— **8 KB 约束按层重分配**（1 KB／4 KB／不限） |
| 5 | **登记为机制** | ✅ `CHARTER.md` **机制 30**（顺延自 29）＋「机制之间的关系」块补 30 ＋ **§九 挂"会话开始读调用层"指针** |
| 6 | **2 条过期任务删除并复核** | ✅ 两条**均删除**；**处置后 `list` 复核确认不在**；★ **平台任务 39 → 33 → 31**（**累计删 8 条**） |
| 7 | **过期任务扫描常态化写入 `TEMPLATES`** | ✅ **§11.3 新增一行**「★ 平台任务检查（2026-09-22 起）」＋ **`T-047`**；★ **并入清单类任务**（不新建机制） |
| 8 | **`T-041` 剔除层暂不扩** | ✅ **`§54.4` 追认**；`scan_exclude.json` **零改动** |
| 9 | **指令索引今后每轮内联写入** | ✅ **`§54.5` 规则条文**（写入 `BASELINE.md`）；★ **本轮即首例**（`§五十四` 开头已内联本轮报告名） |
| 10 | **C15 复算通过，编号表连续无缺号无重号** | ✅ 机制 **1..30**／`T` **1..47**／节号 **0..54** **连续无缺重**；Δ ＝ 机制 **+1**／`T` **+2**／节标题 **+1** |
| 11 | **CI 全绿** | ✅ 见 1 |
| 12 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行 |

---

## ⑥ 未完成项（清单 ＋ 原因）

| # | 未完成项 | 原因 |
|---|---|---|
| 1 | ★★ **三层落点是否改到 `~/.workbuddy/`（用户级）** | 指令原文写用户级；**实测该位置＝跨项目用户记忆（13 184 B）＋ 平台托管缓存目录** ⇒ **覆盖＝数据丢失** ⇒ **本轮落在工作区**（保护性口径），**待你复核**（见 ⑪①） |
| 2 | **三层的机器判据** | 指令未要求；**可机器化**（调用层 <1024／索引层 <4096）—— **本轮不立** |
| 3 | **内容层文件今后是否再分** | 现 4 文件；**索引层满 4 KB ⇒ 才拆**（机制 30 纪律③） |
| 4 | **`~/.workbuddy/MEMORY_详情.md`（2696 B）** | ★ **实测存在**（用户既有文件），**本轮未动、未并入**（**不在授权内**） |
| 5 | **指令索引"未内联"轮次** | 依裁定 4 **不追改历史**（**改为今后每轮内联**）⇒ 历史"未内联"**保留现状** |

---

## ⑦ 真实缺陷（**B2 ＋ C1**：本轮新增告警 ＋ 来源溯源）

**★ 六族告警行（`C1` 载体 · 固定一行）**：**六族告警：本轮 3 条**（`C22`／`T-00`／`TERM-999`，**均元引用**）**｜对比上轮 ±0**｜**真缺口 0 条**。

| 级 | 缺陷 | 来源／证据 | 性质 |
|---|---|---|---|
| **P0** | ★★★ **三层迁移漏掉 MEMORY 头部「底图＝根 `README.md`＋`docs/`」一行** | `check_source_of_truth.py` **当场判红**：`FAIL [MEMORY.md] 缺底图硬指针（须同时出现 README.md 与 docs/）` | **真实回归（由我引入）** ⇒ ★ **判据在"记忆重构"上同样有效**（机制 22 健康度实证）；**已修：调用层补短指针 ＋ `ironlaws.md` 留原文整句** |
| **P1** | **迁移脚本 v1 的转义 bug**：`stop=r"\n\n"` 是**字面量**而非换行 ⇒ `find` 恒 −1 ⇒ **每块"取到文件尾"** | `state.md` 一度写成 **33 153 B（≈10 倍）** | **自捕获**：由**回读断言**＋**"正文核心 ≤ 原文＋400"上限**抓出 ⇒ v2 修正后 **PASS** |
| **P1** | **迁移中一处真实重复**：`新入口` 行同时落入「编号」段与自身段 | 自检比对块长时发现（**+313 B**） | **自捕获并去重**（**未静默保留**） |
| **P2** | **调用层补回指针后一度 1127 B ＞ 1024** | 三层纪律②（调用层满 1 KB ⇒ 压） | **已精简至 977 B** |
| **P2** | **索引层一度 4398 B ＞ 4096** | 三层纪律③ | **已压至 4016 B** |
| **P2** | **上一轮"清单不全"的延续**：漏网 2 条过期任务 | v0.317 轮列 6 条实漏 2 条 | **本轮删除 ＋ `T-047` 常态化**（治根因） |

---

## ⑧ 三量台账（**四量**）

| 量 | 内容 |
|---|---|
| **存量** | 机制 **30**｜`T` **47**｜`TERM` **14**｜`C` **21**｜`R` **91**｜`D` **55**｜判据脚本 **10**｜`ci.yml` `test` **32 步**｜台账节标题 **54**｜平台任务 **31**（本机）｜**记忆三层**：调用 **977 B**／索引 **4016 B**／内容 **10212 B**（4 文件） |
| **变量** | **本轮改动 3 份跟踪文件**：`CHARTER.md`／`TEMPLATES.md`／`BASELINE.md`；**新增 1 份**：本报告；**工作区记忆**：**重写调用层** ＋ **新增 5 份**（索引层 1 ＋ 内容层 4）；**本机**：删 2 条平台任务；**编号表变动**：机制 **+1**／`T` **+2** |
| **补充增量** | ① **记忆按需读取**（会话开始只读 **977 B**，而非 8 KB）② **`T-044` 升级为分层上限**（约束未取消）③ **过期任务扫描常态化**（`T-047`）④ **裁定 3／4 的追认与规则**（剔除层不扩／每轮内联）⑤ **平台任务清零**（无过期遗留） |
| **创新增量** | ① ★ **"保护性口径"处理路径冲突**（指令要用户级、实测会被平台覆盖 ⇒ **不改、先报告**）② ★★ **判据 B 在"重构记忆"上当场判红** —— **证明机制 22 的覆盖不止于代码**③ ★ **"回读断言 ＋ 上限哨兵"双保险**抓出迁移 bug 与重复（**若只看体积会静默通过**）④ ★ **"分级留痕"防再漏**（调用层短指针 ＋ 内容层原句，**两级都留**）⑤ ★ **三个上限（1 KB／4 KB／不限）在建立当日即各触发一次压缩** ⇒ **上限不是摆设** |

---

## ⑨ 边界说明（**四类**）

| 类 | 内容 |
|---|---|
| **本地跑过** | ① **三层拆分脚本**（含**写后回读断言 6/6**）② **体积复核**（977／4016／10212）③ **全量判据 10/10 `rc=0`**（`check_migration_fidelity` 需传参 ⇒ rc=64）④ `check_id_set_diff` 实跑告警 **3** ⑤ `check_report_structure` 实跑告警 **0**（140 份／报告体 75）⑥ **判据 F 复跑 PASS**（**先红后绿，留痕**）⑦ **C15 复算** |
| **CI 过** | ✅ **全绿**：`test` ＋ `宿主 field-render` ＋ `元内核引导镜像（QEMU）` **三 job 全 success**（★ **run 号按 C20 只记日报**）。逐行核实：「报告结构门禁」**真跑且 PASS**；`Manifest UI wasm` **通过**；`UI 自动目验` **通过**；**重试均未触发**（首试即过） |
| **真实跑过** | ① **GitHub Actions 真实执行** ② **本机真实文件系统**（记忆三层文件、回读断言、体积量取）③ **平台任务真实删除**（**删后 `list` 复核**）④ **`git push` 真实成功** |
| **未验证** | ① **三层**：**未入 CI**（**不在仓库内**；其**规则**在仓内、**内容**在仓外）② **调用层的"会话开始必读"无判据**（**机制 30 无机器判据**）③ **`~/.workbuddy/` 方案未执行**（**待你复核**）④ **`MEMORY_详情.md` 未并入** ⑤ **重试路径仍未触发**（`R90`／`B5`）⑥ **`TERM-999` 仍在告警**（裁定 3：**不扩剔除层**） |

---

## ⑩ 决策评估（七要素 · 本轮**新决策 1 项**）

### 决策：三层的**落点** —— **甲 用户级 `~/.workbuddy/`（照字面）** ／ **乙 工作区 `.workbuddy/memory/`（本轮执行）**

| 要素 | 内容 |
|---|---|
| **评估** | 指令写 `~/.workbuddy/`；**实测**：`~/.workbuddy/MEMORY.md` ＝ **13 184 B「用户长期记忆（跨项目）」**；`~/.workbuddy/memory/` ＝ **平台托管缓存**（`{UUID}_memory.md`）。★ **`T-044` 自身原文**写的是「**工作区记忆** `.workbuddy/memory/MEMORY.md`」 ⇒ **本条约束的对象本就是工作区** |
| **优势** | **乙**：**零数据风险**、与 `T-044` 原文一致、与现有云内核记忆同域｜**甲**：与指令字面一致 |
| **劣势** | **甲**：**会覆盖跨项目用户记忆**（决策呈现规范／项目安全总则／发布铁律…全在那一份里）＋ **内容层落进平台托管目录，会被平台覆盖**｜**乙**：与指令写的路径**不一致**（**已如实报**） |
| **风险** | **甲**：**不可逆的数据丢失**（个人/跨项目记忆被 6 条云内核铁律顶掉）⇒ **必须用户明示**｜**乙**：**几乎无**（可随时再迁） |
| **机会** | **乙** 顺带发现**两个既有物**：`~/.workbuddy/MEMORY_详情.md`（2696 B，用户既有"详情"分离尝试）与**平台托管目录**的边界 ⇒ **"用户级 vs 工作区"的分层口径值得单独成文** |
| **建议** | **维持乙**（本轮已执行）；★ 若你确要走用户级 ⇒ **须先答"跨项目记忆搬去哪"**（不能原地覆盖） |
| **等待决策** | ✅ **等你**（见 ⑪①） |

---

## ⑪ 需要用户决策（**2 项**）

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★★ **三层落点**：维持**工作区**（本轮）还是改**用户级 `~/.workbuddy/`** | **维持工作区**（**零风险**）；★ 若要用户级，须**先定跨项目记忆的落点**（可考虑：用户级只放**跨项目铁律**、项目三层仍留工作区） |
| ② | **`MEMORY_详情.md`（2696 B，用户级既有文件）如何处置** | **三类**：**(甲) 并入用户级调用层**（若走用户级方案）｜**(乙) 保留不动**（**默认**）｜**(丙) 归档**。★ **我推荐 (乙)**（**它不在授权内，且可能是别处引用的指针**） |

---

## ⑫ 下一步建议

1. **先裁 ⑪①**（三层落点）—— 它决定**后续所有记忆写入的目标**；不裁则每轮都要重复判断一次。
2. **建议给机制 30 加机器判据**（调用层 < 1024／索引层 < 4096）—— **本轮已三次触线**（1127／4398），**人工容易忘**。
3. **触发式**（无新触发则不跑）：`docs` 第八批｜`advisor_brief` 复扫｜汇编（**随 `docs/` 更新**）。
4. ★ **建议把"回读断言 ＋ 上限哨兵"写进 `TEMPLATES`** —— 本轮**纯靠它**抓出迁移 bug（**体积达标但内容错 10 倍**是典型"绿灯陷阱"）。

---

## ⑬ 启动回执

已读 `CHARTER.md`（§三 机制表 ＋ 关系块／§九）。
**当前状态**：`count = 325`（起点 · 实测）｜阶段二 · 2.4 边界层已验。
**本次任务**：MEMORY 三层 ＋ 4 项待裁。
**是否继续**：执行。
**★ 本报告版本（依 `T-045` ＝「首次提交时 `count`」）＝ `327`**（★ **提交后以 `git rev-list --count HEAD` 实测复核**；若不符则按 `T-045` 在订正处标注订正时 `count`，**不改本行**）。

---

## 附加表 · INTEL 状态表（依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| **INTEL-001** 工信部 AI 中小企业创业支持计划 | **观察中** | 组合：企业注册完成 ＋ 阶段三启动 | **✗ 未触发**（当前阶段二 · 2.4） | 无 |
| **INTEL-002** GPT-6 安全风险 | **观察中** | 事件：L7–L10 落地后 | **✗ 未触发**（L7 设计完成／L8–L10 冻结） | 无 |

**J1–J4 自查**：J1 ✅ 本表在（**且置于十四项之后**）｜J2 ✅ 未触发即未改状态（**不算违规**）｜J3 ✅ 无"待准备"项｜J4 ✅ 无"准备中"项。
★ **机制 29 本轮无状态变更**；**下轮开工第 1 步仍＝读 `coordination/intel/INDEX.md`**（★ 现另有 **机制 30 第 1 步：读 `.workbuddy/memory/MEMORY.md`（调用层）** ⇒ **两步并存**，**次序待裁**）。
