【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# v0.247 · R84 收口 ＋ 底图行 89 修订 · 报告

> ⚠️ **本轮即 R84 的收口轮 —— 版本号口径当场切换**：本轮起，版本号**一律 = `git rev-list --count HEAD`（实时取）**，
> **不再推算 `count − 5`**。故本报告标签 = **提交后实时 count**（预期 **v0.247**；复核命令 `git rev-list --count HEAD`）。
> 底图 `README.md` **行 89** 已于本轮**修订**（删去「实质性」歧义措辞），判据 F 基线**已重生成**。

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**（无密钥／账号／本机绝对路径／内网 IP／用户名）。**可整份转发**。
（说明：本报告引用 `coordination/` 内的治理文件与 `CHECK` 脚本，均为仓库内可公开内容；改动的 6 个文件经泄漏扫描 —— **用户名 0 处、本轮新增绝对路径 0 处**。）

**② 全景状态卡**（★ 数字为**生成时快照**；提交／推送动作本身会推高版本号 ⇒ **现值一律以复核命令为准**，R82）
```
┌─ 云内核项目 · 全景状态卡 ────────────────────────────────────────┐
│ 版本   以 git rev-list --count HEAD 为准（不写死，C2）           │
│        本轮起点 count = 246                                      │
│        ★ 本机制 27 生效首轮：**label = count（实时取）**         │
│        ⇒ 本报告标签 = 提交后实时 count（预期 v0.247）            │
│        （不再推算 −5；R84 已结案）                               │
│ 阶段   阶段二 · 2.4 纯算层 ✅ ｜ 2.4 边界层 ✅（QEMU 已证）      │
│        2.4 product 路径已接线（v0.237，待 CI 真实引导证成）      │
│ 本轮   裁定落地：R84 结案 ＋ 补明文规则 ＋ 底图行 89 修订        │
│        改动 6 文件 ＋ 1 报告（本报告）                           │
│ 未推送 快照 0（提交后按 gh 实取）                                │
│ CI     按 **C20**：结论**只进日报／对话返回**，**不入仓库**      │
│        ⇒ 复核命令 `gh run list --limit 3`                        │
│ 决策   新增待裁 3 项：① 速查表 C16–C20 缺口是否补全            │
│        ② 汇编覆盖范围口径（R86）③ 仓库外汇编快照是否重生成      │
│ 暂缓   场源扩屏（选 B，先不定）｜D-7 身份档案（按指令暂缓）      │
└──────────────────────────────────────────────────────────────────┘
```
复核命令（R71：数值不抄进文档）：`git rev-list --count HEAD`｜`git rev-list --count origin/main..HEAD`｜`gh run list --limit 3`

**③ 任务**：基准 **count = 246**｜总规划定位：**R84 收口 ＋ 底图行 89 修订**｜类型：**裁定落地**｜优先级 **P1**。
约束：按 `CONSTRAINTS.md`｜禁止内联含反引号／特殊字符命令（**全程脚本文件**，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**本轮不派其他任务**。

**④ 状态：完成**（含 **3 处诚实边界**：① **CI 结论不入库**（C20）⇒ 本报告不预写"全绿"；② **汇编合订本只收录 `docs/` 34 份且不含根 `README.md`** ⇒ 改 README 后"重生成"是**逐字零差异**（已实测），**不得据此说"汇编已同步底图"**；③ **仓库外汇编快照**（`私有资产/` 内）**本轮未动、未核**——不在私有资产目录内，交用户裁定。）

**⑤ 验收对照**（用户本次 8 条验收标准原文）

| # | 标准（用户原文） | 实测 |
|---|---|---|
| 1 | R84 结案记录到 BASELINE | ✅ `BASELINE.md` **§二十三 §23.1**：根因（**count 225–229 的 5 条「夜间 P3」提交被排除**）＋ **三段式反证表**（同类事实三处处理不同：187–190 未排除｜204–206 未排除｜225–229 被排除）＋ **四规则假设检验表**（全否证）＋ **先例**（count 77→78，底图行 93 已承认）＋ **跳号说明**（下一版标签跳至 count 实时值）。行 **1236（R84）** 状态由「登记待裁」更新为**已结案** |
| 2 | 明文规则写入治理文件 | ✅ 三处落明文：① `CONSTRAINTS.md` 新增 **C21**（行 **449**）：版本号口径唯一，**`夜间 P3-n：` 前缀只用于区分来源（机制 23），不改变版本号；版本号一律 = count**；② `CHARTER.md` 新增 **机制 27**（行 **104**）＋ 机制 2 行号段改 **C1–C21** ＋ 关系块 ＋ §四速查表补 C21 行；③ `TEMPLATES.md` 新增 **T-036**（行 **351**）记录本轮变更 |
| 3 | 底图行 89 修订，逐字对照给出 | ✅ 逐字对照见 §23.3 与下表 ⑧；修订前后**唯一变化**＝把「**每次实质性迭代（代码提交／模块交付／验收通过）累加 1**」改为「**每次提交累加 1（不区分提交类型 …… 不得以"非实质性"为由排除任何提交）**」，**删去与行 90「版本号 = git 提交计数」相冲突的「实质性」歧义措辞** |
| 4 | 判据 F 更新并 PASS | ✅ **三态实证**（§23.4）：① 改 README 后判据 F **判红并点名 `README.md`**（**阳性对照**，证明 2026-09-19 扩项非空转）→ ② `--update-baseline` 重生成（基线 **36 份**；`git diff --numstat` = **1 行**）→ ③ **CRLF→LF 归一化**（39 处；字节 3374 → 3335）→ **改后 PASS**（`check_source_of_truth.py --ci` exit=0） |
| 5 | 汇编合订本重生成，逐字保真通过 | ✅ 用**既有脚本**逐字装配（§23.5）：`asm_batch1..4.py` ＋ `asm_binder.py` 重生成 —— 第1批 9 份/1844 行｜第2批 8 份＋1 指针节/1389 行｜第3批 6 份/1221 行｜第4批 12 份/1644 行（含泄漏掩码）｜合订本 **6415 行 / 487057 字节**。**反向自检**：跑前跑后 **5 文件 SHA-256 逐一相同** ＋ `git diff --numstat -- coordination/discussions/` **为空** ⇒ **逐字零差异** |
| 6 | C15 复算通过，编号表连续无缺号无重号 | ✅ 改前 → 改后：机制 **26→27（+1）**／C **20→21（+1）**／T **35→36（+1）**／D **44→44（±0）**／R **48→49（+1）**。三张编号表：**机制 1–27／C 1–21／T 1–36，缺号 无、重号 无、连续 是**；对照（阳性＝表内最大号／阴性＝最大号+1）**6/6 PASS**。**同一脚本取两次数**（C18 同源，`c15_take.sh` 改前/改后共用） |
| 7 | CI 全绿 | ⏳ **按 C20：结论不进仓库** ⇒ 以命令实取（`gh run list --limit 3`），**结论写在当轮对话返回／日报**。仓库侧本报告**只留命令**，**不写结论** |
| 8 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行 ＝ **`TEMPLATES.md` 行 104–118 逐字抽取**（D40 不手写），并**回读全等自检 PASS**（见 ⑭） |

**⑥ 未完成项**
① **CI 全绿待 push 后实取**（C20：只记日报／对话返回，**不预写结论、不入仓库**）；
② **汇编覆盖范围缺口（R86）** —— 合订本**实际只收录 `docs/` 34 份**（缺 `REFERENCE_APP_DESIGN.md`）、**不含根 `README.md`**；本轮**不擅改**（属**范围扩大**，须授权）⇒ 登记 **R86** ＋ §23.9 待裁②；
③ **仓库外汇编快照**（`私有资产/底图原件汇编_第1批_设计定位层_逐字_20260917.md`）**本轮未动** ⇒ §23.9 待裁③；
④ **`CHARTER.md` §四速查表 C16–C20 缺口** —— 本轮只补 **C21** 行，**C16–C20 未列**（已登记待裁①）；
⑤ **场源扩屏**（选 B，按指令**先不定**）｜**D-7 身份档案**（按指令**暂缓**）。

**⑦ 真实缺陷（本轮 1 条 ＋ 1 条口径缺口）**

① ★★ **R86：汇编副本的"覆盖范围"口径与实际不符** —— 合订本 §五 称「底图变更后必须重生成」（**暗示覆盖全部底图**），**实际只收录 `docs/` 34 份（缺 `REFERENCE_APP_DESIGN.md`）且不含根 `README.md`**。
   **实测**：① 汇编覆盖核对脚本得出 **34/35 收录**（阳性对照 `VISION`／`LAYER` 命中 = True，证明脚本非空转）；② 改 `README.md` 后重生成汇编 ⇒ **5 文件 SHA-256 全等、`git diff` 为空**。
   ⇒ **后果**：若只改 `README.md`，"重生成汇编"是**空转**（无变化），而**规则表述让人以为已同步**。
   **性质**：与 **R83**「退化／跳过／空转**必须与"通过"取不同值**」**同族** —— **空转必须与生效取不同值**。
   **处置**：本轮**不擅改**（范围扩大须授权）⇒ 登记 **R86**（`BASELINE.md` 行 **1423**）＋ §23.9 待裁②。

② **口径缺口（非缺陷，登记）** —— `CHARTER.md` §四「硬规则速查表」长期**只列 C1–C15**（C16–C20 未列）。本轮**只补 C21 行**并在标题与覆盖说明块中**明示 C16–C20 未列、本轮未补、已登记待裁**；**未擅自扩表**（避免顺手改动扩大面）。

**⑧ 三量台账（四量）**

| 量 | 内容 |
|---|---|
| **存量** | 带入的：crate 8 个｜`nostd` lib **477** 测试｜host 镜像 **13**｜机制 **26**｜C **20**｜T **35**｜D **44**｜R **48**｜CI 三 job（`test`／`boot-image`／`field-render`）｜第 ⑫ 段（自检 121–125／product 126–128／evolve 129–132／退化 133–134）｜底图 = 根 `README.md` ＋ `docs/` 35（判据 F 基线 **36 份**） |
| **变量** | 本轮**改动 6 文件 ＋ 1 报告**：底图 1（`README.md` 行 89）＋ 基线 1（`coordination/security/basemap_hashes.txt` 1 行）＋ 治理 3（`CONSTRAINTS.md`／`CHARTER.md`／`TEMPLATES.md`）＋ 台账 1（`BASELINE.md` 追加 §二十三，**+132 行**）＋ 报告 1（本文件）。`git diff --stat` = **6 files changed, 193 insertions(+), 5 deletions(-)** |
| **补充增量** | **既有内容的补全与订正**（非新能力）：① 底图行 89 **歧义措辞删除**（与行 90 对齐）；② `CONSTRAINTS.md` **行 7 活相列表**补记 **C20、C21**（顺带订正上轮漏记的 C20）；③ `CHARTER.md` **机制 2 行**的号段 `C1–C20` → `C1–C21`；④ 判据 F 基线**重生成 ＋ LF 归一化** |
| **创新增量** | **全新能力／条目（本轮首次出现）**：① **C21**（版本号口径唯一）；② **机制 27**（版本号口径机制）；③ **T-036**（变更记录）；④ **R86**（汇编覆盖范围缺口）；⑤ `BASELINE.md` **§二十三** 全节（9 个子节：23.1–23.9） |

**⑨ 边界说明（四类 · 与机制 26 四层对齐）**

| 类 | 层 | 本轮实测内容（**命令级**） |
|---|---|---|
| **本地跑过** | **M** | 判据脚本 **14 项全绿**（机制 17／18／21／22／24／25 ＋ 迁移闭包／保真 ＋ 屏幕转储，含 `--selftest` 与实跑，`run_all_checks_0920c.sh` ⇒ **14/14 exit=0**）｜机制 22 实跑：`底图 .md 数：36` ⇒ **PASS**｜机制 25 实跑：`扫描范围 46 份`／`机器·最大段号 ⑫`／`机器·迁移收口 剩余 0` ⇒ **✅ 通过（P1/P2 双侧/P3/P4）**｜**C15** 双向计数通过（6/6 对照）｜汇编**幂等性**：5 文件 SHA-256 跑前跑后全等｜**泄漏扫描**：改动 6 文件用户名 0、新增绝对路径 0 |
| **CI 过** | **CB** | ⏳ **待本轮 push 后实取**（`gh run list --limit 3`）。**按 C20：结论不进仓库** ⇒ 见**当轮对话返回／日报**。**不预写"全绿"** |
| **真实跑过** | **CQ** | 不适用本轮（**未改代码／未改内核**）—— 本轮只动**底图 ＋ 治理文件 ＋ 台账 ＋ 基线**。⑫ 段的真实引导（QEMU）结论**沿用上轮**，本轮**不重跑、不新增断言** |
| **未验证** | **H** ＋ 上述缺口 | ❌ **CI 结论**（待 push）｜❌ **汇编快照覆盖范围**（R86，未授权扩大）｜❌ **仓库外汇编快照**（未核）｜❌ **场源扩屏**（暂缓）｜❌ **D-7 身份档案**（暂缓）｜❌ 真机（H 层）帧缓冲回读 ——**能力矩阵唯一空白层** |

**⑩ 决策评估（七要素）** —— 本轮**新增三项待裁**（口径缺口类，非功能决策）

| 要素 | 内容 |
|---|---|
| **评估** | 本轮收口 R84 时，顺带测绘出**三处"口径与实际不符／待补全"**：① `CHARTER.md` §四速查表**只列 C1–C15**（C16–C20 长期未列）；② **汇编覆盖范围**（§五）表述**暗示覆盖全部底图**，实测**只收 `docs/` 34 份且不含根 `README.md`**（R86）；③ **仓库外**存在一份汇编快照（`私有资产/底图原件汇编_第1批_…_20260917.md`），**是否随底图修订而失效**未核 |
| **优势** | ① **补全速查表**：治理文件**自洽**，后人查 C16–C20 不用翻正文；② **写明汇编覆盖清单**：让"我重生成过了"与"我什么都没同步"**可区分**（治 R86）；③ **快照标注**：读者不会把**历史快照**当**现值**（治 R78／R82） |
| **劣势** | ① 速查表扩 5 行 ⇒ **改动面变大**（超出本轮"改动清单"授权范围）；② 汇编覆盖清单**要改汇编脚本或合订本 §五 措辞** ⇒ 动**生成物或其源头**，须评估幂等性；③ 快照处理**落在仓库外**（私有资产），**我本轮未进入该目录**，不了解其结构与用途 |
| **风险** | ★ **不做的风险**：口径缺口**继续静默**（"汇编已同步"的错觉在下次改底图时会**真的**造成漏同步）。**做的风险**：① 速查表扩项若**与本轮 R84 收口混在一个提交**，会让"本轮只做 R84"的**边界变模糊**；② 汇编脚本改动若**破坏幂等性**（R78 副本静默过期），代价大于收益 |
| **机会** | 三项都是**低成本、可回退**的治理补全 —— 可**下一轮单独派 1 个"治理口径补全"小任务**（改动清单先列出、经确认再动），**不与本轮混提** |
| **建议** | **推荐：本轮不擅改，三项全部登记待裁**（§23.9）。**理由**：三者都属"**范围扩大**"，按流水线**须先列改动清单、经确认再执行**；且与本轮"R84 收口"是**不同主题**，混提会**污染本轮边界**。**可下一轮合并为一个小任务**（推荐先做 ① 速查表补全，成本最低、收益最直接） |
| **等待决策** | 见 ⑪ ①②③（各自 (a)/(b) 选项） |

**⑪ 需要你决策**
① **`CHARTER.md` §四速查表 C16–C20 缺口**：**(a)** 下一轮**补全 C16–C20 五行**（使其自洽）／**(b)** 维持现状（**只列至 C20 有争议**，或干脆只留指针）／**(c)** 本轮**已补 C21 行 ＋ 明示缺口**，暂不动，待有需要再补。**推荐 (a) 下一轮单独做**。
② **汇编覆盖范围口径（R86）**：**(a)** 在**合订本 §五 写明覆盖清单**（"本汇编收录 `docs/` **34** 份，**不含根 `README.md`**；底图含 README 变更时须另行处理"）／**(b)** 把**根 `README.md` 纳入汇编装配**（**真正的范围扩大**，须改装配脚本）／**(c)** 只登记 R86，**本轮与下轮都不动**。**推荐 (a)**（成本最低、直接治"空转与生效不可区分"）。
③ **仓库外汇编快照**（`私有资产/底图原件汇编_第1批_设计定位层_逐字_20260917.md`）：**(a)** 标注为**历史快照**（点名其对应 count，读者知其为旧）／**(b)** 按新底图**重生成**／**(c)** 不动、仅登记。**说明**：该文件在**仓库外**（私有资产目录），**本轮未进入、未核实**；**若你认为该目录本轮不该动，选 (c) 即可**。

**⑫ 下一步建议**
1. **本轮 push 后**：取三 job（`test`／`boot-image`／`field-render`）结论 —— **按 C20 只记日报／对话返回**，**不改仓库**；
2. **R84 现已收口** ⇒ 下一轮可回到**功能主线**：**场源扩屏**（需先定**网格尺寸策略**，`2^k+1` 与屏幕分辨率的映射）—— 按你上轮"选 B，先不定，多跑几轮"的指示，**仍可再等**；
3. **治理口径补全**（本轮登记的三项）：推荐**下一轮单独派一个小任务**（先列改动清单、经确认再动），**从 ① 速查表补全起**；
4. **H 层判据**：真机帧缓冲回读（WC 显存语义）—— 目前 **0 判据**，是能力矩阵**唯一空白层**；
5. **可顺手做**：`fmath.rs` doc 旧值订正（**长期登记待办**，为不动被测物而未改）。

**⑬ 启动回执**：已读 `CHARTER.md`。版本 = `git rev-list --count HEAD`（**不写死**，C2；本轮起点 count = **246**；**本轮起口径切换为 label = count**，本报告标签 = **提交后实时 count，预期 v0.247**）｜本次任务：**R84 收口 ＋ 底图行 89 修订**｜**是否继续：等待裁定（⑪ ①②③）**。

**⑭ 本轮读了底图哪几份、哪几行**（D55／D52：报告须写明；**行号均当轮实测**，R75）

- **根 `README.md` 全文 132 行**（实测 `wc -l` ＝ **132**）—— 本轮**核心引用**：**行 87–94**（「版本规则」全节）：
  - **行 89** ＝ **本轮修订对象**（格式固定 `v0.xxx`；修订后 = 「每次提交累加 1（不区分提交类型）」）；
  - **行 90** ＝ 「**版本号 = git 提交计数**」，**R84 的底图出处**（与行 89 原「实质性」措辞构成**张力**，即漂移的思想来源）；
  - **行 93** ＝ 旧人工口径切回提交计数的**明文先例**。
  另实取引用：**行 13–20**（三层次表）。
- **`docs/` 目录 35 份**（实测 `ls docs/*.md | wc -l` ＝ **35**，与判据 F 基线 **36 份** ＝ `README` ＋ `docs/` 35 一致）—— **grep「版本号」命中 4 处**（实测）：
  - `docs/L4_VALIDATION_REPORT.md:30`（`v0.002` 旧人工口径）
  - `docs/L5_VALIDATION_REPORT.md:86`（`v0.081（本提交 #81）`）
  - `docs/LAYER_BASEMAP_L0_L6.md:334`（L3-U1 快照 schema 版本号，**待补项**）
  - `docs/SELF_DIAGNOSIS_REPORT.md:114`（`版本号 = 提交计数（v0.094）`）
  ⇒ 四处均为**历史快照／待补项**，**无一处与行 89 冲突** ⇒ **底图行 89 无派生副本**（坐实：本修订只需动 `README.md` 一处 ＋ 基线重生成）。
- **判据工具（底图合规的判据源）**：`coordination/tools/check_source_of_truth.py` **全文**（确认 `targets = [repo / "README.md"] + sorted((repo / "docs").glob("*.md"))` ⇒ **根 `README.md` 在基线内**；2026-09-19 已扩项 35 → **36 份**，带**阴性对照 4**）｜`coordination/security/basemap_hashes.txt` **全文 36 条目**。
- **汇编（改动落点）**：`coordination/discussions/2026-09-18_汇编正文_第1/2/3/4批*.md` ＋ `2026-09-19_汇编合订本.md`（**重生成 ＋ 幂等核对**）。
- **脚手架（非底图，改动落点）**：`coordination/CHARTER.md`（机制表 ＋ **行 104 机制 27** ＋ 机制 2 行 ＋ §四速查表）｜`coordination/CONSTRAINTS.md`（**行 449 C21** ＋ 行 7 活相列表）｜`coordination/TEMPLATES.md`（**行 104–118** ＝ 阅读指令逐字源｜**行 351 T-036**）｜`coordination/BASELINE.md`（**行 1328 起 §二十三**，9 子节）。
- **被测物**：**本轮零代码改动** ⇒ 未改任何 `.rs`／`.yml`／`.toml`。

---

**底图行 89 修订 · 逐字对照（用户第 3 条验收标准要求"逐字对照给出"）**

| | 内容（**逐字**） |
|---|---|
| **修订前** | `- **格式固定为 v0.xxx**：xxx 为三位数字，从 001 起，每次实质性迭代（代码提交 / 模块交付 / 验收通过）累加 1：001 → 002 → … → 009 → **010**（不是 0.01）→ … → 100。恒三位，不省前导零。` |
| **修订后** | `- **格式固定为 v0.xxx**：xxx 为三位数字，从 001 起，**每次提交累加 1**（**不区分提交类型** —— 代码／文档／治理／CI／**夜间自动化**提交**一律计入**；**不得以"非实质性"为由排除任何提交**）：001 → 002 → … → 009 → **010**（不是 0.01）→ … → 100。恒三位，不省前导零。` |
| **差异** | 删「**每次实质性迭代（代码提交 / 模块交付 / 验收通过）累加 1**」→ 改「**每次提交累加 1（不区分提交类型 …… 不得以"非实质性"为由排除任何提交）**」。**目的**：删去与行 90「版本号 = git 提交计数」**相冲突的「实质性」歧义措辞**（该措辞正是 count 225–229 五条被排除的**思想来源**） |

---

**一句话**：**R84 收口完成** —— 版本号口径**回到 `label = count`（实时取）**，**底图行 89 的「实质性」歧义措辞已删**，**C21／机制 27／T-036** 三处明文落定，**判据 F 三态实证 PASS**（改前判红点名 `README.md` → 重生成 → LF 归一化 → PASS），**汇编重生成逐字零差异**，**C15 双向计数 6/6 PASS**（机制 27／C 21／T 36／D 44／R 49，三表连续无缺号无重号）。**唯一需要你点头的：本轮登记的三项治理口径补全（⑪ ①②③，推荐 (a)/(a)/(c)）。**
