【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# 推送放行 ＋ 5 项裁定落地（2026-09-20 · v0.259）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。
（本轮改动文件泄漏扫描：新增行用户名 **0**、绝对路径 **0**。）

**② 全景状态卡**（★ 数字为**生成时快照**；提交会推高版本号 ⇒ **现值以复核命令为准**，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 258** ⇒ 收尾 **259**（本轮 **1 个本地 commit**；**未 push** —— 待你放行）⇒ label **v0.259**（`label = count`，C21） |
| **阶段** | 阶段二 · **2.3b ✅ 收口**｜**2.4 纯算层 ＋ 边界层已落地**（待 CI 复验） |
| **编号** | 机制 **1–27**｜C **1–21**｜T **1–36**｜D **1–44**（§九）｜**R 1–52**（本轮**无新增 R**） |
| **三表** | **连续无缺号无重号**（C15 实测，对照 6/6 PASS） |
| **工作树** | 见 ⑬（本报告提交前为「已改待提交」） |

**③ 任务**：基准 **count = 258**｜总规划定位：**6 个 commit 推送放行 ＋ 5 项裁定落地**｜类型：**放行 ＋ 裁定落地**。

**④ 状态：完成**（含 **3 处诚实边界**：① **CI 三态按 C20 只记当日日报**（仓库外），本报告**不内联**；② **本轮内核对代码零改动** ⇒ **无 QEMU／裸机侧新结论**；③ **平台任务处置仅在本机** ⇒ **仓库内不可观测**）。

---

## ★ 主体

### A. Push 放行（用户 §〇）

| 项 | 实测 |
|---|---|
| **推送** | `git push origin main` ⇒ **`bad7881..eeb1495`**；**退出码 0**；push 后**未推送数 ＝ 0**；远端 `main` ＝ 本地 `HEAD` ＝ **`eeb1495`** |
| **内容** | **6 个 commit**：`61a98c3`(v0.253)｜`09e7eea`(v0.254)｜`47a8bd9`(v0.255)｜`727b346`(v0.256)｜`39e5187`(v0.257)｜`eeb1495`(v0.258) |
| **重点核实（P3①）** | v0.257 改 `check_doc_consistency.py` ⇒ 由 **CI `test` job「机制 25 文档一致性门禁」**（`ci.yml` L331–337：`--selftest` ＋ 实跑）覆盖 ⇒ **该 job 三态 ＝ 端点**（**R89：只用运行期证据**） |
| **CI 结论** | **按 C20 记当日日报（仓库外，`.workbuddy/memory/2026-09-20.md`）** —— **本报告不内联三态**（治 R82 自指递归） |

### B. 裁定 A：`ROADMAP.md:53` 加括注（用户 §一）

**逐字对照**
```diff
- > **新路线：元内核自举**——无 Linux 依赖，DRM/KMS 直接渲染。
+ > **新路线：元内核自举**——无 Linux 依赖，DRM/KMS 直接渲染。（★ **2026-09-19 口径修正**：改为**轻量帧缓冲 ＋ L6 呈现层接入**，**不引入 DRM/KMS** ⇒ 降为**远期可选** —— 见下方 **2.4 行**）
```
- **理由**：2.4 口径已于 **2026-09-19**（`bb72eec9`）修正，但 L53 叙述句（2026-09-16 修正记录）仍留旧口径 ⇒ **C17「落后 ≠ 虚假」**（`CONSTRAINTS.md:355` 先例）⇒ **只加括注、不改原文**。
- **C15**：编号表 **全 ±0**（详见 ⑤-8）；**台账落点**：`BASELINE.md §28.1`。

### C. 裁定 B：`advisor_brief.md` 按核对稿（二）订正 **7 处**（用户 §二）

**口径**：**只动活跃段**；**历史段（G1／G2）只加括注／后记、不改写**（C19）。**改动量**：`7(+)/7(-)`，**行数不变 280**。

```diff
# T1 · §五 L235（技术主线 · 活跃）
- …第一里程碑是"**能在 QEMU 里跑起来并按 DRM/KMS 输出场域画面**"。
+ …第一里程碑是"**能在 QEMU 里跑起来并输出场域画面**"（★ **2026-09-19 口径修正**：**轻量帧缓冲 ＋ L6 呈现层接入**；**不引入 DRM/KMS**，降为**远期可选** —— 见 `ROADMAP.md` 2.4 行）。

# T2 · §六 L267（D 编号清单 · 活跃）
- **历史摘录（**当时**的快照）**：D1–D31 全部收口；D32 GHA nightly、D33 夜间是否 push 待拍板
+ **历史摘录（当时快照；D 行状态以 §四 表为准）**：D1–D31 全部收口；**D32／D33 已于 2026-09-17 裁定**（D32 ✅ 暂不创建／D33 ✅ 夜间默认不 push）

# G1 · §二 L31（历史段 · 只加括注）
- …ACPI/CPUID 软识别 → QEMU 验证。
+ …QEMU 验证。 **（★ 2026-09-19 再修正：此处「DRM/KMS 直渲」已改为 **轻量帧缓冲 ＋ L6 呈现层接入** —— 见 `ROADMAP.md` 2.4 行）**

# G2 · §三 L85（历史段 · 只加后记）
- （23:00 开工尚未发生；09-18 08:00 晨报＋08:30 独立复核会把证据落到文件）。
+ （同上）。**（★ 后记：23:00 开工已于首夜发生 ✅ —— 见 §四 D33 行）**

# N1 · 头部 L7（数值型 · 活跃）
- > （十块：阅读指令／项目身份／…／开场白）。
+ > （**卷首「阅读指令」＋ §1–§10 共 10 节**：项目身份／…／开场白）。

# P1 · §六 L266（R 清单行 · 自指）
- 以 `BASELINE.md` 实测为准；清单见 §六。
+ 以 `BASELINE.md` 实测为准；清单见 `BASELINE.md`（引路径；R 清单实体在 `BASELINE.md`，不循环指向本节 §六）。

# P3 · §七 L280（仓外指针 · 活跃）
- 生成格式见 `CONTINUITY.md`「顾问侧接续」章节。
+ 生成格式见 `CONTINUITY.md`「顾问侧接续」章节（★ **仓库外**：`<工作区>/.workbuddy/memory/CONTINUITY.md`；**仓库内不可校验** —— D3：须引路径 ＋ 校验命令）。
```
- **台账落点**：`BASELINE.md §28.2`。**未纳入**：**P2**（待你判）／**U1**（已核实 ACTIVE ⇒ 不改）。

### D. 裁定 C：`docs/` 订正 **4 处** ＋ 基线·汇编同步（用户 §三）

**改动量**：`docs/LAYER_ARCHITECTURE.md` `4(+)/2(-)`｜`docs/SELF_DIAGNOSIS_REPORT.md` `2(+)/1(-)`｜`docs/API_GATEWAY_DESIGN.md` `1(+)/1(-)`｜`docs/LAYER_BASEMAP_L0_L6.md` `1(+)/1(-)`。

```diff
# B-1 · LAYER_ARCHITECTURE.md L65（活文档 · 同文件自相矛盾）
- …**已定义、尚未实现**。
+ …**已定义；判定与记账（T1 闭环）已实现、执行侧待确认**。
+ > ★ 2026-09-20 订正：依据＝**代码落点**（`npb-gateway/src/l7_exec.rs` ＋ `docs/L7_EXECUTION_DESIGN.md`），与 §2.1 标题及 §1 纪律 5 一致。
# B-1 · LAYER_ARCHITECTURE.md L323
- > L7 为**已定义、未实现**的层（v1.4 定名）；…
+ > L7 为**已定义、判定与记账（T1 闭环）已实现**的层（v1.4 定名）；…
+ > ★ 2026-09-20 订正：原「未实现」已按**代码落点**修正 —— 与 §2.1 标题口径一致。

# B-2 · SELF_DIAGNOSIS_REPORT.md 头部（＋后记）
  > 诊断日期：2026-09-14
+ > ⚠️ **后记（2026-09-20）**：本报告为 **2026-09-14 时点快照**；计数以复核命令为准（R71／R82，**不追改** —— C19）；**L7 状态已变**。（来源：`BASELINE.md` §二十八）
# B-2 · SELF_DIAGNOSIS_REPORT.md L38（状态行）
- … | 🟡 已定义未实现（本报告原为推演） | — |
+ … | 🟡 已定义未实现（本报告原为推演）**★ 2026-09-20 更新：判定与记账（T1 闭环）已实现**（见 `docs/L7_EXECUTION_DESIGN.md`） | — |

# C-1 · API_GATEWAY_DESIGN.md L3（状态行 · 活跃）
- … ｜ 阶段：L3（实现中）
+ … ｜ 阶段：**L3（已实现并发布）**（★ 2026-09-20 订正：`npb-gateway/` 已发布、机制 24 已入 CI；**端点表唯一权威＝`LAYER_ARCHITECTURE.md §4.L3`**）

# C-2 · LAYER_BASEMAP_L0_L6.md L44（状态行 ＋ 死指针 · 活跃）
- | L3 | … | 依赖；端点表不统一（见 §3.3-①） |
+ | L3 | … | 依赖（★ 2026-09-20 订正：**端点表已统一** —— 权威见 `LAYER_ARCHITECTURE.md §4.L3`、判据＝机制 24；原指针为**死指针**，本文件 §3.3 无 ① 项） |
```

**D.2 判据 F 更新（用户 §3.2）**
- **动作**：`check_source_of_truth.py --update-baseline` ⇒ 重生成 `coordination/security/basemap_hashes.txt`（**36 份**）。
- **阳性对照（★ 改前）**：改 `docs/` 后**未更新基线**时跑判据 F ⇒ **`FAIL（4 项）`**，逐份点名 4 个改动文件 ⇒ **判据确实感知底图变更**（非空转）。
- **改后复跑** ⇒ **`PASS`**。**哈希行改动**：4 行（对应 4 份底图）。

**D.3 汇编合订本重生成（用户 §3.3）**
- **脚本（既有）**：`asm_regen_0920.sh` ⇒ `asm_batch1.py`／`asm_batch2_v2.py`／`asm_batch3.py`／`asm_batch4_v3.py`（从 `docs/` 重生成四批）＋ `asm_binder.py`（装配合订本）。**逐字装配、不手抄**。
- **产物**：`coordination/discussions/` 四批 ＋ `2026-09-19_汇编合订本.md`；**第 2 批无涉本次改动（SHA 不变）**。
- **幂等性**：**重跑 SHA 完全一致**（`9a252ff1…`／`ba6502f4…`／`1127a73b…`／`7e4c93fb…`）⇒ 装配**确定性**。
- **反向自检（逐字保真）**：`asm_selfcheck_0920.py` ⇒ **`PASS`** —— 结构稿三段 ＋ 四批正文**均为合订本子串**；**覆盖清单 34 份一致**；「不含根 `README.md`」＋「空转」断言**均在**。

### E. 裁定 D：机制 25 混合行「**只登记、不做试点**」（用户 §四）

- **结论（用户已确认）**：**先不做试点，只登记**。23 行「混合盲区」**已在 v0.257 量化**（`reports/2026-09-20_夜间判据自检加固记录.md`），**无需额外动作**。
- **★ 触发条件**：**一旦真出现「该判红没判红」的实例** ⇒ 把「精确豁免」落成**机器判据**。
- **本轮零脚本改动**（`check_doc_consistency.py` 未动）。**台账落点**：`BASELINE.md §28.4`。

### F. 裁定 E：两条过期平台任务下线（用户 §五 · **只在本机，不入库**）

| # | 任务（一次性 · 已过期 · 原 ACTIVE） | 处置 |
|---|---|---|
| 1 | `云内核·首夜结果晨报（09-18 08:00）`（`613e33b2-…`） | ⇒ **`PAUSED`（禁用）** |
| 2 | `云内核·首夜晨报**独立复核**（09-18 08:30）`（`db63f5e7-…`） | ⇒ **`PAUSED`（禁用）** |

- **「删除或禁用」二选一 ⇒ 取禁用**，理由＝① **可逆**；② 与**本平台既有退役一次性任务惯例一致**（同清单中 `自动盘点（09-14）`／`资源巡检·首次验证运行` 均 `PAUSED`）；③ **保留记录**（留痕）。**非删除**。
- **验证**：处置后**复核清单 ⇒ 两条均 `PAUSED`**。**范围**：仅本机平台配置，**不进仓库**。
- **⚠️ 顺带观察（新增 · 未处置）**：清单中**另有第三条**已过期一次性任务仍 `ACTIVE` —— `云内核·夜间任务晨报（一次性）`（`scheduledAt 2026-09-20T08:00`，`2b4ece8e-…`）。**不在用户点名的「两条」内 ⇒ 本轮未动**（列 ⑪）。
- **台账落点**：`BASELINE.md §28.5`。

---

**⑤ 验收对照**（用户 §八 共 9 条，逐条实测）

| # | 验收标准 | 实测 |
|---|---|---|
| 1 | **push 完成，CI 三 job 全绿** | ✅ push **实测 `bad7881..eeb1495`／exit 0／未推送数 0**；**CI 三态按 C20 记当日日报**（仓库外），**其结论＝三 job 全绿** |
| 2 | `ROADMAP:53` 加括注，逐字对照 | ✅ 加括注（**＋14 字括注**，原文一字未改）；逐字对照见 B；**C15 全 ±0** |
| 3 | `advisor_brief` 7 处订正，逐字对照 | ✅ **7 处**（T1/T2/G1/G2/N1/P1/P3）；`7(+)/7(-)`、**行数不变 280**；逐字对照见 C |
| 4 | `docs/` 4 处订正，逐字对照；**判据 F 更新并 PASS** | ✅ **4 处**（B-1 两行／B-2 头部＋L38／C-1／C-2）；判据 F **改前 FAIL(4) → `--update-baseline` → 改后 PASS** |
| 5 | **汇编合订本重生成，逐字保真** | ✅ `asm_regen_0920.sh` 重生成；**幂等（重跑 SHA 一致）**；**反向自检 PASS**（逐字子串 ＋ 覆盖 34 份） |
| 6 | **机制 25 混合行登记到 BASELINE** | ✅ `BASELINE.md §28.4`（**只登记、不试点**；**触发条件**已写明） |
| 7 | **两条过期平台任务下线** | ✅ 两条 **`PAUSED`**（禁用）；**验证后复核清单**；**仅本机、不入库** |
| 8 | **C15 复算通过，编号表连续无缺号无重号** | ✅ 机制 27／C 21／T 36／D 44／R 52／§四表 21 —— **改前＝改后 全 ±0**；三表**连续、无缺号、无重号**；对照 **6/6 PASS** |
| 9 | **下一次返回第 0 行含 9 步阅读指令** | ✅ 本报告第 0 行逐字自检 **PASS 15/15**（同源脚本，C18） |

**⑥ 未完成项**：**无**（用户派定 5 项 ＋ 放行 1 项全部落地）。留给外部的是**本轮新 commit 的放行**（见 ⑪）。

**⑦ 真实缺陷（项目代码侧 0 条）＋ 0 条新 R ＋ 2 条过程观察**

- **本轮零内核代码改动** ⇒ **不构成任何内核／boot 侧新结论**（**不反推**）。
- **过程观察 1（口径核对）**：`docs/` 核对稿 **B-2** 内部存在**张力** —— §1 建议「整体加标注、**不改数**」，而 §7 把 **B-2（L38）** 列为「**必改（实质误导）**」。**本轮处置**＝**两者都做且不冲突**：**保留原状态留痕 ＋ 追加后记指针**（**与 §二十五「状态传播」同法**），**数字照旧不动**（C19）。⇒ **建议**：把该处置法**写进 `docs/` 核对稿**（免下次重判）。
- **过程观察 2（R77 相邻）**：汇编第 4 批收录 `docs/FULL_AUDIT_REPORT.md` 时**掩码 16 行**、`PERFORMANCE_REPORT.md` **掩码 1 行** ⇒ **重生成后掩码仍生效**（**未把底图既存泄漏扩为 2 份**，R25／R77 维持）。

**⑧ 三量台账（四量）**

| 量 | 内容 |
|---|---|
| **存量** | **底图 4 份**（获授权改）｜`security/basemap_hashes.txt`（36 份）｜**汇编 5 份副本**｜`ROADMAP.md`／`advisor_brief.md`｜`BASELINE.md`（**1896 → 2071 行**） |
| **变量** | 底图 **4 files**（`7(+)/6(-)`）｜`basemap_hashes.txt` `4(+)/4(-)`｜`advisor_brief` `7(+)/7(-)`｜`ROADMAP` `1(+)/1(-)`｜汇编四批＋合订本（**825/822** 合订本为主）｜`BASELINE` **＋175 行**；**count 258 → 259** |
| **补充增量** | ① **「历史报告状态订正」统一法**（**留痕 ＋ 后记指针**，覆盖 B-2 张力）；② **判据 F 的「阳性对照流程」**（**先 FAIL 再 update 再 PASS**）—— 把「改底图」变成**可证伪**动作 |
| **创新增量** | ① **「汇编链反演」**：`docs/` → 四批 → 合订本，**三跳全自动、幂等 SHA 可验**；② **「禁区白名单式验收」**：改 `docs/` 是**授权例外**，用**数量断言（恰 4 份）**把「例外」也纳入机器可验 |

**⑨ 边界说明（四类 · 与机制 26 四层对齐）**

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | 14 项判据 **PASS=14／FAIL=0**（含机制 22 F 的 **FAIL→PASS 阳性对照**）；C15 **6/6 对照 PASS**；汇编**反向自检 PASS ＋ 幂等 SHA 一致**；第 0 行自检 **15/15 PASS** |
| **CI 过** ✅ | **`test`／`宿主 field-render`／`元内核引导镜像` 三 job 全绿**（**sha `eeb1495`**）；**端点＝机制 25 门禁（`ci.yml` L331–337）**；**⚠️ 结论按 C20 只记当日日报（仓库外），本报告不内联** |
| **真实跑过** | **不适用**（零 boot／内核改动） |
| **未验证** ⚠️ | ① **本轮新 commit（v0.259）尚未 push ⇒ 其 CI 未跑**（**不得引用 eeb1495 的 CI 结论**）；② 平台任务状态**会变**（不写死，R71）；③ `docs/` **第二批未扫** |

**⑩ 决策评估（七要素）** —— 本轮**无新建制决策**（5 项均由用户派定）。⑪ 的 3 项为**新暴露分支**，按「决策呈现规范」摆全要素：

- **① 第三条过期任务（`云内核·夜间任务晨报`）**：评估＝计划时刻 `09-20 08:00` 已过、属"退役"类｜优势＝**清单归零**、消除误导｜劣势＝无（**该产物已被今晨会话取代**）｜风险＝不做则清单持续出现"僵尸任务"｜机会＝可把"过期一次性任务自动归档"做成巡检项｜建议＝**下线（`PAUSED`）**｜**等你一句话**。
- **② `advisor_brief:35`（P2「不做 f64 版」）**：评估＝`≤4 ULP` 是 **f32** 口径，**f64 侧从无门线**（D-1 已结项）｜优势＝消除"f64 不存在"的误读｜劣势＝属历史段（**需按 G1／G2 同法加括注**）｜风险＝不做则顾问可能误判 f64 缺失｜建议＝**按核对稿 §7 加限定括注**｜**等你一句话**。
- **③ `docs/` 时效第二批**：评估＝第一批只**按家族定向扫**（非逐行穷举，已声明）｜优势＝覆盖 §6 未覆盖清单｜劣势＝量大（**建议夜间跑**）｜风险＝无｜建议＝**可夜间跑**。

**⑪ 需要你决策（1 项 ＋ 2 项顺带）**：

1. **★ 本轮新 commit（v0.259）是否 push 放行**（**机制 23：push 须当轮明示** —— 本轮仅授权了 v0.253–v0.258 六个，**故 v0.259 停在本地**）。
2. **顺带**：第三条过期一次性平台任务（`云内核·夜间任务晨报`，`09-20 08:00`）是否同样下线（**推荐：下线**）。
3. **顺带**：`advisor_brief:35`（P2）是否按稿加限定（**推荐：按稿**）。

**⑫ 下一步建议**（按优先级）：**P1** 你裁 ⑪-1（放行 v0.259）⇒ 同轮 push ＋ 交 CI｜**P2** ⑪-2／⑪-3（小步、可逆）｜**P2** `docs/` 时效**第二批**（**可夜间跑**）｜**P3** 场源扩屏（**选 B，仍暂缓**）｜**P3** D-7 身份档案（**暂缓**）。

**⑬ 启动回执**：已读 `CHARTER.md`。版本 = `git rev-list --count HEAD`（**不写死**，C2；本轮起点 **258**，收尾 **259**）。本次任务：**6 个 commit 推送放行 ＋ 5 项裁定落地**。**⚠️ 本轮仅授权 push v0.253–v0.258（已执行）；v0.259 待放行**；**CI 结论按 C20 不入仓库**（只记当日日报）。

**⑭ 本轮读了底图哪几份、哪几行**（D55／D52：报告须写明；**行号均当轮实测**，R75）

| 底图文件 | 读的行 |
|---|---|
| `README.md` | **L108–L120**（仓库结构；确认未改） |
| `docs/LAYER_ARCHITECTURE.md` | **L45–L75**（§2 总表 ＋ **L53 标题**／**L65 正文（B-1）**）｜**L318–L336**（**L321 标题**／**L323 正文（B-1）**） |
| `docs/SELF_DIAGNOSIS_REPORT.md` | **L1–L52**（**L4 版本**／**L6 诊断日期**／**L38（B-2）** 表行） |
| `docs/API_GATEWAY_DESIGN.md` | **L1–L12**（**L3 设计状态（C-1）**） |
| `docs/LAYER_BASEMAP_L0_L6.md` | **L36–L53**（**L44（C-2）** ＋ L48 L6 对齐反证） |
| **（另）`coordination/` 台账（非底图）** | `ROADMAP.md` L40–L85｜`advisor_brief.md` **全文 280 行**｜`BASELINE.md` §二十七（L1742–L1896）｜`CHARTER.md` 机制 24/25 行｜`CONSTRAINTS.md` L344–L372｜`TEMPLATES.md` 第〇项｜`.github/workflows/ci.yml` L130–L360｜实现侧 `npb-gateway/src/*`（判据侧） |

---

> **保密**：⛔ 机密 0 处；**报告内未收录**任何内网 IP／密钥／账号／本机绝对路径（R25）。
> **本报告已过**：第 0 行逐字自检 **15/15 PASS**（`rpt_head_check.py`，同源 C18）。
