【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-24 · v0.333 第七补（`云内核-C3-009`）· push 放行 ＋ **判据 CI 可证伪** ＋ **端口号敏感性调研** ＋ 长期 🟡 登记 ＋ 调研先行原则

> ★ **版本与轮次（`T-050` 标名）**：**版本 `count = 391`（开工实测）→ `394`（收口 · 3 组提交）** ｜ **轮次 `v0.333`** —— 两者不等。
> ★ **指令序号（`T-060`）**：**本轮 `云内核-C3-009`** ｜ **下一轮 `云内核-C3-010`** ｜ **下一轮基准 `count`＝394**。
> ★ **性质**：**放行 ＋ 调研（单一主类型，辅以放行）**。
> ★ **本件要解决的一件事**：**把"判据已立"推进到"判据可被证伪"，并在"端口号是否敏感"上先出事实、不下结论**。

---

## ① 保密标记（`C12` · 逐件分级 ｜ 链上限见 `security/chain_visibility.md`）

| 产出物 | 级别 | 说明 |
|---|---|---|
| 本报告 | 🔒 **可拷** | 不列具体端口数值／真实 IP／主机名／二进制体积 |
| `discussions/2026-09-24_端口号敏感性调研稿.md`（**新**） | 🔒 **可拷** | ★ **全稿不列任何具体值**（已过本判据 ⇒ 告警 0） |
| `security/known_accepted.md`（**新**） | 🔒 **可拷** | 登记表 |
| `coordination/tools/check_chain_visibility.py`（**只读、未改**） | 🔒 **可拷** | 规则为形态级描述 |
| 判据实跑原始输出（**仓库外**） | ⛔／🔐 **不入库** | 含具体端口值 |

- ⛔ **绝不拷**：本机绝对路径／账号／内网 IP／**端口号**（**待调研后裁定**）／**密钥与 token**／二进制路径与体积 —— ★ **本报告一律不列具体值**（★ 仓库为 **PUBLIC**）。
- 🔐 **屏蔽后拷**：函数签名／阈值常量／T0–T3 判定顺序／场域读数。
- 🔒 **可拷**：crate 与模块名／相对路径／测试计数／层级映射／治理编号。
- 🔓 **直接拷**：版本号／缺陷编号／机制条文／阶段口径定义。

> 【给用户的提示】本报告 **⛔ 具体值 0 处** ⇒ 拷给顾问时**无需额外屏蔽**。

---

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡（C3-009 / v0.333 第七补）────┐
│ 版本   count 391（开工）→ 394（收口 · 3 组提交）        │
│ 轮次   v0.333 ｜ 指令 云内核-C3-009 ｜ 下一轮 C3-010    │
│ 阶段   ROADMAP 口径·阶段二（可引导 ISO）·子任务 2.4     │
│ CI     run completed/success；三 job 全 success（含 CQ）│
│        ★ step「机制 31 对话链可见性判据」= completed/success│
│ 判据   16/16 PASS（套件口径 16）                         │
│      只告警类：链可见性 3／编号差集 3／其余 0            │
│ ★证伪  step 级＝已证（在跑）；CI 告警数＝未证（日志 403） │
│ ★调研  端口号敏感性调研稿已出（6 维度 · 6 选项 · 无结论）│
│ 决策   见 ⑪（3 项）                                      │
└─────────────────────────────────────────────────────────┘
```

## ③ 任务（含基准／定位／优先级）

| 项 | 内容 |
|---|---|
| **指令基准** | `count = 【自动生成并核实】`（预生成值 391）⇒ **实测 391**（＝上轮收口值） |
| **总规划定位** | **两口径并存**：README 四阶段（产品视角）× ROADMAP 三阶段（工程视角）；**引用阶段号必写明口径** |
| **任务类型** | **放行 ＋ 调研（单一主类型，辅以放行）** |
| **优先级** | ① push 放行 ＋ 等 CI **P0** ｜ ② 判据证伪测试 **P0** ｜ ③ 端口号敏感性调研 **P0** ｜ ④ ②-b／③ 长期 🟡 处置 **P1** ｜ ⑤ "不确定先调研"原则登记 **P1** |

**★ 本轮读了底图／权威件哪几份、哪几行**

| # | 文件 | 行／范围 | 读它做什么 |
|---|---|---|---|
| 1 | `coordination/security/chain_visibility.md` | §一 登记表／§六（判据落地 ＋ 起点） | 判据证伪的对照基准 ＋ 新增 §6.4 |
| 2 | `coordination/security/leak_baseline.txt` | 全文（含 §换壳说明 与 5 条保留行） | ★ **查明"内网地址类暴露"已有基线、口径＝历史留痕不追改** |
| 3 | `coordination/tools/check_private_pointers.py` | 行 200／297–308（扫描范围） | ★ **查明其扫 `coordination/ docs/ deploy/`，`reports/` 明确不在范围** |
| 4 | `npb-gateway/src/main.rs` · `http.rs` | 全文（**只读**） | 威胁模型关键事实：默认绑定回环 ＋ 端口可参数化 |
| 5 | `docs/FULL_AUDIT_REPORT.md` | **只读抽检**（不列值） | ★ 查明仓库含真实内网地址／主机名／地址:端口组合 |
| 6 | `docs/LAYER_BASEMAP_L0_L6.md` | 612–640（`D16'` ②-b／③ 块） | ④ 登记的指向目标 |
| 7 | `coordination/TEMPLATES.md` | §7.2（`T-060`–`T-066`）＋ 体例注行 | 追加 `T-067`／`T-068` |
| 8 | `.github/workflows/ci.yml` | 判据 step 区段 | 核对判据 step 名与状态 |
| 9 | `coordination/BASELINE.md` | §七十五 ＋ 末节标题 | 承接 ＋ 受检件口径 |

## ④ 状态

**状态**：**完成**（①–⑤ 全部达成；★ **②为"半达成"** —— 详见 ⑤-2 与本报告 §证伪测试；3 项待裁）

## ⑤ 验收对照（9 条 · 逐条实测）

| # | 验收标准（指令原文要点） | 结论 | 实测 |
|---|---|---|---|
| 1 | push 完成，CI 三 job 全绿；**判据首次进 CI** | ✅ | ★ **首次 push 静默失败**（无输出、`origin/main` 未动）⇒ 复核指针后重跑成功（`f19faff..b14087b`，exit 0）；run **completed/success**；三 job 全 `success`（含 **CQ**）；★ **step 列表实测含「机制 31 对话链可见性判据」＝ `completed`/`success`** |
| 2 | **证伪测试**：放假 ⛔ 值 → **CI 日志出现告警** → CI 仍绿；测试文件已清理 | ⚠️ **半达成** | ★ **"判据真在 CI 里跑"＝已证（step 级）**；★ **"CI 日志出现告警数"＝未证** —— **job 日志接口未认证返回 `403`**；★ 且 **step `success` 只代表 rc 双零** ⇒ **告警 0 与告警 3 状态相同**（`R83` 同族）。★ **替代证据**：**本地同源假值实跑 ⇒ 告警 2 条**（假端口 ＋ 假 token），**临时件在仓库外、用后即删（已核验不存在）**。★ **未在仓库内放测试件**（理由见 ⑦-①） |
| 3 | 端口号敏感性调研稿出（6 维度 · 选项 · 依据 · 影响 · **不给结论**） | ✅ | `discussions/2026-09-24_端口号敏感性调研稿.md`（**六维度事实 ＋ 六选项 ＋ 依据/影响/代价**，**无结论**）；★ 该稿**已过本判据 ⇒ 告警 0** |
| 4 | ②-b／③ 长期 🟡 登记"已知·已裁定接受" | ✅ | 新建 `security/known_accepted.md`（**唯一权威**）＋ 登记 `A-001`／`A-002`（含裁定来源、理由、**除名条件**、**只增行**纪律）⇒ `T-068` |
| 5 | "不确定先调研"原则登记完成 | ✅ | 写入内容层 `ironlaws.md §十一` ＋ **规范条文 `T-067`**（含适用范围四类、执行四步、与 `C11`／`C10` 的关系） |
| 6 | 引用编号全部带登记状态 | ✅ | `T-067`／`T-068`（**已登记**）；`check_id_set_diff` **`MECH` 告警 0** |
| 7 | `C15` 复算通过，编号表连续无缺号无重号 | ✅ | **T 66 → 68（+2）**；其余 ±0｜★ **`TEMPLATES` 体例注行一度被我误删**，当场发现并补回（见 ⑦-②） |
| 8 | 判据 16/16 PASS | ✅ | `run_all_checks.py` rc=0，**16/16**（套件口径已为 16） |
| 9 | 下一次返回第 0 行含 9 步阅读指令 | ✅ | 本件第 1 行起即 9 步指令 |

## ⑥ 未完成项

| # | 项 | 说明 |
|---|---|---|
| 1 | **"CI 里告警数"仍不可读** | ★ **渠道不存在**（日志 `403`）；**修法＝让 step 用 `::warning::` 输出计数**（进入 `annotations` ⇒ 未认证可读）⇒ **待裁**（⑪-①） |
| 2 | **端口号是否属 ⛔ —— 等发起人判断** | 依 `T-067`，本稿**只出事实与选项**（⑪-②） |
| 3 | **判据规则 A 的"本机绝对路径"覆盖不全** | ★ **本轮证伪测试意外暴露**：形如 `盘符:\FAKE\…`（**不含常见用户目录段**）**不告警**；只有含 `Users/Documents/Desktop/AppData/Windows` 的盘符路径才告警 ⇒ **系规则 A 的口径缺口**（⑪-③） |
| 4 | **两判据形态覆盖有缝** | `reports/` 里的内网地址／主机名、`docs/` 里的端口，**两侧都不在净检查区**（调研稿 §七-2） |

## ⑦ 真实缺陷（本轮自捕获 3 条）

| # | 缺陷 | 严重度 | 处置 |
|---|---|---|---|
| ① | ★★ **`C3-009` §二.2 与 禁区⑥ 直接冲突**：前者要求"把测试件**推送到远端**"，后者要求"**除第一节外不 push**" | **P2** | **按禁区⑥ 处理**（**未推送**）＋ ★ **并查明"推送也买不到可测结果"**：即便推送，**CI 告警数仍在 `403` 的日志里读不到** ⇒ **推送只会在 PUBLIC 仓库历史里留下一个人造件**。**改以"step 级存在性 ＋ 本地同源假值实跑"双证据替代**，并把冲突**如实登记**（⑪-①） |
| ② | ★ **`TEMPLATES` §7.2 的体例注行被我误删**（与上轮 `T-064` 误删**同型**：`Edit` 以"锚定行"为 `old_string`、`new_string` 未回写该行） | **P2** | **当场 `grep -c` 复查发现该行为 0 ⇒ 立即补回**（并连带保住其下"旧口径留痕"行的指代关系）。★ 教训：**凡以某行为锚点插入内容，`new_string` 必须把被锚定的原行一并回写**（→ 已并入 `ironlaws` 铁律④） |
| ③ | ★ **`check_chain_visibility` 的"本机绝对路径"规则覆盖不全** | **P2** | **如实登记，本轮不改规则**（**无授权**；且规则变更会使其"首跑基线"失去可比性）⇒ ⑪-③ |

## ⑧ 三量台账（**四量**）

| 量 | 本轮内容 |
|---|---|
| **存量** | 机制 31 ／ C 21 ／ T 66（→68）／ TERM 14 ／ tools 判据 16 ／ `ci.yml` step 59 ／ 底图 44 份 ／ reports 181 ／ `BASELINE` 顶层节 114 |
| **变量** | ① **T +2** ② reports **+1** ③ `BASELINE` 顶层节 **+1** ④ 新增 `security/known_accepted.md` 与 `discussions/…端口号敏感性调研稿.md`（**非判据计数项**） |
| **补充增量** | ① **`T-067`**「不确定先调研」原则（内容层 ＋ 条文）② **`T-068`** ＋ **`security/known_accepted.md`**（已知·已裁定接受清单）③ **端口号敏感性调研稿**（6 维度 ＋ 6 选项 ＋ 无结论）④ **`chain_visibility.md §6.4`**（判据 CI 可证伪性实测：step 级已证／告警数未证／`annotations` 为可用通道）⑤ **本地假值证伪**（2 条）⑥ **本报告** ⑦ 台账 **§七十六** |
| **创新增量** | ★★ **"判据可用"的判据链被补全**：本轮把"判据已立"细分为**三层可证伪要求** —— **(i) 进了 CI 吗（step 存在性）→ (ii) 跑了吗（step 状态）→ (iii) 抓到了吗（告警数）**，并**实测出 (iii) 在 CI 侧无通道**（`R83` 同族：**"空转"与"通过"取同值**）。★ **方法论增量（可复用）**：**"告警数不可读"本身就是判据的缺陷** —— 只告警类判据若只在 stdout 打印计数，则**在 CI 上等价于没有计数**；**`::warning::` → `annotations` 是一条未认证可读的现成通道**（本轮实测）。★ **调研增量**：**"为什么敏感"这个问题被拆成了两个不同的问题** —— 文档**提及**端口 vs 主机**开放**端口（前者不产生可达性）；且**业界对"端口号是否需脱敏"并无统一口径，而对"真实 IP／主机名"口径明确**（必须替换）。 |

### 8.1 `C15` 双向计数（同脚本同口径）

| 项 | PRE（`HEAD`＝`b14087b`） | FINAL | 差值 | 预期 |
|---|---|---|---|---|
| count | 391 | **394** | **+3** | ✅ 3 组提交 |
| 机制数（表口径） | 31 | 31 | 0 | ✅ |
| C 数 | 21 | 21 | 0 | ✅ |
| T 数 | **66** | **68** | **+2** | ✅ `T-067`／`T-068` |
| TERM 数 | 14 | 14 | 0 | ✅ |
| tools 判据数 | 16 | 16 | 0 | ✅ **本轮未改判据** |
| `ci.yml` step 数 | 59 | 59 | 0 | ✅ **本轮未改 CI** |
| 底图 .md 份数 | 44 | 44 | **0** | ✅ **本轮未改底图** |
| reports 文件数 | 181 | **182** | **+1** | ✅ 本报告 |
| `BASELINE` 顶层节 | 114 | **115** | **+1** | ✅ §七十六 |

## ⑨ 边界说明（四类 · 齐全）

- **本地跑过（M）**：★ **判据证伪（本地同源，假值 2 条）**｜判据全跑 **16/16**｜`C15`｜只读取证（代码 + `grep` + CI 接口探测）。★ **本轮未改代码／未改判据／未改 CI／未改底图** ⇒ **未跑 `cargo build/test`**（非省略）。
- **CI 过（CB／CQ）**：★ **本轮有** —— run **completed/success**；三 job 全 `success`（含 **CQ：QEMU 真实引导**）；★ **step 级实测：判据 step 存在且 `completed`/`success`**。按 `C20`：**run 号只记日报、不入库**。
- **真实跑过（H）**：**无**。
- **未验证**：★ **CI 里的告警数**（通道缺失，`403`）｜★ **端口号是否敏感**（等裁定）｜★ 规则 A 的盘符路径覆盖缺口未修｜★ 两判据形态覆盖缝未补｜★ 调研稿的"条件性可利用"为**推理**、未做攻击面实测。

## ⑩ 决策评估（七要素）

| 要素 | 内容 |
|---|---|
| **评估** | 五项达成（②为半达成，**原因在外部通道而非执行**）；★ **本轮把"判据有效性"从一句声明拆成了三层可证伪要求，并实测出 (iii) 缺通道** |
| **优势** | ① **step 级已证**（判据在 CI 中确实被跑到，非"接了线没生效"）② **发现了一条未认证可读通道**（`annotations`）③ **调研稿给了 6 个选项且不下结论**（`T-067` 首个实例）④ **"已知·已裁定接受"清单把"没人管"与"已明确接受"分开**，且**不阻断判据** ⑤ **本轮未改判据／CI／底图** ⇒ 口径未被顺手改动 |
| **劣势** | ① **CI 告警数仍不可读**（下轮须先修通道）② **规则 A 覆盖缺口未修**（刻意不擅改）③ 调研稿**未做攻击面实测**（限于 `C10`） |
| **风险** | ① **"step success＝判据没问题"被当成结论** ⇒ 正是 `R83` 家族（**已在 §6.4 与本报告双处明写"success 不能证明抓到了东西"**）② **端口号口径悬空** ⇒ 3 条告警**每轮重复出现**（可按 `T-068` 同法建基线，但**未经裁定不擅自做**）③ **"reports 里的内网地址"无人净检** |
| **机会** | ① 若下轮给 step 加 `::warning::` ⇒ **CI 告警数变成未认证可读** ⇒ 证伪测试**可完整闭环并可复现**（**可被证伪**：故意放一个假值 ⇒ `annotations` 应出现计数）② 若端口号口径裁定为"非默认口才敏感"或"端口+IP 才敏感" ⇒ **告警可归零而不牺牲严格度**（**从"改判据"变成"改定义"**）③ 若把"内网地址／主机名"补入净检区 ⇒ **覆盖缝被补上** |
| **建议** | ① 下一轮第一节放行本轮改动（`P0`）② **先修"CI 告警数可读"通道**（`::warning::`，`P0`）⇒ 再把证伪测试完整重跑一次 ③ 裁端口号口径（`P0`，本稿已备齐事实）④ 裁规则 A 盘符路径缺口（`P1`） |
| **等待决策** | 见 ⑪（3 项） |

## ⑪ 需要用户决策（3 项）

| # | 待裁项 | 选项 | 推荐（★ 依 `C11` 给建议，但**不代裁**） |
|---|---|---|---|
| ① | **`§二.2 推送到远端` 与 `禁区⑥ 不另行 push` 冲突；且 CI 告警数不可读** | (甲) 下轮**先修通道**（step 用 `::warning::` 输出计数）⇒ 再完整重跑证伪 (乙) 下轮**推送一个假值测试件**（按 §二.2 字面） (丙) 接受现状（以 step 级为证伪终点） | **甲** —— ★ **先例：即使推送也读不到日志（`403`）** ⇒ **甲 是唯一能让证伪"可复现"的路**；且**不必在 PUBLIC 仓库留人造件** |
| ② | **端口号是否属 ⛔**（本稿已备齐 6 维度事实 ＋ 6 选项） | (甲) 维持 (乙) 只告警非默认口 (丙) 只告警"端口＋可定位目标" (丁) **移出 ⛔、另补"真实内网地址／主机名"入 ⛔** (戊) 维持定义但为 3 处建"只减不增"基线 (己) 其它 | **不代答** —— 依 `T-067`：**本稿只出事实与选项**；★ 附**决策相关事实**：**我方仓库同时存在"默认口提及"与"真实内网地址＋主机名"两类**，后者**已在基线内、口径＝历史留痕不追改** |
| ③ | **`check_chain_visibility` 规则 A 的"本机绝对路径"是否放宽**（现只认含 `Users/Documents/Desktop/AppData/Windows` 的盘符路径） | (甲) 放宽为"**任意盘符绝对路径**" (乙) 维持（宁少报，防误报） | **甲（但须同批加"自述剔除"）** —— ★ 取舍：放宽后**会命中"文档里讲路径格式"的片段** ⇒ 需配排除规则（`T-041`）；**本轮不擅改**（无授权） |

## ⑫ 下一步建议

1. **下一轮第一节放行本轮全部改动**（`P0`，固定首项）。
2. **修"CI 告警数可读"通道**（`P0`）⇒ 完整重跑证伪测试 ⇒ 本项从"半达成"转"达成"。
3. **裁 ⑪-② 端口号口径**（`P0`；本稿已备齐）。
4. **下一轮基准 `count`＝394**。

## ⑬ 启动回执

> **已读 `coordination/CHARTER.md`**（§三 机制表 1–31）。
> **当前状态**：`count = 391`（开工）→ **394**（收口）｜阶段口径＝**README 四阶段 / ROADMAP 三阶段 并存**。
> **指令序号**：`云内核-C3-009` ｜ **下一轮** `云内核-C3-010` ｜ **下一轮基准 `count`＝394**。
> **本次任务**：push 放行 ＋ 判据证伪 ＋ 端口号调研 ＋ 长期 🟡 处置 ＋ 调研先行原则登记。
> **是否继续**：**执行完毕**。

---

## ★ 证伪测试结果（**脱敏** · 逐项附【本地跑过／CI过／真实跑过／未验证】）

| 步 | 问 | 结果 | 证据 | 归属 |
|---|---|---|---|---|
| 1 | **判据进了 CI 吗？** | ✅ **是** | `test` job 的 step 列表含「**机制 31 对话链可见性判据 —— `T-063`（只告警 · v0.333 新增）**」 | **CI 过（CB）** |
| 2 | **跑了吗？** | ✅ **跑了**（`completed` / `success`） | 同上 step 状态 | **CI 过（CB）** |
| 3 | **CI 日志里出现告警了吗？** | ⚠️ **读不到**（**接口 `403`**） | 三 job 日志接口**全部 `HTTPError 403`**（未认证不可读） | **未验证** |
| 4 | **CI 里能读到告警数的通道存在吗？** | ✅ **存在一条**（未认证可读） | `check-runs/{id}/annotations` —— 本轮实测三 check-run 各返回 **2–3 条 annotation** | **本地跑过（M）** |
| 5 | **CI 仍绿吗？** | ✅ **绿** | run `completed`/`success`；三 job 全 `success` ⇒ `continue-on-error` 生效 | **CI 过（CB）** |
| 6 | **本地假值实跑告警几条？** | ✅ **2 条**（**假端口** ＋ **假 token**） | 临时件置于**仓库外**，`--files` 定向扫描 ⇒ `告警合计：2` | **本地跑过（M）** |
| 7 | **测试文件清理了吗？** | ✅ **已清理**（目录不存在） | 脚本内 `rm -rf` 后判定"目录不存在" | **本地跑过（M）** |
| 8 | ★ **意外发现** | ⚠️ 假值件里**形如 `盘符:\FAKE\…`（不含常见用户目录段）** 的那一行**未被告警** | 同一次实跑中该行无命中 ⇒ **规则 A 的盘符路径口径缺口**（⑦-③） | **本地跑过（M）** |

> ★ **口径（防拔高）**：
> **(i) 已证**：判据在 CI 中**确实被执行**（step 级：存在 ＋ `completed` ＋ `success`）⇒ **"接了线没生效"被排除**。
> **(ii) 未证**：**CI 中的告警数** —— **通道不存在**（日志 `403`）；且 **step 的 `success` 只能证明 rc 双零**，**不能区分"告警 0"与"告警 3"**（`R83` 同族）⇒ ★ **不得据 `success` 宣称"判据抓到了东西"**。
> **(iii) 替代证据**：**本地同源假值实跑 ⇐ 告警 2**（**判据的规则确实会告警**，且**用的是同一判据文件**）。
> **(iv) 冲突如实登记**：指令 §二.2「推送到远端」与 禁区⑥「除第一节外不 push」**直接冲突** ⇒ 本轮**按禁区⑥ 处理**（**未推送**），并把 **"推送也买不到可测结果"** 一并登记（⑦-①）。

---

## ★ 端口号敏感性调研稿（**路径 ＋ 摘要**）

> **路径**：`coordination/discussions/2026-09-24_端口号敏感性调研稿.md`（**只出稿 · 不预设结论**；★ **全稿不列任何具体值** ⇒ **已过本判据、告警 0**）

**摘要（六维度 · 均为"事实"，**无结论**）**：

| 维度 | 关键事实（摘要） |
|---|---|
| **零 · 前提** | ★ **须区分两件事**：**文档里"出现"端口号（甲）** ≠ **某主机"开放"端口（乙）**；**端口扫描读的是 (乙)，扫描器不会去读你的 README** |
| **一 · 技术** | 扫描是攻击链侦察第一步；(乙) 成立后可 banner/版本扫描比 CVE；**真实案例全为"服务被暴露"型**（SMB/RDP/Redis/MongoDB 等）；全端口扫描是常规动作 ⇒ 改非默认口**只减噪声、不减被扫概率** |
| **二 · 类型** | 业界风险标注针对"**暴露（expose）**"而非"**提及（mention）**"；默认口常标 High if exposed、alternate 口"Expose with care"、非默认口 Medium；**内网 vs 公网**才是拓扑级差别 |
| **三 · 仓库** | PUBLIC ⇒ 历史永久、机器人分钟级扫、GitGuardian 2024 数据（1,280 万条／每日 3.5 万提交／中位 94 天）；业界三分类含 **"基础设施情报（主机名／IP／内部端口）"**；★ **业界"常见误报"清单不含端口号，但明确要求替换"真实 IP／内网主机名"** ⇒ **业界对端口号无统一口径** |
| **四 · 行业（3 例）** | ① 公开示例项目 README **明写默认口**；② 主流框架**官方文档公开默认口**与配置项；③ 公共 loopback 指南站**逐条公开"默认口↔框架"映射** ⇒ **默认口＝公共常识**；★ 业界**更看重"绑定地址（回环 vs 全网）"** |
| **五 · 我方威胁模型（R20）** | 网关**默认绑回环**（外部不可达）、端口可参数化、**仓库无公网 IP**；★ 但 `docs/FULL_AUDIT_REPORT.md` **含真实内网地址（13）／用户名（3）／本机路径（3）＋ 主机名 ＋ 地址:端口组合**，**已在 `leak_baseline.txt` 基线内、口径＝历史留痕不追改**；★ 判据覆盖有缝（`reports/` 内网地址、`docs/` 端口两侧均非净检区） |
| **六 · 判据** | 列 **6 个选项**（甲维持／乙只告警非默认口／丙只告警"端口＋可定位目标"／丁移出 ⛔ 并补强形态／戊建只减不增基线／己其它）**逐项给依据·影响·代价**，★ **不评价优劣、不推荐** |

---

## 接续包数据块（**增量** · 置于尾部、`KS_` 伴行之前）

> **数据层（WorkBuddy 填）** ｜ **顾问层（16–18 项）由顾问填**。

**0 元信息**：2026-09-24｜触发＝指令指定（〇之三）｜链号 **`C3`**｜**增量**。
**1 项目身份**：云内核（元内核）项目 · 电信号版；共生而非替代；终态＝可安装 OS 形态；不替代用户原系统、内核不主动夺胎。
**2 三方关系**：决策人＝用户｜执行者＝WorkBuddy｜顾问＝**外部专家（`C3`）**｜指令格式＝**十必带**（`T-060`）＋ 保密与可见性块 ＋ ★ **新增「不确定先调研」**（`T-067`，试用中）。
**3 当前位置**：阶段＝ROADMAP 口径·阶段二·子任务 2.4｜`count`＝**391 → 394**｜轮次 v0.333｜未推送＝**本轮尾巴 3 组本地提交**（禁区⑥）。
**4 全景状态卡（带证据）**：L1–L5 ✅M｜L6①✅M｜**L6② 🚧 路径 A 已通且稳定 3/3（M）· 路径 B ❌ 暂不立项**｜**L7 T1 ✅（受 CI 覆盖）**｜L7 T2·T3 ❌｜2.4 边界层 ✅**CQ**｜效力列 🟡 **0**｜机制 31 **✅ 生效 ＋ 判据已立（step 级已在 CI 跑到；告警数通道缺失）**。
**5 命名族**：见 `docs/LAYER_DETAILS.md §3.2.1`。
**6 规则与机制**：机制 **31**｜C **1–21**｜判据 **16**（入 CI **12**；仅本机 **4**）｜报告十四项｜能力矩阵 **M／CB／CQ／H**｜`C12` ＋ `T-060`｜七要素｜★ **`T-067`「不确定先调研」**（试用中，起点 v0.333）＋ **`T-068` 已知·已裁定接受清单**。
**7 三量台账**：见 §⑧（四量齐）。
**8 最近裁定（5 条）**：①判据证伪＝**step 级已证、告警数未证**（通道缺失）②②-b／③ 记 **「已知·已裁定接受」**（裁决甲：不改判据不移标记）③**「不确定先调研」立为条文**（`T-067`）④端口号敏感性＝**只出调研稿、等裁定**⑤规则 A 盘符路径缺口＝**登记不擅改**。
**9 待办**：见 ⑫（4 项）。
**10 待裁定**：见 ⑪（3 项）。
**11 风险**：`R-A` "step success＝判据没问题"被当结论（P2）｜`R-B` 端口号口径悬空 ⇒ 告警每轮重复（P2）｜`R-C` **`reports/` 里的内网地址无净检**（P2）｜`R-D` CI 告警数不可读 ⇒ 证伪无法闭环（P1，下轮首项）｜`R-E` 规则 A 盘符路径漏报（P3）。
**12 权威文件路径**：底图＝根 `README.md` ＋ `docs/`｜治理＝`coordination/`｜链可见性登记＝`coordination/security/chain_visibility.md`（**§6.4 ＝ CI 可证伪性**）｜**已知接受清单＝`coordination/security/known_accepted.md`**。
**13 边界说明**：见 ⑨（四类齐）。
**14 指令序号机制**：链号 `C3`｜本轮 `云内核-C3-009`｜下一轮 `云内核-C3-010`｜下一轮基准 `count`＝**394**。
**15 顾问接手须知**：阅读指令 9 步（第 0 行）｜出件＝**十必带**｜★ **脑补清单**＝①四步 vs 五步未归一 ②L8–L10 已冻结 ③"接管级 L0–L4"与架构同号不同义 ④六波罗蜜无来源｜★ **已知缺口**＝路径 B 暂不立项／②-b·③ 长期 🟡（已登记为"已知接受"）／"图↔控制机制↔代码"仍缺控制机制列／L4·L5 控制律无编号／**判据守不住"对话件"**／**CI 告警数无通道**／**规则 A 盘符路径覆盖缺口**。

**16 当前最需要处理的三件事**：（**顾问填**）
**17 顾问审核结论**：（**顾问填**）
**18 下一轮建议**：（**顾问填**）

---

### INTEL 状态表（依 `T-042` · 置于十四项之后）

| 编号 | 来源 | 状态 | 本轮触发否 | 处置 |
|---|---|---|---|---|
| `INTEL-001` | 外部信息（观察中） | 观察中 | **未触发**（触发＝企业注册完成 ＋ 阶段三启动；均未发生） | 维持观察中；**不为"好看"硬触** |
| `INTEL-002` | 外部信息（观察中） | 观察中 | **未触发** | 同上 |

> **J1** ✅｜**J2** 两条均未触发 ⇒ 不违规｜**J3／J4** 无待准备／准备中项 ⇒ 不违规。

### 停点报告

> **本轮指令未设"中途停点"** ⇒ 无停点报告。★ 如顾问需要，可按〇之二 8 项格式补出。

### 告警行（`B2` ＋ `C1`）

- **`B2`（本机判据告警）**：**16/16 PASS**｜只告警类：**链可见性 3**（**未变**：仍为那两份同日报告的默认口提及）｜编号差集 **3**（`C22`／`T-00`／`TERM-999`，**历史元引用**，未新增）｜效力列 **0**｜报告结构 **0**｜其余 **0**。
- **`C1`（CI 告警）**：**无** —— run `completed/success`，三 job 全 `success`（按 `C20` run 号只记日报）。

### 台账内联（依 §54.5）

> **本轮报告名**：`coordination/reports/2026-09-24_v0.333第七补_判据CI可证伪与端口号敏感性调研.md`
> **对应台账节**：`coordination/BASELINE.md` **§七十六**。
