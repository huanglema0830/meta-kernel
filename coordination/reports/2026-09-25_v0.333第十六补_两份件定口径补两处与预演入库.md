【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

> **版本口径（★ 依 `T-045`：报告内版本号 ＝ 报告「首次提交」时的 `count`）**：本报告正文内的 **`429`** 为**首次提交时点值**，**后续订正提交不回改此处**。
> **后续任一轮若要取本轮「最终」的 `count`**：请以 `git rev-list --count <本轮最后一个 commit>` **实跑为准**。

# v0.333 第十六补轮 · **中断确认** ＋ **push 全量** ＋ **23:00 首验（明确跳过）** ＋ **两份件定口径补两处** ＋ **预演产物 apply** ＋ **两项到期处置**

> **指令序号**：`云内核-C3-018`（下一轮 `云内核-C3-019`）
> **轮次**：`v0.333`（两口径并存：版本＝`count`）｜**版本 `count = 428`（开工）→ `429`（g1 提交后实时值）**
> **任务类型**：**放行 ＋ 首验 ＋ 阶段性目标推进**（单一主类型，辅以放行）
> **本轮报告落点**：`coordination/reports/2026-09-25_v0.333第十六补_两份件定口径补两处与预演入库.md`

## ★ 执行模式回执（`T-075` · 第 6 个样本 · 试用已延至 `C3-022`）

- **本轮执行模式**：**正常**（用户在场指令会话；commit 前缀＝`v0.xxx`）
- **本轮任务逐项标注**：① 中断确认＋push 全量＝**正常** ② 23:00 首验＝**正常**（**明确跳过**，非待核）③ 两份件定口径＋补两处＝**正常**（产出＝**草稿待确认**）④ 预演产物 apply＝**正常** ⑤ `T-079` 延长试用＝**正常** ⑥ `T-077` 维持 10＝**正常** ⑦ 回执第 6 样本＝**正常**（自动）
- **夜跑产物（草稿待确认）**：`coordination/discussions/2026-09-25_两份件完整度口径.md`（**94 行**）
- **自动化产物**：**无**
- **是否改仓库状态**：**是** —— 改了 `TEMPLATES.md`（`T-086`／`T-087` ＋ `T-077`／`T-079` 行末追加）／两份件（**追加节**）／新增 `tools/README.md`／新增口径稿／新增台账 §八十五；**确认方式＝用户在场指令授权**
- **另有一类"改远端状态"动作**：`push` **2 个**（开工首项全量）—— **已由用户 §一 放行**

---

## ① 保密标记（`C12`）

- **本链**：`C3` ｜ **接收方＝外部顾问** ｜ **可见级别上限 🔒 半公开**
- **本轮 ⛔ 清单（六类）**：本机绝对路径 ／ 账号·口令 ／ 内网 IP ／ 端口＋可定位目标 ／ 密钥与 token ／ 二进制路径与体积
- **拷前屏蔽提示**：本报告**无**绝对路径（补丁位置只写"仓库根同级 `patches/nightly-p3/<日期>/`"）／**无**端口＋可定位目标／**无**内网 IP／**无**密钥具值。
- **本报告自检**：以上六类均 **0 命中**；`check_chain_visibility` 实跑＝**告警 0**。

## ② 全景状态卡

| 字段 | 值 |
|---|---|
| **版本 `count`** | **428**（开工，与预生成值**一致**）→ **429**（g1 提交后） |
| **HEAD** | `c5a1ec7`（开工）→ **`803e908`**（g1 提交后） |
| **`origin/main`** | **`2632c58` → `c5a1ec7`**（本轮 **1 次 push**，**2 个**）；**回读一致** |
| **未推送 commit** | **1 个**（本轮 g1）；按 `T-081` 口径②「本轮内不再 push」⇒ **留待 `C3-019` 开工首项** |
| **CI** | **三 job 全绿**（test ／ 元内核引导镜像 ／ 宿主 field-render），`status=completed`、`conclusion=success`；★ **run 号依 `C20` 只记记忆日志、不入库** |
| **阶段定位（两口径并存）** | **README 四阶段**（能力刻度）／**ROADMAP 三阶段**（交付刻度）—— **本轮未推进任何阶段刻度**，属**放行/首验/阶段性目标推进**层动作 |
| **待决策项** | **两份件完整度口径待确认**（口径稿 §五 三问）｜**其余 10 项缺口何时排**｜`T-079` 新再评点 ＝ `C3-022` |

## ③ 任务（含**基准**与**总规划定位**，优先级）

- **基准**：`count = 428`（先读 `BASELINE.md` 自动核实，**与预生成值一致**）。
- **总规划定位**：**两口径并存** —— README 四阶段（能力刻度）／ROADMAP 三阶段（交付刻度）；本轮**不触及阶段刻度**。
- **优先级**：**P0①** push 全量 ＋ 等 CI｜**P0②** 23:00 首验（真实样本或明确跳过）｜**P0③** 两份件定口径＋优先补两处｜**P1④** 预演产物 apply 入库｜**P1⑤** `T-079` 延长试用｜**P1⑥** `T-077` 维持 10｜**P1⑦** 回执第 6 样本。

## ④ 状态（**完成** —— 七项全部完成）

| # | 任务 | 状态 | 一句话实测 |
|---|---|---|---|
| ① | 中断确认 ＋ push | ✅ **完成** | **无残留**（四类实测）；实测未推送 ＝ **2** ⇒ `2632c58..c5a1ec7`；回读一致；**三 job `success`** |
| ② | 23:00 首验 | ✅ **完成（跳过）** | 开工 **11:42** ⇒ **仍在 23:00 之前** ⇒ 按 §2.1 **明确跳过、不计未完成**；**不重复登记"待核"**；排入 `C3-019` 第一项 |
| ③ | 两份件口径＋补两处 | ✅ **完成** | 口径稿 **94 行**；补① **573→611 行**；补② **374→417 行**；**两件原文一字未改** |
| ④ | 预演产物 apply | ✅ **完成** | `coordination/tools/README.md`（**67 行**，新增）；判据复跑 **16/16** |
| ⑤ | `T-079` 延长试用 | ✅ **完成** | 登记行末追加「延长试用至 `C3-022`」（**只增不改**） |
| ⑥ | `T-077` 维持 10 | ✅ **完成** | 登记行末追加「维持 10」（**只增不改**）；现值实测 **10** 未变 |
| ⑦ | 回执第 6 样本 | ✅ **完成** | 已填；逐项标注 7 项全中 |

## ⑤ 验收对照（标准 vs 实测，逐条）

| # | 验收标准 | 实测 | 判定 |
|---|---|---|---|
| 1 | 中断状态确认完成 | 四类实测 ⇒ **无残留**（补丁目录 2 文件＝**待 apply 产物**，非残留，本轮已处置） | ✅ |
| 2 | push 完成，CI 三 job 全绿；`origin/main` 已回读 | 实测未推送 **2** ⇒ `2632c58..c5a1ec7`；三 job `success`；回读一致 | ✅ |
| 3 | 23:00 首验：真实样本核验**或**明确跳过 | **明确跳过**（开工 11:42 早于 23:00）＋ 排期写入 `C3-019` 第一项 | ✅ |
| 4 | 《两份件完整度口径》稿出；优先补两处已补入 | 口径稿出；补①（规格映射层）＋补②（机制 31／32）**均已追加** | ✅ |
| 5 | 预演产物 apply 入库完成 | `coordination/tools/README.md` 新增 67 行；**调整仅"性质"一处** | ✅ |
| 6 | `T-079` 延长试用至 `C3-022` 已登记 | 登记行末追加说明（实测含"延长试用至"＝True） | ✅ |
| 7 | `T-077` 维持 10 已登记 | 登记行末追加说明（实测含"维持 10"＝True） | ✅ |
| 8 | 引用编号全部带登记状态 | **`T-086`／`T-087`** 先登记后引用；`C15`：`T-001`–`T-087` **无缺号无重号** | ✅ |
| 9 | `C15` 复算通过；判据 16/16 PASS | `T` 85→87｜`discussions` 30→31｜`reports` 193→193｜`tools` 16→16｜顶层节 123→**124**；**16/16 PASS**；`doc_consistency` PASS | ✅ |
| 10 | 下一次返回第 0 行含 9 步阅读指令；返回头部含「执行模式回执」 | 本报告第 0 行＝9 步原文；头部含「★ 执行模式回执」（**第 6 个样本**） | ✅ |

## ⑥ 未完成项

1. **真实定时触发的 23:00 样本** —— **本轮按规则跳过**；**已排入 `C3-019` 第一项**（本轮**不再登记为"待核"**，遵用户 §2.1）。
2. **两份件完整度口径待你确认** —— 口径稿 §五 三问（第 3 要素是否必须带编号／第 6 条"截至时点"是否硬要求／是否建自动抽取通道）。
3. **其余 10 项缺口** —— 判据数同步／`T` 体系独立章／固定相项数／量子层 △／规格映射层的 **L8–L10 断链** 等，**本轮未动**（禁区⑩：只补两处）。
4. **两份件"输出"动作本身** —— 本轮完成"**定口径 ＋ 补两处**"；**正式输出（对外/对顾问成件）尚未做**。

## ⑦ 真实缺陷（**本轮自捕获 2 条**）

| # | 缺陷 | 等级 | 处置 |
|---|---|---|---|
| ① | ★ **补入历史件时的"异体字误伤"风险复现** —— 两份件历史行实测有 **14 处**异体字命中（含日文中点 U+30FB 等） | **P2**（`C19` 风险） | **本轮只扫不改**：经核**全部落在追加节之外**（追加节内 **0 命中**）⇒ **历史行一字未动** ✅（**`C3-016` 教训的正面应用**） |
| ② | ★ **两份件的"老化"是结构性的** —— 件内 11 判据 / 机制 1–30 / 固定相旧数，**权威源随轮增长** | **P2**（口径类） | 在**口径稿 §二 标准 6**提出"**件内标明截至时点**"作为**廉价防误读手段**，并列为**待你确认**项；**未擅自建自动抽取**（涉新增脚本 ⇒ `C10`） |

> ★ **另一条正面记录**：**追加型脚本幂等保护连续两轮生效**（本轮台账追加脚本命中"已存在 §八十五 ⇒ 跳过"）⇒ **未产生重复节**（顶层节 124、重复节号 0）。

## ⑧ 三量台账

| 量 | 本轮值 | 说明 |
|---|---|---|
| **存量** | 推送面 **2 → 0**；**新存量**（本轮 g1／g2，按新口径留待下轮） | 一轮一结 |
| **变量** | `push` **1 次**（2 个）｜`origin/main` `2632c58`→`c5a1ec7`｜**CI 三 job 全绿**｜**两份件 ＋81 行**（573→611／374→417）｜**新增件 ＋2**（`tools/README.md`／口径稿） | 远端对齐；**阶段性目标首次产生实物增量** |
| **补充增量** | 登记 **＋2**（`T-086`／`T-087`）｜`T-077`／`T-079` 行末追加更新｜台账 **§八十五** | 治理类增量（**全部只增不改**） |
| **创新增量** | **"规格映射层"（设计→代码的桥）** —— 把此前"**不能从设计推到代码**"变为**可查**（层→落点→验证通道），且**对无落点的四层显式标注** | **候选**，**不擅自升格**（禁区③） |

## ⑨ 边界说明（四类 · 齐全）

| 类 | 本轮内容 |
|---|---|
| **本地跑过** | 判据 **16/16 PASS**（多次）；四类中断残留检查；`git ls-files` 与目录扫描（**规格映射层的落点全部实测**）；`wc -l` 行数实测（573→611／374→417）；**追加节内异体字 0 命中**（历史行命中隔离核验）；`C15` 双向计数 |
| **CI 过** | **三 job 全部 `completed` ＋ `success`**（`headSha` ＝ `c5a1ec7`）⇒ 远端实测 |
| **真实跑过** | **push 真实执行**（`2632c58..c5a1ec7`）＋ **回读真实** ＋ **CI 三 job 真实完成** ＋ **补入真实落盘**（两份件行数实测增长） |
| **未验证** | ① **真实定时触发的 23:00 行为**（本轮跳过；`C3-019` 第一项）② 补①中 **L8–L10 的"无落点"** 是否等于"未实现"（**本轮只证"未找到落点"**）③ 口径稿标准 1–5 的**长期可维持性**（无自动抽取 ⇒ 仍会漂） |

## 附加表一 · **23:00 首验结果（本轮＝明确跳过）**

| 判定项 | 实测 | 结论 |
|---|---|---|
| 开工时刻 | **2026-09-25 11:42**（本地） | **仍在 09-25 23:00 之前** |
| 适用分支 | 用户 §2.1：**仍在 23:00 之前 ⇒ 走甲·明确跳过，不计未完成** | ✅ **跳过** |
| 是否登记为"待核" | **否**（用户明写"**不再重复登记为『待核』**"） | ✅ 未登记 |
| 排期 | **`C3-019` 第一项** | ✅ 已排 |

> ★ **事实补充**：自 `T-078`（09-25 00:51）落规以来，**连续四轮**（`C3-014`／`C3-015`／`C3-016`／`C3-018`）会话**均在白天**、**均早于下一个 23:00** ⇒ **"窗口未到"属时序必然**；**可核的充分条件＝存在一个"已过 23:00"的时点之后的会话**（最早 **09-26**）。

## 附加表二 · **《两份件完整度口径》稿路径 ＋ 摘要**

- **路径**：`coordination/discussions/2026-09-25_两份件完整度口径.md`（**94 行**）

| 件 | 完成标准（摘要） |
|---|---|
| **① 设计图** | **每层四要素**：① 它是什么（定义）② 给谁供血（输出到哪层）③ 靠什么律在跑（**机制／约束编号**）④ **代码落点在哪**（可查的文件／模块）＋ **外加：规格映射层**（设计→代码的桥）。**判定**：D-完成 ⇔ 四要素齐备 **且** 映射层落点**可核** |
| **② 运行机制** | ① 机制 31／32 全含（**含红线**）② 判据 **16** 全同步（编号／名称／是否入 CI）③ **`T` 登记体系有独立章**（现至 **`T-087`**）④ 固定相项数同步（**十一项**）⑤ C1–C21 全含 **＋ ⑥ 件内标明"截至时点"**（防误读） |

> ★ **口径稿 §五 三问（等你确认）**：第 3 要素是否**必须带编号**／第 6 条"**截至时点**"是否**列为硬要求**（稿中推荐：列为硬要求）/ 是否**建自动抽取通道**（长期不漂，涉新增脚本 ⇒ `C10`）。

## 附加表三 · **优先补两处：补入前后对照**

| 件 | 补入前 | 补入后 | 补入内容 |
|---|---|---|---|
| **① 设计图**（技术与产品 · 详尽版） | **573 行** | **611 行**（**+38**） | **追加「附 · 规格映射层」**：`层 → 实现落点 → 验证通道` 表（L0–L10）；★ **无落点者显式标注** —— **L0（概念层，属正常）／L8／L9／L10 未找到落点**；**L6 对齐子功能未实现**（契约在 `archive/reserved-interfaces/`）＋ 「设计→代码」三步走 ＋ 已知缺口 4 条 |
| **② 运行机制**（管理与运行 · 详尽版） | **374 行** | **417 行**（**+43**） | **追加「第九章 · 机制 31／32 补入」**：机制 31（对话链可见性）＋ **机制 32（一句话／定义逐字／关键特征／★红线／「改仓库状态」口径／执行三步／与相邻机制分工／字段载体／追溯适用／首次实际调整／与 `§11.8` 的关系）** ＋ 9.3「本章未补的到期缺口」3 条 |

> ★ **方式**：**一律追加**（`C19`：**两件原文一字未改**）｜**不涉底图 ⇒ 不走 `D16'`**｜**只补两处**（其余 10 项不动，禁区⑩）。
> ★ **历史保护实测**：两份件历史行有 **14 处**异体字命中，**全部在追加节之外**；**追加节内 0 命中** ⇒ **未动历史** ✅。

## 附加表四 · `INTEL` 状态表（机制 29 动作①）

| 编号 | 状态 | 本轮是否触达 | 处置 |
|---|---|---|---|
| `INTEL-001` | 观察中 | 否（企业注册 ＋ 阶段三 均未达） | **维持，不虚触** |
| `INTEL-002` | 观察中 | 否（L7–L10 未落地） | **维持，不虚触** |

## 附加表五 · **`B2` ＋ `C1` 告警行**（如实连续记录）

| 项 | 当前值 | 说明 |
|---|---|---|
| `B2` 主线推进 | **本轮＝阶段性目标推进（首次产生实物增量）** ：两份件 **+81 行**、新增件 **×2** | ★ **与前五轮不同**：本轮**不再是纯治理停顿**，而是**直接朝"两个内容做输出"前进** |
| `C1`（画布活跃度） | **本轮落盘 6 类改动**（`T-086`／`T-087`／两件追加节／`tools/README.md`／口径稿）＋ 台账 §八十五 ＋ `T-077`／`T-079` 行末更新 | 活跃于**产出面** |

## 附加表六 · 本轮**读了底图哪几份、哪几行**

| 文件 | 读了哪几行 | 用途 |
|---|---|---|
| `coordination/BASELINE.md` | §八十四 全文｜尾部 | 基准核实 ＋ §八十五 续写锚点 |
| `coordination/TEMPLATES.md` | `§7.2` 尾（`T-085` 行）｜`T-077`／`T-079` 行 | 登记与"行末追加"落点 |
| `coordination/reports/…第一部分_技术与产品_详尽版.md` | `§2.0` 完整表（**L114–129**）｜章节全扫 | 补①（规格映射层） |
| `coordination/reports/…第二部分_管理与运行_详尽版.md` | **章节全扫**（八章） | 补②（机制 31／32） |
| `docs/LAYER_ARCHITECTURE.md` | **L37–61**（层 → 一句功能 → 戒律 → **实现落点**） | 规格映射层的**落点权威源** |
| `docs/L7_EXECUTION_DESIGN.md` | `§0.1 实现状态`（模块 → 落点 → 状态） | L7 落点核验 |
| 代码树实测 | `meta-kernel-core/src/`（模块清单）｜`npb/`／`npb-gateway/`／`manifest-ui/`／`host/` 目录 | 落点存在性核验 |
| `coordination/tools/check_*.py` | **16 个脚本头部扫描** | 入库件（`README.md`）内容 |
| `git` 事实面 | `status`／`rev-list`／`ls-files`／`rev-parse origin/main` | 门控、`C15`、行数 |

## 附加表七 · **接续包数据块**（增量 · `CONTINUITY §8`）

```yaml
round: 云内核-C3-018
baseline_count: 428            # 开工实跑，与预生成值一致
head_after_commit: 429         # g1 提交后实时值
origin_main: c5a1ec7           # 本轮 1 次 push（全量 2 个）后回读
pushed: 2                      # 以 rev-list origin/main..HEAD 实测为准（T-085）
unpushed: 1                    # 本轮 g1（g1 时点值）；按 T-081 口径② 留待下轮开工首项
ci: {jobs: 3, status: completed, conclusion: success}   # run 号依 C20 不入库
interrupt_check: 无残留         # 工作区0行 / 台账123节0重复 / 未推送2属正常 / 补丁目录2文件=待apply产物(本轮已处置)
first_check_2300: 明确跳过（开工 11:42 早于 23:00；按 §2.1 甲；不登记待核；排入 C3-019 第一项）
deliverables:
  - coordination/discussions/2026-09-25_两份件完整度口径.md      # 草稿待确认 · 94 行
  - coordination/tools/README.md                                  # 新增 · 67 行（T-087 入库）
  - reports/…第一部分_技术与产品_详尽版.md                         # 追加「附·规格映射层」573→611
  - reports/…第二部分_管理与运行_详尽版.md                         # 追加「第九章 机制31/32」374→417
  - coordination/reports/2026-09-25_v0.333第十六补_两份件定口径补两处与预演入库.md
due_items_done:
  - T-079 → 延长试用至 C3-022（用户裁定乙）
  - T-077 → 维持 10（用户裁定）
pending_user:
  - 两份件完整度口径三问（第3要素是否带编号 / 第6条"截至时点"是否硬要求 / 是否建自动抽取通道）
  - 其余 10 项缺口何时排（判据数 / T体系章 / 固定相项数 / 量子层△ / L8–L10 落点断链）
next_round_first_item: 核验 09-25 23:00 真实定时触发样本
judges: {total: 16, pass: 16, alerts: {chain_visibility: 0, id_set_diff: 3, report_structure: 0, rewrite_pipeline: 0, version_label: 0}}
c15: {T: [85, 87], discussions: [30, 31], base_top_sections: [123, 124], reports: [193, 193], tools: [16, 16]}
```

## ⑩ 决策评估（**七要素** · 本轮新决策＝**完整度口径** 与 **规格映射层**）

| 要素 | 内容 |
|---|---|
| **评估** | 本轮首次把"**两个内容做输出**"从口号变为**可判定的口径 ＋ 实物增量**（两件 **+81 行**、入库件 ×1、口径稿 ×1）；**规格映射层**直接回答"能不能从设计推到代码"（**答案从"不能"变为"可查"**）。 |
| **优势** | ① **口径先行**（先定"什么叫做完"再补）⇒ 避免"补了仍不合口径"；② **只补两处**严格守界；③ 追加式补入 + 历史行隔离核验 ⇒ **`C19` 零冲突**；④ `tools/` **首次有自述件**。 |
| **劣势** | ① 补①暴露 **L8–L10 无落点**（"设计→代码"**仍断三层**）；② 件内其他到期缺口**仍未补**（判据数／`T` 体系章／固定相项数）⇒ **两件仍非"当前口径下的完成态"**。 |
| **风险** | ① 若不设"**截至时点**"标注 ⇒ 读者（含顾问）**易把过时编号当现行**（**机制条文属 🔓 可见面**）；② 无自动抽取 ⇒ **每轮都在制造新的漂移**；③ **`C3-019` 若仍在白天** ⇒ 23:00 样本**第五次顺延**（故本轮已**明确排期为第一项**）。 |
| **机会** | ① 规格映射层可**继续下沉到接口级**（本轮止于**文件/模块级**）；② 若接受自动抽取 ⇒ 两件**从此不再手写漂移**；③ 入库件为后续"**判据侧文档化**"开了头。 |
| **建议** | ① **口径稿三问**中，**第 6 条"截至时点"建议列为硬要求**（唯一廉价防误读手段）；② **L8–L10 落点断链**应在下一批缺口里**优先于"判据数同步"**（前者阻塞"设计→代码"，后者只是数字）；③ **`C3-019` 第一项＝23:00 真实样本**（若仍未到 ⇒ 建议直接把该核验做成"**下个白天会话的第一动作**"，不再逐轮排期）。 |
| **等待决策** | 口径三问｜其余 10 项排期｜自动抽取通道是否建。 |

## ⑪ 需要用户决策

1. **口径三问**：① 第 3 要素（靠什么律）**是否必须带编号**？② 第 6 条"**件内标明截至时点**"**是否列为硬要求**（荐：是）？③ **是否建"自动抽取通道"**（长期不漂，涉新增脚本 ⇒ `C10`）？
2. **缺口排期**：其余 10 项里，**先排哪几项**？（荐：**L8–L10 落点断链**优先）
3. **两份件正式"输出"的形态**：仍按"导读＋指针"型，还是"逐字完整"型？（口径稿 §三 已给两种判定路径）

## ⑫ 下一步建议

1. **`C3-019` 第一项**：**核验 09-25 23:00 真实定时触发样本**（若会话仍在白天 ⇒ 建议改为"**首个已过 23:00 的时点后的第一动作**"）。
2. 按你口径三问的答复，更新口径稿并（若同意）给两件加"**截至时点**"行。
3. 按你排期答复，启动下一批缺口。
4. `C3-019` 开工首项**全量推送**（含本轮 g1／g2）。

## ⑬ 本轮指令原文归档（关键句逐字）

> - 「**若当前时间仍在 09-25 23:00 之前**：**走甲·明确跳过，不计未完成**（连续第四轮窗口未到已证为时序必然）。**把核验排入 `C3-019` 第一项**。**不再重复登记为"待核"**——避免第四轮空转。」
> - 「**先定"什么叫做完"，再补。**」
> - 「**优先补两处**：**补①设计图——规格映射层**；**补②运行机制——机制 31/32**。**其余 10 项等前两项做完再排。**」
> - 「补入方式：**追加式**（`C19` 兼容），不改原文。」
> - 「**apply 入库**——把预演产物正式入库为 `coordination/tools/README.md`。」
> - 「**乙·延长试用至 `C3-022`。**理由：5 次全对但全靠人工；样本量小，未遇『填错』案例；再跑 5 轮看有无真实错误。」
> - 「**维持 10。**」｜「再评点＝有新证据时。／**不改代码**。」
> - 保密：「**补丁路径**：只写相对路径或目录名。」

## ⑭ 启动回执

> 已读 `CHARTER.md`。
> 当前状态：开工 `count = 428`（自动生成，与预生成值一致）｜阶段口径＝README 四阶段 / ROADMAP 三阶段 **并存**
> 指令序号：`云内核-C3-018` ｜ 下一轮：`云内核-C3-019` ｜ 下一轮基准 `count`：**429**（g1 提交后实测；取最终值须实跑）
> **中断状态**：**无残留**
> 执行模式：**正常**
> 本次任务：push 全量 ＋ 23:00 首验（跳过）＋ 两份件定口径补两处 ＋ 预演 apply ＋ 到期项处置
> 是否继续：**执行（已完成）**
