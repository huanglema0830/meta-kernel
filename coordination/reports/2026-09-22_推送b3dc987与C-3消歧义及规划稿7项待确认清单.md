【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · push `b3dc987` ＋ `C-3` 消歧义（选乙）＋ 规划稿 7 项待确认清单（v0.322）

> **本轮交付两份文件**：① **规划稿 7 项待确认清单** ＝ `coordination/reports/2026-09-22_规划稿7项待确认清单.md` ② **本报告**。

## ① 保密标记

🔓 **公开** —— 本轮**零机密内容**：只涉及**机制编号／文件路径（仓库内相对路径）／`ROADMAP` 条文**，均为**公开仓库内文件**。
★ **如实边界**：**不含**任何**本机绝对路径**（★ 本报告正文**不出现**用户名／盘符路径）、**密钥／账号**、**保密资产真实值**、**公式／阈值／权重**（机制 17／20 的实现层一律**只引指针**）。

> 【给用户的提示】本次报告包含 **0** 处机密、**0** 处保密。可直接拷给顾问。

## ② 全景状态卡

| 项 | 值 |
|---|---|
| **阶段** | **阶段二 · 2.4 显示（纯算层 ＋ 边界层）已验** |
| **基准 → 收口** | `count 333`（**实测一致**）⇒ **334**（**本报告 ＋ 清单 ＋ `ROADMAP` 别名行** 同批提交）|
| **本报告版本号** | **334**（＝**本报告首次提交时的 `count`**，依 **`T-045`** 口径） |
| **push** | ★ **〇 已推**：`cb3179f..b3dc987`（**exit 0**，**本轮起点**）｜**本轮新产出待放行** |
| **CI 状态** | **全绿**（**三 job 全 success**；★ **run 号与逐 job 三态按 `C20` 只记日报、不入库**） |
| **本轮性质** | **放行 ＋ 裁定落地**（★ **本轮首次改动 `ROADMAP.md`**：**仅 §一 加别名行**；`docs/`／`CHARTER`／`CONSTRAINTS`／`TEMPLATES`／`ci.yml`／`tools/` **均零改动**） |

## ③ 任务（含基准／定位）

**指令任务**：**〇 Push 放行（2 个 commit）** ｜ **一、`C-3` 消歧义（选乙）** ｜ **二、规划稿 7 项待确认整理** ｜ **三、本轮不派其他任务** ｜ **四、下一份报告（十四项）**。
**任务类型**：**放行 ＋ 裁定落地**（**非**新功能／非新机制）。
**总规划定位**：**push ＋ `C-3` 消歧义 ＋ 规划稿 7 项待确认整理**。
**基准**：`count = 333`（`git rev-list --count HEAD`）—— ★ **实测与指令一致**（`3` 位实测：`333`；**无基准偏差**）。
**优先级**：★ **指令未指定 P** —— **如实标注**；据机制 6／机制 23（push 须当轮明示），本轮属**用户明示放行＋落地**，**非 P3 自主项**。

**★ 本轮读了底图哪几份、哪几行**（**逐份列名 · 如实记**）：

| 底图（＝根 `README.md` ＋ `docs/`） | 读的范围 | 用途 |
|---|---|---|
| 根 **`README.md`** | **全文 L1–L138**（重点：**§阶段路线（主线）L96–L106**「四阶段」表 ＋ **§文档数值口径 L128–L132** ＋ **§版本规则 L87–L94**） | **确认底图四阶段原文口径**（**`C-3` 别名行的对照基准**）；确认**判据 F 所依的底图总数＝36**（**根 1 ＋ `docs/` 35**） |
| （`docs/` 其余 35 份） | ★ **本轮未读**（**如实记**） | ★ **本轮为 `ROADMAP` 内部改动 ＋ 清单整理 ＋ 放行**，**不涉底图内容** ⇒ **无需读**；**不以摘要冒充逐字**（`C5`） |

> ★ **底图读取的诚实边界**：本轮**只读 1 份**（根 `README.md`，**因 `C-3` 需引用其四阶段原文**）；其余 35 份**未读**，**原因如上**，**不虚报**。

**读了哪些脚手架（非底图）**：`ROADMAP.md`（**L1–L120** 改动定位 ＋ **改后 L19–L41** 复核 ＋ **C15 全文件取数**）／`CONSTRAINTS.md`（**`C15` 段 L285–L308**）／`TEMPLATES.md`（**§二 报告模板 L100–L183**）／`BASELINE.md`（**§五十七 L4276–L4373**，**格式对齐**）／**本规划稿**（`coordination/reports/2026-09-22_云内核完整规划与行动路线图.md`，**§附 L426–L436** 取 7 项）。

## ④ 状态：**完成**

**完成** —— 三项任务（**〇 push／一 `C-3` 选乙／二 7 项清单**）**全部交付**；**验收 9 条全过**（见 ⑤）。

## ⑤ 验收对照

| # | 验收标准（指令原文） | 结果 |
|---|---|---|
| 1 | **push 完成，CI 三 job 全绿（按 `T-038` 门控）** | ✅ `cb3179f..b3dc987` **exit 0**；门控 **`READY_TO_EDIT`**（**completed / success**；**三 job 全 success**；`##[error]` **0**）｜★ **`C20`：run 号与三态只记日报** |
| 2 | **`C-3` 选乙落地：`ROADMAP` §一 加别名行，逐字对照** | ✅ **已落地**（`ROADMAP.md` §一 末，**第 31–35 行**）；**逐字前后对照见 ⑦ 之附** |
| 3 | **`C-3` 并入 `R` 族（仅述"拟并入"，不造字面编号）** | ✅ **只述"拟并入"** —— **全文 0 处字面编号**（**避自指污染**）；已记 **`BASELINE §58.5`**（**状态：待授权**） |
| 4 | **规划稿 7 项待确认清单出** | ✅ `coordination/reports/2026-09-22_规划稿7项待确认清单.md`（**7 项 × 问题／分类／选项／建议／七要素**） |
| 5 | **零触底图（判据 F 不受影响）** | ✅ `git status` 实测：**`docs/` 零改动**；判据 F **PASS**（底图 `.md` **36** —— **与上轮同值**） |
| 6 | **`C15` 复算通过，编号表连续无缺号无重号** | ✅ **`BASELINE §58.6`**（机制 `1..30`／`C 1..21`／`T 1..48`／`TERM 1..14`／节号 **`0..58`** **连续无缺重**） |
| 7 | **CI 全绿** | ✅ 三 job 全 success（**结论按 `C20` 只记日报，不入库**） |
| 8 | **下一次返回第 0 行含 9 步阅读指令** | ✅ 本报告**第 0 行**含（**9 步**）；★ **本轮两份新文件**（本报告 ＋ **7 项清单**）第 0 行**均含**（`check_reading_instruction.py` **PASS**） |
| 9 | **版本号自动生成，不手写** | ✅ 本报告版本号 **334** ＝ **首次提交时 `count`**（`T-045`） |

## ⑥ 未完成项

| # | 项 | 原因 |
|---|---|---|
| 1 | **规划稿 7 项中「涉底图回写」4 项**（第 2／3／4／5） | **须专项裁定**（**触判据 F ＋ 汇编重生成 ＋ 机制 25 P3 复核**）⇒ **本轮只整理成清单，不施工** |
| 2 | **`C-3` 并入 `R` 族的"实际登记"** | ★ **编号登记须授权** ⇒ **本轮只述"拟并入"**（**待授权**） |
| 3 | ★ **承上轮未结**：`C-1`（`v` 号与 `count` 脱钩）／`C-2`（判据无统一运行器）／`check_version_label.py`／`run_all_checks`／`F-编号口径` | **指令明示"下一轮处理"**（本轮"三、其他任务：本轮不派其他任务"）⇒ **继续挂起** |
| 4 | **底图其余 35 份未读** | **本轮范围＝`C-3` 所需的根 `README.md` 1 份**；**如实标注**（`C5`） |

## ⑦ 真实缺陷

**★ 本轮新发现「自指污染第 7 例」（已当场修正 · 自捕获）**

| 项 | 内容 |
|---|---|
| **现象** | 我写的 **7 项清单**首部**为声明"本文件不是报告体"，逐字写出了判据的判定特征词**（即 `check_report_structure.py` 用来判定"报告体"的那个标记）⇒ **判据当场把清单判为"报告体"** ⇒ **告警 1 份**（清单缺 4 项：`3 任务`／`4 状态`／`6 未完成项`／`11 需要用户决策`） |
| **定性** | ★ **自指污染家族第 7 例** —— **"为否定某标记而写出该标记"即造出该标记**。**先例**：机制 28 ／ `T-045`（第 5 例）／ `C-3` 登记（第 6 例，**写未登记的 `R` 编号 ⇒ 判出"引用无登记"**） |
| **修法** | 改为**不复述该标记**（只述"**报告体判定标记**"并指向判据本身）⇒ **复跑告警回 `0`** |
| **教训** | ★ **机器判据的特征词一旦写进正文，就改变了判据的输入** —— **元层级表述天然危险**：**凡"讨论判据本身"的文字，须先自问"这句话会不会被自己的判据读到"** ⇒ 与**跨项目铁律**同源（**"模拟通过 ≠ 实机生效"**：**别在一处验过就当全局成立**） |

**★ 另：`C-3` 本轮已消解（非缺陷，记录闭环）**

- `C-3`（**三套阶段刻度并存 ＋ 阶段一命名不一致**）的**两部分**：① **阶段一命名** ⇒ **本轮选乙落地**（`ROADMAP §一` 别名行）；② **三套刻度并存** ⇒ **本轮在别名行内显式列明三套刻度**（**底图四阶段〔能力〕／本文件三阶段〔交付〕／规划稿三阶段〔工程〕**）⇒ **歧义消除**。
- ★ **`C-3` 本体状态**：**已消解（治疗已落地）**；**其"并入 `R` 族登记"仍需授权**（见 ⑪ ①）。

### ★ 附：`C-3` 落地逐字前后对照（同位置：`ROADMAP.md` §一 末）

**改动前**：
```text
> **两者不互相替代。** 本文件中的"阶段一／二／三"**不得**被读作底图的"阶段一／二／三"；引用时须写明是哪一个口径。

（下接 §二 标题行：原为顶格「## 二、三阶段总览」—— ★ 此处以文字指代，避免被 `grep '^## '` 误计为顶层节）
```

**改动后**：
```text
> **两者不互相替代。** 本文件中的"阶段一／二／三"**不得**被读作底图的"阶段一／二／三"；引用时须写明是哪一个口径。

> #### ★ 规划稿阶段口径别名（2026-09-22 · `C-3` 消歧义 · 选乙）
> - **别名：本表"阶段一"＝纯逻辑层（对应规划稿阶段一）。**
> - 三套刻度并列：**底图四阶段**〔内核能力 · L 层递进〕｜**本文件三阶段**〔交付口径〕｜**规划稿三阶段**〔工程口径〕。
> - ★ **本行只作别名登记**：**不改**本表阶段一的目标文案（仍为"可安装浏览器"），**不改** §二 三阶段总览表（**判据 E 与之相扣**）。
> - 引用"阶段一／二／三"时**须写明是哪一个口径**。

（下接 §二 标题行：原为顶格「## 二、三阶段总览」—— ★ 此处以文字指代，同上）
```

**改动量**：**净增 6 行**（**1 个 5 行引用块 ＋ 1 空行**）；★ **未改** §二 三阶段总览表（**判据 E 相扣，不得破坏**）。

## ⑧ 三量台账

| 量 | 内容 |
|---|---|
| **存量** | 底图 **36** 份（根 `README.md` ＋ `docs/` 35）｜脚手架 7 份（`CHARTER`／`CONSTRAINTS`／`BASELINE`／`TEMPLATES`／`ROADMAP`／`TERMS`／`ASSETS_INVENTORY`）｜**机制 1–30**｜**约束 C1–C21**｜**模板条文 `T-001–T-048`**｜**术语 `TERM-001–014`**｜**11 个机器判据**｜记忆三层（调用 977 B／索引 3931 B／内容 4 份）｜`reports/`（**本轮前 145**） |
| **变量** | 本轮任务＝**push 放行 ＋ `C-3` 选乙落地 ＋ 规划稿 7 项待确认整理**（★ **首次改动 `ROADMAP.md`**） |
| **补充增量** | ① **`ROADMAP.md §一` 别名行**（**5 行引用块**）② **规划稿 7 项待确认清单 1 份**（**新出**；含**"涉底图 / 不涉底图"分类维度**）③ **本报告 1 份**（十四项 ＋ 附件 A–B）④ **`BASELINE §五十八`**（台账） |
| **创新增量** | ① ★ **`C-3` 的治疗首次落地**（**三套刻度由"隐性并存"变为"显式列明 ＋ 别名"**）② ★ **7 项待确认的"涉底图 / 不涉底图"二分法** —— 把"能否直接处理"变成**可判定**（**涉底图 4 项须专项、不涉 3 项可直接**）③ ★ **自指污染第 7 例的"元层级表述"教训**（**凡讨论判据本身的文字，须先自问"会不会被自己的判据读到"**）④ ★ **清单类文件的形态核方法**（**不含报告体标记 ⇒ 不受十四项约束**） |

## ⑨ 边界说明

- **本地跑过** ✅：**只读取证**（`git rev-parse`／`git log`／`git rev-list --count`；`wc -l`／`grep -c`／`sed -n`）；**`C15` 双向计数**（**编辑前取数 ⇒ 编辑 ⇒ 编辑后复算**，脚本化）；**11 个判据实跑**（**依 `T-048`**，**两次**：**改动中一次 ＋ 收口确认一次**）；**门控脚本**（复用 `.workbuddy/tmp/ci_gate_0922c.py`，**仓库外**）。
- **CI 过** ✅：**〇 push 的 `b3dc987`** ⇒ CI **三 job 全 success**（`test`／`宿主 field-render`／`元内核引导镜像`）｜★ **run 号／三态按 `C20` 只记日报，不入库**。
- **真实跑过** — ❌ **无**：本轮**无真实硬件／真机动作**（**如实记**）。
- **未验证** — ⚠️：① **7 项中涉底图 4 项未施工**（**须专项裁定**）② **`C-3` 并入 `R` 族未实际登记**（**待授权**）③ **未跑构建／未跑 QEMU**（本轮非代码轮；**CI 已跑，本机未跑**）④ **底图其余 35 份未读**（本轮范围外）⑤ **`2.5`／`2.6` 未做**（**卡决策／卡 C9**）。

## ⑩ 决策评估（本轮无新决策 ⇒ 记录既有裁定的执行）

★ **本轮为"执行既有裁定"**（`C-3` 选乙 ／ `C-3` 拟并入 `R` 族），**未产生新决策** ⇒ **七要素表见上一轮（v0.321）报告 ⑩**（**评估／优势／劣势／风险／机会／建议／等待决策** 齐全），**本轮不再重复**（**避免同源口径两份**，`C19`）。

★ **唯一新增的"评估类"内容** ＝ **自指污染第 7 例的教训**（见 ⑦）—— 属**教训沉淀**，**非决策**，故不计入七要素。

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★ **`C-3` 并入 `R` 族登记 —— 是否授权落编号** | **荐授权**（**`R38` 家族**；**若并则 `R` 族 +1**）⇒ **授权后下一轮按同族登记**（**先授权、后登记、再引用**，三步不可倒） |
| ② | ★ **规划稿 7 项逐项裁定**（见清单） | ★ **建议：涉底图 4 项（第 2／3／4／5）合并为一次"L0–L6 专题"**裁定；**不涉 3 项**（第 1／6／7）**已可结**（第 1 项本轮已落地） |
| ③ | **push 放行**（**本轮新产出**：`ROADMAP` 别名行 ＋ 清单 ＋ 本报告 ＋ `BASELINE §五十八`） | **放行**（**机制 23**：push 须当轮明示） |
| ④ | ★ **承上轮未结**：`C-1`（`v` 号 vs `count`）／`C-2`（`run_all_checks`）／`check_version_label.py`／`F-编号口径`（轮内 vs 全局） | **荐：`C-1` 走"乙→丙"**；**`C-2` 固化运行器**；**`F-编号口径` 成文为"轮内序号"**（★ 指令已定"下一轮处理"） |
| ⑤ | ★ **上轮欠发的指令段**（**裁定 ③~⑤ 与报告专属要求原文**；截断点＝「**在 `TEMPLATES §11.3` 新增一条：**」） | **若仍需，请补发**；★ **本轮已按显式指令办完可承接部分** |

## ⑫ 下一步建议

1. **若授权 ①** ⇒ 下一轮按 **`R38` 同族**登记一条 `R` 编号（**`BASELINE` §一 缺陷表 ＋ 节内登记**）—— ★ **字面值由该轮登记时确定**（**本轮不预写**）。
2. **若授权 ②** ⇒ **先办"不涉底图"3 项**（**已可结**）；**涉底图 4 项**开**一次"L0–L6 专题"**（**可合并 `M6`／`M8` 前置裁定**）。
3. **若授权 ③** ⇒ **放行本轮 commit**，随后按 **`T-038` 门控** 等 `completed` 再落 CI 结论（**`C20`：只记日报**）。
4. ★ **元层级表述纪律（本轮新增建议）** ⇒ **凡在文档里"讨论某判据本身"**，**须先自问：这段文字会不会被该判据自己读到？**（**自指污染已 7 例**）—— 建议**写入** `BASELINE` 或 `TEMPLATES` 的**判据维护纪律**（★ **须授权**，本轮**只建议、不施工**）。
5. **持续**：`2.5`（卡决策）／`2.6`（卡 C9）**属决策类 P1 ⇒ 不宜夜间自主推进**（**等裁定**）。

## ⑬ 启动回执

已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD` 的输出 ⇒ 起点 `count 333`，本轮收口（本报告同批提交）＝ `334`**（★ **本报告版本号 ＝ 334**，依 **`T-045`**）。
★ **标题 `（v0.322）` 系沿用现状"轮次号"写法** —— 它与 `count` **不等**，**此不符即 `C-1` 所报之缺陷**（**两口径并列，便于比对**；★ **`C-1` 仍未关闭**）。

**本轮状态**：**阶段二 · 2.4 已验** ｜ **本次任务：push ＋ `C-3` 消歧义 ＋ 规划稿 7 项待确认整理** ｜ **是否继续：等待指令 / 待 ⑪ 裁定**。

---

## 附件 A · `B2`＋`C1` 告警行（固定一行）

**★ 六族告警（`C1` 载体 · 固定一行）**：**六族告警：本轮 3 条**（`C22`／`T-00`／`TERM-999`，**均为元引用**）**｜对比上轮 ±0**｜**真缺口 0 条**。
**★ `B2` 本轮新增告警**：**1 条**（★ **清单被误判为"报告体"** ⇒ `check_report_structure.py` 报"缺 4 项"）—— ★ **已当场修正**（**自指污染第 7 例**，见 ⑦）⇒ **修正后复跑告警回 `0`**（**报表以修正后为准**）。

（口径：`check_id_set_diff.py` ＋ `check_report_structure.py` 实跑；**3 条全在 `A2` 豁免登记表**；**剔除 6 层**）

---

## 附件 B · INTEL 状态表（附加表 · 依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| INTEL-001 工信部 AI 中小企业计划 | 观察中 | 组合：企业注册完成 ＋ **阶段三启动** | ✗ **未触发**（现处**阶段二 · 2.4**） | 无 |
| INTEL-002 GPT-6 安全风险 | 观察中 | 事件：**L7–L10 落地后** | ✗ **未触发**（L7 设计完成／L8–L10 **冻结**） | 无 |

**J1–J4 自查**：**J1** 报告须含状态表 ✅｜**J2** 触发条件满足但状态未变 ⇒ 违规 —— **两条均未触发** ✅｜**J3** 待准备但无准备稿 ⇒ 违规 —— **两条均非"待准备"** ✅｜**J4** 准备中但无具体行动 ⇒ 违规 —— **不适用** ✅。

★ **机制 29 本轮无状态变更**（**不虚触**）。
