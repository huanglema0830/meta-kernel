【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

> ⚠️ **步数口径说明（本文件已按权威源取 9 步）**：本次任务指令写的是「8 步」，但**权威源已是 9 步**——
> `TEMPLATES.md` §二 **第〇项**（`> **步数**：**9 步**`）＋ `CONSTRAINTS.md` **C13** ＋ `CHARTER.md` 机制 18／§四 C13 行，
> 均记 **T-018：2026-09-17 用户新增第 9 步「对齐用户意图」，✅ 生效**。
> ⇒ 本文件**取 9 步**（以权威源为准），并把"指令里写 8 步"这件事作为**真实缺陷**登记（见下 §真实缺陷-1）。

---

# 今夜 P3 清单 · 2026-09-17（机制 19 夜间自动化 · 开工前置）

**保密标记**（对应 C12；**拷贝给顾问前先看这一段**）：
- ⛔ 机密（**绝不拷**）：**无** —— 本清单按 C12 已预屏蔽：不含凭证、账号、本机绝对路径、网络细节、参数库取值
- 🔐 保密（**屏蔽后拷**）：**无**
- 🔒 半公开（**可拷**）：仓库内相对路径、编号体系（R／D／C／T／机制号）、模块名与行数口径
- 🔓 公开（**直接拷**）：版本号、阶段定位、清单条目、验收结论

> 【给用户的提示】本次报告包含 **0** 处机密、**0** 处保密。无需屏蔽即可整份拷给顾问。

**全景状态卡**（自动生成；含版本号／阶段定位／待决策项／CI 状态）：

```
┌─ 云内核项目 · 全景状态卡 ─────────────┐
│ 版本   v0.184（rev-list --count HEAD） │
│ 阶段   ROADMAP 阶段二 · 子任务2        │
│        （2.3b 分片迁移进行中）          │
│       片1–5 ✅ ｜ 剩余 片6–片8 =        │
│        7 模块 / 2,887 行（脚本口径）    │
│ CI     最近一次 run：全绿（按 BASELINE  │
│        快照 run 35077993177 / c84afc0， │
│        本次未新触发）                   │
│ 漏洞   R1–R37（本轮无新增 R）           │
│ 决策   待决策 5 项：D41／D42／D43／      │
│        D44／D45                         │
└────────────────────────────────────────┘
```

**任务**（含**基准**与**总规划定位**，含**优先级 P0–P4**）：生成「今夜 P3 清单」（机制 19／C14 开工前置）｜基准 v0.184｜定位：阶段二 · 子任务2 · 2.3b 分片迁移｜**优先级 P3**（无需决策、可自主推进）
**状态**：完成
**验收对照**（标准 vs 实测，逐条）：

| # | 验收标准（指令原文拆解） | 实测 | 结论 |
|---|---|---|---|
| 1 | 输出**第 0 行**＝「顾问阅读指令」固定文本（C13） | 本文件字面第 1 行即为该段（Markdown 起首无前置空行） | ✅ |
| 2 | 清单**只取 P3 且「可夜间跑＝✅」** | 4 项，逐项标 P3 + 可夜间跑 ✅（见 §一） | ✅ |
| 3 | 每项写清【任务／依据（ROADMAP 哪一行）／预计时长／可夜间跑】 | 4 项四字段齐全；依据含行定位 | ✅ |
| 4 | **排除**需用户决策（P1）项 | 2.4（P1）／子任务6（P1）已排除并联原因 | ✅ |
| 5 | **排除**需真实硬件／真机验证项 | 子任务4／子任务5／2.7 已排除并联原因 | ✅ |
| 6 | **排除**「对外可见／不可逆」动作 | 子任务6／剩余换壳（D35/D36 遗留）已排除 | ✅ |
| 7 | **排除治理文件类改动**（CONSTRAINTS／CHARTER／ROADMAP／BASELINE／TEMPLATES／`.github/workflows/`） | 清单 4 项**无一**含治理文件或 CI 配置改动；CI 优化类**整类排除**并联原因 | ✅ |
| 8 | 写入 `coordination/reports/<今日日期>_今夜P3清单.md`（同日只保留一份） | 本文件（2026-09-17）；核查：同目录无同名文件，为**新写**非覆盖 | ✅ |
| 9 | 清单**不含 ⛔ 内容** | 实测：无凭证／账号／本机绝对路径／网络细节／参数库取值（阈值一律只写"见编号"，不复述数值） | ✅ |
| 10 | **不推送、不提交**、不改代码 | 全流程只读；`git status` 未产生提交；未改动任何源文件 | ✅ |
| 11 | 汇报 ≤6 行 | 对话侧汇报按要求压缩 | ✅ |

**一、今夜 P3 清单（4 项，按建议执行顺序）**

> **选入口径（机械过滤，不是人工挑）**：① 优先级＝**P3** ②「可夜间跑」＝**✅**（ROADMAP 原表口径）
> ③ 过 P3 白名单八类 ④ 不被排除清单命中。**片7／片8 虽同为 P3✅，但脚本分层为「层2／层3」、依赖片6 完成 ⇒ 今晚不可开工**，已进排除项。

---

### P3-1「片6 替换类论证清单」出证（含 D41 论证）

| 项 | 内容 |
|---|---|
| **任务** | 对 **片6 的 4 个模块**（`l1_mapping` 822 行／`positive_source` 757 行／`l5_diagnosis` 468 行／`l7/repair` 290 行）逐个做**"替换类"普查**，产出论证清单；**只读分析、不改代码** |
| **依据（ROADMAP）** | `ROADMAP.md` **§三 子任务2 子步表 · 2.3b 行** —— 原文：『剩余片6–片8 = 7 模块 / 2,887 行（脚本口径）。⚠️ 片6 已探到 `std::collections::HashMap`（**类型替换类，须单独论证**，同 D37 口径）。**可夜间跑 ✅**』<br>辅：`BASELINE.md` §九 **D41 行**（"建议：先出**用法论证清单**（读用法＋编译探针），确认后再迁片6"） |
| **预计时长** | 90 min |
| **可夜间跑** | ✅（纯读 + 写报告；**不迁代码**） |
| **初判线索（未复核，不得当结论）** | 剩余 7 模块中 `HashMap` 仅见于 `positive_source`：1 处 `use` ＋ 1 个字段 `twin_index: HashMap<u64, usize>` ＋ 1 处 `.get()`；**粗看未见迭代写法** ⇒ 与 **D37 同型**（`HashSet` 只用 insert/len、从不迭代 ⇒ 逐位等价）。⚠️ **必须以"读全部用法 + 编译探针"复核，不得以 grep 代替论证**（对照 **R29**：凡断言，期望值必须先读实现） |
| **产出物** | `coordination/reports/2026-09-17_片6替换类论证清单.md`，须含 4 段：① 逐模块替换类普查（`HashMap`／`HashSet`／`VecDeque`／`BTreeMap`／`std::` 残留／`f64` 方法面）② 每条给「是否被迭代 ／ 是否逐位等价 ／ **结论失效前提**（何时必须重评）」③ **R34 要求的「待同步白名单条目」清单**（迁移白名单 ＋ 保真度白名单；**本次只列、不改**）④ **片6 冻结清单**（由 `coordination/tools/check_migration_closure.py --plan` 产出、以 `--mods` 形态落盘，供执行时直接消费 —— 依据 **R33**「输出一开始执行就必须冻结」） |

---

### P3-2「2.6 ACPI/CPUID 软件层识别」调研 + 执行体归属判定

| 项 | 内容 |
|---|---|
| **任务** | 调研硬件信息读取的**公开规范层**，并按**机制 21（执行体归属）**给出归属判定与分层切分建议；**不猜算法、不填取值** |
| **依据（ROADMAP）** | `ROADMAP.md` **§三 子任务2 子步表 · 2.6 行** —— 原文：『通过 **ACPI/CPUID** 读取硬件信息（软件层面识别）｜**P3**｜✅』<br>辅：`ROADMAP.md` §三 子步表下方★注『**硬件识别第一阶段只做软件层面**（读 ACPI 表、CPUID），**不做场域指纹**』 |
| **预计时长** | 75 min |
| **可夜间跑** | ✅（纯调研；**不碰真机**、不需用户决策） |
| **内容要点** | ① 表结构与读法的规范层调研（只写"做什么"）② **归属判定**：**读表＝感知/动作 ⇒ 只能落 boot 层或宿主**；**对已读入的数据做解析＝纯算 ⇒ 可进内核**（机制 21 一句话判据）③ 按**机制 17** 给"骨架（公开）／实现（仓库外）"切分建议 ④ 明确**本轮不产码** |
| **产出物** | `coordination/reports/2026-09-17_2.6ACPI-CPUID调研与归属判定.md` |

---

### P3-3 fmath 精度回归测试补缺（测试补充）

| 项 | 内容 |
|---|---|
| **任务** | 把 **f64 超越函数的精度口径**固化为**回归测试**（防后续改动静默回归）；**先核已有覆盖，只补缺口** |
| **依据** | `BASELINE.md` **§七 指标表 · alloc 迁移（2.3b）行** —— 原文：『**f64 精度实测（vs `std`）**：`sqrt` 1 ULP／`log2` 1／`ln` 1／`exp` 2／`powi` ≤4／`round`、`rem_euclid` 完全相等』<br>辅：`BASELINE.md` §九 **D29 行**（精度判据＝**ULP**，不做 `f64` 版）＋ `TEMPLATES.md` §三 **P3 清单"测试类：补充单元测试"** |
| **预计时长** | 45 min |
| **可夜间跑** | ✅（只动测试区，不改产品语义） |
| **⚠️ 必守前提** | ① **门线取 D29 已定口径**（具体数值见 `BASELINE.md` §九 D29，**本清单不复述**，C12）② 目标文件 `meta-kernel-core-nostd/src/fmath.rs` 与 `meta-kernel-core-nostd/tests/mirror_bare_assertions.rs` **已含 ULP 字样 ⇒ 可能已有部分覆盖** ⇒ **先核对、后补缺** ③ **已有则如实报 0，不得为凑数造测试**（不打绿、不充数） |

---

### P3-4 本机门禁复核 + 与 `BASELINE.md` 声明逐条比对（只读）

| 项 | 内容 |
|---|---|
| **任务** | 跑 **CI 覆盖不到的那一半**门禁，并把实测输出与权威文件的**声明**逐条比对；不符即如实记录（**不改治理文件**） |
| **依据** | `BASELINE.md` **§六 · R25 行** —— 原文要点：『**要点不是"有泄漏"，而是"历轮报告每轮都写『⛔ 已预屏蔽』"——声明与事实不符**』<br>辅：`BASELINE.md` §六 **R31 行**（"判据没跟上机制的形态变化"）＋ `TEMPLATES.md` **§十三.3**（机制 21 机器判据＋`--selftest` 四侧） |
| **预计时长** | 30 min |
| **可夜间跑** | ✅（只读） |
| **执行内容** | ① `coordination/tools/check_private_pointers.py --mode=resolve` —— **该模式不入 CI**（入 CI 的只有 `syntax`）⇒ **本机这一跑是 CI 完全覆盖不到的一半** ② `coordination/tools/check_kernel_purity.py` 含 `--selftest`（四侧自检）③ 与 `BASELINE.md` 对应声明**逐条比对**；**报数写明口径**（行／处／文件，依 **R35**） |
| **产出物** | 追加记录到本清单所在目录的同轮报告（**不为一项另开文件**） |

**二、本期明确排除项（附原因 —— 排除也是结论，一并留痕）**

| 被排除项 | 出处 | 排除原因（命中哪条排除规则） |
|---|---|---|
| **2.4 DRM/KMS 直渲** | ROADMAP §三 2.4 行 | **P1**（C9 白名单须**逐条放行** ⇒ 需用户决策）；「可夜间跑」＝⚠️**部分** |
| **2.7 可引导 ISO** | ROADMAP §三 2.7 行 | **P2**（须验证才可宣告达成）；「可夜间跑」＝⚠️部分 |
| **子任务4 硬件适配** | ROADMAP §三 子任务4 | **❌ 需真机**（"是否真的点亮"必须上真机） |
| **子任务5 安装程序** | ROADMAP §三 子任务5 | 依赖**子任务4**（真机）⇒ 前置未达成；本条虽有 P3✅，但**不具备开工条件** |
| **子任务6 ISO 分发** | ROADMAP §三 子任务6 | **P1** ＋「**对外可见**」⇒ C10 须用户明确确认 |
| **片7 ／ 片8** | ROADMAP §三 2.3b 行 | 脚本分层为**层2／层3**，依赖**片6 完成** ⇒ 今晚不可开工 |
| **CI 优化 / `.github/workflows/` 任何改动** | 指令排除项 ＋ 23:00 任务禁止清单第 1 条 | **治理文件类改动**，**整类排除**（P3 白名单虽列"CI 优化"，**排除规则优先**） |
| **D35／D36 剩余换壳（`docs/` 历史文件）** | BASELINE §九 D35／D36 行 | 涉"**对外可见／不可逆**"边界 ＋ 须用户决策；且**已推送内容撤不回** |
| **D42／D43／D44／D45** | BASELINE §九 | 均为 **⏳ 待确认**（需用户决策） |

> **两条"看起来该做但其实不能做"的澄清（防误读）**：
> ① **P3 白名单里的"CI 优化"今晚用不上** —— 白名单与排除清单冲突时**以排除清单为准**（`.github/workflows/` 是治理文件）。
> ② **P3-1 出的是"论证"，不是"决策"** —— 论证出证后，**片6 仍须用户对 D41 给一句放行**才可开工（C11：用户决策前不得擅自执行）。

**三、真实缺陷（本次发现的）**

**缺陷-1（新发现）：阅读指令步数的「活文件引用」不同步 —— 3 行仍写 8 步，权威源已是 9 步**

- **事实**（口径＝**行**；命令 `grep -rn "8 步\|9 步" coordination/*.md`）：
  - ✅ 已同步为 **9 步**：`CONSTRAINTS.md`（C13 正文）／`CHARTER.md` 机制 18 行 ／`CHARTER.md` §四 C13 行 ／`TEMPLATES.md` §二 第〇项（并显式标 `> 步数：9 步`）／`TEMPLATES.md` §7.2 生效项数行
  - ❌ **仍写 8 步（共 3 行）**：`BASELINE.md` **第 88 行**（机制 18/19 行内"固定文本 8 步"）｜`BASELINE.md` **第 171 行**（§八 C13 摘要"顾问按 8 步执行"）｜`CHARTER.md` **第 181 行**（§九 索引表"顾问收到返回后该做什么（8 步）"）
- **影响**：顾问若从 `CHARTER.md` §九 索引或 `BASELINE.md` §八 摘要进入，会读到**过期步数**；而 C13 的第 9 步恰恰是"对齐用户意图"——**漏读它，前 8 步全过也拦不住"答偏问题"**。
- **为何今晚不改**：这三处均在**治理文件**（`BASELINE.md`／`CHARTER.md`）内，**属夜间清单的强制排除项**。⇒ 登记于此，建议**白天一处修**（对齐 T-018；编辑时按 **C15** 双向计数）。
- **附带说明（防"误报"）**：`BASELINE.md:84`、`BASELINE.md:127`、`TEMPLATES.md:325/330`、`advisor_brief.md:40` 中的"八步／8 步"**不算缺陷** —— 前两处是**历史留痕**（描述当时的形态、本就应保留），后几处指**通用工作流的八步**（另一件事，同名不同指）。

**四、三量台账**

- **存量**：[带入] `ROADMAP.md` 阶段二子步表（2.1–2.7）与里程碑 M1–M7；`BASELINE.md` 未完成项表／R1–R37／D1–D45；`TEMPLATES.md` §三 优先级与 §十一 夜间自动化；`CHARTER.md` 机制 1–21 与 C1–C15
- **变量**：[本次任务] 生成「今夜 P3 清单」——**只读 + 只写清单**；不改代码、不改治理文件、不提交、不推送
- **补充增量**：[新增] 今夜 P3 清单 **1 份（4 项）**＋**排除项 9 条及其原因**（排除理由本身是新信息）＋ **真实缺陷 1 条**（步数引用不同步，3 行）
- **创新增量**：[产出] **无** —— 本任务性质为**编排与过滤**（把"可夜间跑的 P3"从路线图里**选出来**），不产出新能力、新算法、新机制

**五、边界说明**

- **本地跑过**：`check_migration_closure.py --plan`（只读，产出 3 片分层清单）｜`grep` 类只读核查（替换类线索、步数引用、归档计数）｜版本号取自 `git rev-list --count HEAD`（＝**184**，C2）
- **CI过**：**不适用** —— 本任务未新增代码、未触碰 CI，**未触发任何 CI run**（本文件内引用的 CI 结论均为 `BASELINE.md` 快照，非本轮实测）
- **真实跑过**：**不适用**（本任务无运行期产物）
- **未验证**：① P3-1 的**替换类初判**（`HashMap` 仅 1 处 `.get()`）**未经编译探针验证** —— 已在文中**显式标为"线索、非结论"** ② 4 个 P3 项的**实际可执行性**未预跑（它们**今晚才被执行**）

**六、决策评估**（如有新决策，按 §九 七要素）：**本轮无新决策**。D41 仍为 ⏳ 待确认，本清单只出**论证**、不代行决策（C11）。

**七、需要用户决策**

1. **D41**：片6 的 `std::collections::HashMap` 是否放行（**建议路径**：先看 P3-1 产出的论证清单，再给一句放行 ⇒ 片6 才转为"可夜间跑"）
2. **是否修正 3 行过期步数引用**（`BASELINE.md:88`／`:171`／`CHARTER.md:181`）—— 属治理文件，须白天执行

**八、下一步建议**

1. **21:00–23:00 内可调整本清单**（增、删、换序均可，说一句即可；调整后以你的一句话为准，覆盖本文件）
2. **无异议 ⇒ 23:00 自动开工**，只跑上述 4 项；每项**单独本地 commit**（**不 push**，推送留待次日一句话放行）
3. **若想让片6 今晚就能开工**，请在白天对 **D41** 给一句放行 —— 但**放行前仍建议先看 P3-1 的论证清单**（它是 C4"改前列清单"的那份清单）
4. **兜底**：若 4 项因故均无法推进，**什么都不做、不猜不造**，如实汇报（与 23:00 任务收尾条款一致）

**启动回执**：已读 `CHARTER.md`。当前状态：**v0.184**（`git rev-list --count HEAD`）｜ROADMAP **阶段二 · 子任务2**（2.3b 分片迁移进行中；片1–片5 ✅ ／ 剩余片6–片8 ＝ 7 模块 / 2,887 行）｜本阶段目标：完成 alloc 迁移、元内核成为可引导内核｜本次任务：生成今夜 P3 清单（P3，只读 + 只写清单）｜是否继续：等待指令 / 自主推进 P3

---

*（本文件为 2026-09-17 当夜**唯一一份**「今夜 P3 清单」；同日晚间若再生成，覆盖本文件。）*
