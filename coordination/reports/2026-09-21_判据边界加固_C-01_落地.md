【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 判据边界加固 · **C-01 落地**（多文件同时过期 ⇒ 逐份点名）· 2026-09-21 · v0.272

> **性质**：**落地**（用户 §五.2「判据继续加固：如 C-01 等未做项，继续做」）。
> **对象**：`coordination/tools/check_doc_consistency.py`（**机制 25**）。**未改治理文件**。
> **保密标记**（C12）：🔓 公开。⛔ 机密 **0**、🔐 保密 **0**。

---

## 0. 一句话

**C-01 是 C-02／C-03／C-04 的补完**：前三条治「**退化/跳过/空转**与『通过』同值」，C-01 治
「**部分生效**与**全生效**同值」—— 若实现里存在"命中即 `break`／只取第一份"，**多文件同时过期会只报一份**，
其余**静默漏报**（**R83 同族**）。

## 1. 改动（**只动 1 个文件、只加自检侧，未改判定逻辑**）

- 新增自检 **侧⑫**（**正反对照**）：
  - **(a) 阳性**：夹具放 **2 份**同时过期（`A.md` ⑧段、`B.md` ⑨段）⇒ **必须两份都被点名**；
  - **(b) 阴性对照**：只留 **1 份**过期 ⇒ **只点名 1 份**（防"误报成多份"）。
- **`--selftest` 由十一侧扩为十二侧**；docstring 与帮助文本同步（`自检（十二侧）`）。
- **判定逻辑未改**：`check()` 的 P1／P4 本就逐文件 `append` ⇒ **C-01 只是把"这一点"钉成判据**
  （**先有断言、后有实现**的逆序：此处是**先有实现、后补断言**，用于**防将来回归**）。

## 2. 实跑证据

```
[侧⑫] C-01 多文件同时过期（a）2 份 ⇒ ✅ 逐份点名 ['A.md', 'B.md']；（b）对照·1 份 ⇒ ✅ 只点名 1 份
自检结论：✅ 十二侧全部符合预期
（实跑）结果：✅ 通过（P1 段号一致 / P2 收口一致〔双侧 P2a＋P2b〕 / P3 内部自洽 / P4 模块数一致）
```

## 3. 边界说明（四类）＋ 声明

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | `--selftest` **十二侧全绿**（rc=0）；无参实跑 **✅ 通过**（rc=0） |
| **CI 过** | **待 CI**（改动 `tools/**`，由 CI `test` job 的机制 25 门禁步骤验证；**本轮新 commit 未 push ⇒ CI 未跑**） |
| **真实跑过** | **不适用**（判据脚本） |
| **未验证** ⚠️ | ① 侧⑫ 只覆盖 **2 份**同期的情形（**≥3 份**未单独设侧，逻辑同一路径）；② **C-05 仍暂缓**（机制 26 轻判据，**须授权改 `TEMPLATES.md`**） |

> **声明**：本项**只改判据脚本**；**未改底图、未改治理文件、未改 boot 层**。

## 4. 至此的边界加固全景

| 编号 | 主题 | 状态 |
|---|---|---|
| **C-01** | 多文件同时过期 ⇒ 逐份点名 | ✅ **本轮落地**（侧⑫） |
| **C-02** | 豁免词**只减不增** | ✅ v0.268 落地（侧⑨） |
| **C-03** | 零陈述 ⇒ **`NO_CLAIM`**（≠ `PASS`） | ✅ v0.268 落地（侧⑩） |
| **C-04** | 机器事实源退化 ⇒ **判红** | ✅ v0.268 落地（侧⑪） |
| **C-05** | 机制 26 矩阵完整性轻判据 | ⏳ **暂缓**（**改 `TEMPLATES.md`** ⇒ 须授权） |

⇒ **机制 25 自检现为十二侧**；**C-01…C-04 已全部落地**，仅 **C-05 待授权**。
