【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-23 夜间 P3-2 · 汇编合订本 ↔ 底图 逐区块逐行对账

> **来源**：`coordination/reports/2026-09-23_今夜P3清单.md` · **§一 P3-2**；依据 `BASELINE.md` **§68.5** ＋ 本日《接续包核验补全表》**⑨ 未验证-5**。
> **性质**：**纯只读** —— 合订本属 `discussions/`（**历史快照**）⇒ **不写入、不重生成、不改一字**。
> **测量件**：三个探针建在**仓库外**（不入库、不随推送出去）。

## ① 保密标记（C12）

- ⛔ 机密（绝不拷）：**有 —— 且本件已全部预屏蔽**。被屏蔽项＝**合订本与底图之间"R25 掩码 vs 原文"的 20 处差异所涉及的取值**（**局域网 IP 形式**、**本机用户名**、**本机绝对路径**）。本件**一律只写掩码占位符形态**（`〈局域网 IP〉`／`〈本机用户名〉`），**不写任何取值**。
  > ★ **重要**：这 20 处的"原文侧"**载体是底图 `docs/FULL_AUDIT_REPORT.md` 等件的既存泄漏项**（`R25`），**本件不复制、不指向、不摘要其取值**；行址明细在 `coordination/security/leak_baseline.txt` 本体。
- 🔐 保密（屏蔽后拷）：**无**。
- 🔒 半公开（可拷）：仓库内相对路径与行号、区块编号、`R`／`D`／机制编号、计数口径。
- 🔓 公开（直接拷）：版本号、行数、清单条目、结论。

> 【给用户的提示】本件按上列屏蔽后**可整份拷给顾问**；拷给**外部**方前，建议**只保留 §一／§三／§四**（结论与方法），§二 的逐条差异明细含 `R25` 行址形态，**对象限内部**。

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡（本件时点）────┐
│ 版本   v0.364（本机 rev-list --count）  │
│        HEAD c448c68 ＋本夜 P3-1         │
│ 阶段   ROADMAP 阶段二 · 子任务2         │
│ CI     本夜未触发（按 C20 不入库）      │
│ 底图   44 份（README + docs/ 43）       │
│ 合订本 7809 行 ／ 收录区块 39           │
│ 待裁   §68.9 六项 ＋《补全表》⑪ 七项    │
└─────────────────────────────────────────┘
```

## ③ 任务 · 状态 · 优先级

| 项 | 内容 |
|---|---|
| 任务 | ① 按区块围栏切分合订本，逐区块取"快照行数" ② 与底图对应文件行数比对 ③ **逐行 diff**（`difflib`）④ 判定差异是否为 **v0.332 已声明的 +817 增量**所应然包含 ⑤ 一句话结论 ＋ 需重生成区块清单 |
| 基准 | **v0.364** |
| 定位 | ROADMAP **阶段二 · 子任务2** |
| 优先级 | **P3** |
| 状态 | **完成** |

---

## 一、区块切分与行数对账（39 区块）

**切分口径（本件定义并写明）**：区块＝合订本正文中以 **`## <编号> <标题> — \`docs/<文件名>\``** 为头、紧随 **4 反引号围栏**包裹的**收录段**。
**行数口径**：**必须先剥行号前缀** `^\s*\d+│`（`C5` 口径 C5「保留行号」⇒ 合订本收录行**逐行带行号**；**不剥会得假红** —— 已登记教训）。

| # | 收录文件 | 合订本行 | 底图行 | 行差 | 逐行 diff（＋/－/改） | 状态 |
|---|---|---|---|---|---|---|
| 1 | `VISION.md` | 86 | 86 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 2 | `CONTRAST_REPORT.md` | 50 | 50 | 0 | ＋0/－0/**改4** | ⚠️ 差异 |
| 3 | `COSMIC_COMPUTING.md` | 142 | 142 | 0 | ＋0/－0/**改10** | ⚠️ 差异 |
| 4 | `ONTOLOGY_SPEC.md` | 88 | 88 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 5 | `LAYER_ARCHITECTURE.md` | 343 | 343 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 6 | `LAYER_DETAILS.md` | 626 | 626 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 7 | `ORIGIN_OS_DESIGN.md` | 276 | 276 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 8 | `ORIGIN_BROWSER_DESIGN.md` | 232 | 232 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 9 | `DEFENSE_DESIGN.md` | 281 | 281 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 10 | `LAYER_BASEMAP_L0_L6.md` | 690 | 690 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 11 | `MATH_SPEC.md` | 156 | 156 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 12 | `PERTURBATION_MODEL.md` | 79 | 79 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 13 | `L5_TRANSLATION_TABLE.md` | 64 | 64 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 14 | `L5_DESIGN.md` | 191 | 191 | 0 | ＋0/－0/**改5** | ⚠️ 差异 |
| 15 | `L6_DESIGN.md` | 110 | 110 | 0 | ＋0/－0/**改6** | ⚠️ 差异 |
| 16 | `FIELD_PRESENTATION_DESIGN.md` | 183 | 183 | 0 | ＋0/－0/**改3** | ⚠️ 差异 |
| 17 | `L7_EXECUTION_DESIGN.md` | 305 | 305 | 0 | ＋0/－0/**改2** | ⚠️ 差异 |
| 18 | `SILA_IMPLEMENTATION.md` | 119 | 119 | 0 | ＋0/－0/**改1** | ⚠️ 差异 |
| 19 | `FIELD_REPAIR_DESIGN.md` | 168 | 168 | 0 | ＋0/－0/**改2** | ⚠️ 差异 |
| 20 | `FIELD_RENDER_SOURCELESS_DESIGN.md` | 132 | 132 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 21 | `WORK_CONSOLE_PLAN.md` | 113 | 113 | 0 | ＋0/－0/**改3** | ⚠️ 差异 |
| 22 | `GENE_LIBRARY_DESIGN.md` | 275 | 275 | 0 | ＋0/－0/**改3** | ⚠️ 差异 |
| 23 | `APPLICATION_FRAMEWORK_DESIGN.md` | 216 | 216 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 24 | `REFERENCE_APP_DESIGN.md` | 93 | 93 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 25 | `API_GATEWAY_DESIGN.md` | 143 | 143 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 26 | `HOST_BUILD.md` | 203 | 203 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 27 | `HOST_UI_DECISION.md` | 235 | 235 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 28 | `QUANTUM_CLOCK_DESIGN.md` | 81 | 81 | 0 | ＋0/－0/**改7** | ⚠️ 差异 |
| 29 | `L4_VALIDATION_REPORT.md` | 47 | 47 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 30 | `L5_VALIDATION_REPORT.md` | 118 | 118 | 0 | ＋0/－0/**改3** | ⚠️ 差异 |
| 31 | `DIAGNOSIS_VALIDATION.md` | 81 | 81 | 0 | ＋0/－0/**改7** | ⚠️ 差异 |
| 32 | `PERFORMANCE_REPORT.md` | 122 | 122 | 0 | ＋0/－0/**改8** | ⚠️ 差异 |
| 33 | `PHASE1_REVIEW.md` | 23 | 23 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 34 | `PHASE1_ASSUMPTIONS_REVIEW.md` | 129 | 129 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 35 | `PHASE4_ACCEPTANCE_REPORT.md` | 41 | 41 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 36 | `SELF_DIAGNOSIS_REPORT.md` | 449 | 449 | 0 | ＋0/－0/**改8** | ⚠️ 差异 |
| 37 | `FULL_AUDIT_REPORT.md` | 267 | 267 | 0 | ＋0/－0/**改18** | ⚠️ 差异 |
| 38 | `CREATION_LOG.md` | 55 | 55 | 0 | ＋0/－0/改0 | ✅ 同步 |
| 39 | `SECURITY_TROUBLESHOOTING.md` | 138 | 138 | 0 | ＋0/－0/改0 | ✅ 同步 |

**合计**：区块 **39**；**行差 = 0 的区块 = 39/39**；**存在行内差异的区块 = 16**（2／3／14／15／16／17／18／19／21／22／28／30／31／32／36／37）。

**★ 关键口径发现（本件新增 · 直接影响 ④ 的判法）**：
> **「行数比对」对本次改动100% 失明** —— **16 个差异区块的 `行差` 全部为 0**，而 diff 显示 **＋0/－0/改 N**。
> 根因：**v0.331（`ca68e5f`）的命名同步是「等量替换」**（该提交 `--shortstat` ＝ **25 文件，158 insertions / 158 deletions**，**增删完全相等**）⇒ **行数不变、只有字变**。
> ⇒ **推论（应固化为方法）**：**"行数相同"不构成"内容同步"的证据**；**判"汇编是否同步底图"必须逐行 diff，行数比对只能当粗筛。**

**§68.5「7 区块」口径复核**（本件逐份实算，`BASELINE §68.5` 给出行数 86／343／690／626／276／232／281）：

| 区块 | 合订本 | 底图 | 结论 |
|---|---|---|---|
| `VISION.md` | 86 | 86 | ✅ |
| `LAYER_ARCHITECTURE.md` | 343 | 343 | ✅ |
| `LAYER_BASEMAP_L0_L6.md` | 690 | 690 | ✅ |
| `LAYER_DETAILS.md` | 626 | 626 | ✅ |
| `ORIGIN_OS_DESIGN.md` | 276 | 276 | ✅ |
| `ORIGIN_BROWSER_DESIGN.md` | 232 | 232 | ✅ |
| `DEFENSE_DESIGN.md` | 281 | 281 | ✅ |

⇒ **`§68.5` 的 7 个行数 7/7 复核通过**（**该 7 区块且内容亦全部 ✅ 同步**）。

---

## 二、逐行 diff 结果（三分类 · 90 处差异全部归因）

**方法**：对 16 个差异区块逐行 diff（`difflib.SequenceMatcher`，`autojunk=False`），把每一处差异尝试归入三档：
**A 命名同步**（快照＝旧名，底图＝新名，含中／英文变体）｜**B R25 掩码**（快照＝`〈…〉`占位符，底图＝原文 ⇒ **有意掩码，非过期**）｜**C 其他**。

| 档 | 处数 | 说明 |
|---|---|---|
| **A 命名同步滞后** | **68** | 旧名 → 新名的**同位置等量替换**（`基因内核`→`DNA内核`／`云操作系统`→`正源操作系统`／`空天浏览器`→`正源浏览器`／`云海大模型`／`采微大模型`→`正源大模型`，含英文名） |
| **B R25 掩码差异** | **20** | ★ **假红** —— 合订本按 `R77` **统一掩码**，底图保留原文 ⇒ **不是过期** |
| **C 其他（命名变体）** | **2** | ① `QUANTUM_CLOCK_DESIGN` 第 23 行：`与经典/基因内核同构` → `与**当前实现**的 DNA内核 同构`（**裁定 A 的表述统一**）；② `SELF_DIAGNOSIS_REPORT` 第 313 行：命名处**多加粗体标记**。**两条均属命名同步族** |
| **合计** | **90** | **归因率 90/90 ＝ 100%**（**0 处"来源不明"**） |

**A 类按文件分布（68 处）**：`COSMIC_COMPUTING` 10 ／ `DIAGNOSIS_VALIDATION` 7 ／ `PERFORMANCE_REPORT` 7 ／ `SELF_DIAGNOSIS_REPORT` 7 ／ `L6_DESIGN` 6 ／ `QUANTUM_CLOCK_DESIGN` 6 ／ `L5_DESIGN` 5 ／ `CONTRAST_REPORT` 4 ／ `FIELD_PRESENTATION_DESIGN` 3 ／ `WORK_CONSOLE_PLAN` 3 ／ `GENE_LIBRARY_DESIGN` 3 ／ `L7_EXECUTION_DESIGN` 2 ／ `FIELD_REPAIR_DESIGN` 2 ／ `FULL_AUDIT_REPORT` 2 ／ `SILA_IMPLEMENTATION` 1（**15 文件**）。

**B 类按文件分布（20 处）**：`FULL_AUDIT_REPORT` 16 ／ `L5_VALIDATION_REPORT` 3 ／ `PERFORMANCE_REPORT` 1（**3 文件**）。⇒ ⛔ **取值一词不录**（见 §①）。

**典型 A 类样例（形态示意，已脱敏）**

| 区块 | 快照（合订本） | 底图（当前） |
|---|---|---|
| `CONTRAST_REPORT.md` @8 | `用户操作 \| 传统方式 \| 云内核方式（云操作系统 / 空天浏览器）` | `…（正源操作系统 / 正源浏览器）` |
| `COSMIC_COMPUTING.md` @4 | `**云海大模型与"算力"到底是什么关系？**` | `**正源大模型与"算力"到底是什么关系？**` |
| `L6_DESIGN.md` @1 | `# L6 设计：空天浏览器（云操作系统 · 用户交互层）DESIGN v1.0` | `# L6 设计：正源浏览器（正源操作系统 · 用户交互层）DESIGN v1.0` |
| `GENE_LIBRARY_DESIGN.md` @8 | `它是基因内核（Gene Kernel）的**核心资产**…` | `它是DNA内核（DNA Kernel）的**核心资产**…` |

---

## 三、④ 差异是否为 v0.332 已声明的「+817 增量」所应然包含？—— **否**

**+817 的构成（`BASELINE §68.5` ＋ 本件实测）**：`合订本 6992 → 7809`；组3（`d483c00`）对合订本 `--numstat` ＝ **＋1360 / −543 ⇒ 净 +817**，全部来自**新增 3 个区块**（`2.1c`／`2.1d`／`2.1e`）。
⇒ **+817 是"新增"的量**，其对应差异形态应为 **`insert`（＋行）**。

**本件实测的形态与之不符**：

| 检验 | 实测 |
|---|---|
| 90 处差异的 opcode 类型 | **全部为 `replace`（同位置等量替换）** —— **`insert` = 0、`delete` = 0** |
| 16 个差异区块的行差 | **全部为 0** |
| 差异方向 | **快照＝旧名／掩码占位符，底图＝新名／原文** |

⇒ **结论（明确否定）**：这 90 处差异 **不是 +817 增量所应然包含的**；它们是**另一件事** —— **v0.331（`ca68e5f`「组1 裁定A 命名族统一：底图全量同步」）只改了底图，未回溯到合订本**。

**证据链（三段，均可复现）**：
1. `ca68e5f` 的 `--shortstat` ＝ **25 files changed, 158 insertions(+), 158 deletions(-)** ⇒ **等量替换**，且 **改动文件全部在 `docs/` 与根 `README.md`**，**未含 `discussions/` 的合订本**。
2. **合订本 旧名计数在组3 前后基本不变**：`云操作系统` **28 → 29**、`空天浏览器` **47 → 49**（`git show <sha>:<合订本> | grep -o`）⇒ **组3 未对既有区块做命名同步**（其 543 行删减来自**单块重生成** ＋ **新增 3 区块的装配**）。
3. 反例对照（**证明检索式有效**）：**被组3 重新生成过的 `LAYER_DETAILS` 块 → ✅ 同步（改 0）**；**未重生成的 15 块 → 命中 A 类差异**。⇒ **"同步/滞后"与"是否重生成"严格对应**，非随机噪声。

⇒ **台账覆盖缺口（登记）**：`BASELINE §68.5` 的 `+817` 与「7 区块逐字保真 PASS」**只覆盖"新增 3 区块"与"那 7 块"**，**未覆盖"其余 15 块的命名同步滞后"** ⇒ 与 P3-1 §四-3 同族（**命名同步的"覆盖面登记不全"**）。

---

## 四、⑤ 一句话结论 ＋ 需重生成的区块清单

> **一句话结论**：**合订本已与底图不同步** —— 39 个收录区块中 **16 块存在行内差异（90 处，全部可归因：命名同步滞后 70 处 ＋ `R25` 掩码 20 处）**，**源起于 `v0.331`（`ca68e5f`）底图全量命名同步未回溯合订本**；**`v0.332` 的 `+817` 只解释了"新增 3 区块"，不解释这 90 处**。⚠️ **本件只判定、不重生成**（`discussions/` 为历史快照，重生成属白天决策）。

**需重生成的区块清单（15 块 · 均有 A 类差异）**

| # | 区块 | A 类处数 | 备注 |
|---|---|---|---|
| 1 | `COSMIC_COMPUTING.md`（1.3） | 10 | — |
| 2 | `DIAGNOSIS_VALIDATION.md`（8.3） | 7 | — |
| 3 | `PERFORMANCE_REPORT.md`（8.4） | 7 | 同块另有 **1 处 B 类掩码**（**重生成须保留掩码**） |
| 4 | `SELF_DIAGNOSIS_REPORT.md`（9.1） | 7 | 另 1 处 C 类（加粗变体） |
| 5 | `L6_DESIGN.md`（5.3） | 6 | — |
| 6 | `QUANTUM_CLOCK_DESIGN.md`（5.6） | 6 | 同块另有 **1 处 C 类**（表述统一） |
| 7 | `L5_DESIGN.md`（5.2） | 5 | — |
| 8 | `CONTRAST_REPORT.md`（1.2） | 4 | — |
| 9 | `FIELD_PRESENTATION_DESIGN.md`（5.3） | 3 | — |
| 10 | `WORK_CONSOLE_PLAN.md`（7.4） | 3 | — |
| 11 | `GENE_LIBRARY_DESIGN.md`（4.1） | 3 | **组1 已改底图 ⇒ 本块属"本轮新增滞后"** |
| 12 | `L7_EXECUTION_DESIGN.md`（5.4） | 2 | — |
| 13 | `FIELD_REPAIR_DESIGN.md`（5.5） | 2 | — |
| 14 | `FULL_AUDIT_REPORT.md`（9.2） | 2 | 同块另有 **16 处 B 类掩码**（**重生成必须保留 `R77` 掩码**，否则 `R25` 由 1 份扩为 2 份） |
| 15 | `SILA_IMPLEMENTATION.md`（5.4） | 1 | — |

**无需重生成（24 块）**：其中 **23 块全维同步**；**`L5_VALIDATION_REPORT`（8.2）** 属**纯 B 类掩码差异（3 处）** ⇒ **同步，非滞后**（⚠️ **若按行 diff 裸判会误列** ⇒ **必须扣除掩码**）。

---

## 五、附带发现（只登记不改）

**发现-1（`R78` 自身规则 vs `discussions/` 快照身份 · **张力**）**
- 合订本 §五 **`R78`** 明写：**"本合订本与四批正文都是底图的副本 ⇒ 底图变更后必须重生成"**，并称"**同步判据 ＝ 逐字自检脚本（比对各源当前内容）——它本身就是『汇编 ↔ 底图』的同步判据**"。
- 而本夜清单 **P3-2 风险提示**写：**合订本属 `discussions/`（历史快照）⇒ 只读、不写入、不重生成**。
- ⇒ **两处口径相抵**：**该件自己要求"必须重生成"，而现行夜间口径要求"不重生成"**。**登记待裁**（属"历史快照是否随底图重生成"的边界问题）。

**发现-2（`R86` 家族 · 自检块自身过期）**
- 合订本 **§五 覆盖范围**声称：**收录 34 份 ＋ 未收录 1 份（`REFERENCE_APP_DESIGN.md`）＝ 35**；**附录 B** 声称 `docs/` **35/35 全部落位**（**含 `REFERENCE_APP_DESIGN.md`** ⇒ **与本文件 §五 自相矛盾**）。
- **实测**（本件逐条抽取 `## … — \`docs/X\``）：**收录 39 份**；**未收录 4 份** ＝ `DNA_KERNEL_DESIGN`／`INCARNATION_DESIGN`／`QUANTUM_CLOCK_DESIGN_DETAIL`／`SELF_DIAGNOSIS_FRAMEWORK`；`docs/` 实 **43 份**；`REFERENCE_APP_DESIGN.md` **确实已收录**（落位 **5.2-b**，93 行、✅ 同步）。
- ⇒ **§五 自检块与附录 B 均已过期，且互不一致** ⇒ **两份"对账用"表格本身不可用于对账**（`R86` 家族第 2 例）。

**发现-3（附录 B 的行数列与实况不合）**
- 附录 B 行数抽样 vs 实际：`VISION` **75 → 86**｜`LAYER_ARCHITECTURE` **450 → 343**｜`LAYER_BASEMAP_L0_L6` **686 → 690**｜`MATH_SPEC` **130 → 156** ⇒ **附录 B 行数列为旧快照**（且 `REFERENCE_APP_DESIGN` 落位标 5.2，实为 5.2-b）。

**发现-4（`R77` 掩码机制为"设计行为" · 需在对账里显式扣除）**
- 合订本 §五 `R77` 明写：收录 `FULL_AUDIT_REPORT.md` 时**所有收录行统一掩码**，且**掩码正则直接 `import` 判据**（同源 `C18`）。
- ⇒ **"汇编 ↔ 底图"的逐字比对天然存在 20 处掩码差异** ⇒ **任何**此类对账**必须先把掩码折算成通配**，否则会产出 **20 处假红**（**本件为第 2 次实证**，与 09-20 「掩码差异 ≠ 过期差异」同族）。

**发现-5（口径微差 · 仅登记）**
- `ca68e5f` 提交信息写"**底图全量同步 225 处/27 文件**"，而 `--shortstat` 实测 **25 files changed** ⇒ **文件数口径差 2**（**未穷究成因，不判定**）。

---

## 六、验收对照

| # | 验收标准（清单 P3-2 原文拆解） | 实测 | 结论 |
|---|---|---|---|
| 1 | ① 按区块围栏切分 **7 个区块**，逐区块取"快照行数" | **实切 39 区块**（**收录段**口径）；`§68.5` 所称 **7 区块**（86/343/690/626/276/232/281）**7/7 复核通过** | ✅（**含口径澄清**：7 ≠ 全量，7 是 §68.5 点名的那 7 块） |
| 2 | ② 与底图行数比对（`LAYER_DETAILS` 626 已实测；其余六件同理） | **39 区块全部逐份比对**；**`LAYER_DETAILS` = 626 ✅**；**7 件 7/7 ✅**；**行差全 0** | ✅ |
| 3 | ③ **逐行 diff**（`difflib`），输出**差异行清单**（新增／删除／改写） | **39 区块逐行 diff**；差异 **90 处**＝**改写 90 ／ 新增 0 ／ 删除 0**；三分类见 §二 | ✅ |
| 4 | ④ 判定差异**是否为 `+817` 所应然包含** | **否**（`+817` 是新增量；90 处全是 `replace`）＋ **三段证据链** | ✅ |
| 5 | ⑤ "**是否仍与底图同步**"一句话结论 ＋ **需重生成区块清单** | **不同步**；**需重生成 15 块**；**无需 24 块**（含 1 块纯掩码） | ✅ |
| 6 | **合订本属 `discussions/` ⇒ 只读、不写入、不重生成** | 全程只读；**未改合订本一字**；本件仅新增报告 | ✅ |
| 7 | 报告落 `coordination/reports/` | 本件 | ✅ |

---

## 七、未完成项

| # | 未完成 | 原因 |
|---|---|---|
| 1 | **未重生成合订本** | **明令排除**（历史快照只读）⇒ 15 块的"修复"**只在清单里，未落码** |
| 2 | **未做"重生成后"的保真度回归** | 依赖第 1 项；**属白天决策后动作** |
| 3 | **未穷究 `ca68e5f` "27 文件 vs 实测 25" 的成因** | `R32` 教训 ⇒ **只登记数字，不判定** |
| 4 | **B 类 20 处未做逐处"是否全部为有意掩码"的定性** | 判据同源方在 `check_private_pointers.py::LEAK_PATTERNS`；本件用形态匹配（`〈…〉` vs 原文）判定 ⇒ **未与判据实现逐行比对** |
| 5 | **未纳入"是否把本对账固化为常设判据"** | 属 P1 待裁（涉新判据） |

---

## 八、真实缺陷

> 口径：**只登记不改**。

1. **汇编 ↔ 底图不同步（`R78` 自身规则被违反）**：16/39 区块存在行内差异；**需重生成 15 块**；**根因＝命名同步轮未回溯合订本**。
2. **`R86` 家族第 2 例**：合订本 **§五 覆盖范围块** 与 **附录 B** **双双过期且互相矛盾**（35 vs 39 收录；`REFERENCE_APP_DESIGN` 既"未收录"又"已落位 5.2-b"）。
3. **方法学缺口**：**"行数比对"对等量替换型改动 100% 失明**（16 块行差全 0）⇒ 任何"以行数判同步"的做法都不可靠。
4. **台账覆盖缺口**：`§68.5` 的 `+817`／"7 区块 PASS"**未覆盖其余 15 块的滞后**。

---

## 九、三量台账（C6）

| 量 | 内容 |
|---|---|
| **存量** | 合订本 **7809 行**全文；**39 区块**逐块快照行数 ＋ 逐行 diff；`docs/*.md` **43 份**；`§68.5` 的 7 行数锚；`ca68e5f`／`d483c00` 两个提交的 `--shortstat`／`--numstat`；合订本旧名计数（两个 sha × 4 词） |
| **变量** | **0** —— 未改任何既有文件（合订本、底图、治理文件、代码、CI 一律未动） |
| **补充增量** | **1 份**：本报告 |
| **创新增量** | **3 条**：① ★ **"行数比对对等量替换失明"的实证**（16/16 行差为 0 却内容不同）⇒ 方法级结论；② ★ **`+817` 与 90 处差异的"归因切割"**（`insert` vs `replace` 判据）⇒ 把"台账声明"与"实际滞后"分开；③ ★ **`R25` 掩码的"假红折算"**（`〈…〉` → 通配）**第 2 次实证**，并首次给出"**纯掩码差异块不算滞后**"的判定口径（`L5_VALIDATION_REPORT` 案） |

---

## 十、边界说明（C5；四类齐全）

- **本地跑过** ✅：**三个仓库外探针**（区块切分＋行数对账／命名归因／三分类）全部**本机实跑可复现**；`git show <sha>:<path>`／`--shortstat`／`--numstat`／`grep -o`／`wc -l`；合订本与底图的逐行 `difflib`（**39 区块 × 2,300 余行**）；判据在 P3-1 已复跑 **15/15 rc=0**。
- **CI 过** ➖：**无** —— 只读任务**未触发 CI**；按 `C20` **CI 结论不入库**。
- **真实跑过** ❌：**无** —— 未跑 QEMU、未上真机、未构建。
- **未验证** ❌（逐条）：
  1. **未重生成合订本** ⇒ "重生成后是否即 100% 同步"**未验**（属白天）。
  2. **B 类 20 处未与判据实现逐行比对**（用形态匹配代替）。
  3. **C 类 2 处未逐处追溯到具体裁定条文**（只归入"命名同步族"）。
  4. **未查 `.gitignore`／提交历史之外**的合订本副本（若仓外另有副本，本件未覆盖）。
  5. **`ca68e5f` 文件数口径差（27 vs 25）成因未查**。

---

## 十一、决策评估（C11 七要素）

**本轮无新决策。** 本件为**只读对账**；"是否重生成合订本"**不由本件代行** —— 涉及 `discussions/` 历史快照的**改写**，属 **P1**（`C10` 第 4 项：覆盖／改写类）。§五 发现**只登记 ＋ 给建议**。

---

## 十二、需要用户决策（白天；本件只列，不催不动）

| 级别 | 事项 | 来源 |
|---|---|---|
| P1 | ★ **合订本 15 块是否重生成**（重生成＝改写 `discussions/` 历史快照 ⇒ `C10` 第 4 项） | 本件 §四 |
| P1 | ★ **`R78` 与"历史快照只读"的口径相抵**：**定谁优先**（① 合订本随底图重生成 ② 合订本冻结为快照、另立"现行汇编"件 ③ 维持现状＋登记差异） | 本件 §五-1 |
| P1 | **"汇编 ↔ 底图 逐行对账"是否固化为常设判据**（含**掩码折算**与**行数不可作判据**两条口径） | 本件 §八-3／§八-4 |
| P1 | **合订本 §五 自检块 ＋ 附录 B 的过期**是否随重生成一并修 | 本件 §五-2／§五-3 |
| P1 | **命名同步的"覆盖面登记"是否补全**（`§68.5` 台账未覆盖 15 块滞后） | 本件 §三 |

---

## 十三、下一步建议

1. **白天先裁 §十二 第 2 行**（`R78` vs 快照）—— **这一条决定另外三条**（是否重生成／是否立判据／是否修自检块）。
2. 若决定重生成：**必须保留 `R77` 掩码**（否则 `R25` 由 1 份扩为 2 份）＋ **重生成后跑保真自检**。
3. **把两条方法口径写进规范候选**：① **行数相同 ≠ 内容同步**；② **掩码差异须折算，否则假红**。

---

**启动回执**：已读 `CHARTER.md`（机制 **1–30**／§四 速查 **C1–C21**／§五 启动回执／§六 C10）＋ `CONSTRAINTS.md`（**C10／C12／C13／C14 全文**）＋ `TEMPLATES.md`（§二 第〇项／§三 优先级／§十一 夜间自动化）。
当前状态：**v0.364**（`HEAD c448c68`＝本夜 P3-1 后）；未 push 数 ＝ 原 4 ＋ 本夜累积。
本次任务：**夜间 P3-2 · 汇编合订本 ↔ 底图 逐区块逐行对账**（**P3**，只读 ＋ 只写本报告）。
是否继续：**P3-3 起继续**（本夜不推送、不与远端同步）。
