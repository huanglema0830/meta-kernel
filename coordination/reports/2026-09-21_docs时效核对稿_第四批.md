【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# `docs/` 时效核对稿 · **第四批**（定向深核 · 2026-09-21 · v0.269）

> **性质**：**只出稿，不改文件**（用户 §五「只出稿，不改文件」）。
> **保密标记**（C12）：🔓 公开。⛔ 机密 **0**、🔐 保密 **0**（只用仓库相对路径）。
> **边界**：本稿**未运行任何程序**；"接线"判定＝**只读调用点检索**（项目范式：**"函数存在 ≠ 被调用" ⇒ 须直查调用点**）。

---

## 0. 批次口径（**第三批已覆盖全部 35 份 ⇒ 第四批转为「定向深核」**）

| 批次 | 覆盖 |
|---|---|
| 第一批 | 定向扫 **8 份** |
| 第二批（夜间 P3-1） | 未覆盖的 **9 份** |
| 第三批 | **剩余 20 份** ⇒ **35 份全部覆盖完毕** |
| **第四批（本稿）** | **无"未覆盖文件"** ⇒ 转为 **① 第三批遗留待核项取证；② 复审第二批结论（补漏）** |

**本批任务**（对应用户 §四／§五.1）：**主任务＝`L5_DESIGN` L172／L191 待核项取证**；**附带＝复审第二批对 `GENE_LIBRARY_DESIGN` 的结论**。

## 1. 主任务 · `docs/L5_DESIGN.md` L172／L191 **待核项**（结论：**未过期，成立**）

- **原文（L172）**：`⚠️ 边界：**L5 诊断主链尚未改为"从场景公式读取本底场"**（当前仍用 l5_baseline 数值地图）——下一步接线。`
- **原文（L191）**：`- **边界**：语境/场景公式模块已实现并测试；**L5 诊断主链与基因库的接线尚未完成**（下一步）。`
- **取证（只读）**：
  1. **`scene_baseline`／`scene_baseline_of` 的唯一实现处＝`l5_context.rs`**（两 crate 各一份）；**全仓检索其调用点 ⇒ 除 `l5_context.rs` 自身（含其单元测试）外，命中 0**（非 `.rs` 命中均为**汇编副本/报告引述**，非调用）。
  2. **L5 主链入口签名**：`meta-kernel-core/src/l5_diagnosis.rs:192` ⇒ `pub fn diagnose(s: &[f64; 7], base: &BaselineField, object: &str) -> Diagnosis`；同文件 **第 16 行** `use crate::l5_baseline::BaselineField;`。
  3. ⇒ 主链**仍以 `l5_baseline` 提供的 `BaselineField` 为入参**；**未见**"由语境 → 场景公式层取本底场"的调用链落点。
- **判定**：**L172／L191 与代码实况一致 ⇒ 未过期**（**不需要改**）。
- **可选增强（非必须）**：可在 L172 加一句锚，写明"`scene_baseline()` 已于 v0.107 实现并测试，**缺的是调用点**" —— 使"已实现"与"未接线"两个事实**同处可见**（防读者误读为"模块不存在"）。

## 2. ★ 附带发现（**新**）· `docs/GENE_LIBRARY_DESIGN.md` L245 的「**已接线**」**缺调用点支撑**

- **原文（L245）**：`| 第 2 层 场景公式（场景层） | ✅ **已实现 + 已接线** | L5 经 \`l5_context::scene_baseline\` 按语境取本底场（未命中则建立并登记） |`
- **代码证据**：如 §1 所述 —— **`scene_baseline` 无外部调用点**。按本项目范式（**"函数存在 ≠ 被调用" ⇒ 断言须直查调用点**），**"已接线"这一表述缺乏调用点支撑**。
- **文档间矛盾（C19 同族）**：

| 事实 | 一处 | 另一处 |
|---|---|---|
| L5 是否已接场景公式本底场 | `GENE_LIBRARY_DESIGN` L245 ⇒ **✅ 已接线** | `L5_DESIGN` L172/L191 ⇒ **尚未接线（下一步）** |

- **`L5_DESIGN` 与代码一致 ⇒ 应以「**已实现、主链调用点待接线**」为准。**
- **建议（待裁 · **改底图须授权**）**：把 L245 的状态列改为
  `✅ **已实现**（\`scene_baseline\`／\`scene_baseline_of\`，含单测）；**主链调用点待接线**（见 \`L5_DESIGN\` §7 边界）`。
- **⚑ 复审第二批（补漏）**：第二批（`…P3-1_docs时效核对稿第二批.md`）曾把 **§8 L245 当作"实测事实"引用**（其报告内明写"实测事实：§8 L245＝…"）⇒ **该项被当作依据、未被当作过期项**。**本批指出：该"事实"本身就是待订正项**（补第二批之漏，R82／R79 同族）。

## 3. 5.2 `advisor_brief.md` 复扫（**无新增过期项**）

- **对象**：`coordination/advisor_brief.md`（**280 行**）。
- **方法**：风险词全文匹配（`未实现｜尚未｜待实现｜剩余｜计划实现｜将实现｜已实现`）＋ 逐条回到上下文判活跃/历史。
- **结果**：命中 **6 行**，**全部为历史段或已带后记锚**：
  - L85 ＝**已有后记**（"23:00 开工已于首夜发生 ✅"）；
  - L131／L135／L137 ＝**登记类引述**（"无剩余片"／R33/R34/R35 的**当时口径**）；
  - L175 ＝**盘点结论**（"其余类别尚未分配编号"—— 属**当时事实陈述**，与 `TERMS.md` 现况一致：TERM-006…009 为 2026-09-21 新增，**该行指"换壳试点"类别**，不再精确）；
  - L198 ＝**D40 已确认并落地**（内容与现况一致）。
- **判定**：**无新增过期项** ⇒ **6.1／5.2 视为收口**（沿用上轮结论）。
- **备注（低风险，供参考）**：L175 的"尚未分配编号"若与 `TERMS.md` §一之二 并读，**可读作**"TERM-006…009 未分配" —— 建议**将来**在 L175 加半句限定（"指**换壳试点**类别"），**本轮不改**。

## 4. 边界说明（四类）＋ 只出稿声明

| 类 | 结果 |
|---|---|
| **本地跑过** ✅ | 只读检索实跑：`grep -rn "scene_baseline"`（全仓、排除 `.git`/`target`）｜`ls` 模块定位｜读取 `l5_diagnosis.rs` 入口签名 |
| **CI 过** | **不适用**（本稿不改文件） |
| **真实跑过** | **不适用**（未运行 L5／网关） |
| **未验证** ⚠️ | ① **"已接线"的判定基于"调用点检索"**（**静态**）；若存在**动态派发/宏生成**的调用，本检索会漏 ⇒ 置信度＝**高但非穷尽**；② §2 的订正**未落地**（待授权）；③ 第四批**不含"未覆盖文件"**（35 份已全覆盖） |

> **只出稿声明**：本稿**未改 `docs/` 任何一份、未改治理文件、未提交**。
