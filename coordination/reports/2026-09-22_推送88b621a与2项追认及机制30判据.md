【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · push `88b621a` ＋ 2 项追认 ＋ 机制 30 机器判据（v0.319）

## ① 保密标记

🔓 **公开** —— 本轮**零机密内容**：只涉及**机制编号／字节数／判据源码**，全部为仓库内公开文件与工作区结构信息；**未触及任何密级资产**（`⛔机密` 零出现）。

## ② 全景状态卡

| 项 | 值 |
|---|---|
| **阶段** | **阶段二 · 2.4 边界层已验** |
| **基准 → 收口** | `count 327`（实测一致）⇒ **329**（**2 个本地 commit**：`ee30855` 判据／本报告） |
| **push** | `6b8afd5..88b621a`（**exit 0**）｜`ahead 0` |
| **CI 状态** | **全绿**（**三 job 全 success**；★ **run 号与逐 job 三态按 C20 只记日报、不入库**） |
| **本轮性质** | 放行 ＋ 2 项追认 ＋ 机制建设（**1 个新判据**） |

## ③ 任务（含基准／定位）

**指令**：`push` ＋ **2 项裁定落地** ＋ **机制 30 加机器判据**。
**总规划定位**：放行 ＋ 裁定落地 ＋ 机制建设。
**基准**：`count = 327`（`git rev-list --count HEAD`）—— ★ **实测与指令一致**（**本轮无基准偏差**）。

**★ 本轮读了 `coordination/` 哪几份、哪几行**（**底图零读取，如实记**）：
| 文件 | 行／范围 | 用途 |
|---|---|---|
| `coordination/CHARTER.md` | **L109**（机制 30 整行）｜**L243**（§九 记忆三层行）｜§三 机制表区段（编号 1–30） | 判据指针落点 ＋ 连续性核对 |
| `coordination/tools/check_source_of_truth.py` | **全文 259 行**（**L46–48 `find_repo`／L229–235 记忆路径解析／L70–72 判据 B `--ci` 降级**） | ★ **同源先例**：路径怎么找仓库外记忆 ＋ CI 怎么降级 |
| `coordination/tools/check_report_structure.py` | **全文 328 行**（**L158–247 自检十侧／L273–323 只告警主流程**） | 判据**写法约定**（只告警／自检侧数／打印口径／`repo_root`） |
| `coordination/BASELINE.md` | **L4055–L4125**（§五十四 全节） | 上轮留痕与 C15 基准 |
| `.github/workflows/ci.yml` | 判据调用点 grep（**L212–L395**） | 确认**既有 10 判据入 CI 的写法** ＋ 本判据**未入 CI** |
| **底图**（根 `README.md` ＋ `docs/`） | **0 份／0 行** | ★ **如实**：本轮任务为**机制判据建设**，**不涉底图**；**判据 F PASS** 佐证底图未变 ⇒ **未触发底图读取** |

## ④ 状态：完成

**完成**（〇 push ＋ §一／§二 两项追认 ＋ §三 机制 30 判据落地；**验收 7 条全部达成**）。

## ⑤ 验收对照

| # | 验收标准 | 结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（T-038 门控） | ✅ `6b8afd5..88b621a`（**exit 0**）；**三 job 全 success**；门控证据 **`READY_TO_EDIT`**（`##[error]` **0 行**） |
| 2 | 三层落点维持工作区，记录到 BASELINE | ✅ **§55.1**（**追认** ＋ 理由 ＋ **明文禁止动跨项目记忆**）；★ 同步 `CHARTER` 机制 30 行「▲ 待用户复核」→「★ 用户裁定：维持工作区」（**不留过期待办**） |
| 3 | `MEMORY_详情.md` 保留不动，记录到 BASELINE | ✅ **§55.2**（**实测 2696 B，本轮零改动**） |
| 4 | 机制 30 加机器判据（先只告警），自检正反对照 | ✅ **§55.3**；`check_memory_layers.py` **自检十侧全 PASS**；**实跑告警 0** |
| 5 | CHARTER 机制 30 条目加判据指针 | ✅ **L109** 行「机器判据：**暂无**」→ **本判据指针**（含**效力范围 ＋ 未入 CI 声明**） |
| 6 | C15 复算通过，编号表连续无缺号无重号 | ✅ 机制 `1..30`／`T 1..47`／节号 `0..55` **连续无缺重**（**§55.4**） |
| 7 | CI 全绿 | ✅ 见 1 |

## ⑥ 未完成项

| 项 | 状态 | 原因 |
|---|---|---|
| **判据入 CI** | ❌ **未做** | **本轮指令未授权**（§三 口径只列"新建脚本／自检／只告警／记录 BASELINE"）⇒ **列待裁** |
| **"每轮跑全部判据"写进 `TEMPLATES §11.3`** | ❌ **未做** | **需改 TEMPLATES**（**本轮未授权**）⇒ **F-5 的治法**，**列待裁** |
| **机制 30 的"会话开始读调用层"行为判据** | ❌ **未做** | **行为不在文件里** ⇒ **无可机器判项**（**如实记**，非遗漏） |
| 内容层**正确性**校验 | ❌ **未做** | **判据只管"非空"**；正确性由 `T-046` 纪律 ＋ 人工守（**边界**） |

## ⑦ 真实缺陷

### ★ **F-5（本轮新立）：新判据「有判定、无动作、无触发」**

**现象**：`check_memory_layers.py` 落地即达标，但**既未入 CI**、**也未写进任何"每轮必跑"清单** ⇒ **没有任何东西会在对的时候把它跑起来**。

**为什么这是缺陷**（**同源**）：与 **`reports/2026-09-21_机制绑动作评估稿.md`** 的三判据（**绑动作／绑触发／绑判定**）**同族** —— 本判据**三有里只落"判定"一项**，**缺"动作 ＋ 触发"**；亦与 **INTEL 试用期观察项 A（"动作是否真绑"）** 同族。

**⇒ 若不补绑定，它会与"人工守"同样失效** —— 即**新造了一个"写了但不会跑"的判据**（**R83 空转 ／ R89「声明≠发生」**同族）。**这是本轮最有价值的发现**。

### 一处**代码自捕获（当场修）**

`_make()` 初版写 `if call is not None:` 再判一次 —— 而 `None` **已被上一行替换为默认值** ⇒ **死分支 ＋ 语义误导**（后人会以为传 `None` ＝"造缺文件"，实际是"用默认值"）。**已改为直接写并写明参数语义**（缺／空场景须显式 `os.remove` / 写空串）。

### 一处**环境类未验证（非缺陷）**

★ **`--ci` 降级分支在本机走不到**（本机记忆目录存在）⇒ 本机**只能验"分支语义"**（自检侧⑧⑨**手造"不存在目录"**），**"CI 上真会走到该分支"未验** —— **R70 家族：环境类差异**（与 v0.310 门控脚本"未传 `cwd`"同族）。

## ⑧ 三量台账

| 量 | 内容 |
|---|---|
| **存量** | **机制 30**（**不变**）｜**判据脚本 11 个**（**10 → 11**）｜**台账 55 节**（54 → 55）｜`T` **47**／`TERM` **14**／`C` **21**／`R` **90**／`D` **55**（**全 ±0**）｜**记忆三层**：调用层 **977 B**／索引层 **4020 B**／内容层 **4 份 4022・1288・3399・1706 B** |
| **变量** | `coordination/CHARTER.md`（**机制 30 行**：落点裁定 ＋ 判据指针）｜`coordination/BASELINE.md`（**新增 §五十五**，+68 行） |
| **补充增量** | ① **2 项追认入账**（§55.1／§55.2）② **`coordination/tools/check_memory_layers.py` 新增**（+356 行含台账）③ **1 份报告** |
| **创新增量** | ① ★ **"边界值双侧"自检**（**1024 ⇒ 告警**／**1023 ⇒ 不告警**；**4096／4095** 同理）—— 防"把 `<` 静默写成 `≤`"这类**口径漂移**（**两侧都验才叫验过边界**）② ★ **CI／非 CI 对照**（自检⑧⑨）—— **"目录不存在"必须与"合规"取不同值**（**R83／R89**）③ ★ **"效力范围"写进判据 docstring 与机制条目本身**（**不假装 CI 也管**）④ ★ **F-5：判据的"三有"体检**（把机制绑动作评估稿的口径**反过来自查新判据**） |

## ⑨ 边界说明

| 类 | 内容 |
|---|---|
| **本地跑过** | ✅ 新判据**自检 10/10 PASS** ＋ **实跑告警 0**（**读真实目录，非夹具**：6/6 份读到）｜**10 判据 `rc=0`**｜**C15 复算**（节号 `0..55` 连续、机制／`T` 连续）｜`CHARTER`／`BASELINE` 改动**逐处复核** |
| **CI过** | ✅ **三 job 全 success**（`test`／`宿主 field-render`／`元内核引导镜像`；**T-038 门控已执行**；**run 号按 C20 只记日报**）。★ **本判据未入 CI ⇒ 它在 CI 上无任何证据**（**不得因"CI 绿"而认为判据在 CI 被验过**） |
| **真实跑过** | ⚠️ **部分**：判据**读的是本机真实记忆目录**（非夹具）⇒ **判定逻辑在真实输入上跑过**；但 **`--ci` 降级分支**只在"目录不存在"时成立，**本机不可达** ⇒ **该分支只跑了手造场景，未在真实环境跑到** |
| **未验证** | ① **`--ci` 在真 CI 上是否会走到降级分支**（**本机不可达**）② **判据入 CI 后**的行为（**未接线**）③ **内容层正确性**（判据只管"非空"）④ **机制 30 的"会话开始读调用层"行为**本身**无判据** ⑤ **`R90`／`B5` 重试路径**仍未触发（**等真实抖动**）⑥ **`~/.workbuddy/MEMORY.md`（跨项目记忆，13 184 B）** 是否也需三层化 —— **不在本机制范围，两处均未动** |

## ⑩ 决策评估（七要素 · 本轮**新决策 1 项**：新判据是否入 CI）

| 要素 | 内容 |
|---|---|
| **评估** | 三案：**甲 入 CI**（`--ci` 只 warn）｜**乙 不入 CI、改纳"本机例行"**｜**丙 入 CI ＋ 把记忆快照入库后再判** |
| **优势** | **甲**：有 CI 留痕、与既有 10 判据齐整｜**乙**：**零噪声**，且**本机正是唯一能真判的环境**（判据效力本就在本机）｜**丙**：CI 上也能**真判**（唯一能绕开"降级分支"的路） |
| **劣势** | **甲**：CI 上**必然走降级分支** ⇒ 每轮只多一条"目录不存在" warn（**是噪声，不是判据**）｜**乙**：**无 CI 留痕** ⇒ 依赖人记得跑（**正是 F-5 的成因**）｜**丙**：**须把工作区记忆复制进 git** ⇒ 与「**内容在仓外、规则在仓内**」的载体设计**正面冲突**（且**项目状态与坑长期留痕**，**体积/隐私**双输） |
| **风险** | **甲**：**假安心** —— "判据进 CI 了"，而它**从未在 CI 上真判过任何东西**（**R89 同族**）｜**乙**：**静默失效**（记不住 ⇒ 不跑 ⇒ 等于没有）｜**丙**：**改变载体设计**，须先改机制 30；且记忆含**本机路径/坑**，入库即**永久留痕** |
| **机会** | ★ **乙 ＋ 机械绑定**：把本判据**并入 `TEMPLATES §11.3` 清单类任务**（"每轮跑全部 11 判据"）⇒ **同时补上"动作 ＋ 触发"** ⇒ **三有齐**，**F-5 当场治掉**，且**成本极低**（一行清单） |
| **建议** | **选乙**（**不入 CI**）**＋** 建议把"每轮跑全部判据（含本判据）"写进 `TEMPLATES §11.3`（**后者需授权改 TEMPLATES ⇒ 列待裁**）。★ **先例支持**：`check_report_structure.py` 立项时也是**先不入 CI**（次轮才接线），**本轮同样按"先观察、后接线"节奏**；但★ **区别**：`report_structure` 当时**已有入 CI 的价值**（CI 上能真判，`reports/` 在仓库内），**本判据则不能** ⇒ **不宜照搬先例**，**须按本判据的效力范围单独裁**（**不做"照抄式接线"**） |
| **等待用户决策** | ✅ **等**：见 ⑪ ①② |

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★★ **新判据是否入 CI**（**甲**入／**乙**不入／**丙**入＋快照入库） | **选乙（不入 CI）** —— 理由见 ⑩：**CI 上必然只走降级分支，入 CI 是噪声**；★ 若选甲／丙 请明示口径 |
| ② | ★★ **是否把"每轮跑全部判据（含本判据）"写进 `TEMPLATES §11.3`** | **荐写**（**补"动作＋触发"，治 F-5**）；★ **需授权改 TEMPLATES**（**本轮未授权**） |
| ③ | **内容层"正确性"是否机器化**（如 verbatim 校验和／逐段比对） | **暂不**（成本高、收益低；`T-046` 纪律 ＋ 人工守已够，且 v0.318 的三次自捕获均由**建设期自检**抓住） |
| ④ | **`MEMORY_INDEX.md` 每条是否须含指向内容层的路径**（可机器判） | **先观察**（本轮索引层**已含**指向；**先积 3 轮样本再立**，避免"立了就改"） |
| ⑤ | **`~/.workbuddy/MEMORY.md`（跨项目记忆，13 184 B）是否也三层化** | **本轮不议**（**属另一机制**；★ 若做，**须单独立机制 ＋ 单独授权**，**不得与项目记忆混用同一套文件**） |
| ⑥ | **push 放行**（本轮 **2 个本地 commit**，v0.319） | **放行**（**机制 23**：push 须当轮明示） |

## ⑫ 下一步建议

1. **若授权 ②** ⇒ 下一轮把"每轮跑全部 11 判据（含 `check_memory_layers.py`）"写进 `TEMPLATES §11.3` 清单类任务，**并在台账留痕**（**F-5 关闭**）。
2. **若授权 ① 选甲/丙** ⇒ 甲＝接线 `ci.yml`（**只 warn**）＋ 明确记"CI 上只验降级分支"；丙＝**先改机制 30 的载体设计**（**不建议**）。
3. **建议顺带把"判据的效力范围"列为机制登记字段**（**机制 30 本轮已试写**："效力范围＝本机 ＋ `--ci` 降级"）—— 供你裁是否推广到其余判据（**可机器化：判据脚本是否读仓库外文件 ⇒ 反推效力范围**）。
4. **`R90`／`B5` 重试路径**：**继续等真实抖动**（**追认，不人为造红**）。

## ⑬ 启动回执

已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD` 的输出 ⇒ 329**（**本报告所在提交的 count**，依 **`T-045`** 口径）；**起点 `count 327`，本轮 2 个本地 commit**。

---

## 附件 A · `B2`＋`C1` 告警行（固定一行）

**★ 六族告警（`C1` 载体 · 固定一行）**：**六族告警：本轮 3 条**（`C22`／`T-00`／`TERM-999`，**均为元引用**）**｜对比上轮 ±0**｜**真缺口 0 条**。

（口径：`check_id_set_diff.py` 实跑；**3 条全在 `A2` 豁免登记表**（现 **5 行**）；**剔除 6 层**）

---

## 附件 B · INTEL 状态表（附加表 · 依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| INTEL-001 工信部 AI 中小企业计划 | 观察中 | 组合：企业注册完成 ＋ 阶段三启动 | ✗ **未触发**（现处阶段二 · 2.4） | 无 |
| INTEL-002 GPT-6 安全风险 | 观察中 | 事件：L7–L10 落地后 | ✗ **未触发**（L7 设计完成／L8–L10 冻结） | 无 |

**J1–J4 自查**：J1 报告须含状态表 ✅｜J2 触发条件满足但状态未变 ⇒ 违规 —— **两条均未触发，状态保持"观察中"正确** ✅｜J3 待准备但无准备稿 ⇒ 违规 —— **两条均非"待准备"** ✅｜J4 准备中但无具体行动 ⇒ 违规 —— **不适用** ✅。

★ **机制 29 本轮无状态变更**（不虚触）。
