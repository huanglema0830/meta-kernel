【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · push `a78acf9` ＋ 轮次号机制定型（`T-050`）＋ L0–L6 专题与 `M6`／`M8` 逐条呈现

> ★ **本轮起按新条文 `T-050` 标名两口径**：**版本 `count=337`** ｜ **轮次 `v0.324`** —— **两者不等、必须标名**（`T-050` 规则 ③）。**版本号**＝机器事实（`git rev-list --count HEAD`，服从 `C21`）；**轮次号**＝工作会话序号（每轮递增，与 `count` 无关）。**本文两口径并列，便于比对**。

## ① 保密标记

🔓 **公开** —— 本轮**零机密内容**：只涉及**机制编号／约束编号／术语编号／判据源码／计数口径／L0–L10 规划正文**，均为仓库内公开文件。
⚠️ **本轮主动守住一条边界**：**密钥级别相关字面量（密钥本体／具体路径／指纹）一律不写**（`C12` 密钥专项级别，**⛔ 机密**）；**用户级记忆与项目记忆的落点只写"工作区"**，**不写本机绝对路径**（`C12` 第 3 条：具体路径属 ⛔）。

## ② 全景状态卡

| 项 | 值 |
|---|---|
| **阶段** | **阶段二 · 2.4 边界层已验** |
| **基准 → 收口** | `count 337`（`a78acf9`）⇒ **337**（**本轮改动未提交；收口待放行**） |
| **push** | `6cea56f..a78acf9`（**exit 0**）｜**本轮产出 1 批待放行**（**机制 23**：push 须当轮明示） |
| **CI 状态** | **全绿**（三 job 全 success；★ **run 号与逐 job 三态按 `C20` 只记日报、不入库**） |
| **本轮性质** | 放行 ＋ **轮次号机制定型（`T-050`）** ＋ **只读取证**（L0–L6 专题 6 项 ＋ `M6`／`M8`） |
| **INTEL** | 观察中 **2**／待准备 **0**／准备中 **0**／已实施 **0**／已归档 **0**（**本轮无状态变更**，见 附件 B） |

## ③ 任务（含基准／定位）

**指令（本轮）**：**〇 push 放行**（2 个 commit，先 push 再动文件，`T-038` 门控，`C20`）＋ **一、轮次号机制定型（选丙）**（`v` ＝轮次号／版本号＝`count`／先只告警／历史豁免；写入治理文件 ＋ 补 `T` 编号 ＋ 过 `C15` ＋ 记 `BASELINE`）＋ **二、L0–L6 专题 6 项逐条呈现（只读）** ＋ **三、`M6`／`M8` 逐条呈现（只读）** ＋ **四、本轮不派其他任务**。
**总规划定位**：放行 ＋ 裁定落地 ＋ 只读取证。
**基准**：`count = 337`（`a78acf9`）—— ★ **实测与指令一致**。

**★ 本轮读了底图哪几份、哪几行**（**如实记**）：

| 文件 | 行／范围 | 用途 |
|---|---|---|
| `coordination/CONSTRAINTS.md` | **C21 全节（行 449–495）** | 轮次号机制定型（`T-050`）的**关系声明** ＋ **交叉引用行落点**（`C21` 只新增行、不放松） |
| `coordination/TEMPLATES.md` | **§7.2 `T-048`–`T-049` 行**（**行 380–382**）／**§十九 全文（行 1016–1058）** | **`T-050` 行落点** ＋ **§二十 落点**（**照 `T-049` 的体例**） |
| `coordination/BASELINE.md` | **§五十九 全节（行 4489–4606）**／**§六 缺陷表 `R91` 行（行 135）**／**§59.3 表（行 4530–4539）** | C15 基准 ＋ **`§五十九` 体例**（本节照其写）＋ **发现 `§59.3` 第 6／7 项与源清单不符** |
| `coordination/reports/2026-09-22_L0-L6专题_规划稿7项涉底图部分.md` | **全文（144 行）** | **L0–L6 专题 6 项**（**§六 待确认**）**逐条呈现** |
| `coordination/reports/2026-09-22_规划稿7项待确认清单.md` | **全文（148 行）** | **核对 L0–L6 6 项的"源"** ＋ **发现 `§59.3` 第 6／7 项不符** |
| `coordination/discussions/2026-09-19_L0-L6修正说明.md` | **§六 待裁问题（行 185–196）**／**§七 `M1–M6` 裁定落地（行 200–230）**／**附 A 边界（行 232–237）** | **`M6`／`M8` 逐条呈现**的**源**（问题／倾向／剩余动作） |
| `coordination/reports/2026-09-22_轮次号机制方案稿.md` | **全文（130 行）** | 轮次号机制**可落地形态**（§二／§三 判据 P1–P3 ＋ 正反对照） |
| `coordination/reports/2026-09-22_云内核完整规划与行动路线图.md` | **§附 7 行（行 426–436）** | **核对"7 项"的源清单**（第 6／7 项＝数值时点化／本稿未做） |
| `coordination/intel/INDEX.md` | **全表** | 附件 B INTEL 状态表 |

> **底图（`docs/`）本轮零读取、零改动**（**如实记**）—— 本轮**不动底图**；末尾 §5.2 四步连带**本轮未执行**。⚠️ **上表均为 `coordination/` 内部文件，非底图**。

## ④ 状态：完成

**完成** —— **〇 push ＋ 轮次号机制定型（`T-050`）＋ L0–L6 专题 6 项 ＋ `M6`／`M8`** 均已办；**无截断、无未收部分**；★ **只读取证项 3 条新发现已如实登记（见 ⑦）**。

## ⑤ 验收对照

| # | 验收标准 | 结果 |
|---|---|---|
| 1 | 〇 push 完成，CI 三 job 全绿，`T-038` 门控 | ✅ `6cea56f..a78acf9`（**exit 0**）；**`READY_TO_EDIT`**（`gh run list` **completed**；**三 job 全 success**；`##[error]` **0 行**；**报告结构门禁 step 真跑＝YES**）；★ **push 先于任何文件改动**（**时序已核**） |
| 2 | 轮次号机制定型，写入治理文件，`T` 编号补记 | ✅ **`T-050`**（`TEMPLATES.md` **§7.2 行** ＋ **§二十 正文**）＋ `CONSTRAINTS.md` **C21 交叉引用行**（**只新增**）；**过 `C15`**（`T` **49→50**、`TEMPLATES` 顶层节 **25→26**） |
| 3 | L0–L6 专题 6 项逐条呈现 | ✅ **6 项齐**（专题稿 **§六 待确认** ①–⑥；**逐项给** 编号／问题／选项／建议 ＋ **七要素指向**）—— 见 ⑥ 说明 ＋ ⑧ |
| 4 | `M6`／`M8` 逐条呈现 | ✅ **逐条齐**（**编号／问题原稿／源稿状态／剩余动作** ＋ **施工前置** ＋ **口径不一如实标注**）—— 见 ⑧ |
| 5 | 只读，不改文件（除轮次号机制授权项） | ✅ **改动仅 3 文件**（`TEMPLATES.md`／`CONSTRAINTS.md`／`BASELINE.md`），**均在"轮次号机制"授权内**；**`docs/`／`CHARTER`／`.github/`／`tools/` 零改动**；**L0–L6 与 `M6`／`M8` 只读取证**（**未改**） |
| 6 | `C15` 复算通过，编号表连续无缺号无重号 | ✅ 机制 `1..30`／`C 1..21`／`T 1..50`／`TERM 1..14`／`R` 去重 `1..91`／节号 `0..60` **连续无缺重**（见 §60.6） |
| 7 | CI 全绿 | ✅ 三 job 全 success（★ run 号按 `C20` 只记日报） |
| 8 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告字面第 0 行＝固定文本（**判据 `check_reading_instruction` PASS**） |

## ⑥ 未完成项

| 项 | 状态 | 原因 |
|---|---|---|
| **`check_version_label.py` 实现** | ⬜ **未做（只出方案稿）** | **用户裁定"先只告警"**，且**实现属另一步**（**须实现轮次号取数 ＋ 历史豁免名单**）⇒ **本轮只定型条文** |
| **L0–L6 专题 6 项"裁定"** | ⬜ **待裁** | **本轮任务＝"呈现"**（**只读**）；**裁定权在用户**（**涉底图回写** ⇒ 专项裁定） |
| **`M6`／`M8` 裁定** | ⬜ **待裁** | **本轮任务＝"呈现"**；**属决策类**（**不涉底图**，可直接处理，但须用户拍板） |
| **`§59.3` 表第 6／7 项更正** | ⬜ **未做（只读取证）** | ★ **越出本轮授权**（"只读取证"）⇒ **只登记、不改**（**见 ⑦ 发现 2**） |
| **"每轮跑全部判据"的机器强制** | ⬜ **未做** | **`T-048` 靠运行器 ＋ 报告承载**，**无 CI 兜底**（**如实边界**） |

## ⑦ 真实缺陷

**★ 本轮发现 3 条"台账/口径"问题**（**均在"只读取证"范围内 · 本轮不修 · 如实登记**）：

| # | 发现 | 性质 | 处置 |
|---|---|---|---|
| **1** | **`M6` 状态两处不一** —— 源稿 `2026-09-19_L0-L6修正说明.md` **§七** 记 **"2026-09-19 用户裁定：选乙"**（L4 并列两通道，**登记未施工**）；而 **专题稿 §5.1** 与 **`BASELINE §59.3` 第 6 项** 均记 **"`M6`／`M8` 未裁"** | **口径不一**（**`C19` 家族**：同一对象两种口径） | **只读登记**；**请用户裁定统一**（**未擅改**） |
| **2** | **`BASELINE §59.3` 表第 6／7 项与源清单不符** —— 源清单（`reports/2026-09-22_规划稿7项待确认清单.md`）**第 6 项 ＝ `count` 数值时点化**、**第 7 项 ＝ "本稿未做"**；而 **`§59.3` 表** 记为 **第 6 项 ＝ `M6`／`M8`**、**第 7 项 ＝ 新判据是否入 CI**（且**同表合计行仍写"不涉 3 项（第 1／6／7）"**）⇒ **表内自相矛盾**（**上一轮引入**） | **台账错误**（**表内自相矛盾**） | **只读登记**；★ **本轮不改**（**越出授权**）⇒ **待裁定更正** |
| **3** | **`M6` 同名不同域** —— 本仓库另有 **`M6` ＝ 里程碑"可安装到硬盘"**（`reports/2026-09-17_云内核总纲+规划+行动路线图_全量汇编.md` 行 254），与本节 `M6`（**`L0–L6` 修正序号**）同名 | **命名冲突**（**`C19` 家族**的同名不同域型） | **只读登记** |

> ★ **本轮 0 条"实现类"新缺陷**（**无代码/构建缺陷**）；★ **3 条均为"文字/台账"级**，**如实记、不静默**。

## ⑧ 三量台账

| 量 | 内容 |
|---|---|
| **存量** | **机制 30**（不变）｜**`C` 21**（不变）｜**`T` 50**（49→50）｜**`TERM` 14**｜**`R` 去重 55／最大 91**（本轮 ±0）｜**判据脚本 11 个**（**只跑不改**；`check_version_label.py` **仍只出方案**）｜**台账 60 节**（59→60）｜**`reports/` 153**（152→153）｜**记忆三层**：调用层 **977 B**／索引层 **3931 B**／内容层 **4 份** |
| **变量** | `coordination/TEMPLATES.md`（**§7.2 `T-050` 行 ＋ 新增 §二十**）｜`coordination/CONSTRAINTS.md`（**C21 交叉引用行 · 只新增**）｜`coordination/BASELINE.md`（**新增 §六十**） |
| **补充增量** | ① **轮次号机制定型**（`T-050` ＋ §二十 ＋ `C21` 交叉引用）② **`BASELINE §六十`**（push ＋ 定型 ＋ 6 项 ＋ `M6`／`M8` ＋ 3 条新发现 ＋ C15）③ **本轮报告（十四项）** ④ **L0–L6 专题 6 项 ＋ `M6`／`M8` 逐条呈现**（**只读**） |
| **创新增量** | ① ★★ **轮次号与版本号正式分家** —— 把**始终混在一起的两个量**（**叙事序号 vs 机器事实**）**显式切开**，**两者各自可自证**；**`C21` 不放松**（**只把 `v` 前缀划归轮次号**）② ★ **"先只告警 ＋ 转判红条件（连续 5 轮为 0）"** —— 把**新判据的启用**从"人拍板"变为**可观测条件驱动**（**同 `T-048` 立法思路**）③ ★ **只读取证产出 3 条台账问题**（**其中 1 条为上一轮自引入的表内矛盾**）—— **体现"回读即审计"** |

## ⑨ 边界说明

| 类 | 内容 |
|---|---|
| **本地跑过** | ✅ **`C15` 复算**（编辑前／中／后 **三次取数**，差值符合预期）｜**11 判据全跑**（经 `run_all_checks.py`，**读真实仓库**）｜**门控实跑**（`READY_TO_EDIT`） |
| **CI过** | ✅ **三 job 全 success**（`test`／`宿主 field-render`／`元内核引导镜像`；**`T-038` 门控已执行**；**run 号按 `C20` 只记日报**） |
| **真实跑过** | ⚠️ **部分**：11 判据**读真实仓库**跑过；但 **`migration_fidelity` 跑的是 `--selftest`（造例）**，**未对真实迁移文件跑**（**本轮无迁移**）⇒ **非全真** |
| **未验证** | ① **`check_version_label.py` 实现**（**未实现**，只定条文）② **轮次号取数规则**（**"每轮递增"的人工判定边界**未机检）③ **L0–L6 6 项／`M6`／`M8` 的裁定后果**（**待用户裁**）④ **`§59.3` 表第 6／7 项更正**（**未做**）⑤ ★ **底图回写的四项连带（判据 F ／ 汇编重生成 ／ 机制 25 P3 ／ 行号复核）本轮均未执行**（**因本轮"只读取证、不改底图"**） |

## ⑩ 决策评估（七要素 · 本轮**新决策 1 项**：轮次号机制的定型方式）

| 要素 | 内容 |
|---|---|
| **评估** | 三案：**甲 维持现状**（`v` 号继续与 `count` 脱钩、只观察）｜**乙 回正为 `count`**（把 `v` 号改回 `count`，含历史回填）｜**丙 另立"轮次号"**（`v` 号＝轮次号，与版本号**分家**，各自成文）—— **用户本轮已选"丙"** |
| **优势** | **甲**：零风险、零改动｜**乙**：一次性对齐 `C21`｜**丙（采纳）**：**最诚实** —— 现状事实上就是"轮次号"；**两条口径各自能自证**（**版本＝机器事实、轮次＝叙事序号**） |
| **劣势** | **甲**：漂移继续（本报告 `v0.324` ↔ `count 337`，差 **−13**）｜**乙**：改元规则 ＋ 改写历史陈述（**与 `C19`"历史只新增不改写"冲突**）｜**丙（采纳）**：**新增一套编号语义**（**须防登记不严又漂**） |
| **风险** | **甲**：读者据 `v` 号做算术 ⇒ 结论错｜**乙**：动了不该单方动的东西（元规则 ＋ 历史）｜**丙**：**两套号并存 ⇒ 引用更易混**（**已由规则 ③"必须标名、不得混写"封口**；残余风险＝**执法靠自觉 ＋ 判据先只告警**） |
| **机会** | ★ **把 `C21` 从"条文"变成"判据"**（**`check_version_label.py`**）—— **正是 `F-5`（有判定、无动作、无触发）的治法**；★ **转判红条件（连续 5 轮为 0）可复用于其他新判据的启用** |
| **建议** | **已采纳"丙"并落地**：`T-050` ＋ §二十 ＋ `C21` 交叉引用行；★ **下一步**：**实现 `check_version_label.py`**（**先只告警**）＋ **定历史豁免名单**（**`count 312–323` 区段**） |
| **等待用户决策** | ✅ **等**：见 ⑪ ①（**`check_version_label.py` 是否实现、豁免名单是否照此定**） |

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★ **`check_version_label.py` 是否实现、历史豁免名单是否＝`count 312–323`** | **荐实现（先只告警）** ＋ **豁免照本条件（`312–323`）**；**转判红条件 ＝ 告警连续 5 轮为 0**（**已写入 `T-050`**） |
| ② | ★ **L0–L6 专题 6 项逐项裁定**（**第 2 项** 荐丙／**第 3 项** 荐乙／**第 4 项** 荐甲／**第 5 项** 荐甲／**是否分两批** 荐同意／**`M6`・`M8` 何时裁** 荐同批） | **请在专题稿逐项回"第 N 项：选 X"**；**确认后一次回写底图**（**四步连带**：触判据 F ＋ 汇编重生成 ＋ 机制 25 P3 ＋ 行号复核） |
| ③ | ★ **`M6` 状态以哪一处为准**（**源稿 `§七`"已裁定选乙"** vs **专题稿／`§59.3`"未裁"**） | **荐先统一口径**（**若"已裁" ⇒ 本节 `M6` 只是"登记未施工"；若"未裁" ⇒ 须补齐裁定**） |
| ④ | **`BASELINE §59.3` 表第 6／7 项是否更正如源清单** | **荐更正**（**第 6 项＝数值时点化／第 7 项＝本稿未做**；★ 或改为**不编号**、以名称指称，**避再错**） |
| ⑤ | **本轮 push 放行**（**1 批本地 commit**：`TEMPLATES §二十`／`CONSTRAINTS C21 交叉引用`／`BASELINE §六十`／本报告） | **放行**（**机制 23**：push 须当轮明示） |

## ⑫ 下一步建议

1. **若确认 ①** ⇒ **实现 `check_version_label.py`**（**先只告警**）＋ **建历史豁免名单**；**观察至连续 5 轮告警为 0 后转判红**。
2. **若确认 ② / ③** ⇒ **L0–L6 一次回写底图**（**四步连带**）＋ **`M6`／`M8` 落地**（**源稿口径以用户裁定为准**）。
3. **若确认 ④** ⇒ **`BASELINE §59.3` 表按源清单更正**（**或改为以名称指称、不编号**）。
4. **继续**：`R90`／`B5` 重试路径**继续等真实抖动**（**追认，不人为造红**）。
5. **本轮已如实标注**：**底图回写四项连带未执行**（**因"只读取证、不改底图"**）。

## ⑬ 启动回执

**版本 `count=337`** ｜ **轮次 `v0.324`** —— 按 **`T-050`** **标名并列**（**版本＝`git rev-list --count HEAD` 输出 ⇒ 337**；**轮次＝会话序号，每轮递增**）。**起点 `count 337`**（`a78acf9`）；**本轮产出 1 批待放行**。★ **两口径差值 = −13**（`v0.324` ↔ `count 337`）—— **此即"分家"的现行状态；自本轮起两者各自成文、各自可自证**。

---

## 附件 A · `B2`＋`C1` 告警行（固定一行）

**★ 六族告警（`C1` 载体 · 固定一行）**：**六族告警：本轮 3 条**（`C22`／`T-00`／`TERM-999`，**均为元引用**）**｜对比上轮 ±0**｜**真缺口 0 条**。

（口径：`check_id_set_diff.py` 实跑；**3 条全在 `A2` 豁免登记表**；**剔除 6 层**。★ 本轮新登记 **`T-050`** ⇒ `T` 族引用/登记 **齐**，**未新增告警**。）

---

## 附件 B · INTEL 状态表（附加表 · 依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| INTEL-001 工信部 AI 中小企业计划 | 观察中 | 组合：企业注册完成 ＋ 阶段三启动 | ✗ **未触发**（现处阶段二 · 2.4） | 无 |
| INTEL-002 GPT-6 安全风险 | 观察中 | 事件：L7–L10 落地后 | ✗ **未触发**（L7 设计完成／L8–L10 冻结） | 无 |

**J1–J4 自查**：J1 报告须含状态表 ✅｜J2 触发条件满足但状态未变 ⇒ 违规 —— **两条均未触发** ✅｜J3 待准备但无准备稿 ⇒ 违规 —— **两条均非"待准备"** ✅｜J4 准备中但无具体行动 ⇒ 违规 —— **不适用** ✅。

★ **机制 29 本轮无状态变更**（不虚触）。
