【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · push `e572cb9` ＋ 裁定 ①「新判据不入 CI」＋ 裁定 ②「判据全跑入 §11.3」（v0.320）

> ⚠️ **本轮特殊性（须知）**：**上一轮（v0.319）的用户指令在执行前被截断**（中断点＝「**二、裁定 ② → 2.2 口径 → 在 `TEMPLATES §11.3` 新增一条：**」），**该轮未执行**。本轮由用户指令「**继续完成未完成的任务**」驱动 ⇒ **只办已收到部分**（〇 push ＋ 裁定 ① ＋ 裁定 ② 的**方向**）；**未收到部分（裁定 ③~⑤、下一份报告专属要求、验收标准）不得臆测**（**C5**），**列 ⑪ 待补发**。

## ① 保密标记

🔓 **公开** —— 本轮**零机密内容**：只涉及**机制编号／判据源码／计数口径**，均为仓库内公开文件。

★ **本轮主动做了一次保密订正**：内联说明初稿含**平台／账户层报错字样** ⇒ 判定**不宜进入公开仓库**（**C12**）⇒ 已改为「**中断原因属平台／账户层，按 C12 保密口径不在本仓库留痕**」，并**在提交前 amend 清除**（**该提交尚未推送**）。

## ② 全景状态卡

| 项 | 值 |
|---|---|
| **阶段** | **阶段二 · 2.4 边界层已验** |
| **基准 → 收口** | `count 329`（实测一致）⇒ **331**（**2 个本地 commit**：`fe9e531` 裁定／本报告） |
| **push** | `88b621a..e572cb9`（**exit 0**）｜**ahead 2**（本轮的 2 个本地 commit **待放行**） |
| **CI 状态** | **全绿**（三 job 全 success；★ **run 号与逐 job 三态按 C20 只记日报、不入库**） |
| **本轮性质** | 放行（上轮）＋ **2 项裁定落地** ＋ **判据套件首次全跑** |

## ③ 任务（含基准／定位）

**指令（已收到部分）**：`〇 push 放行` ＋ **裁定 ①「新判据不入 CI」** ＋ **裁定 ②「每轮跑全部 11 判据 → `TEMPLATES §11.3`」（治 F-5）**。
**总规划定位**：放行 ＋ 裁定落地 ＋ 机制建设。
**基准**：`count = 329`（`git rev-list --count HEAD`）—— ★ **实测与指令一致**（**本轮无基准偏差**）。

**★ 本轮读了 `coordination/` 哪几份、哪几行**（**底图零读取，如实记**）：

| 文件 | 行／范围 | 用途 |
|---|---|---|
| `coordination/TEMPLATES.md` | **L378–L381**（`T-045`／`T-046`／`T-047` 三行）｜**L592–L607**（§11.3 全表）｜§7.2 表头（**L382–L383**） | `T-048` 的**格式与同族先例**（`T-037`／`T-047` 两条 §11.3 行） |
| `coordination/CONSTRAINTS.md` | **L415–L445**（**C20 全文**）｜**C21 全节** | CI 结论**能否入库**的边界 ＋ **版本号口径**（★ 本轮由此**发现 C21 判红**，见 ⑦） |
| `coordination/BASELINE.md` | **L4127–L4193**（§五十五 全节） | 上轮留痕、`F-5` 原文、C15 基准 |
| `coordination/tools/check_*.py` | **11 份各前 14 行**（docstring ＋ `add_argument`） | ★ **判据套件的调用姿势**（本轮**实测两例用法错**，见 ⑦） |
| `reports/2026-09-22_推送88b621a…md` | **全文 166 行** | **十四项版式**对齐 ＋ 上轮待裁 ①② 的原文 |
| **底图**（根 `README.md` ＋ `docs/`） | **0 份／0 行** | ★ **如实**：本轮为**机制／判据**工作，**不涉底图**；**判据 F PASS** 佐证底图未变 ⇒ **未触发底图读取** |

## ④ 状态：部分完成

**部分完成** —— **〇 push ＋ 裁定 ① ＋ 裁定 ② 已办**；**裁定 ③~⑤ 与报告专属要求未收到**（**指令截断**，**C5 不臆测**）。

## ⑤ 验收对照

| # | 验收标准（依已收到部分设定） | 结果 |
|---|---|---|
| 1 | 〇 push 完成，先 push 后动文件（T-038） | ✅ `88b621a..e572cb9`（**exit 0**）；★ **push 先于任何文件改动**（时序已核） |
| 2 | CI 三 job 全绿，门控证据 | ✅ **`READY_TO_EDIT`**（`gh run list` **completed**；**三 job 全 success**；`##[error]` **0 行**；**报告结构门禁 step 真跑＝YES**） |
| 3 | 裁定 ①：新判据不入 CI，记录到 BASELINE | ✅ **§56.1**（**追认** ＋ 理由 ＋ **与"照抄先例"的区别**）；`.github/` **零改动** |
| 4 | 裁定 ②：每轮跑全部 11 判据写入 `TEMPLATES §11.3` | ✅ **§11.3 新增行**（**★ 判据全跑（2026-09-22 起）**）＋ **`T-048`**（§7.2 表尾）；落点 **§56.2** |
| 5 | ★ **裁定 ② 的当场实证**：11 判据**真跑一遍** | ✅ **11/11 PASS**（**判据真红 0**）；见 **§56.3** | 
| 6 | C15 复算通过，编号表连续无缺号无重号 | ✅ 机制 `1..30`／`T 1..48`／节号 `0..56` **连续无缺重**（**§56.4**） |
| 7 | 保密：公开仓库不留账户层信息 | ✅ 已 **amend 清除**（提交未推送） |

## ⑥ 未完成项

| 项 | 状态 | 原因 |
|---|---|---|
| **裁定 ③~⑤** | ❌ **未做** | **指令截断 ⇒ 内容未知**（**C5：不得臆测**）⇒ **待补发** |
| **本轮报告专属要求／验收标准** | ❌ **未做** | 同上；**本轮报告按通用十四项版式出**（**如与用户原意不符，请订正**） |
| **"11 判据全跑"的机器强制** | ❌ **未做** | **无 CI 兜底**；"跑没跑"**记在报告里**，**非机器可判**（**如实边界**，见 §56.2 边界 ①） |
| **`F-编号` 口径（轮内 vs 全局）** | ❌ **未定** | 见 ⑦；**属元规则 ⇒ 不擅改**，**列 ⑪ 待裁** |

## ⑦ 真实缺陷

### ★★ **C-1（本轮新发现 · P0）：`v` 标签与 `count` 再次脱钩 —— C21 判红（R84 同型再发）**

**现象（逐条取证 · 全量提交）**：`label == count` 在 **count 311 及以前严格成立**（`v0.291`↔`291`…`v0.311`↔`311`，**逐条零漂移**）；**自 count 312 起脱钩**：

| count | 提交 | 标签 | 差 |
|---|---|---|---|
| **311** | `v0.311 · push v0.310…` | **v0.311** | **0** ✅ |
| **312–315** | 4 条「**夜间 P3-n：**」（按 **C21 ②** 属**另一前缀**，**不计版本号**，合规） | （—） | — |
| **316–323** | 8 条「**A 组／B 组…／报告：**」 | （**均无** `v` 标签） | — |
| **324** | `v0.317 · push 32b4042…` | **v0.317** | **−7** ❌ |
| **326** | `v0.318 · MEMORY 三层…` | **v0.318** | **−8** ❌ |
| **328** | `v0.319 · 2 项追认…` | **v0.319** | **−9** ❌ |

**⇒ 两处硬事实**：
1. **`v` 号无法用 `git rev-list --count <该提交>` 复现**（**C21 判据三问之二 ⇒ 判红**）。
2. ★ **`v0.312`–`v0.316` 五个号从未使用（跳号）** —— 而 `v0.316` 被 §五十二 标题当**轮次名**引用过。

**为什么是缺陷（不是风格差异）**：`C21` 的来历**正是**"**count 230–246 段因排除夜间提交而漂移 −5**"（**R84**，2026-09-20 已结案并要求 `label = count`，**含 `README.md` 行 89／90 修订**）。**⇒ 同一病象在 count 312 起再次出现** —— 属 **R84 同型再发**，**不是新病**。

**★ 为什么本轮才看到**：**C21 自带判据（"能否用 `count` 复现"）从未被机器执行过** —— **没有 `check_version_label.py` 这类判据**；★ **本轮的 11 判据全跑恰好把"口径类"缺口暴露出来**（**同族：`F-5`**：**有判定、无动作、无触发**）。

**处置（不擅改 · 元规则属 A 类）**：**版本号口径＝元规则** ⇒ **不得单方更动**（按项目规则须走**完整流水线 ＋ 专家审核**）⇒ **本轮只取证、只上报**，**列 ⑪ 待裁**。★ **本轮报告标题仍用 `（v0.320）`**（**沿用现状口径、不在报告里单方改口径**），**同时把 `count 331` 写进 ⑬ 回执**（**两口径并列，便于用户比对**）。

### **C-2（本轮新发现 · P1）：判据套件"无统一运行器 ＋ 调用姿势不一" ⇒ 有"把用法错误记为真红"的风险**

**现象（本轮实测两例）**：
- 给 `check_source_of_truth.py` 传 `--repo .` ⇒ `repo.parents[0]` **IndexError**（`.` **parts 为空**）⇒ **rc=1**；
- 对 `check_migration_fidelity.py` **不带参数** ⇒ **rc=64（用法错）**。
**两者均非判据真红**，**改用正确姿势后皆 PASS**。

**为什么是缺陷**：裁定 ② 把"每轮跑全部 11 判据"写成清单，但**11 份脚本的调用契约各不相同**（有的要 `--selftest`、有的要文件参数、有的**不能传 `--repo .`**）⇒ **执行者极易把 `rc≠0` 当作"判据红"上报** ⇒ **假红**（**R83 空转 ／ R89「声明≠发生」同族**）。

**处置**：**已写进 `TEMPLATES §11.3` 新增行**（"**调用姿势按各脚本自身为准**"**逐字点出两例**）；**未建运行器脚本**（**授权范围＝新增一行 ＋ 一条 `T` 条文**）⇒ **"是否把 `run_all_checks` 固化进 `tools/`"列 ⑪ 待裁**。

### 一处**未验证（非缺陷 · 如实记）**

★ **"CI 上 11 判据的实际执行"未验**：`check_memory_layers.py` **未入 CI**（裁定 ①），`check_private_pointers.py` 在 CI 走 `--mode=syntax`、**本机实跑亦用同款** ⇒ **两类姿势在 CI／本机的差异未单测**（**R70 家族：环境类差异**）。

## ⑧ 三量台账

| 量 | 内容 |
|---|---|
| **存量** | **机制 30**（不变）｜**判据脚本 11 个**（**只跑不改**）｜**台账 56 节**（55 → 56）｜**`T` 48**（47 → 48）｜`TERM` **14**／`C` **21**／`R` **90**／`D` **55**（**±0**）｜**记忆三层**：调用层 **977 B**／索引层 **3931 B**／内容层 **4 份** |
| **变量** | `coordination/TEMPLATES.md`（**§11.3 新增 1 行 ＋ `T-048` 1 行**）｜`coordination/BASELINE.md`（**新增 §五十六**） |
| **补充增量** | ① **2 项裁定入账**（§56.1／§56.2）② **`T-048` 登记**③ **11 判据首跑留痕**（§56.3）④ **1 份报告** |
| **创新增量** | ① ★★ **C-1：把 `label == count` 做成"全量提交逐条对照"取证**（**不只查最近几条**）⇒ 抓出 R84 同型再发（**差 −7／−8／−9**）② ★★ **C-2：判据套件的"调用契约"被首次盘点** ⇒ 发现"**姿势错＝假红**"这一**假红新来源**③ ★ **保密即时订正**（内联说明含账户层字样 ⇒ **提交前 amend 清除**，**公开仓库不留痕**）④ ★ **保密＝执行项而非口号**：把 C12 从"标记"落到"**改一条、撤一条提交**" |

## ⑨ 边界说明

| 类 | 内容 |
|---|---|
| **本地跑过** | ✅ **11 判据全跑**（**11/11 PASS**，读**真实仓库**，非夹具）｜**C15 复算**（机制／`T`／节号连续）｜**`label↔count` 全量逐条对照**（**count 311–330 全列**）｜**门控脚本实跑**（`READY_TO_EDIT`） |
| **CI过** | ✅ **三 job 全 success**（`test`／`宿主 field-render`／`元内核引导镜像`；**T-038 门控已执行**；**run 号按 C20 只记日报**） |
| **真实跑过** | ⚠️ **部分**：11 判据**读真实仓库**跑过；但 **`migration_fidelity` 跑的是 `--selftest`（造例）**，**未对真实迁移文件跑**（**本轮无迁移**）⇒ **非全真** |
| **未验证** | ① **裁定 ③~⑤ 的内容**（**指令截断**）② **"每轮跑全部判据"的机器强制**（**无 CI**）③ **C-1 的修法**（**待裁，元规则**）④ **`run_all_checks` 是否固化**（**待裁**）⑤ **CI 与 本机 判据姿势差异**（**未单测**）⑥ **`F-编号` 口径**（**待裁**） |

## ⑩ 决策评估（七要素 · 本轮**新决策 1 项**：C-1 的处置方式）

| 要素 | 内容 |
|---|---|
| **评估** | 三案：**甲 立即回正**（把 `v` 号改成 `count`，含历史 §45–§56 回填）｜**乙 只取证上报、本轮不动**（**本轮做法**）｜**丙 另立"轮次号"机制**（承认 `v` 号＝轮次号，与版本号**分家**，各自成文） |
| **优势** | **甲**：**一次性对齐 C21**，判据可复现｜**乙**：**零风险**，把元规则判断留给完整流水线（**符合项目规则**）｜**丙**：**最诚实** —— 现状事实上就是"轮次号"；**两条口径各自能自证** |
| **劣势** | **甲**：**改元规则 ＋ 改写历史陈述**（**违反 C19"历史只新增不改写"**；且须专家审核）｜**乙**：**漂移继续**（下轮 `v0.321` ↔ `count 333`，差更大）｜**丙**：**新增一套编号**（须防与 `T`／`R`／`C` 族同病：**登记不严则又漂**） |
| **风险** | **甲**：**动了不该单方动的东西**（元规则 ＋ 历史），**风险最高**｜**乙**：**读者据 `v` 号做算术 ⇒ 结论错**（若有人以为 `v0.319`＝count 319）｜**丙**：**两套号并存 ⇒ 引用时更易混**（**须明写"哪处用哪个号"**） |
| **机会** | ★ **乙 → 丙 的两步走**：**本轮乙**（取证上报）⇒ **一旦授权，走"丙 ＋ 补机器判据"**（`check_version_label.py`：`v` 号须能用某规则复现，**规则由用户定**）⇒ **把 C21 从"条文"变成"判据"**（**正是 F-5 的治法**） |
| **建议** | **本轮选乙**（**已执行**）；**下一步建议丙 ＋ 补判据**（**须用户定"`v` 号该等不等于 count"**）。★ **不建议甲**（**须改历史陈述，与 C19 冲突**） |
| **等待用户决策** | ✅ **等**：见 ⑪ ①② |

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★★ **C-1：`v` 号与 `count` 脱钩如何处置**（**甲** 回正为 `count`／**乙** 只观察／**丙** 另立"轮次号"机制 ＋ 补判据） | **走"乙→丙"**：**本轮已乙**；请裁定是否**另立轮次号机制**（则 `v0.320` 成立）**或**要求**回正为 `count`（则应为 `v0.331`）。★ **两者不可混**（C19 家族） |
| ② | ★★ **是否新建判据 `check_version_label.py`**（把 C21 的"能否复现"变成机器判据） | **荐立**（**F-5 的正治法**：**判定＋动作＋触发齐**）；★ **须先定规则**（依赖 ① 的裁定） |
| ③ | **是否把 `run_all_checks` 固化进 `coordination/tools/`** | **荐固化**（**治 C-2**：**一条命令跑全套，姿势内建**，**消除"用法错＝假红"**）；★ **本轮未授权，未建** |
| ④ | **裁定 ③~⑤ 与报告专属要求的原文** | **请补发**（**指令截断段**；★ 收尾于「**在 `TEMPLATES §11.3` 新增一条：**」） |
| ⑤ | **`F-编号` 口径**（**轮内序号** vs **全局序号**） | **先明写**：★ 实证显示 **`F-1`／`F-2`／`F-5` 各指**多物（`docs` 过期项／§7.2 状态列／判据三有）⇒ **若为轮内序号则合规，但须成文** |
| ⑥ | **push 放行**（本轮 **2 个本地 commit**，`fe9e531` ＋ 本报告） | **放行**（**机制 23**：push 须当轮明示） |

## ⑫ 下一步建议

1. **补发被截断的指令段**（从「**二、裁定 ② → 2.2 口径 → 在 `TEMPLATES §11.3` 新增一条：**」起，含 ③~⑤）—— ★ **本轮 ② 的措辞即据该处重建，若与原文不符请直接订正**。
2. **若授权 ①** ⇒ **丙案**：`CHARTER` 立「轮次号机制」（`v` 号＝轮次序号，**与 count 分家**）＋ `CONSTRAINTS C21` **补一句边界**（"**轮次号不属版本号**"）⇒ **避免两大口径混用**（**这是把 C-1 变成资产的路**）。
3. **若授权 ②③** ⇒ **补两个判据**（`check_version_label.py` ／ `run_all_checks`）**并入 §11.3 清单**（**F-5 治法推广**）。
4. **持续**：`R90`／`B5` 重试路径**继续等真实抖动**（**追认，不人为造红**）。

## ⑬ 启动回执

已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD` 的输出 ⇒ 331**（**本报告所在提交的 count**，依 **`T-045`** 口径）；**起点 `count 329`，本轮 2 个本地 commit**。★ **标题 `（v0.320）` 系沿用现状"轮次号"写法 —— 它与 `count 331` 不等，此不符即本轮 C-1 所报之缺陷**（**两口径并列，便于比对**）。

---

## 附件 A · `B2`＋`C1` 告警行（固定一行）

**★ 六族告警（`C1` 载体 · 固定一行）**：**六族告警：本轮 3 条**（`C22`／`T-00`／`TERM-999`，**均为元引用**）**｜对比上轮 ±0**｜**真缺口 0 条**。

（口径：`check_id_set_diff.py` 实跑；**3 条全在 `A2` 豁免登记表**（现 **5 行**）；**剔除 6 层**）

---

## 附件 B · INTEL 状态表（附加表 · 依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| INTEL-001 工信部 AI 中小企业计划 | 观察中 | 组合：企业注册完成 ＋ 阶段三启动 | ✗ **未触发**（现处阶段二 · 2.4） | 无 |
| INTEL-002 GPT-6 安全风险 | 观察中 | 事件：L7–L10 落地后 | ✗ **未触发**（L7 设计完成／L8–L10 冻结） | 无 |

**J1–J4 自查**：J1 报告须含状态表 ✅｜J2 触发条件满足但状态未变 ⇒ 违规 —— **两条均未触发** ✅｜J3 待准备但无准备稿 ⇒ 违规 —— **两条均非"待准备"** ✅｜J4 准备中但无具体行动 ⇒ 违规 —— **不适用** ✅。

★ **机制 29 本轮无状态变更**（不虚触）。
