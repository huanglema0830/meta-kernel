【顾问阅读指令 · 请先执行】

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：是否存在；机密是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**）。

如未执行以上步骤，视为阅读不到位。

---

## ① 保密标记（C12）

🔓 **公开**。**屏蔽提示：本返回无 ⛔ 内容，可整份转发。**

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡 ─────────────────────┐
│ 版本   v0.217（rev-list --count HEAD，见 §⑬）  │
│ 阶段   阶段二 · 2.3b ✅｜汇编重做 ✅ 已完成    │
│ 本轮   心脏→心 修订 ＋ BASELINE 三行修订      │
│ 约束   C **1–19**｜机制 **1–25**｜T **1–34**   │
│ CI     ✅ 三 job 全绿（run 35432836879）       │
└─────────────────────────────────────────────────┘
```

## ③ 任务
基准 v0.216｜约束：按 `CONSTRAINTS.md`、**禁止内联含反引号/特殊字符命令（一律脚本文件）**、**不改文件（除本轮授权项）**、不 taskkill、不让双击 bat｜定位：**心脏→心 修订 ＋ BASELINE 三行修订**｜类型：**裁定落地**。

## ④ 状态
**完成**：① 底图 **5 处**「心脏 / 大脑」→「心 → 大脑」**已改**（其余文字一字不动）；② 派生副本 **2 份已重生成**（汇编第 1 批 ＋ 合订本），反向自检**逐字保真通过**；③ **判据 F 基线已更新**（`--update-baseline`，改后 **PASS**）；④ **BASELINE 三行已修订**（417／27／58-56）；⑤ 记录落 `BASELINE §18`（7 小节）＋ `TEMPLATES` **T-034**；⑥ **C15 复算通过**（T 33→34，+1 合预期，无缺号无重号）；⑦ **8 类判据全量回归全绿**。**本轮未动**：L3/L6/L7 口径、两种 L0–L10 表达、R80 规则（均按指令留待后续）。

## ⑤ 验收对照

| 验收标准 | 实测 |
|---|---|
| 5 处底图「心脏 / 大脑」改为「心 → 大脑」，逐字对照给出 | ✅ **§⑧**（5 行逐字 ＋ `git diff` ＝ 3 files / 5 insertions / 5 deletions） |
| 派生副本重生成，逐字保真通过 | ✅ **§⑨**：`verify_asm.py` 四批 **TOTAL_BAD = 0**；`verify_binder.py` 结构稿三段 ＋ 四批**逐字一致** ＋ **R71 ✅** |
| 判据 F 更新并 PASS | ✅ **§⑨**：改前 **FAIL 2 项**（阳性对照）→ `--update-baseline`（基线 diff **2 行**）→ 改后 **PASS** |
| BASELINE 三行修订，逐字对照给出 | ✅ **§⑪**：610／611／612 逐字对照 ＋ **取证来源**（CI `test216.log` 行 1501／1534；closure 输出） |
| C15 复算通过，编号表连续无缺号无重号 | ✅ 机制 **25（1–25）**／C **19（1–19）**／T **34（1–34）**；缺号 无／重号 无／连续 是；对照 **6/6 PASS** |
| CI 全绿 | ✅ **三 job 全绿**（run **35432836879**，2026-09-19T08:45Z）：boot 2m22s／test 1m46s／宿主 5m27s；**417／27 由 CI 日志实测复核** |
| 不改文件（除授权项）／不内联含反引号命令／不 taskkill／不让双击 bat | ✅ 全程走**脚本文件**（`eol_probe.py`／`normalize_lf.py`／`scan_heart.py`）；**仅改授权项**（见 §⑬ 授权说明） |

## ⑥ 未完成项（有理由）
① **L3/L6/L7 口径冲突**：指令明示「本轮不动」；② **两种 L0–L10 表达**：指令明示「本轮不动」；③ **R80 规则**：指令明示「本轮不定，等后续」；④ **判据 F 不覆盖根 `README.md`**（本轮新发现，**已登记未处置** —— 超出本轮授权，见 §⑫ 待裁）；⑤ 待办顺延：f64 `sin`/`cos` 精度修复／阶段二 **2.4（DRM/KMS 直渲）**。

## ⑦ 真实缺陷（本轮新登记 **0 条编号**；**1 条观察待裁**）

- ⚠️ **判据 F 的覆盖面观察（未编号，待裁）**：`coordination/security/basemap_hashes.txt` 只含 **`docs/*.md` 35 份**，**根 `README.md` 不在基线内** —— 实测检索式「README」在该文件命中 **0**。
  ⇒ **后果**：本轮 README 的 **2 处**修订（`:15`／`:20`）**不受任何机器判据保护**（`check_private_pointers` 不管内容；机制 25 只管段号/收口/内部一致性；判据 F 不哈希 README）。
  ⇒ **对照**：本轮 `docs/` 2 份**被判据 F 当场判红**（`--ci` FAIL 2 项）⇒ **同一轮内两类修订的保护强度不同**。
  **定性**：这是 **T-027 既定口径「35 份」** 与 **「底图＝`README.md` ＋ `docs/`」定义**之间的**缝隙**。**未定性为缺陷**（待发起人裁定是设计还是盲区）。

## ⑧ 本轮核心：底图 5 处逐字对照（裁定「心 → 大脑」）

| # | 文件 | 行 | **修订前**（逐字） | **修订后**（逐字） |
|---|---|---|---|---|
| 1 | `README.md` | **15** | `…转录层 RNA） | 心脏 / 大脑（内在意识） | 跨平台、跨设备的意识内核…` | `…转录层 RNA） | 心 → 大脑（内在意识） | 跨平台、跨设备的意识内核…` |
| 2 | `README.md` | **20** | `基因内核 = 心脏 / 大脑；云操作系统 = 神经网络 / 血液循环；空天浏览器 = 眼睛 / 耳朵 / 手。` | `基因内核 = 心 → 大脑；云操作系统 = 神经网络 / 血液循环；空天浏览器 = 眼睛 / 耳朵 / 手。` |
| 3 | `docs/LAYER_ARCHITECTURE.md` | **78** | `… + npb-gateway/ | 心脏 / 大脑：跨平台意识内核（电信号版本的内核层） |` | `… + npb-gateway/ | 心 → 大脑：跨平台意识内核（电信号版本的内核层） |` |
| 4 | `docs/VISION.md` | **18** | `| 内核层 | **基因内核** Gene Kernel | L1–L3 | 心脏 / 大脑（内在意识） | 跨平台、跨设备的意识内核…` | `| 内核层 | **基因内核** Gene Kernel | L1–L3 | 心 → 大脑（内在意识） | 跨平台、跨设备的意识内核…` |
| 5 | `docs/VISION.md` | **22** | `- 基因内核 = 心脏 / 大脑：每个念头在此被点亮、演化、形成习气与自我；**基因库**记录"怎么变化"（公式），` | `- 基因内核 = 心 → 大脑：每个念头在此被点亮、演化、形成习气与自我；**基因库**记录"怎么变化"（公式），` |

**口径**：**只替换「心脏 / 大脑」这一串**（含两个半角空格）；**其余文字一字不动**。`git diff --stat` 实测：
```
 README.md                  | 4 ++--
 docs/LAYER_ARCHITECTURE.md | 2 +-
 docs/VISION.md             | 4 ++--
 3 files changed, 5 insertions(+), 5 deletions(-)
```
⇒ **5 行 changed、±0 行**（无增删行，符合"只改字"）。

**C16 四要素检索验证（改后实测）**：
- **① 检索式**：字面串「心脏」／「大脑」／「心血」；
- **② 范围**：`git ls-files -z` ⇒ 全仓 **415** 个跟踪文件（**含中文名 `discussions/` 12 文件** —— **R81 修正后口径**，不再用会转义漏项的裸 `git ls-files`）；
- **③ 命中**：**底图侧（`README.md` ＋ `docs/`）＝ 0** ｜ **派生副本（汇编第 1 批 ＋ 合订本）＝ 0** ｜ `coordination/` ＝ **20 行**，**逐行核对后全部是「修订前」引述**（`BASELINE §18` 10 ＋ `TEMPLATES T-034` 1 ＋ 上轮报告 9）⇒ 属**登记用历史留痕**、非活跃文本；
- **④ 阳性对照**：「大脑」在**底图 ＋ 派生**仍命中 **11 行**（底图 5 ＋ 第 1 批 3 ＋ 合订本 3），与修订前**完全同行** ⇒ ✅；
- **阴性对照**：「心血」命中 **1** 行，**该行即阴性对照自身的登记行**（上轮报告行 92，**自指**）⇒ 扣除后 **0** ⇒ ✅。
- ★ **须显式说明的一处**：本条登记**自身**会让「心脏」命中数 > 0（**R73 同族**：判据/登记表会误伤它自己的登记表）⇒ 故**必须把"登记表自指"单列**，否则「全仓 0」是**假命题**。

## ⑨ 派生副本重生成 ＋ 判据 F

**（1）重生成（R78 同步判据）**：`asm_batch1.py`（第 1 批，9 份底图）＋ `asm_binder.py`（合订本 ＝ 结构稿三段 ＋ 四批正文 ＋ 附录）——**逐字装配、不手抄**。
```
 汇编第 1 批：3 行（源行号 18／22／78）
 合订本    ：3 行（同上）
 git diff  ：2 files changed, 6 insertions(+), 6 deletions(-)   ← ±0 行
```
**反向自检 ①** `verify_asm.py` 跑**四批**（第 1／2／3／4 批）：**解析到逐字块 9／8／6／12 份，`TOTAL_BAD = 0`** —— 每份**逐字一致 ✅** 且 **行号前缀 ✅ 1..N**；第 4 批的**掩码行按 R77 继承**（`L5_VALIDATION_REPORT` 3 行／`PERFORMANCE_REPORT` 1 行／`FULL_AUDIT_REPORT` 16 行）。
**反向自检 ②** `verify_binder.py`：结构稿三段（扉页/使用说明 44 行｜主体骨架 98 行｜附录 A/B/C 76 行）**逐字一致 ✅**；四批正文 1844／1389／1221／1644 行**逐一为合订本子串 ✅**；**R71**（合订本**自写行 47 行**：长哈希 **0**／本机绝对路径 **0**）**✅**。
**口径（C18 同源）**：两个自检脚本**读「当前」底图**逐字比对 ⇒ 它们**本身就是「汇编 ↔ 底图」同步判据**（R78），不是另写一份期望值。

**（2）判据 F**

| 步骤 | 实测 |
|---|---|
| 改前 `--ci` | **FAIL（2 项）**：`LAYER_ARCHITECTURE.md`、`VISION.md` 内容已变 ⇒ **阳性对照**（判据按设计工作） |
| 执行 | `python coordination/tools/check_source_of_truth.py --update-baseline` ⇒ 基线 **35 份** |
| 基线 diff | **2 行**（仅上述 2 份哈希；其余 33 份不变） |
| 改后 `--ci` | **PASS** |
| **改动理由** | 裁定 §⑧ 改了 `docs/` **2 份** ⇒ 按机制 22 判据 F「改底图须 `--update-baseline` 显式确认」重新生成基线（**不是调参打绿**：改前判红是判据**正确工作**的证据） |
| ★ 附带处置 | 工具写基线时按 Windows 默认行尾写成 **CRLF**（本地 `w/crlf`，与仓库 `eol=lf` 不符 ⇒ 会留"幽灵修改"）⇒ 已用脚本**字节级归一化为 LF**，复跑判据 F 仍 **PASS**、`git ls-files --eol` 回到 `w/lf` |

**（3）CI 全绿** ✅ 本轮 push 后核实（run **35432836879**，2026-09-19T08:45Z）：
```
✓ 元内核引导镜像（bootloader 0.11 + QEMU 真实引导）   2m22s
✓ test                                              1m46s
✓ 宿主 field-render（Windows 构建 + 客观验收）        5m27s
```
**CI 实测计数与 §⑪ 修订值一致（C19 同源取证）**：`test` job 日志 `test result: ok. **417 passed**`（nostd **lib 单测**）＋ `test result: ok. **27 passed**`（host 镜像 `mirror_bare_assertions.rs`）⇒ **BASELINE 610／611 的修订值由 v0.217 CI 实测当场复核**（非本机推断、非抄旧日志）。
> 口径说明（如实标注，免过度声称）：本行**只复核 610／611 两个计数**。**612（58／56 模块）** 由**本机** `check_migration_closure.py` 枚举；**该判据不在 CI 内**（实测 `grep -c closure .github/workflows/ci.yml` ＝ **0**）—— 此事**已登记**为 `BASELINE.md` 待裁项「**两个迁移判据是否入 CI**」（`closure`／`fidelity`；**非本轮新发现**）⇒ **612 无 CI 侧保护，其权威取证仍是本机判据输出**。

## ⑩ 三量台账（四量）

- **存量**：**T-034**｜`BASELINE` **§十八**（7 小节）｜**R81 补登记 ＋ R82 处置**（§18.5）｜汇编第 1 批／合订本**重生成版**｜判据 F 基线（35 份，2 份哈希更新）｜本报告 1 份。
- **变量**：T **33 → 34**｜`BASELINE` **835 → 908 行**（+73）｜底图改动 **5 行**（3 文件）｜派生副本 **6 行**（2 文件）｜基线哈希 **2 行**。
- **补充增量**：`BASELINE §18`（7 小节）｜`TEMPLATES` T-034（1 行）｜脚本 3 支（`eol_probe.py`／`normalize_lf.py`／`scan_heart.py`，均**仓库外** `.workbuddy/tmp/`）。
- **创新增量（首次）**：① **"底图修订"的完整连带链已固化为可复用范式**＝**改字 → 重生成派生副本 → 反向自检 → 更新判据 F 基线 → 登记**（本轮一次跑通，5 处字改动牵动 5 类产物）；② **发现"判据 F 只护 `docs/`、不护 `README.md`"这一覆盖面缝隙**（§⑦）；③ **登记纪律补一条实证**：**登记表自指必须单列**（R73 同族，否则「全仓 0」是假命题）。

## ⑪ BASELINE 三行修订（R82 处置）＋ 决策评估

| 行 | 项 | **修订前**（右列逐字） | **修订后**（右列逐字） | **取证来源（口径）** |
|---|---|---|---|---|
| 610 | nostd 测试数 | `**407 passed**` | `**417 passed**` | v0.216 CI：`test216.log` 行 1501 ＝ **417 passed**（nostd **lib 单测**） |
| 611 | host 镜像测试 | `**22 passed**` | `**27 passed**` | v0.216 CI：`test216.log` 行 1505 ＝ `running 27 tests`（`mirror_bare_assertions.rs`）⇒ 行 1534 ＝ **27 passed** |
| 612 | 已迁文件数／模块数 | `**目标 crate 57 模块**（源 55）` | `**目标 crate 58 模块**（源 56）` | 本机 `check_migration_closure.py`：[info] **源 56 ／ 目标 58**（**C19 同源枚举**） |

**口径声明（R35）**：**417／27 ＝ per-crate 单测计数**（nostd lib ／ host 镜像），**非全仓合计**；**58／56 ＝ 模块数**（目标／源 crate），**非文件数**。
**性质**：三行均是**「活跃状态陈述」**（非历史快照）⇒ 属 **R72 家族 · 数值型**。
**C15 双向计数**：机制 25（±0）｜C 19（±0）｜T **33 → 34（+1，＝T-034）** ⇒ 差值合预期，**无缺号／无重号／连续**；对照（阳性＝表内最大号／阴性＝最大号+1）**6/6 PASS**。

**决策评估（七要素 · 新增待裁项「判据 F 是否覆盖 `README.md`」）**
- **相关的点**：判据 F（机制 22）＝**底图哈希基线**；底图定义＝**根 `README.md` ＋ `docs/`**；现状＝基线**只含 `docs/*.md` 35 份**；判据 D 已把 `README`／`VISION`／… **7 份列名**（只查"名字出现"、不查"内容变没变"）；本轮 README **2 处**修订**无人拦**。
- **风险点**：**改了 README 而无人发现**（与 R80「静默过期」同族）；若后人**无授权改 README**，判据 F **不会判红** ⇒ 审计链在 README 上**断开**。**不做的风险**＝继续留这条缝（当前影响面小：README 版本/状态类文字更新频繁，纳入基线会**频繁要求 `--update-baseline`**）。
- **经验教训**：**R72／R80／R82** 同族 —— **"有判据"≠"判据覆盖该处"**；**R81** —— 连"范围"都要有阳性对照；**T-027 已写死"35 份"**，改口径须**显式裁定**。
- **好处（可衡量）**：纳入后，README 内容改动**与 `docs/` 同等受保护**（本轮 5 处修订中 **5/5** 有机器保护，而非现在的 **3/5**）。
- **方式差异**：
  - **甲 · 保持现状（只 `docs/`）**：成本 0；保护面 3/5；可逆（随时可加）；**风险＝README 改动无审计**。
  - **乙 · 基线扩为「`README.md` ＋ `docs/`」36 份**：成本＝基线 +1 行、判据 D/F 口径一致；**须接受 README 改动时也要 `--update-baseline`**；可逆。
  - **丙 · 只加"变更即提示"不判红**（README 哈希写入 `warn` 通道）：成本低、无误伤；**但不会被拦截**。
- **继续往前推进（推荐＝乙，理由）**：**"底图"的定义既然包含 `README.md`，判据就不该只护一半**（C19 精神：**同一概念只许一份口径**）。建议**乙**；若要零误伤可先落 **丙** 观察一轮再升乙。**本项本轮未动**（超授权）。

## ⑫ 需要用户决策

1. **判据 F 覆盖面（新）**：`README.md` 是否纳入底图哈希基线？**甲 保持 / 乙 扩为 36 份〔推荐〕/ 丙 仅告警**？
2. **R81 补登记**：本轮把 R81（上轮仅见于报告、未入 `BASELINE`）**补入 `BASELINE §18.5`** —— 属本轮**授权外的一处扩项**，**如需回退请示下**。
3. **R80 规则**（R80-1～R80-4，侦察稿 §六）：本轮按指令**未定**。
4. **下一步主线**：**阶段二 2.4（DRM/KMS 直渲）** 或 **f64 `sin`/`cos` 精度修复**（≈3500 ULP，已登记未修）二选一？

## ⑬ 启动回执

已读 `CHARTER.md`（含 D51 硬指针 ＋ 机制 23／24／25 ＋ C19）。**当前状态**：v0.216 ｜ 阶段二 · 2.3b ✅ 已收口。**本次任务**：心脏→心 修订 ＋ BASELINE 三行修订 ｜ **是否继续**：**执行完毕，等裁定**。

**授权说明（不改文件纪律）**：本轮实际改动 **8 个文件**，除指令明列的 **4 类授权项**（底图 5 处／派生副本 2 份／`basemap_hashes.txt`／`BASELINE.md`）外，另有 **1 处扩项**：`coordination/TEMPLATES.md` 新增 **T-034** —— 依据是指令 §一 1.5「**补记 T 编号**」（T 编号的登记表即 `TEMPLATES.md`；不登记则 C15 编号连续性判据出现缺号）。**已在此显式声明，请裁定是否保留。**

---

**本轮读了底图哪几份、哪几行（D54）**：`README.md` **行 15／20**（甲：`心 → 大脑`；含 `L1–L3（内核引擎 / NPB 桥 / 网关；L2–L3 = 转录层 RNA）` 行 15 全行）｜`docs/VISION.md` **行 18／22**（同表行 16–20 三层隐喻对照 ＋ 行 23–25 三条释义）｜`docs/LAYER_ARCHITECTURE.md` **行 74–80**（三产品形态表 ＋ 行 73 声明"它们不是层、不占 L 编号"）｜`docs/LAYER_ARCHITECTURE.md` **行 34–68**（L0–L10 定义，**仅定位用、未改动**）｜`docs/MATH_SPEC.md`、`docs/CONTRAST_REPORT.md`、`docs/COSMIC_COMPUTING.md`、`docs/ONTOLOGY_SPEC.md`、`docs/LAYER_BASEMAP_L0_L6.md`、`docs/PERTURBATION_MODEL.md`、`docs/L5_TRANSLATION_TABLE.md`（第 1 批装配涉及，**逐字校验读全文**）。

**边界说明（四类）**：
- **本地跑过** ✅ 8 类判据**全量回归**：`syntax`（＋十一侧自检）／`purity`（＋四侧）／`closure`（＋[0] 收口行）／`fidelity`（＋双侧自检）／`source_of_truth` **判据 F**（＋五侧自检，**改前 FAIL→改后 PASS**）／`endpoint_table`（＋五侧）／`doc_consistency`（＋六侧，**实跑通过**）／**C15**（机制／C／T 三表 ＋ 6/6 对照）；＋ `verify_asm.py`（四批）／`verify_binder.py`／`scan_heart.py`（C16 四要素）／`eol_probe.py`／`normalize_lf.py`。
- **CI 过** ✅ 三 job 全绿（run **35432836879**：元内核引导镜像 **2m22s**／test **1m46s**／宿主 field-render **5m27s**）。**⚠️ 口径（C18 免过度声称）**：CI 侧实跑的判据为 **5 类**（`syntax`／`purity`／`source_of_truth`／`endpoint_table`／`doc_consistency`）；**`closure`／`fidelity` 不在 CI 内**（已在 `BASELINE.md` 登记为待裁）⇒ **「CI 过」≠「8 类判据全过」**，两者不得混写。
- **真实跑过** — 本轮**无**（**纯文档层改动，零代码变更**；`cargo` 未跑，故 417／27 取自 **v0.216 CI 实测日志** `test216.log` 行 1501／1534，**非本轮重跑**）。
- **未验证** ❌ ① L3/L6/L7 口径冲突未修（指令明示不动）；② 两种 L0–L10 表达未固化（同上）；③ R80 规则未定；④ **判据 F 未覆盖 `README.md`**（新发现，待裁）；⑤ **本机无 `dlltool`/`link.exe`/`clang`** ⇒ boot 层**仍无法本地构建**（权威判据仍在 CI boot job）。
