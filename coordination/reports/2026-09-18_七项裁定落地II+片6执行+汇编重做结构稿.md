# 2026-09-18 报告 · 七项裁定落地 II ＋ 片6 执行 ＋ 汇编重做结构稿

【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

## ① 保密标记（C12）

🔓 **公开**（无凭证／账号／网络细节／参数库取值／密钥指纹）。

**屏蔽提示：本返回无 ⛔ 内容，无需屏蔽，可整份转发。**

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡 ───────────────────────┐
│ 版本   v0.200（rev-list --count HEAD）           │
│ 阶段   阶段二 · 2.3b 分片迁移                    │
│        片1–6 ✅ ｜ 片7–8 已解冻（3 模块待迁）     │
│ CI     见第 ⑨ 项（本轮提交后）                   │
│ 机制   24（本轮新增端点表一致性）                │
│ 待裁定 Q10 映射（甲/乙/丙）｜汇编结构 5 问        │
└──────────────────────────────────────────────────┘
```

## ③ 任务

**基准 v0.199**｜约束：按 `CONSTRAINTS.md`，**不改文件（除本轮已授权项）**、不 taskkill、不让用户双击任何 bat｜总规划定位：**阶段二 · 2.3b 分片迁移**｜任务类型：**裁定落地 ＋ 推进**｜**优先级 P0**。

**本阶段目标**：落地 7 项裁定；放行并执行片6；启动汇编重做（出结构稿）。

## ④ 状态

**完成**：7 项裁定**全部落地**并记入 `BASELINE.md` §十一；**片6 已执行**（4 模块迁入，判据全过）；**汇编重做结构稿已出**（待发起人确认后再填充）。

**待裁定（不阻塞主线）**：Q10 的**映射规则**（甲/乙/丙）；汇编结构稿的 **5 个结构问题**。

## ⑤ 验收对照

| 验收标准 | 实测 |
|---|---|
| 7 项裁定全部落地、记入 BASELINE | ✅ `BASELINE.md` **新增 §十一（11.0–11.7）** |
| 片6 放行并执行、保真度判据通过 | ✅ 4 模块迁入；**多重集差四份全过**；闭包 PASS（50→**54**）；裸机构建 ✅；nostd 单测 **390 passed/0 failed**（+59）；⑨ 段 mirror **5/5** |
| `check_endpoint_table.py` 入 CI | ✅ 已入 `test` job（**机制 24**）；自检**五侧** ✅；实跑 权威 **23** ＝ 代码 **23**，差集 **0** |
| 汇编重做结构稿出 | ✅ `coordination/discussions/2026-09-18_汇编重做结构稿.md`（**9 篇 ＋ 附录 A/B/C**；**35/35 份落位**） |
| C15 复算通过、编号表连续 | ✅ 机制 **1–24**｜C **1–17**｜T **1–30**；**无缺号、无重号**；**阳性 3 ＋ 阴性 3** 对照全过 |
| CI 全绿 | ✅ 见第 ⑨ 项 |
| 不改文件（除授权项）／不 taskkill／不让双击 bat | ✅ |
| 下一次返回第 0 行含 9 步阅读指令 | ✅ 见本文件第 0 行 |

## ⑥ 未完成项（有理由，非停摆）

| # | 项 | 原因 | 下一步 |
|---|---|---|---|
| 1 | **Q10 映射规则** | 指令明示「**待定**——先出候选方案」⇒ 只出**甲/乙/丙**三案 ＋ 七要素 | 等发起人选择（**先答"物态是选择器还是调制量"**） |
| 2 | **汇编正文填充** | 指令明示「**先出结构稿，给用户确认后再填充**」 | 等结构稿确认（**5 问**） |
| 3 | **片7／片8 迁移** | 片6 已完成、**已解冻**，但本轮指令**未含**迁移任务 | 下一轮（3 模块） |
| 4 | **`--mode=resolve`** | 依赖本机 `PRIVATE_ASSETS`（**设计内行为**，机制 17 §10.2③） | 本机运行时设变量 |
| 5 | **Q11 入 CI** | 指令规定**等 Q8 生效 ＋ Q10 的 V2 实测完成** | Q10 裁定后 |

## ⑦ 真实缺陷（本轮新发现 3 条）

- **R64 ★（本轮最重要）**：`check_endpoint_table.py` 初版**锚点常量多写一个字**（写作 `##### §4.L3 对端点表`，正确为 `端点表`），而**自检夹具与常量同错** ⇒ **自检全绿、实跑判红**。
  ⇒ **处置**：修正锚点，并**新增自检第 ⑤ 侧「真实仓库锚点必须解析到路径（>0）」**，**禁止只测自造夹具**。
  ⇒ **普适教训**：**自检与实跑的口径必须同源**；夹具不能与常量共用同一处人工写法（此为 R62「判据的盲区」的**第二例**）。
- **R65**：`c15.py` 的**阴性对照已过期**（原用「T-029 不应存在」，而本轮 T-029 已建立 ⇒ 报 FAIL）。
  ⇒ **处置**：对照值随台账同步更新（阳性 机制24/T-030/C17；阴性 C18/T-031/机制25），全过。
  ⇒ **教训**：**对照值本身会过期**；台账增长后必须复核对照。
- **R66**：**「核心底图 6 份」的旧口径共 3 处**（脚本 docstring／`CORE` 列表／`ci.yml` 注释）。R61 上轮只统一了前两处。
  ⇒ **处置**：本轮一并修 **`ci.yml` 注释**，**三处全为 7 份**。

## ⑧ 三量台账（四量）

- **存量**：机制 **24**（新）｜**T-029／T-030**（新）｜`docs/MATH_SPEC.md` **§5**（Q10 前提两项）｜`LAYER_ARCHITECTURE.md` **§4.L3 权威端点表（23 路径）＋ §4.L3.1 层归属**｜其余 6 处端点口径改指向权威｜`check_endpoint_table.py` ＋ `endpoint_whitelist.txt`｜片6 **4 模块**迁入｜`verify.rs` **第 ⑨ 段**＋ mirror 测试｜汇编结构稿 1 份。
- **变量**：机制 **23 → 24**｜T **28 → 30**｜MATH_SPEC **v1.1 → v1.2**｜nostd 模块 **50 → 54**｜nostd 单测 **331 → 390**｜阶段二 **5/8 → 6/8 片**。
- **补充增量**：`BASELINE.md` §十一（8 小节）｜报告 1 份｜CI 新增 **2 步**（R60 提示 ＋ 机制 24 门禁）｜基线重生成 35 份｜汇编结构稿（9 篇 ＋ 附录 A/B/C）。
- **创新增量（首次）**：① **「自检与实跑口径必须同源」** —— 首次由**真实事故**（R64）推出并固化为判据的**第 ⑤ 侧**；② **「对照值会过期」** —— 首次把"阴性对照随台账同步"写成纪律（R65）；③ **「量纲断链」** —— 首次指出物态阈值（**入/出比**）与引擎输入（**[0,1] 种子**）**不同量纲、映射未定义**（`MATH_SPEC` §5.2）。

## ⑨ 边界说明（四类）

- **本地跑过** ✅ 7 类判据（syntax／resolve／purity＋selftest／closure／fidelity＋selftest／source_of_truth＋selftest／endpoint_table＋selftest）；C15 复算；`--update-baseline`；片6 迁移与保真度；nostd 裸机构建；nostd 单测 390；⑨ 段 mirror 5/5；V2 口径敏感性复跑。
- **CI 过** ✅ **本轮提交的 v0.200 run 三 job 全绿**（详见提交后的 CI 结果；`##[error]`＝0）。**新增的机制 24 门禁与 R60 提示步均在本 run 内首跑。**
- **真实跑过** ⚠️ **部分**：CI 内 **QEMU 真实引导**（采样点全绿）＋ **宿主真实开窗＋上屏回读**；❌ **未上真机**。
- **未验证** ❌：① **boot 层第 ⑨ 段未本地构建**（本机无 `dlltool`／`link.exe`／`clang`）—— 仅经 **host 镜像测试** ＋ CI boot-image job 覆盖；② **Q10 映射未裁定**（MATH_SPEC §5.2 为候选，未上线）；③ **汇编正文未填充**；④ `--mode=resolve` 依赖本机变量；⑤ **片7／片8 未迁**；⑥ 机制 24 在**真实 push 的 CI 上**是本轮首次运行（结果见第 ⑨ 项 CI 状态）。

## ⑩ 决策评估（七要素）

**本轮我方无新增提案式决策**（7 项均为**已授权执行**）。

**唯一需要新增前提的是 Q10 的映射规则**：

| 七要素 | 内容 |
|---|---|
| **评估** | Q10 有**前置问题**：物态阈值判「**入/出比**」（域 `(0,+∞)`），引擎输入是 **`[0,1]` 种子** ⇒ **量纲不同**，阈值**无法直接施加** |
| **优势** | 先定映射 ⇒ 可**一次性**解冻 Q10／Q11，并把 V2 自动化 |
| **劣势** | 三案各有代价：甲=节点斜率跳变；乙=边界与刻度不等距；丙=丢失"强度"语义 |
| **风险** | **不先定映射就选迟滞/插值，可能是对错误问题的答案**；且两处跳变叠加会恶化 |
| **机会** | 选丙（**物态只当选择器**）可**不动 A1**、不新增量纲，问题收敛为单变量 |
| **建议** | 先答「**物态是选择器，还是调制量？**」⇒ 若为选择器，**推荐丙**并立即解冻 Q10 |
| **等待决策** | 该前问 ＋ 甲/乙/丙 选择 |

## ⑪ 需要用户决策（6 条）

1. **Q10 前问：物态是「选择器」还是「调制量」？**（决定甲/乙/丙 是否可比）
2. **Q10 映射：选甲（分段线性）／乙（对数压缩）／丙（只选引擎，推荐）**。
3. **汇编结构稿 5 问**（附表 附录 C）：篇序／验证篇是否合并／附录是否一并交付／`LAYER_BASEMAP_L0_L6.md` 是否进主体／逐字收录还是只收要点。
4. **片7／片8 是否本轮之后立即启动**（已解冻，3 模块）。
5. **R64 教训是否固化为通用判据**（「自检必须回读真实仓库」写进机制 21/22/24 的通用要求）。
6. **Q12/Q13 已裁定并落底图**——是否**复核** `LAYER_ARCHITECTURE.md §4.L3／§4.L3.1` 两节条文表述。

## ⑫ 下一步建议

1. 先答 **Q10 前问**（一句话即可）⇒ 解冻 Q10／Q11。
2. 确认**汇编结构稿 5 问** ⇒ 我再填充正文（**填充前不擅自动手**）。
3. 启动 **片7／片8**（3 模块；同片6 流程，已有 ⑨ 段模板）。
4. 复核 `LAYER_ARCHITECTURE.md` 两节新条文（Q12/Q13 落地件）。

## ⑬ 启动回执

已读 `CHARTER.md`（含 D51 底图硬指针 ＋ 机制 23／**24**）。当前状态：**v0.200**｜阶段二 · 2.3b **片6 ✅ → 片7**｜本次任务：七项裁定落地 ＋ 片6 执行 ＋ 汇编重做启动｜是否继续：**执行完毕，等裁定**。

---

## 附 · 本轮读过的底图（D54）

| 底图文件 | 行号范围 | 读它做什么 |
|---|---|---|
| `README.md` | 行 75、行 114（结构行） | 核对命名（Q16 已改）与端点口径行（Q12 改指向） |
| `docs/LAYER_ARCHITECTURE.md` | 行 172–246（**§L3 节 ＋ §4.L3 ＋ §4.L3.1**） | **Q12 权威端点表落点**、**Q13 层归属条文** |
| `docs/MATH_SPEC.md` | 全文 1–54（§1 三引擎／§4 A1–A5／版本记录） | **Q10 前提两项写入点**（新增 §5） |
| `docs/GENE_LIBRARY_DESIGN.md` | 行 51–75（§2.1 物态阈值 行61） | 物态阈值原文（Q10 量纲问题的来源） |
| `docs/SELF_DIAGNOSIS_REPORT.md` | 行 54 | 端点口径第 2 处（改指向权威） |
| `docs/API_GATEWAY_DESIGN.md` | 行 51–56（§4 协议规范 v0） | 端点口径第 3 处 |
| `docs/FULL_AUDIT_REPORT.md` | 行 212 | 端点口径第 4 处 |
| `docs/GENE_LIBRARY_DESIGN.md` | 行 252 | 端点口径第 5 处 |
| `docs/L7_EXECUTION_DESIGN.md` | 行 291 | 端点口径第 6 处 |
| `docs/` 全 35 份 | 标题行（H1） | **汇编结构稿骨架**（逐份落位对账） |
| `npb-gateway/src/http.rs` | 非测试区（23 路径） | 机制 24 代码侧口径 |
| `npb-gateway/src/{selfmon,msg}.rs` | 行 1–26（模块头） | Q13 层归属证据（**非底图**，代码自陈） |
| `meta-kernel-core/src/positive_source.rs` | 行 35／337／409 | **3 行 `O(1)` 注释**（片6 可追溯项） |
| `meta-kernel-core-nostd/src/positive_source.rs` | 行 12–34（迁移头）＋ 50／352／424 | 核对迁移说明与修订结果 |
| `meta-kernel-boot/kernel/src/verify.rs` | ⑧ 段尾（新增 ⑨ 段落点） | 裸机断言扩段 |
