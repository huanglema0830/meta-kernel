【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · push `6cea56f` ＋ 5 项裁定落地（含 `R91` 登记）＋ 整体规划两部分出稿（v0.323）

> ⚠️ **本轮口径提示**：本报告标题的 `（v0.323）` **沿用现状"轮次号"写法**；而 **`git rev-list --count HEAD` ＝ 335**（见 ⑬）。**两者不等** —— 此即承上轮未结的 **`C-1`（`v` 号与 `count` 脱钩）** 的现状，**本报告两口径并列，便于比对**（**处置待裁**，见 ⑪）。

## ① 保密标记

🔓 **公开** —— 本轮**零机密内容**：只涉及**机制编号／约束编号／术语编号／判据源码／计数口径／规划正文**，均为仓库内公开文件。
⚠️ **本轮主动守住一条边界**：**密钥级别相关字面量（密钥本体／具体路径／指纹）一律不写**（`C12` 密钥专项级别，**⛔ 机密**）；**用户级记忆与项目记忆的落点只写"工作区"**，**不写本机绝对路径**（`C12` 第 3 条：具体路径属 ⛔）。

## ② 全景状态卡

| 项 | 值 |
|---|---|
| **阶段** | **阶段二 · 2.4 边界层已验** |
| **基准 → 收口** | `count 333`（`b3dc987`）⇒ **335**（`6cea56f`；**本轮 2 个本地 commit**） |
| **push** | `b3dc987..6cea56f`（**exit 0**）｜**本轮产出 1 批待放行**（**机制 23**：push 须当轮明示） |
| **CI 状态** | **全绿**（三 job 全 success；★ **run 号与逐 job 三态按 `C20` 只记日报、不入库**） |
| **本轮性质** | 放行 ＋ **5 项裁定落地** ＋ **整体规划两部分出稿（只出稿）** |
| **INTEL** | 观察中 **2**／待准备 **0**／准备中 **0**／已实施 **0**／已归档 **0**（**本轮无状态变更**，见 附件 B） |

## ③ 任务（含基准／定位）

**指令（本轮）**：**〇 push 放行**（2 个 commit，先 push 再动文件，`T-038` 门控，`C20`）＋ **一、5 项裁定落地**（① `C-3` 并入 `R` 族；② 规划稿 7 项分两路；③ 承上轮未结 `C-1`／`C-2`／`check_version_label.py`／`F-编号口径`；④ 上轮欠的指令段；⑤ 元层级表述纪律入条文）＋ **二、整体规划两部分出稿**（第一部分六章／第二部分八章）。
**总规划定位**：放行 ＋ 裁定落地 ＋ **只出稿**。
**基准**：`count = 333`（`b3dc987`）—— ★ **实测与指令一致**。

**★ 本轮读了底图哪几份、哪几行**（**如实记**）：

| 文件 | 行／范围 | 用途 |
|---|---|---|
| **底图** `docs/LAYER_ARCHITECTURE.md` | **§2 总表**／**§2.1 L7–L10**／**§3 产品形态**／**§3.2 命名两族**／**§4.1.2 回写表**（**477 行**） | 第一部分 §2.1 架构总览 ＋ §2.2 三层形态（**效力列**来源） |
| **底图** `docs/VISION.md` | **全文（74 行）**；重点 **§3 不替代** | 第一部分 §1.1 定位 ＋ §3 云操作系统的"外头能做的我们不做" |
| **底图** 根 `README.md` | **§版本规则（行 87–94）** | 第二部分 §七 版本号口径（机制 27／`C21`） |
| `coordination/CHARTER.md` | **§三 全部机制（260 行全文）**／**§四 C1–C21 速查** | 第二部分 **§一 机制体系 ＋ §二 约束体系** |
| `coordination/CONSTRAINTS.md` | **全文（494 行）** | 第二部分 §二／§七／§八（`C15`／`C18`／`C19`／`C20`／`C21` 全文） |
| `coordination/TERMS.md` | **全文（122 行；§一～§一之四）** | 第二部分 **§三 术语体系（`TERM-001–014`）** |
| `coordination/TEMPLATES.md` | **§二 十四项**／**§7.2（`T-049`）**／**§十九** | 第二部分 §五 报告格式 |
| `coordination/BASELINE.md` | **§六 缺陷表**／**§五十七**／**§五十八 全节** | `R91` 登记落点 ＋ 上轮留痕 ＋ C15 基准 |
| `coordination/intel/INDEX.md` | **全表** | 附件 B INTEL 状态表 |

## ④ 状态：完成

**完成** —— **〇 push ＋ 5 项裁定 ＋ 整体规划两部分** 均已落地；**无截断、无未收部分**。

## ⑤ 验收对照

| # | 验收标准 | 结果 |
|---|---|---|
| 1 | 〇 push 完成，CI 三 job 全绿，`T-038` 门控 | ✅ `b3dc987..6cea56f`（**exit 0**）；**`READY_TO_EDIT`**（`gh run list` **completed**；**三 job 全 success**；`##[error]` **0 行**；**报告结构门禁 step 真跑＝YES**）；★ **push 先于任何文件改动**（**时序已核**） |
| 2 | `C-3` 并入 `R` 族，逐字对照，记 BASELINE | ✅ **登记 `R91`**（`BASELINE.md` **§六 缺陷表行** ＋ **§一 口径行**；**逐字对照见 §59.2**）；**过 `C15`**（`R` 去重 **54→55**、最大号 **90→91**） |
| 3 | 规划稿 7 项分两路处理 | ✅ **涉底图 4 项**（第 2／3／4／5）⇒ **专题稿** `2026-09-22_L0-L6专题_规划稿7项涉底图部分.md`（**只出稿**）；**不涉 3 项**（第 1／6／7）**逐项列出**（见 §59.3） |
| 4 | `C-1`／`C-2` 落地（`run_all_checks.py`／轮次号方案稿／`F-编号`成文） | ✅ **`C-2` 固化** ⇒ **新建** `coordination/tools/run_all_checks.py`（**自检 PASS**）；**`C-1` ⇒ 轮次号机制方案稿**（`2026-09-22_轮次号机制方案稿.md`，**只出稿**）；**`F-编号口径` 成文**（**§59.4**，**逐字**）；**`check_version_label.py` ⇒ 方案稿 §三**（**只出稿**） |
| 5 | 元层级表述纪律入条文 | ✅ **`T-049`**（`TEMPLATES.md` **§7.2 行** ＋ **§十九 正文**）；**过 `C15`**（`T` **48→49**、顶层节 **24→25**） |
| 6 | 第一部分出（**六章**） | ✅ `2026-09-22_云内核整体规划_第一部分_技术与产品.md`（**六章齐**） |
| 7 | 第二部分出（**八章**） | ✅ `2026-09-22_云内核整体规划_第二部分_管理与运行.md`（**八章齐**） |
| 8 | 两部分独立可读可备份 | ✅ 每份**自带 9 步阅读指令（字面第 0 行）** ＋ **边界说明四类**；**互指关系见两稿 §交叉引用** |
| 9 | 只出稿，不改底图 | ✅ **`docs/` 零改动**；**判据 F PASS**（底图 `.md` **36 份**，与上轮同值） |
| 10 | `C15` 复算通过，编号表连续无缺号无重号 | ✅ 机制 `1..30`／`C 1..21`／`T 1..49`／`TERM 1..14`／`R` 去重 `1..91`／节号 `0..59` **连续无缺重**（见 §59.7） |
| 11 | CI 全绿 | ✅ 三 job 全 success（★ run 号按 `C20` 只记日报） |
| 12 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告字面第 0 行＝固定文本（**判据 `check_reading_instruction` PASS**） |

## ⑥ 未完成项

| 项 | 状态 | 原因 |
|---|---|---|
| **轮次号机制定型** | ⬜ **未做（只出方案稿）** | **指令明示"只出稿、等确认后再改"** ⇒ **施工门槛未过**（**①–⑤ 待确认**） |
| **`check_version_label.py` 实现** | ⬜ **未做（只出方案稿）** | **须先定 `C-1`**（**指令明示**） |
| **L0–L6 专题 6 项确认** | ⬜ **待裁** | **涉底图回写** ⇒ **须专项裁定**（**本轮只出专题稿**） |
| **`M6`／`M8` 裁定** | ⬜ **待裁** | **属决策类**（**不涉底图**，可直接处理，但须用户拍板） |
| **"每轮跑全部判据"的机器强制** | ⬜ **未做** | **`T-048` 靠运行器 ＋ 报告承载**，**无 CI 兜底**（**如实边界**） |

## ⑦ 真实缺陷

**★ 本轮发现 0 条新缺陷**（如实记）。逐类说明：

- **`R91`（并入 `R` 族）**：**属上轮已识别**（v0.321 首次以"规划整合度"为对象），**本轮＝授权后登记**，**非新发现**。
- **自指污染（本轮第 8 例 · 自捕获 · 未入库即成稿前修正）**：`R91` 首稿曾误加进 `BASELINE.md` **§一 指针表**（**引号块前缀 `> |` 不在登记四形态内**）⇒ `check_id_set_diff` 告警 **3→4** ⇒ **改按 §六 缺陷表行登记** ⇒ **复跑回 3**。**法**：**先定位判据的登记形态（行首锚），再决定落点**（**同 `T-049` 立法思路**）。
- **一处未验证（非缺陷 · 如实记）**：★ **CI 与本机判据姿势差异未单测**（`check_private_pointers` 在 CI 走 `--mode=syntax`、**本机实跑亦用同款**）—— **R70 家族：环境类差异**。

## ⑧ 三量台账

| 量 | 内容 |
|---|---|
| **存量** | **机制 30**（不变）｜**`C` 21**｜**`T` 49**（48→49）｜**`TERM` 14**｜**`R` 去重 55／最大 91**（54／90 → 55／91）｜**判据脚本 11 个**（**只跑不改**）｜**台账 59 节**（58→59）｜**`reports/` 152**（147→152）｜**记忆三层**：调用层 **977 B**／索引层 **3931 B**／内容层 **4 份** |
| **变量** | `coordination/BASELINE.md`（**§六 `R91` 行 ＋ §一 口径行 ＋ 新增 §五十九**）｜`coordination/TEMPLATES.md`（**§7.2 `T-049` 行 ＋ 新增 §十九**）｜**新建** `coordination/tools/run_all_checks.py` |
| **补充增量** | ① **`R91` 登记**（裁定①）② **`T-049` 登记**（裁定⑤）③ **`F-编号口径` 成文**（裁定③）④ **`run_all_checks.py`**（`C-2` 固化）⑤ **轮次号机制方案稿**（`C-1`）⑥ **L0–L6 专题稿**（裁定②）⑦ **整体规划第一部分（六章）** ⑧ **整体规划第二部分（八章）** |
| **创新增量** | ① ★★ **`run_all_checks.py`：首个"判据统一运行器"** —— **姿势内建（`CALLS` 唯一出处）**，**消除"用法错＝假红"**（**`C-2` 治法**；**自检五侧**含**空调用 rc=64／不存在 rc=127**）② ★ **`T-049` 元层级表述纪律**：**零成本治自指污染**（**仅表述约束、无需判据脚本**）③ ★ **规划两部分**：把散落三条线（技术／管理／运行）**首次整合为两册可独立阅读的规划稿** |

## ⑨ 边界说明

| 类 | 内容 |
|---|---|
| **本地跑过** | ✅ **11 判据全跑**（**11/11 PASS**，读**真实仓库**，非夹具）｜**`run_all_checks.py --selftest` PASS**（五侧）｜**`C15` 复算**（机制／`C`／`T`／`R`／节号连续）｜**门控实跑**（`READY_TO_EDIT`） |
| **CI过** | ✅ **三 job 全 success**（`test`／`宿主 field-render`／`元内核引导镜像`；**`T-038` 门控已执行**；**run 号按 `C20` 只记日报**） |
| **真实跑过** | ⚠️ **部分**：11 判据**读真实仓库**跑过；但 **`migration_fidelity` 跑的是 `--selftest`（造例）**，**未对真实迁移文件跑**（**本轮无迁移**）⇒ **非全真** |
| **未验证** | ① **轮次号机制定型**（**待裁**）② **`check_version_label.py` 实现**（**待 `C-1`**）③ **L0–L6 专题 6 项**（**待确认**）④ **`M6`／`M8`**（**待裁**）⑤ **规划两部分"一句话"栏为索引性摘要** —— **未逐条回读原文并比对**（**如实标注**）⑥ **CI 与本机判据姿势差异**（**未单测**） |

## ⑩ 决策评估（七要素 · 本轮**新决策 1 项**：轮次号机制的定型方式）

| 要素 | 内容 |
|---|---|
| **评估** | 三案：**甲 维持现状**（`v` 号继续与 `count` 脱钩、只观察）｜**乙 回正为 `count`**（把 `v` 号改回 `count`，含历史回填）｜**丙 另立"轮次号"**（`v` 号＝轮次号，与版本号**分家**，各自成文） |
| **优势** | **甲**：**零风险、零改动**｜**乙**：**一次性对齐 `C21`**，判据可复现｜**丙**：**最诚实** —— 现状事实上就是"轮次号"；**两条口径各自能自证**（**本方案稿已给落地形态**） |
| **劣势** | **甲**：**漂移继续**（本报告 `v0.323` ↔ `count 335`，差 **−12**）｜**乙**：**改元规则 ＋ 改写历史陈述**（**与 `C19`"历史只新增不改写"冲突**）｜**丙**：**新增一套编号**（**须防与 `T`／`R`／`C` 族同病**：**登记不严则又漂**） |
| **风险** | **甲**：**读者据 `v` 号做算术 ⇒ 结论错**（**概率中／影响中**）｜**乙**：**动了不该单方动的东西**（元规则 ＋ 历史，**概率中／影响高**）｜**丙**：**两套号并存 ⇒ 引用更易混**（**须明写"哪处用哪个号"**；**概率中／影响中**） |
| **机会** | ★ **"乙→丙"两步走**：**先经乙（观察、如实记录）** ⇒ **再落丙（另立轮次号 ＋ 补机器判据 `check_version_label.py`）** ⇒ **把 `C21` 从"条文"变成"判据"**（**正是 `F-5` 的治法**） |
| **建议** | **采纳"丙"**（**方案稿已备**）：`v` 号＝**轮次号**（**与 `count` 分家**）＋ `C21` **补一句边界**（"**轮次号不属版本号**"）＋ **同步 `T-045`**（改称"报告提交计数"，另加"报告轮次号"）⇒ **两类口径各自能自证**。★ **不建议乙**（**改历史陈述 ⇒ 触 `C19`**） |
| **等待用户决策** | ✅ **等**：见 ⑪ ① |

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★★ **轮次号机制是否定型**（**名称／形态**：`RND-nnn`？`轮次-n`？／**取数规则**：按会话？按日？／**`T-045` 是否随之改称**／**`check_version_label.py` 是否入 CI**／**历史报告是否豁免**） | **荐定型为"丙"**：**先只告警**（同 `check_id_set_diff` 先例）＋ **历史报告豁免**（同 `C13` 时间边界先例）；★ **定型前不写进任何条文**（**避自指污染**） |
| ② | ★ **L0–L6 专题 6 项确认**（涉底图 **4 项** ＋ 共同前置 `M6`／`M8`） | **请在专题稿逐项裁定**；**确认后一次回写底图**（**四步连带**：触判据 F ＋ 汇编重生成 ＋ 机制 25 P3 复核） |
| ③ | **`M6`／`M8` 裁定** | **荐先裁**（**不涉底图** ⇒ 可直接处理，**但与专题稿同批更省一轮**） |
| ④ | **本轮 push 放行**（**1 批本地 commit**：`R91`／`T-049`／`run_all_checks.py`／两份规划稿／报告） | **放行**（**机制 23**：push 须当轮明示） |

## ⑫ 下一步建议

1. **若确认 ①** ⇒ **施工轮次号机制**：`CHARTER` 立「轮次号机制」＋ `CONSTRAINTS C21` **补边界一句** ＋ **同步 `T-045`** ＋ **实现 `check_version_label.py`**（**先只告警**）。
2. **若确认 ② / ③** ⇒ **L0–L6 一次回写底图**（**四步连带**）＋ **`M6`／`M8` 落地**。
3. **继续**：`R90`／`B5` 重试路径**继续等真实抖动**（**追认，不人为造红**）。
4. **补齐**：**规划两部分"一句话"栏**待用户抽检；**如需逐条回读原文比对**，**另派一轮**（**本轮已如实标注为未验证**）。

## ⑬ 启动回执

已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD` 的输出 ⇒ 335**（**本报告所在提交的 count**，依 **`T-045`** 口径）；**起点 `count 333`，本轮 2 个本地 commit**（`e38d1df`／`6cea56f`）。★ **标题 `（v0.323）` 系沿用现状"轮次号"写法 —— 它与 `count 335` 不等**（**差 −12**），**此不符即承上轮未结之 `C-1` 缺陷**（**两口径并列，便于比对**）。

---

## 附件 A · `B2`＋`C1` 告警行（固定一行）

**★ 六族告警（`C1` 载体 · 固定一行）**：**六族告警：本轮 3 条**（`C22`／`T-00`／`TERM-999`，**均为元引用**）**｜对比上轮 ±0**｜**真缺口 0 条**。

（口径：`check_id_set_diff.py` 实跑；**3 条全在 `A2` 豁免登记表**；**剔除 6 层**。★ 本轮 `R91` **已登记** ⇒ `R` 族引用/登记 **齐**，**未新增告警**。）

---

## 附件 B · INTEL 状态表（附加表 · 依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| INTEL-001 工信部 AI 中小企业计划 | 观察中 | 组合：企业注册完成 ＋ 阶段三启动 | ✗ **未触发**（现处阶段二 · 2.4） | 无 |
| INTEL-002 GPT-6 安全风险 | 观察中 | 事件：L7–L10 落地后 | ✗ **未触发**（L7 设计完成／L8–L10 冻结） | 无 |

**J1–J4 自查**：J1 报告须含状态表 ✅｜J2 触发条件满足但状态未变 ⇒ 违规 —— **两条均未触发** ✅｜J3 待准备但无准备稿 ⇒ 违规 —— **两条均非"待准备"** ✅｜J4 准备中但无具体行动 ⇒ 违规 —— **不适用** ✅。

★ **机制 29 本轮无状态变更**（不虚触）。
