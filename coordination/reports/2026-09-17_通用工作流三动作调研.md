【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

**保密标记**（C12）：⛔ 机密（**绝不拷**）密钥本体／密钥存放路径／账号凭证与登录态文件路径／原术语／全拼／本机绝对路径／内网 IP → **已预屏蔽**（本文只写结构性描述，**不写具体路径**）｜🔐 方法形态｜🔒 模块结构·统计数·版本号｜🔓 编号·平台公开定价

> 说明：**操作级细节（真实路径、命令原文、账号状态快照）写在仓库外的项目私记里**，本文只保留骨架级内容 —— 与机制 17「骨架-实现分离」同口径。

```
┌─ 通用工作流 · 三动作调研（2026-09-17）───────────────────────┐
│ 方法  通用工作流·场域演化 **八步**（骨架层；**实现层仍为占位**）  │
│ 动作① 省钱  平台口径已取到｜**本机免费额度在流失**               │
│ 动作② 浏览器 对手实测：Edge 690MB ＋ WebView2 668MB ／ SLBrowser 1,615MB │
│ 动作③ 操作系统 对手实测：Windows 11 25H2 build 26200.x          │
│ 云内核底盘 模块 46 个｜**显示/输入/网络/存储/驱动 = 0**          │
│ 判定  ①**现在就能省** ②**有现实起点（宿主侧）** ③**不可达（差一个阶段）** │
│ 待拍板 **4 项**（见 §八）                                       │
└──────────────────────────────────────────────────────────────┘
```

# 通用工作流 · 三动作调研报告

> 归口：通用工作流（八步）｜日期：2026-09-17｜性质：**调研 + 可行性判定**（不改代码、不改配置）
> ⚠️ **方法边界**：本次只用**八步骨架**（步名／顺序／输入输出），**不涉实现层公式与阈值** ——
> 因为 `{PRIVATE_ASSETS}/workflows/sub/01..08_*_impl.md`（**模式**写法：指 01–08 八个文件的合集；**8 个文件目前全是占位**，每个 26 行、9 处「待填」）。
> 这一点先说，避免把"框架走了一遍"误读成"算法跑了一遍"。

---

## 第 1 步 · 罗列（场域全息采样：把三块场域的现实记下来）

### A. 平台与账号面（动作①场域）

| 项 | 实测（平台公开口径） |
|---|---|
| 个人版档位 | **体验版 免费** ⇒ **500 积分/月**（内置模型**部分可用**，Auto 调度）｜标准版 99 元/月 ⇒ 实得 **4,000**｜高级版 199 元 ⇒ **9,000**｜旗舰版 999 元 ⇒ **50,000** |
| 折扣机制 | 连续包月 7 折；**连续包年在 7 折基础上再 8 折 ＝ 5.6 折**（标准版 **672 元/年**） |
| ⚠️ **有效期** | **加赠积分自到账起 1 个月内有效** —— **不用即作废** |
| 计费口径 | **积分消耗取决于「任务复杂度 ＋ 模型档位」**；官方明说：**Auto 调度有时比手动选高档模型更省积分** |
| 补充渠道 | ① **邀请好友**；② **社区发文**；③ 升档 |
| 本机免费渠道 | **Buddy 加油站签到 100/天（连续第 7 天 1,000）**；**派猫猫 5–10/天** |
| ★ **本机账号实测** | **今日未签到**（连续天数 **0**）；**猫闲置、可派遣** |
| ★ **流程缺口** | 已登记的 30+ 条定时任务里，**没有任何一条做积分签到/派遣** ⇒ **免费额度在持续流失** |
| 项目自身设施 | 机制 17 第二实例「大模型接入骨架」：**任务分级／降级规则／成本记录** 三接口 —— 公开层 4 文件 ✅；**实现层 4 文件全部「待填」** |

### B. 浏览器面（动作②场域）

| 对手 | 版本 | 安装日期 | 安装体积 |
|---|---|---|---|
| **Microsoft Edge** | **150.0.4078.99** | 2026-07-25 | **690 MB** |
| **Edge WebView2 Runtime** | 同代 | —— | **668 MB** |
| **联想 SLBrowser**（对手二） | **9.0.8.8311** | 2026-09-02 | **1,615 MB** |

- 联想同族预装（构成"生态绑定"）：`PCManager`（电脑管家）／`LeAppStore`（应用商店）／`VantageService`／`baiying`（百应）／`LSF`／`LenovoInternetSoftwareFramework`／`Lenovo Speech`／`Smart Meeting`
- 用户数据规模：`%LOCALAPPDATA%` 下 **1,934 MB** ／ `%APPDATA%` 下 **39 MB**
- 全机已装程序：**93 项**（64 位 40 ＋ 32 位 53）

### C. 操作系统面（动作③场域）

| 项 | 实测 |
|---|---|
| 系统 | **Windows 11 25H2，build 26200.9457** |
| ⚠️ 判据陷阱 | 注册表 `ProductName` 仍写着 **"Windows 10 Home China"** —— 这是 Win11 的**已知陈旧值**，**不能按它判版本**（本次靠 `DisplayVersion=25H2` ＋ `CurrentBuild=26200` 才判对） |
| CPU | **Intel Core Ultra 5 125H** |
| 磁盘 | C: 501G（已用 203G，41%）｜D: 452G（**92%**）｜F: 932G（**99%**，仅剩 18G） |
| 云内核自身底盘 | 已入 nostd **46 个模块**（纯算法/逻辑层）；**显示 / 输入 / 网络 / 存储 / 驱动 ＝ 0** |
| 宿主侧家底（非内核） | `host/field-render`（渲染器，独立 crate，11 个验收模式）｜`host/sky-browser`（降级保留） |
| ROADMAP 定位 | 阶段二＝**可引导 ISO（U 盘起，不经过 Windows）**🚧｜**阶段三＝可安装操作系统、「像装 Ubuntu 一样替换 Windows」—— 已登记为后续阶段** |

---

## 第 2 步 · 分析（四态场分析）

| 动作 | 存量（已有） | 变量（在动） | 补充增量（缺口） | 创新增量（新稳态） |
|---|---|---|---|---|
| **① 省钱** | 免费档 500/月；签到＋派猫猫；Auto 调度；LLM 骨架三接口 | 积分**每日自然流失**（未领取） | ① 签到自动化 ② 实现层三接口填值 ③ 用量可观测 | **"每任务积分单价"成为可度量指标**（当前**不可测**） |
| **② 浏览器** | 宿主 `field-render`（真渲染器）＋`sky-browser`（降级）；对手两台可实测 | 对手持续升级（Edge 已 150 代） | 标签/书签/密码/扩展/同步/PDF/下载/多进程隔离/内核沙箱 | **宿主态"够用即换"**（不追内核先做浏览器） |
| **③ 操作系统** | 阶段二可引导镜像（QEMU 已验证）；46 模块逻辑层 | 阶段二未完成 | **显示（DRM/KMS）→ 输入 → 存储 → 网络 → 驱动 → 用户空间** | 阶段三"可安装、可替换" |

**一句话**：三块的**性质完全不同** —— ①是**流程问题**（今天就能动），②是**工程问题**（有起点、要排期），③是**路线问题**（差一整个阶段，不是"深挖一下"能到）。

---

## 第 3 步 · 推演（时间演化：三条时间线）

| 动作 | 立即（本周） | 中期（1–3 个月） | 长期（6 个月+） |
|---|---|---|---|
| ① | **接上签到＋派猫猫自动化**；把"用量可见"做出来 | 填 LLM 实现层（档位映射／降级链／成本台账）；建立**每任务单价** | 形成"**先用免费 → 再用最省 → 才用高档**"的自运转 |
| ② | 定目标形态（宿主态 or 内核态）；出**功能对照清单** | 宿主态 MVP：**打开/多标签/前进后退/书签/下载/PDF** | 追平"日常可替代"（密码、扩展、同步、性能） |
| ③ | 只做**只读对比基线**（本次已起头） | 阶段二收口：**可引导 ISO 起得来 + 显示/输入最小闭环** | 阶段三：存储/网络/驱动/用户空间 ⇒ 才谈"替换" |

---

## 第 4 步 · 拆颗粒（尺度分解：把"替换"拆到最小颗粒）

**"替换 Edge/SLBrowser"** 拆成 9 颗：
渲染与合成｜HTML/CSS/JS 引擎｜网络栈（HTTP/2/3、TLS、代理）｜存储（Cookie/缓存/IndexedDB）｜
进程与沙箱｜扩展机制｜账号与同步｜输入法/剪贴板/拖拽｜系统集成（默认浏览器、协议唤起、PDF 预览）

**"替换 Windows"** 拆成 8 颗：
引导与固件接口｜内核与调度｜驱动模型（GPU/网卡/存储/输入/外设）｜显示与窗口系统｜
存储栈与文件系统｜网络栈｜系统服务与安全（账号/权限/更新）｜用户空间与兼容层（Win32/POSIX）

> **颗粒级实测**：云内核当前在②③的 17 颗里，**已具备 0 颗**（逻辑层 46 模块不落在任何一颗上）；宿主侧在②里**部分具备 1 颗**（渲染与合成）。

---

## 第 5 步 · 找思路（局部动力学：每颗粒的"规则／力／边界条件"）

| 颗粒组 | 力的来源 | 边界条件（硬约束） |
|---|---|---|
| ① 省钱 | **积分单价 × 任务复杂度**；免费额度**过期作废** | 免费档模型**部分可用**；额度不可透支；加赠**1 个月**有效 |
| ② 浏览器 | 生态兼容（网页按 Chrome 行为写）＋ 用户习惯 | **必须兼容 Chromium 行为**，否则"能打开但用不了"（银行/政务页面尤其） |
| ③ 操作系统 | 硬件驱动生态（GPU/网卡/存储）＋ 应用生态 | **驱动与兼容层由第三方定义**，不是自己能随便定的 —— 这是"替换 Windows"的真正闸门 |

---

## 第 6 步 · 化合（自组织涌现：能组合出的最小可用体）

- **① 最小可用体**＝「签到/派遣定时任务」＋「用量读数」＋「档位策略」三件合起来 ⇒ **当天可成**
- **② 最小可用体**＝宿主 `field-render` ＋ 最小网页容器 ⇒ **宿主态浏览器**（不动内核）
- **③ 最小可用体**＝阶段二镜像「能起来、能显示、能输入」 ⇒ **先做"能用的最小系统"，不做"替换"**

---

## 第 7 步 · 执行（真实场演化：本次已做 / 待做）

**本次已执行（只读，无副作用）**
1. 平台计费口径取证（官方文档口径）
2. 本机账号状态取证（**读到了：今日未签到、连续 0 天、猫闲置**）
3. 两台对手浏览器实测（版本／体积／生态绑定／用户数据规模）
4. 操作系统与硬件实测（版本／构建／CPU／磁盘占用）
5. 云内核底盘清点（46 模块；显示/输入/网络/存储/驱动 = 0；宿主侧 2 个 crate）
6. 发现并**绕通**一个工具链缺陷（见 §七）

**待执行（等你拍板，见 §八）**：签到自动化｜LLM 实现层填值｜②③ 的深度扫描与对照清单

---

## 第 8 步 · 输出（相图：三动作判定）

| 动作 | 判定 | 依据（实测） | 最短可行第一步 |
|---|---|---|---|
| **① 省钱** | 🟢 **现在就能做，且有"正在漏"的确定损失** | 免费渠道存在但**零自动化**；账号实测未签到 | **把签到＋派猫猫接成每日定时任务** |
| **② 替换浏览器** | 🟡 **有现实起点，但要先定形态** | 对手可实测；云内核 GUI 面无，但**宿主有真渲染器** | 定"宿主态 or 内核态" ⇒ 出功能对照清单 |
| **③ 替换操作系统** | 🔴 **当前不可达：差一个完整阶段** | ROADMAP 阶段三才谈；**显示/输入/存储/网络/驱动全为 0** | 只做**只读对比基线**；主攻阶段二收口 |

> ⚠️ **必须说直白的一句**：动作③的"至少能替换 Windows"，**在阶段二没完成之前不是一个可排期目标**。
> 不是我不做，是**中间隔着驱动模型与用户空间这两个由第三方生态定义的闸门**。
> 谁要说"深挖一下就能替换 Windows"，那是把**路线图问题当成了调研问题**。

---

## 三量台账（四量）

- **存量**：平台口径（4 档）｜免费渠道 2 条｜对手 2 台实测｜OS 实测｜云内核 46 模块＋宿主 2 crate｜LLM 骨架 4＋实现 4（待填）
- **变量**：积分每日流失｜对手持续升级｜磁盘 F 盘告急（99%）
- **补充增量**：签到自动化｜用量读数｜LLM 实现层｜浏览器功能对照清单｜OS 只读对比基线
- **创新增量**：**「每任务积分单价」可度量**｜**"宿主态替换"路线**（不动内核先换浏览器）

## 边界说明（四类）

| 类 | 内容 |
|---|---|
| **本地跑过** | 账号状态只读查询 ✅｜浏览器/OS/磁盘实测 ✅｜云内核底盘清点 ✅ |
| **CI 过** | 不涉及（本次为只读调研，未改代码） |
| **真实跑过** | **无**——本次**没有跑**任何"省钱自动化"或"替换验证"（**未验证就是未验证**） |
| **未验证** | ① 签到自动化的实际收益（能领多少、会不会中断）② 宿主态浏览器的可行性（未做原型）③ 三个阶段的时间量级（**没有估算依据，不给假数**） |

## 真实缺陷（本轮 5 条）

1. ★ **免费积分零自动化**（流程缺口，**正在造成确定损失**）
2. **技能文档路径写错**：`workbuddy-reward-helper` 把 Windows 登录态写成 `%APPDATA%`，实测在 **`%LOCALAPPDATA%`** ⇒ 直接跑报"登录态读取失败"；**用环境变量覆盖可绕通**（细节在仓库外私记）
3. **判据陷阱**：注册表 `ProductName` 在 Win11 上仍写 "Windows 10" ⇒ **按它判版本会判错**
4. **磁盘风险**：F: 99%（剩 18G）
5. **工具链异常**：本回合 PowerShell 通道**完全无回显**（最小连通性测试也空），已全程改走 Bash/`reg` 通道，未影响取证

---

## 八、需要你拍板（4 项，各一句）

| 编号 | 事项 | 说明 | 我的建议 |
|---|---|---|---|
| **D42** | 是否把「签到＋派猫猫」接成**每日定时任务** | 可逆、零成本、当天见效；不接＝继续漏 | **接**（建议每日 09:00，与既有"晨间汇总"错开） |
| **D43** | 动作②的**目标形态** | ① **宿主态**（用 `field-render` 做浏览器容器，不动内核）② 内核态（等阶段三） | **① 宿主态**，先"够用即换" |
| **D44** | 动作③的**下一步口径** | ① 只做**只读对比基线**（把 8 颗粒逐条量化）② 主攻阶段二收口（显示/输入最小闭环） | **先①再②**：基线给"差多少"的硬数，避免空谈 |
| **D45** | 深度扫描的**范围与边界** | 只读枚举：注册表项／服务／驱动／浏览器内部配置。**只读、不删不改** | **授权只读枚举**；任何写操作另报 |

**下一步（无需你决策即可做）**：把动作②的 **9 颗粒功能对照清单**先出出来（纯只读 + 公开资料），它不依赖 D43。
