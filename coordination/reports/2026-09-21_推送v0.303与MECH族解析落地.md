【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# push v0.303 ＋ MECH 族选项 B 落地 ＋「自指污染」观察（2026-09-21 · v0.304）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 303** ⇒ 收尾 **304**（本轮 **1 个本地提交**）｜**label v0.304**（`label = count`，C21） |
| **HEAD／远端** | 推前 `63edb1e`（= `origin/main`）｜**已推 `d015fce..63edb1e`**（v0.303） |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 = 执行层 |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–39**｜D **1–44**（＋待核）｜R **1–52**（＋ R70 已补登）｜TERM **1–13** |
| **本轮性质** | **放行 ＋ 裁定落地**（§一 MECH 族选项 B 落地／§二 追认「自指污染」先观察） |

**③ 任务与约束**
- 基准 **count = 303**（指令填值，实测一致）｜总规划定位：**push v0.303 ＋ 3 项裁定落地**（实为 **「1 放行 ＋ 1 落地 ＋ 1 追认」**）。
- 约束：按 `CONSTRAINTS.md`｜**禁止内联含反引号／特殊字符命令**（全程脚本文件，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**按 T-038 门控**｜**按 C20** CI 结论不入库。

**④ 状态：完成**（**3 项全落地**；**5 条验收全 ✅**，见 ⑤）。

---

## ⑤ 验收对照（用户 §五 · 5 条）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（按 T-038 门控） | ✅ `d015fce..63edb1e`（exit 0）；✅ 按 **T-038 门控**执行（阻塞至 `completed` 后取值，门控证据 **`READY_TO_EDIT`**）—— ★ **job 级三态按 C20 只记当日日报、不入库** |
| 2 | MECH 族专用解析落地，自检**正反对照** | ✅ `--selftest` **八侧全 PASS**（原四侧 ＋ MECH 四侧：⑤正例／⑥反例／⑦**区段限定**／⑧**空转防护**） |
| 3 | 「自指污染」观察记录到 BASELINE | ✅ `BASELINE §四十五 · 45.2`（观察指标 ＋ 触发再评估条件） |
| 4 | C15 复算通过，编号表**连续无缺号无重号** | ✅ **改前＝改后 全 ±0**（机制 27／C 21／T 39／D 行 45／R 首格 50／R 唯一 53／§四 21／TERM 13） |
| 5 | CI 全绿 | ✅ 门控取值＝`completed`（**按 C20 只记日报**） |

---

## §一 裁定 · **MECH 族专用解析落地（选项 B：区段限定）**（用户 §一 · 改判据脚本）

### 1.1 改动对象与规模
- **对象**：`coordination/tools/check_id_set_diff.py`（**195 → 301 行**，+106）。
- **零治理文件改动**（`CONSTRAINTS.md`／`CHARTER.md`／`TEMPLATES.md`／`ROADMAP.md` 全未动）。

### 1.2 新增内容（逐项）
| # | 新增 | 说明 |
|---|---|---|
| 1 | **族定义启用 MECH** | 引用侧正则 `机制\s*(\d+)(?![0-9])`（**自带族名、全仓无歧义**）；`special: "mech"` 标记走专用分支 |
| 2 | **区段限定解析（4 函数）** | `mech_seg_slice()`（切出 `CHARTER.md`「## 三、全部机制」**区段**，至下一个 `## ` 或 EOF）／`mech_seg_lines()`／**`scan_mech_seg()`（登记集＝仅区段内表格首格裸数字）**／`scan_mech_ref()`（引用集＝全仓「机制 N」） |
| 3 | **空转防护** | 区段解析到 **0 行** ⇒ 打印「**判据空转**」**并计入告警**（**与「通过」取不同值** —— C-04 / R83） |
| 4 | **自检扩至八侧** | ⑤ MECH 引用无登记 ⇒ **必须**告警｜⑥ MECH 区段首格登记 ⇒ **不得**告警｜⑦ **区段外**裸数字 **不得**入登记（**区段限定生效**）｜⑧ 空区段 ⇒ 登记为空（触发空转告警） |

### 1.3 逐字对照（核心逻辑）
| | 内容 |
|---|---|
| **改前**（族表尾） | `# ★ MECH（机制编号）族**暂不启用** —— 其权威载体 CHARTER.md 机制表的**首格为纯数字**（非「机制 N」字面），与其余五族的 A/B/C/D 形态口径不同 ⇒ 需专用解析（另议）。` |
| **改后**（族表尾） | `"MECH": {"pat": r"机制\s*(\d+)(?![0-9])", "auth": ["coordination/CHARTER.md"], "special": "mech"},`（＋ 三行注释：**引用侧自带族名／登记侧区段限定**；**依据＝选项 B 评估稿**） |
| **改前**（主循环） | 六族统一 `scan()` ⇒ MECH 无解（未启用） |
| **改后**（主循环） | 新增 `if cfg.get("special") == "mech":` 分支：区段切片 → 登记/引用分算 → **`if not reg:` ⇒ 空转告警**（`total += 1`）→ 否则正常差集 |

### 1.4 自检结果（八侧）
```
侧①（正例·有引用无登记 ⇒ 必须告警）: PASS  差集=['R99']
侧②（反例·A 表格首格行登记 ⇒ 不告警）: PASS
侧③（反例·C 分条列表登记 ⇒ 不告警）: PASS
侧④（反例·D 节标题登记 ⇒ 不告警）: PASS
侧⑤（正例·MECH 引用无登记 ⇒ 必须告警）: PASS  差集=[99]
侧⑥（反例·MECH 区段首格登记 ⇒ 不告警）: PASS
侧⑦（反例·区段限定：区段外裸数字不入登记）: PASS  登记=[5]
侧⑧（反例·MECH 空区段 ⇒ 登记为空 ⇒ 触发空转告警）: PASS
自检结论：PASS（八侧：R 五族 1 正 + 3 反 ＋ MECH 3 反 + 1 空转防护）   rc=0
```

### 1.5 实跑结果（229 个 `*.md`／55 495 行）
| 族 | 引用 | 登记 | 引用但无登记 |
|---|---|---|---|
| R | 90 | 90 | **0** ✅ |
| D | 55 | 55 | **0** ✅ |
| C | 23 | 23 | **0** ✅ |
| T | 41 | 41 | **0** ✅ |
| TERM | 14 | 14 | **0** ✅ |
| **MECH** | **24** | **27** | **1** ⚠️（`28`） |

**告警合计：1**｜退出码 **0**（**先只告警、不判红**）。

### 1.6 ★ 告警 `28` 溯源（**不调参打绿**）
- 全仓「机制 28」**唯 1 处** ＝ `coordination/reports/2026-09-21_MECH族专用解析评估稿.md` **L103**，原文为选项 A 劣势栏的**举例文字**：
  > "放弃机器检查 ⇒ **"机制 28 被引用但未登记"这类不一致仍靠人眼**"
- ⇒ 属**元引用／示例文字**（判据把"讨论它自己的文字"当输入 —— **自指污染**，与 `R99`／`T-00` 同族），**非真实引用、非真缺口**。
- **处置**：**如实保留、不排除、不调参**（R73 教训：**隐藏规则削弱可信度**）。⇒ 本告警**恰好作为 §二 追认的又一实证**。

---

## §二 追认 · **「自指污染」先观察**（用户 §二 · 无文件改动）

### 2.1 现象
判据会把「**讨论其自身告警的文本**」纳入输入 ⇒ **告警数受报告内容影响**（**与 C20 所治的 R82 自指递归同族**）。

### 2.2 两轮实证（同仓同码，仅报告文字变）
| 轮 | 告警数 | 成分漂移 |
|---|---|---|
| v0.303 轮 | **3 → 2** | `C05`／`TERM-014` **消失**（报告里"告警说明表" `\| **C05** \|` 被四形态判为**登记** ⇒ **假阴性**）；`R99` **新增**（判据稿自检示例表）；`T-0` → **`T-00`**（"讨论 T-0 告警"的元引用） |
| **v0.304 轮（本轮）** | **2 → 1** | 仅余 **`28`**（MECH 评估稿 L103 举例文字） |

### 2.3 口径与触发条件
- **口径**：**先观察、不专项处置**。★ 若当初加"上下文排除"，本轮会因"**说明表被当登记**"而**误报归零** ⇒ **实测已证**排除法的风险。
- **观察指标**：**告警数受报告内容影响的程度**（同仓同码、仅报告文字变化时的**告警漂移量**）。
- **触发再评估**：① 告警数**连续 N 轮为 0**（**N = 5**）；② 或**出现新的自指污染类型**。

---

## ⑥ 未完成项
- **无本轮遗留**。MECH 族选项 B **已落地并自证**；「自指污染」按裁定**留观察**（**非遗漏，是裁定口径**）。

## ⑦ 真实缺陷
- **0 缺陷**。**1 条过程观察（正向）**：**空转防护当场自证** —— 自检侧⑧ 用空区段验证"解析到 0 行 ⇒ 登记为空集"，**与"通过"取不同值**（C-04/R83 直接落地于新族）；避免"MECH 解析失效但静默变绿"。

## ⑧ 三量台账（四量）
| 量 | 内容 |
|---|---|
| **存量** | v0.303 前的全部资产：五族判据（`check_id_set_diff.py` **195 行**）＋ 登记四形态口径 ＋ R70 补登 ＋ 台账 §一–§四十四 |
| **变量1**（需求） | 本轮指令：push v0.303 ＋ **MECH 族选项 B 落地**（区段限定）＋「自指污染」**先观察** |
| **补充增量1**（答案1） | **MECH 族专用解析**（+106 行：区段切片 ＋ 裸数字登记 ＋ 机制引用 ＋ 空转防护 ＋ 自检 4 侧 ⇒ **八侧**） |
| **创新增量** | 存量 ＋ 变量1 ＋ 补充增量1 **融化融合化合** ⇒ **六族齐备的编号集差集判据**（R/D/C/T/TERM/**MECH**），且**每族各有一套正反对照自检** |

## ⑨ 边界说明（四类）
| 类 | 内容 |
|---|---|
| **本地跑过** | `--selftest` **八侧全 PASS**（rc=0）｜**实跑** 229 文件／55 495 行（R/D/C/T/TERM 归零、MECH 1 告警）｜**C15 双向计数**（全 ±0）｜全量判据 **9/9 rc=0** |
| **CI 过** | ✅ 已跑并按 **T-038 门控**等到 `completed`（**job 级三态按 C20 只记当日日报、不入库**） |
| **真实跑过** | —（本判据纯静态扫描，无真机/硬件面） |
| **未验证** | **MECH 族是否入 CI**、**告警 `28` 是否需"示例标记"约定** —— **均待裁**（见 ⑪） |

## ⑩ 决策评估（C11 七要素）—— 针对「**MECH 族是否入 CI**」
| 要素 | 内容 |
|---|---|
| **评估** | MECH 族本轮已落地且六族齐备；是否与其余五族一并纳入 CI `test` job |
| **优势** | 入 CI ⇒ 编号表漂移在**推送时**即被机器发现（不依赖人眼） |
| **劣势** | 判据**先只告警不判红** ⇒ 入 CI 也无门禁效力（**现状 = 白跑一次扫描**） |
| **风险** | 若同时转判红，则会因**自指污染**（告警数随报告文字漂移）**频繁误红** ⇒ **不应先转红** |
| **机会** | 可借"告警连续 N 轮为 0"作为**转判红的客观前置条件**（已有口径） |
| **建议** | **仍先观察**（与 §44.2 口径一致）；**待告警稳定归零后**再议"入 CI ＋ 转判红"**合并落地** |
| **等待决策** | 见 ⑪-② |

## ⑪ 本轮待判
| # | 项 | 推荐 |
|---|---|---|
| **①** | ★ **push 本轮 1 个 commit（v0.304）是否放行** | **放行**（机制 23 须当轮明示） |
| **②** | **MECH 族是否入 CI**（现与五族同为"先只告警、未入 CI"） | **仍先观察**（告警稳定归零后再合议） |
| **③** | **告警 `28` 是否需"示例标记"约定**（如示例一律用保留号段） | **暂不做**（避免新增隐藏规则；R73 教训） |

## ⑫ 下一步建议
1. **首选**：放行 **v0.304**（本轮唯一 commit）。
2. **次选**：观察窗口内**记录告警漂移量**（每次报告后跑一次判据，记数）；连续 5 轮为 0 ⇒ 提请"入 CI ＋ 转判红"。
3. **暂缓**：`docs` 批次与 `advisor_brief` 复扫**均为触发式**（本轮未触发 ⇒ 未跑，合规）。

## ⑬ 启动回执
> 已读 `CHARTER.md`。当前状态：**count = 303**（基准）⇒ 收尾 **304**｜阶段二 · **2.4 边界层已验**｜本次任务：**push v0.303 ＋ 3 项裁定落地**｜是否继续：**执行完毕**。

## ⑭ 本轮读了底图哪几份、哪几行
| 文件 | 行 | 用途 |
|---|---|---|
| `coordination/CHARTER.md` | **`## 三、全部机制` 段**（机制表首格纯数字，**27 行**） | MECH 族**区段限定**解析的**权威落点**（`mech_seg_slice()` 的切点） |
| `coordination/tools/check_id_set_diff.py` | **全 195 行**（改前）→ **301 行**（改后） | 落地对象；通读以确认四形态与主循环插入点 |
| `coordination/BASELINE.md` | **L3151–3195**（§四十四 尾部锚点） | 台账续写定位（新增 §四十五） |
| `coordination/reports/2026-09-21_MECH族专用解析评估稿.md` | **L103** | 告警 `28` **溯源**（确认属举例文字、非真缺口） |
| `coordination/reports/2026-09-21_推送v0.302与R70补登及MECH族评估.md` | **L1–L40**（第 0 行块 ＋ ①–④） | 十四项格式对齐 |
