【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# v0.237 · 授权同步 3 处机制描述 ＋ D-1 结项 ＋ 2.4 product 路径接线 · 报告

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**（无密钥／账号／本机绝对路径／内网 IP）。**可整份转发**。

**② 全景状态卡**（★ 数字为**生成时快照**；提交／推送动作本身会推高版本号 ⇒ **现值一律以复核命令为准**，R82）
```
┌─ 云内核项目 · 全景状态卡 ─────────────────────────────────────┐
│ 版本   以 git rev-list --count HEAD 为准（不写死，C2）         │
│        本轮起点 v0.236（241）→ 提交即变（本报告随提交升版）      │
│ 阶段   阶段二 · 2.4 纯算层 ✅ ｜ 2.4 边界层：自检路径 ✅ ＋       │
│        product 路径 ✅ **本轮接线完成**（待 CI 真实引导证成）     │
│ 本轮   代码 3 文件 ＋ CI 1 文件 ＋ 治理/台账 5 文件；1 次 push   │
│ 未推送 快照 0（提交后按 gh 实取）                               │
│ CI     本轮 push 结论见 ⑨ 边界说明（不以记忆代替实取）           │
│ 决策   待裁：① 场源替换时机（SDF → 场演化图像）② 是否把           │
│        「boot 本机可 check」固化为常规姿势；D-1 **已结项**        │
└───────────────────────────────────────────────────────────────┘
```
复核命令（R71：数值不抄进文档）：`git rev-list --count HEAD`｜`git rev-list --count origin/main..HEAD`｜`gh run list --limit 3`

**③ 任务**：基准 **v0.236**｜总规划定位：授权同步 3 文件 ＋ 2.4 product 路径接线｜类型：**裁定落地 ＋ 推进**｜优先级 **P1**。
约束：按 `CONSTRAINTS.md`｜禁止内联含反引号／特殊字符命令（**全程脚本文件**）｜不 `taskkill`｜不让用户双击任何 bat｜不改底图。

**④ 状态：完成**（含 **1 处需说明的边界**：boot 层 **full build ＋ QEMU 真实引导本机不可做** ⇒ 交 CI，见 ⑨）。

**⑤ 验收对照**

| 标准 | 实测 |
|---|---|
| 3 处机制描述同步完成 | ✅ `CHARTER.md` 机制 25 行（三条→**四条**／六侧→**八侧**／`verify.rs`→**boot 全部 `.rs`**）｜`TEMPLATES.md` **T-032**（补 P4＋八侧＋段号源口径）｜`ROADMAP.md` **行 59**（活跃段号 **⑪→⑫**） |
| **C15 双向计数通过** | ✅ 机制 **25**／C **19**／T **34** **改前后均 ±0**；`BASELINE` D **44**／R **45** ±0；三表**无缺号无重号**；对照（阳性必命中／阴性必不命中）**全 PASS** |
| D-1 结项记录到 BASELINE | ✅ **§21.3**；口径＝**截断到界**（maxAbs 1.110e-15 < 界 2.0038e-15）／**ULP 未达标**（`cos` maxULP = 10；那条 ≤4 ULP 是 **f32** 口径）／**暂不追**（**不再派 Payne-Hanek**） |
| **2.4 product 路径接线完成** | ✅ `present.rs` 新增 **`present_product_selftest`**（**真调 `present_field`**）＋ `main.rs` 的 `run()` **真调**它；编号 **126–128** |
| **host 单测通过** | ✅ `mirror_present` **8 passed**（**5 → 8**）；`nostd` lib **474 passed**；`no_std` 构建门禁 **OK** |
| CI 全绿 | ⏳ 见 ⑨（本轮 push 结论以 `gh` 实取；**不以"本机绿"代替**，R14） |
| 第 0 行含 9 步阅读指令 | ✅ 自 `TEMPLATES.md` **逐字抽取**（D40，不手写）并回读全等自检 **PASS** |
| 不改底图 | ✅ `docs/` ＋ 根 `README.md` **零改动**（本轮只读底图，见 ⑭） |

**⑥ 未完成项**：① **boot 层 full build ＋ QEMU 真实引导本机不可做**（本机缺 `dlltool.exe`／无 `qemu-system-x86_64`，实测）⇒ **交 CI boot job**（⑨）；② **场源仍是最小 product 路径**（SDF 依帧缓冲几何）——**真正的场演化图像**（B 路径／NCA）待 **2.4 × L6 集成**；③ **真实硬件**（非 QEMU）帧缓冲回读**未验证**（WC 显存可能读到陈旧值）；④ D-7（身份档案）**按指令暂缓**；⑤ **源码注释里的数值型陈述仍无判据覆盖**（`fmath.rs` doc 的"≈3500 ULP／875×"已过期）—— 已知盲区，登记未处置。

**⑦ 真实缺陷（本轮 4 条）**

① ★ **「boot 层本机不可验证」的旧结论口径不完整（R14 同族）** —— 旧记录写「boot 层**连 `cargo check` 都跑不起来**（无 MSVC 库／clang／lld）」。本轮实测把口径**分开**了：
   - `cargo +nightly check`（宿主解析到 **MSVC**）⇒ **失败**；根因是 **`bootloader_api` 的宿主构建脚本在链接期缺 MSVC 导入库**（`kernel32.lib`／`ntdll.lib`／`userenv.lib`／`ws2_32.lib`／`dbghelp.lib`／`synchronization.lib`）—— **根本没走到内核 crate 的代码**；
   - **`cargo +nightly-x86_64-pc-windows-gnu check -p meta-kernel-boot-kernel --target x86_64-unknown-none` ⇒ ✅ 通过**（`Checking meta-kernel-boot-kernel … Finished`；**全新 target-dir 复跑同样通过**）；
   - `cargo +nightly-…-gnu **build**`（full build）⇒ ❌ **仍失败**，卡 `error calling dlltool 'dlltool.exe': program not found`（`windows-sys` raw-dylib，**R1 老问题**）；
   - ⇒ **可用结论**：**boot 层本机可做「类型检查」**（GNU 宿主工具链），**不可做「full build ＋ QEMU」**。

② ★ **§20.3 遗留的悬空指针** —— `BASELINE §二十` 写「CI 结论见 **§二十一**」，而 **§二十一 当时并不存在**（R82 家族：写"将要在别处记录"而别处未建）。**本轮已补 §二十一** ＋ 把指针改为「**§二十一 · 21.0**」并实填 CI 结论。

③ ★ **我自己的测试期望值写错（已按纪律修测试、不改被测物）** —— 新增的 `mirror_present_127` 首版在 (1,1) 写像素，却拿 `buf[..n]`（＝偏移 0）比对 ⇒ 本机当场判红。**被测物正确，错的是期望值** ⇒ 改为**由 `FbDesc` 现算偏移**（`off = (1*4+1)*3 = 15`）。属既有纪律「**断言失败先自查期望值**」的同族。

④ **同名不同物：「呈现」在底图里有明确落点，须与本轮接线区分** —— 底图 `LAYER_ARCHITECTURE` **行 80／307** 与 `LAYER_BASEMAP_L0_L6` **行 535** 都写明：**L6 呈现** 的落点＝`manifest-ui/`（**空天浏览器**，产品形态名不占层号）。本轮接线的 **boot 层帧缓冲呈现**（`present.rs`，2.4 边界层）是**内核自呈现**，**层级与落点都不同** ⇒ **报告中不得混称**（否则等于把 boot 帧缓冲冒充成 L6 产品呈现）。

**⑧ 三量台账**（四量）
- **存量**：机制 25 四条判据（P1/P2/P3/**P4**）｜⑫ 段**两条入口**（自检 121–125 ＋ product 126–128）｜host 镜像 8 项｜14 项判据本机回归
- **变量**：`present.rs`（新增 product 自检 ＋ 3 码）｜`main.rs`（`run()` 增一段接线）｜`mirror_bare_assertions.rs`（**+3 测试**，5→8）｜`ci.yml`（⑫ 步骤：`5 passed`→**`8 passed`** ＋ 3 条新断言）｜`CHARTER.md`／`TEMPLATES.md`／`ROADMAP.md`（3 处描述同步）｜`BASELINE.md`（**新增 §二十一**）
- **补充增量**：`present_product_selftest`（**产品入口第一次真的被裸机调用**）｜`.workbuddy/tmp/c15_take.sh`（**改前／改后同源**的 C15 取数脚本）｜`boot_check4.sh`（GNU 宿主工具链的本机类型检查姿势）
- **创新增量**：★ **"product 入口接线"成为可判据形态** —— CI 断言里直接 `grep "present_field(buf, info"`：**函数存在 ≠ 被调用**，只有断言"**真的被调**"才防住 `never used` 型静默空转；★ **C15 用同一份脚本取两次数**（把"同源"从自检纪律推广到**编辑纪律**）；★ **阳性对照写成"图案灰度种类 ≥ 8"**（把"判据是否空转"从感觉变成可算的量：实测 **32 种**）

**⑨ 边界说明**（四类）
- **本地跑过** ✅：① 14 项判据全 **exit=0**（机制 18／21／22／24／25 各自 `--selftest` ＋ 实跑、私有指针 syntax、迁移闭包、迁移保真度 `--selftest`、屏幕转储 `--selftest`）；② 机制 25 自检 **八侧全过**；③ host 镜像 `mirror_present` **8 passed**；④ `nostd` lib 单测 **474 passed**；⑤ `x86_64-unknown-none` 构建门禁 **OK**；⑥ **boot kernel crate `cargo check`（GNU 宿主工具链）通过**，且 `function present_field is never used` 计数 **0**；⑦ C15 双向计数 ±0。
- **CI 过** ⏳：本轮 push 结论**以 `gh run list --limit 3` 实取**（提交后回填；**run 号属环境派生值，R71**）。★ **不以"本机绿"代替 CI 绿**（R14：本机绿是假绿）。
- **真实跑过** ⏳：**本轮本机未真实引导**（无 `qemu-system-x86_64`）⇒ ⑫ 段 product 路径的「写 4 像素→回读→逐字节一致」**仍须 CI boot job 在真实帧缓冲上证成**（上一轮自检路径已有 QEMU 实证：帧缓冲 **1280×720** 不落退化分支 ＋ 绿屏）。
- **未验证** ❌：① boot **full build**（本机缺 `dlltool`）；② **QEMU 真实引导**（本机无 qemu）；③ **真实硬件**帧缓冲回读（WC 显存）；④ 场源仍是**最小 product 路径**（SDF 依几何），**场演化图像**未接。

**⑩ 决策评估（七要素）** —— 本轮新增 1 个决策点：**何时把场源从"SDF 依几何"换成"场演化产生的图像"，以及是否把「boot 本机可 check」固化为常规姿势？**

| 要素 | 内容 |
|---|---|
| **评估** | ① 场源现状＝`field::sdf` 依**帧缓冲几何**采样（**合规但最简**）；底图 `LAYER_ARCHITECTURE` **行 313**「**呈现不增义**：都来自**内核真实状态投影**」＋ D8 已把方向定死 ⇒ 下一步自然是**接真正的场**（B 路径／NCA）；② 本机姿势的旧记录**不完整**（⑦①），若固化可让**编译错误在提交前就被筛掉** |
| **优势** | ① 场源换成真实场 ⇒ **2.4 呈现链路完整**，`present_field` 从"演示"变"产品"；② 固化**本机类型检查**⇒ 省 CI 往返（本轮已实证省下一轮） |
| **劣势** | ① 换场源要接 `field`／`nca` 的**演化循环**（含网格 `2^k+1` 铁律），工作量大于接线；② 固化姿势需改**文档／脚本**（治理文件）⇒ 又要一次授权 |
| **风险** | ① 场演化在 boot 层**耗时／栈开销**未知（本机不可测，只能靠 CI）⇒ 有"首跑红"风险；② 过度依赖本机 check 会**再次产生"本机绿是假绿"**（check ≠ build ≠ QEMU）⇒ 必须写明**它只覆盖编译错误** |
| **机会** | 把「**本机可做的层**」与「**只能 CI 做的层**」写成一张**能力矩阵**（check／build／QEMU／真机），**每一层配自己的正反对照** ⇒ 正面对治 R14 家族 |
| **建议** | ① **先让本轮 product 路径过 CI**（确认接线与回读在 QEMU 上成立），**再**换场源；② 「本机可 check」**先记为经验**（已写入 `BASELINE §21.5`），固化进治理文件**等下次一并授权** |
| **等待决策** | 用户一句话（见 ⑪①②） |

**⑪ 需要你决策**
① **场源替换的时机**：本轮 product 路径**先过 CI**（推荐）再换真实场演化图像？还是**立刻**把 2.4 × L6 集成的场源一并做掉？
② **是否把「boot 层本机可 `cargo check`（GNU 宿主工具链）」固化**进治理文件／本项目惯例（新增"提交前本机自检"步骤）？（涉及改治理文件 ⇒ 需授权）
③ **D-1 已结项**（本轮落地 §21.3）—— 如需改口径请指出；否则该项**闭环**。
④ **D-2 后续**（按你本轮指令）：**先跑几轮，遇真实案例再定口径** ⇒ 本轮**不调判据**，已照办。

**⑫ 下一步建议**
1. 等本轮 **CI 三 job** 结论（重点看 boot job 是否出现 **226–228** 区段的红屏 —— 若红，先查**退化出口**与**回读比对**，**不调参打绿**）；
2. **2.4 × L6 集成**：把场源换成**场演化产生的图像**（B 路径／NCA；注意多重网格 **`2^k+1`** 铁律）；
3. 把「**边界层退化出口 ⇒ 判红/绿**」从**人工反推**改成**常驻机器判据**（本轮仍靠人读日志）；
4. **能力矩阵**（本机 check／CI build／CI QEMU／真机）写入文档，配各自正反对照。

**⑬ 启动回执**：已读 `CHARTER.md`。版本 = `git rev-list --count HEAD`（**不手写**，C2）｜本次任务：授权同步 3 文件 ＋ D-1 结项 ＋ 2.4 product 路径接线｜**是否继续：等待裁定（⑪ 四项；其中 ③④ 已闭环）**。

**⑭ 本轮读了底图哪几份、哪几行**（D55／D52：报告须写明）
- **根 `README.md` 全文 132 行**（三层次＝行 13–20；命名规范＝行 64–85；版本规则＝行 87–94；阶段路线＝行 96–106；仓库结构＝行 108–126）。
- **`docs/LAYER_BASEMAP_L0_L6.md`**：行 **1／3／5／6**（版本与性质、v0.2 三条更正）｜行 **31–63**（两产品形态 L0–L6 总表；**行 47–48＝L6 呈现 ✅／L6 对齐 ❌**）｜行 **94／145**（戒律分层／P3 不得外显源读数）｜行 **300／325**（接口与授权链引述）｜行 **364–365**（`/v1/msg`·`/v1/report` 底图 0 定义）｜**行 528–563（§8 L6 与外界交互层：呈现＋对齐；行 535＝呈现落点 `manifest-ui/`；行 538＝「呈现不增义」；行 542＝V1–V6 用例；行 546–563＝L6-M1／M2／U1／U2／V1／V2）**。
- **`docs/LAYER_ARCHITECTURE.md`**：行 **72–80**（产品形态名不占层号；**行 80＝空天浏览器＝L6 呈现子功能，落点 `manifest-ui/`**）｜**行 92–94（§3.2 命名体系＝全库命名唯一权威）**｜行 122（空天浏览器）｜**行 301–316（§4 L6：行 307＝呈现 ✅ 落点 `manifest-ui/`；行 313＝「呈现不增义：都来自内核真实状态投影」）**｜行 413｜行 441–442（v1.6 变更）。
- 脚手架（**非底图**）：`CHARTER.md` 机制 25 行（**改动落点**）｜`TEMPLATES.md` **T-032**（改动落点）｜`ROADMAP.md` **行 59**（改动落点）｜`BASELINE.md` §十九／§二十（**行 955 起**）＋新增 §二十一｜`CONSTRAINTS.md` **C15（行 285–308）**。
- **被测物**：`meta-kernel-boot/kernel/src/present.rs`（全文，**新增 product 自检**）｜`main.rs`（全文，**`run()` 增接线**）｜`meta-kernel-core-nostd/tests/mirror_bare_assertions.rs`（⑫ 段镜像段 505–635 ＋ **追加 126–128**）｜`.github/workflows/ci.yml`（**⑫ 步骤行 62–90**）｜`nostd` 侧 `project.rs`／`field.rs`／`field/sdf.rs`／`fb.rs`（只读，取 API）｜`lib.rs`（模块表）。

---

**一句话**：**3 处机制描述已按授权同步**（`CHARTER` 机制 25／`TEMPLATES` T-032／`ROADMAP:59`，C15 **±0**）；**D-1 结项**（截断到界、ULP 未达标、暂不追）；**2.4 product 路径真正接线** —— `present_field` **第一次被裸机路径调用**（新编号 **126–128**、host 镜像 **5→8 passed**、CI 断言直查"`present_field(buf, info` 真被调"），并**顺带纠正**了「boot 层本机连 `cargo check` 都跑不起来」这条**口径不全**的旧结论（**GNU 宿主工具链下 check 本机可跑**，full build／QEMU 仍只能交 CI）。**唯一待你点头的：场源何时换成真正的场演化图像？**
