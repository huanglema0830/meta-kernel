【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-24 · v0.333 补正轮（`云内核-C3-004`）· push 放行 ＋ **判据盲区修复** ＋ **底图追认** ＋ **机制 31 建立**

> ★ **版本与轮次（`T-050` 标名）**：**版本 `count = 373`（开工实测）→ `376`（收口 · 3 组提交）** ｜ **轮次 `v0.333`** —— 两者不等。
> ★ **指令序号（`T-060` 试用第 3 轮）**：**本轮 `云内核-C3-004`** ｜ **下一轮 `云内核-C3-005`** ｜ **下一轮基准 `count`＝376**。
> ★ **性质**：**缺陷修复 ＋ 放行（单一主类型，辅以放行）**。
> ★ **本件要解决的一件事**：`C3-003` 发现的那条**"改写即隐身"判据盲区**（`R83` 同族：**"没验到"被读成"验过了"**）—— 本轮把它**修到能看见**，并验证"**告警数上升才是对的**"。

---

## ① 保密标记（`C12` · 逐件分级 —— `T-060` 第 10 项格式）

| 产出物 | 级别 | 说明 |
|---|---|---|
| 本报告 | 🔒 **可拷** | 无本机绝对路径／端口具体值／二进制体积 |
| `docs/LAYER_ARCHITECTURE.md`（追认 4 处 ＋ 留痕 1 处） | 🔓 **直接拷** | 效力列与依据 |
| `coordination/tools/check_effect_column.py`（词表统一 ＋ 2 排除 ＋ 3 自检侧） | 🔒 **可拷** | 判据逻辑，无敏感取值 |
| `coordination/CHARTER.md`（机制 31）／`TEMPLATES.md`（`T-061`） | 🔓 **直接拷** | 机制条文与编号 |
| 血管／CI 原始取证（**仓库外**） | 🔐 **屏蔽后拷** | 含七维读数与**非默认端口** |

- ⛔ **绝不拷**：本机绝对路径／账号／内网 IP／**端口号**／**密钥与 token**／二进制路径与体积 —— ★ **本报告一律不列具体值**（★ 仓库为 **PUBLIC**）。
- 🔐 **屏蔽后拷**：函数签名／阈值常量／T0–T3 判定顺序／七维读数。
- 🔒 **可拷**：crate 与模块名／测试计数／层级映射／治理编号。
- 🔓 **直接拷**：版本号／缺陷编号／机制条文／阶段口径定义。

> 【给用户的提示】本报告 **⛔ 具体值 0 处** ⇒ 拷给顾问时**无需额外屏蔽**。

---

## ② 全景状态卡

```
┌─ 云内核项目 · 全景状态卡（C3-004 / v0.333 补正）───────┐
│ 版本   count 373（开工）→ 376（收口 · 3 组提交）        │
│ 轮次   v0.333 ｜ 指令 云内核-C3-004 ｜ 下一轮 C3-005    │
│ 阶段   ROADMAP 口径·阶段二（可引导 ISO）·子任务 2.4     │
│ CI     run completed/success；三 job 全 success（含 CQ）│
│ 判据   15/15 PASS；只告警类：报告结构 0／效力列 0／      │
│        编号差集 3（历史元引用）／其余 0                  │
│ 决策   见 ⑪（3 项）                                      │
└─────────────────────────────────────────────────────────┘
```

## ③ 任务（含基准／定位／优先级）

| 项 | 内容 |
|---|---|
| **指令基准** | `count = 【自动生成并核实】` ⇒ **实测 373**（＝上轮收口值） |
| **总规划定位** | **两口径并存**：README 四阶段（产品视角）× ROADMAP 三阶段（工程视角）；**引用阶段号必写明口径** |
| **任务类型** | **缺陷修复 ＋ 放行（单一主类型）** |
| **优先级** | ① push 放行 ＋ 等 CI **P0** ｜ ② 判据盲区修复 **P0** ｜ ③ 底图 🟡 裁定落地 **P1** ｜ ④ `T-061` 编号授权 **P1** ｜ ⑤ 遗留 2 报告处置 **P2**（＋ `at` **P2·暂不做**） |

**★ 本轮读了底图／权威件哪几份、哪几行**

| # | 文件 | 行／范围 | 读它做什么 |
|---|---|---|---|
| 1 | `coordination/tools/check_effect_column.py` | 61–68（常量）／143–169（`aging_alerts`）／196–275（自检） | **盲区根因**（行 159）与修点 |
| 2 | `docs/LAYER_ARCHITECTURE.md` | 100–106（§2.2.1 效力纪律块）／160–168（§2.2.4 冻结/状态更新）／245–248（法身报身应身）／271–311（§2.3／§2.4 效力块） | **4 处追认 ＋ 留痕/规范句定位** |
| 3 | `coordination/CHARTER.md` | 116–119（机制 29／30 行）／120–138（机制关系块） | 机制 31 登记位置 |
| 4 | `coordination/TEMPLATES.md` | 324–400（§7.2）／415–430（§7.5） | `T-061` / `T-062` / `OBS-004` 状态更新 |
| 5 | `coordination/reports/2026-09-23_今夜P3清单.md` | 1–24／末 6 行 | 遗留件识别（P2⑤） |
| 6 | `coordination/reports/2026-09-23_接续包核验补全表_顾问九步复核回应.md` | 1–24／末 6 行 | 同上 |
| 7 | `coordination/security/basemap_hashes.txt` | 全文（44 份） | 判据 F 基线（改底图连带） |
| 8 | `coordination/BASELINE.md` | §七十（上轮） | 基准与承接 |

## ④ 状态

**状态**：**完成**（①–⑤ 全部达成；⑥ 无未完成项，仅 3 项待裁）

## ⑤ 验收对照（10 条 · 逐条实测）

| # | 验收标准（指令原文要点） | 结论 | 实测 |
|---|---|---|---|
| 1 | push 完成，CI 三 job 全绿（`T-038`） | ✅ | push `3affb80..974f8a0`（exit 0）；run **completed/success**；三 job 全 `success` |
| 2 | 判据盲区修复完成；复跑后告警数变化如实记录 | ✅ | 词表统一 ＋ 2 条排除 ＋ 3 自检侧；★ **告警 4 → 7（现形 2 条）→ 0（追认 ＋ 排除）**，见 ⑦-① |
| 3 | 底图 4 处裁定落地（3 追认 ＋ 第 5 处一并裁）🟡→🟢 | ✅ | 行 275／300／311／247 **全部转 🟢**（**注明"经用户追认"**）；行 163 补历史留痕注 |
| 4 | `T-061` 编号授权落地，走 `T-049` 登记，试用 3 轮 | ✅ | **`CHARTER.md §三` 新增机制 31**；`T-061` 状态更新为"编号已授权＝机制 31"；仍 **提议稿·未生效（起点 v0.333）** |
| 5 | 遗留 2 份报告处置建议出 | ✅ | 见 §⑫-附（**入库 ＋ 补台账登记**；**本轮未擅自处置**） |
| 6 | 探针 `at` 记为"暂不处理" | ✅ | 已记（见 ⑥-③ 与 ⑫） |
| 7 | 引用编号全部带登记状态 | ✅ | `T-049`／`T-060`–`T-062`／机制 31（**已登记**）；`MECH` 告警 **0** |
| 8 | `C15` 复算通过，编号表连续无缺号无重号 | ✅ | **机制 30 → 31（+1 ✅）**；T 62／C 21／TERM 14／tools 15 **±0** |
| 9 | 判据 15/15 PASS | ✅ | `run_all_checks.py` rc=0，**15/15** |
| 10 | 下一次返回第 0 行含 9 步阅读指令 | ✅ | 本件第 1 行起即 9 步指令 |

## ⑥ 未完成项

| # | 项 | 说明 |
|---|---|---|
| 1 | **机制 31 无机器判据** | 如实标注（与机制 26／28 同口径：**先立机制、后议机器化**） |
| 2 | **`T-062` 仍提议稿·未生效** | 走 `§7.1` 试用 3 轮（起点 v0.333） |
| 3 | **探针 `at` 真时间戳＝暂不处理** | 用户裁定：**乙·维持**（不改内核公共类型 `l5_senses.rs::FieldReading`）；★ 若未来要改，**单独立项** |

## ⑦ 真实缺陷（★ 本轮自捕获 2 条 ＋ 修复验证）

| # | 缺陷 | 严重度 | 处置 |
|---|---|---|---|
| ① | ★★ **判据盲区（"改写即隐身"）**：`check_effect_column.py` 同一判据**两套词表** —— 规则 B（行 117）要求「**待裁定**」，而时效检查（行 159）**只认字面「待复核」** | **P1** | **已修**：时效检查改为**同时接受两种词**；★ **复跑实测告警 4 → 7**（★ **如指令预期：上升才是对的**，新现形 2 条）|
| ② | ★ **修后连带暴露两类误报**：① 词表统一使**规范句**（`§2.2.1`「取值只有两种」行）与**历史陈述**（述及"曾经 🟡"、已裁定）被误报 | P2 | **已修**：加 **2 条排除**（**历史留痕词表 11 词** ＋ **规范句判据＝同行兼有 🟢🟡**），★ 依 `T-041`：**剔除规则本身是正式规则** ＋ **每次扫描打印剔除行数**（可见，不隐藏） |

> ★ **告警数变化如实记录（`R83`）**：**4**（`C3-003` 末，含 3 条"双词保留"可见 ＋ 1 条新超期）→ **7**（词表统一后，**新现形 2 条**：行 163／167 的 🟡 →后经认定为历史陈述）→ **3**（加历史留痕排除后）→ **0**（4 处追认转 🟢 ＋ 规范句/历史句排除）。
> **★ 关键判断：`4 → 7` 这一段是"变好"，不是"变差"** —— 它把**原本看不见的**变成**看得见的**（`R83` 的反面）。

## ⑧ 三量台账（**四量**）

| 量 | 本轮内容 |
|---|---|
| **存量** | 机制 **30** ／ C 21 ／ T 62 ／ TERM 14 ／ tools 判据 15 ／ `ci.yml` step 58 ／ 底图 44 份 ／ reports 176 ／ 合订本 7809 ／ `BASELINE` 顶层节 109 |
| **变量** | ① **机制 30 → 31**（`+1`）② 判据 `check_effect_column` 自检 **十二侧 → 十三侧** ③ 底图哈希基线**更新**（44 份） |
| **补充增量** | ① **判据词表统一** ＋ **2 条排除**（历史留痕／规范句）② **3 条自检侧**（侧⑪⑫⑬）③ **底图 4 处 🟡→🟢（追认）** ＋ 1 处历史留痕补注 ④ **`CHARTER` 机制 31** ⑤ **`T-061` 编号授权** ⑥ **本报告** |
| **创新增量** | ★★ **判据从"会隐身"变成"会现形"** —— 本轮首次证明：**同一判据内部的词表不一致，会让"改对了"变成"看不见了"**；并把这一形态固化为**可回归的自检侧⑪**（**下一个人再触发同类问题，判据自己会报**）。★ 附带确立一条**通用治则**：**判据范围要写清"什么不算"**（历史留痕／规范句），且**剔除规则本身必须可见（`T-041`）**。 |

### 8.1 `C15` 双向计数（同脚本同口径）

| 项 | PRE（`HEAD`＝`974f8a0`） | FINAL | 差值 | 预期 |
|---|---|---|---|---|
| count | 373 | **376** | **+3** | ✅ 3 组提交 |
| 机制数（`CHARTER` 直读口径） | **20**（表口径 30） | **21**（表口径 **31**） | **+1** | ✅ **机制 31**（**与预期一致**） |
| C 数 | 21 | 21 | 0 | ✅ 无缺号 |
| T 数 | 62 | 62 | 0 | ✅ 本轮未新增 T 条 |
| TERM 数 | 14 | 14 | 0 | ✅ |
| tools 判据数 | 15 | 15 | 0 | ✅ 只改脚本内容、未增删脚本 |
| `ci.yml` step 数 | 58 | 58 | 0 | ✅ 本轮未改 CI |
| 底图 .md 份数 | 44 | 44 | **0** | ✅ 只改内容、未增删文件 |
| reports 文件数 | 176 | **177** | **+1** | ✅ 本报告 |
| `BASELINE` 顶层节 | 109 | **110** | **+1** | ✅ §七十一 |

## ⑨ 边界说明（四类 · 齐全）

- **本地跑过（M）**：判据 `--selftest`（**十三侧**，含新增侧⑪⑫⑬）｜判据实跑（效力列 **0 告警**）｜**判据全跑 15/15**｜`C15` ｜判据 F 基线更新并复跑 PASS。
- **CI 过（CB／CQ）**：★ **本轮有** —— run **completed / success**；三 job 全 `success`（含 **`CQ`：元内核引导镜像 ＋ QEMU 真实引导**）。★ 按 `C20`：**run 号只记日报、不入库**。
- **真实跑过（H）**：**无**（本轮无真机验证需求）。
- **未验证**：★ **本轮后续改动（判据／底图／治理）未再 push**（禁区⑥：push 只按第一节）⇒ 其"绿"**仅本机有效**（机制 26）｜★ 机制 31 **无机器判据**（人工承载）。
  ★ **对照**：`C3-003` 的代码修复**已被本轮 CI 覆盖**（`974f8a0` 为推送末端 ⇒ `set_port` 与 health 计数**已进 CI**）。

## ⑩ 决策评估（七要素）

| 要素 | 内容 |
|---|---|
| **评估** | 盲区已修且**用"告警数上升"证明修对了**；底图 4 处追认落地、效力列**真归零**；机制 31 建立 |
| **优势** | ① **判据从"看不见"变"看得见"**（`4 → 7` 是可验证的"修好了"）② **新增 3 条自检侧**，防同类回归 ③ 底图 🟡 **真归零**（不是靠改词）④ `set_port` 修复**已受 CI 保护** |
| **劣势** | ① 机制 31 **无机器判据** ② 本轮改动**未再 push**（仅本机绿） |
| **风险** | ① 排除规则若过宽，**又会变成"新的隐身"**（**警报**：`HIST_MARKS` 任一词出现在真待裁行即被排除）② `T-062` 未生效前，**不得按新格式要求指令** |
| **机会** | 把"**判据范围要写清什么不算**"从个例升为**通则**（**可被证伪**：给任一判据加一条"规范句/历史句"扰动，若仍误报即通则不成立） |
| **建议** | ① 机制 31 立机器判据（与 `T-062` 同批）② 下一轮起 push 常态覆盖本轮尾巴 ③ 遗留 2 报告入库 |
| **等待决策** | 见 ⑪（3 项） |

## ⑪ 需要用户决策（3 项）

| # | 待裁项 | 选项 | 推荐 |
|---|---|---|---|
| ① | **机制 31 是否立机器判据**（拷链前可见度上限过滤，可否机器校验） | (甲) 立项 (乙) 人工承载 | **乙**（与机制 26／28 同口径，先跑 3 轮再说） |
| ② | **`HIST_MARKS` 排除规则是否过宽**（现 11 词：原文保留／原为／已处置／订正／引述／当时／删除线／已被／取代／不追改／保留留痕） | (甲) 维持 (乙) 收窄到「原文保留／保留留痕」两词 (丙) 改白名单式标记 | **乙**（更严；但需接受个别历史句复现告警） |
| ③ | **遗留 2 份报告处置** | (甲) 入库＋补台账登记 (乙) 入库不登记 (丙) 丢弃 | **甲** |

## ⑫ 下一步建议

1. **遗留 2 报告入库**（`P2`）：见 §⑫-附；建议并入下一轮 push。
2. **机制 31 判据立项**（`P3`）：待 3 轮试用后。
3. **下一轮基准 `count`＝376**。
4. ★ **本轮尾巴未 push**（判据／底图／治理／报告）⇒ 建议下一轮第一节一并放行。

### §⑫-附 · 遗留 2 份未跟踪报告处置建议（**本轮只出建议，未处置**）

| 件 | 性质（识别） | 属轮 | 行数 | 建议 |
|---|---|---|---|---|
| `coordination/reports/2026-09-23_今夜P3清单.md` | **机制 19 夜间自动化**的当夜 P3 清单（第 0 行 9 步齐；含"本夜 0 项可跑"结论与排除口径 21 类） | **2026-09-23 夜间 P3 轮** | 283 | **入库**（★ 且**补 `BASELINE` 一行**：夜间轮次亦须台账可查） |
| `coordination/reports/2026-09-23_接续包核验补全表_顾问九步复核回应.md` | 对**指导专家 9 步复核**的回应件（六核证据链逐项补到"可核"粒度；含待裁七项） | **2026-09-23 断线重连轮** | 272 | **入库**（★ 同上补台账；其"待裁七项"**宜与当前待裁合并去重**） |

> **判断依据**：两件**均为正式报告体**（第 0 行 9 步齐、含保密标记），**非过程草稿**；**无敏感内容**（自带 ⛔＝无）。⇒ **建议入库**；★ **不擅自处置**（本轮授权只到"出建议"）。

## ⑬ 启动回执

> **已读 `coordination/CHARTER.md`**（§三 机制表 1–31／机制关系块）。
> **当前状态**：`count = 373`（开工）→ **376**（收口）｜阶段口径＝**README 四阶段 / ROADMAP 三阶段 并存**。
> **指令序号**：`云内核-C3-004` ｜ **下一轮** `云内核-C3-005` ｜ **下一轮基准 `count`＝376**。
> **本次任务**：push 放行 ＋ 判据盲区修复 ＋ 底图 🟡 裁定 ＋ `T-061` 授权 ＋ 遗留报告处置。
> **是否继续**：**执行完毕**。

---

## 接续包数据块（**增量** · 置于尾部、`KS_` 伴行之前）

> **数据层（WorkBuddy 填）** ｜ **顾问层（16–18 项）由顾问填**。

**0 元信息**：2026-09-24｜触发＝指令指定（〇之三）｜链号 **`C3`**｜**增量**。
**1 项目身份**：云内核（元内核）项目 · 电信号版；共生而非替代；终态＝可安装 OS 形态；不替代用户原系统、内核不主动夺胎。
**2 三方关系**：决策人＝用户｜执行者＝WorkBuddy｜顾问＝**外部专家（`C3`）**｜指令格式＝**十必带**（`T-060`，**试用第 3 轮**）。
**3 当前位置**：阶段＝ROADMAP 口径·阶段二·子任务 2.4｜`count`＝**373 → 376**｜轮次 v0.333｜未推送＝**本轮尾巴 3 组本地提交**（禁区⑥）。
**4 全景状态卡（带证据）**：L1–L5 ✅M｜L6①✅M｜**L6②❌**｜**L7 T1 ✅（已受 CI 覆盖）**｜L7 T2·T3 ❌｜2.4 边界层 ✅**CQ**｜★ **效力列 🟡 全部归零**（`check_effect_column` 0 告警）。
**5 命名族**：见 `docs/LAYER_DETAILS.md §3.2.1`。
**6 规则与机制**：机制 **31**（★ 新增）｜C **1–21**｜判据 **15**（入 CI **11**；仅本机 **4**）｜报告十四项｜能力矩阵 **M／CB／CQ／H**｜`C12` ＋ **指令侧密级义务（`T-060`）**｜七要素。
**7 三量台账**：见 §⑧（四量齐）。
**8 最近裁定（5 条）**：①判据词表统一（修复）②底图 4 处追认 → 🟢 ③机制 31 建立（编号授权）④`T-061`＝机制 31 ⑤探针 `at`＝暂不处理。
**9 待办**：见 ⑫（4 项）。
**10 待裁定**：见 ⑪（3 项）。
**11 风险**：`R-A` 排除规则可能过宽（P2）｜`R-B` 机制 31 无判据（P3）｜`R-C` 本轮尾巴未 push（P2）。
**12 权威文件路径**：底图＝根 `README.md` ＋ `docs/`｜治理＝`coordination/`。
**13 边界说明**：见 ⑨（四类齐）。
**14 指令序号机制**：链号 `C3`｜本轮 `云内核-C3-004`｜下一轮 `云内核-C3-005`｜下一轮基准 `count`＝**376**。
**15 顾问接手须知**：阅读指令 9 步（第 0 行）｜出件＝**十必带**｜★ **脑补清单**＝①四步 vs 五步未归一 ②L8–L10 已冻结 ③"接管级 L0–L4"与架构同号不同义 ④六波罗蜜无来源｜★ **已知缺口**＝L6② 未实现／L4·L5 控制律无编号／机制 31 无判据／`T-062` 未生效。

**16 当前最需要处理的三件事**：（**顾问填**）
**17 顾问审核结论**：（**顾问填**）
**18 下一轮建议**：（**顾问填**）

---

### INTEL 状态表（依 `T-042` · 置于十四项之后）

| 编号 | 来源 | 状态 | 本轮触发否 | 处置 |
|---|---|---|---|---|
| `INTEL-001` | 外部信息（观察中） | 观察中 | **未触发**（触发＝企业注册完成 ＋ 阶段三启动；均未发生） | 维持观察中；**不为"好看"硬触** |
| `INTEL-002` | 外部信息（观察中） | 观察中 | **未触发** | 同上 |

> **J1** ✅｜**J2** 两条均未触发 ⇒ 不违规｜**J3／J4** 无待准备／准备中项 ⇒ 不违规。

### 告警行（`B2` ＋ `C1`）

- **`B2`（本机判据告警）**：**15/15 PASS**｜只告警类：**效力列 0**（★ 本轮修复目标，已归零）｜编号差集 **3**（`C22`／`T-00`／`TERM-999`，**历史元引用**，未新增）｜报告结构 0｜其余 0。
- **`C1`（CI 告警）**：**无** —— run `completed/success`，三 job 全 `success`（按 `C20` run 号只记日报）。

### 台账内联（依 §54.5）

> **本轮报告名**：`coordination/reports/2026-09-24_v0.333补正_判据盲区修复与底图追认及机制31建立.md`
> **对应台账节**：`coordination/BASELINE.md` **§七十一 · v0.333 补正轮**。
