【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · **push `32b4042` ＋ 8 项待裁落地**（机制 11/28 边界 ／ 指针反向可达 ／ `T-045` 补记 ／ 节标题两格式 ／ 指令索引 ／ 6 条过期任务删除 ／ 并发观察 ／ `R90`·`B5` 追认）

> 归口：**本轮指令 §〇–§八**｜日期：2026-09-22｜性质：**放行 ＋ 裁定落地（分组）**
> ★ **版本口径（`T-045`）**：**报告内版本号 ＝ 本报告「首次提交」时的 `count`** ⇒ 见 ⑬ 启动回执。

---

## ① 保密标记（C12）

| 项 | 内容 |
|---|---|
| **保密级别** | 🔓 **公开**（本报告**只含仓库内公开信息**） |
| **四级清单** | 🔓 公开／🔒 半公开（顾问可读）／🔐 保密（仅本机）／⛔ 机密（绝不外发） |
| **屏蔽提示** | **无屏蔽项** —— 不含 `{PRIVATE_ASSETS}` 真实值、**本机绝对路径**、用户名、密钥；记忆文件路径一律写作**工作区相对路径** `.workbuddy/memory/MEMORY.md`。 |

---

## ② 全景状态卡（自动生成）

| 项 | 值 |
|---|---|
| **版本** | **起点 `count 323`**（指令给的基准）⇒ **本轮收口 `count 324`**（`77f7b74`）；**本报告 ＝ 325** |
| **阶段定位** | 阶段二（可引导 ISO）· **2.4 边界层已验** |
| **待决策项** | **4 项**（见 ⑪）★ 较上轮 **8 项 → 4 项**（8 项已全部落地） |
| **CI 状态** | **全绿**（三 job 全 success；★ **run 号与逐 job 三态按 C20 只记日报、不入库**） |
| **仓库** | `ahead 1`（本轮 1 个新 commit **未 push**）；工作树**待提交本报告** |

---

## ③ 任务（含基准与总规划定位）

- **基准**：`count = 323`（**实测一致**）。
- **总规划定位**：push 未推 commit ＋ **8 项待裁落地**。
- **★ 本轮读了哪些文件、哪几行**（如实）：

| 文件 | 读的位置 |
|---|---|
| `coordination/CHARTER.md` | **§三 机制表（机制 11 行／机制 28 行／「机制之间的关系」块）**｜**§九 按需深入** |
| `coordination/BASELINE.md` | **§24–§52 全部轮次节标题**（用于生成指令索引）｜**§47.3「A2 豁免登记表」全文**｜**文件尾**（追加点） |
| `coordination/TEMPLATES.md` | **§7.2 表尾（`T-043`／`T-044` 行）**｜文本末尾 |
| `coordination/workflows/universal_workflow.md` | **全文 62 行**（头部插入点：`> 最后更新于…` 之后） |
| `coordination/security/basemap_hashes.txt` | **全文件**（核 `universal_workflow.md` 是否在底图内 ⇒ **命中 0**） |
| `coordination/instructions/` | **全部 21 份文件名**（生成索引 §一） |
| `coordination/CONSTRAINTS.md` | **§C1–C21 节标题** |
| **底图（根 `README.md` ＋ `docs/` 35 份）** | ★ **本轮未读**（**本轮零改动底图** ⇒ 按 `CHARTER §九`「按需」不读；**不是漏读，是未触发**） |

---

## ④ 状态

**状态**：**完成**（§〇–§八 **8 项全部落地**；§九 报告即本件）。

---

## ⑤ 验收对照（指令「验收标准」逐条）

| # | 验收标准 | 实测 |
|---|---|---|
| 1 | **push 完成，CI 三 job 全绿（按 T-038 门控）** | ✅ `891ef30..32b4042`（**exit 0**）；**三 job 全 success**；门控证据 **`READY_TO_EDIT`**（`##[error]` **0 行**） |
| 2 | **机制 11/28 边界明写** | ✅ 三处落点：机制 **11** 行「骨架层」／机制 **28** 行「裁定后记 · 选乙」／**「机制之间的关系」块 28 条重写为边界声明**（**只读骨架不经入口 ⇒ 不违规**） |
| 3 | **指针反向可达（骨架回指入口）** | ✅ `universal_workflow.md` 头部新增「★ 调用入口」行；★ **重跑 A.4 脚本 ⇒ 全链 12/12 PASS（跳 10 由 FAIL 转 PASS）** |
| 4 | **`T-045` 补记** | ✅ `TEMPLATES §7.2` 新增 **`T-045`**（**报告内版本号口径**）；★ **副作用：`T-045` 元引用告警自动消失**（告警 4 → 3） |
| 5 | **台账节标题暂不统一，记录** | ✅ `§53.4` 追认；现状**两种格式实测计数**（`## N、` **48** ／ `## §N ·` **5** ＝ **53**） |
| 6 | **历史指令只补索引** | ✅ 新增 `instructions/INDEX.md`（**21 份已归档 ＋ 断档期 4 天 ＋ 43 个轮次节**）；★ **并订正"断档 5 天"的误计** |
| 7 | **6 条过期任务删除** | ✅ **6 条全部删除**；★ **处置后 `list` 复核**（非"发完即算"） |
| 8 | **平台任务并发登记观察** | ✅ `§53.7`（4 条自动任务 ＋ 人工夜间指令；**触发再评估＝真出现冲突时**） |
| 9 | **`R90`/`B5` 重试路径记录** | ✅ `§53.8` 追认（**等真实抖动、不人为造红**）；**"抖动时能否自愈"仍列未验证** |
| 10 | **C15 复算通过，编号表连续无缺号无重号** | ✅ 机制 **1..29**／`T` **1..45**／节号 **0..53** **连续无缺重**；Δ ＝ 机制 **±0**、`T` **+1**、节标题 **+1** |
| 11 | **CI 全绿** | ✅ 见 1 |
| 12 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行 |

---

## ⑥ 未完成项（清单 ＋ 原因）

| # | 未完成项 | 原因 |
|---|---|---|
| 1 | ★ **`R90`／`B5` 的"重试路径"仍未验** | **两轮 CI 均"首试即过"** ⇒ 需**真实抖动**才能验；**不人为造红**（用户裁定） |
| 2 | **另有 2 条"已过期"平台任务未处置** | **不在本轮 6 条授权内**（① `社保事项双提醒` @09-06 **仍 ACTIVE**；② `自动盘点` @09-14 **PAUSED**）⇒ **非云内核任务，登记待裁** |
| 3 | **指令索引"未内联"的轮次** | 43 个轮次节中，**部分节正文未内联 `reports/…md`** ⇒ 索引**如实标"未内联"**，**不代填** |
| 4 | **`T-045`／`T-044` 的机器判据** | 指令未要求；与机制 26 同口径**先立条文、后议机器化** |
| 5 | **并发互斥** | 用户裁定**先登记观察** ⇒ **未加锁** |
| 6 | **报告独立性** | 本报告与 `§五十三` 为**同一轮的两处载体**（报告＝十四项细节；台账＝结论与裁定） |

---

## ⑦ 真实缺陷（**B2 ＋ C1**：本轮新增告警 ＋ 来源溯源）

**★ 六族告警行（`C1` 载体 · 固定一行）**：**六族告警：本轮 3 条**（`C22`／`T-00`／`TERM-999`，**均元引用**）**｜对比上轮 −1**（上轮 4 条）｜**真缺口 0 条**。

| 级 | 缺陷 | 来源／证据 | 性质 |
|---|---|---|---|
| **P1** | ★★ **门控脚本"同族假阳性"第 2 例**：v7 仍把 **`run:` 回显**当成运行期判红（`下载/校验失败：1 次首试 + 3 次重试后仍失败` 命中 **1 行**） | 逐行取证：该行为 `2026-09-22T…Z ^[[36;1m echo "::error::wasm-bindgen …"^[[0m` ⇒ **带 ANSI ＋ `echo \"` 包裹 = 回显** | **R89 家族**；★ **根因：该文案不含变量 ⇒「代入特征」对它无效** ⇒ **已订正为"判红 0 行"**，并**回写技能**（写明适用条件） |
| **P2** | ★ **"断档 5 天"是我自己写错的数** | `ls coordination/instructions/` **实测 21 份**（`09-16` 12／`09-17` 8／`09-22` 1）⇒ **断档 ＝ 4 天**（`09-18`～`09-21`） | **计数类陈述出错**（**正是"计数须给可复现命令"观察期要观察的类型**）⇒ **已在 `INDEX.md` 与 `§53.5` 订正留痕** |
| **P2** | **告警成分**同日两次漂移：**3 → 4 → 3** | 4（`机制 28` 消警／`TERM-999`＋`T-045` 新增）→ 3（`T-045` 因补记消警） | **§四十四 同族第 3／4 例** ⇒ **只看总数会连续误判两次**（先误判"变差"、再误判"变好"） |
| **P2** | **另有 2 条已过期平台任务**（1 条仍 ACTIVE） | `list` 实测：`社保事项双提醒` @`2026-09-06` **ACTIVE**（已过期 16 天）／`自动盘点` @`2026-09-14` PAUSED | **清单不全**（上轮只列 6 条）⇒ **登记待裁** |
| — | `TERM-999` | `reports/2026-09-22_A组_指针验证稿.md`（**A.4 阴性对照串**） | **元引用**（**A2 已登记**，非真缺口） |

---

## ⑧ 三量台账（**四量**）

| 量 | 内容 |
|---|---|
| **存量** | 机制 **29**｜`T` **45**｜`TERM` **14**｜`C` **21**｜`R` **91**｜`D` **55**｜判据脚本 **10**｜`ci.yml` `test` **32 步**｜台账节标题 **53**｜平台任务 **33**（本机） |
| **变量** | **本轮改动 4 份跟踪文件**：`CHARTER.md`／`TEMPLATES.md`／`BASELINE.md`／`workflows/universal_workflow.md`；**新增 2 份**：`instructions/INDEX.md`／本报告；**本机变更**：**删除 6 条**平台任务；**编号表变动**：`T` **+1**、机制 **±0**、`C ±0` |
| **补充增量** | ① **机制 11/28 边界成文**（**消解续两轮的悬空**）② **指针链闭环**（12/12）③ **`T-045` 入表**（版本号口径可检索）④ **指令索引建立**（断档 4 天有据可查）⑤ **6 条过期任务清除** ⑥ **并发风险登记在册** |
| **创新增量** | ① ★ **"加性边界声明"化解机制冲突**（**不撤号、不覆盖**，只明写分工 —— 选乙的可操作化）② ★ **"索引只到轮级、未内联就写未内联"**（**拒绝代填**）③ ★ **门控假阳性的"适用条件"被写进技能**（**"代入特征"只对含变量文案有效**）④ ★ **"消除告警"的两种正当路径被并列实证**：**编号真实登记**（机制 28／T-045——**非调参**）⑤ ★ **自查出一处"自己写错的数"并订正留痕**（断档 5 天 → **4 天**） |

---

## ⑨ 边界说明（**四类**）

| 类 | 内容 |
|---|---|
| **本地跑过** | ① **指针验证 12/12 PASS**（**跳 10 已转 PASS**）② 全量判据 **9/9 rc=0**（`check_migration_fidelity` 需传参 ⇒ rc=64）③ `check_id_set_diff` 自检 **22/22**、实跑告警 **3** ④ `check_report_structure` 自检 **10/10**、实跑告警 **0** ⑤ **C15 复算**（机制／`T`／`TERM`／`C`／节标题）⑥ **底图归属核查**（basemap 命中 **0**） |
| **CI 过** | ✅ **全绿**：`test` ＋ `宿主 field-render` ＋ `元内核引导镜像（QEMU）` **三 job 全 success**（★ **run 号按 C20 只记日报**）。逐行核实：「报告结构门禁」**真跑且 PASS**；`Manifest UI wasm` **通过**；`UI 自动目验` **通过** |
| **真实跑过** | ① **GitHub Actions 真实执行** ② **本机真实文件系统**（索引生成、底图核查、判据）③ **平台任务真实删除**（**删除后 `list` 复核**，6 条确已不在）④ **`git push` 真实成功**（`891ef30..32b4042`） |
| **未验证** | ① ★ **重试路径**（`R90`／`B5`）**从未触发** ⇒ **"抖动能否自愈"未验** ② **网络层本机不可复现** ③ **`T-045`／`T-044` 无机器判据** ④ **指令索引的报告列**：**未内联者未逐一人工核对** ⑤ **并发互斥**（未加锁）⑥ **`T-041` 剔除层对本轮"对照串"的处理**（`TERM-999` 仍在告警中，**未扩容**） |

---

## ⑩ 决策评估（七要素 · 本轮**新决策 1 项**）

### 决策：机制 11／28 冲突的处置 —— **甲 撤 28** ／ **乙 保留两条 ＋ 明写边界** ／ **丙 改机制 11 触发**

| 要素 | 内容 |
|---|---|
| **评估** | 本轮**已按你裁定选乙执行**：**两条并存**，把「**11 ＝ 骨架层（按需调用）／28 ＝ 调用层（所有动作经入口）**」写进机制 11 行、机制 28 行、**「机制之间的关系」块**（**三处**）；★ **机制 28 原句保留、不回改**（C19） |
| **优势** | **乙**：**不动编号锚点**（机制 20 规则 4）、**两句各管一事**、**"只读骨架不经入口"不违规**（**保住机制 11 的"按需"原意**） |
| **劣势** | **乙**：**两条机制讲同一对象** ⇒ 新人仍可能只看一条 ⇒ 靠**交叉引用**（三处均已互指）弥补 |
| **风险** | **甲**：**撤号＝坏锚点**（历史报告指 28）；**丙**：**改 11 触发＝把"按需"变"强制"**，**与 `§九`"不属开工必读"相抵** |
| **机会** | 乙的写法**顺带产出"边界声明"模板** —— 今后**两条机制重叠**时可**照此处置**（**明写分工 ＋ 不改原句**） |
| **建议** | **维持乙**（已执行）；★ 若后续再出现第三条同族机制，**改用"合并到一条的边界表"**（**避免 N 条互指**） |
| **等待决策** | ❌ **无需**（你已裁定并已落地）；本项**仅供复核** |

---

## ⑪ 需要用户决策（**4 项**）

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★ **另有 2 条"已过期"平台任务**：`社保事项双提醒`（@`09-06`，**仍 ACTIVE**）／`自动盘点`（@`09-14`，PAUSED） | **分两类**：**云内核无关者**（如生活提醒）⇒ **由你决定去留**；**PAUSED 的过期一次性任务** ⇒ **删** |
| ② | **过期任务的"扫描"是否常态化** | **建议**：**并入现有"清单类"任务**（如每日 P3 清单顺带列"平台过期任务"）—— **不新建机制**；**触发＝清单生成时** |
| ③ | **`T-041` 剔除层是否扩容**以覆盖"**阴性对照串**"（`TERM-999`） | **暂不扩**（**扩容＝把"谈论编号"也剔掉，会削弱判据的可见性**）；**维持 A2 兜底** |
| ④ | **指令索引是否补"未内联"轮次的报告** | **暂不补**（**需逐轮人工翻查**，成本高、收益低）；★ **改为**：**今后每轮在台账节内显式内联本轮报告名**（**一次投入、长期受益**） |

---

## ⑫ 下一步建议

1. **先裁 ⑪①**（2 条过期任务去留）—— 涉及"平台垃圾清理"，**风险最低、收益最直接**。
2. **次裁 ⑪④**（"今后每轮内联报告名"）—— 它是**指令索引可用性**的根因（**本轮索引"未内联"多，正是因为历轮未内联**）。
3. **触发式**（无新触发则不跑）：`docs` 第八批｜`advisor_brief` 复扫｜汇编（**随 `docs/` 更新**）。
4. ★ **建议把本轮"门控假阳性两例"的教训并入 `T-038`/`T-045` 同区**：**取值判据须写明"文案必须含变量"这一前提**（否则回显与运行行逐字相同）。

---

## ⑬ 启动回执

已读 `CHARTER.md`（§三 机制表 ＋ 关系块／§九）。
**当前状态**：`count = 323`（起点 · 实测）｜阶段二 · 2.4 边界层已验。
**本次任务**：push ＋ 8 项待裁落地。
**是否继续**：执行。
**★ 本报告版本（依 `T-045` ＝「首次提交时 `count`」）＝ `325`**（★ **提交后以 `git rev-list --count HEAD` 实测复核**；若不符则**按 `T-045` 在订正处标注订正时 `count`，不改本行**）。

---

## 附加表 · INTEL 状态表（依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| **INTEL-001** 工信部 AI 中小企业创业支持计划 | **观察中** | 组合：企业注册完成 ＋ 阶段三启动 | **✗ 未触发**（当前阶段二 · 2.4） | 无 |
| **INTEL-002** GPT-6 安全风险 | **观察中** | 事件：L7–L10 落地后 | **✗ 未触发**（L7 设计完成／L8–L10 冻结） | 无 |

**J1–J4 自查**：J1 ✅ 本表在（**且置于十四项之后**）｜J2 ✅ 未触发即未改状态（**不算违规**）｜J3 ✅ 无"待准备"项 ⇒ 无准备稿义务｜J4 ✅ 无"准备中"项。
★ **机制 29（INTEL）本轮无状态变更**；**下轮开工第 1 步仍＝读 `coordination/intel/INDEX.md`**。
