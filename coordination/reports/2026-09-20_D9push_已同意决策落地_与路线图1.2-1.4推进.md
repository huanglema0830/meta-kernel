【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-20 · D-9 push ＋ 已同意决策落地 ＋ 路线图 1.2–1.4 推进

## 1. 保密标记（C12）

🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**（无密钥／账号／本机绝对路径／内网 IP）。**可整份转发。**

## 2. 全景状态卡

```
┌─ 云内核项目 · 全景状态卡 ────────────────────────────────┐
│ 版本   以 git rev-list --count HEAD 为准（不写死，C2）   │
│        本轮起点 v0.227（232）→ 本轮末 235（含本提交）    │
│ 阶段   阶段二 · 2.3b ✅ 收口 ｜ 2.4 纯算层 ✅ ＋ 边界层开工│
│ 本轮   push 14 个 commit（CI 3 job 全绿）＋ 6 个本地 commit│
│ 未推送 本轮末 3 个（D-9 已 push；新增 3 未 push，夜间纪律）│
│ CI     最近绿 run 35468969321（D-9 push 后 3 job 全绿）   │
│ 决策   待裁 D-1…D-9（剩余）＋ O-1…O-3（O-1/O-3 本轮已办） │
└──────────────────────────────────────────────────────────┘
```

> **复核命令**（R71：数值不抄进文档）：
> `git rev-list --count HEAD`｜`git rev-list --count origin/main..HEAD`｜`gh run list --limit 3`

## 3. 任务

基准 **v0.227**｜总规划定位：**D-9 push ＋ 已同意决策落地 ＋ 行动路线图 1.2–1.4**｜类型：**裁定落地 ＋ 推进**｜优先级 **P1**（用户指定）。
约束：按 `CONSTRAINTS.md`；禁内联含反引号／特殊字符的命令（全程脚本文件）；不 `taskkill`、不让用户双击 bat。

## 4. 状态：**完成**（1.4 边界层为「开工」口径，见第 6／9 项）

## 5. 验收对照

| 验收标准 | 实测 |
|---|---|
| push 完成，CI 三 job 全绿 | ✅ push 14 个 commit；`run 35468969321`：`test` ✅／`boot-image` ✅／`host-windows` ✅ |
| D-1 落地 | ✅ `sin` 补 `r¹⁵/15!`；host 单测与 std 逐点对照；lib **474 passed / 0 failed** |
| D-3 落地 | ✅ 11 处「目标 5x」处置（3 治理文件：`BASELINE`／`CONSTRAINTS`／`ROADMAP`），过 C15 |
| D-4 落地 | ✅ 口径改「用户提供的演化型场方程」（未取得出处已写明） |
| D-5 落地 | ✅ 查证 arXiv:2512.11138 仅线性齐次 ⇒ 采用「齐次主部 ＋ 源项特解」分解 |
| D-6 落地 | ✅ 全库引用强制带全称「物理场神经网络（PFNN）」 |
| D8 三项落地 | ✅ 帧缓冲 Q5 ＋ 场方程 Q5／Q10（同一答案，两稿同写） |
| 1.2–1.4 推进 | ✅ 1.2 `predict.rs`（265 行／7 单测）＋ 1.3 `project.rs`（186 行／7 单测）＋ 1.4 boot `present.rs`（＋接线＋host 镜像 5 项＋CI 接线断言） |
| `2^k+1` 约束已写入文档 | ✅ 场方程设计稿 §2.2.3(2)「必须写明的口径」清单（**上轮遗留 5.1**：上轮未做，本轮补做） |
| P4 第三项完成 | ✅ **确认上轮已完成**（`_EM` 粗体容忍 ＋ `history_table_lines` 表格级历史豁免，实测在案） |
| O-3 接续文件同步 | ✅ `advisor_brief.md` ＋ 仓库外 `CONTINUITY.md` 同步为接续包口径（**本轮新办**；接续包升为权威入口） |
| C15 复算通过，编号表连续无缺号无重号 | ✅ 编号表条目 **0 处改动**（diff 实证，差值合预期）；C／T／机制 编号连续 PASS |
| CI 全绿 | ✅ D-9 push 后 3 job 全绿（本轮新增 3 个本地 commit **未 push** ⇒ 不触发，遵夜间纪律） |
| 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行即 9 步指令全文（自 `TEMPLATES.md` 行 104–118 **逐字**抽取） |

## 6. 未完成项

| # | 项 | 原因 | 处置 |
|---|---|---|---|
| 1 | **1.4 的"真实引导"未验证** | boot 层本机**完全不可构建**（无 MSVC 库／`clang`／`lld`，连 `cargo check` 都失败 —— 本轮实测） | 逻辑已镜像成 host 测试（5 项）；**真实引导交 CI `boot-image` job**（本轮 3 个 commit **未 push** ⇒ **本轮未跑到**） |
| 2 | 场方程设计稿「§2.2.8 Q7 出处」 | 用户提供的演化型场方程**出处未知** | 已在文中登记「请补出处」，不动 |
| 3 | 分解（齐次主部＋特解）的**文献出处** | 未展开检索 | 已在场方程稿 R4／Q5 行登记「分解的出处待补」 |
| 4 | 机制 25 **测不到 present 的 ⑫ 段** | 段号源仅取 `verify.rs` 段标题；present 在 `main.rs`／`present.rs` | **本轮新发现**，见第 7 项缺陷 ③，登记待裁（**不擅自扩大判据**） |

## 7. 真实缺陷（本次发现的）

| # | 缺陷 | 证据 | 状态 |
|---|---|---|---|
| ① | **R82 第 2 次实例（数值型静默过期）** | 本轮落地 `predict`／`project` 后，`BASELINE` 的 lib 计数 417→**474**、镜像 27→**32**、目标模块 58→**65** 三处同时过期 | **已处置**：按 R71 改为「引路径 ＋ 校验命令」（同类已第 4 次过期，写死即过期） |
| ② | 机制 25 的「表格级历史豁免」**可能过宽** | `BASELINE §18.4` 自述「三行均为**活跃状态陈述**」，但其表头含历史词（修订前／修订后）⇒ 被整表豁免 ⇒ P4 报「活跃模块数陈述 **0 条**」 | **已登记**（本轮用 R71 文案消除该表的三处数值依赖，不从判据侧改） |
| ③ | **机制 25 段号源单一 ⇒ 新段对判据不可见** | 机器「最大段号 ⑪」只统计 `verify.rs` 段标题；`present.rs` 的 ⑫ 段（121–125）**不在统计内** | **已登记**（R80 家族新盲区，待裁：是否扩段号源到 boot 其它文件） |
| ④ | boot 层**不可本机编译检查**（环境事实） | `cargo +nightly check` 实测：`rust-lld: could not open 'kernel32.lib'`（无 MSVC 库）；`which clang/gcc/ld.lld/lld` 全无 | **已登记**（既有认知的**首次机器实证**，写入 `present.rs` 头与报告） |

## 8. 三量台账（四量）

- **存量**：D-1／D-3／D-4／D-5／D-6／D8／O-3 落地成果；1.2／1.3 纯算层（`predict`／`project`）；1.4 边界层（`present`）；上轮遗留 5.1／5.2 处置；既有判据 9 类；C1–C19／机制 1–25／R1–R82／T-001–T-034。
- **变量**：`coordination/discussions/` 三份稿（D-4/D-5/D-6/D8 ＋ 5.1）；`BASELINE.md`（R71 处置）；`advisor_brief.md` ＋ 仓库外 `CONTINUITY.md`（O-3）；`ci.yml`（＋⑫ 段接线断言步骤）；`lib.rs`（＋2 模块）；`mirror_bare_assertions.rs`（＋5 项 ＝ 27→32）；测试计数 lib 460→**474**；模块 目标 63→**65**；未推送 0→**3**。
- **补充增量**：① **boot 层不可本机编译**的机器实证（`rust-lib` 缺 MSVC 库）；② **「裸机新增段必须配 CI 接线断言」**的落地范式（⑫ 段：文件存在＋模块声明＋`run()` 真调用＋五码齐备＋host 镜像 5 项）；③ **R82 第 2 次实例**（自造模块即推高机器计数 ⇒ 文档写数值必过期）。
- **创新增量**：★ **「呈现 ＝ 内核状态的直接投影（`Project(S)`），无渲染管线」**由裁定转为**可判据的实现**（`project.rs`：规格非法／输入不足一律返回空 `Vec`，**不冒充成功**；越界**钳位不绕回**；单调性正向对照）；★ **双向通路的双向性判据**（`top_down` **不得**改动 `prior` —— C19 概念分离的可执行形态）；★ **帧缓冲边界层"写入 → 回读 → 逐字节比对"三态自检**（含**退化路径显式返回 0 而非假装验过**，且**写入必被后续整屏 `fill()` 覆盖 ⇒ 不改变既有绿/红门禁语义**）。

## 9. 边界说明（四类）

- **本地跑过** ✅ `cargo test -p meta-kernel-core-nostd --lib` = **474 passed / 0 failed**；`--test mirror_bare_assertions` = **32 passed / 0 failed**（含 ⑫ 段 5 项）；判据回归 **13 项全 PASS**：机制 18（`--selftest` ＋ 实跑）／机制 21（双侧）／机制 22（`--selftest` ＋ `--ci`）／机制 24（双侧）／机制 25（双侧）／机制 17 `--mode=syntax`／迁移闭包／迁移保真度（`--selftest`）／屏幕转储判据（`--selftest`）；⑫ 段 CI 接线断言**本地预演全 OK**。
  > ⚠️ **一处自查更正（阴性先怀疑工具链）**：`check_migration_fidelity.py` **裸跑** exit=64 —— 实为**用法错误**（该脚本需 `源文件 迁移后文件` 两个位置参数，或 `--selftest`），**非判红**；补 `--selftest` 后 exit=0 且「两侧都符合预期」。**报数须写清调用方式**（R35）。
- **CI 过** ✅ **D-9 push 后**：`run 35468969321` **3 job 全绿**。⚠️ **本轮新增的 3 个 commit 未 push ⇒ CI 未触发**（遵夜间不 push 纪律）⇒ **新增代码与 `ci.yml` 新步骤尚未经 CI**。
- **真实跑过** — **无**。1.4 的「真实引导」**未跑**（boot 层本机不可构建，见缺陷 ④）。
- **未验证** ❌ ① boot 层新增代码**未经任何编译**（本机不可编译；CI 未触发）；② ⑫ 段在 QEMU 上的 121–125 实际行为；③ 场方程出处（Q7）与「齐次主部＋特解」分解出处；④ 1.2／1.3 与后续 L6 呈现层的**集成**（当前只到纯算层）。

## 10. 决策评估（七要素）

**新决策：是否把 1.4 的 boot 改动与 `ci.yml` 新步骤**（尚未经 CI 的 3 个 commit）**在本轮 push？**

| 要素 | 内容 |
|---|---|
| **评估** | 用户验收含「CI 全绿」，而 D-9 的 push 已使 CI 全绿；本轮新增 3 个 commit 按夜间纪律**默认不 push**。二者张力点：**不 push ⇒ 1.4 永不验证**；**push ⇒ 可能换来一次红**。 |
| **优势** | push：唯一能让 1.4 从「未验证」转为「已验证」的路径；且 `present` 的写入被整屏 `fill()` 覆盖 ⇒ **既有绿屏门禁语义零变化**（回归风险面小）。不 push：零风险，但 1.4 停在「开工」且代码**未经编译器**。 |
| **劣势** | push：boot 层代码本机不可编译 ⇒ 若语法/借用写错，需**再等一轮 CI** 才拿到行号（一轮约 2–5 分钟）。 |
| **风险** | ① 编译错误（可修，不调参）；② ⑫ 段在真实 `FrameBufferInfo` 上的**退化路径**（宽高 < 2 或格式不支持 ⇒ 返回 0，**不判红**）⇒ 若 QEMU 真退化，**CI 仍绿但 1.4 实未验到** ⇒ **必须靠日志与报告区分**（已在 `present.rs` 头显式写清）；③ `boot-image` job 需 nightly ＋ QEMU（CI 侧已具备）。 |
| **机会** | 顺带把 **⑫ 段接线断言**纳入 CI ⇒ 未来「present 被误删/漏挂」会**机器判红**，不再依赖人记得。 |
| **建议** | **push 这 3 个 commit**（用户放行即可），并在 push 后**当场核对** `boot-image` job：① 是否整屏绿；② 是否出现 121–125 任一编号。**若 boot 构建失败 ⇒ 按纪律查失败步骤，不调参打绿**。 |
| **等待决策** | **是**（属 D-9 家族的延续：本轮的 3 个 commit 是否放行 push）。 |

## 11. 需要用户决策

1. **本轮 3 个本地 commit 是否 push**（= 上表建议项；**最急**，决定 1.4 能否验证）。
2. **D-2**：机制 25 P4 是否**转判红**（第三项已完成，前置条件已满足）。
3. **缺陷 ③**：机制 25 段号源是否扩到 boot 其它文件（否则 ⑫ 段永远对判据不可见）。
4. **D-1 续**：`sin`／`cos` 是否继续补项（当前主区间 ≤4 ULP **已达标**；残差转由**大参数归约**主导 ⇒ 下一步是 **Payne-Hanek**，**成本高**）。
5. **D-7**：身份档案 I1–I3（名字／称呼／`BOOTSTRAP.md` 删留）—— 本轮未办（暂缓项）。

## 12. 下一步建议

1. 先答第 11 节 **第 1 问**（push 与否）—— 它卡住 1.4 的闭环。
2. push 后**当场**核对 `boot-image` job 日志（是否 3 job 全绿 ＋ 是否出现 121–125）。
3. 若绿：1.4 转「已验证」；再议 **L6 呈现层接入**（把 `present` 从自检路径接成真实呈现路径 —— 属新裁定，须先出改动清单）。
4. 若红：按纪律**只查失败步骤**，先自查期望值（本项目的首因统计：期望值写错 > 实现写错）。

## 13. 启动回执

已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD`**（不手写，C2）｜本次任务：D-9 push ＋ 已同意决策落地 ＋ 路线图 1.2–1.4｜**是否继续：等待裁定（第 11 节 5 项）**。

## 附：本轮读了底图哪几份、哪几行（D54）

| 底图／脚手架 | 读了哪里 | 用途 |
|---|---|---|
| 根 `README.md`（132 行） | **全部** | 状态／阶段／命名唯一权威／版本规则；接续包块 1／块 2 的底图依据 |
| `docs/LAYER_BASEMAP_L0_L6.md` | 生成模型／预测编码／呈现（L6）相关节 | 1.2 双向通路的「大脑是预测机器」口径；`predict.rs` 头引用 |
| `docs/LAYER_ARCHITECTURE.md` §3.2／§4.L3 | 命名与端点表（**本轮未改动**，仅对齐口径） | 确认「呈现」属 L6，未越名 |
| `coordination/CONSTRAINTS.md`（409 行） | **C1／C3／C5／C9／C12／C13／C15／C19 原文** | 落地纪律；`project`／`predict`／`present` 三条守约引用 |
| `coordination/CHARTER.md`（228 行） | §三 机制 21（执行体边界）／机制 25（文档一致性） | 1.4 落点判据；缺陷 ③ 的「段号源」取证 |
| `coordination/TEMPLATES.md` | **行 32–58（十四项）＋ 行 98–125（第〇项 9 步逐字块 ＝ 行 104–118）** | 报告结构与**第 0 行逐字抽取**（D40，不手写） |
| `coordination/BASELINE.md` | §12（分片实测）／§14.7（行 153 旧口径）／**§18.4（R82 处置表）** | 缺陷 ①／② 的取证与处置落点 |
| `coordination/discussions/2026-09-19_接续包.md` | 块 0／块 4／块 9 | O-3 的同步基准 |
| `meta-kernel-boot/kernel/src/{main,verify}.rs` ＋ `meta-kernel-core-nostd/src/{fb,field,project}.rs` | 接口与段号布局（**实现侧，非底图**） | 1.4 接线与编号区间选择 |
