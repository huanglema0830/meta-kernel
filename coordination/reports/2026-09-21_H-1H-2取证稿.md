【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# H-1 / H-2 取证稿（2026-09-21 · v0.279）

> **性质**：**只出稿，不改文件**（用户 §二 明示）；本稿**不含任何 `docs/` 改动**。
> **保密标记**（C12）：🔓 公开。⛔ 机密 **0**、🔐 保密 **0**（只用仓库相对路径）。
> **边界**：**未运行窗口宿主**；"已交付/已接入"判定＝**静态落点检索（`include_str!`／`create_shader_module`／CLI 参数／CI 断言）**，非运行期证据（见 §3）。

---

## 0. 全景状态卡

- **版本**：count = **279**（实时取；label＝count）。HEAD `8eefdeb`（v0.279）。
- **远端**：`origin/main = db4d37f`（v0.278，**已推**，本轮 §〇 完成）。
- **阶段定位**：阶段二 · 2.4 边界层已验（与 `ROADMAP.md` 一致）。
- **本轮已处理待裁**：⑬-1 push 5 commit ✅｜⑬-2 `docs` 第六批 8 处落地 ✅（`8eefdeb`）｜⑬-3 **H-1/H-2 取证（本稿）**｜⑬-4 `advisor_brief` 改触发式（见 `BASELINE §三十四`）。

---

## 1. H-1：`docs/HOST_UI_DECISION.md` 待核 —— **结论：「未交付窗口宿主」类陈述已过期**

### 1.1 被核对的陈述（逐字）

| # | 位置 | 原文（逐字） |
|---|---|---|
| A | **L4**（头部结论） | `> 结论：**选 egui + wgpu**；但本轮**未交付可运行的窗口宿主**，原因与下一步见 §3/§4（诚实说明）。` |
| B | **§4 L69** | `\| 1 窗口能打开并看到场域画面 \| ⏳ **未交付**（见 §3） \|` |
| C | **§4 L70** | `\| 2 地址栏可输入 URL \| ⏳ 未交付（方案已定：egui 文本框） \|` |
| D | **§4 L71** | `\| 3 多标签可切换 \| ⏳ 未交付（方案已定） \|` |
| E | **§4 L72** | `\| 4 下载能触发 \| ⏳ 未交付（方案已定：写 .ppm + .json） \|` |
| F | **§6.4 L122** | `地址栏 / 多标签 / 下载 / egui —— **属第三步后续步骤**，等 UI 路线裁决（§1 三方案）。` |

### 1.2 实测（代码落点 ＋ CI 断言）

| 能力 | 落点 / 证据 |
|---|---|
| **开窗（winit 0.30）** | `host/field-render/src/window.rs` L22 `use winit::application::ApplicationHandler;`、L177 `impl ApplicationHandler for App` |
| **egui UI** | `host/field-render/Cargo.toml`：`egui = "0.30"` ＋ `egui-wgpu` ＋ `egui-winit`；`window.rs` L1 自述「**窗口宿主（winit 0.30 + wgpu surface + egui 0.30 UI）**」、L7「UI pass：egui 0.30」、L198 `egui_state.on_window_event` |
| **地址栏 / URL 取源码** | `window.rs` L139 `safe_url()`、L151 `fetch_source()`（系统 `curl` 逐参数传参）；CI 以 `--url <url>` 驱动（见下） |
| **多标签** | `window.rs` L74 `struct Tab { url, … }`、L84 `make_tab(...)`、L252–253 `NewTab` / `CloseTab(usize)` |
| **下载** | `window.rs` L256 `Action::Download`、L729 `fn download()`（写 `field.json` ＋ `frame.ppm`）、L1036 / L1069 动作分派；**产物实证**：`host/field-render/downloads/1789514105/{field.json, frame.ppm, page.html, world.txt}` |
| **CI 硬断言（验收④）** | `.github/workflows/ci.yml` **L1016** `- name: 验收 ④ 真实开窗 + URL 取源码 + 上屏回读（门禁）`；L1033 起本地起服务、L1034 `"$EXE" --url http://…/page.html --frames 120`；L678–679 注释：**「④ 真实开窗 + URL + 上屏回读：**门禁**（run `35044362728` 上真实验证通过后，本轮按 D10 去掉 `continue-on-error`，改为硬断言…）」** |
| 文件内**自相矛盾** | 同文件 **§6（v1.1）**：`## 6. 第三步**第一步已完成**：窗口宿主（不含 UI）`，§6.2 客观验收表：`窗口能打开` ⇒ ✅ **1536×1536**（HiDPI）＋ 三样本方差 **43.9 / 1182.8 / 1370.7** ＋ 呈现口径 60–62 FPS ＋ `llvm-objdump` 检 WebView2 = **0 处** |

### 1.3 判定

- **A/B/C/D/E/F 六处均过期**：`host/field-render` 已交付**可运行窗口宿主 ＋ egui UI ＋ 地址栏取源码 ＋ 多标签 ＋ 下载**，且**上屏回读**已入 CI **硬门禁**（非 `continue-on-error`）。
- **同文件内部矛盾（C19）**：**§4（v1.0 口径）称「未交付」** vs **§6（v1.1 口径）称「窗口宿主已完成且上屏自检通过」**；§6 更新、§4 未同步。
- **仍成立的（不属过期）**：§4 L73 帧率 ✅ / L74 无 WebView2 ✅；**§8.5「仍未做」**（① 老笔记本实测 ② 地址栏依赖系统 `curl`）——**该两行与实况一致，维持**。
- **建议（待授权改底图）**：给 **L4 ＋ §4 表 L69–L72 ＋ §6.4** 加锚（口径＝「上述状态为 **v1.0／v1.1 时点**；**至 v0.279 已交付**：窗口 + egui + 地址栏取源码 + 多标签 + 下载，CI 验收④硬门禁」），**原句不改**（C19）。

---

## 2. H-2：两处 wgsl 接入待核 —— **结论：均已接入（`include_str!` 铁证）**

### 2.1 待核问题的由来

`docs/FIELD_RENDER_SOURCELESS_DESIGN.md` §2.3（L79–L82）称 `host/sky-browser/shaders/{preprocess,render}.wgsl` 为**设计骨架**；本轮裁定 2 已在 **L82** 加锚（其中含"两文件是否已接入 **待核**"一句）。

### 2.2 实测（编译期内嵌 ＋ 模块创建）

| 证据 | 位置 |
|---|---|
| **两 wgsl 编译期内嵌** | `host/field-render/src/main.rs` **L317**：`source: wgpu::ShaderSource::Wgsl(include_str!("../../sky-browser/shaders/preprocess.wgsl").into())`；**L325**：同法内嵌 `render.wgsl` |
| **真创建 shader module** | 同文件 L315 `create_shader_module(… "preprocess")`、L323（render）、L319/321（`sort.wgsl`） |
| **真进渲染管线** | L447 `entry_point: Some("preprocess")`；L585/L615 `record_field_passes`：**preprocess → 双调排序 → 渲染**；`window.rs` L6 同述 |
| **CI 覆盖** | `ci.yml` 验收 ①（离屏自检、方差 > 1）＋ 验收 ②（GPU 双调排序）＋ 验收 ④（上屏回读） |

### 2.3 判定

- **H-2 结论＝「两文件已接入 `host/field-render`」**（非"设计骨架"停留在纸面）；`include_str!` 为**编译期**凭证 ⇒ 文件被删/路径失效会**编译失败**，可证"确有引用"。
- **★ 回填（本轮，同授权位置）**：因裁定 2 的 **F-7 锚**中「现仅 `shaders/sort.wgsl`／待核」系**执笔方本轮新引入的事实错误**，已按 C19 在**同一授权位置**订正为「**已接入**」并注明来源；**原句仍未改**。（diff：`docs/FIELD_RENDER_SOURCELESS_DESIGN.md` 1 行；判据 F FAIL→update→PASS；汇编 第2批 1 行／合订本 1 行；反向自检 PASS。）
- **注意（F-6 不变）**：`FIELD_PRESENTATION_DESIGN` L57 的「**用于场域同步**」接线仍属**待核**（`mesh.rs` 提供互联能力，但"场域同步"的调用点**未核**）⇒ 该处锚**维持**。

---

## 3. 边界说明（四类）

- **本地跑过**：✅ 只读检索（`grep` 读 `window.rs`／`main.rs`／`Cargo.toml`／`ci.yml`；`ls` 读 `downloads/` 产物）。
- **CI 过**：✅ 相关证据取自 `ci.yml` **既有**门禁（验收④为硬断言）＋ 本轮 §〇 推送的 CI（结论按 C20 只记日报）。
- **真实跑过**：⛔ **未在本机运行窗口宿主**（无显示环境/未起服务）；"已交付"的最终确认**属 CI 运行期证据**（L678–679 记 run `35044362728` 通过）。
- **未验证**：⚠️ ①H-1 中「egui UI 在真机的交互手感」；②H-2 中两 wgsl 的**渲染效果**（只证"已接入"，未证"效果达标"）；③F-6 的「场域同步」接线。

## 4. 三量台账（存量 / 变量 / 补充增量 / 创新增量）

| 量 | 本稿内容 |
|---|---|
| **存量** | `docs/HOST_UI_DECISION.md`；`host/field-render`（window.rs／main.rs／Cargo.toml／downloads/）；`.github/workflows/ci.yml` |
| **变量** | **1 处**：`docs/FIELD_RENDER_SOURCELESS_DESIGN.md` **L82 锚内一句**（「待核」→「已接入」；**属裁定 2 授权位置的准确性订正**） |
| **补充增量** | H-1 **6 处过期陈述**（L4／§4 L69–L72／§6.4）＋ H-2 **结论：已接入**（含 `include_str!` 凭证） |
| **创新增量** | 0（取证类，无新机制/新模块） |

## 5. 本轮读了底图哪几份、哪几行

| 底图 | 行 | 用途 |
|---|---|---|
| `docs/HOST_UI_DECISION.md` | L1–6、L55–76、L80–89、L93–123、L163–193 | H-1 |
| `docs/FIELD_RENDER_SOURCELESS_DESIGN.md` | L74–97 | H-2 |
| `host/field-render/src/window.rs` | L1–10、L22、L53、L74–90、L133–169、L177、L197–198、L252–256、L729–762、L1036、L1069、L1210 | H-1 |
| `host/field-render/src/main.rs` | L7–9、L315–325、L447、L585–615 | H-1／H-2 |
| `host/field-render/Cargo.toml` | L1–30 | H-1 |
| `.github/workflows/ci.yml` | L672–684、L926–949、L985–1034 | H-1 |
| `coordination/TEMPLATES.md` | L102–119 | 本稿第 0 行逐字对齐 |
