【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · **A–G 七组落地**（八步入口文件 ＋ `R90`/`B5` 加固 ＋ `F-2` 修列 ＋ INTEL 登记为机制 29 ＋ 平台任务下线 ＋ 版本号口径 ＋ §五十二 汇总）

> 归口：**本轮指令 A–G 组**｜日期：2026-09-22｜性质：**机制建设 ＋ 裁定落地（分组）**
> ★ **版本口径（依本轮 F 组新规）**：**报告内版本号 ＝ 本报告「首次提交」时的 `count`** ⇒ **见 ⑬ 启动回执**。

---

## ① 保密标记（C12）

| 项 | 内容 |
|---|---|
| **保密级别** | 🔓 **公开**（本报告**只含仓库内公开信息**） |
| **四级清单** | 🔓 公开（可对外）／🔒 半公开（顾问可读）／🔐 保密（仅本机）／⛔ 机密（绝不外发） |
| **屏蔽提示** | **无屏蔽项** —— 本报告**不含** `{PRIVATE_ASSETS}` 真实值、**本机绝对路径**、用户名、密钥、真实任务数据。★ 记忆文件路径一律写作**工作区相对路径** `.workbuddy/memory/MEMORY.md`。 |

---

## ② 全景状态卡（自动生成）

| 项 | 值 |
|---|---|
| **版本** | **起点 `count 315`**（★ 指令写 **311**，**实测 315**，差 4 ＝ 夜间 4 个 commit）⇒ **收口 `count 322`**（`891ef30`） |
| **阶段定位** | 阶段二（可引导 ISO）· **2.4 边界层已验**|
| **待决策项** | **8 项**（见 ⑪） |
| **CI 状态** | **全绿**（三 job 全 success；★ **run 号与逐 job 三态按 C20 只记日报、不入库**） |
| **仓库** | `ahead 0`（A–G **7 个 commit 已 push**）；工作树**待提交 2 份**（本报告 ＋ 指令归档） |

---

## ③ 任务（含基准与总规划定位）

- **基准**：`count = 315`（**实测**；指令给 311）。
- **总规划定位**：八步入口文件 ＋ **8 项待裁**（**分组处理**）。
- **本次任务**：按 **A→B→C→D→E→F→G** 七组执行，**每组独立本地 commit**，**最后统一 push**（T-038 门控）。
- **★ 本轮读了哪些文件、哪几行**（如实）：

| 文件 | 读的位置 |
|---|---|
| `coordination/CHARTER.md` | **§三 全部机制表全文**（机制 1–27）｜**§九 按需深入（含开工顺序行）** |
| `coordination/BASELINE.md` | **§六 R 编号指针表**｜**§47.3 A2 豁免登记表**｜**§五十一 末节**｜**文件尾**（追加点） |
| `coordination/TEMPLATES.md` | **§〇 固定相 A／B**｜**§二 第〇项（顾问 8 步）**｜**§7.2 变更记录表尾（`T-041`/`T-042`/`T-043`）** |
| `coordination/ROADMAP.md` | **L33–39（阶段表）／L44–70（子任务2 表 ＋ 口径脚注）／L132–140（里程碑）** |
| `coordination/TERMS.md` | **§一之一／一之二／一之三（`TERM-001`…`013`）／文件尾更新记录** |
| `coordination/CONSTRAINTS.md` | **全文节标题（`C1`…`C21`）** |
| `.github/workflows/ci.yml` | **L440–530**（wasm step ＋ UI 目验 step）｜L738–795（`R88` 既有重试写法） |
| **底图（根 `README.md` ＋ `docs/` 35 份）** | ★ **本轮未读**（**零改动** ⇒ 按 `CHARTER §九`"按需"不读；**不是漏读，是未触发**） |

---

## ④ 状态

**状态**：**完成**（A–G 七组全部落地；H 组 push 完成；工作树待提交**本报告** ＋ 指令归档）。

> ★ **一处格式订正（★ 判据即时自证 · 第 2 次）**：本节初稿只写标题 `## ④ 状态` ＋ 次行「完成」，**未按固定相 B 第 4 项的"字段"形态写成同行** ⇒ 被 `check_report_structure.py` **当场判缺第 4 项**。★ **处置＝改报告（不改判据）**：固定相 B 第 4 项确是**字段**（`状态：完成/部分完成/未完成`），**格式偏离是我方缺陷**。★ **先例**：`F-4`（v0.308 轮报告把 INTEL 状态表顶掉第 6 项）—— **同族**（**新判据建立过程中即时抓出我的输出缺陷**）。

---

## ⑤ 验收对照（指令「验收标准」逐条）

| # | 验收标准 | 实测 |
|---|---|---|
| 1 | **A 组**：`ENTRY.md` 建立｜`TERM-014` 登记｜机制登记｜指针验证稿出 | ✅ **逐字落地**（`ENTRY.md` 首行＝`【本入口对应的自定义指令】`）｜`TERMS.md` **新增 §一之四** ＋ `TERM-014`｜**机制 28**｜验证稿 **11/12 PASS** |
| 2 | **B 组**：`R90` ＋ `B5` 加重试，逐字对照 | ✅ `ci.yml` **两处 `until` 循环**（1 首试 ＋ 最多 3 重试、间隔 5s）＋ **重试耗尽 `exit 1`**；🔓 **逐字对照见 §52.B**；★ **`R90` 另加 `tar -tzf` 验真** |
| 3 | **C 组**：`F-2` 乙案修列，逐字对照 | ✅ `2.5`／`2.6` **P3 → P1** ＋ **❌ 不可夜间跑** ＋ **最后复核 2026-09-22** ＋ **新增「标注型列」条文**；🔓 对照见 §52.C |
| 4 | **D 组**：INTEL 正式登记为机制｜MEMORY 软上限 8 KB | ✅ **机制 29**（★ **28 已被 A 组占用 ⇒ 顺延**，如实记）｜**`T-044`**（软上限 8 KB ＋ 30 天复核） |
| 5 | **E 组**：6 条过期平台任务下线 | ⚠️ **部分**：**6 条中仅 1 条需处置**（仍 ACTIVE ⇒ **已置 PAUSED**）；**其余 5 条本就 PAUSED**（**无需动作**）；**未删除**（指令只要求 PAUSED） |
| 6 | **F 组**：报告内版本号口径写入 | ✅ **§52.F 规则条文**（**写入 `BASELINE.md`**，非 `TEMPLATES.md` —— 合规于指令"或"字） |
| 7 | **G 组**：本轮成果并入 `BASELINE §五十二` | ✅ **§五十二**（**`52.A`…`52.G`**，含 **C15 复算**） |
| 8 | **C15 复算通过，编号表连续无缺号无重号** | ✅ 机制 **27 → 29（+2）**｜`T` **43 → 44（+1）**｜`TERM`（文件口径）**13 → 14（+1）**｜`C 21 ±0`；机制 `1..29`／`T 1..44` **连续无缺重** |
| 9 | **CI 全绿（按 T-038 门控）** | ✅ **三 job 全 success**；门控证据 **`READY_TO_EDIT`**（★ **C20：run 号／三态不入库**） |
| 10 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本报告第 0 行 |

---

## ⑥ 未完成项（清单 ＋ 原因）

| # | 未完成项 | 原因 |
|---|---|---|
| 1 | **指针链「反向可达」**（骨架 → 入口） | `workflows/universal_workflow.md` **不在 A 组授权文件内** ⇒ **不擅改**（修法一行，见 ⑪） |
| 2 | **机制 11 与机制 28 的触发口径冲突** | 属**元规则级**（机制定义）⇒ **须用户裁定**（本轮**只留痕、未改机制 11**） |
| 3 | **INTEL 的 J1–J4 机器判据** | 指令**未要求**；按机制 26 同口径**先立条文、后议机器化** |
| 4 | **重试「重试路径」未在 CI 触发过** | **本轮首跑即成功**（首试即过）⇒ **只有"成功路径"被验证**（见 ⑨） |
| 5 | **6 条过期任务的「删除」** | 指令只要求"置 PAUSED" ⇒ **未删** |
| 6 | **`§五十二` 未补 `T-045`** | 指令 `D.2` 明确要求补 `T` 编号、`F` 组**未要求** ⇒ **按字面不补** |
| 7 | **指令归档断档 5 天**（09-18 ～ 09-21 各轮指令**未归档**） | ★ **本轮才发现**（见 ⑦ P2-3）；**本轮已归档**，历史**未补**（待裁） |

---

## ⑦ 真实缺陷（**B2 ＋ C1**：本轮新增告警 ＋ 来源溯源）

**★ 六族告警行（`C1` 载体 · 固定一行）**：**六族告警：本轮 4 条**（`MECH` 无／`C22`／`T-00`／**`TERM-999`**／**`T-045`**，**均为元引用**）**｜对比上轮 ±+1**（上轮 3 条）｜**真缺口 0 条**。

| 级 | 缺陷 | 来源／证据 | 性质 |
|---|---|---|---|
| **P1** | ★★ **门控脚本 v6 首版把 `run:` 回显当成运行期证据** ⇒ 报「**本次触发重试 = YES**」 | 本轮实测：切片内 `::warning::wasm-bindgen` **命中 1 行**，但用**代入特征** `第 [0-9]+ 次重试` 复筛 ⇒ **0 行**（那 1 行是**脚本正文回显**，带 ANSI `^[[36;1m` 前缀） | **R89 家族（第 N 次复现）** ★ **门控脚本自己犯了它要防的错** ⇒ **已订正为「未触发重试」** |
| **P1** | **`TERM-999`／`T-045` 两条新告警** | 均为**我自己的产出自指污染**：`TERM-999` ＝ A.4 **阴性对照串**；`T-045` ＝ **52.F 里"讨论要不要补这个号"** | **自指污染第 4／5 例** ⇒ **已入 `A2` 兜底表**（`A2` 区段已被登记侧排除 ⇒ **登记不会让告警消失**） |
| **P2** | ★ **同一事实两个数**：`TERM` **判据读数 14** vs **文件口径 13** | 指令写「C15：**TERM 14→15**」；`git show 4fae49a:coordination/TERMS.md \| grep -c` **= 13**。★ **根因实测**：`TERM-014` **在 A 组前已被引用**（`git grep -o` 实测含 `TERM-014`）⇒ 判据「登记集扫全仓」把它算成**已登记** | **R79 家族（同一事实两个数）** ⇒ **以文件口径为准** |
| **P2** | ★ **台账节标题两种格式**：`## N、`（**47**）／`## §N ·`（**33–37 共 5**） | 本轮实测 —— 只认一种格式的检查会**误判"缺 33–37"** | **口径缺陷**（节号 **实际连续**；★ **本轮先误判、后自查订正**，见 ⑨ 未验证） |
| **P2** | ★★ **指令归档断档 5 天**（机制 9 ＋ 收工四步③） | `ls coordination/instructions/` **20 份全为 09-16／09-17** | **机制空转**（**R83 家族**：长期未执行 **且无判据**） |
| — | `MECH 28` **已消警** | 机制 28 **于 A 组真实登记** ⇒ `MECH` **引用 27／登记 29／差异 0** | ★ **不是调参打绿，是编号真的存在了** |

---

## ⑧ 三量台账（**四量**）

| 量 | 内容 |
|---|---|
| **存量** | 机制 **29**（＋2）｜`T` **44**（＋1）｜`TERM` **14**（＋1，**文件口径**）｜`C` **21**｜`R` **91**｜`D` **55**｜判据脚本 **10**｜`ci.yml` `test` **32 步**｜台账节标题 **52** 节 |
| **变量** | **本轮改动 7 份跟踪文件**：`CHARTER.md`｜`TERMS.md`｜`TEMPLATES.md`｜`ROADMAP.md`｜`BASELINE.md`｜`ci.yml`｜（新增）`workflows/ENTRY.md`；**新增 3 份**：`ENTRY.md`／验证稿／指令归档；**编号表变动**：机制 **+2**／`T` **+1**／`TERM` **+1**／`C ±0` |
| **补充增量** | ① **八步入口链贯通**（自定义指令 → `TERM-014` → `ENTRY.md` → 机制 28）｜② **`R90`＋`B5` 加固上线并首跑通过**｜③ **INTEL 正式成为机制**｜④ **`F-2` 修列**（标注与实质对齐）｜⑤ **报告版本号口径成文**｜⑥ **8 项待裁中 6 项落地** |
| **创新增量** | ① ★ **"加性登记"化解机制冲突**（新机制定位为"入口与适用"，**不覆盖机制 11**）｜② ★ **`tar -tzf` 验真式重试**（治 **curl 对损坏响应不报错** 这一 R90 真实成因）｜③ ★ **"阴性对照串"造成自指污染的实证**（第 4 例）｜④ ★ **"讨论编号要不要存在"本身即自指**（第 5 例）—— **元引用无法靠"少谈论"规避**｜⑤ ★ **门控脚本自查自纠**（把自己的假阳性当场掀开）｜⑥ ★ **"同一事实两个数"的第 2 次实证**（`TERM` 14／13） |

---

## ⑨ 边界说明（**四类**）

| 类 | 内容 |
|---|---|
| **本地跑过** | ① **指针验证 12 项**（**11 PASS／1 FAIL**）② **重试循环语义正反对照 3/3**（`sleep` 压 0s）③ **全量判据 9/10 `rc=0`**（`check_migration_fidelity` **需传参 → rc=64**）④ **`--selftest`**：`id_set_diff 22/22`／`report_structure 10/10`／其余 5 项均 `rc=0` ⑤ **`pyyaml` 校验 `ci.yml`**（jobs ＝ `test`／`boot-image`／`host-windows`）⑥ **C15 取数**（机制／`T`／`TERM`／`C`／节标题） |
| **CI 过** | ✅ **全绿**：`test` ＋ `宿主 field-render` ＋ `元内核引导镜像（QEMU）` **三 job 全 success**（★ **run 号与三态按 C20 只记日报**）。★ **逐行核实**：「报告结构门禁」step **第 3 次执行、真跑且 PASS**（运行期证据 4 项齐；实跑「扫描 138 份 ｜ 报告体 73 ｜ 非报告体 65」）；**`Manifest UI wasm` step 通过**（`MANIFEST_UI_WASM_OK`） |
| **真实跑过** | ① **GitHub Actions 真实执行**（非 mock）② **本机真实文件系统**（指针验证的存在性/文本比对）③ **平台定时任务真实读写**（`automation_update` 对 1 条任务**真实置 PAUSED**，`view` 复核）④ **`git push` 真实成功**（`4fae49a..891ef30`） |
| **未验证** | ① ★ **`R90`／`B5` 的"重试路径"未在 CI 触发过** —— **首跑即成功**，故**只验了成功路径**；**网络层本机无法复现** ⇒ **"抖动时是否真能自愈"仍未验** ② **网络层未验**（M 层对照只验控制流）③ **INTEL 的 J1–J4 无机器判据**（人工承载）④ **指针链反向不可达** ⑤ **`2.5`／`2.6` 的 P1 决策本体未做**（改标 ≠ 决策已下）⑥ **历史指令未补归档** ⑦ **节号连续性**：★ 我**先误判"缺 33–37"**，后自查发现是**两种标题格式**所致 ⇒ **如实留痕**（**R89 家族：先下结论、后取证**） |

---

## ⑩ 决策评估（七要素 · 本轮**新决策 2 项**）

### 决策 1：INTEL 的编号 —— **顺延 29** 还是**回填 28**？

| 要素 | 内容 |
|---|---|
| **评估** | A 组已按指令"查最大编号顺延"取 **28**；D 组 INTEL 只得取 **29**，而上轮待裁文字写的是"**机制 28**" |
| **优势** | **顺延**：**编号只增不改**（机制 20 规则 4）、**可复现**（谁先登记谁得号）｜**回填**：与上轮文字一致 |
| **劣势** | **顺延**：与上轮"机制 28"表述不一致（**已如实记**）｜**回填**：**改已有编号 = 破坏稳定锚点**，且 A 组已入库（`307cdb7`） |
| **风险** | **回填**：**回头改号**会让历史报告指向错号（与 **`C21`／机制 27** 同族风险） |
| **机会** | 顺延**顺带验证**了"编号锚点"纪律在**跨组并发登记**下仍然成立 |
| **建议** | **顺延 29、不回填**（已执行） |
| **等待决策** | ⚠️ **已按推荐执行**；若你要回填，一句话可改（成本：1 行 ＋ 1 处引用） |

### 决策 2：`T-045`（报告版本号口径的 T 编号）—— **补** 还是 **不补**？

| 要素 | 内容 |
|---|---|
| **评估** | `F` 组要求"写入 `TEMPLATES.md` **或** `BASELINE.md`"、**未要求补 T 编号**；而 **`D.2` 明确要求补** ⇒ **指令有意区别** |
| **优势** | **不补**：忠于指令、不擅自扩编号｜**补**：口径规则**可被 `§7.2` 统一检索到** |
| **劣势** | **不补**：规则**落在 `§五十二`（轮次节）**，未来可能**搜不到**（**R69／R72 家族**）｜**补**：**又造 1 条 `T` 号**（且我"讨论 T-045"已**先造出一条假告警**） |
| **风险** | **不补**：**规则静默失效**（这正是本轮 F 组要治的病） |
| **机会** | **补**的话可**顺带消掉 `T-045` 那条元引用告警**（**先例＝机制 28**） |
| **建议** | ★ **补**（把口径从"轮次节"提升到"可检索的 `T` 表"）—— **但指令未要求，本轮不擅补** |
| **等待决策** | ✅ **等你**（一句话：补 `T-045` / 维持不补） |

---

## ⑪ 需要用户决策（**8 项**）

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | ★★ **机制 11 与机制 28 的触发口径冲突**（"按需调用" vs "所有动作"） | **三选一**：**(甲) 并入机制 11**（撤 28，`ENTRY.md` 作为机制 11 的入口）｜**(乙) 保留两条**但声明各自适用范围｜**(丙) 改机制 11 触发为"所有动作"**。★ **我推荐 (乙)**：机制 11 是**方法本体**、机制 28 是**入口纪律**，两者职责不同，**明写边界即可** |
| ② | **指针反向可达**（骨架 → 入口） | **补一行**（`universal_workflow.md` 定位节加"统一入口见 `ENTRY.md`"）；**需授权**（该文件不在 A 组范围） |
| ③ | **`T-045` 是否补记**（见 ⑩ 决策 2） | **补**（使版本号口径可检索；顺带消 1 条元引用告警） |
| ④ | **台账节标题是否统一格式**（`## N、` vs `## §N ·`） | **暂不统一**（**只登记**）：5 节格式差异**不影响人读**；★ 但**建议给"节号连续"加判据时**先明确**两种格式都认** |
| ⑤ | **历史指令是否补归档**（09-18 ～ 09-21，**断档 5 天**） | **不补全文、补索引**（在 `instructions/README` 或台账记"该期指令仅在日报/报告留痕"）—— 全文多已散落在各轮报告，**回填成本高、收益低** |
| ⑥ | **6 条过期任务是否删除**（现仅 PAUSED） | **删除**（一次性任务已过期 4–28 天，留着易误触发）；**须你点头** |
| ⑦ | **并发风险**：`21:00 清单`／`21:05 快照`／`21:07 批量调度`／`23:00 自动开工` 与"人工夜间指令"**可能撞车** | **加互斥**（如人工指令轮次开始前先暂停当日自动调度）；**机制 19 双通道未规定互斥** ⇒ **先议** |
| ⑧ | **`R90`／`B5` 重试路径仍未验** | **保持现状**（**等真实抖动来验**）；★ **不建议**为验重试而人为造失败（**会污染 CI 红绿语义**） |

---

## ⑫ 下一步建议

1. **先裁 ⑪①**（机制 11／28 口径）—— 它是**元规则级**，不裁则后续所有"是否必须走八步"的判断都悬空。
2. 次裁 **⑪② / ⑪⑥ / ⑪⑦**（都是**小改动、高收益**）。
3. **触发式**（无新触发则不跑）：`docs` 第八批｜`advisor_brief` 复扫｜汇编（**随 `docs/` 更新**）。
4. ★ **建议把本轮两个"方法级"教训写进 `TEMPLATES §十五` 同区**：**(a) 门控取值必须用"代入特征"区分运行期输出与 `run:` 回显**（本轮实证）；**(b) "讨论某编号"本身即自指**（第 5 例）。

---

## ⑬ 启动回执

已读 `CHARTER.md`（§三 机制表／§九）。
**当前状态**：`count = 315`（起点 · 实测）｜阶段二 · 2.4 边界层已验。
**本次任务**：八步入口文件 ＋ 8 项待裁（A–G 组）。
**是否继续**：执行。
**★ 本报告版本（依 F 组新规 ＝「首次提交时 `count`」）＝ `323`**（★ **提交后以 `git rev-list --count HEAD` 实测复核**；若不符则**按 F 组规则在订正处标注订正时 `count`，不改本行**）。

---

## 附加表 · INTEL 状态表（依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| **INTEL-001** 工信部 AI 中小企业创业支持计划 | **观察中** | 组合：企业注册完成 ＋ 阶段三启动 | **✗ 未触发**（当前阶段二 · 2.4） | 无 |
| **INTEL-002** GPT-6 安全风险 | **观察中** | 事件：L7–L10 落地后 | **✗ 未触发**（L7 设计完成／L8–L10 冻结） | 无 |

**J1–J4 自查**：J1 ✅ 本表在（**且置于十四项之后**）｜J2 ✅ **未触发即未改状态**（**不算违规**）｜J3 ✅ 无"待准备"项 ⇒ 无准备稿义务｜J4 ✅ 无"准备中"项。
★ **本轮 INTEL 机制的"身份变更"**：由**试用中** → **正式登记为机制 29**（见 §52.D）。
