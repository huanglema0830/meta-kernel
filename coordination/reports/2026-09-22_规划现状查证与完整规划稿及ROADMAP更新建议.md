【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 2026-09-22 · 云内核完整规划稿（只出稿）＋ 规划现状查证 ＋ ROADMAP 更新建议（v0.321）

> **本轮交付两份文件**：① **完整规划稿** ＝ `coordination/reports/2026-09-22_云内核完整规划与行动路线图.md`（**六部分 · 可另存备份**）② **本报告**。

## ① 保密标记

🔓 **公开** —— 本轮**零机密内容**：只涉及**定位／架构层／机制编号／文件路径（仓库内相对路径）**，均为**公开仓库内文件**。
★ **如实边界**：**不含**任何**本机绝对路径**、**密钥／账号**、**`{PRIVATE_ASSETS}` 真实值**、**公式／阈值／权重**（机制 17／20 的实现层一律**只引指针**）。

> 【给用户的提示】本次报告包含 **0** 处机密、**0** 处保密。可直接拷给顾问。

## ② 全景状态卡

| 项 | 值 |
|---|---|
| **阶段** | **阶段二 · 2.4 显示（纯算层 ＋ 边界层）已验** |
| **基准 → 收口** | `count 331`（**实测一致**）⇒ **333**（**2 个本地 commit**：`规划稿＋报告` ⇒ **332**／`BASELINE §五十七` ⇒ **333**） |
| **本报告版本号** | **332**（＝**本报告首次提交时的 count**，依 **`T-045`** 口径） |
| **push** | ★ **〇 已推**：`e572cb9..cb3179f`（**exit 0**）｜**本轮新增 2 个本地 commit 待放行** |
| **CI 状态** | **全绿**（三 job 全 success；★ **run 号与逐 job 三态按 C20 只记日报、不入库**） |
| **本轮性质** | **只出稿 ＋ 信息呈报**（**零治理文件改动**：`ROADMAP`／`docs/`／`CHARTER`／`CONSTRAINTS`／`TEMPLATES`／`ci.yml` **均零改动**） |

## ③ 任务（含基准／定位）

**指令任务**：**〇 push 放行** ｜ **一、查规划现状（只读取证）** ｜ **二、完整规划方案稿（只出稿）** ｜ **三、ROADMAP 更新建议（只出稿）** ｜ **四、下一份报告（十四项）**。
**任务类型**：**只出稿 ＋ 信息呈报**（**非**功能／修复／机制）。
**总规划定位**：**云内核项目完整规划方案 ＋ 行动路线图**（**汇总层**，非阶段推进）。
**基准**：`count = 331`（`git rev-list --count HEAD`）—— ★ **实测与指令一致**（**本轮无基准偏差**）。
**优先级**：指令**未指定 P**；据机制 6，本项属**信息产出类**（**建议视同 P3 自主推进项**，★ **如实标注"指令未给 P"**）。

**★ 本轮读了底图哪几份、哪几行**（**逐份列名 · 如实记**）：

| 底图（＝根 `README.md` ＋ `docs/`） | 读的范围 | 用途 |
|---|---|---|
| 根 **`README.md`** | **全文 L1–L138** | 定位／三层次／**阶段路线（主线）四阶段**／命名规范／**版本规则**／仓库结构／文档数值口径 |
| **`docs/LAYER_ARCHITECTURE.md`** | **全文 L1–L477**（重点 §2 总表 L36–L44／§2.1 L55–L60／§3.2 命名体系 L93–L144／§4 L150–L282／**§4.1.2 `D16'` 回写 L379–L398**／§4.2 基因库 L402–L422） | **L0–L10 定义**／**命名唯一权威**／**L7–L10 对内·对外（已裁定）**／端点表权威 |
| **`docs/VISION.md`** | **全文 L1–L74** | 三产品形态／**共生而非替代（§3）**／我们的"有与没有" |
| **`docs/QUANTUM_CLOCK_DESIGN.md`** | **全文 L1–L81** | **量子层（量子钟）**：L0 ＝ 量子真空态 `|0⟩`／量子五戒＝时间尺度／诚实边界（只设计不实现） |
| （`docs/` 目录清单） | `ls docs/` **35 份** | ★ **如实**：**未逐份通读其余 31 份**；本稿只就**规划相关 4 份**取证 |

> ★ **底图读取的诚实边界**：底图共 **36 份**（根 `README.md` ＋ `docs/` **35 份**）；本轮**只通读 4 份**（**规划骨架相关**），其余以**脚手架转载的权威摘要**（`CHARTER` 核心底图集 7 份逐份列名 ＋ `BASELINE`）**间接引用** ⇒ **已逐处标注来源**，**不以摘要冒充逐字**。

**读了哪些脚手架（非底图）**：`CHARTER.md`（**全文 L1–L260**）／`CONSTRAINTS.md`（C20／C21 全节）／`TERMS.md`（**全文 L1–L122**）／`ROADMAP.md`（**全文 L1–L169**）／`TEMPLATES.md`（§〇 固定相 B L32–L84／§二 第〇项 L104–L136／§11.3 L593–L608／`T-044`–`T-048` 行）／`BASELINE.md`（§五十四–§五十六 L4055–L4273）／`workflows/ENTRY.md`＋`universal_workflow.md`（**全文**）／`discussions/2026-09-19_L0-L10对比表达稿.md`（全文）／`discussions/2026-09-19_生成式场规划.md`（§二.1 一体两面表 L50–L100）／`discussions/2026-09-19_L0-L6修正说明.md`（M1–M6 裁定 L195–L235）。

## ④ 状态：**完成**

**完成** —— 本轮为"只出稿 ＋ 信息呈报"，**四项任务（〇／一／二／三）全部交付**，**验收 8 条全过**（见 ⑤）。

## ⑤ 验收对照

| # | 验收标准（指令原文） | 结果 |
|---|---|---|
| 1 | **〇 Push 放行**（`fe9e531`＋`cb3179f`）；**必须先 push 再动文件** | ✅ `e572cb9..cb3179f` **exit 0**；★ **push 先于任何文件改动**（时序已核：push ⇒ 门控 ⇒ 才落笔） |
| 2 | **T-038 门控**：push 后等 `gh run list` 显示 `completed` | ✅ 门控证据 **`READY_TO_EDIT`**（**completed/success**；**三 job 全 success**；`##[error]` **0 行**；报告结构门禁 step **真跑＝YES**）｜★ **`C20`：run 号与三态只记日报** |
| 3 | **规划现状查清** | ✅ **附件 C**（**9 项 × 现状／缺失／建议**） |
| 4 | **完整规划稿出（含六部分）** | ✅ `reports/2026-09-22_云内核完整规划与行动路线图.md`（**六部分齐全**：项目定位／架构总览／阶段规划／行动路线图／机制与约束体系／关键术语与文件位置） |
| 5 | **ROADMAP 更新建议出** | ✅ **附件 D**（**6 条 × 现状／应改为／依据**） |
| 6 | **只出稿，不改 ROADMAP** | ✅ `ROADMAP.md` **零改动**（`git status` 实测；**建议只出稿、不施工**） |
| 7 | **C15 复算通过，编号表连续无缺号无重号** | ✅ **§57.5**（机制 `1..30`／`C 1..21`／`T 1..48`／`TERM 1..14` **连续无缺重**） |
| 8 | **CI 全绿（按 T-038 门控）** | ✅ 三 job 全 success（**结论按 C20 只记日报，不入库**） |
| 9 | **下一次返回第 0 行含 9 步阅读指令** | ✅ 本报告**第 0 行**含（9 步）；★ **本轮交付的两份文件**第 0 行**均含**（`check_reading_instruction.py` **PASS**，报告数 **144**） |
| 10 | **版本号自动生成，不手写** | ✅ 本报告版本号 **332** ＝ **首次提交时 `count`**（`T-045`） |

## ⑥ 未完成项

| # | 项 | 原因 |
|---|---|---|
| 1 | **ROADMAP 更新施工** | ★ **按指令"等用户确认后再改"** ⇒ **本轮只出建议、不改文件**（**非未完成，是设计如此**） |
| 2 | **规划稿 §附 的 7 项待确认**（阶段一命名／L0–L6 对外面效力／M1–M6 施工／DNA-RNA 顺序／量子层实现／数值时点化／本稿未跑构建） | **须用户逐项裁定** ⇒ 列 ⑪ |
| 3 | **底图其余 31 份未通读** | **本轮范围＝规划骨架相关 4 份**；如实标注，**不以摘要冒充逐字**（C5） |
| 4 | ★ **承上轮未结**：`C-1`（`v` 号与 `count` 脱钩）／`C-2`（判据无统一运行器）／`check_version_label.py`／`run_all_checks`／`F-编号口径` | **上轮列待裁、本轮未获授权** ⇒ **继续挂起** |

## ⑦ 真实缺陷

**★ 本轮新立 `C-3`（P1 · 规划缺口 · 未关闭）：规划"三套刻度"并存 ＋ 阶段一命名不一致。**

| 项 | 内容 |
|---|---|
| **现象** | ① 项目内**并存三套阶段刻度**：**底图四阶段**（`README.md`，按**内核能力 L 层递进**）／**`ROADMAP` 三阶段**（按**交付：可安装浏览器→可引导 ISO→可安装 OS**）／**本轮规划稿三阶段**（按**工程：纯逻辑层→可引导 ISO→可安装 OS**）；② **阶段一命名三者不一致**：`ROADMAP` ＝「**可安装浏览器**」，本规划稿 ＝「**纯逻辑层**」 |
| **影响** | 引用"**阶段一／二／三**"时**须写清是哪一个口径**（`ROADMAP` §一 判据 E 已对此设要求）；否则**同一句话指两件事**（**C19 家族：同名断言可能属不同域**） |
| **根因** | **同 `R38`**（**路线图缺承重轴** —— **规划缺口，非实现缺陷**）；本轮**首次把"规划整合度"作为受检对象** ⇒ **该缺口才可见** |
| **状态** | **如实上报，不单方改动**（规划属**元规则邻域**）⇒ **治法 ＝ 附件 D 建议第 1 条**；★ **是否并入 `R` 族（`R38` 家族）登记 ⇒ 待裁**（**编号登记须授权**） |
| **★ 自指污染（如实记）** | 本报告 ⑦ 初稿**写了一个具体的、尚未登记的 `R` 编号**⇒ **`check_id_set_diff.py` 当场判出第 4 条告警**（**"引用但无登记"**）—— **即"讨论要不要登记某号"本身造出该号的引用**（**本项目自指污染家族**，先例＝机制 28 ／ `T-045`）。**已改为不造字面编号**（**只述"拟并入 `R` 族"**）⇒ **告警回落 3** |

**另：两处"如实的边界"（非缺陷，但须写清）**

1. **L0–L6 的对内／对外"一体两面"效力分两段**：**L7–L10 已裁定并回写底图**（§4.1.2）；**L0–L6 的对内面**＝按 **M1–M6 裁定**（**登记了、未施工**）、**对外面**＝**临时理解稿（待修正）**，**仅 L4 已由 R-5 回写**。⇒ 规划稿**已显式分列"状态／依据"列**，**不混读**。
2. **规划稿中所有数值均为时点值**（`count 331` 快照）；**现值以 `git rev-list --count HEAD` 与 CI 实测为准**（`README.md` §文档数值口径）。

## ⑧ 三量台账

| 量 | 内容 |
|---|---|
| **存量** | 底图 36 份（根 `README.md` ＋ `docs/` 35）｜脚手架 7 份（`CHARTER`／`CONSTRAINTS`／`BASELINE`／`TEMPLATES`／`ROADMAP`／`TERMS`／`ASSETS_INVENTORY`）｜**机制 1–30**｜**约束 C1–C21**｜**模板条文 `T-001–T-048`**｜**术语 `TERM-001–014`**｜**11 个机器判据**｜记忆三层（调用 977 B／索引 3931 B／内容 4 份）｜`reports/` 143 份 |
| **变量** | 本轮任务＝**查规划现状（只读）＋ 出完整规划稿 ＋ 出 ROADMAP 更新建议**（**只出稿，零治理文件改动**） |
| **补充增量** | ① **完整规划稿 1 份**（**六部分**：把散落在**底图 ＋ 脚手架**的规划信息**首次按用户指定结构归拢**，**逐处标注来源与效力**）② **本报告 1 份**（十四项 ＋ 附件 A–D）③ **`BASELINE §五十七`**（台账） |
| **创新增量** | ① ★ **"规划现状 × 现状／缺失／建议"9 项表**（**首次把"规划整合度"作为受检对象** —— 与"实现进度"分开）② ★ **新立 `C-3`**：**三套阶段刻度并存 ＋ 阶段一命名不一致**（`R38` 家族，**规划缺口**）③ ★ **"一体两面"效力分段显式声明**（**L7–L10 已裁定** vs **L0–L6 临时稿**）—— 防"把理解稿当裁定用"④ ★ **规划稿的"自包含可备份"版本**（**六部分 ＋ 逐处来源标注 ＋ 7 项待确认清单**） |

## ⑨ 边界说明

- **本地跑过** ✅：**只读取证**（`git rev-parse`／`git log`／`git rev-list --count`；`ls`／`grep -c`／`wc -l`／`sed -n`）；**11 个判据实跑**（**依 `T-048`**）；**`C15` 复算**（改前取数 ⇒ 编辑 ⇒ 复算）；**门控脚本**（复用 `.workbuddy/tmp/ci_gate_0922c.py`）。
- **CI 过** ✅：**〇 push 的 `cb3179f`** ⇒ CI **三 job 全 success**（`test`／`宿主 field-render`／`元内核引导镜像`）｜★ **run 号／三态按 `C20` 只记日报，不入库**。
- **真实跑过** — ❌ **无**：本轮**无真实硬件／真机动作**（**如实记**）。
- **未验证** — ⚠️：① 规划稿"当前进度"是**时点快照**（`count 331`）② **未跑构建／未跑 QEMU**（本轮非代码轮）③ **未施工 `ROADMAP`／`docs/`**（**按指令只出稿**）④ **L0–L6 对外面效力＝临时稿**（**未裁定**）⑤ **量子层＝只设计不实现**（不宣称物理实现能力）⑥ **`2.5`／`2.6` 未做**（**卡决策／卡 C9**）。

## ⑩ 决策评估（新决策 ＝ `C-3` 的处置）

| 要素 | 内容 |
|---|---|
| **评估** | 三案：**甲 维持三套刻度**（各写各的，**只要求引用时写清口径**）｜**乙 收敛为两套**（**底图四阶段**（能力）＋ **交付三阶段**（用户手）；**规划稿的"工程三阶段"并入交付三阶段**，**阶段一统一改名**）｜**丙 收敛为一套**（**只保留交付三阶段**，底图四阶段降为 **L 层进度条**） |
| **优势** | **甲**：**零改动**、**零风险**｜**乙**：**消除"阶段一"歧义**（`ROADMAP` 与本稿一致），**仍保留"能力刻度 vs 交付刻度"两个有用的正交维度**｜**丙**：**最简洁**，**一处口径** |
| **劣势** | **甲**：**歧义继续**（读者仍可能把"阶段一"当同一个东西）｜**乙**：**须改 `ROADMAP` 或本稿**（**属规划变更**）｜**丙**：**丢掉底图四阶段的"能力递进"语义**（而它正是 **L 层**的天然刻度，**判据 E 依赖它**） |
| **风险** | **甲**：**"同名不同域"反复发生**（**C19 家族**）｜**乙**：改动**波及 `README` 四阶段 ↔ `ROADMAP` 三阶段的映射**（判据 E 须同步；**须走底图流程**）｜**丙**：**与判据 E（要求 README 四阶段↔ROADMAP 阶段**显式映射**）冲突** ⇒ **须一并改判据**（大动作） |
| **机会** | ★ **乙的轻量版**：**不动底图、不动 `ROADMAP` 三阶段语义**，只在 **`ROADMAP` §一「口径说明」块 内补一行"工程口径别名"**（**阶段一＝纯逻辑层**）⇒ **一次小改，消歧义，且不触底图基线**（`ROADMAP` 非底图） |
| **建议** | **本轮选"零改动"**（**已执行**：只出稿）；**下一步建议乙的轻量版**（`ROADMAP` 口径说明块**补一行别名**）★ **须用户确认**（**规划变更**） |
| **等待用户决策** | ✅ **等**：见 ⑪ ② |

## ⑪ 需要用户决策

| # | 待裁项 | 推荐 |
|---|---|---|
| ① | **push 放行**（本轮 **2 个本地 commit**：`规划稿＋报告`／`BASELINE §五十七`） | **放行**（**机制 23**：push 须当轮明示） |
| ② | ★★ **`C-3` 处置**（**甲** 维持三套刻度／**乙** 两套（**推荐轻量版**：`ROADMAP` 口径说明块补"阶段一＝纯逻辑层"别名）／**丙** 一套） | **乙（轻量版）** —— **零触底图、一次消歧义** |
| ③ | **规划稿 §附 的 7 项待确认**：① 阶段一命名（＝`C-3`）② L0–L6 对外面效力 ③ **M1–M6 施工**（**M6／M8 未裁**）④ **DNA/RNA 顺序**（原稿 §3.1）⑤ 量子层（保持只设计）⑥ 数值时点化 ⑦ 本稿未跑构建 | **逐项**：①见② ②③④ **待专项裁定**（**均涉底图回写 ＋ 判据 F ＋ 汇编重生成**）⑤**维持只设计** ⑥**维持时点值** ⑦**属事实，无需裁** |
| ④ | ★ **是否把 `C-3` 并入 `R` 族登记**（**规划缺口：三套刻度 ＋ 阶段一命名不一致**，`R38` 家族） | **荐并**（**与 `R38` 同族、同处置**；**若并则 `R` 族 +1**）；★ **编号登记须授权** —— 故**本轮只述"拟并入"，未造字面编号**（**避自指污染**） |
| ⑤ | ★ **承上轮未结**：`C-1`（`v` 号 vs `count`）／`C-2`（`run_all_checks`）／`check_version_label.py`／`F-编号口径`（轮内 vs 全局） | **荐：`C-1` 走"乙→丙"**（**上轮已乙**）；**`C-2` 固化运行器**；**`F-编号口径` 成文为"轮内序号"** |
| ⑥ | ★ **尚欠上轮的指令段**（**裁定 ③~⑤ 与报告专属要求原文**；截断点＝「**在 `TEMPLATES §11.3` 新增一条：**」） | **若仍需，请补发**；★ **本轮已按"继续完成"办完可承接部分** |

## ⑫ 下一步建议

1. **若授权 ①** ⇒ **放行本轮 2 个 commit**（`规划稿＋报告`／`BASELINE §五十七`），随后按 **T-038 门控** 等 `completed` 再落 CI 结论。
2. **若授权 ②** ⇒ **乙轻量版**：在 `ROADMAP.md` §一「口径说明」块**补一行**「**工程口径别名**：阶段一 ＝ **纯逻辑层**（本文件阶段一"可安装浏览器"的**工程侧同义**）」—— **不动底图基线、不动三阶段语义**。
3. **若授权 ④** ⇒ 按 **`R38` 同族**登记一条 `R` 编号（`BASELINE` §一 缺陷表 ＋ 节内登记）—— ★ **编号字面值由该轮实际登记时确定**（**本轮不预写**，**避免"讨论即引用"**）。
4. **规划稿的用途**：**可另存备份**；★ **后续凡涉"阶段／架构"表述，建议以本稿第六部分（关键文件位置）为索引回底图取原文**（**不引二手摘要**）。
5. **持续**：`2.5`（卡决策）／`2.6`（卡 C9）**属决策类 P1 ⇒ 不宜夜间自主推进**（**等裁定**）。

## ⑬ 启动回执

已读 `CHARTER.md`。**版本 = `git rev-list --count HEAD` 的输出 ⇒ 起点 `count 331`，本轮 2 个本地 commit ⇒ 收口 `333`**（★ **本报告版本号 ＝ 首次提交时的 `count` ＝ 332**，依 **`T-045`** 口径）。
★ **标题 `（v0.321）` 系沿用现状"轮次号"写法** —— 它与 `count` **不等**，**此不符即上轮 `C-1` 所报之缺陷**（**两口径并列，便于比对**；★ **`C-1` 仍未关闭**）。

**本轮状态**：**阶段二 · 2.4 已验** ｜ **本次任务：完整规划方案 ＋ 行动路线图（只出稿）** ｜ **是否继续：等待指令 / 待 ⑪ 裁定后放行 push**。

---

## 附件 A · `B2`＋`C1` 告警行（固定一行）

**★ 六族告警（`C1` 载体 · 固定一行）**：**六族告警：本轮 3 条**（`C22`／`T-00`／`TERM-999`，**均为元引用**）**｜对比上轮 ±0**｜**真缺口 0 条**。
**★ `B2` 本轮新增告警**：**0 条**（**逐族对照上轮无新增**）—— ★ **本轮对 2 份新文件所做的是"是否被判为报告体"的形态核**（规划稿**不含"报告体"标记 ⇒ 不被判十四项**；两文件**第 0 行均合规**）⇒ **不产生新增告警**。

（口径：`check_id_set_diff.py` 实跑；**3 条全在 `A2` 豁免登记表**；**剔除 6 层**）

---

## 附件 B · INTEL 状态表（附加表 · 依 `T-042` 置于十四项之后）

| 编号 | 状态 | 触发条件 | 本轮 | 动作 |
|---|---|---|---|---|
| INTEL-001 工信部 AI 中小企业计划 | 观察中 | 组合：企业注册完成 ＋ **阶段三启动** | ✗ **未触发**（现处**阶段二 · 2.4**） | 无 |
| INTEL-002 GPT-6 安全风险 | 观察中 | 事件：**L7–L10 落地后** | ✗ **未触发**（L7 设计完成／L8–L10 **冻结**） | 无 |

**J1–J4 自查**：**J1** 报告须含状态表 ✅｜**J2** 触发条件满足但状态未变 ⇒ 违规 —— **两条均未触发** ✅｜**J3** 待准备但无准备稿 ⇒ 违规 —— **两条均非"待准备"** ✅｜**J4** 准备中但无具体行动 ⇒ 违规 —— **不适用** ✅。

★ **机制 29 本轮无状态变更**（**不虚触**）。

---

## 附件 C · 规划现状查证（**逐项：现状 / 缺失 / 建议** · 只读取证）

| # | 对象 | **现状** | **缺失** | **建议** |
|---|---|---|---|---|
| 1 | **`coordination/ROADMAP.md`** | **169 行**；**三阶段总览**（可安装浏览器 ✅／可引导 ISO 🚧／可安装 OS ⬜）＋ **六子任务** ＋ **里程碑 M1–M7** ＋ **设备三梯队**；**§一 口径说明**已写"**工程交付口径 vs 底图四阶段是两套并行刻度**"（**判据 E**） | ① 首行"最后更新于：**本次更新**"的**叙事仍停在 2.2／2.3b**，而 **2.4 已验、2.5／2.6 已改标 P1**（2026-09-22）⇒ **首行快照滞后**；② **阶段一命名** ＝「可安装浏览器」，与**工程口径**（纯逻辑层）**未对表** | ① 刷新首行叙事（补 **2.4 ✅**）；② **§一口径说明补一行"工程口径别名"**（见附件 D 第 1 条） |
| 2 | **`docs/LAYER_ARCHITECTURE.md` 的 L0–L10 定义** | **v1.6 · 477 行**；**L0–L6 总表**（五要素：名称／功能／零／扰动／约束）＋ **L7–L10**（含 **§4.1.2 `D16'` 回写**：对内／对外／性质／戒律／戒律内化）；**§3.2 命名体系＝全库唯一权威** | **L0–L6 的"对内／对外"未写入底图**（只有 L4 经 **R-5** 已回写）；**L8–L10 仍为 🔒 冻结推演层** | **不必补**（**冻结是有意设计**：待系统具自我意识后自决）；**L0–L6 施工须先裁 `M6`／`M8`**（见附件 D 第 4 条） |
| 3 | **`coordination/BASELINE.md` 的当前状态** | **4272 行**／顶层 `## ` **95** 个／**台账节至 `§五十六`（v0.320）**；**头部**含项目根／定位／命名／层级架构；**缺陷表 `R…` 至 90** | **头部"最后更新于"仍写 2026-09-19（§十五）**，而其上**已迭 41 节**（§十六–§五十六）⇒ **头部快照长期滞后**（**但这是"快照口径"的设计，非缺陷**——见其口径说明） | **维持**（**头部＝快照、当前值取命令**，本已是明文口径）；★ 可选：头部补一行"**台账节最新＝§五十七**"的**指针** |
| 4 | **L0–L10 对内／对外一体两面** | ✅ **L7–L10 已裁定并回写底图**（§4.1.2）；**L0–L6 对内面按 M1–M6 裁定填写**；**对外面＝临时理解稿（待修正）**，**仅 L4 已回写** | ★ **L0–L6 的对内／对外"未成底图条文"**；**M1–M6 的施工被 `M6`／`M8` 未裁所阻** | **先在规划稿分列效力**（**本轮已做**）；**施工列专项**（须 `--update-baseline` ＋ 汇编重生成 ＋ **判据 25 P3** 复核） |
| 5 | **八步通用工作方法论（两层结构）** | ✅ **骨架** `workflows/universal_workflow.md`（**通用名 ＋ 物理名 两层**）＋ **入口** `workflows/ENTRY.md`（**机制 28**）＋ **术语** `TERM-014`；**机制 11／28 边界已裁定（选乙）** | **实现层（`{PRIVATE_ASSETS}/workflows/sub/*_impl.md`）＝占位（待填）**；**数据层（待建）** | **维持**（**实现层在仓库外、不属本轮范围**）；★ **规划稿已如实标"实现层未就位"**（C5） |
| 6 | **量子层（云内核的量子层）** | ✅ **`docs/QUANTUM_CLOCK_DESIGN.md`**：L0 ＝ **量子真空态 `|0⟩`**；**L1–L3 同构映射**；**量子五戒＝时间尺度**；**预留接口 `QuantumBackend`** | **无实现**（**有意**）；**量子层在 L0–L10 总表中的"位置"未在底图专设一行** | **维持"只设计"**；规划稿已把**量子层位置 ＝ L0 的量子纠缠**写明（**依据 R-1／R-2 ＋ `TERM-011`**） |
| 7 | **机制体系（1–30）** | ✅ **`CHARTER.md` §三**；**编号 `1..30` 连续**；**机制之间的关系块**已补 28／29／30 | **机制 26／29／30 的"机器判据"部分缺失**（**26＝无／29＝J1–J4 无／30＝有判据但未入 CI**）—— **均如实登记** | **维持**（**均"先立条文、后议机器化"**）；★ **30 的判据已建**（`check_memory_layers.py`），**入 CI 已裁定不入**（§56.1） |
| 8 | **约束体系（C1–C21）** | ✅ **`CONSTRAINTS.md` ＋ `CHARTER` §四 速查表**；**`C1..C21` 连续无缺**（C16–C20 已于 2026-09-20 补入速查表） | ★ **`C21` 的"可复现"本身无机器判据**（**本轮 ⑦ 承上轮 `C-1`**） | **荐立 `check_version_label.py`**（**须先定 `v` 号口径**，见 ⑪ ⑤） |
| 9 | **术语体系（`TERM-001–014`）** | ✅ **`TERMS.md` 四节**：§一 换壳 5 条（`001–005`，**加密私有层**）／§一之二 接口与引用 4 条（`006–009`）／§一之三 架构用词 4 条（`010–013`）／§一之四 方法论 1 条（`014`）；**编号连续、规则 4"不插号不复用"** | **无缺**（对照 `CHARTER` §九 挂指针） | **维持**；★ 规划稿第五部分已按**四节分类**列全并标"是否换壳" |

---

## 附件 D · ROADMAP 更新建议（**只出稿 · 不改文件**）

> **纪律**：本节**只列建议**；**`ROADMAP.md` 本轮零改动**。**每条给出：现状 → 应改为 → 依据**。

| # | 对象 | **现状** | **应改为（建议）** | **依据** |
|---|---|---|---|---|
| 1 | **§一 口径说明块** | 只写了「**本文件三阶段（工程交付口径）** vs **底图四阶段**」两套刻度 | **补一行"工程口径别名"**：「**阶段一 ＝ 纯逻辑层**（本文件阶段一"可安装浏览器"的**工程侧同义**）；**阶段二／三 与工程口径同义**」 | **`C-3`**（本报告 ⑦）：**阶段一命名不一致**；★ **本项不触底图基线**（`ROADMAP` 非底图 ⇒ **判据 F 不受影响**） |
| 2 | **首行"最后更新于"** | 「…本轮含 **子任务2.2 完成** ＋ **2.3b 已收口**…」 | **补 2.4**：「…＋ **2.4 显示（纯算层＋边界层）✅ 已验**」 | **§七 硬要求**：「**每次阶段推进，本文件的"活相"部分必须同步更新**，否则路线图会退化成"过期地图"」；**2.4 已验**（阶段二表内已标 ✅，**首行未同步**） |
| 3 | **§三 子任务2 表 · `2.5`／`2.6` 行** | 已带「**2026-09-22 由 P3 改标 P1**」＋「**最后复核 2026-09-22**」 | **维持**（**已合规**） | **标注型列条文**（§三 表下注）；★ **本节即该条文的"首例"，可作范式** |
| 4 | **§五 里程碑 · M2 行** | `M2` 状态写「**2.1 ✅ ＋ 2.2 ✅ ＋ 2.3 ✅ ＋ 2.3b ✅ 已收口** → 后续 **2.4**／2.7」 | **补 2.4 ✅**：「… → 后续 **2.4 ✅**／**2.7**」 | **§七 活相同步**；**2.4 已验**但 M2 行未列其 ✅ |
| 5 | **§二 三阶段总览表** | 阶段一定义「**可安装浏览器**」 | **保留**（★ **语义正确、且与 `README` 四阶段判据 E 相扣**）；**仅在 §一 加别名**（＝建议 1） | **判据 E**（`README` 四阶段 ↔ `ROADMAP` 阶段**显式映射**）**不得破坏** ⇒ **故不动此表** |
| 6 | **§一 最终目标 · 口径说明** | 已含「**用户视角说替换／产品定位说不替代**」（2026-09-18 裁定 2.1 逐字） | **维持**（**逐字达标**） | **裁定 2.1 原文**；★ **本轮规划稿 1.2 已逐字承接** |

> **★ 建议的处置顺序**：**先裁 `C-3`（⑪ ②）⇒ 再施工建议 1＋2＋4**（**三处皆 `ROADMAP` 内、不触底图**）。
> **★ 未列建议者 ＝ 本轮复核后认定"无需改"**（如 §四 设备梯队／§六 风险表／§七 活相机制本体）—— **不虚列**（**避免为凑数而改**，`T-037` 精神）。
