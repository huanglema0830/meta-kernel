【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# `docs/` 时效核对稿 · **第六批**（全库系统复扫 ＋ **内部一致性交叉核** · 2026-09-21 · v0.275）

> **性质**：**只出稿，不改文件**（用户 §四.1「只出稿，不改文件」）。
> **保密标记**（C12）：🔓 公开。⛔ 机密 **0**、🔐 保密 **0**（只用仓库相对路径）。
> **边界**：**未运行渲染/内核程序**；"已实现/已接线"判定＝**只读符号检索**（项目范式：**函数存在 ≠ 被调用**）。

---

## 0. 本批口径（**方法升级**）

| 批次 | 覆盖 | 方法 |
|---|---|---|
| 第一/二/三批 | 35 份 **全覆盖**（8＋9＋并集 15 → 剩余 20） | 「声称 X ⇒ 对照代码落点 Y」 |
| 第四/五批 | 定向深核 | 同族未授权项收口 |
| **第六批（本批）** | **全 35 份系统复扫** | 新增**内部一致性交叉核** —— 不仅「声称 vs 代码」，还查**同文件 / 跨文件口径自相矛盾**（**C19**） |

- 扫描口径：对 `docs/*.md` 全 35 份，抓「未实现/尚未/未做/未接线/⏳/🟡」等**声称行**，**排除已带历史留痕（订正/原判/原文/加锚/时点）的行** ⇒ 得 **88 行候选**，逐项交叉核。
- **本批新发现 8 处**（其中 **3 处为"同文件自相矛盾"**，是本批方法升级后**才浮现**的），另 **2 处维持原判定**（正当未做）。

---

## 1. ★ 本批新发现（**8 处**，均含实证）

### F-1 `docs/GENE_LIBRARY_DESIGN.md` **§2 落点 ↔ §8 边界 自相矛盾（3 处）** —— C19

同一文件内，**§2 各层"落点（现状）"** 与 **§8「边界与现状」表** 对同一事实给出**相反口径**：

| # | §2 行（旧口径） | §8 行（v1.2 更新口径） | 实证 |
|---|---|---|---|
| **1** | **L65**：第 1 层「**当前为常量实现，尚未做到"从基因库读取"**」 | **L244**：第 1 层「✅ **已实现 + 已接线**｜L4 经 `threshold::from_library` 读取」 | `threshold::from_library` **有真实调用点**（`l4_gate.rs:66` ＋ `host/field-render/src/main.rs:1373`）⇒ **§8 为真** |
| **2** | **L91**：第 3 层「当前运算关系写在内核代码里，**尚未作为"公式条目"入库**」 | **L246**：第 3 层「✅ **已实现**｜`GeneLibrary::relation` + `add_relation`（尚未被上层读取）」＋ **L255**「已**入库**但尚未被上层读取」 | `gene_library.rs` 有 `add_relation`（L400）／`relation_formula`（L459）；**全仓无外部调用点** ⇒ **§8 为真**（"已入库、未被读取"） |
| **3** | **L102**：第 4 层「**尚未使用哈希链**（当前为普通记录）」 | **L247**：第 4 层「✅ **已实现**｜篡改可检出；持久化后仍可校验（`verify_chain`）」 | `gene_library.rs` 有 `ChainLink{ prev_hash, … }`（L253）／`chain: Vec<ChainLink>`（L310）／持久化 `chain` 行（L525–594）⇒ **§8 为真** |

⇒ **判定**：**§2 三处为过期口径**（§8 系 v1.2 更新，权威）。**建议：给 §2 三处加锚**（「★ 2026-09-21 加锚：本层口径已由 §8 更新为…」），**原句不改**（C19）。**改底图须授权**。

### F-2 `docs/L7_EXECUTION_DESIGN.md` **§0 L40 ↔ §0.1 自相矛盾** —— C19

| 行 | 内容 |
|---|---|
| **§0 L40** | `\| **现状** \| 🟡 **已定义、未实现**（本文件为设计…） \|` |
| **§0.1 L13–22** | `\| **多内核互联** … \| ✅ **已实现**（9 项测试）`｜`动作分级 T0–T3 … ✅ 已实现（11 项测试）`｜`动作账（哈希链）… ✅ 已实现（6 项测试）`｜`修复建议 … ✅ 已实现（8 项测试）`｜`宿主侧执行器 … ✅ 已实现（9 项测试）`｜`修复执行机制 … ✅ **已实现（v0.109）**` |

⇒ 同一文件 **"未实现"与"已实现"并存**；且与 `LAYER_ARCHITECTURE §2.1`（2026-09-20 订正为「判定与记账（T1 闭环）已实现、执行侧待确认」）**口径相反**。**建议：给 L40 加锚**（不改原句）。**改底图须授权**。

### F-3 `docs/LAYER_BASEMAP_L0_L6.md` **L346**「L7 未实现」⇒ 过期

原文：`⇒ 但 L7 之后 L3 由谁驱动、tick 从哪来，底图未写。**当前不影响实现（L7 未实现）**，但属已识别未决项。`
⇒ 依据 F-2：L7 **T1 闭环已实现（v0.109）** ⇒ **括号内前提过期**。建议加锚（不改原句）。

### F-4 `docs/LAYER_BASEMAP_L0_L6.md` **L668 / L685**「未做：汇编重做」⇒ 过期

原文（v0.1 / v0.2 版本记录）：`- **未做**：汇编重做（指令 2.3，另见后续）…` ／ `- **仍未做**：汇编重做（裁定 2.3）…`
⇒ 实测：**汇编已重做** —— `coordination/discussions/` 下 **四批正文 ＋ 合订本**齐备，且**本轮（09-21 04:40）刚重生成**。⇒ **该"未做"已过期**。建议加锚。

### F-5 `docs/WORK_CONSOLE_PLAN.md` **L94**「L7 执行层 ⏳ 下一阶段」⇒ 过期

原文：`\| **二 · L7 执行层** \| ⏳ 下一阶段（发起人阶段二） \| 多内核互联（发现→握手→交换）；修复执行（…→哈希链）；T0–T3；单测 \| … \|`
⇒ 实测：上列各项**均已在 `meta-kernel-core/src/l7/` ＋ `npb-gateway/src/l7_exec.rs` 落地并测试**（见 F-2）。⇒ **"⏳ 下一阶段"过期**。建议加锚。

### F-6 `docs/FIELD_PRESENTATION_DESIGN.md` **L57**「多内核互联 ⏳ 待接」⇒ **能力侧过期**

原文（§2 对齐表）：`\| **多内核互联** \| 用于**场域同步**（局域网内多台互相提供本底参考） \| ⏳ **待接** \|`
⇒ 实测：`meta-kernel-core/src/l7/mesh.rs`（**发现→握手→交换**，9 项测试）**已实现** ⇒ **"互联能力"侧过期**；**但"用于场域同步"这一接线**是否完成**未核**。⇒ 建议**加锚（区分"能力已实现／接线待核"）**。
> 同表 **L55（痕迹/习气 ⏳ 待接）／L56（摩尼宝珠 ⏳ 待接）** 与 L58（T1 闭环 ✅）**与代码一致**，**维持原判定**。

### F-7 `docs/FIELD_RENDER_SOURCELESS_DESIGN.md` **L82**「尚未接入 wgpu」⇒ **后半句过期，前半句待核**

原文（§2.3 WGSL 设计资产）：`- 两文件为**设计骨架**（**尚未接入 wgpu**；**第二阶段实现时编译验证**）`
⇒ 实测：**第二阶段（呈现器）＋第三阶段（宿主）已完成**（`host/field-render` wgpu 独立管线 ＋ CI 验收）⇒ **"第二阶段实现时编译验证"已过期**。
⇒ 但 `host/sky-browser/shaders/{preprocess,render}.wgsl` **两文件本身是否已接入** `host/field-render`（该 crate 现仅有 `shaders/sort.wgsl`）⇒ **待核**，**不擅判"已接"**。建议**加锚 ＋ 列待核项**。

### F-8 `docs/SELF_DIAGNOSIS_REPORT.md` **L300/L308/L341/L358**「执行器未实现」⇒ **部分被后记覆盖**

- 文件头 **L7 有后记（2026-09-20）**，但后记**只指向 §1.1 表 L7 行**；而 **L341（"行动（执行器）…缺"）／L358（"执行器未实现"）** 位于他节，**未被后记覆盖**。
⇒ 建议**补锚或扩后记范围**（不改原报告结论，C19）。**本报告为 2026-09-14 时点快照**，性质同 `SELF_DIAGNOSIS`，故**优先级低于 F-1～F-5**。

---

## 2. 维持原判定（**正当未做**，非过期）

| 项 | 位置 | 依据 |
|---|---|---|
| **L6「对齐」❌ 未实现**（`WorldAdapter` 缺） | `LAYER_ARCHITECTURE` L309/L411/L415｜`LAYER_BASEMAP` L48/L536/L565｜`SILA_IMPLEMENTATION` L90/L91 | 全仓无 `WorldAdapter` 实现 ⇒ **与底图"L6-V2 对齐未实现"洞一致**（已登记） |
| **性能「待老笔记本实测」⏳** | `PERFORMANCE_REPORT` L16/L17/L118 | **需真机**（`M4` 里程碑，非本机可验） |

## 3. 待核（**不擅判**）

- **H-1**：`docs/HOST_UI_DECISION.md` **L4**「本轮**未交付可运行的窗口宿主**」／**§6.4 L121**／**§8.5 L190**「仍未做」 —— 与 `ci.yml` field-render job 的「**真实开窗 ＋ URL ＋ 上屏回读**」硬断言**张力**；但该文档为 **v1.3（2026-09-15）历史轮次**，可能**指当时轮次**⇒ **须先核"是否已交付窗口宿主"**，再定是否加锚。
- **H-2**：F-7 的 `preprocess.wgsl` / `render.wgsl` **接入状态**（见 F-7）。

---

## 4. 边界说明（四类）

- **本地跑过**：✅ 全 35 份 `docs/` 只读扫描（88 行候选）＋ 符号检索（`grep` 读 `gene_library.rs`／`l7/`／`ci.yml`／`coordination/discussions/`）。
- **CI 过**：⛔ 不适用（本批无代码/底图改动）；本轮 §〇 推送的 CI 见十四项主报告。
- **真实跑过**：⛔ 未运行任何渲染/内核程序（本批为**只读核对**）。
- **未验证**：⚠️ F-7 前半句、H-1、H-2 ⇒ **待核**，已在正文标注。

## 5. 三量台账（存量 / 变量 / 补充增量 / 创新增量）

| 量 | 本批内容 |
|---|---|
| **存量** | `docs/` 35 份既有陈述；`meta-kernel-core/src/l7/`、`gene_library.rs`、`host/field-render`、`coordination/discussions/` 既有落点 |
| **变量** | **0**（只出稿，未改任何文件） |
| **补充增量** | 8 条新发现（F-1…F-8）＋ 2 条待核（H-1/H-2）＋ 2 条维持原判定 |
| **创新增量** | **1 条方法升级**：新增「**内部一致性交叉核（C19）**」扫描维度 —— 同文件/跨文件口径矛盾（本批据此发现 F-1/F-2 等 **3 处**，为前五批**未覆盖**的盲区） |

## 6. 本轮读了底图哪几份、哪几行

| 底图 | 行 | 用途 |
|---|---|---|
| `docs/GENE_LIBRARY_DESIGN.md` | L60–105（§2 各层落点）、L203–258（§8 边界表） | F-1 |
| `docs/L7_EXECUTION_DESIGN.md` | L1–22（头部＋§0.1）、L30–45（§0 现状） | F-2、F-5 |
| `docs/LAYER_BASEMAP_L0_L6.md` | L344–348、L664–686 | F-3、F-4 |
| `docs/WORK_CONSOLE_PLAN.md` | L88–98（§5 后续阶段） | F-5 |
| `docs/FIELD_PRESENTATION_DESIGN.md` | L48–62（§2 对齐表） | F-6 |
| `docs/FIELD_RENDER_SOURCELESS_DESIGN.md` | L74–97（§2.3＋§3 表） | F-7 |
| `docs/SELF_DIAGNOSIS_REPORT.md` | L1–10（头部后记）、L296–360 | F-8 |
| `docs/{LAYER_ARCHITECTURE,LAYER_BASEMAP_L0_L6,SILA_IMPLEMENTATION}.md` | L309/L411/L415／L48/L536/L565／L90/L91 | 维持原判定 |
| `docs/HOST_UI_DECISION.md` | L1–14、L65–75、L118–125、L188–192 | H-1（待核） |
| `coordination/TEMPLATES.md` | L102–119 | 本稿第 0 行逐字对齐 |
