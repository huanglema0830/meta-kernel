【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

# 4 项裁定落地（接续包后记 ＋ 两项"维持" ＋ ci.yml 重试）＋ advisor_brief 时效核对 · 报告

> **本轮性质**：**裁定落地 ＋ 推进**（用户 2026-09-20 指令）。**零内核代码改动**。
> **一句话**：接续包**加后记不改写**；`REFERENCE_APP_DESIGN` 与仓库外快照**维持现状（只登记）**；`ci.yml` 下载步**加 shell 循环重试**（本地同源模拟 3/3 PASS）；`advisor_brief` **只出时效核对稿**（7 处活跃段待订正）。

---

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。（本轮改动文件泄漏扫描：用户名 **0**、新增行绝对路径 **0**。）

**② 全景状态卡**（★ 数字为**生成时快照**；提交／推送会推高版本号 ⇒ **现值以复核命令为准**，R82）

| 项 | 值 |
|---|---|
| 项目 | 云内核项目（元内核系统）｜仓库可见性 **PUBLIC**（机制 17 骨架-实现分离） |
| 阶段 | 阶段二 · 可引导 ISO ｜ **2.3b ✅ 已收口（全源）** ｜ **2.4 纯算层 ＋ 边界层 ✅ 已落地**（⑫ 段已机器化） |
| **基准 count** | **249**（起点）⇒ 提交 **250** ⇒ **R89 补登 251** ⇒ label **v0.251**（`label = count`，C21） |
| 编号 | 机制 **1–27**｜C **1–21**｜T **1–36**｜D **1–44**（§九）｜R **1–52**（**本轮新增 R88 ＋ R89**） |
| 待裁 | **D-7 暂缓**（身份档案）；**本轮新增 3 项**（见 ⑪） |
| 复核 | `git rev-list --count HEAD`｜`git status --porcelain`｜`gh run list --limit 3` |

**③ 任务**：基准 **count = 249**｜总规划定位：**4 项裁定落地 ＋ advisor_brief 时效核对**｜类型：**裁定落地 ＋ 推进**｜优先级 **P1**。

**④ 状态：完成**（含 **3 处诚实边界**：① **零内核代码改动** ⇒ 无 QEMU／裸机侧新结论；② **CI 结论不进本报告**（C20）⇒ 见 ⑬ 推送后核对；③ **`ci.yml` 重试只在本机做了「控制结构 ＋ 常量」的同源模拟**，**真实网络下的行为由 CI 验证**）。

**⑤ 验收对照**（用户 8 条验收标准，逐条实测）

| # | 标准 | 实测 |
|---|---|---|
| 1 | 接续包加后记，逐字对照 | ✅ **＋38 ／ −0**（**纯追加**、删除 0）；SHA 前 `be04b21f…` → 后 `1afac43d…`；行数 **223 → 261** |
| 2 | `REFERENCE_APP_DESIGN` 维持现状，记 BASELINE | ✅ `§26.2`（汇编收 **34 份**、**缺 1 份**、**不含根 README**；缺口已在合订本 §五写明） |
| 3 | 仓库外快照维持，记 BASELINE | ✅ `§26.3`（**不重建、不删除**；**本轮未触碰该文件**；按指令**不记录内容**） |
| 4 | `ci.yml` 加重试，逐字对照 | ✅ **＋16 ／ −1**；逐字 diff 见 `§26.4`；**本地同源模拟 3/3 PASS**；`bash -n` OK；`pyyaml` 解析 OK；**CI 见 ⑬** |
| 5 | advisor_brief 时效核对稿出 | ✅ `coordination/reports/2026-09-20_advisor_brief时效核对稿.md`（**109 行**；**只出稿、未改文件**） |
| 6 | C15 复算，编号表连续 | ✅ 机制 **27→27**｜T **36→36**｜D **44→44**｜**R 50→52（＋2：R88 ＋ R89）**｜C **21→21**｜§四表 **21→21**；三表**连续无缺重无缺号**；对照 **6/6 PASS** |
| 7 | CI 全绿 | ✅（**按 C20 结论不入仓库** ⇒ 复核命令 `gh run list --limit 3`；**run 号／三 job 三态／用时见当日日志与对话返回**） |
| 8 | 下一次返回第 0 行含 9 步阅读指令 | ✅ 本条＋本报告第 0 行 ＝ `TEMPLATES.md` 行 104–118 **逐字**，回读自检 **PASS** |

**⑥ 未完成项**：**无**（用户派定的 5 项全部落地）。**唯一留给外部的是 CI 结论**（第 7 条验收），按 C20 在推送后取证。

**⑦ 真实缺陷（项目代码侧 0 条）＋ 2 条风险登记（R88／R89）＋ 1 条过程观察**

- **项目代码侧 0 条新缺陷**。**新增 2 条风险登记（均已同轮处置／登记）**：
  - **R88**（外部下载步缺重试 ⇒ 单次网络抖动即判红；**curl 自带重试开关不覆盖 TLS 类错误** ⇒ 加错 = 空转）——**已在同轮处置**（改 shell 循环重试）。
  - **R89**（**「声明」与「发生」不可区分 ⇒ 判据假阳性**：`gh run view --log` **回显 run 脚本**，用脚本文本 grep 日志会把**脚本行**当成**运行证据**）——**本轮亲历并纠正**（**本机 CI 监视脚本**误判"重试被触发"，逐行回看时间戳＋ANSI 色码后定为**脚本回声**）；**已登记 `BASELINE §26.9` ＋ 给出机器可判据**。
- **过程观察（如实记录，非缺陷）**：并行跑「判据全量回归」与 `git status` 时，曾看到未跟踪文件 `meta-kernel-core/src/__purity_selftest.rs` —— 查证为 `check_kernel_purity.py` 自检的**瞬时探针**（该脚本有 `finally: os.remove`）；**现已消失、从未入库**。
  ⇒ **教训**：**判据运行中不要并行取 `git status`**（会抓到中间态，容易被误读成"污染"）。

**⑧ 三量台账（四量）**

| 量 | 内容 |
|---|---|
| **存量** | `BASELINE.md` §二十六（26.1–**26.9**）｜`R88` 行｜**`R89` 行（§26.9）**｜接续包 后记（**D-1…D-9／O-1…O-3 状态表**）｜`advisor_brief` 时效核对稿（**7 处活跃段 ＋ 1 处同源镜像 ＋ 1 处待核**）｜`ci.yml` 重试块＋同源模拟脚本 |
| **变量** | **R 计数 50 → 52**（**＋2：R88 ＋ R89**）｜接续包 **223 → 261 行**｜`ci.yml` **＋16／−1**｜`BASELINE.md` **＋131／−0 ＋ §26.9（＋16）** |
| **补充增量** | ① **接续包后记**（把「待裁 D-1…D-9」的**实际归宿**一次性讲清，保留快照不改写）② **ci.yml 下载韧性**（+ 对照组证明"**重试真的改变了行为**"= 非空转）③ **advisor_brief 时效核对稿**（含**同源镜像**辨析：D41 应改台账而非改镜像） |
| **创新增量** | **无新建制**（本轮**未新增机制／C／T**；R88 属**风险登记**，非新能力） |

**⑨ 边界说明（四类 · 与机制 26 四层对齐）**

| 类 | 内容 |
|---|---|
| **本地跑过** ✅ | 14 项判据全绿（含机制 18 补第 0 行后 **PASS**、自检 5/5）｜`C15` 改前／改后同源取数｜`bash -n`｜`pyyaml` 解析｜**重试逻辑同源模拟（3 用例 × 阳性/阴性/对照）**｜泄漏扫描 |
| **CI 过** ✅ | **已达成**（**按 C20 结论不入仓库**：仓库侧**只留复核命令** `gh run list --limit 3`／`gh run view <id> --json status,conclusion,jobs`；**run 号／三 job 三态／用时见当日日志与对话返回**） |
| **真实跑过** ❌ **不适用** | **本轮零内核代码改动** ⇒ **不构成任何内核／boot 侧新结论**（**不反推**） |
| **未验证** ⚠️ | ① **`ci.yml` 重试分支（第 2–4 次调用／`sleep 5`／`::error::` 退出）本轮未被真实网络走到** —— CI 实跑**首试即成功、重试未被触发**（逐行核对时间戳＋色码确认），故该分支只有**本机同源模拟**支撑；② `advisor_brief` L43「平台定时任务 ACTIVE」（**平台侧配置，仓库内无法观测 ⇒ 不反推**） |

**⑩ 决策评估（七要素）** —— 本轮**无新建制决策**（4 项裁定均由用户下达）。⑪ 的 3 项为**已有分支的取舍**，按「决策呈现规范」摆全要素：

- **【A】`advisor_brief.md` 是否按核对稿订正？**
  **评估**：核对稿列 **7 处活跃段**过期（3 处实质性 + 3 处自相矛盾 + 1 处低严重度）。**优势（确定的）**：改后顾问侧恢复读到**正确阶段与正确待决状态**。**劣势（确定的）**：需一轮改动（4 文件内 7 处）。**风险**：**误改历史段**会变"篡改留痕"（须严守 C19）。**机会（可能的、可证伪）**：若**同步改台账 §九 D41**，可**一并消除 R87 家族的一个实例**——**证伪方式**：改后台账内 D41 状态与片6 实际（`BTreeMap` 已用）**是否一致**。**建议**：**按稿改**，但**只改活跃段**，历史段（§二 下半 ＋ §三）**只加"当时快照"标注、不改数**。**等待你决策。**
- **【B】`CONTINUITY.md`（**仓库外**）是否同口径更新？**
  **评估**：该文件 v5.0 标「O-3 同步」，但**同样停在「待裁项 ＝ `D-1…D-9`」旧框**（与接续包后记冲突）。**优势**：三处（接续包／advisor_brief／CONTINUITY）口径一致。**劣势**：涉**仓库外**文件，超出仓库边界。**风险**：不同步 ⇒ 顾问若从 CONTINUITY 恢复，**仍会读到旧框**。**机会**：可把「恢复入口」的话术统一（**可证伪**：改后 `grep -c 'D-1…D-9'` 在三个文件中**同时归零或同时带后记**）。**建议**：**同步**（加同款后记块）。**等待你决策。**
- **【C】R89 的对策（剔除回声行／改用运行期证据）是否**固化为 CI／工具断言**？**
  **评估**：R89 的陷阱（`--log` 回显脚本）出现在**本机的 CI 监视脚本**里，**不在仓库内的 CI 断言里**（本轮仓库断言未新增此类 grep）。**优势（确定的）**：若把「剔除回声行」做成**通用取数函数／断言前置**，同类误读**无法再次发生**。**劣势（确定的）**：本轮**无仓库内实例** ⇒ 固化 ＝ **为潜在问题新建制**（与「非必要不新建制」有张力）。**风险**：过早抽象 ⇒ 若它将来只用于本机脚本，则**固化在仓库里是错位**。**机会（可能的、可证伪）**：若下轮再出现「用 `--log` 做断言」的需求，**证伪方式**＝看是否**再次踩同一坑**。**建议**：**本轮只登记（`§26.9`）＋先不固化**；若你倾向固化，我下轮把它做成 `coordination/tools/` 下的**日志取数小函数**（只读 log、内置剔除回声行）。**等待你决策。**

**⑪ 需要你决策（3 项）**：**A** `advisor_brief` 是否按核对稿改（**推荐：按稿改，只动活跃段**）｜**B** `CONTINUITY.md`（仓库外）是否同口径加后记（**推荐：同步**）｜**C** R89 对策是否**固化**为 CI／工具断言（**推荐：本轮只登记、先不固化**）。**另**：**⑨「未验证①」**（`ci.yml` 重试分支的真实网络行为）是否**下一轮实测一回**（等一次真实偶发，或人为打断网络试一次）——请你一句话。

**⑫ 下一步建议**（按优先级）：**P1** 你裁 A／B 后落 `advisor_brief`（＋ 台账 §九 D41）｜**P2** `CONTINUITY.md` 同口径｜**P3** 若你裁 **C**，落 `coordination/tools/` 日志取数小函数｜**P3** 场源扩屏（**选 B，仍暂缓**）｜**P3** D-7 身份档案（**暂缓**）。

**⑬ 启动回执**：已读 `CHARTER.md`。版本 = `git rev-list --count HEAD`（**不写死**，C2；本轮起点 count = **249**，收尾 **251**）。本次任务：**4 项裁定落地 ＋ advisor_brief 时效核对**。**⚠️ CI 结论按 C20 只记日报／对话返回，不入仓库** ⇒ 见下方「推送后核对」（**仓库侧只写复核命令**；**run 号与三 job 结论记当日日志**）。

**⑭ 本轮读了底图哪几份、哪几行**（D55／D52：报告须写明；**行号均当轮实测**，R75）

- **底图 ＝ 根 `README.md`（132 行）＋ `docs/*.md`（35 份）**。**本轮定向检索命中**：
  - `README.md` **行 90**（「**版本号 = git 提交计数**：`git rev-list --count HEAD` 即当前版本号」）＋ **行 93**（「自本规则起一律以提交计数为准」）—— **用于佐证**接续包后记的**版本口径订正**（R84／C21）。
  - `docs/HOST_BUILD.md` **行 87**（「共 **11 个验收模式**」）—— **用于核对** `advisor_brief.md` L259 的「11 个验收模式」（**结论：一致，无需改**）。
  - `docs/*.md` **「版本号」命中 4 处**（`L4_VALIDATION_REPORT.md:30`／`L5_VALIDATION_REPORT.md:86`／`LAYER_BASEMAP_L0_L6.md:334`／`SELF_DIAGNOSIS_REPORT.md:114`）—— **均为历史报告内的历史值**（不追改）。
  - `docs/*.md` ＋ `README.md` **「灵境引擎／Vekua／PFNN」＝ 0 命中** ⇒ 印证 **D-4／D-5／D-6 的引用位置在 `coordination/discussions/` 两稿，非底图**。
- **本轮读取（非底图，供复现）**：`coordination/{BASELINE,CHARTER,CONSTRAINTS,TEMPLATES,advisor_brief}.md` 相关段｜`coordination/discussions/2026-09-19_{接续包,场方程求解器设计稿,帧缓冲抽象层设计稿}.md`｜`.github/workflows/ci.yml`（`host-windows` job 全段）｜`coordination/tools/check_*.py`＋`c15.py`｜`meta-kernel-boot/kernel/src/{main,present}.rs`（核 ⑫ 段行号 121–125／126–128／129–132／133–134）｜`docs/HOST_BUILD.md`。
- **定点结论（供顾问复核）**：**`advisor_brief` §三.3 的 ⑫ 段行号（121–125／126–128／129–132／133–134）与仓库现状一致** ⇒ 该处**无需改**（此前怀疑为过期，**已现场否证**）。

---

## 推送后核对（C20：**结论不入仓库**）

> **C20**：**本轮 CI 的结论**（run 号／三 job 三态／用时）**一律只记当日日志与对话返回**，**不入任何跟踪文件**；仓库侧**只留复核命令 ＋ 取数时点**（治 R82 自指递归）。

| 项 | 值 |
|---|---|
| 复核命令 | `gh run list --limit 3`｜`gh run view <id> --json status,conclusion,jobs` |
| 取数时点 | 本报告定稿时 |
| 本轮推送 | **两次**（①「4 项裁定落地」／②「**R89 补登**」＝ 本报告 ＋ `BASELINE §26.9`）；**两次的 CI 结论均只记当日日志** |
| 断言纪律（承 **R89**） | 若要在日志里断言「重试是否被触发」，**必须剔除 `gh run view --log` 的脚本回声行**（带 ANSI 色码、与脚本源逐字相同），**或改用运行期证据**（退出码／后续步骤成败／运行期产物）—— **不得用脚本文本 grep 日志** |
