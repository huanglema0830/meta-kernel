【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 夜间 P3-1 · `docs/` 时效核对稿「第二批」（只出稿，不改 `docs/`、不改基线）

**保密标记**（C12）：⛔ 机密 **无**｜🔐 保密 **无**｜🔒 半公开：仓库内相对路径与行号、编号体系、模块名与计数口径｜🔓 公开：结论与条目。
> 本报告**不含**凭证、账号、本机绝对路径、网络细节、参数库取值。仓外物一律只写「仓外」。

**任务**（机制 19／C14 夜间 P3）｜**基准** v0.259｜**定位**：阶段二 · 子任务2｜**优先级 P3**｜白名单类别＝**调研／文档**｜**边界：只读 ＋ 只新增本报告；不改 `docs/`、不改治理文件、不改代码、不提交远端**

**状态**：完成（**出稿**）

---

## 一、做了什么

对第一批**未覆盖**的 9 份 `docs/` 文档做**定向时效扫**（家族：**设计-实现对齐**／**版本号族**），逐项给【文件:行／原文／应改为／依据／活跃段 or 历史段】。

**9 份文档**（依据：`2026-09-20_docs文档时效核对稿.md` §6 诚实清单）：

| # | 文件 | 行数 | 本批结论 |
|---|---|---|---|
| 1 | `SILA_IMPLEMENTATION.md` | 119 | ✅ 无需改（与家族 E 一致） |
| 2 | `HOST_UI_DECISION.md` | 235 | ⚠️ **1 处过期（活跃段）** |
| 3 | `WORK_CONSOLE_PLAN.md` | 113 | ⚠️ 1 处**计数型**过期（历史段，建议加锚） |
| 4 | `REFERENCE_APP_DESIGN.md` | 93 | ✅ 无需改（实现侧核对通过） |
| 5 | `APPLICATION_FRAMEWORK_DESIGN.md` | 216 | ⚠️ 1 处**措辞风险**（低） |
| 6 | `DIAGNOSIS_VALIDATION.md` | 81 | ✅ 无需改 |
| 7 | `L5_TRANSLATION_TABLE.md` | 64 | ✅ 无需改 |
| 8 | `GENE_LIBRARY_DESIGN.md` | 275 | ★ **4 处过期 ＋ 4 处死指针（本批最重）** |
| 9 | `LAYER_BASEMAP_L0_L6.md` | 685 | ⚠️ 3 处**指针口径**（低）＋ 1 处**版本基准锚** |

---

## 二、过期点清单（逐项：文件:行／原文／应改为／依据／活跃段 or 历史段）

### ★ 必改（实质误导）

**D-1｜`GENE_LIBRARY_DESIGN.md:65`（活跃段）**
- **原文**：`**当前为常量实现，尚未做到"从基因库读取"**（见 §7 边界）。`
- **实测事实**：同文件 §8 现状表（**L244**）＝「第 1 层 基础公式（元素层）｜✅ **已实现 + 已接线**｜…L4 经 `threshold::from_library` 读取」。
- **应改为**：删去「尚未做到」或改为「（**已于 v1.2/v0.107 落地**，见 §8）」；指针 `§7` → `§8`。
- **依据**：同文件 L244（同文件内自证）＋ `grep -n "from_library" meta-kernel-core/src/l4/threshold.rs`（**实存**）。

**D-2｜`GENE_LIBRARY_DESIGN.md:79`（活跃段）**
- **原文**：`当前本底场仍以"数值地图"形式持久化，**尚未按"场景公式 + 参数"存储**（见 §7 边界）。`
- **实测事实**：§8 L245＝「第 2 层 场景公式（场景层）｜✅ **已实现 + 已接线**｜L5 经 `l5_context::scene_baseline` 按语境取本底场」。
- **应改为**：同 D-1。
- **依据**：同文件 L245＋`grep -n "scene_baseline" meta-kernel-core/src/l5_context.rs`。

**D-3｜`GENE_LIBRARY_DESIGN.md:91`（活跃段）**
- **原文**：`当前运算关系写在内核代码里，**尚未作为"公式条目"入库**（见 §7 边界）。`
- **实测事实**：§8 L246＝「第 3 层 计算关系（公式层）｜✅ **已实现**｜`GeneLibrary::relation` + `add_relation`（**尚未被上层读取**，属"备而可用"）」。
  ⇒ 后半个「尚未被上层读取」**是对的**（§8 亦如实标注）；错的是「尚未作为公式条目入库」。
- **应改为**：保留「尚未被上层读取」，删去「尚未入库」；指针 `§7` → `§8`。
- **依据**：同文件 L246＋`grep -n "fn add_relation\|fn relation" meta-kernel-core/src/gene_library.rs`。

**D-4｜`GENE_LIBRARY_DESIGN.md:102`（活跃段，★ 本批最重）**
- **原文**：`**已实现"验证后入库"与"命中计数"，但尚未使用哈希链**（当前为普通记录，见 §7 边界）。`
- **实测事实**：① §8 L247＝「第 4 层 验证记录（哈希链）｜✅ **已实现**｜篡改可检出；持久化后仍可校验（`verify_chain` 通过）」；② §9 版本记录 L262＝「`to_text`/`from_text`：四层 + 哈希链**完整往返**」；③ **实现实存**：`meta-kernel-core/src/gene_library.rs:607`（追加链节）／`:616`（`校验哈希链完整性`）。
- **交叉佐证**：另一份文档 `WORK_CONSOLE_PLAN.md:74`「哈希链验证｜基因库验证记录层（篡改可检出）｜✅」——与实现一致 ⇒ **冲突点判定为 `GENE_LIBRARY_DESIGN:102` 过期**（非反向）。
- **应改为**：删去「尚未使用哈希链」，或改为「（**v0.087 时**为普通记录；**v0.107 起为哈希链**，见 §8）」；指针 `§7` → `§8`。
- **依据**：上列三条 ＋ `grep -n "verify_chain" meta-kernel-core/src/gene_library.rs`（→ 命中 ≥2）。

> **★ D-5｜同族第 3 处：`GENE_LIBRARY_DESIGN.md:213`（诚实边界块，带 `(v0.087)` 标记 ⇒ 可辩护为历史段）**
> - **原文**：`也**尚未使用哈希链**。`
> - **判断**：上一句自带 `（v0.087）` 版本标记 ⇒ **倾向历史段**；但读者易与 §8 冲突。**建议**加「（截至 v0.087；**现行状态见 §8**）」，**不建议删**（守 C19 保留留痕）。

### 必改（标题型）

**D-6｜`HOST_UI_DECISION.md:63` 表标题 ＋ `:69`（活跃段标题 ＋ 内容过期）**
- **原文**：`## 4. 现状与验收对照（第三阶段六项验收）`｜表内 `| 1 窗口能打开并看到场域画面 | ⏳ **未交付**（见 §3） |`
- **实测事实**：**同文件** §6（L93 起）＝「第三步**第一步已完成**：窗口宿主（不含 UI）」；§6 验收表（**L108**）＝`| 窗口能打开 | — | ✅ **1536×1536**（HiDPI…）`、**L109**＝`| 能看到场域画面 | ✅ 三样本方差 43.9／1182.8／1370.7，均 非纯黑非纯白=true`。
- **性质**：§4 标题**无版本锚** ⇒ 读作"现状"，但与同文件 §6 **直接矛盾**。
- **应改为**：把 §4 标题改为「**（v1.0 时）**现状与验收对照」，或给 1–4 行加「→ 已由 §6 交付」后记指针。
- **依据**：同文件 L63／L69 ↔ L93／L108／L109（同文件内自证）。

### 建议改（计数型／措辞／指针，低）

**D-7｜`WORK_CONSOLE_PLAN.md:72`（历史段，建议加锚）**
- **原文**：`| 单元测试 | 314 项（内核 215） | ✅ |`
- **实测事实**（代理口径＝`#[test]` 计数，见 §五「未验证」第 ② 条）：`meta-kernel-core` **421**｜`meta-kernel-core-nostd` **517**｜`meta-kernel-core-mem` `n/a`（目录为 `meta-kernel-mem`）｜`npb-gateway` **40**｜`manifest-ui` **4**｜`host/field-render` **35**｜`host/sky-browser` **7** ⇒ 合计 **≈1,024**。
- **判断**：该表属 §4.1「已有机制」，随 **v1.1／v0.105** 快照（`:3` 有「对应版本 v0.105」⇒ **历史段**）。
- **应改为**：表标题加「**（v0.105 时）**」，**不建议改数字**（改动即篡改历史快照，C19）。

**D-8｜`APPLICATION_FRAMEWORK_DESIGN.md:185`（措辞风险，低）**
- **原文**：`✅ **一期采用**（零新部署，网关已发布）`
- **实测事实**：`git remote -v` ⇒ 单一私有远端；仓内**无**对外分发配置。句意应为「**本地形态已具备/已交付**」。
- **性质**：**非虚假**，但「已发布」三字在**过程产品不外发**（C10）语境下易被误读为"对外”。
- **应改为**：`（零新部署，网关**已交付本地形态**）`。
- **依据**：`git remote -v`（私有）＋ 仓内无发布流水线（`.github/workflows/` 仅 CI）。

**D-9｜`LAYER_BASEMAP_L0_L6.md:43／:48／:106`（指针口径与目标漂移，低）**
- **`:43` 原文**：`wasm 差异未定义（见 §3.2-③）` ⇒ §3.2 的子项编号实为 **`L1-U1／U2／U3`**（L194–202），**无 ①②③** ⇒ **应改为 `§3.2-U3`**。
- **`:48` 原文**：`缺失（见 §4.2-①）` ⇒ §4.2 子项实为 **`L2-U1`**（L280）⇒ **应改为 `§4.2-U1`**。
- **`:106` 原文**：`（见 §2.3-V1 漏洞）` ⇒ §2.3 的 `L0-V1` 已于 **2026-09-18** 被判**执笔方误判并撤销**（L110–113，现为"已撤销＋残留"）⇒ **应改指其残留句**，不宜再称"漏洞"。
- **依据**：`grep -n "^### " docs/LAYER_BASEMAP_L0_L6.md`（章节目录）＋ 逐段实读 L192–203／L278–283／L108–116。

**D-10｜`LAYER_BASEMAP_L0_L6.md:8`（版本基准锚；双读）**
- **原文**：`本轮 git 基准：**v0.190**（git rev-list --count HEAD 实测，HEAD 2fdb277）`
- **实测事实**：当前 **v0.259**（`git rev-list --count HEAD`）。
- **判断**：`本轮`二字＋置于「状态/变更」说明区 ⇒ **倾向历史段（撰写时基准）**；非错误。
- **应改为**（可选）：`本轮（**撰写时**）git 基准`，避免被读作现状。

### ✅ 核对通过（明确「无需改」，亦为结论）

| 文件:行 | 内容 | 结论与依据 |
|---|---|---|
| `SILA_IMPLEMENTATION.md:90–91` | `WorldAdapter…**尚未实现**`｜`L6＝呈现（已实现）+ 对齐（未实现 = WorldAdapter）` | ✅ **与家族 E 一致**：第一批 §5 已证 `LAYER_ARCHITECTURE` L44／L308、`LAYER_BASEMAP` L48／L536、`archive/reserved-interfaces/l6_interface.rs`（仅契约）三处自洽 |
| `REFERENCE_APP_DESIGN.md:45` | `"rounds": 1, // 已完成的 99→0 回融轮次` | ✅ **实现侧核对通过**：`manifest-journal/src/lib.rs:88`（`pub rounds: u32`）／`:195`（`self.entry.rounds = self.engine.rounds`）／**:395–408**（`fn simulate_reaches_99_and_retreats_with_rounds`：`assert_eq!(s.engine.rounds, 1)`）／`:421`（归档不计完成轮次） |
| `DIAGNOSIS_VALIDATION.md:67–69` | 三例实测表 | ✅ 属**留痕型验证报告**，通篇为当时快照（C19：不当逐条订正） |
| `L5_TRANSLATION_TABLE.md:3／:64` | `状态：v0.1（2026-09-09）`｜`配套：L5_DESIGN.md（最终版 v2.0）` | ✅ 文档版号，非实现状态；无需改 |
| `WORK_CONSOLE_PLAN.md:74` | `哈希链验证｜基因库验证记录层（篡改可检出）｜✅` | ✅ **与实现一致**（见 D-4 依据）——**本条不属过期，反为 D-4 的佐证** |

---

## 三、三量台账（C6）

| 量 | 内容 |
|---|---|
| **存量** | 9 份文档全文定向扫（合计 **1,871** 行）＋ 依据链回读（`GENE_LIBRARY_DESIGN` §8/§9、`HOST_UI_DECISION` §5/§6、`LAYER_BASEMAP` §1–§5、第一批核对稿 §6）＋ 实现侧取证 6 处 |
| **变量** | **0** —— **未改动任何 `docs/` 正文、治理文件、代码、CI、判据** |
| **补充增量** | **1 份**：本报告（**新写非覆盖**） |
| **创新增量** | **0**（D-1…D-10 属"存量过期点的显式化"） |

---

## 四、与第一批的差集（防静默积压 · R87 家族）

| 第一批结论 | 本批复核 |
|---|---|
| 家族 E（L6 对齐族）三处一致 | ✅ **仍成立**（本批追加 `SILA_IMPLEMENTATION:90–91` 为第 4 处一致） |
| 「含日期的历史报告类」建议统加标注 | ⏳ **仍在**（`DIAGNOSIS_VALIDATION`／`PHASE*`／`L4/L5_VALIDATION` 等）；**未加标注** |
| 必改 B-1／B-2（`LAYER_ARCHITECTURE` L65／L323、`SELF_DIAGNOSIS` L38） | ⏳ **仍在**（本批未动；改底图须授权 ＋ 过判据 F） |
| 本批未覆盖 9 份 | ✅ **本批已完成**（本报告） |

---

## 五、边界说明（C5；四类齐全）

- **本地跑过**：✅ 有。`docs/` 逐文件 `grep`＋定点 `sed` 实读；实现侧 6 处取证（`from_library`／`scene_baseline`／`add_relation`／`verify_chain`／`manifest-journal rounds`／`git remote -v`）；**阴性对照**：`grep -rn "zzz_rounds"` ⇒ **0**（检索式有效）。
- **CI 过**：❌ **无** —— 本项只读出稿，**未触发 CI**。按 **C20**：CI 结论不入库。
- **真实跑过**：❌ **无** —— 未跑 QEMU、未上真机（本项不涉）。
- **未验证**：① 各项均为**出稿**，**未改 `docs/`**（正本执行须用户授权 ＋ `--update-baseline`，留白天）；② **D-7 的测试计数为代理口径**（`#[test]` 静态计数，**≠ `cargo test` 通过数**；且 `meta-kernel-boot` 检出 0 因本机不可构建，非"无测试"）⇒ **只作量级参照，不作判据**；③ D-2/D-3 仅取实现侧符号存在性（`grep`），**未跑该路径测试**；④ 9 份文档**未逐行穷举**，取"设计-实现对齐／版本号族"两个家族定向扫。

---

## 六、建议动作（**均待你确认**；**本批不改任何底图**）

1. **必改（实质误导）**：**D-1／D-2／D-3／D-4**（`GENE_LIBRARY_DESIGN` 4 处「尚未…」＋4 处 `§7`→`§8` 死指针）——**建议四处一处改**（同文件、同根因），改后过 **C15 双向计数** ＋ 判据 F `--update-baseline`。
2. **必改（标题型）**：**D-6**（`HOST_UI_DECISION` §4 加版本锚或加后记指针）。
3. **建议改（低）**：**D-7**（加版本锚）／**D-8**（措辞）／**D-9**（3 处指针）／**D-10**（可选）。
4. **不改**：D-5（历史段，建议仅加括注）＋ §二「核对通过」5 条。

---

**启动回执**（`CHARTER.md` §五）：已读 `CHARTER.md`（机制 1–27）＋ `CONSTRAINTS.md`（**C5／C6／C12／C14／C19／C20**）＋ `TEMPLATES.md` §二 第〇项 ＋ 09-20 清单 P3-1 项。
当前状态：**v0.259**（`git rev-list --count HEAD`）｜本次任务：`docs/` 时效核对稿第二批（**P3**，只读 ＋ 只写本报告）｜是否继续：**是**（下一步 P3-2）。
