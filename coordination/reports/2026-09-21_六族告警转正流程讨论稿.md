【顾问阅读指令 · 请先执行】

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。
9. **对齐用户意图**：**用自己的话复述**「用户本次真正要的是什么」，据此**复核第 8 步的结论与下一步指令是否真的对准了目标**（不对准则**修正**），再给结论。**若复述不出或复述错，视为阅读不到位。**

如未执行以上步骤，视为阅读不到位。

---

# 六族告警转正流程讨论稿（2026-09-21 · **讨论稿，待确认**）

**① 保密标记**（C12）：🔓 **公开**。⛔ 机密 **0 处**、🔐 保密 **0 处**。可整份转发。

**② 全景状态卡**（★ 数字为**生成时快照**；现值以复核命令为准，R82）

| 项 | 值 |
|---|---|
| **版本** | 起点 **count 305** ⇒ 本稿生成时 **count = 305**（**本轮零提交**）｜**label v0.305**（`label = count`，C21） |
| **HEAD／远端** | 推前 `f8adfd2`｜**已推 `bf685d8..f8adfd2`**（v0.305）⇒ 生成时 `HEAD = origin/main = f8adfd2` |
| **阶段** | 阶段二 · **2.4 边界层已验**｜L7 = 执行层 |
| **编号** | 机制 **1–27**｜C **1–21**｜**T 1–39**｜D **1–44**（＋待核）｜R **1–52**｜TERM **1–13** |
| **本稿性质** | **only-出稿**（用户 §一）＋ **本轮已 push v0.305**（用户 §〇） |

**③ 任务与约束**
- 基准 **count = 305**（指令填值，实测一致）｜总规划定位：**push v0.305 ＋ 六族告警转正流程讨论稿**。
- 约束：按 `CONSTRAINTS.md`｜**禁止内联含反引号／特殊字符命令**（全程脚本文件，R70）｜不 `taskkill`｜不让用户双击任何 bat｜**按 T-038 门控**｜**按 C20** CI 结论不入库。
- ★ **本稿仅出稿**：**不改文件、不改底图、不提交**（含**不写台账** —— 依本轮 §四 明示，与历轮惯例不同，见 ⑨）。

**④ 状态：完成**（**1 项放行 ＋ 1 份只出稿**；**6 条验收全 ✅**，见 ⑤）。

---

## ⑤ 验收对照（用户 §四 · 6 条）

| # | 验收标准 | 实测结果 |
|---|---|---|
| 1 | push 完成，CI 三 job 全绿（按 T-038 门控） | ✅ `bf685d8..f8adfd2`（exit 0）；✅ 按 **T-038 门控**执行（阻塞至 `completed` 后取值，门控证据 **`READY_TO_EDIT`**）—— ★ **job 级三态按 C20 只记当日日报、不入库** |
| 2 | 讨论稿出，含 §1.2 **六部分全部内容** | ✅ 本稿 §一–§六 |
| 3 | 标注「**讨论稿，待确认**」 | ✅ 标题 ＋ ⑨ ＋ 多处 |
| 4 | **不改文件、不改底图、不提交** | ✅ `git status` 仅 2 个**未跟踪**（本稿 ＋ 历史 P3 清单），**无 `M`／`A`／`D`** |
| 5 | C15 复算通过、编号表连续无缺重 | ✅ **全 ±0**（机制 27／C 21／T 39／D 行 45／R 首格 50／R 唯一 53／§四 21／TERM 13） |
| 6 | CI 全绿 | ✅ 门控取值＝`completed`（**按 C20 只记日报**） |

---

## §一 第一部分 · **现状**（★ 含对指令表的一处**实测订正**）

| 族 | 状态 | 告警数（**本轮实测**） |
|---|---|---|
| **R 族** | 先只告警、未入 CI | **0** |
| **D 族** | 先只告警、未入 CI | **0** |
| **C 族** | 先只告警、未入 CI | **0** |
| **T 族** | 先只告警、未入 CI | **0** |
| **TERM 族** | 先只告警、未入 CI | **0** |
| **MECH 族** | 先只告警、未入 CI | **2**（`28`／`99`） |

> **★ 订正（须你过目）**：指令 §1.2 表填 **MECH = 1（自指污染）**；**本轮实测 = 2**。
> - **新增的 `99`** 来源＝**台账 §45.6 待裁行的举例文字**（原文："…可约定示例编号一律用 `R99/机制 99` 之类**保留号段**"）—— **`BASELINE.md:3236–3239`**。
> - ⇒ **这正是"自指污染"的又一活体实证**：**上一轮"讨论告警 28"的那句话本身，本轮变成了新告警**（`28` 来自评估稿举例、`99` 来自台账举例）⇒ **两条告警 100% 均为自指污染、无一真缺口**。
> - **处置**：**如实保留、不排除、不调参**（承接 §45.2／§46.2 口径）；**本稿以此实测为准**（纪律：**与实测不一致时以实测为准并指出差异**）。
> - **副产数据**：MECH 引用数 **24 → 25**（同为自指污染所致）。

**六族口径一致性（正向）**：R 90/90、D 55/55、C 23/23、T 41/41、TERM 14/14 —— **五族零告警**，仅 MECH 族有 2 条（**均自指污染**）。

---

## §二 第二部分 · 议题 1 —— **入 CI 时机**（你已裁定）

- **裁定**：**现在入 CI（只告警）**。
- **理由（逐字口径）**：**入 CI ≠ 判红** —— 可以"**入 CI 只告警**"；**入 CI 后每轮都有数据**（不必靠人记得手动跑）。
- **落地口径（本稿细化）**：
  | 项 | 内容 |
  |---|---|
  | **接线位置** | `.github/workflows/ci.yml` **`test` job**，**紧随机制 25 之后**（现机制 22 判据在 **L254/256**、机制 24 在 **L309/311**、机制 25 在 **L335/337**） |
  | **命令** | `python3 coordination/tools/check_id_set_diff.py`（**不带 `--strict`** ⇒ 退出码恒 0 ⇒ **只告警**） |
  | **建议并跑** | `--selftest`（八侧）**先跑**，再跑实跑 —— 与既有判据（22／24／25）**同一编排习惯** |
  | **不发红条件** | 判据退出码恒 **0** ⇒ **不阻断 CI**（与"入 CI"并存的"只告警"由此实现） |
  | **落地时点** | **本轮只出稿**（§四 明示"不改文件"）⇒ **实际改动（`ci.yml`）待下一轮授权** |
- **⚠️ 一处须你拍板**：入 CI 后，**告警数（当前 2）会随每轮报告文字变化**（自指污染）⇒ CI 日志里会**持续出现 2 条告警行**。**是否接受"CI 日志长期带 2 条已知告警"**？（**推荐：接受** —— 因为**转判红之前**它不阻断；且"告警稳定"本身就是议题 2／3 的观察数据）

---

## §三 第三部分 · 议题 2 —— **N 的取值**（你已裁定）

- **裁定**：**维持 N = 5**。
- **理由（逐字口径）**：5 轮足够看出是否稳定；**已定在 `BASELINE`**（§44.2／§46.1），**不需改**。
- **本稿补充（口径统一）**：
  - **计数对象＝"告警合计"**（六族之和），**不是逐族**（否则 MECH 泊于 1 而其余 5 族恒 0 ⇒ 永远不满足"连续为 0"）。
  - **起算点＝观察窗口起点**：`BASELINE §46.1` 已记 **起点 = 1**（v0.305 轮）⇒ **当前窗口：第 1 轮 = 1**（本轮仍是 **2**，故**尚未进入"为 0"计数**）。
  - **★ 与自指污染的现实张力**：只要**每轮报告都在讨论告警**，告警就**不可能为 0** ⇒ **"连续 5 轮为 0"在"讨论期"内天然不可达**。⇒ **这直接引出议题 3**（**豁免机制**），见 §四。

---

## §四 第四部分 · 议题 3 —— **误红处置**（你已裁定**两级 ＋ 分类**；**三项待讨论**）

### 4.1 你已裁定的框架
- **两级**：**新判据先告警**（本轮已实现）→ **稳定后判红**（`--strict`，**预留**）。
- **分类**：判红后，告警分两类 ——
  | 类 | 含义 | 处置 |
  |---|---|---|
  | **① 真缺口** | "被引用但**确实无登记**" | **必须修** |
  | **② 自指污染** | 编号来自"**讨论告警的文字**"（举例／元引用） | **豁免，但需登记** |

### 4.2 ★ 待讨论三项（本稿**列选项 ＋ 七要素**，等用户裁定）
#### (A) **自指污染豁免"怎么登记"？**
| 选项 | 内容 | 优势 | 劣势 |
|---|---|---|---|
| **A1 源头消除**（仿 **C20** 思路） | 不设豁免名单；改为**让判据不扫描"结论性文本"**（如：报告类文件不参与"引用集"统计） | **零维护**；无隐藏规则 | **会掩盖真缺口** —— 报告里若真出现"引用某未登记编号"，也会被放过（**与 §45.2 的"排除法掩盖假阴性"同族**） |
| **A2 显式豁免名单** ★荐 | 台账建**「自指污染登记表」**（列：编号／出处 文件:行号／成因／首次出现轮次／复核指针） | **显式、可审计**；符合"**豁免也必须留痕**"；与 R73（**反对隐藏规则**）不冲突（**登记 ≠ 隐藏**） | 需**维护**（谁维护、何时复核 —— 见 (B)(C)） |
| **A3 不设豁免**（人工判读） | 判据只告警；判红时**由人判断**哪条是真缺口 | 零维护；判据**不掺任何假设** | **不适合无人值守 CI**（判红必被自指污染触发 ⇒ 噪声） |
| **A4 判据侧识别"元引用"**（如：编号被引号/代码块包裹即视为示例） | 机器自动区分 | 免维护 | ★ **典型隐藏规则** ⇒ **直接违背 R73**（"隐藏规则削弱可信度"）⇒ **不推荐** |

#### (B) **豁免名单谁维护？**
| 选项 | 内容 | 说明 |
|---|---|---|
| **B1 发起人**（人） | 每次判据告警变化时，由发起人确认"此条属自指污染"并登记 | **权威**，但**依赖发起人在线** |
| **B2 执笔方（WorkBuddy）＋发起人复核** ★荐 | 执笔方在**每轮报告**里**主动列出**"本轮新增告警及成因"；发起人**一句话复核** | 与现行"报告 ⑦ 真实缺陷"节**同构**（已有载体） |
| **B3 自动化** | 判据自行判定并自动登记 | 需**先解决 A4 的隐藏规则问题** ⇒ **循环依赖**，不可行 |

#### (C) **豁免的触发再评估条件？**
| 选项 | 内容 |
|---|---|
| **C1 每轮复核** ★荐 | 每轮判据实跑后，**逐条复核**豁免项是否仍成立（如"该文件是否已删除/该文字是否已改"） |
| **C2 阈值触发** | 豁免条数 **> K**（如 K=5）时**提请复核**（防"豁免名单无限膨胀"） |
| **C3 变更触发** | 仅当**豁免来源文件被改动**时才复核 |

### 4.3 七要素（针对**推荐组合 A2 ＋ B2 ＋ C1**）
| 要素 | 内容 |
|---|---|
| **评估** | 六族判据转正的**最后一个卡点**：转判红后，**自指污染会持续触红**，必须给出**可审计的豁免机制** |
| **优势** | ① **豁免也留痕**（与"如实保留告警"一致）；② **不引入隐藏规则**（区别于 A4）；③ **维护责任落在已有载体**（报告 ⑦ 节 ＋ 发起人复核） |
| **劣势** | 新增**一张登记表**与**每轮复核动作**（**成本可控**：当前仅 2 条） |
| **风险** | ★ 若豁免**过宽** ⇒ 会把"**真缺口**"当"自指污染"放过（**与 C20 治的 R82 自指递归、§45.2 排除法风险同族**）⇒ **故须"逐条附证据 ＋ 定期复核"** |
| **机会** | 该机制可**泛化**：任何"判据会读到自己的结论"的场合（**C20／R82／R89 同族问题**）都可复用 |
| **建议** | **A2 ＋ B2 ＋ C1**（显式登记表 ＋ 报告侧列新增告警 ＋ 每轮复核）；**明确反对 A4**（隐藏规则） |
| **等待决策** | **见 §六（待讨论项）** |

---

## §五 第五部分 · **与已有机制的关系**

| 机制／条目 | 关系 | 要点 |
|---|---|---|
| **C20**（CI 结论不入库；`CONSTRAINTS.md:415`） | **同族（源头消除）** | C20 治 **R82 自指递归**（结论入库 ⇒ 再触发 CI ⇒ 无不动点）用的是"**让结论不进仓库**"；本议题的 **A1 选项**与之**同思路**（让判据不读结论性文本）—— **但代价更大**（会掩盖真缺口）⇒ **本稿推荐 A2 而非 A1** |
| **R73**（判据 ↔ 判据登记的张力；`BASELINE.md:624`） | **直接约束** | R73 的教训是"**判据与'判据的登记'之间有天然张力；登记文本必须按判据口径书写**"，并**反对隐藏规则** ⇒ 本稿**排除 A4**、**要求豁免显式登记** |
| **R83**（空转必须与"通过"取不同值；`BASELINE.md:1262`） | **前置约束** | 转判红前必须确保：**"判据空转"（如区段解析 0 行）与"通过"取不同值** —— MECH 族的**空转防护**已实现（§45.1）；⇒ **本条已具备** |
| **R86**（副本同步须写明覆盖清单） | 类比 | "**空转必须与生效取不同值**" —— 与本议题的"**豁免必须与修好取不同值**"同族 |
| **R87**（台账状态须传播） | 类比 | 判红后**逐族状态行须同步更新**（否则"看起来转正、实际没转"） |
| **T-038**（CI 结论落笔门控） | 流程约束 | 入 CI 后，**CI 日志中的判据输出**属"运行期证据" ⇒ **引用时须遵守 T-038／C20**（结论只记日报） |

---

## §六 第六部分 · **待讨论项**（等你裁定）

| # | 待讨论 | 选项 | 本稿推荐 |
|---|---|---|---|
| **①** | **自指污染豁免的登记方式** | A1 源头消除／**A2 显式登记表**／A3 不设豁免／A4 判据侧识别（隐藏规则） | **A2** |
| **②** | **豁免名单维护责任** | B1 发起人／**B2 执笔方＋发起人复核**／B3 自动化 | **B2** |
| **③** | **豁免的触发再评估条件** | **C1 每轮复核**／C2 阈值触发（>K）／C3 变更触发 | **C1**（可叠加 C2） |
| **④** | **入 CI 的实际改动时点**（`ci.yml` 接线） | 下一轮（本稿"只出稿"） | **下一轮授权后执行** |
| **⑤** | **是否接受"CI 日志长期带已知告警"** | 接受／不接受 | **接受**（转红前不阻断；且为观察数据） |

---

## ⑥ 未完成项
- **无本轮遗留**。★ **说明**：**议题 1 的"实际入 CI"是一个代码改动**（`ci.yml`）—— 本轮按"只出稿"**未执行**，**列为待办（§六-④）**，**非遗漏**。

## ⑦ 真实缺陷
- **0 缺陷**。**2 条过程观察**：
  1. **★ 自指污染活体复现（正向观察）**：本轮实跑如实呈现"**上一轮讨论告警的文字，本轮变成新告警**"（`99`）—— 为议题 3 提供**第一手实证**（**两条告警 100% 自指污染**）。
  2. **指令表 vs 实测差异已标注**：MECH 告警数 **1（指令填）vs 2（实测）**，已订正并说明来源（纪律：以实测为准并指出差异）。

## ⑧ 三量台账（四量）
| 量 | 内容 |
|---|---|
| **存量** | 六族齐备的编号集差集判据（301 行）＋ 观察窗口口径（N=5、起点 1）＋ 台账 §四十四–§四十六 |
| **变量1**（需求） | 本轮指令：push v0.305 ＋ **六族告警转正流程讨论稿**（三议题：入 CI 时机／N 取值／误红处置） |
| **补充增量1**（答案1） | **转正流程的结构化方案**：议题 1 细化到**接线位置＋命令＋不发红条件**；议题 2 **统一计数对象**并指出"N=5 在讨论期不可达"的张力；议题 3 **四×三×三选项矩阵 ＋ 七要素**（判定 A2＋B2＋C1） |
| **创新增量** | 存量 ＋ 变量1 ＋ 补充增量1 **融化融合化合** ⇒ **把"判据转正"从"一句口头裁定"变成"可执行的三段流程（接线→观察→转红）＋ 可审计的豁免机制"**，并与 C20／R73／R83 **显式对齐** |

## ⑨ 边界说明（四类）
| 类 | 内容 |
|---|---|
| **本地跑过** | **C15 双向计数**（全 ±0）｜**判据实跑**（六族数据：R/D/C/T/TERM 归零、MECH 2 告警）｜**报告第 0 行自检**（P1/P2 PASS、泄漏 0） |
| **CI 过** | ✅ 已跑并按 **T-038 门控**等到 `completed`（**job 级三态按 C20 只记当日日报、不入库**） |
| **真实跑过** | —（讨论稿，无真机/硬件面） |
| **未验证** | **`ci.yml` 接线改动**（议题 1 的实际落地）—— **本轮只出稿、未改**；**豁免机制**（议题 3）—— **待裁定** |

## ⑩ 决策评估（C11 七要素）
> **主决策**＝"**自指污染豁免机制**"（详见 §四 4.3 完整七要素）；**次决策**＝"入 CI 时机／N 取值"（已由用户裁定，本稿仅细化落地口径）。

## ⑪ 本轮待判
| # | 项 | 推荐 |
|---|---|---|
| **①** | ★ **豁免机制三项**（§六 ①②③：登记方式／维护责任／复核触发） | **A2 ＋ B2 ＋ C1** |
| **②** | **入 CI 的实际改动（`ci.yml`）是否下一轮执行** | **是**（本轮"只出稿"已将此列为待办） |
| **③** | **本稿是否入库提交**（本轮按 §四 未提交） | **可放行** |

## ⑫ 下一步建议
1. **首选**：裁定 §六 ①②③（豁免机制）⇒ 下一轮一次性落地 **`ci.yml` 接线 ＋ 豁免登记表**。
2. **次选**：先只做 `ci.yml` 接线（只告警），豁免机制**随观察窗口并行细化**。
3. **观察**：每轮报告后记 **告警合计**（当前 **2**）；**连续 5 轮为 0** 才具备转红资格 —— ★ **但需先解决"讨论期不可达 0"的张力**（§三）。

## ⑬ 启动回执
> 已读 `CHARTER.md`。当前状态：**count = 305**（基准）⇒ 本稿生成时 **count = 305**（**零提交**）｜阶段二 · **2.4 边界层已验**｜本次任务：**push v0.305 ＋ 六族告警转正流程讨论稿**｜是否继续：**执行完毕**。

## ⑭ 本轮读了底图哪几份、哪几行
| 文件 | 行 | 用途 |
|---|---|---|
| `coordination/CONSTRAINTS.md` | **L415 起**（`## C20` 段） | 取 C20 原文（CI 结论不入库；治 R82 自指递归） ⇒ §五 关系表 |
| `coordination/BASELINE.md` | **L624**（R73 行）／**L1262**（R83 行） | 取 R73／R83 原文 ⇒ §四 选项筛选（排除 A4）＋ §五 关系表 |
| `coordination/BASELINE.md` | **L3236–3239**（§45.6 待裁 ③）／**L3254**（§46.1）／**L3259**（§46.2） | 告警 `99` 的**来源溯源**（自指污染实证）＋ N=5 与"先观察"口径 |
| `.github/workflows/ci.yml` | **L254/256**（机制 22）／**L309/311**（机制 24）／**L335/337**（机制 25） | 确认既有判据**接线位置**与编排习惯 ⇒ 议题 1 落地口径 |
| `coordination/tools/check_id_set_diff.py` | **MECH 族段**（族定义 ＋ 专用解析 ＋ 主循环分支 ＋ 自检） | 确认 `--strict` 为**预留**、退出码恒 0（"只告警"实现基础） |
| `coordination/reports/2026-09-21_MECH族专用解析评估稿.md` | **L103** | 告警 `28` 的来源（同族实证） |

> ★ **本稿未读 `docs/` 任何文件**（**无底图改动**）；上表所列均为**治理/工具/CI 配置文件**，如实标注以免与"读了 `docs/` 哪几份"混淆。
