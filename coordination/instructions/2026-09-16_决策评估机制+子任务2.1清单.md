# 指令归档 · 决策评估机制（D22）+ 子任务2.1 改动清单

**基准**：v0.143
**约束**：按 `CONSTRAINTS.md` 执行
**总规划定位**：阶段二 · 子任务1 收尾 + 机制建设（决策评估机制）
**任务类型**：机制 + 决策
**本阶段目标**：建立决策评估机制，处理 D19 后续，启动子任务2
**归档日期**：2026-09-16

> 本文件是用户下发指令的**忠实结构化归档**（保留全部要求项，未删减）。

---

## 第一部分：D19 后续（等用户）

**用户操作**：把 SSH 公钥加到 GitHub 账号。

**用户加完后**说一句「**公钥已加，切 SSH**」，WorkBuddy 执行：
1. 跑 `bash deploy/github_switch_to_ssh.sh`
2. 验证推送成功
3. 输出结果

**如果用户未加**：**保持 HTTPS**，继续现有推送重试机制。

## 第二部分：建立决策评估机制（D22）

### 2.1 更新 `CHARTER.md`

「硬规则速查」新增：

| C11 | 凡涉及决策，顾问必须给六要素，用户决策 | CONSTRAINTS.md |

「全部机制清单」新增：

| 决策评估机制 | TEMPLATES.md §决策评估 | 六要素：评估/优势/劣势/风险/建议/等待决策 |

### 2.2 更新 `TEMPLATES.md` 新增「决策评估模板」

```markdown
## 决策评估模板（顾问用）

凡涉及决策，顾问必须按此格式：

【决策评估】
- 事项：X
- 评估：背景和现状
- 优势：至少2条，具体到可验证
- 劣势：至少2条，具体到可验证
- 风险：至少2条，标注概率和影响
- 建议：明确推荐哪个选项，附理由
- 等待用户决策

当选项多于2个时，加"各选项对比"表。
```

### 2.3 更新 `CONSTRAINTS.md` 新增 C11

（原文见 `CONSTRAINTS.md` C11 节：为什么／破了会怎样／要求三条／适用范围＝**顾问侧**）

### 2.4 更新 `BASELINE.md`

记录 **D22** 决策评估机制。

## 第三部分：启动子任务2 · 2.1

**3.1 任务内容**：元内核编译为 `x86_64-unknown-none`（no_std）。参考：purpleK2/kernel

**3.2 改动清单（先列清单，等确认）**：
- 新增 `meta-kernel-core-nostd/` 或类似目录
- **保留原 `meta-kernel-core/` 不动**（宿主仍需 std 版本）
- 新增 `#![no_std]` 入口
- 验证能编译

**3.3 验收标准**：
- `cargo build --target x86_64-unknown-none` 成功
- 内核测试仍全绿（std 版本）
- CI 全绿

**注（用户原文）**：这是首次转向 no_std，可能有大量兼容性问题。**先出改动清单，等确认后再执行。**

## 第四部分：验收标准

- D19：等用户加公钥
- 决策评估机制：CHARTER / TEMPLATES / CONSTRAINTS / BASELINE 更新
- 子任务2.1：**改动清单产出（不改代码）**
- CI 全绿

## 第五部分：汇报格式

按报告模板，**含全景状态卡**。

## 第六部分：版本号

自动生成，不手写。
