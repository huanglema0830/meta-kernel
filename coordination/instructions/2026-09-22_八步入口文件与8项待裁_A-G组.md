# 指令归档 · 2026-09-22 · 八步入口文件 ＋ 8 项待裁（A–G 分组）

> 归档依据：**机制 9（归档机制）** ＋ `CHARTER §九`「收工四步③ 指令归档 `instructions/`」。
> ★ **本文为用户指令原文（零修改）**；执行结果见 `coordination/reports/2026-09-22_A-G七组落地_八步入口与R90加固及INTEL登记.md` 与 `coordination/BASELINE.md` §五十二。

---

## 用户附言（分组评估）

## 评估：可以一起下，但要分组+定序

### 判断

| 考量 | 结论 |
|---|---|
| 任务冲突 | 无——八步入口、R90 修复、F-2 修列、台账并入互不影响 |
| 顺序依赖 | 有——④（台账并入）需在①–③、⑤–⑧ 完成后才记录 |
| 治理文件改动集中度 | 高——CHARTER / TEMPLATES / BASELINE / ROADMAP 都要动，需集中一次改完 |
| 一轮工作量 | 中——8 项 + 入口文件，不算大，但需分组清晰 |

**结论：可以一起下，但需按“组”组织，避免混乱。**

---

## 分组

| 组 | 任务 | 涉及文件 |
|---|---|---|
| **A** | 八步入口文件 + TERM-014 + 机制登记 + 指针验证 | ENTRY.md（新建）、TERMS.md、CHARTER.md、BASELINE.md |
| **B** | R90 + B5 同批加重试 | ci.yml |
| **C** | F-2 乙案修列 | ROADMAP.md |
| **D** | INTEL 正式登记为机制 28 + MEMORY.md 软上限 | CHARTER.md、MEMORY.md、BASELINE.md |
| **E** | 6 条过期平台任务下线 | 本机 |
| **F** | 报告内版本号口径 | BASELINE.md（规则记录） |
| **G** | 本轮成果并入 BASELINE §五十二 | BASELINE.md（汇总） |

---

## 指令（整块复制即可）

```markdown
【指令】
基准：count = 315（git rev-list --count HEAD，自动生成）
约束：按 CONSTRAINTS.md 执行；禁止内联含反引号/特殊字符的命令，一律用脚本文件；不 taskkill、不让用户双击任何 bat
总规划定位：八步入口文件 + 8 项待裁落地（分组处理）
任务类型：机制建设 + 裁定落地
本阶段目标：按 A–G 七组完成；每组独立本地 commit；最后统一 push

---

## 〇、说明

- **本轮分组处理，按 A→B→C→D→E→F→G 顺序。**
- **每组完成后本地 commit**（避免一次巨大 commit）。
- **治理文件改动集中在各组内**，同组内一次改完。
- **最后统一 push**（T-038 门控）。
- 如遇指令未覆盖的内容，**按 C5 如实报告，不猜测**。

---

## A 组：八步通用工作方法论

### A.1 建入口文件

文件：`coordination/workflows/ENTRY.md`

内容：

```markdown
【本入口对应的自定义指令】

所有动作以「八步通用工作方法论」按 TERM-014 执行。

## 调用方式

1. 读骨架：`coordination/workflows/universal_workflow.md`
2. 按需读子步：`coordination/workflows/sub/NN_*.md`
3. 实现层：`{PRIVATE_ASSETS}/workflows/sub/*_impl.md`（占位，待填）

## 八步速览

| 步 | 名称 | 做什么 |
|---|---|---|
| 1 | 罗列 | 场域全息采样 |
| 2 | 分析 | 四态场分析 |
| 3 | 推演 | 时间演化 |
| 4 | 拆颗粒 | 尺度分解 |
| 5 | 找思路 | 局部动力学 |
| 6 | 化合 | 自组织涌现 |
| 7 | 执行 | 真实场演化 |
| 8 | 输出 | 相图 |

## 指针

| 层 | 位置 | 级别 |
|---|---|---|
| 骨架 | `coordination/workflows/universal_workflow.md` | 🔓 公开 |
| 子步 | `coordination/workflows/sub/NN_*.md` | 🔓 公开 |
| 实现层 | `{PRIVATE_ASSETS}/workflows/sub/*_impl.md` | 🔐 私有 |

## 适用时机

所有需要"从问题到方案"的动作。
```

### A.2 TERM-014 登记

在 `coordination/TERMS.md` 新增：

| 编号 | 英文名 | 中文功能说明 | 指针 |
|---|---|---|---|
| TERM-014 | Eight-Step Universal Workflow Methodology | 八步通用工作方法论——所有动作按此执行 | `coordination/workflows/ENTRY.md` |

逐字给出新增内容；C15：TERM 14→15。

### A.3 机制登记

查 `CHARTER.md` 机制表最大编号，顺延新编号（如机制 28 空闲则用 28）。登记：
- 机制名称：八步通用工作方法论
- 入口：`coordination/workflows/ENTRY.md`
- 触发：所有动作

### A.4 指针验证

模拟：WorkBuddy 读到自定义指令 → 读 TERM-014 → 查 `coordination/TERMS.md` → 找到 `coordination/workflows/ENTRY.md` → 按入口执行。逐步走一遍，出验证稿。

### A.5 本地 commit

---

## B 组：R90 + B5 同批加重试

### B.1 目标

- `ci.yml:451` wasm-bindgen 下载（R90）加重试。
- B5（`npx playwright install`，`ci.yml:513`）同批加重试。
- 保留“重试耗尽仍判红”。

### B.2 口径

- 逐字给出改动前后对照。
- 过 C15 双向计数。
- 记录到 BASELINE。

### B.3 本地 commit

---

## C 组：F-2 乙案修列

### C.1 目标

- `ROADMAP.md` 2.5/2.6 改标 P1。
- 加“最后复核”标记。

### C.2 口径

- 逐字给出改动前后对照。
- 过 C15 双向计数。
- 记录到 BASELINE。

### C.3 本地 commit

---

## D 组：INTEL 正式登记 + MEMORY 上限

### D.1 INTEL 正式登记为机制

- INTEL 试用期满（3 轮）。
- 正式登记为机制（查最大编号后顺延）。
- 写入 `CHARTER.md`。
- C15：机制编号 +1。

### D.2 MEMORY.md 软上限

- 软上限 8 KB。
- 30 天复核。
- 写入 `TEMPLATES.md` 或 `CONSTRAINTS.md`。
- 补记 T 编号。

### D.3 记录到 BASELINE

### D.4 本地 commit

---

## E 组：6 条过期平台任务下线

- 用户已确认：6 条过期候选下线。
- 逐条列出并下线（置 PAUSED）。
- 只在本机操作，不进仓库。
- 记录到 BASELINE（只记录处置动作）。

### E.1 本地 commit（如涉及仓库文件）

---

## F 组：报告内版本号口径

### F.1 规则

- **报告内版本号 = 报告首次提交时的 count**（时点值）。
- 如后续有订正提交，**在订正处标注"订正时的 count"**，不改原报告版本号。
- 理由：符合现有"数值＝时点值"口径，不无限递归。

### F.2 落地

- 写入 `TEMPLATES.md` 或 `BASELINE.md`。
- 记录到 BASELINE。

### F.3 本地 commit

---

## G 组：本轮成果并入 BASELINE §五十二

- 汇总 A–F 组改动。
- 写入 `BASELINE §五十二`。
- C15 复算通过。

### G.1 本地 commit

---

## H：收尾

### H.1 Push（按 T-038 门控）

- push 全部本地 commit。
- **按 T-038 门控**：push 后等 `gh run list` 显示 `completed`，取逐 job 三态，才动文件。
- 按 C20：CI 结论只记日报，不入库。

### H.2 下一份报告

- 十四项齐全，第 0 行含 9 步阅读指令。
- 三量台账四量齐全。
- 边界说明四类齐全。
- 含"本轮读了底图哪几份、哪几行"。
- 记录 A–G 组全部改动。
- 含"INTEL 状态表"（置于十四项之后，依 T-042）。
- 含 B2+C1 告警行。

---

## 验收标准

- A 组：ENTRY.md 建立；TERM-014 登记；机制登记；指针验证稿出。
- B 组：R90 + B5 加重试，逐字对照。
- C 组：F-2 乙案修列，逐字对照。
- D 组：INTEL 正式登记为机制；MEMORY 软上限 8 KB。
- E 组：6 条过期平台任务下线。
- F 组：报告内版本号口径写入。
- G 组：本轮成果并入 BASELINE §五十二。
- C15 复算通过，编号表连续无缺号无重号。
- CI 全绿（按 T-038 门控）。
- 下一次返回第 0 行含 9 步阅读指令。

## 版本号

自动生成，不手写。

---

> **启动回执**
> 已读 CHARTER.md。
> 当前状态：count = 315 ｜ 阶段二 · 2.4 边界层已验
> 本次任务：八步入口文件 + 8 项待裁（A–G 组）
> 是否继续：执行
```
