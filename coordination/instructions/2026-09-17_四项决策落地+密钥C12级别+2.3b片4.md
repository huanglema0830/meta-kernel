# 归档指令 · 2026-09-17（第五轮）：四项决策落地 + 密钥 C12 级别 + 2.3b 片4 + 首夜汇报

> 来源：用户指令（完整，未截断）
> 基准：v0.177（自动生成）｜约束：按 `CONSTRAINTS.md`
> 总规划定位：2.3b 片4 ＋ D35/D36/D37/D39 确认 ＋ 密钥 C12 级别补全 ＋ 夜间首夜汇报
> 任务类型：推进 ＋ 决策确认 ＋ 机制补全

## 一、决策确认（用户原文）

- **D35**：选 **③**，只换 `deploy/`，不动 `docs/` 历史报告。
- **D36**：选 **②**，`docs/` 的"设计说明类"也换，历史报告不追改。
- **D37**：追认 `HashSet→BTreeSet` 类型替换。
- **D39**：先离线备份密钥（指纹 🔒 `〈本机私记〉`），再放行删除明文。
- 备份方式：**用户自行抄写密钥本体到物理介质**。
- 备份确认后，执行 `purge`。
- **若用户未确认备份，保持明文与密文并存。**
- 四项记录到 `coordination/BASELINE.md`。

## 一之补：密钥 C12 级别标识（用户原文）

- **密钥本体**：⛔ 机密。
- **密钥指纹**：🔒 半公开。
- **密钥存放路径**：⛔ 机密。
- 在 `TEMPLATES.md` §十二 和 `CONSTRAINTS.md` C12 中明确写入。
- **检查器增加判据**：报告/文档中出现的密钥相关内容，必须带级别标识。
- D39 的密钥备份说明中，明确写出"密钥本体=⛔机密，指纹=🔒半公开"。
- 机制 20 三层结构中，**密钥的 C12 级别必须在条文里单独标注**，不能只靠保密标记行笼统带过。

## 二、2.3b 片4（用户原文）

- 按订正清单执行片4：`l1_field_parse`→`l3_world`→`l5_quad`→`l5_context`→`l5_evidence`，2,130 行。
- 守 C1、C3、C9。
- 保真度判据：多重集差。
- 裸机断言扩到第 ⑦ 段。
- ⚠️ 注意 `l5_evidence::adjust` 有中性点 m=0.5 的断言口径（R7 教训）。
- 可夜间跑 ✅。

## 三、夜间首夜汇报（明早）（用户原文）

今夜 21:00 / 23:00 首次触发后，明早汇报：是否真触发／P3 完成项、失败项／**agent 类任务是否真跑通（最大未验证点）**／未推送 commit 清单／需目验放行项／确认未自动 push、未改治理文件。**如实汇报，不粉饰。**

## 四、机制 20 持续实践（用户原文）

本轮报告通篇只用英文名与编号；检查器三模式全 PASS ＋ 阴阳自检；**反例必须用无关占位串（R28 教训）**。

## 五、本轮落地结果（对照）

| 原文要求 | 落地 | 证据 |
|---|---|---|
| D35 ③ | ✅ `deploy/win64/README.md` 1 条掩码 | 门禁 syntax PASS |
| D36 ② | ✅ `docs/` 设计说明类 4 文件 / 8 条掩码；历史报告类不追改 | 泄漏基线 **14 → 5** |
| D37 | ✅ 追认（反例情形写入条文） | `BASELINE.md` D37 行 |
| D39 | ⏳ **未执行 `purge`**（等你"备份完成"确认）；明文仍在 | 见报告 §三 的技术提醒 |
| 密钥 C12 级别 | ✅ `C12「密钥专项级别」` ＋ `TEMPLATES §十二`（密钥行拆三行） | 条文在位 |
| 检查器判据 | ✅ **[5] 密钥级别判据**（入 CI 的 syntax 模式）＋ **阴阳自检** | 注入无标识行 ⇒ 判红 |
| 片4 | ⚠️ **原清单不可执行** ⇒ 按**最小封闭超集 10 模块 / 3,920 行**执行 | 保真度 10/10；闭包判据 PASS |
| 裸机断言 ⑦ 段 | ✅ 含 `l5_evidence::adjust` 的 **m=0.5 中性点**（R7 教训） | host 镜像测试诊断 `m=0.5 ⇒ 0` |
| 首夜汇报 | ✅ **如实报"仍未发生"**；并新建 **09-18 08:00 一次性晨报任务**（只读、按证据） | 报告 §七 |

## 六、⚠️ 一处必须向用户提出的技术异议（已写入报告 §三）

用户定的备份方式＝"**自行抄写密钥本体到物理介质**"。
但**密钥本体是 32 随机字节（64 个十六进制字符）**，**手抄极易出错**，而 AEAD 下**抄错＝完全读不回**（不会"部分可用"）。
⇒ 已建议：**优先整文件复制**；若坚持手抄，**必须用指纹核对**（新增 `terms_crypt.py keycheck`，与 🔒 `〈本机私记〉` 逐字比对）。
**在确认之前，明文保持不动。**

## 七、待用户拍板（下一轮）

- **D39**：一句"备份已完成"（→ 执行 `purge`）
- **D40（新）**：剩余 **24 模块 / 6,528 行**的分片**按闭包重排**（建议用 `check_migration_closure.py` **脚本产出**，不再手写）
