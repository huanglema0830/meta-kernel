# 指令归档 · 顾问侧接续机制 + SSH 切换（D19/D20）

**基准**：v0.140
**约束**：按 `CONSTRAINTS.md` 执行
**总规划定位**：阶段二 · 子任务1 收尾 + 机制建设（顾问侧接续）
**任务类型**：机制 + 决策
**本阶段目标**：建立顾问侧接续机制，处理 D19–D20 决策
**归档日期**：2026-09-16

> 本文件是用户下发指令的**忠实结构化归档**（保留全部要求项，未删减）。

---

## 第一部分：回应上轮待决策

| 编号 | 用户决策 | 要点 |
|---|---|---|
| **D19** | **做** | 生成 SSH key → 输出公钥给用户 → 用户在 GitHub 账号添加 → 切换 remote 到 `ssh.github.com:443` → 验证推送成功。**完全可逆**（删公钥即回退） |
| **D20** | **延后** | 算法层等真正需要时再填（框架层先用） |
| **子任务2** | 下一轮启动 2.1（no_std 编译） | 本轮先清障碍，下轮开工 |

## 第二部分：建立 advisor_brief.md

**2.1 创建 `coordination/advisor_brief.md`** —— 内容结构：
- 项目身份（一句话）
- 当前阶段
- 最近讨论结论（**按时间倒序，最多 5 条**）
- 待决策项
- 方向判断
- 关键术语
- 恢复话术

**2.2 创建 `coordination/discussions/`** 归档旧的讨论记录。

**2.3 更新 `TEMPLATES.md`** 加入"顾问简报更新"流程：
- 触发条件：用户说"这段讨论落库"或"归档"
- 动作：WorkBuddy 更新 `advisor_brief.md`
- 归档：旧讨论移到 `discussions/`

## 第三部分：建立顾问接续包生成机制

**3.1 WorkBuddy 的动作**（当用户说"顾问对话断了，生成接续包"）：
1. 读 `advisor_brief.md`
2. 读 `BASELINE.md`
3. 读 `reports/` 最新
4. 生成"顾问接续包"

**3.2 接续包格式**：见上方设计。

**3.3 更新 `CONTINUITY.md`** 加入"顾问侧接续"章节，说明：
- 用户和顾问的对话断裂时，WorkBuddy 如何生成接续包
- 用户如何用接续包开新对话

## 第四部分：验收标准

- D19：SSH key 生成，公钥输出给用户
- `advisor_brief.md` 创建
- `discussions/` 目录建立
- 顾问接续包生成机制建立
- `CONTINUITY.md` 更新
- `TEMPLATES.md` 更新
- CI 全绿

## 第五部分：汇报格式

按报告模板，**含全景状态卡**。标注本次是"**顾问侧接续机制建设**"。

## 第六部分：版本号

自动生成，不手写。

---

## 附：执行中的两处关键判断（如实记录）

1. **不在公钥生效前切换 remote**：用户指令含"切换 remote → 验证推送"。但公钥需用户先在 GitHub 账号添加，
   若提前切换，本轮及后续推送会因 `Permission denied (publickey)` 全部失败。
   ⇒ 采取**先验证公钥是否生效、再切换**的顺序（脚本幂等 + 失败自动回退），**本轮保持 HTTPS**。
2. **CONTINUITY.md 是旧版（v3.4 / v0.110）且仓库根写错**（指向已归档的 `研发开发/meta-kernel`）。
   按用户"更新 CONTINUITY.md"的要求，**同时修正该历史错误**并加"当前状态 v4.0"块（历史锚点保留作审计）。
