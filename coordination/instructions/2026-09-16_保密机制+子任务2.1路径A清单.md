# 指令归档 · 保密机制（D25）+ 子任务2.1 路径A 清单

**基准**：v0.144｜**约束**：按 CONSTRAINTS.md（C1–C12）执行
**总规划定位**：阶段二 · 子任务1 收尾 + 机制建设（保密机制）+ 子任务2.1 启动
**任务类型**：机制 + 功能｜**本阶段目标**：建立保密机制，启动子任务2.1（**路径A + 数学函数自实现**）
**归档日期**：2026-09-16

> 本文件是用户下发指令的**忠实结构化归档**。

## 第一部分：三个决策确认

| 编号 | 用户确认 |
|---|---|
| **D23** | ✅ **路径 A（分两步）**：2.1 只做"无 alloc 子集"；2.3 承接 alloc 迁移；每步可独立验证，C1 不破 |
| **D24** | ✅ **数学函数自实现**：7 个浮点超越函数，守 C1 不引 libm；**精度要求：与 std 对照 <1e-9** |
| **D19** | ⏳ **用户暂未添加公钥**：**保持 HTTPS**，继续推送重试机制；等用户说「公钥已加，切 SSH」 |

## 第二部分：建立保密机制（D25）

**2.1 分级设计**

| 级别 | 标记 | 含义 | 拷给顾问时 |
|---|---|---|---|
| 公开 | 🔓 | 框架、思想、术语 | 直接拷 |
| 半公开 | 🔒 | 映射关系、部分实现 | 可拷，顾问应知悉 |
| 保密 | 🔐 | 算法公式、阈值、权重 | 屏蔽后拷 |
| 机密 | ⛔ | 密钥、账号、具体路径、网络细节 | 绝不拷 |

**2.2 机密清单（绝不拷给顾问）**：密钥/token（SSH 私钥、API token、登录密钥）｜账号（邮箱、手机号、用户名）｜
具体路径（`C:\Users\...`）｜网络细节（内网 IP、DNS、代理配置）｜服务器地址（具体域名、端口）｜**基因库内容（公式、阈值、权重）**

**2.3 TEMPLATES.md**：报告模板新增"**保密标记**"字段（四级别清单 + 给用户的提示）

**2.4 CONSTRAINTS.md**：新增 **C12 保密机制**（为什么／破了会怎样／要求四条／**适用范围＝三方共用**）

**2.5 CHARTER.md**：机制清单加入**第 15 项**（保密机制）

**2.6 BASELINE.md**：记录 **D25**

## 第三部分：启动子任务2.1（路径 A）

**3.2 路径 A 执行内容**：① 装 target ② 新建 no_std crate ③ **原 `meta-kernel-core/` 不动** ④ `#![no_std]` 入口
⑤ 只迁不依赖 alloc 的模块 ⑥ 自实现 7 个浮点超越函数 ⑦ 验证 `cargo build --target x86_64-unknown-none` 成功

**3.3 改动清单（先列清单，等确认后执行）**——须含：迁移哪些模块／7 个数学函数实现方案／预计新增文件／
预计依赖关系／验收标准。**按 C4，先列清单，等用户确认后再改代码。**

**3.4 验收标准**：`cargo build --target x86_64-unknown-none` 成功｜内核测试仍全绿（std 版）｜数学函数精度 <1e-9｜CI 全绿

## 第四部分：保密机制的首次实践

**本轮报告起按保密机制标注**（示例格式：⛔机密／🔐保密／🔒半公开／🔓公开 ＋ 给用户的屏蔽提示）。

## 第五部分：验收标准

保密机制（TEMPLATES/CONSTRAINTS/CHARTER/BASELINE 更新）｜子任务2.1 **改动清单产出（不改代码）**｜
**保密机制首次实践（报告带保密标记）**｜CI 全绿

## 第六部分：汇报格式

按报告模板，含：**全景状态卡** + **保密标记（首次实践）** + **三量台账** + **边界说明**

## 第七部分：版本号

自动生成，不手写。
