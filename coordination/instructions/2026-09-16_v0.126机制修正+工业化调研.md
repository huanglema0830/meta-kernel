# 指令归档 · 机制修正 + 工业化路径调研

**下达时间**：2026-09-16
**基准**：v0.126
**任务类型**：机制修正 + 工业化路径调研
**约束**：按 CONSTRAINTS.md 执行

---

## 一、机制修正（D-a / D-b）

### 1.1 修正 C2/BASELINE 口径
- BASELINE.md 的"版本：vX.Y"改为"最后更新于：HEAD xxx（对应vX.Y）"
- 明确：这是"更新时的版本"，不是"当前版本"
- CONSTRAINTS.md 的 C2 修正为：约束代码/构建配置/提交信息，文档中记录"最后更新于"允许

### 1.2 修正 BASELINE 验收模式数量
- 把"8个"改为实际数量
- 列出全部11个：selftest/sortcheck/quad-check/like-check/lod-check/world-check/diag-check/link-check/l4-check/persist-check/ui-selftest

### 1.3 归档本次报告（D-b）
- 本次报告写入 `coordination/reports/2026-09-16_coordination建立.md`

---

## 二、项目根统一（D1）

### 2.1 确认 gh_clone2 为唯一权威项目根
- `tmp_a45_review/gh_clone2` 是唯一权威项目根
- `meta-kernel/` 是陈旧分叉（v0.48），需处理

### 2.2 处理 meta-kernel/ 旧分叉
- 先列出74处未提交改动的摘要
- 分类：工程配置 / 文档 / 核心源码 / 宿主探索
- 不丢弃、不合并、不覆盖
- 等用户确认后处理

---

## 三、R1解冻：宿主真实运行调研（D-c）

**只做调研，不改代码。**

### 3.1 查清时间线
- v0.119 声称"窗口宿主能跑"，v0.122后说"不可链接"。中间发生了什么？
- 之前的"能跑"是真实窗口还是离屏自检？
- dlltool 问题是一直存在还是后来出现的？

### 3.2 查清 dlltool 的真实需求
- wgpu/egui 的哪个依赖需要 dlltool？
- Windows GNU 工具链的 dlltool 到底缺什么？
- 有没有替代方案（rust-lld / MSVC target / 其他）？

### 3.3 列出所有可行路径
- 修复 GNU 工具链 / 切换到 MSVC target / 用 rust-lld 替代 / 其他

### 3.4 输出**改动清单**（不改代码），列出可行路径和推荐路径。

---

## 四、阶段二调研：可引导 ISO 路径（D2/D3/D4/D5）

### 4.1 调研 mkosi 可行性
### 4.2 调研裸机 DRM/KMS 渲染（参考 rust_embedded_wgpu）
### 4.3 调研 2015 笔记本硬件适配
### 4.4 设备清单评估（第一梯队：2015笔记本/老台式机；第二梯队：小米电视/先科平板/正方体播放器；第三梯队：iPad 1/老 CD 机/充电宝）
### 4.5 输出**阶段二调研报告**，列出可行路径和改动清单。

---

## 五、验收标准
- C2/BASELINE口径修正
- 验收模式数量修正
- 报告归档
- meta-kernel/ 74处改动摘要
- 宿主真实运行调研报告（改动清单）
- 阶段二调研报告（改动清单）

## 六、汇报格式
按报告模板：基准 / 任务 / 状态 / 验收对照 / 未完成项 / 真实缺陷 / 三量台账 / 边界说明 / 需要用户决策 / 下一步建议

## 七、版本号
自动生成，不手写。
