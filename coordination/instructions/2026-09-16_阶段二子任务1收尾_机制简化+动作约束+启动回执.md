# 指令归档 · 阶段二 子任务1 收尾（机制简化 + 动作约束 + 启动回执）

**基准**：v0.129（指令原文写 v0.135，为顾问侧编号；执行时以 `git rev-list --count HEAD` 为准）
**约束**：按 `CONSTRAINTS.md` 执行
**总规划定位**：阶段二 · 子任务1 收尾（机制简化 + 动作约束 + 启动回执）
**任务类型**：机制修正
**本阶段目标**：把机制从"过度设计"简化为"能跑的最小集"
**归档日期**：2026-09-16

> 本文件是用户下发指令的**忠实结构化归档**（保留全部要求项，未删减）。
> 指令由用户以"活的模板机制讨论稿 + 完整指令 + 应一次性给全局 + 评审的价值与补充判断"四段文本合并下发，
> 其中**第四段（评审的价值 + 补充判断）为最新决定**，与前文冲突时以第四段为准
> （例如：第三段要求"新建 TEMPLATES_CHANGELOG.md 与 OBSERVATIONS.md"，第四段要求"合并为一个文件、删掉这两个"，**按第四段执行**）。
>
> 注：文中涉及 C10 的部分采用中性措辞表述，"须确认动作的完整清单"以 `CONSTRAINTS.md` C10 正文为准
> （本文件为机制登记，不含任何实际操作意图）。

---

## 一、决策 D10–D13

| 编号 | 指令要求 |
|---|---|
| **D10** | **去掉 `continue-on-error`，升为门禁**（依据：CI 上已验证通过） |
| **D11** | **补 C9 的 CI 断言**（unsafe 白名单 + 禁 transmute） |
| **D12 / D15** | **接受现状**：CI 在软件适配器下不断言 ≥30，如实标注 |
| **D13** | **归档** `研发开发/meta-kernel_repair_0907/` |

## 二、CHARTER.md 改为自包含版

结构（九节）：

1. 项目是什么（1 段话）
2. 三方是谁（1 张表）
3. 全部机制（1 张表，每个机制一句话 + 文件位置）
4. 硬规则速查（C1–C10，每条一句话）
5. **启动回执**（回执格式）
6. **动作约束**（须确认动作的清单）
7. 紧急通道（哪些情况可跳过流程）
8. 冲突解决（先协商，后用户拍板）
9. 按需深入（指向其他文件）

**核心要求**：**前八节自包含**——只读 `CHARTER.md` 就知道全局。

## 三、简化活相机制

**把三个文件合并为一个**：

- `TEMPLATES.md` 末尾加"变更记录"节
- 删除 `TEMPLATES_CHANGELOG.md` 和 `TEMPLATES_OBSERVATIONS.md`
- 变更程序保留：**提议 → 试用 3 轮 → 生效/撤销**
- 但不单独建文件维护

**理由（原文）**：机制本身不能成为负担。

## 四、动作约束（新增硬规则）

`CHARTER.md` 加"动作约束"节；`CONSTRAINTS.md` 加 **C10**。

覆盖范围（原文口径，详见 C10 正文）：
外部可见的**外发类动态**（对外公开 / 分享 / 上载）；模型选择与后端切换；任何公开可见的操作；任何不可逆的操作。
配套：自动执行前必须报备，用户可随时叫停。

## 五、启动回执（新增机制）

`CHARTER.md` 加"启动回执"节。WorkBuddy 每次读完 CHARTER.md 后必须回执：
已读 CHARTER.md／当前状态（版本 + ROADMAP 阶段 + 本阶段目标）／本次任务／是否继续。
**回执不出现 → 视为未读。**

## 六、验收标准

- CHARTER.md 自包含版完成
- 活相机制简化为一个文件
- 动作约束加入
- 启动回执加入
- D10–D13 全部执行
- CI 全绿

## 七、汇报格式

按报告模板。**特别标注本次是"机制简化"**；本轮"活相机制"本身是第一次试用，需在报告中标注"试用中"。

## 八、版本号

自动生成，不手写。

---

## 附：前三段文本中已执行部分的回溯核对（用户要求"已执行的对照检查核实"）

| 前文要求 | 状态 |
|---|---|
| R1-b：CI 加 `windows-latest` job 构建宿主 | ✅ 已完成（`host-windows` job，含构建 + 5 项验收） |
| R1-a：修 `--diag-check` 断言（中性点 m=0.5） | ✅ 已完成（新增混合媒体探针覆盖 m<0.5 分支） |
| D1-a：`meta-kernel/` 归档移出 | ✅ 已完成（→ `archive/meta-kernel-v0.48/`，240 文件无删除） |
| D3-a：补 C9（unsafe 边界） | ✅ 已完成（+ `unsafe_whitelist.txt`） |
| 附加：`gene_library.txt` 加 `.gitignore` | ✅ 已完成 |
| 2.1 更新 `TEMPLATES.md`（指令/报告模板 + 无响应 + 接续） | ✅ 已完成（固定相八必带 / 报告十一必带；§三–§五 拆分） |
| 2.2 创建 `ROADMAP.md` | ✅ 已完成 |
| 2.3 更新 `README.md` | ✅ 已完成（本轮改为极简入口） |
| 创建 `CHARTER.md`（机制总纲） | ✅ 已完成（本轮升级为**自包含版**） |
| `llvm-mingw PATH` 写入文档 | ✅ 已完成（`docs/HOST_BUILD.md` + CI 步骤） |
| CI 全绿 | ✅ 已达到（run `35044362728`） |

**结论**：前三段文本的要求**已全部执行**；本轮专注执行第四段（评审 + 补充判断）与 D10–D13。
