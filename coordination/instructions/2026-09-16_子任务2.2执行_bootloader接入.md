# 指令归档 · 2026-09-16 · 子任务2.2 执行（接入 bootloader 0.11）

**基准**：v0.157
**约束**：按 CONSTRAINTS.md 执行
**总规划定位**：阶段二 · 子任务2.2（引导器接入）
**任务类型**：功能
**优先级**：P3
**本阶段目标**：按 D31 决策执行 2.2，接入 bootloader 0.11

---

## 第一部分：D31 四项决策确认

- **D31-a**：`bootloader_api` 只进 boot 层，内核与 nostd crate 保持零依赖 ✅
- **D31-b**：引入 nightly，只在 `meta-kernel-boot/` 内生效，其余仍 stable ✅
- **D31-c**：QEMU 放 CI ✅
- **D31-d**：验证路线选 A（零 unsafe，帧缓冲 + QEMU screendump）✅

## 第二部分：关于 Git 历史的说明（用户已知悉）

- Git 已推送内容撤不回（Git 特性，非 bug）
- 当前版本已做骨架化（`workflows/sub` 改为指针、`ASSETS_INVENTORY` 改为指针）
- 未来版本继续执行拆分机制（骨架-实现分离）
- 语言替换等盘点完成后对话再定
- **不需要做 force push，不需要重写历史**

## 第三部分：执行 2.2

### 3.1 按清单执行（7 新增 + 2 改动）
### 3.2 三条先决条件：nightly 只关在 boot 目录内 / C1 边界 / QEMU 放 CI
### 3.3 验收标准
- `cargo build --target x86_64-unknown-none` 成功
- bootloader 0.11 接入成功
- **QEMU screendump 验证通过（CI）**
- 内核 411 项测试仍全绿
- nostd crate 60 项测试仍全绿
- CI 全绿

## 第四部分：表格编辑纪律（已固化，本轮继续遵守）
追加时必含既有行锚点 / 编辑后必 grep 计数 / 编号连续性是判据 / 同文件禁并行编辑

## 第五部分：保密标记
按 C12 标注

## 第六部分：汇报格式
按报告模板，含全景状态卡、保密标记、三量台账、边界说明

## 第七部分：版本号
自动生成，不手写

---

## 执行结果（WorkBuddy 回填）

| 项 | 结果 |
|---|---|
| D31 四项 | ✅ 全部执行；**口径修正采纳**（GRUB/Limine → `bootloader` 0.11） |
| 7 新增 + 2 改动 | ✅ 落地（两处偏差已上报：① 未建 `rust-toolchain.toml`（因 R21，钉 host 名会同时破坏本机与 CI）② 内核拆为 `kernel/` 子 crate（artifact 依赖需要"内核 ELF"这一构建产物）） |
| `cargo build --target x86_64-unknown-none` | ✅ 成功 |
| bootloader 0.11 接入 | ✅ `bootloader 0.11.17`；产出 `boot-bios.img` 3,638,272 B、MBR 签名 `55aa` |
| **QEMU screendump（CI）** | ✅ **run `35110443157`**：截屏 **1280×720**、**25 采样点全为 `(0,255,0)`**、`[PASS] 引导成功且内核自检通过`、断言 exit=0 |
| 内核 411 项 | ✅ **411 passed / 0 failed** + **源码未改动** |
| nostd 60 项 | ✅ **60 passed / 0 failed** + **源码未改动** |
| CI 全绿 | ✅ 三 job 全 success（首跑 `35109644750` 红 → 修 R23 后全绿） |
| 表格编辑纪律 | ✅ 遵守；**并因此当场抓到我自己一次失误**（第 5 次"追加变替换"，`.gitignore` 丢 `phase1-test.log`）与 **R15 空号** |
| 报告 | `reports/2026-09-16_子任务2.2执行_bootloader接入.md` |
| 清单 | `reports/2026-09-16_子任务2.2_改动清单_待确认执行.md` |
