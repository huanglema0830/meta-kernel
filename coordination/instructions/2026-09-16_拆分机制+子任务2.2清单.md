# 指令归档 · 2026-09-16 · 拆分机制（指针-实现分离）+ 子任务2.2

**基准**：v0.155
**约束**：按 CONSTRAINTS.md 执行
**总规划定位**：阶段二 · 子任务2.1 收尾 + 机制建设（拆分机制）
**任务类型**：机制
**优先级**：P3（机制落地）+ P1（D31 待决策）
**本阶段目标**：建立拆分机制（指针-实现分离）

---

## 第一部分：D30 决策确认

- **D30-1 仓库可见性**：**用户确认：选① 保持现状 + 清单留本地**
- **D30-2 替换档位**：**用户确认：选 C 档（只做增量换壳）**
- **R20 记录**：仓库为 PUBLIC，已记入 BASELINE

## 第二部分：建立拆分机制（指针-实现分离）

### 2.1 设计原则
| 层 | 位置 | 内容 | 保密级别 |
|---|---|---|---|
| **骨架层** | 公开仓库 coordination/ | 只写"做什么" | 公开 |
| **实现层** | 私有文件夹（仓库外） | 写"怎么做" | 保密/机密 |

### 2.2 约定
1. 环境变量 `PRIVATE_ASSETS` 指定私有文件夹位置
2. 公开文件里引用私有实现：`具体见 {PRIVATE_ASSETS}/xxx.md`（**示例**：格式示意，非真实目标，故按"示例"豁免存在性校验）
3. WorkBuddy 需要细节时，读私有文件夹
4. 私有文件夹永不在公开仓库

### 2.3 更新文件
- `coordination/ASSETS_INVENTORY.md`：改为指针（WorkBuddy 上一轮已实现）
- `coordination/workflows/`：框架文件里只写"做什么"，具体实现引用私有文件
- 私有文件夹：存放具体实现

### 2.4 语言替换
**语言替换等盘点完成后，通过你我对话再定。本轮不做。**

## 第三部分：进入子任务2.2

- 优先级 **P3**（不受 D30 阻塞）
- 需要新增 `[[bin]]`；**需要 panic handler**
- 目标：能被 GRUB/Limine 引导
- **按 C4 先出改动清单，等用户确认后执行**

## 第四部分：验收标准
- D30 决策确认
- 拆分机制建立
- 私有文件夹位置确认
- 子任务2.2 改动清单产出
- CI 全绿

## 第五部分：汇报格式
按报告模板，含全景状态卡、保密标记

## 第六部分：版本号
自动生成，不手写

---

## 执行结果（WorkBuddy 回填）

| 项 | 结果 |
|---|---|
| D30 决策确认 | ✅ `BASELINE.md` D30 ✅ 已确认（①保持现状）+ 新增 **D30-b**（替换取 C 档）；R20 如实标注"**未解除**（已推送内容不可撤回），仅停止扩大" |
| 拆分机制建立 | ✅ `TEMPLATES.md` **§十**（两层 + 4 条约定 + 引用格式 + 5 条检查清单）；`CHARTER.md` **机制 17** + §九；`TEMPLATES §7.2` 登记 **T-012** |
| 私有文件夹位置确认 | ✅ 已建立＝**仓库根目录的兄弟目录**；判据用 `realpath` 前缀比较 → 实测"**在仓库外**" |
| 工作流改指针 | ✅ `universal_workflow.md` 分层保密节改写；`sub/01..08` 的"[占位]"→ `{PRIVATE_ASSETS}/...` 指针；**8 个实现文件已建于私有文件夹**（内容"待填"，**不猜测**） |
| 子任务2.2 改动清单 | ✅ 已产出（7 新增 + 2 改动）；**含一条口径纠正**：建议由"GRUB/Limine"改为 **`bootloader` 0.11**（官方文档：内核用 `x86_64-unknown-none` 即可，无需自定义 target / build-std） |
| 语言替换 | ⏸ **本轮未做**（按指令） |
| 代码改动 | ⏸ **零**（2.2 按 C4 等确认） |
| CI | ⏳ 提交后回填 |
| 报告 | `reports/2026-09-16_拆分机制建立+子任务2.2清单.md` |
| 2.2 清单 | `reports/2026-09-16_子任务2.2_改动清单_待确认执行.md` |
