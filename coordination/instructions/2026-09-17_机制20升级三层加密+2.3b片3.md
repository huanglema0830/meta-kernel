# 归档指令 · 2026-09-17（第四轮）：机制 20 升级三层+加密 + 2.3b 片3 + 首夜核查

> 来源：用户指令（**注：原文在 §1.4「内部使用流程」处被截断**，§1.4 之后各节未收到）
> 基准：v0.174（自动生成）｜约束：按 `CONSTRAINTS.md`
> 总规划定位：机制 20 三层+加密改造 ＋ 2.3b 片3 ＋ 夜间首夜汇报
> 任务类型：机制修正 ＋ 推进

## 一、机制 20 升级：三层 + 加密 + 代码代号（**原文，逐条落实**）

### 1.1 最终结构（原文表）

| 层 | 放什么 | 位置 | 加密 |
|---|---|---|---|
| **公开层** | 英文名 + 编号 + 功能说明 + 指针 | 仓库内 | 无 |
| **索引层** | 编号 + 英文名 + **代码代号** + 指针 | 仓库外 | ✅ |
| **内容层** | 全拼 + 原名 + 原始内容 | 仓库外 | ✅ |

### 1.2 核心规则（原文，5 条）

1. **英文名必须是功能描述型**：禁止拼音转写（`<拼音>` ❌），禁止生僻造词。
2. **公开层保留中文功能说明**：中文读者看说明能懂。
3. **代码位置用代号**：如 `CODE-007`，真实路径只在内容层。索引层即使泄露，也不知道代码在哪。
4. **编号是稳定锚点**：换名不改编号，索引层和内容层不动。
5. **索引层不得含全拼/原名**：全拼和原名只在内容层。

### 1.3 加密方案（原文，5 条）

- **加密对象**：索引层 + 内容层（仓库外私有）。
- **加密方式**：AES-256。
- **密钥存放**：本地，与 `PRIVATE_ASSETS` 同级，**绝不进仓库**。
- **解密时机**：内部查阅时按需解密，用完即弃。
- **性能**：文档级操作，对运行时零影响。

### 1.4 内部使用流程

（**原文在此处截断**，未收到）

## 二、本轮交付（对应落实）

| 原文要求 | 交付 | 证据 |
|---|---|---|
| 三层结构 | `TERMS.md`（公开）／`terms/INDEX.md.enc`（索引）／`terms/TERM-00N.md.enc`（内容） | 门禁 syntax+resolve 双 PASS |
| 英文名功能描述型 | `Fine-Granularity Model`／`Unit Composition`／`Pattern Admission Gate`／`Tri-State Classifier`／`Parameter Store` | 公开层判据：原名/全拼/真实路径 **0 命中** |
| 代码代号 | `CODE-001…005`；真实路径只在内容层 | 索引层不含全拼/原名 |
| AES-256 | **AES-256-GCM**（AEAD；CBC 的"被篡改"不可判） | 4 条机器判据全过 |
| 密钥不进仓库 | `PRIVATE_ASSETS` **同级**目录 | 仓库内含密钥文件 **0 个** |
| 用完即弃 | `dec` 只打到 stdout；`purge` **默认不执行**（C10） | 明文仍在（待 **D39**） |

⚠️ **一处必须记录的偏离**：原文写「AES-256」（未指定模式）⇒ 取 **GCM** 而非 CBC，理由＝**CBC 无法判定"被篡改"**，与项目"判据必须能证伪"的要求冲突。已记入 **D38**。

## 三、待用户拍板（本轮新增）

- **D37**：`ontology` 的 `HashSet→BTreeSet` 类型替换**追认**（`no_std` 无 `HashSet`，编译级证据齐）
- **D39**：私有层「删明文」`purge` 放行（**不可逆**：密钥丢失＝永久不可读）
- 遗留：**D35**（`docs/`+`deploy/` ⛔ 换壳范围）｜**D36**（术语换壳范围）

## 四、首夜核查（如实）

- 21:00 / 23:00 两条任务 **ACTIVE**；**首次触发今夜**；**首夜未发生**（无产物、无 commit）。
