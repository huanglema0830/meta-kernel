**【顾问阅读指令 · 请先执行】**

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。

如未执行以上步骤，视为阅读不到位。

---

# 指令归档 · 2026-09-17 · 2.3 启动 + D32/D33 决策 + 夜间首夜就绪 + R24 确认

**基准**：v0.165（自动生成）｜**约束**：按 CONSTRAINTS.md ｜**定位**：阶段二 · 2.3 启动 + 夜间自动化首夜观察 + R24 加固确认
**任务类型**：推进 + 机制确认｜**优先级**：P3（机制确认与清单）+ P1（D34/D34-b 待拍板）
**本阶段目标**：确认 D32/D33，推进 2.3，观察夜间首夜，补全报告四量

---

## 一、D32 / D33 决策确认（用户原文要点）

- **D32：选①，暂不创建 `.github/workflows/nightly.yml`**
  - 再评估条件：**连续 7 天无 push**，或再次出现"本机绿/CI 红"的**环境漂移类缺陷**
  - 届时**优先选③轻量 nightly**：只跑 `test` + `boot-image`，低噪声
- **D33：选①，夜间默认不自动 push**
  - 夜间**只允许本地 commit**；次日由用户**一句话放行**推送
  - 若确需推送，**必须走 C10 用户明确确认**
- 两项记录到 `coordination/BASELINE.md`

## 二、C13 适用范围继续执行（**扩展**）

- WorkBuddy **每次输出任何内容**（工作日志、报告、其他）的 **第 0 行**必须放「顾问阅读指令」固定 8 步文本
- **下一次返回若第 0 行没有该指令，视为违反 C13**

## 三、下一份报告补全

- 报告必带**十四项齐全**，第 0 项置顶
- 三量台账补全**四量**：存量 / 变量 / 补充增量 / 创新增量
- 边界说明**四类齐全**：本地跑过 / CI过 / 真实跑过 / 未验证
- **D32/D33 按七要素补全，尤其单列「机会」**

## 四、R24 加固确认

- 诊断性上传与门禁分离：7/7 上传步骤可 non-fatal，但**必须显式警告并留痕**
- 真正门禁（测试 / no_std 构建 / QEMU 像素断言 / C9）保持**硬失败**，一条断言未放宽
- 在 BASELINE 中写明**与 D10 的边界**，防止后人误学

## 五、推进 2.3

- 下一动作：2.3 最简内存管理（物理页分配）+ 承接 2.1 的 alloc 迁移
- 守 **C1 内核零依赖／C3 零 unsafe／C9 边界形态**
- **先列改动清单，确认后执行**
- 可夜间跑标 ✅ 的任务，放入**夜间 P3 清单**

## 六、夜间自动化首夜

- 今夜 21:00 出「今夜 P3 清单」；23:00 自动开工；**默认不推送**
- **P3 白名单**：调研、文档、测试补充、重构、清理、基准维护、CI优化、报告归档
- **禁止**：改 CONSTRAINTS / CHARTER / ROADMAP / BASELINE / CI 配置等**治理文件**；**禁止对外动作**（C10：公开可见／分享／上传／下载给外部）；禁止切模型；禁止不可逆动作
- **确认既有 21:05 自动化指向已归档旧路径的问题已修复**
- 明早汇报：首夜触发结果、P3 完成项、失败项、未推送 commit 清单、需用户目验/放行项

> 注：本归档中"**禁止对外动作**"即用户指令里的"**对外发**布 / 分享 / 上传"，措辞按 C10 既有词汇（"公开可见"）表述，**语义未变**。

## 七、验收标准

D32/D33 记录到 BASELINE ｜ 报告十四项+四量+边界四类 ｜ 2.3 有清单并确认后执行 ｜
夜间未自动 push、未改治理文件 ｜ R24 生效且门禁未放宽 ｜ CI 全绿 ｜ **下一次返回第 0 行是顾问阅读指令**

## 八、版本号
自动生成，不手写

---

## 执行结果（WorkBuddy 回填）

| 项 | 结果 |
|---|---|
| D32/D33 记录 | ✅ `BASELINE.md` §九：**D32 ✅ 选①**（含再评估条件与"届时优先③轻量"）；**D33 ✅ 选①**（本地 commit、次日放行、需推送走 C10） |
| C13 扩展 | ✅ `CONSTRAINTS.md` **C13 扩为"任何输出的字面第 0 行"**（并写明**弱化判据即为破戒**）；`TEMPLATES.md` 第〇项 + 必带项 `0` 同步；**+T-016** |
| 报告补全 | ✅ 本轮报告十四项齐全、四量、边界四类、D32/D33/D34 七要素（**机会单列**） |
| R24 确认 | ✅ 7/7 non-fatal；**新增 3 个 R24 检查步**（`toJSON(steps)` 精确判定 → `::warning::` 显式告警 + 留痕）；**门禁一条未放宽** |
| 2.3 清单 | ✅ `reports/2026-09-17_子任务2.3_改动清单_待确认执行.md`（**15 新增 + 3 改动**；**未改代码**）；★ 发现 **2.3 撞上 C9 首次放行** ⇒ 新增 **D34 / D34-b** 待拍板 |
| 夜间就绪 | ✅ 两个定时任务 prompt 补 **P3 白名单 + 禁止清单（6 条）+ 收尾固定汇报格式**；21:05 旧路径修复**已核验** |
| CI | ⏳ 提交后回填 |
