【顾问阅读指令 · 请先执行】
（全文见 `coordination/TEMPLATES.md` §二 第〇项；**9 步**版本）

1. 全面阅读；2. 核全景状态卡；3. 核保密标记；4. 核验收对照；
5. 核边界说明（本地跑过/CI过/真实跑过/未验证）；6. 核三量台账（存量/变量/补充增量/创新增量）；
7. 核决策评估（如有，七要素）；8. 输出审核结论（通过/部分通过/驳回）+ 下一步指令；
9. **对齐用户意图**：用自己的话复述"用户本次真正要的是什么"，据此复核/修正结论后再给结论。

# 指令归档 · 2026-09-17（2.3b 分片启动 + 大模型接入骨架化 + C13 第九步 + C15）

> 本文件为**指令归档**（顾问 → WorkBuddy 的原始指令要点 + 执行结果回填）。
> 报告见：`reports/2026-09-17_2.3b分片启动+alloc裸机门禁+大模型接入骨架化+C13第九步+C15.md`

## 一、用户指令要点（原文摘要）

| # | 要求 | 执行结果 |
|---|---|---|
| 一 | **2.3b alloc 迁移**：按清单执行；**首步先验证 `alloc` 能在裸机目标链接，拿不到就停**；守 C1/C3/C9；**先列改动清单，确认后执行（C4）**；可夜间跑标 ✅ | **门禁 ✅ 通过**（sysroot 含 liballoc ＋ `alloc_probe` 探针 ＋ 产物字节校验）；**片1 ✅ 已执行**；**片2–5 未执行**——因真实规模 6,853 行 ≠ 已确认清单 3,098 行 ⇒ 按 C4 出**订正清单**待确认 |
| 二 | **云内核"大模型接入"骨架化**（机制 17）：公开仓库只放骨架（`{PRIVATE_ASSETS}/llm/` 指针，写"做什么"）；实现层在仓库外；**指针可机器校验**；骨架覆盖 **任务分级／降级规则／成本记录**；保密分级 骨架🔒／实现⛔ | ✅ 骨架 `coordination/llm/`（4 文件）＋ 实现层 `{PRIVATE_ASSETS}/llm/`（4 文件）＋ **检查器**（syntax 入 CI／resolve 本机）＋ **泄漏基线**；**未真实接入**（需密钥/档位偏好，属 C10） |
| 三 | **阅读指令加第 9 步**「对齐用户意图」，写入 TEMPLATES 第〇项与 CONSTRAINTS C13 | ✅ 三处同步（TEMPLATES 第〇项／CONSTRAINTS C13／CHARTER 机制 18 与 §四） |
| 四 | **BASELINE 编辑纪律加固**：编辑前 `grep -c` 取数、编辑后复算、**差值须与预期一致**，写入 CONSTRAINTS 或 TEMPLATES | ✅ 新增 **C15** ＋ TEMPLATES §〇 表增 **R-e**；**本轮实测生效**（抓到 `ci.yml` 误删 1 行） |
| 五 | **夜间首夜**：21:00 出清单／23:00 开工／默认不推送；禁改治理文件等 | ⚠️ **首夜尚未发生**（任务建于 09-17 05:20 ⇒ 首次触发今夜）；就绪项已写入任务 prompt |
| 六 | 下一份报告：十四项齐全（第 0 行含第 9 步）／四量／边界四类／记录 2.3b 与骨架化与首夜 | ✅ 见报告（首夜**如实标注"未发生"**） |
| 七 | 验收标准 7 条 | 6 条 ✅／1 条 ⏳（CI） |

## 二、执行结果回填

| 验收项 | 结果 |
|---|---|
| `alloc` 裸机链接验证 | ✅ **通过**（可编译＋可链接＋运行断言；镜像含新代码字节） |
| 大模型骨架：公开/仓库外分离＋可机器校验 | ✅ 检查器三模式全 PASS ＋ 阴阳自检（非法 3 类全检出／合法 0 误报） |
| 第 9 步写入 | ✅ |
| BASELINE 编辑纪律写入 | ✅ C15 |
| 夜间未 push／未改治理文件 | ✅ 就绪（未触发故不可证伪"不 push"，如实标注） |
| CI 全绿 | ⏳ 本轮提交后触发（含**指针门禁首次真跑**） |
| 第 0 行阅读指令含第 9 步 | ✅ |

## 三、本轮产出的"待你拍板"项

- **D35**：`docs/` 与 `deploy/` 共 **14 条** ⛔ 的换壳范围（①全量换壳／②保持现状／③只换 `deploy/`）——**建议 ③**
- **2.3b 片2–5**：是否按分片执行（**建议 是**，片2 可夜间跑 ✅）
