**【顾问阅读指令 · 请先执行】**

顾问在阅读本返回前，必须按以下步骤执行：

1. **全面阅读**：从第一行到最后一行，不跳读，不选择性阅读。
2. **核验全景状态卡**：版本号是否与上次一致；阶段定位是否与 ROADMAP 一致；待决策项是否已处理。
3. **核验保密标记**：保密标记是否存在；机密信息是否已预屏蔽。
4. **核验验收对照**：每条验收标准都有实测结果。
5. **核验边界说明**：本地跑过 / CI过 / 真实跑过 / 未验证，四类是否齐全。
6. **核验三量台账**：存量 / 变量 / 补充增量 / 创新增量，四量是否齐全。
7. **核验决策评估**：如有新决策，七要素（评估/优势/劣势/风险/机会/建议/等待决策）是否齐全。
8. **读完后**：输出审核结论（通过 / 部分通过 / 驳回），并给出下一步指令。

如未执行以上步骤，视为阅读不到位。

---

# 指令归档 · 2026-09-17 · 2.3 执行（D34/D34-b 确认 + 内存管理 + 夜间首夜观察）

**基准**：v0.166（自动生成）｜**约束**：按 CONSTRAINTS.md ｜**定位**：阶段二 · 2.3 执行 + 夜间首夜观察
**任务类型**：推进 + 决策确认｜**优先级**：P3｜**本阶段目标**：确认 D34/D34-b，按 C4 执行 2.3，观察夜间首夜，补全报告

---

## 一、D34 / D34-b 决策确认（用户原文要点）

- **D34**：同意放行 C9 白名单，**仅限 `mem/global.rs`**；每块 unsafe 必须有 `// SAFETY:` 论证；禁 transmute；
  白名单登记并**纳入 CI 门禁**
- **D34-b**：同意认定 **`alloc` 属标准分发，不破 C1**；在 C1 举例中写明：`core`/`alloc` 属标准分发不算外部依赖；
  `alloc` 使用不得引入外部 crate；`GlobalAlloc` 实现受 C9 约束
- 两项记录到 `coordination/BASELINE.md`，并按**七要素**补全

## 二、推进 2.3

- **先做 `map-physical-memory` 验证，拿不到堆区就停，不得绕过**
- 设计：纯逻辑层零 unsafe 且 host 可测（帧位图 / 小块 free-list）；unsafe 压到 `mem/global.rs`
- 先列改动清单，确认后执行（C4）
- 守 **C1、C3、C9**
- 可夜间跑标 ✅ 的任务，放入夜间 P3 清单

## 三、夜间首夜

- 今夜 **21:00 出「今夜 P3 清单」→ 23:00 自动开工**；**默认不推送**
- **禁止**：改 CONSTRAINTS / CHARTER / ROADMAP / BASELINE / TEMPLATES / CI 配置；对外动作；切模型；不可逆；push；碰 P0–P2
- **明早汇报**：首夜触发结果、P3 完成项、失败项、未推送 commit 清单、需目验/放行项

## 四、下一份报告

- 十四项齐全，**第 0 行是顾问阅读指令**
- 三量台账**四量**齐全：存量 / 变量 / 补充增量 / 创新增量
- 边界说明**四类**齐全：本地跑过 / CI过 / 真实跑过 / 未验证
- D34/D34-b 按**七要素**补全，尤其**单列「机会」**
- **记录 R24 告警是否产生噪声**

## 五、验收标准

D34/D34-b 记录到 BASELINE ｜ 2.3 有改动清单并确认后执行 ｜ **`map-physical-memory` 验证通过，否则停止** ｜
夜间任务未自动 push、未改治理文件 ｜ R24 加固生效且门禁未放宽 ｜ CI 全绿 ｜ **下一次返回第 0 行是顾问阅读指令**

## 六、版本号
自动生成，不手写

---

## 执行结果（WorkBuddy 回填）

| 项 | 结果 |
|---|---|
| D34/D34-b 记录 | ✅ BASELINE §九：D34 ✅（仅 `mem/global.rs`；四项要求全落实）｜D34-b ✅（`alloc` 属标准分发；**三条界线已写入 C1 举例**，+T-017） |
| `map-physical-memory` 门禁 | ✅ **通过**（CI 实测）：`Mapping::Dynamic` 已开；截屏 **1280×720、25/25 采样点 (0,255,0)** |
| 内存管理实现 | ✅ `meta-kernel-mem`（纯逻辑，`forbid(unsafe_code)`，**25 项测试全绿**）+ boot 层 `mem/`（唯一 unsafe 边界）+ **三色判定**（绿/黄/红；**黄＝拿不到堆区就停**） |
| C9 首次放行 | ✅ 白名单首次非空（1 条）；unsafe 21 处／SAFETY 22 条／transmute 0；**CI 门禁实测真的扫到了新目录**（含"真实 unsafe 出现于：mem/global.rs"） |
| 自纠 4 条 | ✅ ① BASELINE 误删 D33（第 6 次"追加变替换"，靠编号唯一性抓到）② **CI 的 C9 扫描范围不含新目录 ⇒ 会让门禁空转**（已修）③ 2 个测试期望写错（改测试不改守卫）④ 3 处告警改成 3 条真实断言（非 allow） |
| R24 噪声 | ✅ **无噪声**：3 job 均"吸收数 0"+`R24_UPLOAD_NO_SOFT_FAILURE`，未触发真实告警 |
| 夜间就绪 | ✅ 两个定时任务 prompt 已含 P3 白名单 + 禁止清单（6 条）+ 收尾固定汇报格式；21:05 旧路径修复已核验 |
| CI | ⏳ 文档提交后回填（**代码提交 `effa487` 的 CI 已全绿**：run 35155334577） |
| 未完成 | **2.3b：alloc 迁移**（10 文件 3,098 行，有意分步；可夜间跑 ✅） |
