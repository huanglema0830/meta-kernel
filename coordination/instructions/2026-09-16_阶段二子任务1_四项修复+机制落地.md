# 指令归档 · 2026-09-16

**基准**：v0.127（先读 coordination/BASELINE.md）
**约束**：按 coordination/CONSTRAINTS.md 执行
**总规划定位**：阶段二 · 子任务1（mkosi环境搭建前的基础修复）
**任务类型**：修复 + 机制落地
**本阶段目标**：把"真实跑通的能力"变成可自动回归，并把对话/报告/接续机制固化
**版本号**：自动生成，不手写

---

## 一、立即执行的四项修复

### R1-b（最高优先）：CI 加 windows-latest job 构建宿主
- **问题**：CI 只有一个 ubuntu-latest job，`field-render` 命中 0，宿主从未被 CI 构建。"CI为权威验收"不成立。
- **要求**：新增 `windows-latest` job；构建 `host/field-render`；跑 `--selftest` / `--diag-check` / `--sortcheck`；
  把今天真实跑通的验收标准（开窗/上屏纹理回读/方差>1/帧率>30）固化为 CI 断言；
  PATH 前置 llvm-mingw/bin（写成 CI 步骤）。

### R1-a：修 `--diag-check` 断言
- **问题**：`adjust = gain×(2m−1)`，中性点 m=0.5，脚本却拿 m=0.585 当"低匹配"。
- **要求**：断言与公式中性点对齐；加注释说明中性点；验收：`--diag-check` PASS。

### D1-a：meta-kernel/ 归档后移出
- **要求**：移到 `archive/meta-kernel-v0.48/`（保留，不删）；根目录不再有陈旧分叉；在 BASELINE.md 记录归档位置。

### D3-a：补 C9（unsafe 边界）
- **要求**：CONSTRAINTS.md 增补 C9：unsafe 允许形态（边界集中 + `// SAFETY:` 论证 + 禁 transmute）；
  门禁断言（`grep -rn "unsafe" src/` 结果必须在白名单内）；说明阶段二 DRM/KMS 需要 unsafe 但必须合规。

### 附加：gene_library.txt 加 .gitignore
- 把 `host/field-render/gene_library.txt` 和 `.bak` 加入 .gitignore。

## 二、机制落地：模板体系写入 coordination/

### 2.1 更新 `coordination/TEMPLATES.md`
加入：**指令模板**（五话术：基准/约束/总规划定位/本阶段目标/汇报格式）、
**报告模板**（七话术：基准/总规划定位/状态/验收对照/三量台账/边界说明/需要决策+下一步）、
**无响应推进机制**（P0–P4 分级；P1/P2 的 T+0/30min/2h/4h 流程；P3 清单）、
**断裂接续机制**（开工 7 步只读、报告结束 4 步归档、接续话术）。

### 2.2 创建 `coordination/ROADMAP.md`
三阶段总览（阶段一可安装浏览器 ✅／阶段二可引导ISO 🚧／阶段三可安装操作系统）、
阶段二六个子任务、设备三梯队、M1–M7 里程碑、风险与应对。

### 2.3 更新 `coordination/README.md`
阅读顺序加入 ROADMAP.md 与 reports/ 最新文件。

## 三、验收标准
- R1-b：CI 加 windows-latest job，构建宿主，跑验收，全绿
- R1-a：`--diag-check` PASS
- D1-a：meta-kernel/ 移到 archive/
- C9：CONSTRAINTS.md 增补
- .gitignore：加 gene_library.txt
- TEMPLATES.md：指令模板 + 报告模板 + 无响应机制 + 断裂接续机制
- ROADMAP.md：创建
- README.md：更新阅读顺序
- llvm-mingw PATH：写入文档
- CI 全绿

## 四、汇报格式
按报告模板（基准/总规划定位/任务/状态/验收对照/未完成项/真实缺陷/三量台账/边界说明/需要用户决策/下一步建议）

## 五、版本号
自动生成，不手写

---

**归档说明**：本文件为**忠实结构化归档**（保留全部验收标准与要求条目，未删减要求项）。
原始指令全文由发起人以长文本下发。
