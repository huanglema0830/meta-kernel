# 指令归档 · 子任务2 路线修正 + 通用工作流 + D-a + R2 排查

**基准**：v0.138
**约束**：按 `CONSTRAINTS.md` 执行
**总规划定位**：阶段二 · 子任务1 收尾 + 子任务2 路线修正 + 机制建设
**任务类型**：机制 + 修复 + 调研
**本阶段目标**：清除收尾项，修正子任务2 路线，建立通用工作流
**归档日期**：2026-09-16

> 本文件是用户下发指令的**忠实结构化归档**（保留全部要求项，未删减）。

---

## 第一部分：回应上轮待决策

| 编号 | 用户决策 |
|---|---|
| **D-a** | **做**。在 `host-windows` job 加 `cargo test --bin field-render`。理由：35 项测试不在 CI 里＝没有测试；R18 就是"CI 不跑它"的直接后果 |
| **D16** | **继续试用满 3 轮**，不急着决定 |
| **R2** | **做**。排查方向：网络环境（代理/防火墙/SNI 拦截）、SSH 通道（`ssh.github.com:443`）、直连（绕过代理）、输出根因报告 |
| **子任务2 路线修正** | **放弃 mkosi，走元内核自举路线**（详见下） |

### 子任务2 路线对照

| 原路线 | 新路线 |
|---|---|
| mkosi 生成 Linux 发行版镜像 | **元内核自己成为内核** |
| 依赖 systemd / Linux | **无 Linux 依赖** |
| X11 / Wayland 渲染 | **DRM/KMS 直接渲染** |

| 子步 | 内容 | 参考 |
|---|---|---|
| 2.1 | 元内核编译为 `x86_64-unknown-none`（no_std） | purpleK2/kernel |
| 2.2 | 接入 GRUB/Limine 引导器 | Dan-OS、OusmOS |
| 2.3 | 实现最简内存管理（物理页分配） | 标准内核 |
| 2.4 | 实现 DRM/KMS 驱动，直接输出场域画面 | rust_embedded_wgpu |
| 2.5 | `field-render` 作为第一个用户空间进程 | 标准内核 |
| 2.6 | 通过 ACPI/CPUID 读取硬件信息（软件层面识别） | 标准内核 |
| 2.7 | 生成可引导 ISO，QEMU 验证 | Dan-OS |

**硬件识别第一阶段只做软件层面**（读 ACPI 表、CPUID），**不做场域指纹**。

## 第二部分：新增通用工作流

### 2.1 目录结构

```
coordination/workflows/
  universal_workflow.md      ← 通用工作流（框架层，公开）
  sub/
    01_field_sampling.md          ← 第 1 步
    02_four_state_analysis.md     ← 第 2 步
    03_time_evolution.md          ← 第 3 步
    04_scale_decomposition.md     ← 第 4 步
    05_local_dynamics.md          ← 第 5 步
    06_self_organization.md       ← 第 6 步
    07_real_evolution.md          ← 第 7 步
    08_phase_diagram.md           ← 第 8 步
```

### 2.2 universal_workflow.md 要求

- **定位**：独立文件、不嵌入任何程序、任何系统读它就能用；不同系统调用时**用自己的语言匹配**
- **八步表**（步／通用名／物理名／做什么／子模块）
- **调用协议**：调用方只需提供 `目标、边界、约束`；**工作流不关心调用方是谁**
- **分层保密**：框架层＝公开；映射层＝半公开；算法层＝保密（在子模块）；数据层＝保密

### 2.3 子模块模板

每步子模块按固定格式：**框架（公开）**〔做什么／输入／输出／与上下步的关系〕+ **算法层（保密·占位）**〔具体公式／阈值／权重／边界条件〕+ **调用示例（公开）** + **变更记录**。

### 2.4 更新 CHARTER.md

- 机制清单新增一行：通用工作流机制 → `workflows/universal_workflow.md` → 八步场域演化、子模块化、分层保密
- 启动引导新增：`workflows/universal_workflow.md` —— 通用工作流（**按需调用**）

### 2.5 更新 BASELINE.md

修正子任务2 路线（mkosi → 元内核自举）。

## 第三部分：优先级排序（用户给定）

| 优先级 | 任务 |
|---|---|
| **1** | D-a：CI 测试步 |
| **2** | R2：SNI 排查 |
| **3** | 通用工作流独立文件化 |
| **4** | 子任务2 路线修正（更新 BASELINE） |
| **5** | 子任务2 启动（元内核自举） |

## 第四部分：验收标准

- D-a：CI 加 `field-render` 测试步，全绿
- R2：SNI 根因报告
- 通用工作流：`universal_workflow.md` + 8 个子模块建立
- `CHARTER.md` / `BASELINE.md` / `README.md` 更新
- 子任务2 路线修正记录
- CI 全绿

## 第五部分：汇报格式

按报告模板；特别标注：本轮处理了哪些上轮待决策／通用工作流是否建立／子任务2 路线是否修正／新机制标 🔬 试用中。

## 第六部分：版本号

自动生成，不手写。
