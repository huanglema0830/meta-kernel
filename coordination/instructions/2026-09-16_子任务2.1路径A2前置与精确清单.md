# 指令归档 · 子任务2.1 路径 A-2（前置 + 精确清单）

**基准**：v0.147｜**约束**：按 CONSTRAINTS.md（C1–C12）执行
**总规划定位**：阶段二 · 子任务2 · 2.1（路径 A-2）
**任务类型**：功能 + 环境前置｜**本阶段目标**：完成 2.1 前置、产出 A-2 改动清单（**按 C4 等确认后执行迁移**）
**归档日期**：2026-09-16

> 本文件是用户下发指令的**忠实结构化归档**。

## 第一部分：决策确认

- **D26＝A-2（核心集）** ✅ 用户确认
- **D19＝SSH 切换完成** ✅（推送 13 秒，此前最坏 14 分钟）

## 第二部分：2.1 · 路径 A-2

**2.1 前置操作**：`rustup target add x86_64-unknown-none`

**2.2 改动清单（先列清单，等确认后执行）**——须含：
迁移哪些模块（~15 个核心模块）／7 个数学函数实现方案（sqrt 位技巧+牛顿、exp 范围归约、ln 2·atanh 级数、
sin/cos Cody-Waite 归约+泰勒、atan2 多项式+四象限、powi 平方-乘）／**拆分方案**（计算留 no_std，接口留 std）／
预计新增的文件／预计的依赖关系／验收标准

**2.3 拆分原则**：
- 计算逻辑留 no_std：核心算法、数学函数
- 接口留 std：序列化、文件 IO、GeneLibrary 读写
- **只拆接口不拆算法**：避免逻辑漂移

**2.4 验收标准**：
- `cargo build --target x86_64-unknown-none` 成功
- 内核测试仍全绿（std 版本）
- 数学函数精度 <1e-9（区间 10⁵ 点 + 边界值 + 大参数专项）
- **sin/cos 大参数若达不到 1e-9，回来问用户，绝不放宽门线**
- CI 全绿

## 第三部分：执行顺序

先出改动清单 → 等用户确认 → 执行迁移 → 验证编译 → 输出结果

## 第四部分：保密标记

报告时按 **C12** 标注保密级别。

## 第五部分：汇报格式

按报告模板，含：全景状态卡／保密标记／三量台账／边界说明／决策评估（如有新决策）

## 第六部分：版本号

自动生成，不手写。

---

## 附：执行中的关键判断（如实记录）

1. **本轮只做前置 + 清单，不迁移代码**（用户第三部分明确"先出清单，等确认"）：
   仅执行了指令 §2.1 明确给出的前置命令 `rustup target add x86_64-unknown-none`（→ installed），
   **未创建任何文件、未修改原 core、未执行任何构建**。
2. **实测范围与预期有差，如实上报并请确认**：用户预期"~15 模块"，逐模块实测后**能干净迁移的是 12 个**
   （7 零改造 + 5 需拆分）；另 6 个（`fourier`/`interference`/`energy`/`l5_baseline`/`l5_quad`/`l5_evidence`）
   因 **alloc 在算法内部或结构体字段**，按"只拆接口不拆算法"原则**不该强拆** ⇒ 新增决策 **D27** 请用户确认。
