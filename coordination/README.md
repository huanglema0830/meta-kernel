# 协调文件夹 · 入口

**每次对话开始，先读 [`CHARTER.md`](CHARTER.md)。**

`CHARTER.md` 是**自包含**的：只读它一份，就知道项目是什么、三方是谁、有哪些机制、哪些红线、怎么开工。
读完按 §五 给出**启动回执**（不回执＝未读），再按 §九 的顺序按需深入其余文件。

---

## 本文件夹一览

| 文件 | 职责 |
|---|---|
| **CHARTER.md** | **总入口（自包含）**：项目定位／命名体系／三方角色／机制清单／硬规则 C1–C10／**启动回执**／**动作约束**／紧急通道／冲突解决／按需深入 |
| BASELINE.md | 项目当前状态的唯一权威描述（每版本更新；含漏洞 R1–R18、决策 D1–D20） |
| CONSTRAINTS.md | 硬约束 C1–C10 全文（含"为什么/破了会怎样/判据"）+ 活相说明 |
| TEMPLATES.md | 指令/报告模板（固定相）＋ 优先级 ＋ 无响应推进 ＋ 断裂接续 ＋ 讨论模板 ＋ **§七 变更记录**（活相机制与观察，**合并为一个文件**）＋ **§八 顾问简报更新流程** |
| **advisor_brief.md** | **顾问侧滚动摘要**（结论 ≤5 条）：顾问读一份即可恢复上下文；触发词「这段讨论落库／归档」 |
| ROADMAP.md | 总路线图：三阶段 ＋ 阶段二六子任务（**子任务2 已修正为"元内核自举"**）＋ 里程碑 ＋ 设备梯队 ＋ 活相说明 |
| **workflows/** | **通用工作流机制**：`universal_workflow.md`（八步场域演化框架，**公开**）＋ `sub/01–08`（每步子模块，**算法层保密·占位**）——**按需调用**，不属开工必读 |
| unsafe_whitelist.txt | C9 门禁白名单（当前为空＝不放行任何真实 unsafe） |
| instructions/ | 指令归档 |
| reports/ | 报告归档 |
| discussions/ | 讨论记录归档 |

> **已合并**：原 `TEMPLATES_CHANGELOG.md`、`OBSERVATIONS.md` 的内容并入 `TEMPLATES.md` §七（2026-09-16 简化）。

> **不在本文件夹但同样关键**：接力包 `研发开发/.workbuddy/memory/CONTINUITY.md`
> —— 其中 **§8 顾问侧接续** 说明"顾问对话断了怎么生成接续包"（触发词：**「顾问对话断了，生成接续包」**）。

---

## 接续话术

**WorkBuddy 侧断裂**（任何一方）：

> 继续云内核项目。读 coordination/ 下的最新状态。
> 告诉我：当前到哪一步、下一步是什么、需要我决策什么。

**顾问侧断裂**：

> **「顾问对话断了，生成接续包」** → WorkBuddy 生成 9 节接续包（含 §0 开场白可直接粘贴）
> —— 详见 `CONTINUITY.md` §8
