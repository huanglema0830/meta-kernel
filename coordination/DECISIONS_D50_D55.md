# 运行逻辑六项决策（D50–D55）与三项裁定

> 来源：用户指令（2026-09-18 00:2x，指令自述基准 v0.186；**实测提交计数 v0.190**）
> 状态：**已生效**（2026-09-18 落地）
> 性质：治理文件。**与底图（仓库根 `README.md` ＋ `docs/`）冲突时，以底图为准。**

---

## 第一部分 · 六项决策（**用户指令原文 · 逐字**）

### D50：记忆写入权
- 选 B：WorkBuddy 可以写记忆，但每条记忆必须标注来源（底图哪份、哪行）。
- 这样即使写错，也能追溯。

### D51：CHARTER 加硬指针
- 选是：在 `CHARTER.md` 开头加硬指针——"本项目底图为 `README.md`+`docs/`，本文件为工程脚手架，冲突以底图为准"。

### D52：L2 启动回执复述
- 选 C：回执只复述与本轮任务相关的底图条目。
- 比如本轮做 L0–L6 补全，就复述底图里 L0–L6 的定义原文。

### D53：L3 机器判据入 CI
- 选 C：建 `check_source_of_truth.py`，入 CI。
- 判据：从 `docs/` 动态取底图文件名 → 断言 `CHARTER.md`/`MEMORY.md` 中全部出现；断言 `README §阶段路线` 与 `ROADMAP` 阶段口径有显式映射；底图哈希基线。
- 自带正反样例自检。

### D54：每轮报告含"读了底图哪几份"
- 选是：每轮报告必须包含"本轮读了底图哪几份、哪几行"。
- 这是 L2 的强化版，用户不抽验也能看到。

### D55：五环改法
- 选 B+C：第①–②环改为"先读底图 `README`+`docs/`，再读记忆"。
- 记忆只存跨会话必须记住的事实，不存"读什么"。
- 这样"读什么"由底图决定，不由记忆决定。断点从根上消除。

---

## 第二部分 · 三项裁定（**用户指令原文 · 逐字**）

### 2.1 "替换"表述保留
- ROADMAP 里的"替换浏览器""替换操作系统"**保留**。
- 理由：这是**用户角度的表达**，不是产品定位层面的"替代"。用户用它顶掉原来的位置，因为更好用。
- 在文档里写明这个逻辑，避免再被误读为"造发行版抢 Windows"。
- 产品定位层面的"不替代"（VISION §3）仍然成立。
- **两者不矛盾**：用户视角说"替换"，产品定位说"不替代"。

### 2.2 底图状态
- 底图（`README.md` + `docs/`）是一步步搭起来的，尚未完全搭好。
- **按现有底图先补 L0–L6 的漏洞和不足，特别是 L0–L4。**
- 补的是**底图本身**，不是工程脚手架。
- 补完之后，再看实现有没有实现底图，再根据效果讨论是否需要再完善地图。

### 2.3 汇编重做
- **以底图当骨架重做**汇编。
- WorkBuddy 那套（机制/约束/台账）**降为附录"工程台账"**。

---

## 第三部分 · 落地执行记录

> ⚠️ **本部分是我的执行记录，不是底图原文，也不是用户指令原文。**

| 编号 | 落点 | 状态 |
|---|---|---|
| D50 | `MEMORY.md` 头部加「来源标注规则」；后续每条记忆标来源（底图哪份哪行） | ✅ 已改 |
| D51 | `CHARTER.md` 开头硬指针（替换原"自包含"表述，并点名该表述为缺陷） | ✅ 已改 |
| D52 | 启动回执改为「复述**本轮相关**底图条目」；本轮复述 L0–L6 定义原文 | ✅ 本轮已按此执行 |
| D53 | `coordination/tools/check_source_of_truth.py` ＋ 挂入 `.github/workflows/ci.yml` | 🚧 本轮在建 |
| D54 | 报告模板增加「本轮读了底图哪几份、哪几行」 | ✅ 本轮报告已含 |
| D55 | `MEMORY.md` 入口改为「先读底图，再读记忆」；记忆不存"读什么" | ✅ 已改 |
| 2.1 | `ROADMAP.md` 加「替换＝用户视角」说明段（逐字写明两者不矛盾） | 🚧 本轮在建 |
| 2.2 | L0–L6 补全写回 `docs/`（补**底图本身**） | 🚧 本轮在建 |
| 2.3 | 汇编重做，脚手架降为附录「工程台账」 | 🚧 本轮在建 |
