{
  "name": "meta-kernel",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "features": {
    "ghcr.io/devcontainers/features/rust:1": {
      "version": "stable",
      "profile": "minimal"
    }
  },
  "postCreateCommand": "rustc --version && cargo --version && cargo build --workspace && cargo test --workspace && echo PHASE0_SMOKE_OK",
  "customizations": {
    "vscode": {
      "extensions": ["rust-lang.rust-analyzer"]
    }
  }
}
