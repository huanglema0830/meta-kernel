//! # L7 · 执行层（多内核互联 + 修复执行）
//!
//! 定位（`docs/LAYER_ARCHITECTURE.md §2.1`）：**L7 = 执行层**——第一个"能对外产生写作用"的层，
//! 因此**必须是最克制的一层**。零（上层输出）＝ L6 交互后的个体内核；
//! 扰动 ＝ 另一个内核的场域（互联侧）／已确认的修复需求（执行侧）。**戒律：不侵**。
//!
//! 设计见 `docs/L7_EXECUTION_DESIGN.md`。状态（v0.109）：
//!
//! | 模块 | 内容 | 状态 |
//! |---|---|---|
//! | [`mesh`] | 多内核互联：发现 → 握手 → 交换（含"不侵"硬约束：交换白名单 / 坐标回映射 / 本端为主） | ✅ 已实现 |
//! | [`grade`] | 动作分级 T0–T3 与授权判定（不可回滚者降级为拒绝） | ✅ 已实现 |
//! | [`ledger`] | 动作账（哈希链记录，篡改可检出） | ✅ 已实现 |
//! | [`repair`] | 修复建议：症状→候选动作→分级计划→记账（**内核只出建议，不执行**） | ✅ **已实现**（T1 闭环） |
//! | 宿主侧执行器 | 真正执行（**只接受预置动作 id**、一次性令牌、先备份、可回滚） | ✅ 已实现（`npb-gateway/src/l7_exec.rs`） |
//!
//! **边界（诚实）**：本层不实现任何真实 IO；真实网络探测与系统写入由宿主负责。
//! **首版只开放 T1**（发起人 Q2 裁决）：T2（如网络配置类）**只出建议不执行**；T3 一律拒绝。

pub mod grade;
pub mod ledger;
pub mod mesh;
pub mod repair;
