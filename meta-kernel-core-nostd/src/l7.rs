//! # L7 · 执行层（**no_std 子集**）
//!
//! 【2.3b 迁移说明】本源文件对应 `meta-kernel-core/src/l7.rs`，但**只声明已迁入本 crate 的子模块**
//! （原文件声明 4 个子模块；**未迁入的不得声明**，否则编译期就报缺失——这正是我们要的硬约束）。
//!
//! 迁移进度（**如实**）：
//!
//! | 子模块 | 状态 |
//! |---|---|
//! | [`grade`] | ✅ **已迁入**（2026-09-17，分片第 1 片：自包含、无 std、无浮点） |
//! | [`mesh`] | ✅ **已迁入**（2026-09-17，分片第 2 片：自包含；仅需 `FloatOps` ＋ `alloc`） |
//! | [`ledger`] | ✅ **已迁入**（2026-09-17，分片第 5 片：依赖 `gene_library::fnv1a64`） |
//! | [`repair`] | ✅ **已迁入**（2026-09-18，**片6**：依赖 `grade` ＋ `ledger`，两者均已在册） |
//!
//! **⇒ 4/4 子模块全部迁入**（本层已无未迁子模块）。
//!
//! **边界**：本层不实现任何真实 IO；真实网络探测与系统写入由宿主负责（与原生模块同口径）。

pub mod grade;
pub mod ledger;
pub mod mesh;
pub mod repair;
